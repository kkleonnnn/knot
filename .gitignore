# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/
.env

# App data
**/data/*.db
**/data/*.db-shm
**/data/*.db-wal
# v0.9.0 C2：per-tenant 库目录（platform.db 走上方 **/data/*.db；租户库/上传库在子目录）——
# **/data/*.db 不匹配子目录，缺此则 tenants/<id>/knot.db 裸奔进 git（手册 C4 flag，提前至 C2）。
**/data/tenants/
# v0.9.0 C4：存量迁移的跨进程 flock 锁文件（tenancy_migration.py:359 在数据根建 .c4-migration.lock）——
# 迁完留存无害但属 runtime 产物；缺此则永久挂在 git status 未跟踪区，易被顺手 commit 进仓。
# v0.9.15：`.bak` 的 WAL/SHM 旁文件 —— **两族规则之间的缝**（实测被 checkpoint 工具扫进 git 24 个）。
#   `**/data/*.db-shm` 不匹配 `X.bak-shm`（结尾不是 `.db-shm`）；`*.bak` 也不匹配（结尾不是 `.bak`）。
#   ⇒ 用 `*.bak-*` 一并盖住 `-shm` / `-wal` 以及将来任何 `.bak` 旁文件形态。
**/data/*.bak-*
**/data/*.lock
**/data/logs/

# 业务隐私（v0.2.4 起）：真实业务目录 / few-shot / eval / schema fixture 不进 git
# 仓库只保留 *.example 模板；首次部署 cp 去掉 .example 后填真实业务数据
knot/services/agents/_local_catalog.py
knot/services/few_shots.yaml
tests/eval/cases.yaml
tests/eval/fake_schema.txt
tests/eval/semantic_cases.yaml
tests/eval/semantic_cases.candidate.yaml

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log
logs/

# Node / Frontend
node_modules/
frontend/node_modules/
frontend/dist/
.env.bak
*.bak
