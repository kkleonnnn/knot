# v0.6.2.2 — SQL planner 复合 metric 修复 v2（Stage 4 LOCKED）

> **协议依据**：Loop Protocol v3（Stage 2 资深裁量跳过 — 见 §0.3 治理标注）
>
> **触发**：v4 §2 段 2 余项 A5 + Task #17 生产 bug（"今日合约交易量和充值情况" 返 0/NULL）
>
> **执行者**：v0.6 执行者
> **守护者**：v0.5 守护者（第 21 次 active 草案评 CONDITIONAL APPROVED 4.4/5）
> **远古守护者**：不激活（0 触及 v0.4 资产）
>
> **日期**：2026-05-29
> **状态**：**Stage 4 LOCKED**（commit 1 可启动）
> **supersedes**：`v0.6.2.2-composite-metric.md` Stage 1 草案

---

## §0 触发与治理

### 0.1 根因分层诊断（Stage 1 sustained）

| 层 | 根因 | 确认度 |
|---|---|---|
| **前端（确定）** | `MetricCard.jsx` 只渲染 `numericCols[0]` → 复合 metric 2 列 1 行只显第一个 | ✅ 代码实证 |
| **clarifier/sql_planner（疑似）** | 多表 fan-out / 漏聚合 — commit 1 调查确认 | ⚠️ |

### 0.2 v1 → v2 修订（守护者第 21 次 4 修订 + η1/η2）

| 修订 | v1 → v2 | 依据 |
|---|---|---|
| R-PB-A5-2 守护粒度 | 含糊 → **明示 prompt 软约束 + v0.5.1 AST 硬守护双层** | 守护者 §II.Q2 核心判断 |
| MetricCard LIMIT | 50 → **80**（选项 A 同文件双分支）| η1 + 守护者 §III.1 |
| NRP-A5-1/A5-2 | 未立 → **全立约** | η2 + 守护者 §III.2 |
| label 中文别名 | F1.1 未 cross-ref → **明示依赖 F2/F3 prompt** | 守护者 §III.3 |
| commit 2 active 级别 | 预设强 active → **延后到 commit 1 调查结论后定** | 守护者 §II.议题 5 |

### 0.3 Stage 2 资深裁量跳过（治理标注）

- ζ1 原决定"补做 Stage 2" → 资深 2026-05-29 反转跳过
- **R-LP-v3-EX-1 不完全适用**（本 PATCH 含业务代码 MetricCard + 新立 R-PB-A5-1/2，非"0 业务代码 + 0 红线"）→ 属**资深裁量跳过**，非正式 EX-1
- 守护者第 21 次草案评已覆盖大面（含 fan-out 守护粒度核心判断）→ 实质风险可控
- **闭合动作**：守护者 PATCH 终审（commit 3 PR open）时补"跳 Stage 2 ack"
- 性质：非 OVERRIDE（不计入 override-cumulative-log §1）；属 Stage 2 流程裁量

---

## §1 范围（F1-F4）

### F1 — MetricCard 多值网格（commit 1 / 前端确定 bug）

- F1.1 `frontend/src/screens/chat/ResultBlock/MetricCard.jsx`：
  - `numericCols.length === 1`（单值）→ 现有大数字卡片 **byte-equal sustained**（R-PB-A5-1）
  - `numericCols.length > 1`（复合）→ **多 stat 网格** `repeat(auto-fit, minmax(160px, 1fr))`（v0.5.18 R-461 设计语言复用）
  - 每 stat：值 + 列名/别名 label（mono uppercase）
  - **label 中文友好度依赖 F2/F3 sql_planner prompt 中文别名**（`SUM(volume) AS 交易量`）— cross-ref 明示
  - **NRP-A5-1：>6 列 fallback**（auto-fit 自适应 + >6 列降级 table 或 2-row）
  - **LIMIT 50 → 80**（η1 选项 A — 单值 + 多值双分支同文件内聚）
- F1.2 R-PA-PB-V1 视觉延续：仅依赖 T 25 字段，不扩 buildTheme

### F2 — clarifier 复合 metric 识别（commit 2 / prompt）

- `knot/prompts/clarifier.md`：复合 metric 段（一句问 2+ 聚合 → intent=metric sustained + refined 保留全部指标 + 提示 sql_planner 多指标处理）

### F3 — sql_planner 复合 metric 指引（commit 2 / prompt — 视 commit 1 调查）

- sql_planner prompt：多指标同表单 SELECT 多聚合；跨表 CTE 预聚合 + **中文别名 `AS 交易量`**
- **若 commit 1 调查 SQL 层无 bug → F3 跳过**（纯 clarifier 识别段）

### F4 — 守护测试 + docs（commit 3）

- 多值网格渲染测试（2/4/6/8 列 — 含 NRP-A5-1 >6 列 fallback）
- 单值路径 byte-equal 守护（R-PB-A5-1）
- **NRP-A5-2 测试**：复合 metric 多表 mock → 触发既有 `__REJECT_FAN_OUT__`（v0.5.1 AST runtime 守护）
- 4 处版本同步 0.6.2.1 → 0.6.2.2 + CHANGELOG + CLAUDE.md

---

## §2 commit 序列

```
commit 1  F1 MetricCard 多值网格（LIMIT 80）+ 根因调查（clarifier/sql_planner 层）   指导 0.5-1 天
          → 调查结论记录 PR 描述（守护者第 22 次轻量复核 + 定 commit 2 active 级别）
commit 2  F2 (+F3 视调查) clarifier/sql_planner prompt 复合 metric                 指导 0.5-1 天
          → SQL 层有 bug = 强 active / 纯 prompt = 可选 active（守护者议题 5）
commit 3  F4 守护测试 + docs + 4 处版本同步                                        指导 0.5 天
          → PR open 守护者 PATCH 终审 + 跳 Stage 2 ack 闭合
─────────────────────────────────────────────────────────────────
合计：~1.5-2.5 天指导（范围窄可外推 v0.6.2.1 实绩；非 v0.6.2.3 14 屏规模）
```

---

## §3 红线（含守护者 §II.Q2 守护粒度澄清）

| 红线 | 内容 | 守护层 |
|---|---|---|
| **R-PB-A5-1** | MetricCard 单值路径 byte-equal（numericCols.length===1 与 v0.6.0.2 抽出版一致）| git diff 单值分支 = 0 行 + snapshot 双主题兜底 |
| **R-PB-A5-2**（守护粒度澄清）| 复合 metric 多表查询 CTE 预聚合 — **prompt 指引层（软约束）**；**runtime fan-out 硬守护 sustained v0.5.1 sqlglot AST**（`__REJECT_FAN_OUT__` / `__REJECT_CARTESIAN__`）— 不重造 | 双层（prompt + AST runtime）|
| **NRP-A5-1** 新立 | 多值网格列数上限保护（auto-fit minmax + >6 列 fallback）| commit 1 + F4 测试 |
| **NRP-A5-2** 新立 | fan-out runtime 守护澄清（R-PB-A5-2 prompt ≠ 硬守护；测试覆盖既有 AST reject 触发）| F4 测试 |
| 通用 sustained | R-PA-PB-V1 / R-94 / R-PA-8 / R-PB-GOV-1 | 全 commit |

---

## §4 验收标准

### 功能
- [ ] 复合 metric（"今日交易量和充值"）→ 多 stat 网格全显（不再只 1 个 / 不再 0）
- [ ] 单值 metric（"昨天总盈亏"）→ 大数字卡片 byte-equal（R-PB-A5-1 不回归）
- [ ] >6 列复合 metric → NRP-A5-1 fallback（不拥挤）
- [ ] 复合 metric 多表 → 既有 v0.5.1 AST fan-out 守护触发（NRP-A5-2）

### 红线 + 协议
- [ ] R-PB-A5-1 git diff 单值分支 = 0 行 + snapshot 双主题
- [ ] MetricCard ≤ 80 行（R-94）
- [ ] CI 4/4 全绿 + R-PA-8 + 7 contracts KEPT
- [ ] 4 处版本同步

---

## §5 协议合规

| Stage | 状态 |
|---|---|
| Stage 1 | ✅ 草案 |
| Stage 2 | ⏭️ 资深裁量跳过（§0.3 — 守护者 PATCH 终审补 ack）|
| Stage 3 | ✅ 守护者第 21 次草案评 CONDITIONAL APPROVED + 4 修订（本 v2 已整合）|
| Stage 4 | ✅ **LOCKED 2026-05-29**（资深 η1/η2 + 4 修订整合）|

**守护者激活预约**：第 22 次（commit 1 调查结论轻量复核 + 定 commit 2 active）/ PATCH 终审（PR open）

---

## §6 风险（Stage 1 sustained + 守护者补）

| 风险 | 缓解 |
|---|---|
| 复合 metric 多表 fan-out | **runtime v0.5.1 AST 硬守护**（R-PB-A5-2 澄清）+ prompt 指引辅助 |
| 多值网格列数过多 | NRP-A5-1 >6 列 fallback |
| 单值路径回归 | R-PB-A5-1 git diff 0 行 + snapshot |
| 英文 SQL 别名 label UX 差 | F2/F3 prompt 中文别名 `AS 交易量`（F1.1 cross-ref）|
| commit 1 调查发现 SQL 层 bug → 范围扩 | commit 2 视调查；重则拆独立 PATCH |

---

## §7 资深拍板回顾（已完成）

- η1 MetricCard LIMIT 50 → 80（选项 A）✅
- η2 NRP-A5-1/A5-2 全立约 ✅
- Stage 2 资深裁量跳过（§0.3）✅
- 守护者 4 修订全整合（R-PB-A5-2 粒度 / LIMIT / NRP / label cross-ref）✅
- commit 2 active 级别延后到 commit 1 调查后 ✅

---

**附录**：
- Stage 1 草案：`docs/plans/v0.6.2.2-composite-metric.md`
- 守护者第 21 次草案评（本 v2 已整合 4 修订）
- v0.5.18 R-461 AdminBudgets 4-stat grid 设计先例
- v0.5.1 fan-out/cartesian AST runtime 守护（R-80~93 + `__REJECT_FAN_OUT__`）
