# v0.5.2 — 后端代码瘦身（C2）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

8/8 决策（D1-D8）**与执行者 Stage 1 提议完全一致**；Stage 2 仅在 D5 / D7 加重措辞（强制 / CI 脚本核验）。
新增 5 条红线（R-106~R-110），分别来自 Stage 2（2 条）+ Stage 3（3 条）。
3 条隐性指令落地：D7 CI 脚本核验 / 节奏纪律 1 文件 1 commit / D-2 不新增任何测试。

## 1. 决议表最终裁定（D1-D8 全部锁定）

| ID | 锁定 | 与执行者提议 |
|---|---|---|
| **D1** 双胞胎合并 | ✅ A 保守不合并 | 一致 |
| **D2** 模块组织 | ✅ A 同级 `_*.py` | 一致 |
| **D3** prompt 位置 | ✅ A `.py` 字符串 | 一致 |
| **D4** query 步骤位置 | ✅ A `services/query_steps.py` | 一致 |
| **D5** re-export | ✅ **A 强制要求**（Stage 2 措辞升级） | 一致 |
| **D6** `_is_fan_out` 抽到 tools | ✅ A 抽 | 一致 |
| **D7** 行数硬约束 | ✅ **A 严格执行 + CI 脚本核验** | 一致 + Stage 2 加码 |
| **D8** 新增 contract | ✅ B 不增 | 一致 |

## 2. 红线最终清单（17 条 R-94~R-110）

### Stage 1 草案原 12 条（保留）

| ID | 摘要 |
|---|---|
| **R-94** | 4 主 ≤ 350/300/220/220；新建模块 ≤ 250 |
| **R-95** | 432 tests 100% 绿不可降 |
| **R-96** | 7 contracts KEPT；不动 `.importlinter` |
| **R-97** | sync/async 双胞胎签名 + 行为完全保留 |
| **R-98** | v0.5.1 cartesian + R-91 计数 + v0.4.1.1 fan-out 守护零变更 |
| **R-99** | v0.4.4 R-26 senior budget gate + R-30 BIAgentError 透传 |
| **R-100** | re-export 兼容；测试 import 路径 0 修改 |
| **R-101** | 6 commit 序列锚定，每 commit 独立全闸门绿 |
| **R-102** | routes=72；version=0.5.2；vite build 通过 |
| **R-103** | 不动 `.env.example` / `requirements.txt` / `pyproject.toml` |
| **R-104** | 新文件顶部 module docstring 含"v0.5.2 起从 X 抽出" |
| **R-105** | commit message 注明源行号区间（拆分不能 git mv 保 history） |

### Stage 2 新增 2 条（资深 + Codex）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-106** | **严禁子模块循环引用** — `sql_planner_*.py` / `clarifier.py` / `presenter.py` / `_llm_invoke.py` 等子文件**禁反向 import 主文件**；主文件作"调度员"，子文件作"工具箱" | 拆分时严守单向依赖：主 → 子；用 `python -c "import knot.services.agents.sql_planner_tools"` 单独 import 子模块不应触发主文件加载；每 commit 后跑 `lint-imports`（contract 不增但循环依赖会被 Python import 系统直接报错） |
| **R-107** | **Private 前缀保护** — 子模块内 helper 保持 `_` 前缀（如 `_is_fan_out` / `_run_tool` / `_strip_sql`）；仅当主文件确需通过 re-export 暴露给测试时才升级为 public（实际本 PATCH 全部保持 `_`，测试 import 走 re-export 不影响私有性约定） | grep 子模块函数命名；外部已 public 的函数（如 `run_clarifier` / `build_system_prompt`）保持 public |

### Stage 3 新增 3 条（v0.4 守护者）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-108** | **逻辑平移零损耗** — `llm_client.py` 拆分时，v0.4.3 `calculate_cost` Token 计费 + v0.4.5 Fernet 加密 Key 加载路径必须移动后仍指向正确；commit 2 必须通过 `test_budget_service.py` | commit 2 执行后单独跑 `pytest tests/services/test_budget_service.py tests/services/test_llm_client*.py tests/services/test_few_shots*.py -v` 全绿才进 commit 3；同步加跑 `test_crypto*.py` |
| **R-109** | **SSE 稳定性** — `query_stream` generator 逻辑严禁被封装过深；保持 `yield` 直观，防止函数调用栈过深导致 SSE 断流 | `query_steps.py` 抽出的是**纯业务步骤函数**（输入参数 + 返回结果），**严禁**封装 generator / 把 yield 移到子模块；`query_stream` 路由内**仍保留** `for ... yield` 主控结构，仅把每步的业务计算 delegate 给子模块函数；commit 4 后手测 SSE 端到端 |
| **R-110** | **文档连续性** — 所有抽出的新文件须在 `CLAUDE.md`「关键路径」表同步更新 | docs commit 6 同步增 8 个新文件行：`sql_planner_prompts/_tools/_llm.py` / `clarifier.py` / `presenter.py` / `few_shots.py` / `llm_prompt_builder.py` / `_llm_invoke.py` / `query_steps.py`；每行注明职责 |

## 3. Stage 2/3 三条隐性指令（落地手段）

### 3.1 Stage 2 D7 加码：CI 脚本核验行数

> 建议使用脚本在 CI 中进行统计核验

**落地**：在 `scripts/check_file_sizes.py` 新建轻量校验脚本（≤ 30 行，纯 stdlib），CI 加一步 `python3 scripts/check_file_sizes.py`：

```python
LIMITS = {
    "knot/services/agents/sql_planner.py": 350,
    "knot/services/llm_client.py": 300,
    "knot/services/agents/orchestrator.py": 220,
    "knot/api/query.py": 220,
}
NEW_MODULE_LIMIT = 250  # 8 个新建模块
```

超线 exit(1)；脚本本身不进入 contract 体系（仅 CI 工具）。**注**：CI workflow 不属于 R-103 例外（R-103 锁的是 `.env.example` / `requirements.txt` / `pyproject.toml`，CI workflow 不在其中）。

### 3.2 Stage 3 操作指令 §3：Commit 节奏纪律

> 1 文件 1 commit；拆分导致 pytest 报错必须在该 commit 内修复，严禁带入下一 commit

**落地**：6 commit 序列每个内部独立**全闸门**绿（不仅 pytest，含 lint-imports + ruff + routes + version）。**特别警示**：commit 1 拆 sql_planner 后 432 tests 必须全绿才能开 commit 2；如 commit 2 修了 commit 1 的 import 错误，**必须 amend commit 1** 而非 commit 2 顺手修。

### 3.3 Stage 3 D-2 备注

> 本 PATCH 是重构，不需要新测试

**落地**：**不新增任何测试**（与 v0.5.1 增 ≥ 13 测试完全相反）；432 + 112 skipped baseline 严格不变。任何新测试出现 = 范围溢出。

## 4. 文件级改动清单（最终版）

```
knot/services/agents/
├── sql_planner.py               [EDIT] 653 → ≤ 350
│   - 主文件作"调度员"（R-106）
│   - re-export _is_fan_out / _run_tool / _strip_sql 等给测试
├── sql_planner_prompts.py       [NEW] ≤ 100   docstring "v0.5.2: extracted from sql_planner.py L?-?"
├── sql_planner_tools.py         [NEW] ≤ 200   _is_fan_out + _run_tool（含 cartesian/fan-out 守护）
└── sql_planner_llm.py           [NEW] ≤ 100   _call_llm + _acall_llm（含 R-26 budget gate）
├── orchestrator.py              [EDIT] 535 → ≤ 220   保留 _llm/_allm/_parse_json/_resolve helpers
│   - re-export run_clarifier / arun_clarifier / run_presenter / arun_presenter
├── clarifier.py                 [NEW] ≤ 220
└── presenter.py                 [NEW] ≤ 130

knot/services/
├── llm_client.py                [EDIT] 574 → ≤ 300
│   - R-108: calculate_cost + Fernet 加载路径平移正确
│   - re-export build_system_prompt / get_few_shot_examples 等
├── few_shots.py                 [NEW] ≤ 130
├── llm_prompt_builder.py        [NEW] ≤ 110
├── _llm_invoke.py               [NEW] ≤ 160
└── query_steps.py               [NEW] ≤ 250
    - R-109: 仅纯业务步骤函数，严禁含 generator/yield

knot/api/
└── query.py                     [EDIT] 457 → ≤ 220
    - R-109: query_stream 内仍保留 for...yield 主控；仅 delegate 业务计算

knot/main.py                     [EDIT] version 0.5.1 → 0.5.2
tests/test_rename_smoke.py       [EDIT] R-72 smoke 0.5.1 → 0.5.2
scripts/check_file_sizes.py      [NEW] ≤ 30   CI 行数核验（D7 加码 R-94 落地）
.github/workflows/ci.yml         [EDIT] lint-test job 加 1 步 file-sizes 核验
CHANGELOG.md                     [EDIT] [Unreleased] v0.5.2 段（R-94~R-110 偿还表）
CLAUDE.md                        [EDIT] R-110: 关键路径表 +8 行新文件 + v0.5.x 路线图 v0.5.2 ⏳ → ✅
docs/plans/v0.5.2-backend-slim.md [本锁定手册]
```

## 5. 验收清单（最终版）

### 5.1 必须全绿

- [ ] `pytest tests/ -v` = **432 passed / 112 skipped**（**严格不变**，不允许新增也不允许减少）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（D7 CI 核验）
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.2**
- [ ] `npm run build` 通过

### 5.2 Stage 3 强化验证

- [ ] **R-108**：commit 2 后 `pytest tests/services/test_budget_service.py tests/services/test_crypto*.py tests/services/test_llm_client*.py -v` 全绿
- [ ] **R-109**：commit 4 后**手测 SSE 端到端** — 自然语言查询全链路 SSE 不断流，每 step 事件按序到达
- [ ] **R-110**：CLAUDE.md 关键路径表 grep 8 个新文件名全部命中

### 5.3 不破坏

- [ ] v0.5.0 R-67~R-79 + v0.5.1 R-80~R-93 测试全绿
- [ ] cartesian + fan-out 守护手测：故意诱导 → ReAct 重生成成功
- [ ] R-91 连续 3 次 cartesian 拒收 → 强制终止
- [ ] core/ adapters/ 不动；requirements.txt / pyproject.toml 不动
- [ ] sync/async 双胞胎签名 + 行为 100% 保留（R-97）
- [ ] 测试文件 import 路径 **0 修改**（R-100 D5 强制）

## 6. Commit 序列（最终锁定 — 6 commit；Stage 3 节奏纪律）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.2): sql_planner 拆 prompts/tools/llm 三模块 (R-94/95/97/98/100/106/107)` | 全闸门 + R-98 cartesian/fan-out 测试套绿 | 拆完 import 错必须本 commit 内 amend 修 |
| 2 | `refactor(v0.5.2): llm_client 拆 few_shots/prompt_builder/_llm_invoke (R-94/95/100/108)` | 全闸门 + R-108 budget + crypto 测试套绿 | calculate_cost / Fernet 路径迁移正确 |
| 3 | `refactor(v0.5.2): orchestrator 拆 clarifier/presenter (R-94/95/100)` | 全闸门 | clarifier/presenter 集成测试绿 |
| 4 | `refactor(v0.5.2): api/query 抽 services/query_steps (R-94/95/109)` | 全闸门 + R-109 SSE 手测 | query_stream yield 主控保留 |
| 5 | `chore(v0.5.2): version bump 0.5.1 → 0.5.2 + CI 加 file-sizes 核验脚本` | 全闸门 + scripts/check_file_sizes.py exit 0 | D7 加码落地 |
| 6 | `docs(v0.5.2): CHANGELOG + CLAUDE.md 关键路径 + plan LOCKED 归档 (R-110)` | 全闸门 | 8 新文件 + 路线图 ⏳→✅ |

每 commit 之间存在 import 关系演化 — **必须按文件顺序串行**，不可交错；commit 节奏纪律严守 §3.2。

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 17（v0.5.2 区段 R-94~R-110）
- ✅ 重构原子性 — D5 强制 re-export，432 测试 0 修改
- ✅ 范围最小化 — 8 项 Out-of-Scope 明确推迟（无范围溢出）

## 8. 给执行者（本 Agent）的执行注意

1. **归档锁定手册** → 本文件覆盖 DRAFT；commit 6 一起提
2. **commit 顺序绝对不可换**：`sql_planner → llm_client → orchestrator → api/query → version+CI → docs`；每步全闸门绿才能进下一
3. **R-100 re-export 强制**：每次主文件瘦身后**先**确认 432 tests 绿（说明 re-export 完整），再开下一 commit
4. **R-106 循环引用守护**：每个新建子模块用 `python3 -c "import <module>"` 单独 import 测试**前置**到 pytest 之前 — 循环依赖会立刻 ImportError，比测试报错更早发现
5. **R-109 SSE 手测**：commit 4 后**必须**实际启动后端跑一次 NL → SQL → Presenter 全链路，看 SSE 事件流不断；自动化测试 mock 不到这个稳定性
6. **R-108 commit 2 守护**：拆 llm_client 时务必先列 `grep -n "calculate_cost\|Fernet\|api_key\|MASTER_KEY" knot/services/llm_client.py` 标定所有迁移点再动手
7. **范围抗诱惑**：8 项 Out-of-Scope 中"sync/async 合并""消除 helper 重复""prompt 抽 .md""admin.py 拆分"都很有诱惑 — **本 PATCH 一律不碰**，留 v0.5.3+

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-10）。执行者按本手册实施。
