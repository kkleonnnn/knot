# v0.5.9 — (C5+) Shell 屏复刻 — 首个真正屏复刻 PATCH — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 10 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）
> **依据协议**：v0.5.8 § Visual Replication Protocol（CLAUDE.md 锁定）

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**Q1 是唯一修订**（size=16 → size=20，Atom 轨道在 16px 下细节丢失）。
新增 6 条红线（Stage 2: 3 + Stage 3: 3）：
- **Stage 2**：R-199.5（KnotLogo 解禁仅限 Shell.jsx 一处）+ R-210（CSS 污染防御 — App.css 0 行 diff）+ Q2 加码 ellipsis 落入 R-198 实施细则
- **Stage 3**：R-211（#ff7a3a 全局净空）+ R-212（'admin-' 前缀分流字面 byte-equal）+ R-213（版本同步血缘扩展 — Shell.jsx 严禁 version 字面）

红线总数 22（R-192~R-213，含 R-199.5）。
"宪法级"红线 R-192 13 props 签名 byte-equal 三方共识。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** Sidebar 224 | ✅ A 改 224 | Stage 1 |
| **D2** KnotLogo 三件套 | ✅ A 替换 | Stage 1 |
| **D3** #ff7a3a 偿还 | ✅ A 去除纯 T.accent | Stage 1 |
| **D4** Admin emoji→SVG | ✅ A 全 SVG | Stage 1 |
| **D5** NavItem active | ✅ A absolute span | Stage 1 |
| **D6** NavSectionTitle | ✅ A T.mono | Stage 1 |
| **D7** logoArea 56 | ✅ A 56 + borderBottom | Stage 1 |
| **D8** TopBar 不拆 | ✅ A **绝对同意（宪法级）** | Stage 2 强调 |
| **Q1** Logo size | ⚠️ **B size=20**（Stage 2 修订 16→20） | Atom 轨道 16px 下辨识度低 |
| **Q2** 224 截断 | ✅ A 测试 + ellipsis 加码 | Stage 2 加码 ellipsis 落入 R-198 |
| **Q3** I.book / I.shield 验证 | ✅ A（Shared.jsx 36 names 含 book/shield） | Stage 2 核实 |
| **Q4** absolute span 位置 | ✅ A **右侧**（避 overflow:hidden 裁切） | Stage 2 |
| **Q5** KnotLogo 解禁范围 | ✅ A 加 R-199.5 明确边界 | Stage 2 强制 |

---

## 2. 红线最终清单（22 条 R-192~R-213）

### Stage 1 草案原 18 条（保留 — 含 Q1 修订）

| ID | 摘要 |
|---|---|
| **R-192** | **AppShell 13 props 签名 byte-equal（宪法级）**（`{ T, user, active, sidebarContent, topbarTitle, topbarTrailing, showConnectionPill, connectionOk, hideSidebarNewChat, onToggleTheme, onNewChat, onNavigate, onLogout, children }`） |
| **R-193** | SideHeading + SideNavRow export name + 签名 byte-equal（chat/saved-reports/admin/* 12+ 文件 import） |
| **R-194** | onNavigate / onToggleTheme / onLogout / onNewChat handlers 调用方式不变 |
| **R-195** | active 命名约定 byte-equal（'admin-' 前缀分流 + 'chat' / 'settings' 字面） |
| **R-196** | Connection pill 业务逻辑不变（showConnectionPill + connectionOk props 行为一致） |
| **R-197** | dangerouslySetInnerHTML on label 字符串 byte-equal（`API &amp; 模型` HTML entity） |
| **R-198** | Sidebar 256 → 224（D1）+ SideNavRow Label ellipsis 加码（Q2） |
| **R-199** | Brand 区改 `<KnotLogo T={T} size={20}/>`（D2 + Q1 修订 — 16→20 提辨识度） |
| **R-200** | logoArea 56px 高度 + borderBottom（D7） |
| **R-201** | User row 渐变 `linear-gradient(${T.accent}, #ff7a3a)` → 纯 `T.accent`（D3） |
| **R-202** | Admin nav 3 emoji（💰/🛡️/📋）→ SVG（`I.zap` / `I.shield` / `I.book`）（D4） |
| **R-203** | NavItem active 改 absolute span 2px brand bar，**右侧**（D5 + Q4 — 避 overflow:hidden 裁切） |
| **R-204** | NavSectionTitle 字体 `T.mono`（D6） |
| **R-205** | Shell.jsx 重构后 ≤ 220 行；check_file_sizes LIMITS 29 → 30 |
| **R-206** | backend 432 tests / 7 contracts KEPT / 72 routes 不变 |
| **R-207** | 17 屏 + 12 子模块 byte-equal（`git diff origin/main HEAD -- frontend/src/screens/` 仅 Login.jsx 页脚字面） |
| **R-208** | App.jsx / api.js / index.css / main.jsx / utils.jsx / Shared.jsx / decor/NarrativeMotif.jsx byte-equal |
| **R-209** | 三处版本同步 0.5.8 → 0.5.9（main.py + smoke + Login 页脚） |

### Stage 2 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-199.5** | **KnotLogo 解禁仅限 Shell.jsx LogoArea 一处** — 严禁引入业务屏（Home / ThinkingCard / Login 主体外等） | grep `KnotLogo\|KnotMark\|KnotWordmark` 命中**仅** `Shared.jsx`（定义）+ `screens/Login.jsx`（v0.5.7）+ `Shell.jsx`（v0.5.9 解禁）三文件；其他 0 命中 |
| **R-210** | **CSS 污染防御** — Shell 视觉重构通过 inline styles 完成；严禁在 App.css 增全局类名（除非 18 屏通用布局重置） | `git diff frontend/src/App.css` = 0 行；Shell.jsx 重构 0 新 className 字面 |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-211** | **#ff7a3a 全局净空** — 不止 user row，Shell.jsx 全文 grep `ff7a3a` 必须 = 0 | commit 1 后 `grep -i "ff7a3a\|#ff7" frontend/src/Shell.jsx` = 0 命中 |
| **R-212** | **'admin-' 前缀分流 1:1 保留** — Admin 子页面切 tab 时 Sidebar Active 状态停在 admin nav 大项 | 手测：进 admin/users → admin/budgets → admin/audit；Sidebar nav 始终正确 active；其他 nav 不抢焦点；改 active 视觉时严禁触碰 `active.startsWith('admin-')` if 分支逻辑 |
| **R-213** | **版本同步血缘扩展** — `Shell.jsx 严禁含 version 字面**（保 Login 页脚唯一显示位）** | commit 1 后 `grep "0\.5\.9\|v0\.5" frontend/src/Shell.jsx` = 0 命中 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤锁死）

> 先进行 224px 布局减窄测试，确信不截断后再视觉重构

**子步骤顺序**：

| Step | 动作 | 验证 |
|---|---|---|
| 1 | Sidebar 宽度 256→224 + SideNavRow Label ellipsis（其他不动） | npm build + 手测 admin 12 项 nav row 全展开 + ellipsis 兜底 |
| 2 | grep 验证 I.book / I.shield 已在 Shared.jsx 36 names | Stage 2 已确认，执行者前置 grep 双保险 |
| 3 | Brand 区改 `<KnotLogo T={T} size={20}/>` + logoArea 56px + borderBottom | 视觉手测 |
| 4 | User row 去 #ff7a3a → 纯 T.accent | grep `ff7a3a` = 0 |
| 5 | Admin nav 3 emoji → I.zap / I.shield / I.book | grep `💰\|🛡️\|📋` = 0 |
| 6 | NavItem active → absolute span 2px brand bar **右侧** | CSS `right: 0` 字面验证 |
| 7 | NavSectionTitle 字体 → T.mono | 视觉手测 |
| 收尾 | grep 全守护 + npm build + 18 路由手测 | 见 §5 验收清单 |

### Stage 3 §3 验收关键 — 18 路由端到端手测

commit 1 收尾**强制**端到端手测：
- chat → saved-reports → admin/users → admin/sources → admin/models → admin/budgets → admin/recovery → admin/audit → admin/knowledge → admin/few-shots → admin/prompts → admin/catalog → database → knowledge → catalog → sql-lab → settings → conversations
- 每屏校验：① topbar title 显示正确 ② topbar 右侧 trailing 按钮渲染正常 ③ connection pill 状态（仅 chat 屏显示）④ sidebar active span 停在正确项

### Stage 2 R-199.5 全局边界守护

```bash
grep -rln "KnotLogo\|KnotMark\|KnotWordmark" frontend/src/
```

预期输出仅 3 文件：`Shared.jsx`（定义）/ `screens/Login.jsx`（v0.5.7 已用）/ `Shell.jsx`（v0.5.9 解禁）。其他文件命中 = 范围溢出立即 reset。

### Stage 2 R-210 CSS 污染守护

- `git diff frontend/src/App.css` = 0 行
- Shell.jsx 视觉重构全用 **inline style 对象** + 已有 utility（不新增 className 字面）

### Q2 Ellipsis 加码实施

SideNavRow Label `<span>` 内联样式：

```jsx
<span style={{
  flex: 1,
  minWidth: 0,
  overflow: 'hidden',
  textOverflow: 'ellipsis',
  whiteSpace: 'nowrap'
}} dangerouslySetInnerHTML={{ __html: label }}/>
```

### Stage 3 R-212 'admin-' 前缀守护

NavItem active 判断逻辑 byte-equal — `active.startsWith('admin-')` 模式（v0.4.1.1 hotfix Bug 1 已确立）；改 active span 视觉时**严禁触碰**该 if 分支逻辑。

### Q4 active span 右侧落地

```jsx
{active && <span style={{
  position: 'absolute',
  right: 0, top: '50%',
  transform: 'translateY(-50%)',
  width: 2, height: '60%',
  background: T.accent,
  borderRadius: 2,
}}/>}
```

---

## 4. 文件级改动清单（最终版）

```
frontend/src/Shell.jsx                  [EDIT 172 → ≤ 220 行]
                                        Step 1: Sidebar 256→224 + Label ellipsis [R-198 + Q2]
                                        Step 2: grep 验 I.book / I.shield (前置双保险)
                                        Step 3: Brand 区 → <KnotLogo T={T} size={20}/> [R-199 + Q1]
                                                + logoArea 56px + borderBottom [R-200]
                                        Step 4: User row 渐变去 #ff7a3a → T.accent [R-201 + R-211]
                                        Step 5: Admin nav emoji → I.zap/I.shield/I.book [R-202]
                                        Step 6: NavItem active → absolute span 右侧 [R-203 + Q4]
                                        Step 7: NavSectionTitle font T.mono [R-204]
                                        守护: 13 props 签名 byte-equal [R-192 宪法级]
                                              + 'admin-' 分流字面 [R-212]
                                              + inline styles only [R-210]
                                              + 无 version 字面 [R-213]

scripts/check_file_sizes.py             [EDIT LIMITS +1]
                                        - "frontend/src/Shell.jsx": 220 (29→30 条) [R-205]

knot/main.py                            [EDIT version "0.5.8" → "0.5.9"]  [R-209]
tests/test_rename_smoke.py              [EDIT R-72 smoke "0.5.8" → "0.5.9"]  [R-209]
frontend/src/screens/Login.jsx          [EDIT 页脚 "v0.5.8" → "v0.5.9"]  [R-209]

frontend/src/screens/ (17 屏 + 12 子)   [unchanged byte-equal]   [R-207]
frontend/src/App.jsx                    [unchanged]              [R-208]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx}  [unchanged]  [R-208]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]              [R-208]
frontend/src/App.css                    [unchanged 0 行 diff]    [R-210]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]              [R-206]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.9 段]
                                        R-192~R-213 偿还表 + Loop Protocol v3 第 10 次施行存档
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.9
                                        - v0.5.x 路线图 v0.5.9 ⏳ → ✅
docs/plans/v0.5.9-shell.md              [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（不允许减少）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（30 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.9**

### Stage 2/3 强化验证

- [ ] **R-199.5**：`grep -rln "KnotLogo\|KnotMark\|KnotWordmark" frontend/src/` 仅 `Shared.jsx` / `screens/Login.jsx` / `Shell.jsx` 三文件
- [ ] **R-210**：`git diff frontend/src/App.css` = 0 行；Shell.jsx 无新增 className 字面
- [ ] **R-211**：`grep -i "ff7a3a\|#ff7" frontend/src/Shell.jsx` = 0 命中
- [ ] **R-212**：手测 admin 子页面切 tab — sidebar admin nav 始终正确 active
- [ ] **R-213**：`grep "0\.5\.9\|v0\.5" frontend/src/Shell.jsx` = 0 命中（version 仅 Login 页脚显示）
- [ ] **Q1**：Shell.jsx 含 `size={20}` 字面；Atom 轨道在 224px sidebar 内可辨
- [ ] **Q2**：admin 12 项 nav row 长 label（如 "Few-shot 示例"）ellipsis 截断生效
- [ ] **Q4**：NavItem active span CSS `right: 0` 字面（非 `left: -8`）

### 契约守护（宪法级）

- [ ] **R-192**：AppShell 13 props 签名 byte-equal — `git diff` 检查签名行未变
- [ ] **R-193**：SideHeading + SideNavRow export name + props byte-equal
- [ ] **R-194**：onNavigate / onToggleTheme / onLogout / onNewChat handlers 调用未改
- [ ] **R-195**：active 'admin-' 前缀分流逻辑字面 byte-equal
- [ ] **R-196**：Connection pill 业务逻辑不变（showConnectionPill + connectionOk）
- [ ] **R-197**：`API &amp; 模型` HTML entity 字面 byte-equal

### 范围守护

- [ ] **R-207**：`git diff origin/main HEAD -- frontend/src/screens/` 仅 `Login.jsx` 页脚字面
- [ ] **R-208**：App / api / index.css / main / utils / Shared / NarrativeMotif `git diff` = 0 行

### 端到端

- [ ] **Stage 3 §3**：18 路由切换全部 topbarTitle + topbarTrailing 渲染正常
- [ ] v0.5.0~v0.5.8 既有功能端到端正常（cartesian / fan-out / 7 intent / CSV / 错误边界 / Login pilot）

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.9): Shell.jsx 视觉重构 — KnotLogo size=20 + brand cleanup + emoji→SVG + sidebar 224 ellipsis (R-192~R-204, R-199.5/210/211/212)` | npm build + 18 路由手测 + R-199.5/210/211 grep + R-212 admin tab 切换手测 | **宪法级 R-192 13 props 签名 byte-equal**；7 子步骤顺序锁死；CSS 0 污染；#ff7a3a 全局 0；KnotLogo 仅 3 文件命中 |
| 2 | `chore(v0.5.9): version bump 0.5.8→0.5.9 三处同步 + check_file_sizes LIMITS +1 (R-205/209/213)` | 全闸门 + routes=72 + version=0.5.9 + 30 条 LIMITS | R-209 三处同步：main.py + smoke + Login 页脚；R-213 Shell.jsx 无 version 字面 |
| 3 | `docs(v0.5.9): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED 归档` | 全闸门 | Loop Protocol v3 第 10 次施行存档 |

每 commit 独立全闸门绿。commit 1 是本 PATCH 重头戏 — 7 子步骤顺序不可换。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 10 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 22（v0.5.9 区段 R-192~R-213，含 R-199.5）
- ✅ "宪法级"红线 R-192 13 props 签名 byte-equal 三方共识
- ✅ 范围最小化 — 17 屏 + 12 子模块 + 7 核心非屏文件全部 byte-equal；KnotLogo 解禁严格限 Shell.jsx 一处

---

## 8. 执行者纪律自检（11 条 — Stage 3 守护者建议照单收）

1. ✅ 锁定手册落地 → 当前文件 LOCKED；分支 `feat/v0.5.9-shell`
2. ✅ R-192 宪法级守护 — 13 props 签名复制为 reference；每次保存后 grep 签名行字面对比
3. ✅ Stage 3 §2 子步骤顺序锁死 — 先 224 减窄测试 + ellipsis 兜底，再视觉重构；如实测溢出严重回退 D1 需 Stage 评审追加 ack
4. ✅ R-199.5 解禁边界 — 不碰 Home / ThinkingCard 等其他屏；commit 1 后强制全局 grep 三件套仅 3 文件命中
5. ✅ R-210 CSS 0 污染 — 全用 inline style 对象 + 已有 utility；想加新 className 立即停手判断
6. ✅ R-211 #ff7a3a 全局净空 — commit 1 后强制 grep `ff7a3a\|#ff7` = 0
7. ✅ R-212 'admin-' 分流守护 — 改 active 视觉时严禁触碰 `startsWith('admin-')` if 分支逻辑
8. ✅ Q1 size=20 修订 — 实测视觉确认；如 20 仍偏小调 22 需 Stage 追加 ack
9. ✅ Q4 active span 右侧 — `right: 0; top: 50%; transform: translateY(-50%); width: 2; height: 60%`
10. ✅ 18 路由手测 — Stage 3 §3 启动 dev server + 实际登录 + 逐个切肉眼校验 topbar title + trailing；自动化测试覆盖不到
11. ✅ 范围抗诱惑 — 不顺手优化 Topbar 拆组件 / Home 复刻 / Sidebar collapse 动画 / NavItem 加 badge prop；严守 R-192/207/208 三大契约

---

🛡️ **执行者收到 GO 信号，待资深架构师最终 ack 后启动 Stage 4。**
