# v0.6.2.1 — HTTP 路由稳态收尾 v2（Stage 4 LOCKED）

> **协议依据**：Loop Protocol v3 完整三阶段
>
> **触发**：v2 LOCKED Phase B 完整版路线图 v4 §2 段 2 + 资深 2026-05-28 累计 8 项决策（4 默认 accept + ε1-ε4）拍板
>
> **执行者**：v0.6 执行者
>
> **守护者**：v0.5 守护者（第 18 次 active Stage 1 评草案 CONDITIONAL APPROVED；第 19 次预约 commit 2 强 active）
>
> **远古守护者**：本 PATCH **不激活**（0 触及 v0.4.x 资产；第 4 次预约 sustained v0.6.3.0 B2 audit V2）
>
> **日期**：2026-05-28
>
> **状态**：**Stage 4 LOCKED**（commit 1 可启动）
>
> **supersedes**：`v0.6.2.1-http-routing-stability.md` Stage 1 草案

---

## §0 v1 → v2 修订摘要

| 修订项 | v1 → v2 | 依据 |
|---|---|---|
| 红线数 7 → **8 + 3 NRP**（共 11 条立约）| +R-PB-C2-4 is_clarified 防死循环 + NRP-C1/C2/C3 协同 | Stage 2 关键独立贡献 + ε3 + 守护者 §III.2 + ε4 |
| F1.1 catalog 推断兜底 | "db 名 unknown 才推断" → **fail-fast 熔断**（DataSource 表空 → MetadataException） | Stage 2 Q2 ❌ 严重风险 + ε2 |
| F2 commit 2 切分 | sustained **不拆**（强耦合 + 中间业务断）| 守护者 §III + ε1（驳 Stage 2 拆 2a/2b 建议）|
| F2.4 新增 is_clarified payload | 防死循环 Session 缓存 + R-PB-C2-4 落地 | Stage 2 关键独立贡献 |
| F3.1 sql_planner 拒识 | `startswith("SELECT")` → **strip markdown + first effective token** | Stage 2 Q3 ❌ 强制修订 |
| §1 末尾 Task #17 标注 | "是否合入" → 清理为"δ3 推 v0.6.2.2 独立" | 守护者 §III.3 |
| §6 风险识别 4 项 → **6 项** | +风险 5 follow-up 协同 + 风险 6 multi-catalog 前瞻 | 守护者 §III.4 |
| R-PB-C2-3 编号 | 草案级 → **明示"v4 §3.3 R-PB-C2-1 第三层 sub-redline"** | 守护者 §III.1 选项 B + ε1 |
| commit 2 强 active 强度 | 未定 → **第 19 次 active 略低于 v0.6.2.0 安全 critical 但显著高于 routine** | 守护者 Q5 |

### Stage 4 LOCKED 累计 8 项决策

```
默认 accept 4 项：
  Stage 2 Q3 C3 strip + tokenize（替换 startswith）
  Task #17 推 v0.6.2.2 独立标注清理
  风险 5+6 补充（follow-up + multi-catalog 前瞻）
  v0.4 远古守护者不激活（第 4 次 sustained v0.6.3.0）

资深 explicit 拍板 ε1-ε4：
  ε1 commit 2 不拆 + 强 active（守护者）
  ε2 fail-fast 熔断（Stage 2 严重风险偿还）
  ε3 立约 R-PB-C2-4 防 Clarifier 死循环（Stage 2 关键独立贡献）
  ε4 全立约 NRP-C1/C2/C3
```

---

## §1 范围（F1-F5 — v2 修订）

### F1 — C1 catalog source_type 产品级（R-PB-C1-1/2 + ε2 fail-fast）

| 子项 | v2 内容 |
|---|---|
| **F1.1**（v2 修订）| `catalog._load_from_db` 推断 source_type — **fail-fast 熔断设计**：①DataSource 表查询失败 → `MetadataException` 熔断；②DataSource 表查询成功且非空且 db 名不在表里 → 推断 `source_type="http"`；③DataSource 表查询成功但**为空** → `MetadataException` 熔断（防误推断既有 doris/mysql）|
| F1.2 | `knot/api/admin/datasources.py` source_type 字段 CRUD 持久化 |
| F1.3 | `frontend/src/screens/admin/modals.jsx` SourceFormModal 加 source_type select（默认 "db"）|

### F2 — C2 pick_http_route 三层路由（R-PB-C2-1/2/3 + R-PB-C2-4）

| 子项 | v2 内容 |
|---|---|
| F2.1 | `knot/services/http_planner.pick_http_route` 三层路由（sustained v4 §3.3）|
| F2.2 | `knot/services/agents/clarifier` 加 routing 二次确认 prompt 段 |
| F2.3 | None 分支 `logger.info(...)` 强制诊断日志 |
| **F2.4**（v2 新增 — R-PB-C2-4）| **is_clarified payload 标记 + Session 缓存** — 前端 React 组件 Session 级缓存用户二次确认结果；下次请求 Payload 加 `is_clarified=true`；后端检测到 → 跳过 Clarifier 二次确认（防死循环）|

### F3 — C3 sql_planner 输出拒识（R-PB-C3-1/2 — v2 修订）

| 子项 | v2 内容 |
|---|---|
| **F3.1**（v2 修订）| `knot/services/agents/sql_planner` — **strip + tokenize** 实现：①先剥离 ` ```sql ` markdown 代码块标记；②再剥离行首注释（`--` `/*`）+ 空行；③检测**首个有效 SQL token** 是否属于 `SELECT` / `WITH`；④非 SELECT 起手 → 返路由错误（不喂 validator）|
| F3.2 | `knot/services/sql_validator` fail-open 路径 warn log |

### F4 — 守护测试（v2 扩展）

| 子项 | v2 内容 |
|---|---|
| F4.1 | C1 兜底测试 + **F1.1 fail-fast 熔断测试**（DataSource 表空 → MetadataException 期望）|
| F4.2 | C2 三层路由 + **R-PB-C2-4 死循环防御测试**（is_clarified=true 跳 Clarifier 验证）|
| F4.3 | C3 拒识 + **strip + tokenize 边界测试**（` ```sql `、`-- comment`、空行起手场景 unambiguous）|
| F4.4 | e38de5e76703 类 e2e smoke 回归（admin UI 改 source_type → catalog.reload → 路由切换完整链路）|
| **F4.5**（v2 新增）| **5 边界 case Clarifier 触发测试**（守护者风险 1 缓解）|

### F5 — 文档收官 + 4 处版本同步

| 子项 | 内容 |
|---|---|
| F5.1 | 4 处版本同步 0.6.2.0 → 0.6.2.1 |
| F5.2 | CHANGELOG.md v0.6.2.1 段（含 R-PB-C2-4 立约 + ε2 fail-fast 决议）|
| F5.3 | CLAUDE.md v0.6.x 路线图加 v0.6.2.1 行 |

**估计行数**：~700 行（含 5 新建测试 + 7 修改业务文件，v1 +100 行）

---

## §2 commit 序列（v2 — sustained 5 commits + ε1 不拆 + commit 2 强 active）

```
commit 1  F1 — catalog source_type 推断兜底（含 ε2 fail-fast 熔断）+ admin UI 字段持久化   指导 0.5-1 天
commit 2  F2 — pick_http_route 三层路由 + Clarifier 二次确认 + is_clarified Session 缓存
          ⚠️ 守护者第 19 次 active 强复核（业务正确性 critical — ε1 不拆替代方案）         指导 1.5-2 天
commit 3  F3 — sql_planner strip+tokenize 拒识 + validator warn log                       指导 0.5-1 天
commit 4  F4 — 5 守护测试（含 R-PB-C2-4 + fail-fast + token strip + 5 边界 + e2e）         指导 1 天
commit 5  F5 — 文档收官 + 4 处版本同步                                                    指导 0.5 天
─────────────────────────────────────────────────────────────────────────────
合计：~4-4.5 天指导（v1 3.5-4 天 +0.5 — fail-fast + R-PB-C2-4 + token strip 整合 +0.5 天）
```

**commit 依赖**：1 → 2 → 3 → 4 → 5

---

## §3 红线（v2 — 8 立约 + 3 NRP = 11 条）

### 3.1 主红线（8 条 — v2 修订）

| 红线 | 内容 | 落地 commit |
|---|---|---|
| R-PB-C1-1 | catalog._load_from_db 推断 source_type **+ ε2 fail-fast**（DataSource 异常熔断）| commit 1 |
| R-PB-C1-2 | admin UI source_type select + 持久化 | commit 1 |
| **R-PB-C2-1**（v4 §3.3 sustained）| 三层路由决策（白名单 / Clarifier / diagnostic）| commit 2 |
| R-PB-C2-2 | None 分支 logger.info 强制 | commit 2 |
| R-PB-C2-3（**v4 §3.3 R-PB-C2-1 第三层 sub-redline** — ε1 / 守护者 §III.1 选项 B）| exclusion regex diagnostic warn 不阻断 | commit 2 |
| **R-PB-C2-4**（v2 新增 — ε3 + Stage 2 关键独立贡献）| **Clarifier is_clarified Session 缓存防死循环** — 请求 Payload `is_clarified=true` 标记；后端跳 Clarifier 二次确认；前端 React Session 级缓存 | commit 2 |
| **R-PB-C3-1**（v2 修订 — Stage 2 强制 + ε 默认 accept）| sql_planner **strip markdown + 首个有效 token** 检测；非 SELECT/WITH → 路由错误 | commit 3 |
| R-PB-C3-2 | sql_validator fail-open warn log | commit 3 |

### 3.2 NRP 子条款（3 条 — v2 新增 + ε4）

| NRP | 内容 | 落地 commit |
|---|---|---|
| **NRP-C1**（ε4） | catalog source_type 推断与 reload 协同 — admin 改 source_type 后 catalog cache invalidate；下次 query 必走新 source_type | commit 1 |
| **NRP-C2**（ε4）| Clarifier 二次确认 prompt 与 admin UI prompt 编辑（v0.6.2.0 R-PA-2 sustained）协同 — admin 编辑 Clarifier prompt 删二次确认段 → fallback 默认行为 | commit 2 |
| **NRP-C3**（ε4）| e2e smoke mock 边界 — requests-mock fixture / inline FastAPI TestClient response；防 CI 依赖真实业务方 admin API | commit 4 |

### 3.3 通用红线 sustained

R-PA-PB-V1 / R-PB2-1~16 / R-94 / R-PA-8 / R-PB-GOV-1 / R-LP-v3-EX-2/2.1/3

---

## §4 验收标准（v2 修订）

### 4.1 功能验收
- [ ] 生产 e38de5e76703 类链路：admin UI 改 source_type → catalog.reload → 重路由 HTTP（不再 SQL 静默 fallback）
- [ ] "用户10047725历史持仓" → exclusion regex 命中 → 走 SQL（默认 SQL 路径 + diagnostic warn log）
- [ ] "BTC 持仓"（模糊 entity）→ Clarifier 二次确认
- [ ] "BTC 持仓" 用户确认后再问"那以太坊呢" → 继承上次 routing 决策（is_clarified 缓存 — R-PB-C2-4）
- [ ] sql_planner 输出 ` ```sql\n-- comment\n无法 SQL 查询...` → 路由错误（不喂 validator）
- [ ] **fail-fast 熔断**：mock DataSource 表为空 → catalog.reload 抛 MetadataException（不 fallback http 推断）

### 4.2 红线守护（11 条 + 通用）
- [ ] R-PB-C1-1 + ε2 fail-fast 熔断测试通过
- [ ] R-PB-C1-2 admin UI source_type 字段持久化
- [ ] R-PB-C2-1/2/3 三层路由 + None log + diagnostic warn
- [ ] **R-PB-C2-4 死循环防御**（Clarifier 弹窗 → 用户选 → is_clarified=true → 下次不弹）
- [ ] R-PB-C3-1 strip + tokenize（` ```sql ` / `-- comment` / 空行 起手测试 PASS）
- [ ] R-PB-C3-2 fail-open warn log
- [ ] NRP-C1 catalog cache invalidate
- [ ] NRP-C2 admin prompt 编辑 fallback
- [ ] NRP-C3 e2e mock 边界

### 4.3 协议合规
- [ ] 7 contracts KEPT
- [ ] R-94 / R-PA-8 / 4 CI 全绿
- [ ] 4 处版本同步

---

## §5 协议合规（v3 三阶段 — 已完成）

| Stage | 状态 |
|---|---|
| Stage 1 | ✅ 2026-05-28 执行者草案 v1 |
| Stage 2 | ✅ 2026-05-28 独立第三方 AI 评审（4.1/5 — 3 强制 + 1 关键独立贡献 Clarifier 死循环）|
| Stage 3 | ✅ 2026-05-28 v0.5 守护者第 18 次 active 草案评（4.5/5 — CONDITIONAL APPROVED 附 4 项整合）|
| **Stage 4** | ✅ **2026-05-28 LOCKED** — 资深 8 项决策（4 accept + ε1-ε4） |

### 守护者激活时机

- ✅ 第 18 次（本 Stage 3 草案评）
- ⏳ **第 19 次 — commit 2 (C2 三层路由 + R-PB-C2-4) 强 active 复核**（业务正确性 critical）
- ⏳ 第 20 次 — PR open / PATCH 终审

---

## §6 风险与依赖（v2 — 6 项）

| 风险 | v2 缓解 |
|---|---|
| Clarifier 二次确认 over-trigger | 5 边界 case 测试 (F4.5) + R-PB-C2-4 is_clarified 缓存 |
| catalog 推断 over-trigger | F1.1 fail-fast 熔断 — DataSource 表空 → 拒绝推断（ε2 偿还 Stage 2 Q2 ❌）|
| sql_planner 非 SELECT false positive | strip + tokenize（ε 默认 accept）+ 仅检测首个有效 token |
| admin UI 改 source_type 影响既有 | 默认 "db"，HTTP 显式选 |
| **风险 5（守护者补）** Clarifier 与 R-12 is_followup 协同 | is_clarified 优先级 ≥ is_followup；follow-up 继承上次 routing 决策（同 R-PB-C2-4）|
| **风险 6（守护者补）** multi-catalog (v0.6.2.5 A1) 前瞻性 | F1.1 推断当前 single-catalog sustained；multi-catalog 落地时 R-PB-A1-5 Connection Context 隔离协同 |

---

## §7 自检清单（v2 — 12 项）

- [x] §0 v1→v2 修订摘要（8 项决策）
- [x] §1 F1-F5 范围（含 F2.4 R-PB-C2-4 + F1.1 fail-fast + F3.1 strip + F4.5 5 边界）
- [x] §2 commit 序列 ε1 不拆 + commit 2 强 active 注明
- [x] §3 8 主红线 + 3 NRP = 11 立约（含 R-PB-C2-3 sub-redline 编号澄清）
- [x] §4 验收（功能 / 11 红线 / 协议）
- [x] §5 v3 三阶段 + Stage 4 LOCKED + 守护者第 19 次预约
- [x] §6 6 项风险（v1 4 + 守护者补 2）
- [x] Stage 2 关键独立贡献全采纳（R-PB-C2-4 立约）
- [x] Task #17 标注清理（推 v0.6.2.2）
- [x] R-PB-C2-3 编号策略（选项 B sub-redline）
- [x] v0.4 远古守护者不激活 + 第 4 次 sustained v0.6.3.0
- [x] R-PB-GOV-1 工期指导 4-4.5 天（v1 +0.5 天）

---

## §8 资深 8 项决策回顾（Stage 4 LOCKED）

### 默认 accept 4 项
- C3 strip + tokenize（Stage 2 Q3 强制修订）
- Task #17 推 v0.6.2.2 独立标注清理
- 风险 5+6 补充（follow-up + multi-catalog 前瞻）
- v0.4 远古守护者不激活

### 资深 explicit 拍板 ε1-ε4
- **ε1** commit 2 不拆 + 强 active（守护者 — 驳 Stage 2 拆建议）
- **ε2** fail-fast 熔断（Stage 2 严重风险偿还 — Q2 ❌ DataSource 表空）
- **ε3** 立约 R-PB-C2-4 防 Clarifier 死循环（Stage 2 关键独立贡献）
- **ε4** 全立约 NRP-C1/C2/C3（与 v0.6.2.0 NRP-1/2 模式延续）

---

**附录 A**：v1 → v2 演进归档
- v1 `docs/plans/v0.6.2.1-http-routing-stability.md` — Stage 1 执行者草案
- Stage 2 context pack — `docs/plans/v0.6.2.1-http-routing-stability-stage2-context-pack.md`
- v2 `docs/plans/v0.6.2.1-http-routing-stability-v2.md`（本文档）— **Stage 4 LOCKED**

**附录 B**：关联 Task
- Task #30 v0.6.2.1（in_progress — Stage 4 LOCKED 后 commit 1 启动）
- Task #17 SQL planner 复合 metric bug — v0.6.2.2 独立 PATCH（ε3 已 ack）

**附录 C**：上游 LOCKED 路线图
- `docs/plans/v0.6.2.0-phase-b-roadmap-v4.md` §2 段 2 + §3.3 R-PB-C1/C2/C3
- `docs/governance/override-cumulative-log.md` §6 R-PB-B1-3 修订纪律（v0.6.2.0 立约）
