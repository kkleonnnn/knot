# v0.6.5.10 — 收官前置① type-hint 统一（PEP 585/604 + `from __future__`）· Stage 1 草案

> **性质**：v0.7.0 硬前置①（v0.6.x 收官纯重构）· **协议**：**轻量 v3**（资深 ack 跳 Stage 2 → 守护者 Stage 3 轻量终审 → execute；Q7 ratify）。
> **基线**：main `7af69cc`（v0.6.5.9），3-agent grounding 实测（非信 LOCKED §0.5「5 api 含 admin」—— 见 §0 admin defer）。
> **作者**：v0.7 执行者 · **日期**：2026-06-20 · **状态**：🟡 DRAFT（待资深 ack + 守护者 Stage 3）· **分支** `chore/typehint-unify`。
> **依据**：LOCKED v0.6→v0.7 整体审核 §3.2 + v0.7.0 Stage 1 §0.5-①。

---

## §0 范围与方法 + grounding 校正

**目标**：model + api 层统一 `from __future__ import annotations` + `Optional[X]` → `X | None`，定 metric model 风格（v0.7.0 加 metric/dimension model 前的地基）。**0 行为变更**。

实测校正 LOCKED §3.2 数字：
- 旧式仅 **`Optional[`**（List/Dict/Tuple/Set/Union = **0**）。15 文件有旧式：10 dataclass models + 5 api（admin/audit/feedback/saved_reports/schemas）。
- 其他层（services 34 / repos 19 / core 7 / adapters 17）**0 旧式** → ① 限 model+api 即**完整**（非部分）。
- LOCKED「10/13 models」→ 分母实为 **14** 域 model 文件（10 dirty + 4 clean：audit/errors 有 `__future__`，catalog/llm 无旧式）；分子 10 对。
- **⭐ admin.py defer 到收官②**（执行者判定，见 §1）→ ① 实际范围 = **14 文件**。

**PEP 563 安全实证**：Pydantic **v2**(2.13.4) + FastAPI 0.137.2 + py3.12。当前全部 `Optional[builtin]`（float/str/int/bool）module-scope 可解析 → 机械转换安全；models/ 全 `@dataclass`（非 Pydantic，string 注解 fields() 安全）；无 `get_type_hints`/`model_rebuild`/`cast` 运行时反射 footgun（grep clean）。

---

## §1 决议

1. **范围 = 14 文件**（10 dataclass models + 4 api：audit/feedback/saved_reports/schemas）。
2. **⭐ admin.py defer 到收官②（执行者判定 — 偏离 LOCKED §0.5 列 admin 于①，待守护者/资深 ratify）**：收官② 拆 908 行 admin.py → `knot/api/admin/` 7 文件；其 6 旧式（`BudgetUpdateRequest` 3 字段 L373-375 + `admin_query_history` 3 route param L577-579）随 ② 在新文件**重生为 `from __future__`-clean**。① 碰 admin = ② 立即重写的双改 + 双 review。**defer 更省更安全**。
3. **0 行为变更**：FastAPI OpenAPI schema + Pydantic 验证 byte-equal（CI `import knot.main` + `/docs` OpenAPI gen + 行为 round-trip 验证；PEP 563 解析失败会在 import 期炸整套件，非静默）。
4. **版本 0.6.5.9 → 0.6.5.10**（PATCH — lint/重构级，§MINOR 规则）。

---

## §2 红线 R-TH-1 ~ R-TH-7（轻量系列 · 前缀待批准）

- **R-TH-1（0 行为变更）**：OpenAPI schema + Pydantic 验证逻辑 byte-equal；CI `/docs` 200 + import net 绿 + test_budgets/test_admin_query_history round-trip 绿 + 全套件绿。
- **R-TH-2（范围）**：仅 14 文件（10 models + 4 api）；**admin.py 0 改（defer ②）**；services/repos/core/adapters/其他 api 0 改。
- **R-TH-3（future-import 位置）**：`from __future__ import annotations` 必须是首个 statement —— import-first 文件（schemas.py）放顶部；docstring-first 文件放 **docstring 后**；misplace = SyntaxError（CI import net 守护）。
- **R-TH-4（转换正确性）**：`Optional[X]` → `X | None`；从 `from typing import` **drop `Optional`**；**保留 `Literal`**（agent/budget/prompt 3 model）；0 leftover `Optional`（否则 NameError，import net 守护）。
- **R-TH-5（隔离）**：package.json 0.0.0 0 碰；catalog/llm/errors/audit model + 4 clean 已达标文件 0 碰。
- **R-TH-6（版本 5 源点）**：同步 0.6.5.10 —— main.py:35 / version.js:5 / README.md:16 / test_rename_smoke.py:131+134 / CHANGELOG.md **L8 header + ⭐ L23 inline bridge literal**（易漏 — grounding 钉死）。
- **R-TH-7（PEP 563 future-edit 纪律 · 立约登记）**：`from __future__` 激活后，**严禁在 dataclass/Pydantic field 注解引入 module-local / `TYPE_CHECKING`-only / 不可在 module scope 解析的类型**（当前全 builtin 安全；未来编辑须守，否则 Pydantic v2 build 期 NameError）。

---

## §3 文件改动

**14 in-scope（+`from __future__` + `Optional[]`→`X | None` + drop Optional import / keep Literal）**：
- models（10 dataclass，docstring-first → future 放 docstring 后）：`agent.py`(2,keep Literal) / `budget.py`(2,keep Literal) / `conversation.py`(9) / `data_source.py`(2) / `few_shot.py`(2) / `knowledge.py`(3) / `prompt.py`(2,keep Literal) / `saved_report.py`(9) / `setting.py`(2) / `user.py`(6)
- api（4）：`schemas.py`(22，import-first → future 顶部；11 BaseModel) / `audit.py`(5，route param) / `saved_reports.py`(4，2 BaseModel) / `feedback.py`(1，L77 route param)

**5 源点**：`knot/main.py` + `frontend/src/version.js` + `README.md` + `CHANGELOG.md`(L8+L23) + `tests/test_rename_smoke.py` → 0.6.5.10 + `CLAUDE.md` 路线图（加收官① 行）。**package.json 严禁碰**。

**defer ②**：`knot/api/admin.py`（6 旧式 — 留收官② 拆分时新文件 born-clean）。

---

## §4 验收标准

- [ ] 14 文件：`from __future__` 正确位置（docstring 后 / 顶部）+ **0 `Optional[` 残留** + `Literal` 保留（agent/budget/prompt）
- [ ] CI：`import knot.main` 绿（全 Pydantic model build）+ `/docs` OpenAPI gen 200 + test_budgets（BudgetUpdateRequest round-trip）+ test_admin_query_history（query param 强制）+ 全套件绿
- [ ] `git diff origin/main HEAD` 仅含 14 文件 + 5 源点 + CLAUDE.md；**admin.py 0 改** + services/repos/core/adapters 0 改 + package.json 0 改
- [ ] 版本 0.6.5.10 五源点（doc-invariant CI 绿 + count==1 + CHANGELOG L23 已 bump）
- [ ] check_file_sizes 绿（目标文件不在 LIMITS，line-neutral + 1 行/文件，不触 cap）
- [ ] **本机 python 坏（libexpat）→ 全走 CI**

---

## §5 commit 序列

1. **C1 — 10 dataclass models**：`from __future__`（docstring 后）+ `Optional[]`→`X | None` + drop Optional import（keep Literal）。验收：grep 0 Optional 残留。
2. **C2 — 4 api 文件**（Pydantic 风险面）：schemas.py(顶部)/audit/saved_reports/feedback。验收：import net + /docs 隐含（CI）。
3. **C3 — 版本 + 收官**：5 源点 0.6.5.10（含 CHANGELOG L8+L23）+ CLAUDE.md 路线图 + 本 plan 归档 + 静态闸门。

---

## §6 协议合规 + §7 自检 / Open Questions

- **轻量 v3**（Q7 ratify）：本草案 = Stage 1 → 资深 ack 跳 Stage 2 → **守护者 Stage 3 轻量终审**（草案 + 逐 commit 落地复核）→ execute。0 红线新立争议 + 0 业务逻辑变更 → 适用轻量。
- **自检**：① grounded 实测（范围/PEP 563/版本）✓ ② 0 行为变更（机械转换）✓ ③ admin defer 降双改 ✓ ④ 隔离严守 ✓
- **Open Questions（守护者 Stage 3 / 资深）**：
  1. **admin.py defer 到②**：偏离 LOCKED §0.5（列 admin 于①）—— ratify defer（避免 ② 双改）还是 ① 含 admin？倾向 defer。
  2. **R-TH 前缀**：轻量系列用 `R-TH-N` 批准否？
  3. **commit 粒度**：C1/C2/C3 三 commit（models/api/版本）vs 单 commit？倾向三 commit（api Pydantic 面独立 review）。

---

**附录**：3-agent grounding 实测 main v0.6.5.9（scope inventory / PEP 563 安全 / admin-defer+tests+version）。关联 [[knot-v070-stage1-state]] + v0.7.0 Stage 1 §0.5-①。
