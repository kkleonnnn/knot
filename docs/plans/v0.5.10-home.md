# v0.5.10 — (C5+) Home 屏复刻（ChatEmpty empty state）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 11 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）
> **依据协议**：v0.5.8 § Visual Replication Protocol + v0.5.9 Shell pilot 模板

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；新增 6 条红线（Stage 2: 3 含 R-227.5 + Stage 3: 3）。

**Stage 2 关键加码**：
- D1/D8 **绝对同意**：Composer 0 改（避免层级断层；留 v0.5.11+ 独立 PATCH）
- Q1：标题 36px + `word-break: keep-all` + `max-width 640` 防换行错位
- Q2：**严禁 hex alpha**（如 `T.accent22`），必须用 `color-mix(in oklch, T.accent 13%, transparent)`
- Q5：**R-227.5 字面分流红线首次确立** — "knot · ready" 装饰小写 vs "KNOT 可能出错" 声明大写

**Stage 3 关键加码**：
- R-237 firstName 兜底无感迁移（手测无 user 态 → "Hi 你"）
- R-238 语义 icon 硬编码映射（suggestions 数组显式 `{icon, text}` 结构）
- R-239 垂直黄金分割 `padding-bottom: 10vh` 内容重心略上移

红线总数 27（**R-214~R-239** 含 R-227.5）。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** Composer 0 改 | ✅ A **绝对同意** | Stage 2 强调；Composer 重构留 v0.5.11+ |
| **D2** "解"字 brand 高亮 | ✅ A 新文案 + brand `<span>` | Stage 1 |
| **D3** brand-ready label | ✅ A 加 "knot · ready" | Stage 1 |
| **D4** chip icon 语义化 | ✅ A 4 种（sparkle/chart/users/db） | Stage 1 |
| **D5** chip 文案 | ✅ A 保产品字面 byte-equal | Stage 1 |
| **D6** Footer T.mono | ✅ A | Stage 1 |
| **D7** KNOT 大写 | ✅ A 保大写 | Stage 1 |
| **D8** Composer 外 boxShadow | ✅ B **不加** | Stage 2 强调避免"层级断层" |
| **Q1** 36px 换行 | ✅ A 保 36px + `word-break: keep-all` + `maxWidth: 640` | Stage 2 加码 |
| **Q2** OKLCH alpha | ✅ A `color-mix(in oklch, T.accent 13%, transparent)` 严禁 hex fallback | Stage 2 强制 |
| **Q3** I.chart / I.users / I.db | ✅ A 36 names 已验证（sparkle/chart/users/db 4 全存在） | 执行者前置 grep 双保险 |
| **Q4** Composer 暂不动 | ✅ A 容忍，留 v0.5.11+ | Stage 2 同 D1 |
| **Q5** KNOT 大小写分流 | ✅ A **R-227.5 字面分流红线** | Stage 2 强制加码 |

---

## 2. 红线最终清单（27 条 R-214~R-239 含 R-227.5）

### Stage 1 草案原 21 条（保留）

| ID | 摘要 |
|---|---|
| **R-214** | ChatEmpty 9 props 签名 byte-equal（Chat.jsx 调用方 byte-equal） |
| **R-215** | firstName 计算逻辑 byte-equal（`user?.display_name?.split(' ')[0] \|\| user?.username \|\| '你'`） |
| **R-216** | suggestions 数组 text 字面 byte-equal（4 句中文，i18n 留 v0.6+） |
| **R-217** | Composer.jsx **0 改**（chat 共享组件，Chat.jsx 非空状态也用） |
| **R-218** | ChatEmpty.jsx ≤ 80 行；LIMITS 30→31 |
| **R-219** | backend tests 432 严格不变 / 7 contracts KEPT / 72 routes |
| **R-220** | App / api / index.css / main / utils / Shared / Shell / NarrativeMotif byte-equal |
| **R-221** | 三处版本同步 0.5.9 → 0.5.10 |
| **R-222** | KnotLogo 三件套范围守护（R-199.5 sustained — 仅 Shared+Login+Shell 三文件命中；不蔓延 ChatEmpty） |
| **R-223** | chat/ 其他子模块 byte-equal（Composer / Conversation / ResultBlock / ThinkingCard / intent_helpers / sse_handler 6 文件 0 改） |
| **R-224** | "解"字 brand `<span>` byte-equal — grep `>解</span>` ≥ 1 |
| **R-225** | CSS 0 污染（App.css 0 行 diff；ChatEmpty.jsx 0 新 className） |
| **R-226** | 17 屏 + 11 子模块 byte-equal（git diff origin/main HEAD -- screens/ 仅 Login.jsx 页脚 + ChatEmpty.jsx） |
| **R-227** | "KNOT 可能出错" 大写字面 byte-equal（v0.5.3 R-126 sustained） |
| **R-228** | check_file_sizes.py LIMITS +1（ChatEmpty.jsx: 80） |
| **R-229** | 顶部 "knot · ready" brand label（mono + brand dot + box-shadow ring） |
| **R-230** | 标题 36px + "解"字 brand + letterSpacing -0.035em |
| **R-231** | 副标题改新文案 + KNOT 保大写（"描述你的业务问题，KNOT 会澄清意图 → 生成 SQL → 整理洞察"） |
| **R-232** | chip icon 4 种语义化（I.sparkle / I.chart / I.users / I.db）— 36 names 复用 |
| **R-233** | chip 高度 44 + radius 10 + gap 10 |
| **R-234** | Footer T.mono + marginTop 28 + letterSpacing 0.04em |

### Stage 2 新增 3 条（含 R-227.5）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-227.5** | **KNOT 大小写字面分流首次确立** — 装饰性元素允许小写（"knot · ready"）；品牌命名 + 声明文本必须大写（"KNOT 可能出错"） | grep `"knot · ready"` ≥ 1（装饰）+ grep `"KNOT 可能出错"` ≥ 1（声明）；两者**同 ChatEmpty.jsx** 共存；其他屏 KNOT 字面 sustained |
| **R-235** | **Chip 弹性布局** — `flex-wrap: wrap` + `justify-content: center` 防移动端/窄侧栏横向溢出 | inline style：`display: flex; flex-wrap: wrap; justify-content: center; gap: 10px` 而非固定 grid-template-columns |
| **R-236** | **"解"字样式隔离** — `display: inline-block; min-width: 1.2em` 保视觉抓手 | `<span style={{color: T.accent, display: 'inline-block', minWidth: '1.2em'}}>解</span>` |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-237** | **firstName 无感迁移** — 兜底逻辑 100% 覆盖：有/无 display_name / 匿名态显 "Hi 你" | R-215 已守护；commit 1 后手测 3 态（display_name / username / 匿名 → "Hi 你"） |
| **R-238** | **语义 Icon 硬编码映射** — suggestions 数组扩 `{icon, text}` 显式结构（不依赖 index） | `[{icon: 'sparkle', text: '...'}, {icon: 'chart', text: '...'}, {icon: 'users', text: '...'}, {icon: 'db', text: '...'}]`；R-216 守护 text 字面 byte-equal，本红线允许扩展数据结构加 icon 字段 |
| **R-239** | **垂直黄金分割** — `padding-bottom: 10vh` 内容重心略上移 | ChatEmpty 容器 inline style：`paddingBottom: '10vh'` |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤锁死）

> 先注入 brand-ready 标签 → 再完成标题高亮 → 最后 4 chip 语义化

| Step | 动作 | 红线 |
|---|---|---|
| 1 | 容器 padding `'24px 28px'` → `'0 80px'` + `paddingBottom: '10vh'` + `justifyContent: 'center'` | R-239 |
| 2 | 顶部 "knot · ready" brand label（mono + brand dot + box-shadow ring 用 `color-mix(in oklch, T.accent 13%, transparent)`） | R-229 + R-227.5 装饰小写 + Q2 |
| 3 | 标题 "Hi {firstName}，今天想`<span>解</span>`哪个结？" + 36px + letterSpacing -0.035em + `wordBreak: 'keep-all'` + `maxWidth: 640` + "解"字 `inline-block + minWidth: 1.2em` | R-230 + Q1 + R-236 |
| 4 | 副标题改新文案 + KNOT 大写（"描述你的业务问题，KNOT 会澄清意图 → 生成 SQL → 整理洞察"） | R-231 |
| 5 | suggestions 数组扩 icon 字段（硬编码映射）+ chip 高度 44 + radius 10 + flex-wrap wrap | R-232/233/235/238 |
| 6 | Footer T.mono + marginTop 28 + letterSpacing 0.04em + "KNOT 可能出错" 大写 byte-equal | R-234 + R-227 |
| 收尾 | grep 全守护 + npm build + Chat 屏空+非空双状态手测 | 见 §5 验收 |

### Stage 3 §3 验收关键 — Composer 0 漂移强制

```bash
git diff origin/main HEAD -- frontend/src/screens/chat/Composer.jsx
# 输出 = 0 行

grep -c "0\.5\.10" knot/main.py tests/test_rename_smoke.py frontend/src/screens/Login.jsx
# 各 ≥ 1 命中
```

### Stage 2 R-227.5 字面分流守护

```bash
grep -c "knot · ready" frontend/src/screens/chat/ChatEmpty.jsx   # ≥ 1（装饰小写）
grep -c "KNOT 可能出错" frontend/src/screens/chat/ChatEmpty.jsx   # ≥ 1（声明大写）
```

### Stage 2 Q2 OKLCH color-mix 实施

```jsx
boxShadow: `0 0 0 4px color-mix(in oklch, ${T.accent} 13%, transparent)`
// 严禁：boxShadow: `0 0 0 4px ${T.accent}22`
```

### Stage 2 Q1 36px 换行守护

```jsx
fontSize: 36, letterSpacing: '-0.035em',
wordBreak: 'keep-all', maxWidth: 640,
```

`keep-all` 防中文逐字断行；`maxWidth` 防超宽屏溢出。

### Stage 3 R-238 硬编码映射实施

```jsx
const suggestions = [
  { icon: 'sparkle', text: '今天的订单总量是多少？' },     // 默认 → sparkle
  { icon: 'chart',   text: '最近 7 天每日 GMV 趋势' },    // 数据概览 → chart
  { icon: 'users',   text: '新用户注册数量（本月）' },     // 用户画像 → users
  { icon: 'db',      text: '查看数据库有哪些表' },         // 数据库 → db
];
```

R-216 守护 text 字面 byte-equal，本红线允许扩展数据结构加 icon 字段。

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/chat/ChatEmpty.jsx     [EDIT 40 → ≤ 80 行]
                                            Step 1: 容器 padding '0 80px' + paddingBottom: 10vh + justify center [R-239]
                                            Step 2: "knot · ready" label [R-229 + R-227.5]
                                                    + boxShadow color-mix(in oklch, T.accent 13%, transparent) [Q2]
                                            Step 3: 标题 + "解" span + 36px + word-break keep-all + maxWidth 640 [R-230 + Q1]
                                                    + "解" inline-block + minWidth 1.2em [R-236] + brand color [R-224]
                                            Step 4: 副标题新文案 + KNOT 大写 [R-231]
                                            Step 5: suggestions 加 icon 字段（硬编码映射）[R-238]
                                                    + chip 44 + radius 10 + flex-wrap wrap [R-233/235]
                                            Step 6: Footer T.mono + marginTop 28 + "KNOT 可能出错" 大写 [R-234/227]
                                            守护: 9 props 签名 byte-equal [R-214]
                                                  + suggestions text 字面 byte-equal [R-216]
                                                  + firstName 计算 byte-equal [R-215/237]
                                                  + inline styles only [R-225]
                                                  + 无 version 字面（仅 Login 显示）

scripts/check_file_sizes.py                 [EDIT LIMITS +1]
                                            - "frontend/src/screens/chat/ChatEmpty.jsx": 80 (30→31 条) [R-218/228]

knot/main.py                                [EDIT version 0.5.9 → 0.5.10]  [R-221]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.9 → 0.5.10]  [R-221]
frontend/src/screens/Login.jsx              [EDIT 页脚 v0.5.9 → v0.5.10]  [R-221]

frontend/src/screens/chat/Composer.jsx      [unchanged byte-equal]    [R-217 + Stage 3 §3 验收]
frontend/src/screens/chat/Conversation.jsx  [unchanged]               [R-223]
frontend/src/screens/chat/ResultBlock.jsx   [unchanged]               [R-223]
frontend/src/screens/chat/ThinkingCard.jsx  [unchanged]               [R-223]
frontend/src/screens/chat/intent_helpers.js [unchanged]               [R-223]
frontend/src/screens/chat/sse_handler.js    [unchanged]               [R-223]

frontend/src/screens/ (其余 17 屏 + 11 子)   [unchanged]               [R-226]
frontend/src/App.jsx                        [unchanged]               [R-220]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-220]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]               [R-220]
frontend/src/App.css                        [unchanged 0 行 diff]     [R-225]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                    [unchanged]
backend (knot/) 任何文件除 main.py          [unchanged]               [R-219]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.10 段]
                                            R-214~R-239 偿还表 + Loop Protocol v3 第 11 次施行存档
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.10
                                            - v0.5.x 路线图 v0.5.10 ⏳ → ✅
docs/plans/v0.5.10-home.md                  [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（不允许减少）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（31 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.10**

### Stage 2/3 强化验证

- [ ] **R-227.5**：grep `"knot · ready"` ≥ 1 + grep `"KNOT 可能出错"` ≥ 1
- [ ] **R-235**：chip 容器 inline style 含 `flex-wrap: wrap` + `justify-content: center`
- [ ] **R-236**：`<span>解</span>` 含 `display: 'inline-block'` + `minWidth: '1.2em'`
- [ ] **R-237**：手测无 user 态（or display_name 为空）显 "Hi 你，今天想解哪个结？"
- [ ] **R-238**：suggestions 数组 4 条各显式含 icon 字段（sparkle / chart / users / db）；肉眼校验语义对齐
- [ ] **R-239**：ChatEmpty 容器 inline style 含 `paddingBottom: '10vh'`
- [ ] **Q1**：标题 inline style 含 `wordBreak: 'keep-all'` + `maxWidth`
- [ ] **Q2**：grep `color-mix(in oklch` ≥ 1；grep `T\.accent[0-9a-f]{2}` = 0（无 hex alpha 残留）

### 契约守护（首要红线）

- [ ] **R-214**：ChatEmpty 9 props 签名 byte-equal — diff 函数签名行未变
- [ ] **R-215**：firstName 计算逻辑 byte-equal — grep `display_name?.split(' ')[0]` 命中
- [ ] **R-216**：suggestions text 字面 byte-equal（4 句中文 grep 命中）
- [ ] **R-217**：`git diff origin/main HEAD -- frontend/src/screens/chat/Composer.jsx` = 0 行（Stage 3 §3 强制）

### 范围守护

- [ ] **R-220**：App / api / index.css / main / utils / Shared / Shell / NarrativeMotif `git diff` = 0
- [ ] **R-222**：grep `KnotLogo|KnotMark|KnotWordmark` frontend/src/ 仅 3 文件命中（Shared+Login+Shell — 不增 ChatEmpty）
- [ ] **R-223**：chat/ 其他 6 子模块 `git diff` = 0
- [ ] **R-225**：`git diff frontend/src/App.css` = 0
- [ ] **R-226**：`git diff origin/main HEAD -- frontend/src/screens/` 仅 Login.jsx 页脚 + ChatEmpty.jsx
- [ ] **R-227**：grep `"KNOT 可能出错"` Footer 字面命中

### 端到端

- [ ] Chat 屏空对话状态视觉对照 demo（greeting / suggestion chips / footer）
- [ ] **Composer 不破坏**：Chat.jsx 非空状态（提问 → SQL → ResultBlock）端到端正常
- [ ] v0.5.0~v0.5.9 既有功能（cartesian / fan-out / 7 intent / CSV / 错误边界 / Shell topbar / Login pilot）端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.10): ChatEmpty.jsx Home empty state 视觉重构 — brand label + "解"字高亮 + chip icon 语义化 + mono footer (R-214~R-239)` | npm build + Chat 屏空+非空状态手测 + R-217 Composer diff=0 + R-222 KnotLogo 3 文件 + R-227.5 大小写分流 | 9 props 签名 byte-equal + Step 1→6 顺序 + CSS 0 污染 + Composer 不破坏 |
| 2 | `chore(v0.5.10): version bump 0.5.9→0.5.10 三处同步 + check_file_sizes LIMITS +1 (R-218/221/228)` | 全闸门 + routes=72 + version=0.5.10 + 31 条 LIMITS | R-221 三处同步 grep ≥ 3；R-217 Composer 0 漂移强制 |
| 3 | `docs(v0.5.10): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED 归档` | 全闸门 | Loop Protocol v3 第 11 次施行存档 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→6 顺序不可换。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 11 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 27（v0.5.10 区段 R-214~R-239，含 R-227.5）
- ✅ R-217 Composer 0 改 — 三方共识，留 v0.5.11+ 独立 PATCH
- ✅ R-227.5 KNOT 大小写字面分流首次确立 — 装饰 vs 声明语义边界
- ✅ 范围最小化 — 17 屏 + 11 子模块 + 8 核心非屏 + App.css 全部 byte-equal

---

## 8. 执行者纪律自检（11 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.10-home`
2. ✅ R-217 Composer 0 改纪律 — 绝对不碰；commit 1 收尾 + commit 2 后两次强制 diff = 0
3. ✅ R-222 KnotLogo 不蔓延 — grep 仍仅 3 文件命中（Shared/Login/Shell）；ChatEmpty 严禁引入
4. ✅ R-227.5 KNOT 字面分流首次确立 — commit 1 后强制双 grep 命中
5. ✅ Q2 严禁 hex alpha — 用 color-mix 替代；commit 1 后 grep `T\.accent[0-9a-f]{2}` = 0
6. ✅ R-225 CSS 0 污染 — 全 inline style + 已有 utility；想新 className 立即停手
7. ✅ R-238 硬编码映射 — suggestions 数组扩 `{icon, text}` 显式结构；语义对齐肉眼校验
8. ✅ R-237 firstName 兜底 — 手测 3 态：display_name / username / 匿名 → "Hi 你"
9. ✅ 三处版本同步 — commit 2 同时改 main.py + smoke + Login 页脚
10. ✅ Chat 屏空+非空双状态手测 — commit 1 后启动 dev server + 实际提问校验 Composer 不破坏
11. ✅ 范围抗诱惑 — 不顺手改 Composer/thinking/Favorites/Conversation；严守 R-214/217/220/222/223/226 六大契约

---

🛡️ **执行者收到 GO 信号，待资深架构师最终 ack 后启动 Stage 4。**
