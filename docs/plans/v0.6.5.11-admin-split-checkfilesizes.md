# v0.6.5.11 — 收官前置② admin.py 拆 7 文件 + check_file_sizes 根治 · Stage 1 草案

> **性质**：v0.7.0 硬前置②（v0.6.x 收官纯重构）· **协议**：**完整 v3 三阶段**（结构最重，不适用简化协议 — 资深拍板纯重构走完整 v3，镜像 v0.6.3.2 先例）。
> **基线**：main `acb5dc8`（**v0.6.5.10**，收官① merged），grounded 实测（admin.py 收官①未碰，仍 908/30/6 Optional[/0 future）。
> **作者**：v0.7 执行者 · **日期**：2026-06-21 · **状态**：🟢 DRAFT v2 — Stage 3 守护者终审整合（ACCEPT w/ 2 revisions：ACK 全集 8 + AST-position 哨兵；执行者 R-137 校正守护者 time_resolver 误列）；**待资深拍板 LOCKED → C1**（整合记录见 §8）· **分支** `refactor/admin-split`。
> **依据**：LOCKED §0.5-A.2/A.3（admin 拆须 re-export `_DS_STATS_CACHE` + 收官独立 PATCH）+ B.3（check_file_sizes 根治）+ 守护者收官① Stage 3 硬条件（7 新文件 born-clean）。

---

## §0 范围与方法 + grounding

**目标**：① admin.py 908 行/30 端点 → `knot/api/admin/` 包（7 域文件 + `__init__` 聚合 router）；② check_file_sizes allowlist → backend auto-discover 根治（治巨型文件无闸门复发）。**0 行为变更**（纯结构移动）。

**grounded 实测 main acb5dc8**：
- admin.py = **908 行 / 30 endpoint / 6 Optional[ / 0 from __future__**（收官① defer 未碰）。
- **挂载点**：`knot/main.py:18` `from knot.api import admin` + `:227` `admin.router`（in include_router list）。
- **全 importer = 1 生产 + 3 测试**：main.py（`admin.router`）+ conftest.py:60 / test_isolation_hardening.py:59,76 / test_datasources_stats.py:41（均 `from knot.api import admin as X` → `X._DS_STATS_CACHE`）。
- **_DS_STATS_CACHE**：def@678 + `_DS_STATS_TTL_SEC`@679；读写 L698/699/759/760（`admin_datasources_stats` 内）；3 测试全 **in-place `["data"]=` 突变**（**非 reassign**）。
- **模块级状态**：`router`@23 / `_VALID_ROLES`@46 / `BudgetUpsertRequest`@363 / `BudgetUpdateRequest`@372 / `BudgetConfigRequest`@766 / `_BUDGET_DEFAULTS`@774 / `_DS_STATS_CACHE`+`_TTL`@678。
- **check_file_sizes**：ci.yml:32 `python3 scripts/check_file_sizes.py`；纯 `for rel, limit in LIMITS.items()` 循环（**不在 LIMITS 完全逃逸**）；现 61 条 allowlist。

---

## §1 决议

### 1.1 admin.py → `knot/api/admin/` 包（7 域 + __init__）

按 RESTful 资源拆（端点归域**显式映射** — 不按文件位置推断；earlier grounding 30 端点逐个定位）：

| 新文件 | 端点数 | 端点（method path → handler @旧行） | 随迁模块级 | 估行 |
|---|---|---|---|---|
| `users.py` | 4 | GET/POST `/users`@28/49 · PUT/DELETE `/users/{id}`@68/103 | `_VALID_ROLES` | ~105 |
| `datasources.py` | 5 | GET/POST `/datasources`@114/184 · PUT/DELETE `/datasources/{id}`@198/236 · **GET `/datasources-stats`@682** | **`_DS_STATS_CACHE`+`_TTL`**（同域耦合）| ~245 ⚠️ 最大 |
| `models.py` | 5 | GET `/models`@246 · POST `/models/{k}/default`@263 · `/toggle`@271 · GET/PUT `/agent-models`@283/293 | — | ~80 |
| `api_keys.py` | 2 | GET/PUT `/api-keys`@324/334 | — | ~55 |
| `budgets.py` | 6 | GET/POST `/budgets`@378/383 · PUT/DELETE `/budgets/{id}`@408/427 · GET/PUT `/budget-config`@783/795 | `Budget*Request` ×3 + `_BUDGET_DEFAULTS` | ~165 |
| `stats.py` | 6 | GET `/cost-stats`@307 · `/recovery-stats`@438 · `/metrics`@461 · `/query-history`@574 · `/audit-stats`@617 · `/budgets-stats`@641 | — | ~230 ⚠️ 2nd |
| `or_catalog.py` | 2 | POST `/sync-or-catalog`@815 · GET `/or-catalog`@877 | — | ~95 |
| `__init__.py` | 0 | 聚合：7 子 router include + **re-export `_DS_STATS_CACHE`** + re-export `router` | — | ~30 |

**归域裁定（earlier grounding flag）**：① `datasources-stats`@682 物理在 stats 块但 datasource 耦合（拥 `_DS_STATS_CACHE`）→ **归 datasources.py**（与 db_connector/data_source_repo 同域）。② `budget-config`+`budgets-stats` 散落 → budget-config 归 budgets.py（与 CRUD 同），budgets-stats 归 stats.py（stat 类）。

### 1.2 byte-equal 契约（宪法级）

- `knot/main.py` **0 改**：`from knot.api import admin`（admin 变 package 仍可 import）+ `admin.router`（`__init__.py` 暴露聚合 router）byte-equal。
- 30 端点 method + path + 函数签名 + 业务逻辑 byte-equal（纯移动，0 逻辑变更）。
- 3 测试 importer `admin._DS_STATS_CACHE` 经 `__init__.py` re-export 工作。

### 1.3 _DS_STATS_CACHE re-export 安全（拆分核心风险）

`__init__.py`：`from .datasources import _DS_STATS_CACHE`（绑定**同一 mutable dict 对象**）。3 测试全 **in-place 突变**（`admin._DS_STATS_CACHE["data"] = None`）→ 通过 re-export 绑定突变同对象，datasources.py 路由读同对象 → **可见** ✓。**与 catalog from-import 陷阱的良性表亲**：dict in-place 突变跨绑定可见；仅 **reassign**（`admin._DS_STATS_CACHE = {}`）会破 —— 3 测试 0 reassign（实测全 `["key"]=`）→ 安全。conftest L63 `except (ImportError, AttributeError): pass` 静默吞的安全网 = test_isolation_hardening L82 硬断言（拆错会硬 FAIL 非静默）。

### 1.4 check_file_sizes 根治（LOCKED B.3）

allowlist → **backend auto-discover**：
```python
DEFAULT_CAP = 300
# ACK = backend >300 全集（守护者硬纠1 + 执行者 R-137 实测校正 wc -l 全扫）；≤300 auto-pass 无需登记
ACK = {  # 每条带理由 + split-planned 标注
    "knot/api/admin.py":                  908,  # ⏳ TEMP — 本 PATCH C2 拆后移除
    "knot/services/agents/catalog.py":    460,  # ⏳ TEMP — 收官③ 拆后移除
    "knot/services/http_planner.py":      508,  # futures 下沉 JIT v0.7.2（保留）
    "knot/api/query.py":                  440,  # SSE 协议样板（R-94 ack 保留）
    "knot/repositories/message_repo.py":  376,
    "knot/services/agents/sql_planner.py":359,  # ReAct 调度
    "knot/adapters/db/doris.py":          344,
    "knot/services/engine_cache.py":      337,
}
for p in (repo/"knot").rglob("*.py"):
    rel = p.relative_to(repo).as_posix()
    cap = ACK.get(rel, DEFAULT_CAP)
    if linecount(p) > cap: violations.append(f"{rel}: {n} > {cap}")
```
- **backend auto-discover**（`knot/**/*.py`）→ 新文件不能逃逸（治 admin/http_planner/catalog/repos/adapters 盲区）。
- **DEFAULT_CAP 300**（grounded：250 会误杀 totp_service 258/main 255/base 251/api·catalog 251 等合法文件；300 干净分离真 giants）。
- **⭐ ACK = 全部 >300 backend（实测 8 个，守护者硬纠1）**：admin(temp) + catalog(temp) + http_planner + query + message_repo + sql_planner + doris + engine_cache。**R-137 校正**：`time_resolver`（239 行，350 是 cap 非行数 — 守护者误列）+ `llm_client`（252）**≤300 → auto-pass，不入 ACK**。
- **ACK 演进**：C1 含 admin 908 → C2 拆后移除 admin（7 新文件 ≤300 auto-caught）→ 收官③ 拆后移除 catalog。≤300 旧 LIMITS 项（orchestrator 220/llm_client 252/time_resolver 239 等）auto-pass，松至 300（根治默认 cap 简化，可接受）。
- frontend 暂留 explicit allowlist（LOCKED 只 mandate backend；前端无包根 walk）。

### 1.5 born-clean（守护者收官① Stage 3 硬条件）

7 新 admin 文件全 `from __future__ import annotations` + **0 `Optional[`**（admin.py 6 旧式：Budget*Request 3 字段 + admin_query_history 3 param → `X | None`）—— 偿还收官① 的 admin defer。**位置 = AST-position（守护者硬纠2）**：`from __future__` 是首个 **import**（容忍前导 module docstring — 7 新模块应加 docstring 好风格 + v0.6.5.10 model 先例）→ 在所有其他 import 前，**非字面 L1**（与 v0.6.5.10 R-TH-3 一致：docstring-first → docstring 后）。

### 1.6 OUT-OF-SCOPE（防蔓延）

- ❌ **catalog.py 0 碰**（收官③ 独立高守护 PATCH；**②③ 严禁同批** LOCKED A.4）—— 仅在 check_file_sizes ACK 预登记（待③移除）。
- ❌ http_planner futures 下沉（JIT v0.7.2）。
- ❌ **period-parse helper 去重**（admin.py 4× `(period or "").strip().lower()` + endswith("d")）—— 观察到但**不做**（纯移动优先；去重有行为变更风险，留独立 cleanup；守护者偏好 pure-move）。
- ❌ admin 端点业务逻辑任何变更（纯结构移动）。

---

## §2 红线 R-AS-1 ~ R-AS-10（Admin Split 系列 · 前缀待批准）

- **R-AS-1（byte-equal 契约 · 宪法级）**：main.py `from knot.api import admin` + `admin.router` **0 改**；30 端点 method/path/签名/业务 byte-equal；`__init__.py` 聚合 router + re-export `_DS_STATS_CACHE`。
- **R-AS-2（_DS_STATS_CACHE re-export 安全）**：`from .datasources import _DS_STATS_CACHE`（同对象）；3 测试 in-place 突变可见验证；0 reassign。
- **R-AS-3（born-clean · 守护者硬条件）**：7 新文件全 `from __future__` + **0 `Optional[`**（admin 6 旧式 → `X | None`）；位置 = **AST-position**（首个 import，容忍 docstring — 非字面 L1，守护者硬纠2 + R-TH-3 对齐）。
- **R-AS-4（模块级状态归位）**：_VALID_ROLES→users / Budget*Request×3 + _BUDGET_DEFAULTS→budgets / _DS_STATS_CACHE+_TTL→datasources；每文件独立 `APIRouter()`。
- **R-AS-5（端点归域显式映射）**：30 端点按 §1.1 表显式分配（datasources-stats→datasources / budget-config→budgets / budgets-stats→stats）；不按文件位置推断。
- **R-AS-6（check_file_sizes 根治）**：backend auto-discover `knot/**/*.py` + DEFAULT_CAP 300 + ACK dict = **全部 >300 backend（实测 8：admin+catalog temp + http_planner+query+message_repo+sql_planner+doris+engine_cache）**；time_resolver 239/llm_client 252 ≤300 不入 ACK（R-137 校正守护者 time_resolver 误列）；admin split 后移除 admin ack；frontend 暂 explicit。**起 C1 前 `wc -l` 全扫复定 ACK 全集（size-gate RED 兜底）**。
- **R-AS-7（0 行为变更）**：CI import net + 全 pytest + `/docs` OpenAPI + **3 `_DS_STATS_CACHE` 隔离测试**（conftest reset + isolation_hardening poison/reset + datasources_stats miss）绿；route 计数 CI 真值（不硬编）。
- **R-AS-8（②③ 不同批）**：catalog.py 0 碰（仅 check_file_sizes ACK 预登记，待收官③ 移除）。
- **R-AS-9（隔离）**：除 admin.py→admin/ + check_file_sizes + 版本 5 源点外，其他后端/前端 0 改；package.json 0.0.0 不碰。
- **R-AS-10（版本 + cosmetic 偿还）**：0.6.5.11 五源点；**fold v0.6.5.10 plan「13→14 文件」cosmetic 校正**（守护者收官① deferred 发现）。

---

## §3 文件改动

- `knot/api/admin.py` — **[DELETE]**（908 行 → 拆）
- `knot/api/admin/__init__.py` — [NEW] 聚合 router（include 7 子 router）+ re-export `_DS_STATS_CACHE` + `router`
- `knot/api/admin/{users,datasources,models,api_keys,budgets,stats,or_catalog}.py` — [NEW] 7 域文件（born-clean：`from __future__` + 0 Optional[）
- `scripts/check_file_sizes.py` — **[REWRITE]** auto-discover + DEFAULT_CAP 300 + ACK dict（移除 admin.py ack after split；frontend 段保留）
- `tests/api/test_admin_package.py` — [NEW] 拆分契约哨兵（Stage 2 Q4 + 守护者 ENDORSE）：① `admin.router` 聚合可解析 ② `admin._DS_STATS_CACHE` re-export **id() 同对象锁**（Stage 2 哨兵）③ born-clean **AST-position 断言**（7 文件 `from __future__` 是首个 import，容忍 docstring，**非字面 L1** — 守护者硬纠2）+ 0 `Optional[`
- 版本 **5 源点** 0.6.5.11：`knot/main.py` + `frontend/src/version.js` + `README.md` + `CHANGELOG.md`（top==main & count==1）+ `tests/test_rename_smoke.py` + `CLAUDE.md` 路线图（加收官② 行 + fold v0.6.5.10「13→14」校正）；**package.json 0.0.0 不碰**
- **0 改**：main.py（除 import 行本就 byte-equal）/ catalog.py / 其他 api/services/repos/adapters

---

## §4 验收标准

- [ ] **byte-equal**：`git diff main.py` = 0 行（admin.router 仍解析）；30 端点 route 计数 CI 真值不变；admin 包 import 成功
- [ ] **_DS_STATS_CACHE**：3 测试（conftest/isolation_hardening/datasources_stats）绿 — re-export 同对象 in-place 突变可见；故意 reassign 会破（验证安全网）
- [ ] **born-clean**：7 新文件 grep `from __future__` 全有（首句）+ **0 `Optional[`**；admin.py 已删（旧 6 Optional[ 消失）
- [ ] **check_file_sizes 根治**：auto-discover 捕获 7 新 admin 文件（≤300）；5 giants ack'd；admin.py ack 已移除；**故意建一个 350 行 backend 测试文件 → CI 红**（证 auto-discover 生效）；CI 绿
- [ ] **CI 0 行为变更**：import net + 全 pytest + `/docs` OpenAPI + 隔离测试绿；7 contracts KEPT
- [ ] catalog.py **0 改**（②③ 不同批）；package.json 0 碰
- [ ] 版本 0.6.5.11 五源点（doc-invariant CI 绿 + count==1）+ v0.6.5.10 plan「13→14」已校正
- [ ] **本机 python 坏 → 全走 CI**

---

## §5 commit 序列（完整 v3）

1. **C1 — check_file_sizes 根治（gate 先立）**：auto-discover + DEFAULT_CAP 300 + ACK = **全部 >300 backend（实测 8：admin temp + catalog temp + http_planner/query/message_repo/sql_planner/doris/engine_cache）**；**起 C1 前 `wc -l` 全扫复定 ACK**（守护者硬纠1；time_resolver 239/llm_client 252 ≤300 不入）。验收：CI 绿（全 ack 或 ≤300）+ 故意建 350 行 backend 测试文件 → 红（证 auto-discover）。
2. **C2 — admin.py 拆 7 文件包**：[DELETE] admin.py → admin/ 7 文件 + `__init__`（re-export router + _DS_STATS_CACHE）+ born-clean（`from __future__` **AST-position 容忍 docstring** + 0 Optional[）；移除 admin.py ACK；**test_admin_package.py 哨兵**（id() 锁 + AST-position born-clean 断言）。验收：main.py 0 改 + 3 _DS_STATS_CACHE 测试 + 全 pytest + /docs 绿 + 7 文件 auto-caught ≤300。
3. **C3 — 版本 + 收官**：5 源点 0.6.5.11 + CHANGELOG（demote v0.6.5.10）+ CLAUDE.md 路线图（收官② 行 + fold「13→14」校正）+ plan 归档。

---

## §6 协议合规 + §7 Open Questions

- **完整 v3**：本草案 = Stage 1 → 资深分发 Stage 2 初审 → v0.6 守护者 Stage 3 终审（整合 1+2）→ 资深拍板 LOCKED → 执行者落 C1/C2/C3。
- **角色**：执行者 = v0.7（本会话）｜守护者 = v0.6｜资深 = kk。固定 artifact 纪律（commit 定 hash 后评审期字节不动）。
- **Open Questions — ✅ Stage 2 初审 + 守护者 Stage 3 全部已答（详 §8 整合记录）**：
  1. **R-AS 前缀**（Admin Split）批准否？还是 R-CO2-N（Closeout②）？
  2. **datasources.py ~245 行**（含 datasources-stats）是最大文件，逼近 DEFAULT_CAP 300 但安全。接受，还是把 `admin_list_datasources` 的 `_test_source`/`_http_base_url` 嵌套 helper 抽出（降行）？倾向接受（纯移动优先，不顺手重构）。
  3. **__init__.py re-export Budget*Request？** 仅 budgets.py 内部用（main/tests 不直接 import）→ 倾向**不** re-export（仅 router + _DS_STATS_CACHE）。
  4. **test_admin_package.py 哨兵**：加（守拆分契约 — admin.router + _DS_STATS_CACHE re-export）还是靠既有 3 测试 + import net 兜底？倾向加（拆分契约值得显式哨兵）。
  5. **C1/C2/C3 三 commit** vs admin split 再细分？倾向三 commit（split 是原子移动，细分增 review 负担）。
  6. **check_file_sizes frontend 段**：暂留 explicit allowlist（LOCKED 只 mandate backend），还是顺手也 auto-discover？倾向暂留（收窄 scope；frontend auto-discover 独立评估）。

---

## §8 Stage 3 守护者终审整合记录（2026-06-21）

> 守护者裁定 **ACCEPT with 2 revisions**（承重设计全 confirmed，2 纠为落地护栏非方向）。整合 Stage 1 草案 `e1f9341` + Stage 2 初审（4.98/5，Q1-Q6 + 4 死锁）：

| # | 类型 | 整合 | 落点 |
|---|---|---|---|
| 硬纠1 | check_file_sizes ACK 全集 | ACK = **全部 >300 backend**（非「5 giants」）；执行者 R-137 `wc -l` 实测 = **8**（admin temp + catalog temp + http_planner/query/message_repo/sql_planner/doris/engine_cache）。**执行者校正守护者**：time_resolver（守护者列 350）实为 **239 行**（350=cap 非行数）+ llm_client 252 → ≤300 auto-pass **不入 ACK**（双向实证）。起 C1 前 `wc -l` 复定。 | §1.4 / R-AS-6 / §5-C1 |
| 硬纠2 | born-clean AST-position | `from __future__` 断言 = AST-position（首个 import，容忍 module docstring），**非字面 L1** —— 7 新模块应有 docstring（好风格 + v0.6.5.10 R-TH-3 先例）。弃 Stage 2 死锁#2「L1」，采 Q4「AST 头部」。 | §1.5 / R-AS-3 / §3 / §5-C2 |

**Stage 2 裁决吸收（守护者 ENDORSE）**：Q1 R-AS 前缀 ✅ / Q2 datasources ~245<300 不二级拆 ✅ / Q3 不 re-export Budget*Request DTO ✅ / Q4 test_admin_package.py 哨兵 **YES**（id() 锁 + AST-position born-clean）✅ / Q5 3-commit ✅ / Q6 frontend explicit + backend auto-discover ✅。4 死锁：#1 _DS_STATS_CACHE re-export 无 reassign（守护者实测背书）/ #2 born-clean → AST-position / #3 ACK 无通配符（扩 8）/ #4 catalog 0 行物理隔离 ✅。

**双向实证价值**：守护者 grounded 抓执行者 ACK 漏 query/sql_planner（不 ACK 则 C1 立红）+ sentinel L1 误杀风险；执行者 R-137 回核抓守护者 time_resolver cap-vs-行数 误列。承重命门（_DS_STATS_CACHE re-export in-place 突变安全）守护者实测背书。

---

**附录**：grounded 实测 main `acb5dc8`（admin.py 908/30 收官①未碰 + main.py 挂载 + 全 importer + _DS_STATS_CACHE 3 耦合 + check_file_sizes 结构 + backend >300 全集 8）。关联 [[knot-v070-stage1-state]] + LOCKED §0.5-A.2/A.3/B.3 + 守护者收官① Stage 3 born-clean 硬条件。
