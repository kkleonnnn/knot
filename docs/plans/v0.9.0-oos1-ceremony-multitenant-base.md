# v0.9.0 — OOS-1 红线修订仪式 + 多租户地基第一刀 · 执行手册（Stage 1 草案 · 已过执行者对抗自核）

> **性质**：Loop Protocol v3「红线修订仪式」+ v0.9 多租户 MINOR 第一刀。业务模型重构级，走**完整 v3 三阶段**（本文件 = Stage 1 草案 → Stage 2 辅助 AI 初审 → Stage 3 守护者[v0.8]终审 → 执行），非快通道。
> **来源**：[`v0.8-to-v0.9-overall-review-opinions.md`](v0.8-to-v0.9-overall-review-opinions.md) 三方齐备 + 资深仲裁 LOCKED（A3/B1/C1/D1）。方向已 LOCKED，本手册不重新论证 C 方案。
> **基线**：main @ `a0434a8`（**v0.8.22 已 merged** #242）。**版本目标**：**0.8.22 → 0.9.0**（MINOR，merge 后打 annotated tag `v0.9.0`；实测 v0.9 号未被 tag 占用，无需跳号）。
> **盘点依据**：2026-07-17 执行者 6-agent grounded 盘点 + 3 证伪员对抗自核（§9）+ **Stage 2 Codex 初审（13 redline）+ Stage 3 守护者终审 major-revise（§0）+ 执行者 Stage 3 前 grounded 复现（重锚 a0434a8 + 载重机制独立复核，§0.2）**。所有 file:line 以 **a0434a8** 为准（v0.8.22 仅改 base.py 结构：get_conn 现 `base.py:25`、迁移块入 `migrations.py`；其余锚点经复核无漂移）。

---

## § 0 Stage 3 守护者终审整合（LOCKED · 2026-07-18 · 与 §1-§9 冲突处以 §0 为准）

**流程**：Stage 1 草案（执行者，已过 3 证伪员自核）→ Stage 2 Codex 初审（13 redline）→ Stage 3 守护者[v0.8]终审 **major-revise**（Stage 1 + Stage 2 一起 grounded 核，Codex 13 条全确认无夸大/无证伪，+ 4 守护者增量 G1-G4）→ 本整合。执行者拿 Stage 3 终审意见（不看 Stage 2 原文，避重复消化）+ **Stage 3 前独立 grounded 复现**（无单点可信，守护者亦不橡皮图章）后落地。

### § 0.1 Stage 3 守护者 10 条 blocking must-fix（放行条件，逐条整合去向）
| # | must-fix | 整合去向 |
|---|---|---|
| 1 | 基线重锚 a0434a8/v0.8.22（删 P2「待完成」· 版本 0.8.22→0.9.0 · file:line 重锚） | header + §1 前置链 P2 + 全文锚点（已重锚，见 §0.2） |
| 2 | **SSE ctx = set-without-reset + 真 query-stream 测（Top-1 兼 byte-equal 命门）** | §2 产物 iv set点#1（**升级为镜像 catalog ctx 模式**，见 §0.3）+ §5 验收 |
| 3 | 启动序重排（platform 库建表+seed tenant#1 先于 set ctx，解 chicken-and-egg） | §2 产物 iv set点#2 |
| 4 | 调度器 run_due try/finally reset + 线程复用测 | §2 产物 iv set点#4 + §5 |
| 5 | R-T-GATE 正文列全 gated 项 + 硬 CI `assert_no_second_active_tenant_served` | §3.2 |
| 6 | 迁移 complete-marker 绑校验（schema version + 32 表清单 + 行数 hash + 解密烟测） | §4 C4 |
| 7 | uploads.db 纳入备份/迁移/回滚 + DEPLOY + WAL/-shm 时序（独立连接 checkpoint+close 再 copy/rename） | §4 C4 + §2 产物 ii#4 |
| 8 | production-layout 测（anchor=/tmp/data/knot.db + db_dir='tenants/1'，补 db_dir='.' 之外） | §4 C2 + §5 |
| 9 | flatten=144 精确 snapshot 测（复用 `tests/_route_count.py:39 app_route_paths`）或软化验收 | §4 C3 + §5 |
| 10 | C3 新 CI docstring 显式区分绊线（列缺席）vs 隔离（fail-closed+文件边界） | §4 C3 |

### § 0.2 执行者 Stage 3 前 grounded 复现（无单点可信 · 3 agent 重锚+载重机制独立复核）
- **基线重锚 a0434a8**：手册全部关键 file:line 在 v0.8.22 后**逐条准确、0 stale**（v0.8.22 仅拆 base.py）。订正锚点：get_conn 现 `base.py:25`（仍 `from knot.config import SQLITE_DB_PATH` @:14）· 迁移块入 `migrations.py`（run_pre :21 / run_post :33 / run_startup :213 / _migrate_uploads_to_isolated_db_once :236，SQLITE_DB_PATH 调用期反读 base）· init_db 编排序 `base.py:41-84`。flatten 实测 **144** 不变。get_conn 消费面 **32**（naive grep 得 34，须排除 migrations.py docstring + check_file_sizes.py 注释 2 个 false-positive —— v0.9.0 消费面 CI 须显式排除这两个）。
- **载重机制独立复现（Codex 6 + 守护者 G1，全 CONFIRMED，0 REFUTED）**：① http_planner `pick_http_route` **每 query 无条件 reload**（`http_planner.py:130`，被 `query.py:290-292` SSE generator 内每 query 调）—— byte-equal 真命门（附带发现 `augment_schema_with_http_tables` :553 = 死代码，非第二 reload 热点）· ② 启动 chicken-and-egg 属实（`main.py:64` import 期 init_db）· ③ `run_in_executor(None,…)`（`bi_schedule.py:41-42`）**不 copy_context** 到 executor 线程 + 线程复用 → 比 to_thread 更需 try/finally reset · ④ JWT 无 tid（`deps.py:89` payload={sub,ver,exp}）+ users.id AUTOINCREMENT 从 1（`schema.sql:5`）→ 跨租户 user_id 碰撞属实 · ⑤ file-exists marker 太弱（中途 crash 误判完成）· ⑥ 软下限 `>=80`（`test_rename_smoke.py:152`）守不住精确 144（仓内已有 `test_admin_package.py:36 == 53` 精确先例 + `app_route_paths` 集合 helper）。
- **SSE ctx set-without-reset — 亲验机制成立 + 并发安全（守护者 G1 CONFIRMED，我给出更强修法）**：实测 20 并发交织 + 单 keep-alive 连接顺序请求均 **0 跨请求泄漏**；根因 = uvicorn 每请求 ASGI task per-request context 隔离（默认 copy_context）+ query 热路径 **0 context-fork**（无 create_task/to_thread/run_in_executor）。见 §0.3 修法升级。

### § 0.3 执行者对守护者 G1 的机制升级（比「镜像 request_id」更强）
守护者 G1 建议「tenant middleware 镜像 `set_request_id` 的 set-without-reset」。**执行者 grounded 复现后升级取材**：本仓已有更贴切、更强的现役先例 = **catalog ctx 三层模式**（`catalog.py:63-71` set-without-reset + `query.py:234` generator 内**重捕获** + `query.py:442`/`query_steps.py:262` 执行前 **`assert_catalog_context` runtime tripwire** → 漂移落 audit + raise）。理由：**tenant 是安全边界（≠ request_id 纯日志用途），须有 runtime tripwire 兜底**，裸 request_id 无此护栏。
→ **tenant ctx 落地 = 镜像 catalog 模式**：middleware 请求内 set-without-reset + SSE generator 内重捕获（若需）+ **执行前 `assert_tenant_context`（漂移落 audit + raise，镜像 `assert_catalog_context`）**。
→ **纵深防御（评估项）**：uvicorn 0.49.0 因 cpython #140947（asyncio 可能跨 task 泄漏 contextvar）新增 opt-in `reset_contextvars`（默认 False，本栈未复现泄漏）；tenant 为安全边界，v0.9 评估开 `reset_contextvars=True` + assert 兜底，不裸依赖默认 copy_context 的隐式保证。守护者「ASGI send 后 reset」= 过度工程，**不采**（真 SSE 测 + assert tripwire 已足）。

### § 0.4 守护者一致性核（血脉区）+ 执行者接纳
守护者（OOS-1v2 血脉上游）核实：C 方案 / C1 两审计 / A3 不计 OVERRIDE / **D1 满足**（C3 新 CI #1-3 = 运行期隔离断言，超出改名绊线 #5 → D1 满足；但 CI docstring 须显式区分绊线 vs 隔离 = must-fix #10）/ OOS-1→OOS-1v2 措辞 / P1 P2 排序 —— 全忠实继承仲裁，**无战略冲突**。执行者**全数接纳** Stage 3 major-revise + 10 must-fix + G1-G4；G1 按 §0.3 升级为 catalog-ctx 镜像。

---

## § 1 决议（背景 + 目标 + 范围 + 前置链）

**背景**：v0.8→v0.9 整体审核三方齐备 + 资深仲裁 LOCKED：v0.9 = 多租户，隔离模型 = **C 方案（平台库 + per-tenant SQLite 文件，fail-closed）**，决定性论据 = A 行级 tenant_id 对 LogicForm 编译器 fail-open。加 tenant 概念的第一个 commit 之前必须先走 OOS-1 红线修订仪式（否则 CI 直接红）。

**目标**：
1. 完成 OOS-1 红线修订仪式（announce 留痕 + CLAUDE.md 条目改写 + CHANGELOG 修订声明 + LOCKED 隔离设计 4 产物）。
2. 落多租户地基第一刀：tenants 表（平台库）+ tenant ContextVar + get_conn 双层库解析（fail-closed）+ 存量迁移（knot.db → platform.db + tenants/1/knot.db）。
3. **tenant#1 下全行为 byte-equal**：现单租户用户/数据/API 行为零变化（含启动序、catalog 全局内容、脱敏 alias、调度器行为）。

**前置链（三项，起手前必须全绿）**：
| # | 前置 | 责任方 | 状态 |
|---|---|---|---|
| P1 | **KNOT_SEMANTIC_LAYER 激活收官**（admin-UI 重 applied prompt DB fork + semantic_cases eval 门 + 开 flag + announce）— 资深 2026-07-17 拍板「先激活再起 v0.9.0」 | kk | ⬜ 待完成（v0.9.0 起手唯一真阻塞） |
| P2 | **v0.8.22 base.py→migrations.py 拆** — base.py 373→84 行，迁移块入 migrations.py（[`v0.8.22-migrations-split.md`](v0.8.22-migrations-split.md)） | 执行者 | ✅ **MERGED**（PR #242 · main @ a0434a8 · 1310 passed · 2 证伪员 SAFE_TO_MERGE） |
| P3 | 治理前置（override-log §8 A3 登记 + R-LP-v3-EX-3-1 立约）| 资深 | ✅ 已落（grounded 确认 2026-07-17）|

**范围（4 块）**：
1. 仪式治理产物（§2 四产物 + §3 红线修订文本 + 附录 A/B）。
2. 绊线翻转 + 同 commit 新隔离 CI（D1：schema.sql 15 处 + 测试断言 11 个 + 执行体 4 套 docstring）。
3. tenant 上下文机制（core/tenant_context.py + get_conn 双层 + tenants 表 + 平台库）。
4. 存量迁移（幂等 + 备份 + 校验 + 失败保旧库）+ 版本收尾。

**范围外（明示不做，留后续 PATCH）**：JWT tid claim / 鉴权拆分 / platform_admin vs tenant_admin / 租户开通接线（0.1）· 凭据 per-tenant / Fernet per-tenant key / egress 租户化（0.2）· catalog ContextVar fail-closed / reload 写竞态 / HTTP helpers / 调度器域化（0.3）· 计费/审计租户化（0.4）· 隔离 CI 套件补全 / 进程内缓存与限流桶键租户化（0.5）· uploads.db 租户化**实施**（设计本 PATCH 定局，实施随 0.1 — 资深 2026-07-17 拍板）· 前端任何业务改动。

---

## § 2 LOCKED 隔离设计（4 产物 — 仪式核心，Stage 3 终审后锁定）

### 产物 (i) — catalog_id→tenant 语义继承审计（仲裁 C1 拆分之一）

**结论：C 方案下整条 catalog_id 语义链零改动继承，grounded 成立；有一个启动时序例外须在 C2 闭合。**

- catalog_id 键链全集（schema grounded）：`metrics.catalog_id`（schema.sql:181）· `semantic_query_audit.catalog_id`（:207）· `semantic_monitors.catalog_id`（:342）· `monitor_trigger_audit.catalog_id`（:361）· `bi_report_schedules.catalog_id`（:467）· `users.active_catalog_id`（列本体 :28，OOS-1 注释 :26）· `audit_log.catalog_id`（:287）。全部与 `catalogs` 表同库互引 → **整库迁入 tenants/1/knot.db 后所有键关系零改动**。
- LogicForm 编译器零改：编译发生在所连库内；语义层永不穿线 tenant（守护者 v0.7 权威裁定，C 下成立）。
- `_reject_forbidden` ×3（metric/monitor/schedule repo）+ catalog_repo `_UPDATABLE` 白名单：**执行体全部保留**（租户库内仍严禁 tenant_id 列），仅 docstring 语义改写（§3.1）。
- **⚠️ byte-equal 双命门（Stage 3 守护者 G2/G3 + 执行者 grounded 复现订正）**：
  - **命门#1（次要 · boot 窗口）**：`catalog.py:156` 模块 import 期无条件 `reload()`，router import 链（`main.py:18`→query_steps→agents/catalog）**先于** init_db 与任何 tenant ctx set → fail-closed get_conn 下降级 file/empty 全局。**闭合**：C2 在 main.py init 序（set tenant#1 ctx 之后）显式补调 `catalog_loader.reload(strict=False)` 一次（覆盖 boot→首请求窗口的非请求消费者）——不触 catalog.py 本体。
  - **命门#2（主要 · 真命门 = SSE ctx）**：**grounded 复现推翻「boot 后无人再 reload」** —— `http_planner.pick_http_route`（`http_planner.py:130`）**每 query 无条件 reload**（被 `query.py:290-292` SSE generator 内每 query 调，读 catalog/settings/data_source repo）。故 serving 期 catalog 全局内容**每 query 在 request ctx 下刷新** → byte-equal 真正靠 **§2 产物 iv set点#1 的 SSE ctx（set-without-reset + assert）**：SSE generator 内 tenant ctx 有效 → 每 query reload + alias_map + save_message 全在 tenant#1 ctx 下执行 → byte-equal 成立；**SSE ctx 挂则 byte-equal 破 = #4 同时是正确性 + byte-equal 双 must-fix**。init 补调（命门#1）只保 boot 窗口，非主保障。（附带：`augment_schema_with_http_tables` `http_planner.py:553` = 死代码无调用方，非第二 reload 热点。）
- **明示不继承（v0.9.3 工作面，本 PATCH 0 触碰）**：catalog.py 全局回退（`current_catalog()` 未设 ctx 返模块全局、catalog_id=None，catalog.py:42-60）· 5 个 HTTP helpers 直读全局 TABLES（catalog.py:172-246）· `reload()` 写竞态（:74-152）· 调度器 `run_due()` 不 set catalog ctx（bi_schedule_service.py:80-106）· `bi_schedule.py:69` catalog_id=1 硬编。**运行期**（init 补调 reload 之后）单租户下以上均安全（回退 = tenant#1 自身数据）；多租户暴露由 **R-T-GATE（§3.2）** 挡住直到 0.3 关闭。

### 产物 (ii) — 隔离栈就绪度审计（仲裁 C1 拆分之二 · 6 项逐项：现状 → 目标 → 落位）

| # | 层 | 现状（grounded @a0434a8） | 多租户目标 | 落位 |
|---|---|---|---|---|
| 1 | **Fernet 加密** | 单一全局 KNOT_MASTER_KEY，无轮换无 per-tenant（core/crypto/fernet.py）；加密面 = users 表 doris 凭据 + app_settings 4 敏感键（settings_repo.py:18-21） | per-tenant 数据密钥（候选：HKDF(master, tenant_id) 派生子钥 / per-tenant key 加密存平台库），换钥迁移工具 | **v0.9.2**（凭据分家同 PATCH）。v0.9.0 不动：tenant#1 密文随库迁移，同 key 解密 byte-equal（C4 含解密烟测） |
| 2 | **审计** | audit_log 无 tenant 列（schema.sql:273-289） | C 下 audit_log 随库迁 = **天然租户域（本 PATCH 即解）**；平台侧动作（租户开通/停用）审计落平台库新表 platform_audit + 平台聚合只读视图 | 租户域 **v0.9.0 天然完成**；platform_audit + 聚合视图 **v0.9.4** |
| 3 | **egress / 分享目标** | KNOT_WEBHOOK_ALLOWED_HOSTS / KNOT_HTTP_ALLOWED_HOSTS 纯 env 全局（webhook.py:25-26）+ im_egress.py:13-16 硬编 3 域名；bi_share_targets 表全局 | bi_share_targets 随库迁 = **本 PATCH 即租户域**；env allowlist → per-tenant 载体（租户库 app_settings 键，fail-closed 未配=全拒） | 表侧 **v0.9.0 天然完成**；allowlist 载体 **v0.9.2** |
| 4 | **数据文件** | 单 knot.db（32 表）+ uploads.db（19a 隔离库，SQLITE_DB_PATH.parent 派生，engine_cache.py:18） | platform.db + tenants/<id>/knot.db + tenants/<id>/uploads.db | knot.db **v0.9.0 本 PATCH**；uploads.db 设计本产物定局（目录布局 tenants/<id>/uploads.db、engine 解析走 tenant ctx）、**实施 v0.9.1**（第二租户可开通前必须就绪 — R-T-GATE 联动） |
| 5 | **调度器** | 单全局 KNOT_SCHEDULER_TOKEN（bi_schedule.py:34）+ run_due 无差别迭代 + 不 set 任何 ctx（bi_schedule_service.py:80-106） | token 保持平台级（tick 是平台基础设施）；run_due 按租户迭代 + per-tenant ctx set + 公平性预算 | **v0.9.3**（调度器域化同 commit）。v0.9.0 过渡：run_due 入口 set tenant#1 ctx |
| 6 | **进程内缓存/单例/限流桶** | 缓存键全部无租户维度：engine_cache per-user 引擎键 = user_id · totp_service TTLCache token_version 键 = user_id · `_DS_STATS_CACHE` 单槽 · budgets global fallback · **_rate_limit.py query 桶 per-user_id（对抗自核补）**。**C 下各租户库 users.id 均从 1 起 → user_id 跨租户碰撞**，任何 user_id 键缓存/限流桶在多租户下即跨租户串数据/串量 | 全部键升维 (tenant_id, user_id)；JWT 必须带 tid（否则 sub=user_id 无法定位用户 — 0.1 鉴权拆分的硬依赖，非可选） | **v0.9.5**（缓存/限流键统一租户化）；JWT tid **v0.9.1**。v0.9.0 单租户下无碰撞面，R-T-GATE 挡多租户暴露 |

### 产物 (iii) — 平台库 vs 租户库表划分 + 迁移映射

**v0.9.0 原则：byte-equal 最大化 = 现有 32 张表整体迁入 tenants/1/knot.db，平台库最小化起步。**

- **platform.db（新建，v0.9.0 仅 1 张表）**：
  - `tenants`：`id INTEGER PK / slug TEXT UNIQUE / name TEXT / status TEXT('active'|'suspended') / db_dir TEXT / created_at`。存量迁移 seed 恰 1 行（id=1, slug='default', db_dir='tenants/1', status='active'）。`db_dir` 相对 `SQLITE_DB_PATH.parent` 解析（此设计同时服务测试基建——见产物 iv set 点 #5）。
  - schema 载体：新 `knot/repositories/platform_schema.sql`（含未来平台表预留注释：user_directory[0.1] / platform_admins[0.1] / platform_audit[0.4]，**只留注释不建表**）。
- **tenants/1/knot.db**：现 32 张表全量（users / totp_recovery_codes / conversations / messages / semantic_layer / data_sources / model_settings / file_uploads / knowledge_docs / doc_chunks / user_sources / app_settings / catalogs / metrics / semantic_query_audit / few_shots / prompt_templates / budgets / saved_reports / audit_log / model_catalog_live / frontend_errors / message_feedback / semantic_monitors / monitor_trigger_audit / report_folders / bi_reports / bi_report_tiles / bi_permissions / bi_share_targets / bi_report_schedules / bi_report_schedule_fires）。
- **前瞻迁移指针（不在 v0.9.0 做，逐条登记防散失）**：users→是否上收平台 vs 平台 user_directory 目录（**0.1 鉴权拆分时定**，本手册倾向「users 留租户库 + 平台 user_directory 路由」以保租户 admin 自治）· app_settings 平台/租户键分家（0.2）· model_catalog_live（OR catalog 同步缓存）评估上收平台（0.2）· 平台级 budget 熔断层（0.4）。
- **uploads.db 设计定局（实施 0.1）**：目标路径 `tenants/<id>/uploads.db`；`engine_cache._UPLOADS_DB` 由常量改为 tenant ctx 解析函数；file_uploads 元数据表已随租户库走。v0.9.0 期间 uploads.db 留原全局位置 = 已知 interim（单租户无跨租户面）。

### 产物 (iv) — tenant 上下文解析设计（fail-closed）

- **新模块 `knot/core/tenant_context.py`**（core 横切层 — api/services/repositories 均可依赖，4 层依赖图与 import-linter 9 contracts 零新增方向）：
  - `_active_tenant_ctx: ContextVar[dict | None] = ContextVar(..., default=None)`
  - `set_active_tenant(tenant_row) -> Token` / `reset_active_tenant(token)`
  - `current_tenant() -> dict`：**未设 → raise TenantContextError。无任何全局回退**（与 catalog.py 回退语义刻意相反 — 吸取守护者「带全局回退的租户 ContextVar = 虚假隔离」裁定）。
- **get_conn 双层解析（base.py）**：
  - `get_platform_conn()`（新，显式平台库连接，**ctx-free**——不经 fail-closed 门，供启动序 ❷ platform bootstrap + tenant 解析 + 迁移）→ `SQLITE_DB_PATH.parent / "platform.db"`。
  - `get_conn()`（签名不变，现定义 `base.py:25`）→ `SQLITE_DB_PATH.parent / current_tenant()["db_dir"] / "knot.db"`；无 ctx → **raise**（fail-closed）。**32 个消费文件 0 改动**（26 repo + main + admin/stats + engine_cache + totp_service + 2 scripts；base.py 定义处本 PATCH 改，不在 0 改动集）。⚠️ **消费面 CI 须显式排除 2 个 false-positive**（`migrations.py` docstring L3/L8 + `scripts/check_file_sizes.py` 注释 L32 —— naive `grep -rl get_conn` 得 34，真实 32；v0.8.22 后新增的漂移点）。
  - `SQLITE_DB_PATH` env 语义降级为「数据目录锚点」（parent 派生 platform.db 与 tenants/），**不引新 env**；DEPLOY.md 注明。
- **ctx set 点穷举（fail-closed 的对偶义务 — 对抗自核后扩为 8 行；漏一处 = 该路径 boot 崩 / 500 / 静默失效）**：
  | # | 路径 | v0.9.0 做法 |
  |---|---|---|
  | 1 | HTTP 请求（含 SSE） | **镜像 catalog ctx 三层模式（Stage 3 G1 → 执行者 §0.3 升级，非裸 request_id）**：middleware 于 call_next 前 **set-without-reset**（下游 endpoint + SSE generator 经 uvicorn per-request context 隔离继承——**grounded 实证 20 并发+keep-alive 0 泄漏**；query 热路径 0 context-fork）+ 需要处 generator 内**重捕获**（镜像 `query.py:234`）+ **执行前 `assert_tenant_context`**（漂移落 audit + raise，镜像 `assert_catalog_context` `query.py:442`/`query_steps.py:262`）。**tenant 是安全边界 → 必须有 runtime tripwire**（裸 request_id 无此护栏）。**禁 dependency/endpoint 内 set**（run_in_executor 场景丢 ctx）。**纵深防御评估**：uvicorn 0.49.0 opt-in `reset_contextvars=True`（cpython #140947）。**单租户解析器**：platform.db tenants 表恰 1 active（0 或 >1 → raise；多租户解析 = 0.1 JWT tid）。**验收含真 query-stream 测**（generator 内多次 get_conn() 成功 + assert 不误触）。 |
  | 2 | 启动 init 序（main.py，**Stage 3 #3 重排解 chicken-and-egg**） | ⚠️ **先蛋后鸡**：❶ 建 data dir → ❷ **建 platform.db + executescript(platform schema) + seed tenant#1 row**（走 **`get_platform_conn()` ctx-free 独立连接**，不经 fail-closed get_conn，否则自身撞门）→ ❸ 从 platform.db 读 tenants 列表 → ❹ **`for t in tenants:` set ctx=t（token）→ 该租户库 init_db（run_pre/post_schema + seed admin + _migrate_uploads + run_startup_cleanup，复用 migrations.py）→ finally reset** → ❺ set tenant#1 ctx（token）→ `_seed_prompts()`（`main.py:70`）+ `_apply_totp_rollout()`（`main.py:132`，**漏 set = KNOT_TOTP_REQUIRED 默认 true 下 boot 崩**）+ **补调 `catalog_loader.reload(strict=False)`**（闭合命门#1）→ finally reset（防 ctx 泄进 serving 使 fail-closed 虚化）。**seed admin 是 per-tenant 库操作，必须在 ❹ 的 tenant ctx 内，绝不在 platform 阶段。** |
  | 3 | 启动期 auto-purge task（`main.py:210` fire-and-forget） | `_maybe_purge` 内 set tenant#1 ctx + **finally reset**；purge 备份路径推导改走 tenant 解析（现 `purge_audit_log.py:61` 直读 `_base_mod.SQLITE_DB_PATH`，迁移后该锚点文件不存在 → copy2 FileNotFoundError 静默死）。 |
  | 4 | 调度器 tick `run_due`（Stage 3 #4） | **set 必须在 run_due 入口（`bi_schedule_service.py:80`）+ `try/finally reset`**——tick 端点用 `loop.run_in_executor(None,…)`（`bi_schedule.py:41-42`），executor 默认线程池**复用线程且不 copy_context**（比 to_thread 更需 reset）→ 漏 reset 则 ① 同次 run_due for 循环内上一 tenant ctx 泄漏下一迭代 ② 跨 tick 线程回池残留。0.3 改为按 schedule 归属租户迭代（逐租户独立 ctx 边界天然隔离）。**验收含线程复用测**（同线程先 set 后清 → get_conn raise）。 |
  | 5 | scripts（purge_audit_log / migrate_encrypt_v045 / reset_admin_password） | 加 `--tenant` 参数默认 tenant#1；**db_path/备份路径默认值改经 tenant 解析派生**（migrate_encrypt_v045.py:61/69 直读 sqlite3.connect 同须改）；purge() 函数体内做租户解析使 in-process 调用方（main.py:202-205）同样正确 |
  | 6 | tests | **3 个 conftest**（tests/conftest.py:125 + tests/repositories/conftest.py:19 + tests/integration/conftest.py:20）autouse fixture set tenant#1 + **per-test mkdtemp 目录布局**（anchor=dir/knot.db、dir/platform.db、租户库同 dir——mkstemp 单文件语义与双层库冲突，platform.db 落共享 TMPDIR = 跨测试污染）。**直读 base.SQLITE_DB_PATH 的 8+ 处既有断言**（test_totp_2fa / test_upload_isolation / test_catalog_switch_route 等）经「测试 tenant#1 `db_dir='.'`」设计保持指向真实租户库（db_dir 相对 anchor.parent 解析 → `.` = anchor 本身），零改动；隔离专项测试自建 db_dir='tenants/N' 布局。fail-closed 专项测试显式清 ctx |
- **JWT tid claim 前瞻（0.1 落，此处仅锁设计）**：payload 加 `tid`；登录经平台 user_directory 解析租户；get_current_user 校验 tid 与请求 ctx 一致；产物 (ii)#6 的 user_id 跨租户碰撞使 tid **成为硬依赖而非可选**。

---

## § 3 红线（修订 + 新立 + 保留）

### 3.1 OOS-1 → OOS-1v2 修订（仪式主体）

- **旧文本**（撤回）：「单租户死线：严禁 tenant_id/project_id 列，多租户推 v1.x+」。
- **新文本（OOS-1v2）**：「**多租户隔离靠 per-tenant 文件边界（C 方案）。租户库内仍严禁 tenant_id/project_id 列**（行级租户列 = fail-open，对 LogicForm 编译器不可守护）；租户归属列仅允许存在于平台库（tenants 等平台元数据表）。catalog_id 仍 = 租户内水平切分，≠ 租户隔离。」
- 落点：schema.sql 15 处注释改写（10 张表：26/155/175/181/203/207/338/342/361/389/430/448/455/461/467）+ 源码注释 ~17 行/12 文件 + CLAUDE.md:466 不变量条目 + 附录 A 立约文本置入 CLAUDE.md（资深置入）。
- 11 个测试断言（tests/repositories/test_metric_repo.py ×3 / test_catalog_repo.py ×3 / test_monitor_repo.py ×3 / test_semantic_audit_repo.py ×1 / tests/api/test_metrics_admin.py ×1）：**结构断言全保留**（租户库内无 tenant 列在 OOS-1v2 下仍然成立），docstring/命名改 OOS-1v2 语义；semantic_audit 的 exact-9-列断言不变。
- `_reject_forbidden` ×3 + catalog_repo `_UPDATABLE` 白名单：执行体保留，报错文案「OOS-1 死线」→「OOS-1v2（租户库内禁列）」。

### 3.2 新立红线 R-T-GATE（租户开通解锁门 — 本手册提案，Stage 3 终审裁定）

「**第二租户开通能力接线，直到下列隔离栈就绪项全绿才允许放开**。0.1 只建开通 API 骨架 + platform_admin，接线处被硬 CI gate。」

**gated 就绪项全清单（Stage 3 #5 正文列全）**：
1. **JWT tid + login 租户解析**（0.1）—— `deps.py:89` payload 加 tid；login 经平台 user_directory 解析租户；get_current_user 校验 tid==ctx。**无 tid 则 sub=user_id 跨租户碰撞（各库 id 从 1 起）无法定位库**。
2. **uploads.db 租户化**（0.1）—— `engine_cache._UPLOADS_DB` 由 tenant ctx 解析（现全局 interim = 跨租户上传洞）。
3. **凭据 per-tenant + egress 租户化**（0.2）—— Fernet per-tenant key / app_settings 4 敏感键分家 / webhook allowlist 载体。
4. **catalog ContextVar fail-closed + 调度器域化**（0.3）—— catalog 全局回退消失 + run_due 逐租户 ctx。
5. **进程内 cache/限流桶键升维 (tenant_id, user_id)**（0.5）—— engine_cache / totp TTLCache / `_rate_limit` 桶 / budgets fallback（user_id 碰撞面）。
6. **per-tenant admin bootstrap + per-tenant 开通口令**（0.1）—— 新租户 fresh init 走 seed default admin，`KNOT_INITIAL_ADMIN_PASSWORD` 单一全局 env → **多租户同口令扩散**（正是 R-LP-v3-EX-3-1 grounded 点名形态）→ 开通接线前须改 per-tenant 开通口令/一次性邀请流（血脉守护）。

**硬 CI（Stage 3 #5）**：`assert_no_second_active_tenant_served` —— 平台库 tenants 表**恰 1 行 active**（0.9.0~0.2 期间）；>1 active 即红。配 R-T-GATE⑥（§4 C3）：路由集合不含 `/api/platform/tenants` 开通端点（0.1 骨架落地后升级为接线处 gate 断言）。

理由：仲裁序把 catalog fail-closed 放 0.3，而守护者裁定「带全局回退的租户 ContextVar = 虚假隔离」——两者在弧层面靠本门调和：**catalog 全局回退存活期间（→0.3），系统内永远只有一个可服务租户**。

### 3.3 保留红线 / 不变量逐条声明 / byte-equal 清单

- **tenant#1 byte-equal**：API 契约/路由集合（flatten=144）/错误文案/SSE 行为/前端 `frontend/src/**` 0 改（version.js 除外）。18 屏 byte-equal 平凡成立。启动序 byte-equal 含 catalog 全局内容（产物 i 补调 reload 闭合）。
- **不变量带入清单逐条声明**（对抗自核订正——撤回原「全部 0 触碰」过度宣称）：
  | 不变量 | 本 PATCH 状态 | 守护点 |
  |---|---|---|
  | 加密（Fernet/KNOT_MASTER_KEY/enc_v1） | **实现 0 改；载体变更**（密文所在库文件挪位 + get_conn 解析基座换） | C4 迁移后抽 1 条加密凭据解密烟测 + ctx set 点穷举 CI |
  | 审计（Literal + emit + CI 断言） | **实现 0 改；载体变更**（audit_log 随库迁 + audit_repo 走新 get_conn） | C4 audit_log 行数守恒对账 + 既有 audit CI 断言绿 |
  | R-2FA（token_version 吊销 + 强制 enroll） | **实现 0 改；载体变更**（totp_recovery_codes/users 随库迁 + totp_service TTLCache 不动） | C4 totp 表行数守恒 + test_totp_2fa 全绿（经产物 iv#6 db_dir='.' 设计零改） |
  | **R-LP-v3-EX-3-1（default-admin）** | **专项声明**：seed 逻辑 0 改，但结构性进入 per-tenant init 循环（tenant#1 users 非空 → 幂等不重 seed）；新租户 seed 的全局 env 同口令扩散风险登记进 R-T-GATE 就绪审计（§3.2），0.1 接线前必须解决 | seed 测试保留 + R-T-GATE 条文 |
  | gate 鉴权 / 成本 / async / 脱敏链 / crypto-only contract / 视觉铁律 / doc-invariant CI | 0 触碰（不改任何相关代码；脱敏 alias_map 行为经产物 i reload 补调保持） | 既有 CI |
- 不碰：`services/agents/catalog.py` 本体 · `KNOT_SEMANTIC_LAYER` 两读取点 · require_admin 90 处 · engine_cache/uploads.db · Shared.jsx/任何屏。
- import-linter 9 contracts KEPT（core/tenant_context.py 零新增方向）· check_file_sizes 绿（base.py 已由 v0.8.22 释放余量；新文件 tenant_context.py / tenant_repo.py 目标 <300 免 ACK）。
- 迁移红线：幂等可重入 + 迁移前 checkpoint(TRUNCATE) + 备份 `knot.db.pre-tenancy.bak` + **扩展校验（见 §4 C4）** + 失败回滚保旧库（沿 19a `_migrate_uploads_to_isolated_db_once` 模板）；**严禁删除备份**。

---

## § 4 文件改动（per-commit grounded）

| Commit | 文件 | 内容 |
|---|---|---|
| C1 治理 | CLAUDE.md · CHANGELOG.md · 本手册 · override-log（若 Stage 3 要求补充） | OOS-1v2 立约文本置入（附录 A，资深置入）+ CHANGELOG 修订声明（附录 B）+ 本手册 §2 四产物锁定为 LOCKED + announce 留痕（commit message 引用 override-log §8 既成 announce）|
| C2 机制 | `knot/core/tenant_context.py`（新：ctx + `assert_tenant_context` 镜像 assert_catalog_context）· `knot/repositories/platform_schema.sql`（新）· `knot/repositories/tenant_repo.py`（新：`get_platform_conn` ctx-free + tenants CRUD + 单租户解析器）· `knot/repositories/base.py`（get_conn 双层，改 `:25` 定义处）· `knot/main.py`（**middleware set-without-reset**[产物iv#1] + **启动序重排 ❶-❺**[Stage3 #3]：platform bootstrap→迭代 tenants ctx→tenant#1 ctx seed/totp/reload补调，均 finally reset）· `knot/services/bi_schedule_service.py`（run_due 入口 set + **try/finally reset**[Stage3 #4]）· `knot/scripts/purge_audit_log.py` + `migrate_encrypt_v045.py` + `reset_admin_password.py`（--tenant + 路径派生改租户解析）· **3 个 conftest**（tests/ + tests/repositories/ + tests/integration/：autouse fixture + mkdtemp 双层布局，tenant#1 db_dir='.'） | tenant ctx（catalog-mirror + assert）+ fail-closed get_conn + get_platform_conn + 平台库。全套件在 fixture 下绿；直读 base.SQLITE_DB_PATH 的既有断言经 db_dir='.' 零改 |
| C3 翻转+新CI | `knot/repositories/schema.sql`（15 处注释）· 5 个测试文件（11 断言 docstring）· 3 repo（_reject_forbidden 文案）· 12 文件 ~17 行源码注释 · `tests/test_tenant_isolation.py`（新） | 绊线翻转与新 CI **同一 commit**（仲裁 D1）。**CI docstring 显式区分两类（Stage3 #10）：绊线（列缺席）= ⑤；隔离（fail-closed + 文件边界）= ①-③**。新 CI 八断言：①get_conn 无 ctx raise ②set ctx 后 get_conn 解析到 db_dir 目标文件 ③双租户库文件级隔离——**tenant#2 以 status='suspended' 建、仅 repo 层 set ctx 验证**（active 双行触发单租户解析器 raise）④platform.db 表集合 == {tenants}（防业务表漂回）⑤租户库 schema 仍无 tenant 列（OOS-1v2 改写自原 tripwire = 绊线）⑥R-T-GATE `assert_no_second_active_tenant_served`（恰 1 active）+ 路由集合不含 `/api/platform/tenants`⑦租户库文件不被 git 跟踪（check-ignore，配 C4 .gitignore）⑧**flatten 路由集合 snapshot（Stage3 #9：复用 `tests/_route_count.py:39 app_route_paths`，精确 diff 而非 `>=80` 软下限，报错直指多/少哪条）** |
| C4 迁移 | `knot/repositories/tenant_repo.py` 或独立迁移模块 · **`.gitignore`**（`**/data/tenants/` —— 现 `**/data/*.db` 不匹配子目录，租户库会裸奔进 git）· `DEPLOY.md`（迁移段 + SQLITE_DB_PATH 语义注 + **grep 全部 `data/knot.db` 字面逐处改**：:304 备份 cron→platform.db + tenants/ 整目录、:389、:401、**:502**[复现补：手册前列漏此第 4 处] + uploads.db 纳入备份） | **WAL/-shm 时序（Stage3 #7+G4）**：迁移前用**独立连接** `PRAGMA wal_checkpoint(TRUNCATE)` + **close**（在任何 tenant init_db / import 期 reload 触发连接之前）再 rename `knot.db`(+ -wal/-shm) → tenants/1/ + **uploads.db 一并迁**（现 interim 全局）→ backup `knot.db.pre-tenancy.bak` → `get_platform_conn` 建 platform.db + seed tenants#1 → 失败回滚保旧库。**强 complete-marker（Stage3 #6，非 file-exists）**：schema version + 32 表清单 + 行数 hash（含 audit_log/users/totp_recovery_codes 显式点名）+ 抽 1 条加密凭据解密烟测——全绿才标 complete（复用 `_migrate_uploads_to_isolated_db_once` 的「实际状态判据 + per-unit 校验 + last-good」模板 `migrations.py:236`，抗中途 crash 续跑） |
| C5 收尾 | main.py / version.js / README / test_rename_smoke · CHANGELOG · `knot/static/`（rebuild） | 4 源点 0.9.0 + CHANGELOG 恰 1 [Unreleased] + static rebuild + merge 后 tag `v0.9.0` |

---

## § 5 验收标准

- **byte-equal**：full pytest 绿（既有断言 0 语义变化，仅 OOS-1v2 措辞）· 前端 0 改（version.js 除外）· 迁移后 tenant#1 登录/问数/BI/admin 全链路人工冒烟与 v0.8.22 行为一致 · catalog 全局内容（TABLES/LEXICON/_SOURCE=db）与迁移前一致。
- **⭐ SSE ctx（Stage3 #2 · Top-1 兼 byte-equal 命门）真测**：query-stream generator 内多次 `get_conn()` 成功解析到 tenant#1 库（覆盖 http_planner 每 query reload `http_planner.py:130`）+ `assert_tenant_context` 不误触；20 并发交织 0 跨请求泄漏（复用 §0.2 实测脚手架）。
- **调度器（Stage3 #4）线程复用测**：同 executor 线程 run_due set 后 finally reset → 下次复用该线程的任务 get_conn 无残留 tenant（清 ctx → raise）。
- **production-layout 测（Stage3 #8）**：anchor=/tmp/data/knot.db + db_dir='tenants/1' → get_conn 指 /tmp/data/tenants/1/knot.db（补 db_dir='.' 之外，防路径拼接只在 '.' 下成立的假绿）。
- **boot 冒烟双场景**：fresh 部署（无库）→ 启动序 ❶-❺（platform bootstrap→tenant#1 init）+ prompts seed 成功 + `KNOT_TOTP_REQUIRED=true` 下 boot 不崩；存量部署迁移后同上 + auto-purge 备份路径正确。
- **fail-closed**：清 ctx 后 get_conn() raise（CI ①）· tenants 表 0 或 >1 active → 请求路径 raise 非静默回退（`assert_no_second_active_tenant_served`）· serving context 无残留 tenant ctx（各 token reset 后专项断言）。
- **迁移（Stage3 #6/#7）**：幂等重入 · 中途 crash 后重跑能续（强 marker 非 file-exists）· 失败旧库无损 · `.bak` 存在 · uploads.db 一并迁 · WAL/-shm 迁移后无残留 · 32 表行数对账 + 密文解密烟测通过。
- **新 CI 八断言全绿（含 flatten snapshot #9 + docstring 区分绊线/隔离 #10）+ 改写后 11 断言绿 + _reject_forbidden 行为测试绿**。
- **闸门**：ruff check knot/ + lint-imports 9 KEPT + check_file_sizes + full pytest + frontend lint/build（均 `.venv/bin/` 调用）· clean worktree 复现构建（v0.8.15 教训）· 涉 run_sql_agent 测传显式 openrouter_api_key（v0.8.20 教训）· **get_conn 消费面 CI 排除 migrations.py/check_file_sizes.py 2 false-positive**。
- **治理**：CLAUDE.md OOS-1v2 置入 · CHANGELOG 修订声明 · doc-invariant CI 绿。

## § 6 commit 序列

C1 治理（docs only，先行独立）→ C2 机制（tenant ctx + 双层 get_conn + 平台库 + 启动序 + fixture，全套件绿）→ C3 绊线翻转 + 新 CI（同 commit，D1）→ C4 存量迁移 → C5 版本收尾。
> C2 先于 C3：机制不触绊线（tenants 表在平台库、无 tenant_id 列入租户表——协议核已逐一比对 11 断言确认 C2 不触任何一条红），C3 的新 CI 需要 C2 机制存在；main 走 squash merge，C1-C5 在 main 上原子落地。若 Stage 2/3 认为 C2+C3 应合并为单 commit，执行者接受。

## § 7 协议合规（v3 + §VIII 程序衔接）

- 进度：Stage 1 草案（执行者，3 证伪员自核）✅ → **Stage 2 Codex 初审（13 redline）✅** → **Stage 3 守护者[v0.8]终审 major-revise（§0）✅** → 执行者整合（本版，§0）+ Stage 3 前 grounded 复现 ✅ → **回守护者 Stage 3 复核放行 → 执行**（守护者明确：修完 §IV 10 must-fix 回复核，非直接实施）。
- **§VIII 撤回程序衔接**（v0.6.2.0-phase-b-roadmap-stage3-guardian-v05.md:342-344 三要件，对抗自核后重锚定）：
  - ① explicit announce：**既成证据 = override-cumulative-log.md §8（2026-07-16 资深签字，明示「翻转 OOS-1 死线走正式红线修订仪式」）+ opinions 资深仲裁 LOCKED 段**；v0.9 启动指令与 C1 commit 为补强留痕。✅
  - ② CHANGELOG 撤回/修订声明 = 附录 B（CI 载体 test_doc_invariants）。✅（C1 落地）
  - ③ 召集远古守护者 = 整体审核含 v0.6 远古守护者独立意见（opinions L259-275，其 C5② 正是 OOS-1 tripwire 议题）+ 本 PATCH 走完整 v3。✅
  - **v0.5 存活代说明**：本次整体审核收官名单 = 执行者 v0.8 + 守护者 v0.7 + 远古守护者 v0.6（v0.5 及更早代对话已不在——与第三次整体审核「v0.3 已不在」同型）；「所有存活的远古守护者」要件完备性交 Stage 3 守护者复核确认。
- OVERRIDE 计数：A3 已登记（override-log §8），本仪式不计入维度 A，累计 sustained 3。

## § 8 自检（抗诱惑清单）

- 仅碰 §4 锚点文件；不顺手改 catalog.py 本体 / require_admin 面 / engine_cache / 任何屏 / 语义层。
- 不引新 npm/pip 依赖；不引新 env（SQLITE_DB_PATH parent 派生）。
- 不顺手实施 uploads 租户化 / JWT tid / 开通 API（全部 0.1+）。
- 迁移脚本沿 19a 模板（幂等 + .bak + 校验 + 失败保旧）。
- 落码后 clean worktree 复现构建；版本收尾 4 源点 + CHANGELOG 恰 1 [Unreleased] + static rebuild。

### § 9.1 执行者对抗自核（交 Stage 2 前 · 2026-07-17）
3 只独立证伪员（锚点核对 / fail-closed 设计漏洞 / 协议合规）对草案初版出 **6 REFUTED / 9 PARTIAL / 若干 CONFIRMED**，已全部整合。要点：set 点穷举初版漏 3 个启动期消费者（含 `_apply_totp_rollout` boot 崩级）· catalog import 期 reload 时序破 byte-equal · .gitignore 不盖 tenants/ · conftest 实为 3 处 + mkstemp 布局冲突 · §VIII 要件①锚点重定 · 不变量「0 触碰」过度宣称改逐条声明 · get_conn 消费文件 33→32。

### § 9.2 Stage 3 整合 + 执行者 grounded 复现（2026-07-18）
Stage 2 Codex（13 redline）→ Stage 3 守护者 major-revise（10 blocking must-fix + G1-G4）。执行者落地前**独立 grounded 复现**（无单点可信，3 agent 重锚 a0434a8 + 载重机制）：**Codex 6 + 守护者 G1 全 CONFIRMED，0 REFUTED**。3 处增益：① **SSE ctx 修法升级**——由「镜像 request_id」→「镜像 catalog ctx（set + 重捕获 + `assert_tenant_context` runtime tripwire）」，因 tenant 是安全边界须 tripwire（§0.3）· ② http_planner `pick_http_route:130` 每 query reload = byte-equal 真命门（守护者 G2，附带发现 `augment_schema_with_http_tables:553` 死代码）· ③ flatten 用 `app_route_paths` 集合 snapshot（仓内已有 `==53` 精确先例）。10 must-fix 逐条整合见 §0.1。**下一步 = 回守护者 Stage 3 复核放行。**

---

## § 附录 A — OOS-1v2 立约文本（CLAUDE.md 置入稿，资深置入）

> **OOS-1v2（v0.9.0 红线修订仪式改立 · 原 OOS-1 单租户死线正式翻转）**：多租户隔离模型 = **C 方案（平台库 + per-tenant SQLite 文件，fail-closed）**。**租户库内严禁 tenant_id/project_id 列**——行级租户列对 LogicForm 编译器 fail-open（漏注一条 = 静默跨租户供数），文件边界是唯一隔离载体；租户归属列仅允许存在于平台库。catalog_id 仍 = 租户内水平切分 ≠ 租户隔离。tenant 上下文 fail-closed（无 ctx → raise，严禁全局回退）。配套 R-T-GATE：隔离栈就绪（uploads/凭据/egress/catalog/调度器/缓存与限流键/开通口令）前严禁放开第二租户开通。修订程序：v0.8→v0.9 整体审核三方 + 资深仲裁（A3/C1/D1）+ 本仪式 Stage 1-3 + CHANGELOG 修订声明。

## § 附录 B — CHANGELOG 修订声明稿（置入 v0.9.0 条目）

> **红线修订声明**：本版本经正式红线修订仪式翻转 OOS-1（v0.6.2.0 立，单租户死线）为 OOS-1v2（多租户 C 方案，租户库内禁列语义保留）。程序：整体审核三方独立意见（执行者 v0.8 + 守护者 v0.7 + 远古守护者 v0.6）+ 资深仲裁 LOCKED + v3 三阶段评审 + 绊线翻转与新隔离 CI 同 commit（D1）。不计入 OVERRIDE 维度 A 累计（override-cumulative-log §8 A3 定性）。
