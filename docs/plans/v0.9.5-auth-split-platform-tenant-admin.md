# v0.9.5 — 鉴权拆分 platform / tenant admin（Stage 1 草案）

> **性质**：多租户隔离栈第五刀，切**权限层**。前四刀切状态（ctx/缓存/uploads/catalog）+ 入口（JWT tid）；
> 本刀回答「**admin 是谁家的 admin**」。
> **定性：R-T-GATE 当前硬锁第二租户 ⇒ 今日线上无 live 泄漏**，本片是 lift 前置账 —— 但含**一条真实修复**（§0 F5）。
> **协议**：Loop Protocol v3 完整三阶段（本片动 90 处鉴权站点 + 新增端点，**不适用简化协议**）。

---

## §0 grounded 测绘（15 组事实 · 全部 `file:line` 或实测命令）

> 方法：执行者直读 + 两口径交叉（文本 grep + AST）。**未跑 workflow** —— 该路径本会话连续 **4 次**零可回收
> （详 chore 手册 §9），资深流程结论「别再把它当可依赖的一层」。

### F1 · `role` 在租户库，平台库只有 `tenants` ⇒ **platform admin 今天没有身份存储**
- `knot/repositories/schema.sql:9` — `role TEXT DEFAULT 'analyst'`，**无 DB CHECK**（加角色是「无 schema 改动」）。
- 校验面唯一：`knot/api/admin/users.py:37` `_VALID_ROLES = {"admin","analyst"}`（`:42` `:61` 两处用）。
- `knot/repositories/platform_schema.sql` 全表 = **仅 `tenants`**；`platform_admins` / `user_directory` /
  `platform_audit` 都只是**注释预留**（分别标 [0.1]/[0.1]/[0.4]）。

### F2 · ⭐ `get_current_user` **结构性拒绝无租户身份** —— 这条推翻「加个角色判断就能拆」
1. `knot/api/deps.py:101` — `tid = current_tenant()["id"]`，fail-closed ⇒ **签不出无 tid 的 token**；
2. `deps.py:159-161` — 无 tid / 非正 int → 401 `JWT_NO_TID`；`:163-166` `assert_tenant_context(tid)`
   必须与中间件设的 ctx 相符，否则 401 `TENANT_UNAVAILABLE`；
3. 其后 `get_user_by_id(user_id)` 读**租户库**（`base.get_conn`，fail-closed）。

⇒ **platform admin 必须走平行认证路径**。若硬塞进现有链路，每个平台请求都得先「假装在某个租户里」
—— 正是 OOS-1v2 禁止的 fail-open 形状。

### F3 · 作业面精确数：**90 个 `Depends(require_admin)` 站点 / 22 文件**
实测 `grep -rc "Depends(require_admin)" knot/api/**/*.py` → 总 90、文件 22（源码级与路由级**恰好都是 90**
⇒ 每条被守护路由恰一个站点、无多 method admin 路由）。
分布（前六）：`catalog.py` 7 · `bi_reports.py` 7 · `admin/stats.py` 6 · `admin/monitors.py` 6 ·
`admin/metrics.py` 6 · `admin/datasources.py` 6 · `admin/budgets.py` 6。
> ⚠️ **纠正对 chore 注释的可能误读**：那句「横跨 5 个模块」说的是 **`/api/admin/` 前缀**由 5 个模块提供，
> **不是**守护站点分布。守护站点是 **22 文件**。

### F4 · 测侧还有 **30 处** `require_admin` / **10 文件**
`tests/_route_policy.py` · `tests/test_route_policy.py` · `tests/api/test_admin_package.py` ·
`test_totp_2fa.py` · `test_metrics_admin.py` · `test_admin_metrics.py` · `test_datasources_stats.py` ·
`test_logicform_rerun.py` · `test_admin_query_history.py` · `tests/integration/test_closeout_19b.py`。

### F5 · ⭐ 真实修复：`/api/admin/catalog` 的 `defaults` = **部署级 catalog 泄漏，且是死载荷**
- `knot/api/catalog.py:52` `"defaults": catalog_loader.get_defaults_from_files()`
  → `catalog.py:184-194` → `catalog_loaders.py:19-26` `importlib.import_module("knot.services.agents._local_catalog")`
  = **部署方填的单一进程级模块**（gitignored），**完全绕过 per-tenant 槽**。
- ⇒ 任何**租户 admin** 都能拿到**部署方真实业务库表 + 业务口径**。已登记 R-T-GATE blocker。
- **而它在前端从未被读**：全前端仅两处出现，`frontend/src/screens/Admin.jsx:51`（初始 state）+
  `:108`（`d.defaults || {…}` 存进 state），**零处渲染**。
  「恢复默认」按钮走的是服务端 `POST /api/admin/catalog/reset`（`tab_system.jsx:116` → `Admin.jsx:307`
  toast 响应里的 `r.source`），**不用这个载荷**。
- ⇒ **删字段 = 零 UI 影响的修复**（`:108` 本就有 `|| {}` 兜底），比「路由到 platform 端点」更小更安全。

### F6 · 平台侧审计**无处落**（本片不结清 R-10 的原因）
`audit_service.log` → `audit_repo.insert` → `base.get_conn` = **租户库**；且 v0.9.4 MF3 已让
`audit_service.py:127-128` 对 `TenantContextError` **re-raise**（不再 fail-soft 吞）
⇒ 一个 platform 动作**没有库可写审计**。LOCKED 把 `platform_audit` 预留在 **[0.4]**。

### F7 · R-10 audit-on-drift 明文卡在本片
`knot/core/tenant_context.py:102` — 「本片**刻意不新增 `AuditAction`** —— LOCKED 的 audit-on-drift
**仍未结清**，随 v0.9.5」。它要写「哪个平台租户」⇒ 同样需要 F6 的落点。

### F8 · 改名会打断语义层的**信任论证**（3 处具名 + 2 处 prose）
- `knot/services/semantic/compiler.py:297` — 「admin 信任面（`metric.caliber` / `metric.filters`）不在 lf
  → 天然免验（require_admin gated）」
- `knot/services/semantic/fragment_guard.py:38` — 「严禁碰信任的 `metric.filters` / `caliber`（require_admin gated…）」
- `fragment_guard.py:41` — 「admin caliber / metric filters 信任面 scope 外（require_admin + OOS-1v2 锁）」
- `knot/models/user.py:45` · `knot/services/bi_report_service.py:4`（prose）
⇒ 改名后须重述为「**租户 admin 只对自己租户的 SQL 面被信任**」（文件边界保证不越租户）。

### F9 · 平台面**零 HTTP 表面**
`tenant_repo.list_tenants()` 唯一调用点 = `knot/main.py:82`（启动期）。
全仓 grep `api/platform` / `/api/tenants` → **零命中** ⇒ 运维今天没有任何 API 能看租户状态。

### F10 · 共享密钥模板（E1 要照抄的形状）= `scheduler_tick`
`knot/api/bi_schedule.py:30-40` — 调用期 `os.environ.get("KNOT_SCHEDULER_TOKEN","")`（**非 settings.py**，
同 `webhook.py` R-SL-69 范式，便于 `monkeypatch.setenv`）· 未配 → **503**（安全默认）· 错 → 401 ·
`secrets.compare_digest` **常量时间**比对。
> ⚠️ 但它的 Bearer 解析是**手搓** `presented = auth[7:] if auth[:7].lower()=="bearer "`。
> **本片新写的不照抄这一点** —— v0.9.4 S8 实测：手搓解析比 `HTTPBearer` **更严**，
> 对 RFC 合法的双空格 / 尾 tab 会误拒。新代码用 `get_authorization_scheme_param`（同
> `tenant_resolution.py:_bearer_payload`）。**不改 scheduler_tick 本身**（不在范围内，登记 backlog）。

### F11 · 前端 **19 处** `role === 'admin'` 门 ⇒ E1 的选择让前端**零改动**
`frontend/src/Shell.jsx:22` + `frontend/src/App.jsx:76,151-163`。
platform admin **不住在租户 users 表里**（E1 = out-of-band 共享密钥）⇒ `role` 值域不变
⇒ 前端零改动，保持 v0.9.x 后端-only。

### F12 · 已做完的，别重做
- 限流桶**已** tenant-keyed：`knot/api/_rate_limit.py:93 _tenant_authed_key`，4 处调用
  （`:144` totp_verify · `:158` totp_enroll · `:177` totp_enroll_complete · `:195` query）
  ⇒ LOCKED line 92 那项 **v0.9.1 已清**。
- `admin/datasources.py:37,42` 两个缓存均已过 `tenant_cache_key`（`:106` `:143` `:236`）。

### F13 · `platform_schema.sql` 的预留注释**已陈旧**
`user_directory [0.1] 平台级用户 → 租户路由` —— v0.9.4 kk 决策①（**专属登录链接 `?c=<slug>`**，复用已存在的
`tenants.slug` UNIQUE）让它**不必要**（并顺带避开「用户名须全局唯一」与 seed 逻辑的正面冲突）。
`platform_admins [0.1]` —— 本片 E1 选 out-of-band 共享密钥，**不建此表** ⇒ 注释须同步。

### F14 · 会**按设计必红**的闸门（4 个，逐条实测定位）
| 闸门 | 现值 | 本片后 | 为什么 |
|---|---|---|---|
`tests/test_tenant_isolation.py:172` | `== 144` | **145** | 新增 1 条 platform 路由 |
`tests/test_route_policy.py` 快照 + fixture | 138 路由 / ADMIN 90 | **139** / TENANT_ADMIN 90 + **PLATFORM_SECRET 1** | 改名 + 新类 + 新路由 |
`tests/api/test_admin_package.py:36-49` D12' 注释 | 三轴 53/67/90 | 数字不变、**名字变** | 注释里 `require_admin` 字面 |
`tests/test_route_policy.py:5` docstring | 引 `== 144` | 145 | cosmetic |
> `>= 80` 三处下限（`test_rename_smoke.py:152` / `test_api_smoke.py:35` / `test_intent_accuracy.py:83`）**不受影响**。
> `== 53`（admin **包**）**不受影响** —— platform 路由不在 `/api/admin/` 下。

### F15 · ⭐ 已实测：中间件对「opaque 平台密钥」**不 5xx** ⇒ D2 无需额外守卫
platform 请求带的是 `Authorization: Bearer <不透明密钥>`，而 tenant middleware **恒先于** Depends 运行
（ASGI 包住 router），会拿它去 `jwt.decode`。**实测 6 种形态全部干净返 `None`、零异常**
（`tr._bearer_payload`）：32B hex 随机密钥 · 含点像 JWT 的密钥 · `bearer` 小写+双空格 · 尾 tab ·
空 token · `Basic` scheme。⇒ 不设 ctx、不 5xx，**平台路径天然不进租户解析**。
> 这条在 Stage 1 期就验，因为若它 5xx，D2 就得加一道路径白名单守卫（而白名单正是 v0.9.4 刻意避开的漂移源）。

---

## §0.1 执行者主动加的两点（非评审要求）

1. **F5 的定性从「路由到 platform 端点」改成「删死字段」** —— 测绘时发现前端零处渲染。
   更小、更安全，且顺带 −1 死载荷。**但它使新 dependency 零消费者**（见 §2 D3 的解法）。
2. **新 dependency 的 Bearer 解析不照抄模板的手搓写法**（F10 ⚠️）—— 兑现 v0.9.4 S8 教训。

---

## §1 基线（本片开始前，实测）

- main = `6764e0e`（chore guard 已合）· 版本 **0.9.4**（4 源点一致）
- 全量：**主仓 1606 passed / 114 skipped**；**worktree/CI（`.example` 语料）1560 / 115**
  （差 45 条 = 真 OHX 语料 gitignored，详 `tests/eval/_semantic_eval.py:35-39`；报数须带口径）
- 路由：flatten **144**（138 APIRoute + 6 Mount）· ADMIN **90** / REPORT_PERMISSION 10 /
  AUTHENTICATED 34 / PUBLIC_OR_OUT_OF_BAND 4
- 闸门：ruff · lint-imports 9 contract · check_file_sizes · 4 源点 doc-invariant

---

## §2 决议（资深 2026-07-29 四条拍板已并入）

> 拍板记录：**E1** = out-of-band 共享密钥（非 `platform_admins` 表）· **E2** = 不引入 platform **写**操作 ·
> **E3** = 改名 `require_tenant_admin` · **E4** = 删死 `defaults` + 给 dependency **一个只读消费者**。

### D1 · `require_admin` → `require_tenant_admin`（纯重构，0 行为）
- 90 生产站点 / 22 文件 + 30 测站点 / 10 文件，**一次改完**。
- **不留 `require_admin` 别名** —— 别名 = 漂移路径 + 名字不再诚实（本片的全部意义就是让名字诚实）。
- 策略分类常量 `ADMIN` → `TENANT_ADMIN`，fixture 同步重生成。

### D2 · 新 `require_platform_secret`（E1 · out-of-band）
- 新文件 `knot/api/platform_admin.py`（不塞进 `deps.py` —— 它是**平行**认证路径，物理分开更难混）。
- env **`KNOT_PLATFORM_ADMIN_TOKEN`**，**调用期读**（`os.environ.get`，非 settings.py — 同 F10 范式）。
- 未配置 → **503**（安全默认，与 scheduler_tick 一致）· 不匹配 → **401** · `secrets.compare_digest` 常量时间。
- Bearer 解析用 `get_authorization_scheme_param`（**不手搓** — §0.1-2）。
- ⭐ **严禁设 tenant ctx、严禁触 `get_conn`**（R-v095-1）。

### D3 · `GET /api/platform/tenants`（E4 · **唯一**消费者，只读）
- 返 `[{id, slug, name, status, db_dir, created_at}]`，走 `tenant_repo.list_tenants()`（**ctx-free** 平台库）。
- **读不是 mutation** ⇒ 不违反 E2 ⇒ **不需要 `platform_audit`**（F6 的落点问题不被触发）。
- 顺带补 F9 的空白：运维今天**零** API 能看租户状态。
- ⚠️ **零写操作**：不加建/停/改租户端点（E2）。R-T-GATE 硬锁**一行不动**。

### D4 · 删 `/api/admin/catalog` 的 `defaults`（F5）
- 删 `knot/api/catalog.py:52` 一行 + 该字段的 schema/docstring 提及。
- **前端不动**（`Admin.jsx:51,108` 的死 state 留 backlog）—— 理由：`:108` 本有 `|| {}` 兜底 ⇒ 零行为改动；
  且碰 `Admin.jsx` 会撞 R-PA-PB-V1 的 18 屏 byte-equal 铁律。**请 Stage 3 裁这个取舍。**

### D5 · 策略分类新增 **`PLATFORM_SECRET`** 类（承重，非装饰）
`tests/_route_policy.py::_classify` 加一支：认 `require_platform_secret` **对象身份**。
⭐ **不加这一支，platform 路由会落进 `PUBLIC_OR_OUT_OF_BAND`**（它确实没有 `get_current_user`）
⇒ **本片对我自己那份哨兵不可见**，而那 4 条集合是钉住的、会误报成「认证面新增一条」。

### D6 · prose 重述（F8 · 3 具名 + 2 处）
`compiler.py:297` / `fragment_guard.py:38,41` / `models/user.py:45` / `bi_report_service.py:4`
→ 「**租户 admin 只对自己租户的 SQL 面被信任**」（跨租户由 per-tenant 文件边界结构性挡住，非靠这层信任）。

### D7 · `platform_schema.sql` 陈旧注释修正（F13）
`user_directory` 标**已不需要**（附 v0.9.4 决策①理由）· `platform_admins` 标**本片选 out-of-band、暂不建表**。

### D8 · R-10 audit-on-drift **仍不结清**，且必须明写为什么
E1（无平台身份）+ E2（无平台写操作）⇒ **仍无平台审计落点**（F6）⇒ R-10 **不能在本片关**。
CHANGELOG「未结清」段须写清这个因果，**不得**暗示本片推进了它。

---

## §3 红线

- **R-v095-1**：platform 路径**严禁**设 tenant ctx、**严禁**触 `base.get_conn`；只允许 `get_platform_conn`（ctx-free）。
- **R-v095-2**：共享密钥**调用期读 env**；未配 → 503；比对必须 `secrets.compare_digest`。
- **R-v095-3**：Bearer 解析必须用 `get_authorization_scheme_param`，**不得手搓** `auth[7:]`（v0.9.4 S8）。
- **R-v095-4**：D1 是**纯重构** —— 除策略类名与 fixture，全量结果必须与改名前**逐字相同**；不留别名。
- **R-v095-5**：platform 路由**不得**落进 `PUBLIC_OR_OUT_OF_BAND`（必须有 `PLATFORM_SECRET` 类）。
- **R-v095-6**：**零 platform 写操作**；R-T-GATE 硬锁与 `assert_no_second_active_tenant_served` 一行不动。
- **R-v095-7**：**前端 0 改动**（R-PA-PB-V1 18 屏 byte-equal 保持）。
- **R-v095-8**：R-10 不得被暗示为「已推进」（D8）。

---

## §4 验收（逐条自证；「型」= 违规状态的取材方式 —— revert 若有对应的修 / injection 若是未来状态）

| # | 断言 | 型 |
|---|---|---|
1 | 全仓 `Depends(require_admin)` = **0**；`Depends(require_tenant_admin)` = **90** | AST 哨兵 · injection：留一处旧名 → 红 |
2 | 改名后全量与改名前**逐字相同**（除策略类名/fixture） | 前后两次全量对比 |
3 | 未配 `KNOT_PLATFORM_ADMIN_TOKEN` → platform 端点 **503** | revert：把 503 改成放行 → 红 |
4 | 错 token → **401**；对 token 用 `compare_digest` | injection：改成 `==` → 哨兵红 |
5 | RFC 合法变体（`bearer` 小写 / 双空格 / 尾 tab）**不得** 5xx | injection：换手搓解析 → 红 |
6 | platform 请求**未设** tenant ctx 且**未触** `get_conn` | injection：在 handler 里调 `get_conn` → 红（fail-closed raise） |
7 | platform 路由在快照里是 **`PLATFORM_SECRET`**，且 `PUBLIC_OR_OUT_OF_BAND` 仍恰 4 条 | revert：删 `_classify` 那支 → 红且点名 |
8 | `/api/admin/catalog` 响应**不含** `defaults`，且不含任何 `_local_catalog` 内容 | revert：加回字段 → 红 |
9 | 中间件对 opaque 平台密钥**不 5xx**（**F15 已实测 6 形态**）| 把 6 形态钉成参数化测（防将来解析改动回归） |
10 | 路由计数 `== 145`；`== 53` **不变** | 两处断言实跑 |
11 | R-T-GATE：插第二 active 租户 → 全站仍 fail-closed | 沿用 v0.9.4 那组 |
12 | 4 源点 = **0.9.5** + CHANGELOG 顶部同步 | doc-invariant CI |

---

## §5 commit 序（4 步）

1. **`refactor(auth)`** D1 —— 90 生产 + 30 测站点改名 + 分类常量 `ADMIN`→`TENANT_ADMIN` + fixture 重生成
   + 新增哨兵「全仓禁 `require_admin` 字面」。**验收 1/2**。
2. **`feat(platform)`** D2+D3+D5 —— `platform_admin.py` + `GET /api/platform/tenants` + `PLATFORM_SECRET` 类
   + fixture + 路由计数 144→145。**验收 3~7、9~11**。
3. **`fix(security)`** D4 —— 删死 `defaults`。**验收 8**。
4. **`docs`** D6+D7+D8 —— prose 重述 + schema 注释 + DEPLOY.md 新 env + CHANGELOG + **4 源点 bump 0.9.5**。
   **验收 12**。

---

## §6 自检（抗诱惑）

- [ ] **不**顺手做 B-3 三项（per-tenant file catalog / http_spec 凭据 / egress 租户域化）—— R-T-GATE 清单，独立片
- [ ] **不**顺手 lift R-T-GATE · **不**顺手加租户开通端点（E2）
- [ ] **不**顺手改 `scheduler_tick` 的手搓 Bearer 解析（登记 backlog）
- [ ] **不**顺手动前端（R-v095-7）· **不**顺手建 `platform_admins` 表（E1 已定 out-of-band）
- [ ] **不**顺手结 R-10（D8 —— 且必须明写为什么不能结）
- [ ] **不**把「改了名」当成「拆了权」：本片**没有**任何 platform 写操作，`defaults` 归位是唯一实质权限收紧

---

## §7 给守护者的复核指引

**最该被攻的三处**：
1. **F2 那条结论**（`get_current_user` 结构性拒绝无租户身份 ⇒ 必须平行路径）—— 若它错，整片形状错。
2. **D4 的取舍**（删后端字段但**不动**前端死 state）—— byte-equal 铁律 vs 留 cruft，请裁。
3. **D3 是否算「越出纯拆分」** —— 新增一条只读端点换掉「dependency 零消费者 = 死码」，这个交换值不值。

**我特意没做的**：没写代码（放行前）· 没动 R-T-GATE · 没建平台表 · 没碰前端 · 没结 R-10。
