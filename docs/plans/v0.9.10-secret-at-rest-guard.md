# v0.9.10 弧 —— 静态明文凭据守护（**Stage 1'** · 按 Stage 2 Codex 14 条 + Stage 3 守护者 `major-revise` 重写）

> **Stage 3 裁定**：`major-revise`。方向（派生扫描面 / WARN-vs-hard-fail 分工 / 不做热路径告警）**保留**；
> **R2 · R3 · R14 三条阻断闭合前不得实施**。拆片 + 守护者 §III 三条落进本稿，待守护者复核。
> **本稿相对上一稿**：① 三条阻断我**逐条独立复跑**（§1，不转述）② 切片重排（§2）
> ③ R1 指纹已清除（未推送，`--amend` 彻底）④ v3.1-B 表按新事实重答（§6）。

---

## §1 三条阻断 —— 我独立复跑的结果（不转述 Codex / 守护者）

### R1 指纹入库 —— **成立，且已彻底清除**

上一稿把两个凭据的 SHA-256 前 12 位写进了文档与 commit 消息。
**处置判据先确认推送状态**：`origin/main = 4e79c12`，本地领先 **2 个 commit，均未推送**
⇒ `--amend` 能真正抹掉（不是「按已泄露处理」那条路）。已清除；文档改为只留布尔结论。

**我的定性**（比「谨慎起见」更具体）：73 字符高熵的 API key，48 bit 前缀反推不出，但**是确认预言机**；
而 25 字符的 `doris_password` **若是人选的口令**，48 bit 足以在字典/规律空间里跑出来。
⇒ **R1 成立。而这正是我在别处一直守的 #262 族（凭据派生物进产物）—— 我自己犯了。**

### R3 WAL 备份 —— **成立，我复现了，且实况比 Codex 报的更狠**

```
源库（连接开着 = 服务在跑）: t 行数=1  -wal=12392 字节
① copy2 备份（脚本现在的做法）:  **no such table: t**   quick_check = ok
② SQLite backup API（uploads_relocation.py 已有范式）:  t 行数=1   quick_check = ok
```
⇒ **不只是丢已提交的行 —— 连 `CREATE TABLE` 都在 WAL 里，备份是个空壳，而 `quick_check` 说 `ok`。**
`base.py:46` 确为 `PRAGMA journal_mode=WAL`。⇒ **凭据迁移唯一的回滚路径可以是一个空文件。**

⚠️ **我已经用这个脚本跑过 kk 的真实库**（§3）。逐表核对：两份备份**均无行数差异**。
**但这是运气不是安全** —— 动手前我确认过 dev server 没在跑 ⇒ 库是干净关闭的、WAL 已被 checkpoint 掉。
**缺陷是真的，我那次躲过去了，原因可识别、不是因为机制健全。**

### R2 双 key 混库 —— **成立，我同意守护者「全套最严重」的判定，理由是不可逆**

`is_encrypted` = 纯前缀判断；脚本对带前缀的值 `continue`。⇒ 换一把**格式合法的错 key**：
旧密文被跳过、新明文用新 key 加密 ⇒ **不存在任何一把 key 能解全库**，而脚本 `exit 0` 声称成功。
运维一旦相信它而丢掉旧 key ⇒ **那些凭据永久不可恢复**。

⭐ **守护者指出的自指我完全接受**：§3 里我「先手工解开两个现存密文验证钥匙正确才敢加密」——
**那正是一条只写在文字里、没有守护的规则**，与本片要治的 F1/F2/F3 **同一形状**。
⇒ 机械化它不是新增要求，是**把本片的论点用在本片自己身上**。

### R14 static 未重建 —— **成立，我实测，且它推翻了一个我在 §9 里做过的声称**

```
main.py     version="0.9.9"
version.js  0.9.9
knot/static/assets/*.js 里出现的版本字面: 0.9.6   (bundle mtime Jul 30)
```
⇒ **v0.9.7 / .8 / .9 三片都 bump 了 4 源点、都没重建 static。** 用户可见后果：UI 侧栏 / Login 页脚显示
**v0.9.6**，API 报 **0.9.9**。

⚠️ **我订正守护者的一处归因**（结论不变，形状更糟）：守护者写「v0.9.7 的 §9 声称做了[重建]，那条声称是假的」。
**实查三片手册：「npm run build / 重建 static」0 命中 —— 没有任何一片声称重建过。**
真正的假声称是另一句：

> v0.9.7:290/435 · v0.9.8:330 —— **「前端零改动 ⇒ vitest 83 应逐字不变」**

而 `git diff --name-only` 实测：**三片各自都改了 `frontend/src/version.js`**。

⭐⭐ **它是 by construction 假的，不是偶然写错**：`frontend/src/version.js` **就是 4 源点的第 4 点**
（CLAUDE.md:413）⇒ **任何 bump 版本的片都必然改前端** ⇒ 「前端零改动」在这类片里**永远不可能为真**，
而模板据此结论「不跑前端闸门 / 不重建」。**两条规则互斥 ⇒ 漏构建不是疏忽，是模板保证会发生**
—— 这解释了为什么它连着发生 3 次，也预言了它会**每次**发生。

⚠️ **自捉**：本稿初版 §7 又把「前端未动」写了一遍（P-a/P-b 同样要 bump `version.js`）⇒ **我自己踩进去了**，已修。

⇒ **两个修法，缺一不可**：
1. **结构闸门**（守护者的加项）：`knot/static` 纳入 doc-invariant CI（bundle 版本字面 == `version.js`），漏一次即红；
2. ⭐ **删掉模板里那句话** ——「前端零改动」应改为**「`frontend/src/` 除 `version.js` 外零改动」**，
   且**不得据此跳过重建**（版本串进 bundle）。**只做 1 不做 2，模板会继续教下一片说假话。**

**可行性已核**：`frontend/package-lock.json` 存在（2862 行）⇒ **前端构建可复现**
（与后端 F1「依赖浮动」不同）。用 `npm ci` 重建；`knot/static` 25 文件 git 跟踪，重建产生
「删旧 hash 名 + 加新 hash 名」的正常 diff。

---

## §2 切片（守护者裁「拆两片且顺序反过来」；我**提一个第三片**，请复核）

| 片 | 内容 | 为什么这个顺序 |
|---|---|---|
| **P-0**（**我的加项，请裁**） | R14：重建 static + `knot/static` 纳入 doc-invariant | ⭐ **与密码学完全无关**，且是**此刻用户可见的缺陷**。塞进 P-b 会让一个显示 bug 排在两轮安全评审之后；塞进 P-a 会破坏 P-a 的单一目的（而单一目的正是 v0.9.5 §8.8「顺序当 oracle」的前提）。**两个 commit 即可交付。** 若守护者认为不值单开片，我并回 P-b 末尾。 |
| **P-a**（先做，紧急） | `migrate_encrypt_v045` 硬化 | 它**此刻是危险工具**，而排练阶段二随时会再跑它 |
| **P-b** | 新扫描 + 启动 WARN + 只读 CLI + 哨兵 + R12 豁免 + DEPLOY/事故响应 | 依赖 P-a 的安全脚本 |

⚠️ **P-0 与 P-a 之间有一个顺序约束**：加了 static 不变量后，**在 static 未重建时它就是红的**
⇒ **重建与加断言必须同片**（P-0 内两个 commit，断言在后）。

⚠️ **时效执行项（立即生效，不等评审）**：**排练阶段二在 P-a 合并前不跑 `migrate_encrypt_v045`。**
已登记；阶段二的其余部分（挂真实旧库跑升级）不依赖该脚本，可照常进行。

---

## §3 已发生的事（不在任何片的 diff 内，仅记录 + 遗留处置）

跑 `migrate_encrypt_v045` 于 kk 本地真实库，独立复查：钥匙正确性**先验证**（手工，即 R2 要机械化的那步）→
dry-run 预告 `encrypted: 2` 与手工排查吻合 → 跑后 11 个落点 **0 明文** → **往返验证**两个值
「加密前明文」== 「今天经应用层读回」**相等**（⚠️ 比对在内存里做完**只留布尔**，不记摘要不记长度 —— R1）。

**遗留（人的动作，非本弧代码）**：按 Codex R9 + 守护者升级为**发布检查** ——
历史 `.bak` 涉 **3 张表**（users / data_sources / app_settings），**`doris_password` 同样暴露过**
（上一稿只建议轮换 OpenRouter key，收窄不足）⇒ **逐一盘点仍有效的 DB / API / IM 凭据并轮换**。

---

## §4 P-a 设计 —— `migrate_encrypt_v045` 硬化

| # | 改动 | 对应 |
|---|---|---|
| a1 | **备份改 SQLite backup API + 校验 + fsync**，**复用 `repositories/uploads_relocation.py` 已有范式**（不重新发明）；`0600` 权限；sidecar（`-wal`/`-shm`）生命周期明确 | R3 · 守护者 §I · Codex R4 |
| a2 | ⭐ **写前全量 decrypt preflight**：遍历全部落点，对**带前缀**的值逐个 `decrypt`；**任一失败 ⇒ 零写入、非 0 退出**，且**在建备份之前**（错 key 连备份都不该产生） | R2 |
| a3 | **后置校验复用同一个 `Connection`**（不另开、不依赖 ambient ctx） | R5（顺带解 R7 一半） |
| a4 | **确定性排序 + 按表聚合** —— `_SENSITIVE_KEYS` 实测是 `frozenset` ⇒ 无序**已坐实**；不排序则失败位置每次不同 ⇒ **事故不可复现** | R6 + 守护者强化 |
| a5 | `--all-tenants`；⚠️ **必须先完成 schema 迁移，或对不存在的列显式报错** —— 4 个后增列在旧库里不存在，静默跳过会让「11/11 覆盖」在旧库上**变成谎言** | R8 + 守护者补充（与 v0.9.8「两条平台迁移能串起来」同族） |
| a6 | `rows_updated` / `fields_encrypted` 改名（现 `encrypted` 语义含混）+ **部分迁移的错误语义**写明 | Codex R11 |
| a7 | **`TARGETS` 改派生**（7/11 → 11/11） | 原 D3 · F2 |

**P-a 哨兵**（各配 revert-to-bad）：
- Sa1 **WAL 反例**：源库开着连接 + 未 checkpoint 的已提交行 ⇒ 备份**必须**含该行（退回 `copy2` ⇒ 红）
- Sa2 **错 key 零写入**：注入一把格式合法的错 key ⇒ 必须非 0 退出**且 DB 逐字节未变**（摘 preflight ⇒ 红）
- Sa3 覆盖 == 全部落点（硬编回 `TARGETS` ⇒ 红）
- Sa4 顺序确定性：同一库跑两次，**失败注入点相同 ⇒ 失败位置相同**
- Sa5 旧库缺列 ⇒ **显式报错**而非静默跳过

## §5 P-b 设计 —— 新扫描 + 守护

| # | 改动 | 对应 |
|---|---|---|
| b1 | `secret_at_rest.scan_plaintext_secrets(conn)` —— **接收连接**（R5）；落点集从三个既有真相源派生（不造第 5 份清单）；**Finding 永不含值** | D1 · R5 |
| b2 | ⭐ **oracle 口径写进 docstring**：判据是「**用当前加载的那把 key 解得开**」，不是「有前缀」。混 key 库里所有值都是「某把 key 的合法密文」⇒ 若将来引入 `MultiFernet`，本判据含义会**静默改变** | 守护者 §III-2 |
| b3 | ⭐ **扫描范围 = 租户库；平台库不在内，且写明**。今天平台库无凭据列（`allowed_http_hosts` 不是；`detail_json` 有 v0.9.8 哨兵禁凭据）⇒ 正确；但 B-3 路线图里有 per-tenant 初始口令 ⇒ 将来加凭据列时必须有人想起这里 | 守护者 §III-3 |
| b4 | **启动 WARN 移到 master-key fail-fast（`main.py:103`）之后的独立逐租户循环** —— 现 `:83` 循环早于友好校验，而扫描的真 oracle 需要 key ⇒ 放 `:83` 会让**一个非阻断探测器在 key 缺失时以 traceback 崩掉启动**。WARN 含 **tenant id + 聚合计数 + 有界样本** | R7 + 守护者 |
| b5 | ⭐ **只读 CLI** `python3 -m knot.scripts.scan_secrets_at_rest --all-tenants`，有发现即非 0 退出 —— 它**正好补上 R10 留下的缺口**：R10 正确地禁止在真实库注入正对照 ⇒ 「真实库应报 0 明文」只剩一半验证（能证 0，不能证扫描器没坏）。有了只读 CLI：① 真实库上只读即得可信结论 ② 扫描器的正对照在临时库做（Sb2） ③ **运维事故后有一条可跑命令**，而不是只能重启看日志 | 守护者 §III-1 · R10 |
| b6 | **R12 处置 = 显式豁免 + 具名 backlog 片**（不在本片加密 `monitors.action_target`）。⭐ 理由（守护者裁定，我同意）：加密它就必须**同片迁移存量行**，而存量迁移正是那个脚本 ⇒ **顺序上不可能**。豁免须写明理由 + 登记具名片 + 同步收窄模块/文档命名 | R12 + 守护者不同处置 |
| b7 | **结构哨兵**：schema 里**敏感形状**的列（名字含 password/secret/token/key/credential）**必须**出现在落点集**或**豁免表里 —— 新增敏感列忘登记即红 | Codex R13 · 守护者保留 |
| b8 | 修正 `fernet.py:56` 假注释（**只改注释，刻意不造 per-read 告警** —— 不应为让旧注释变真而造功能） | 原 D4 · F3 |
| b9 | DEPLOY 运维段：启动 WARN 长什么样 + 怎么处置 + **`.bak` 里的历史明文** + 事故响应清单 | R9 |

**P-b 哨兵**：Sb1 扫描面 == **字面期望集合**（扫描面派生 + 期望值字面，PR#263 范式）·
Sb2 注入明文被扫出（⚠️ **注入后先读回裸值确认真的无前缀**，v3.1-B #2）· Sb3 Finding/WARN **不含值**
（断 `record.getMessage()` **不断** `caplog.text` —— 后者含文件行号，v0.9.7 踩过）· Sb4 结构哨兵 b7 ·
Sb5 key 缺失时启动**不因扫描而崩**（R7 反例）

## §5.5 P-0 设计 —— static 重建 + 不变量

- c1 `npm ci && npm run build`（**`npm ci` 不改 lockfile**；构建后须验 `package-lock.json` git diff 为 0）
- c2 `tests/test_doc_invariants.py` 加 `test_static_bundle_version_synced_with_version_js`
- ⭐ c3 **删掉模板里的假话**：CLAUDE.md / 手册模板中「前端零改动」→
  **「`frontend/src/` 除 `version.js` 外零改动；`version.js` 每片必改 ⇒ 必须重建 static + 跑 eslint/vitest」**。
  **只加闸门不改这句，模板会继续教下一片说假话。**
- **Sc1**：把 `version.js` 改成别的版本 ⇒ 新断言必须红（证明它真在比对 **bundle 内容**，而非比对两个源码常量）

---

## §6 v3.1-B 承重面枚举表（按新事实重答 —— 变化处标 ⭐）

1. **fail-open 面**：扫描是探测器（fail-open，接受）；⭐ **P-a 的 preflight 是 fail-closed 且没有两难**
   —— 硬行为放在没有策略题的一侧。
2. **oracle 能力 + 注入真能产生它吗**：⭐ **本轮三条阻断全是「注入真能产生它」的正面示范**
   （WAL 反例、错 key、stale bundle 都是实跑出来的，不是推理的）。Sb2 的注入须读回裸值确认。
3. **「那一行」族**：门 —— P-a preflight 在**第一次写之前**（且在建备份之前）；
   消息 —— WARN 在真扫出明文那条路径且断 `getMessage()`；
   记录 —— 脚本自校验在**声称成功的同一次运行内**（F1 根因就是「声称」与「核实」被分开）。
5. **散文规则**：⭐ 本弧治的就是三条无守护的散文规则（F1/F2/F3），⭐ **而 R2 揭示了第四条 —— 我自己在
   §3 里手工做的 key 验证**。新增散文「Finding 不含值」由 Sb3 守；「豁免须具名」由 b7 守。
6. **声明 vs 生产者**：`scan_plaintext_secrets` ⭐ 现有 **3 个消费者**（启动 WARN / 脚本自校验 / 只读 CLI）。
7. **顶班**：摘 scan 主体 ⇒ Sb2 必须转红；摘 preflight ⇒ Sa2 必须转红。**实跑验证。**
8. **既有测的绿是真的吗（扫两侧）**：`test_legacy_plaintext_row_decrypts_as_passthrough` 理由仍真（WARN 不拒绝）；
   `test_migrate_encrypt_v045.py` **9 条逐条复核**（`test_migrate_encrypts_all_targets` 的「all」口径 7→11）；
   ⭐ **描述侧**：`fernet.py:56` 假注释（b8）+ **v0.9.7 §9 那条假声称**（本稿 §1 R14 已书面推翻）。
9. **契约冲突**：`repositories → repositories` 合法；`knot/scripts/` 层归属**实施时跑 `lint-imports` 验证**。
10. **策略题的影子**：⭐ 原两难（启动 WARN vs 拒启动）**仍在且是真的**（legacy 明文是刻意支持的合法状态）；
    ⭐ 但 R2 暴露出**另一处本不该有的两难**已被消解 —— 「错 key 时该继续还是中止」在 preflight 面前不成立：
    **零写入是唯一正确答案**。
11. **诚实收窄 —— 本弧不声称**：不自动加密（R-41 仍守）· 不阻止明文写入 ·
    不检测日志/备份/上传件/`.bak` 里的明文 · **不能挽回已泄露的值（轮换是人的动作，已升为发布检查）** ·
    不实现 per-read 告警（b8）· **不加密 `monitors.action_target`（b6 显式豁免 + 具名 backlog）** ·
    ⭐ **不声称平台库被扫描**（b3）。

---

## §7 验收（三片各自）

⭐ **三片一律含前端闸门** —— 每片都 bump `version.js` ⇒ **每片都是前端片**（§1 R14 的 by-construction 结论）。
写「前端未动」即为假声称。P-0 的 c2 闸门上线后，漏重建会**当场红**，不再依赖人记得。

- **P-0**：四闸门 + **eslint + vitest** + `npm ci` 后 `package-lock.json` diff 0；**Sc1 revert 实跑**
- **P-a**：四闸门 + **eslint/vitest + 重建 static**；Sa1-Sa5 各一次真跑过的 revert-to-bad，失败信息贴进 §9；
  **既有 9 条逐条给「保留 / 改口径 / 理由」**
- **P-b**：四闸门 + `lint-imports` + **eslint/vitest + 重建 static**；Sb1-Sb5 同上；
  ⭐ **真实库验收改为只读 CLI**（R10：禁在真实库注入正对照）

## §8 待守护者复核（Stage 1'）
1. **P-0 是否单开片**（§2 我的加项）
2. P-a 的 a5「旧库缺列显式报错」是否要求**脚本自己跑 schema 迁移**，还是**只报错让运维先升级**
3. b6 的具名 backlog 片编号

## §9 实施记录（回填）
