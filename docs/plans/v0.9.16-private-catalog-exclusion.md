# v0.9.16 —— 私有 catalog 排出构建上下文（lift 弧 · R-T-GATE 清单项）

> **kk 2026-08-06 拍板**：方案 ①（bind-mount 单文件）。**按提速口径写短。**

## §0 grounded 测绘（全部实读）

| 事实 | 出处 |
|---|---|
| `_local_catalog.py` / `few_shots.yaml` **已 gitignore**（部署方本地文件，不在仓里） | `.gitignore:31,32` |
| 但 `COPY . .` 把它们**烤进镜像** | `Dockerfile:17` |
| `_template_catalog.py` 是**仓内模板**、显式 COPY（要留） | `Dockerfile:22` |
| loader **已有三级回退**：真 catalog → 模板 → 空 | `catalog_loaders.py:30→41`（`except Exception: pass`） |
| few-shot 同样有回退：DB → `few_shots.yaml` → `.example.yaml` | `few_shots.py:22` |
| 拦路哨兵：`_MUST_STAY` **正向断言 `_local_catalog.py` 必须留在上下文** | `test_dockerignore_context.py:211` |

⭐ **排出镜像天然安全** —— 回退链早就存在，文件缺失只会退到模板，不会崩。

## §1 设计

1. **`.dockerignore` 排两个文件**：`knot/services/agents/_local_catalog.py` · `knot/services/few_shots.yaml`。
   ⚠️ **逐文件列而非排目录** —— 同目录下 `_template_catalog.py` / `few_shots.example.yaml` **必须留**。
2. **哨兵改判据，不加豁免**：`_local_catalog.py` 从 `_MUST_STAY` **移入** `_FAMILIES`
   （新增「私有 catalog」族，含 `few_shots.yaml`），并给出「拆掉哪条规则会让它逃逸」。
   ⛔ 不是加例外 —— 是**这个决定本身变了**，判据要跟着变。
3. **DEPLOY.md**：给 bind-mount 的确切命令（`:ro`）+ 说明「不挂 = 退到模板 catalog，不是崩」。
4. 版本 bump v0.9.16（4+1 源点 + 重建 static）。

## §2 v3.1-B 承重面（命中：租户隔离 · 构建上下文）

1. **fail-open 面**：排错文件会不会让隐私仍进镜像？—— 逐文件排 ⇒ **漏排 = 仍进镜像**（fail-open 方向）
   ⇒ 判据必须是**负向 canary**（真造该文件、断言它不进上下文），不是「读 `.dockerignore` 有没有那一行」。
2. **oracle 能力 + 注入真能产生它吗**：canary 用真文件名 + 假内容；摘掉那条规则 ⇒ 它**真的**出现在 tar 里。
3. **「那一行」族 / 入口**：排除只有 `.dockerignore` 一个入口（`COPY . .` 是唯一拷贝点，`COPY knot/prompts/` 等显式行**不含**这两个文件 —— 已核）。
7. **顶班**：`_local_catalog.py` 所在目录**没有**其他规则覆盖（`knot/services/agents/` 不在任何已有族里）⇒ mutant 非空判据。
8. **既有测的绿是真的吗**：`_MUST_STAY` 那条**按设计必红** —— 它正是本片要推翻的决定。
11. **诚实收窄**：不改 loader · 不改 `_template_catalog.py` · 不动 per-tenant file catalog 的 owner-gate（v0.9.6，仍生效）。

## §3 验收
- 负向：canary `_local_catalog.py` / `few_shots.yaml` **不进**构建上下文（真跑 buildx tar）。
- 正向：`_template_catalog.py` / `few_shots.example.yaml` **仍在**。
- 行为：文件缺失时 loader 退到模板（`source_tag == "example"`），不抛。
- 四闸门 + 全量 + DEPLOY 有可照抄的挂载命令。
