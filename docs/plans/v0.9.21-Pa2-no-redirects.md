# v0.9.21（P-a''）—— 出网禁重定向 + 统一出网 helper · **Stage 1**

> ## ⏸ **本片暂缓 —— 前面插一片**（kk 2026-08-11 裁定 C）
>
> Stage 2 发现 **allowlist 能被绕过，且与重定向无关**：门用 `urlparse().hostname`、
> `requests` 用 urllib3，同一串 `http://127.0.0.1:PORT\@api.vendor.corp` 两者给出不同 host
> ⇒ 门放行、请求带凭据直连内网（✅ 执行者逐环复跑，详 `v0.9.21-Pa2-stage2-consolidated.md` §1）。
>
> ⇒ **它是独立且更严重的缺陷**（不需要第二跳），且**本片的禁令对它零作用**
> ⇒ 排在本片**之前**单独一片（`v0.9.21-Pa0-single-url-parser`）。
> ⇒ **本片顺延为 v0.9.22**，且它的 §11 收窄届时可以去掉「解析歧义」那条。
>
> **本文档已按 Stage 2 全部校正 + kk 三条裁定更新**，回来做时直接用。

> 兑现 v0.9.18 明确留下的账：「webhook 的 allowlist **仍只管第一跳**（`requests.post` 未禁重定向）
> ⇒ **不得声称「webhook 出网已受控」**…重定向禁令在 P-a'。」
>
> **kk 2026-08-11 三条裁定**：① `urlopen` → 换 `requests`（统一形态）· ② 做**统一出网 helper** ·
> ③ 重定向策略**先实测再定** ⇒ 已测（§0.3）。

## §0 grounded 测绘（2026-08-11 实测）

### 0.1 ⚠️ 作业面是 **7 处，不是清单里的 4 处**

按「**先解析 import 得到限定名、再找调用**」扫（不是按调用者名字猜）：

| # | 出网点 | 现状 | 干什么 |
|---|---|---|---|
| 1 | `adapters/http/executor.py:116` `requests.get` | ⛔ 无禁令 | HTTP 数据源**查询业务数据** |
| 2 | `adapters/http/executor.py:118` `requests.post` | ⛔ 无禁令 | 同上 |
| 3 | `adapters/notification/lark.py:42` `requests.post` | ⛔ 无禁令 | 取飞书 tenant-token（**带 app_secret**） |
| 4 | `adapters/notification/telegram.py:52` `requests.post` | ⛔ 无禁令 | 发图（**bot token 在 URL 里**） |
| 5 | `adapters/notification/webhook.py:138` `requests.post` | ⛔ 无禁令 | 监控告警（v0.9.18 留的账） |
| 6 | `api/admin/datasources.py:72` `_rq.head` | ✅ **已有** `allow_redirects` | 数据源探活 |
| 7 | `api/admin/or_catalog.py:37` `urllib.request.urlopen` | ⛔ 无禁令 · **形态不同** | 拉 OpenRouter 模型目录 |

⇒ **清单会漂的第四个数据点**：不仅少登记 3 处，连**已经修好的第 6 处也没登记**。

### 0.2 ⚠️ 执行者的探针错了两次（写下来，因为它决定了哨兵该怎么写）

**第一版**按「调用者名字像不像 HTTP 客户端」扫 ⇒ 得 **20 处**：
- **假阳性**：一大半是 `s.get("db_type")` 这类 **dict 访问**（`s` 是 sqlite Row / dict）；
- **假阴性**：漏掉 `import requests as _rq` 的**别名**形态（正是第 6 处）。

⇒ 改成**按 import 解析限定名**才对 —— 这正是本仓 v0.9.4 MF2 学到的那条
（「排除清单打不住，改按 import 解析限定名 ⇒ 零清单」）。
⇒ **本片的哨兵必须用同一口径**，否则它会漏掉下一个 `import requests as _x`。

### 0.3 ⭐ 重定向实测（kk 批准的只读探测：无凭据、无业务数据，只看状态码）

| 目标 | 请求 | 结果 |
|---|---|---|
| 飞书 `tenant_access_token/internal` | 空 body POST（**不带 app_id/secret**） | **200**，无 `Location` |
| Telegram `sendPhoto` | **假 token** `bot000000:AAAA…` | **401**，无 `Location` |

⇒ **禁重定向对这两个官方 API 零功能影响**（实测，非推理）。
⚠️ 第 1/2/5 处打的是**部署方/租户自己配的地址**，本沙箱到不了 ⇒ **测不了**
⇒ kk 裁定：按「**禁 + 可诊断消息**」处理（那些地址本来就只能在真实环境配）。

## §1 ⭐ 四个点**风险不同层**，Stage 1 按层写而不是当成同一件事

| 层 | 点 | 跟随重定向的后果 |
|---|---|---|
| ⭐⭐ **凭据出境** | 3 `lark`（`app_secret` 在 body）· 4 `telegram`（bot token **在 URL 路径里**） | 凭据被送到**未预期主机**。⚠️ Telegram 更狠：token 在 URL 里 ⇒ 重定向时**整条 URL 连 token 一起**进 `Location` 的目标请求 |
| ⭐ **数据出境** | 1/2 `executor` | 业务查询结果 + spec 里的 auth header |
| **内网探测** | 5 `webhook` | allowlist **只管第一跳** ⇒ 302 到内网即绕过（v0.9.18 自陈） |
| （只读元数据） | 7 `or_catalog` | 拉模型目录，风险最低 —— 但它是**形态不同的那个**，正因如此才反复被漏 |

⇒ **本片的价值不只是「加个参数」**：#3/#4 是**凭据**问题，与 #5 的 SSRF 不是同一件事。

## §2 统一出网 helper（kk 裁定做）

### 2.1 ⛔ 承重约束：**helper 绝不能拥有 allowlist 的决定权**

v0.9.18 立过「**两份 allowlist 方向相反、严禁混用**」——
`allowed_http_hosts`（**读**数据源）与 `allowed_webhook_hosts`（**发**告警）。

⚠️ **一个「统一出网 helper」最容易犯的错，就是把它们悄悄合并** ——
那会**回退掉 v0.9.18 修的东西**，而且不会有任何测红（两份名单在多数部署里是空/相同）。

⇒ **设计**：helper 管**所有调用方都相同**的那几件事，**不碰**方向性的那件：

| helper **管** | helper **不管** |
|---|---|
| `allow_redirects=False`（**无默认可覆盖** —— 见 2.2） | **哪份 allowlist** —— 由调用方**显式**传入已校验的 URL |
| 超时（必填，无默认） | 凭据怎么放 |
| 3xx 时的**可诊断错误消息** | 重试策略 |

### 2.2 ⚠️ 禁令必须是**结构性**的，不是「默认值」

若写成 `def request(..., allow_redirects=False)`，调用方一行 `allow_redirects=True` 就能关掉它，
而**没有任何东西会红**。⇒ helper **不接受**这个参数（传了即 `TypeError`），
内部恒 `allow_redirects=False`。
⇒ 配哨兵：`knot/` 内**不得**出现直接的 `requests.*` / `urlopen` 调用（第 6 处一并收编）。

### 2.3 3xx 的处置：**拒绝 + 说清楚**

不静默失败、不静默截断：抛一个明确异常，消息含
「目标返回 3xx（**状态码**），而出网**禁止跟随重定向**；若确需，请把**最终地址**配进来」。
⚠️ **不把 `Location` 的值回显给调用方**（那可能是内网地址 ⇒ #262 同族的泄漏面）——
只记日志。

## §3 `or_catalog` 换形态（kk 裁定 b）

`urllib.request.urlopen` → 走 helper。
**理由不是「统一好看」**：一个能力有两种形态，正是今天让清单和哨兵反复漏项的根因
（§0.1 的第 7 处 + §0.2 的假阴性都是这个）。
⚠️ 换形态**会改变异常类型**（`URLError` → `requests.RequestException`）⇒ 须扫调用方的 `except`。

## §4 验收（每条先答**六问**）

| # | 判据 | 层 | 注入 | 前 → 后 |
|---|---|---|---|---|
| 1 | ⭐ **本地起一个真会 302 的服务**，四个点分别打它 ⇒ **不跟随** | 端到端（真 HTTP） | 让 helper 恒 `allow_redirects=True` | 前：拒绝 + 可诊断消息 → 后：**跟到了第二跳** |
| 2 | ⭐ **302 的目标是「allowlist 外的主机」时，第二跳绝不发生** | 端到端 | 同上 | 直接表达要排除的那个事件（allowlist 只管第一跳） |
| 3 | **凭据不出境**：302 到另一主机时，第二跳**没有**收到 `app_secret` / bot token | 端到端 | 同上 | ⚠️ 判据锚在**第二跳收到了什么**，不是「有没有报错」 |
| 4 | 哨兵：`knot/` 内零直接 `requests.*` / `urlopen` | 元（AST，按 import 解析限定名） | 任一处改回直调 | 前：绿 → 后：红并点名 `file:line` |
| 5 | 哨兵：helper **不接受** `allow_redirects` 参数 | 元 | 给它加上这个参数 | 防「禁令退化成可覆盖的默认值」 |
| 6 | ⭐ **两份 allowlist 没被合并** | 单元 | 让 helper 自己查 allowlist | 前：webhook 与数据源各用各的 → 后：混用 ⇒ 红 |
| 7 | 正对照：**不重定向的目标照常工作** | 端到端 | 无（刻意） | 防「一律拒绝」式假通过 |

⚠️ **#1~#3 必须起真实 HTTP 服务**（本地 `http.server`），不能 mock —— 
「跟不跟随重定向」是 `requests` 的**运行时行为**，mock 掉它就等于在测我自己写的假货。

## §5 v3.1-B 枚举（命中：出网 · 凭据）

1. **fail-open 面**：禁令写成可覆盖的默认值 = fail-open（§2.2 已结构性封死）。
   ⭐ **1 → N 自查**：helper 让出网点从「各写各的」变成「都走一处」——
   **凡依赖「自己那处的特殊写法」的地方都要重扫**（如 `datasources.py:72` 已有的 flag、
   `or_catalog` 的 `URLError` 捕获）。
2. **oracle**：#1~#3 起**真** 302 服务 ⇒ 注入真能产生「第二跳发生」。
3. **「那一行」/ 入口**：禁令在 helper 内部**唯一**一行；入口由 #4 哨兵封死（含别名形态）。
5. **散文规则无守护？** —— v0.9.18 那句「allowlist 只管第一跳」此前**只在 CHANGELOG 里**，本片给它守护。
6. **声明 vs 生产者**：helper 有 7 个消费者（全部出网点）。
7. **顶班**：⚠️ 若某处本来就打不通（超时/连不上），禁令的效果与「本来就失败」不可区分
   ⇒ #7 正对照必须存在。
8. **既有测的绿是真的吗**：`datasources.py:72` 已有 flag ⇒ 它的既有测**不会**因本片变红，
   但收编进 helper 后要确认它守的还是同一件事。
9. **契约冲突**：`adapters` 不得 import `repositories`/`services` ⇒ helper 落 `adapters/http/`。
10. **策略题的影子**：「某地址确需重定向怎么办」= 运维配最终地址（kk 已裁），不是代码策略。
11. **诚实收窄**：本片**不声称** —— SQL 数据源出网受控（**仍零 allowlist**，是代偿门守的三条之一）·
    DNS rebinding / TOCTOU（allowlist 校验主机名与实际连接之间仍有窗口）·
    第二家公司可以进来（代偿门仍在）。

## §6 版本 / 协议

**v0.9.21（PATCH）**。评审：本片**动 5 个文件的生产码 + 换一处形态** ⇒ 建议走 lens（Stage 2）。

## §7 kk 裁定（2026-08-11）

| # | 裁定 | 落点 |
|---|---|---|
| 1 | ⭐ **新文件 `adapters/http/outbound.py`** | `url_allowlist` 管「**能不能去**」，helper 管「**怎么去**」—— 两件事不混住一个文件 |
| 2 | ⭐ **`datasources.py:72` 一并收编**（虽已有禁令） | 留一处例外 ⇒ 哨兵就得留一条豁免，而**豁免会长出第二条**（本仓 v0.9.18 `_REDACTED_IN_AUDIT` 的原话） |
| 3 | ⭐ **走 lens**（Stage 2） | 本片动 5 个文件生产码 + 换一处形态 |

⇒ **收编后作业面 = 7 处全部走 helper，哨兵零豁免。**

## §7-2 ⭐ Stage 2 后的三条裁定（kk 2026-08-11）+ 由此定死的设计

| # | 裁定 | 由此定死的东西 |
|---|---|---|
| 1 | **解析歧义单开一片、排在本片之前** | 本片顺延 v0.9.22；§11 届时去掉「解析歧义」那条收窄 |
| 2 | ⭐ **helper 内部走 `requests.post/get/head` 模块属性查找** | ⛔ **不得**用 `Session()` 或 `requests.request(...)` —— 否则 `no_network` 探针（只 patch 那三个模块属性）与约 20 处 `monkeypatch.setattr(requests,'post',…)` **静默命中 0 次**，而其中 3 条是**反向断言** ⇒ 静默恒真。<br>⇒ 代价：**设不了 `trust_env=False`** ⇒ 代理面改为**响亮说出来**（启动期 WARN：检测到 `*_PROXY` / `~/.netrc` 时告警「门校验的 host 与实际连接目标可能不同」）。<br>⇒ 理由：「探针失效」是**静默**的，代理面**可以被喊出来**。<br>⇒ 配一条测：`no_network` 下调 helper **必须**被探针捕获。 |
| 3 | ⭐ **helper 返回 `Response`，恒不跟随重定向；「3xx 算不算失败」由各调用点按语义决定** | 解掉 §5-① 那个 P0（探活点「任何 HTTP 响应 = alive」不会翻转成 `error`）。<br>⚠️ **代价**：禁令只保证「**不跟随**」，不保证「**拒绝**」⇒ `webhook.py:143` 的 `raise_for_status()` 对 302 **不抛** ⇒ 会把 302 **当成功**。<br>⇒ **必须配哨兵**：凡调 helper 的地方，**要么显式判 3xx，要么在调用处写明「本点 3xx 合法」的理由**（探活点就是后者）。 |

### §7-3 其余 Stage 2 校正（回来做时直接照这份）

- **哨兵口径改掉**（kk 的「零豁免」做不到 —— `outbound.py` 自己必须调 `requests`）：
  从「`knot/` 内零直调」改为「**恰好一个文件**（`adapters/http/outbound.py`）可持有客户端调用」，
  复用现成先例 `test_file_catalog_owner_gate.py::test_no_second_http_client_in_http_adapter`
  的 banned-modules 口径，**作用域从 `adapters/http/**` 抬到整个 `knot/`**，
  并把口径**扩到** `httpx` / `urllib3` / `http.client` / `aiohttp`。
- **§4 #5 必须是运行时判据**，不能是 AST 读签名 —— `**kw` 转发下签名级判据**恒绿**。
- ⭐ **§4 #3 整条重写**（原版是假绿）：三个点各自的状态码 / 头名 / Location 形状：
  `lark` 用 **307**（body 才过去）· `executor` 用 **非 `Authorization` 的头名**（自定义头不被剥）·
  `telegram` 的 `Location` 必须**保路径**。
- **§4 #6 的注入恒定** ⇒ 判据改行为级，且**三份** allowlist 显式配成互不相交
  （第三份是 `im_egress.IM_ALLOWED_HOSTS`，**硬编 frozenset**，文档原先没提）。
- **§4 #2 合并进 #1**（几乎同一条测），oracle 用「hop 数 + hop1 的 Host」。
- **必须处置的既有测 4 条**：`test_all_outbound_requests_calls_live_inside_execute`
  （helper 落 `outbound.py` 后必然违反 ⇒ **改名 + 重写理由，不是删**）·
  `test_model_catalog.py:114`（patch `urlopen` 会变空操作）·
  `test_webhook_notification.py:84/127`（stub 签名无 `**k` ⇒ 多传参即 `TypeError`）·
  `test_datasource_probe_tenant_ctx.py:104`（patch `requests.head` ⇒ 收编后失效）。
- **`urlopen` → `requests` 必须补状态码分流**：`urlopen` 对 500 抛 `HTTPError`、`requests` **不抛**
  ⇒ 否则 or_catalog 的错误 JSON 会被 upsert 进表（fail-closed → fail-open）。
- ⭐ **helper 的 3xx 异常（若某点自己抛）必须继承 `requests.RequestException`**：
  实测 `issubclass(requests.RequestException, URLError)` = **False**，
  且 `telegram.py:59` 的 token 抹除**依赖** catch `RequestException`
  ⇒ 自定义异常会让 **bot token 进 traceback**。
- **helper 签名必须容纳** `data=` + `files=`（multipart）与 `params=`，**且返回 Response**。
- **§11 补三条不声称**：LLM SDK 出网（openai/anthropic 底层 httpx，实测 `follow_redirects=True`，
  带 API key + 完整 prompt；租户不可控 ⇒ 非跨租户面但**是出境面**。
  ⚠️ **A 数 9 处 / B 数 6 处，口径不同、执行者未逐处核** ⇒ 落地前自己扫定）·
  **端口未校验**（allowlist 只比 hostname）· 顺带修 `or_catalog` 那个漂了 30+ PATCH 的 UA 版本字面。

## §8 Stage 2 输入边界（v3.1-A）

给评审者的**只有**：① 本文档 ② 仓库现状。**不给**执行者的探索过程与自辩。
本片 Stage 1 = 0 代码 ⇒ 这一轮只有「包 1」；实施记录作包 2，
须在评审者**交出初步发现之后**才送（v0.9.8 立的排序）。
