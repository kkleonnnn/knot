# v0.5.22 — (C5+) admin tab_system 屏复刻（Catalog）— Stage 1 草案

> **状态**：📝 **Stage 1 草案**（待 Stage 2 资深+Codex 评审 / Stage 3 v0.4 守护者终审）
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **里程碑**：**v0.5.x 第三个 admin tab 子模块复刻** + **Inset 8% 闭环字面第九处扩张**（文件总数 7 → 8）+ **borderLeft 25% 第四处闭环**（设计语言铁律第二维度持续扩张）+ **R-484 'white' 字面 + 蓝色 hex 双残留偿还**（`rgba(43,127,255,0.12)` + `#2B7FFF` + `color="#fff"`）

---

## 0. 关键事实（Stage 1 起手陈述）

**Demo 与现实业务模型相容** — tab_system 视觉重构 + 业务字段 byte-equal 双轨：

| 维度 | Demo catalog.jsx (133 行) | 现实 tab_system.jsx (53 行) |
|---|---|---|
| 业务 | 静态 mock (3 pre code blocks) | 真后端 (3 textarea CRUD + DB overrides + reset/save) |
| Helper banner | brand inset card + info icon | plain text |
| Section header | 22×22 number chip + title + Tag source chip | label + DB 覆盖中 chip + 恢复默认 + 保存 |
| Section body | pre code block (read-only) | **textarea (editable — 业务必须保留)** |

**裁定**：D2 双兼模式延伸 — 保 textarea 业务 + 借 demo Helper banner brand inset + Section header number chip 视觉风格。

---

## 1. 决议表（D1-D6 + Q1-Q5 草案 — 待 Stage 2/3 终审）

| ID | 推荐 | 备注 |
|---|---|---|
| **D1** 范围 | A 整个 tab_system.jsx | 单 sub-tab (Catalog)；不动 Admin.jsx 内 props 传递 |
| **D2** Helper banner brand inset | A 升级 | bg `color-mix(in oklch, ${T.accent} 8%, transparent)`（**R-480 闭环字面第九处扩张** — 文件总数 7 → 8）+ borderLeft 3px 25%（**R-481 第四处闭环扩张**）+ info svg icon |
| **D3** Section header number chip | A 升级 | 22×22 brandSoft 8% + mono + fontWeight 600 + ①②③ → 01/02/03 数字 + 与 Tag source chip 配合 |
| **D4** Section radius | A 10→12（与 demo 一致） | 与 v0.5.21 Card radius 12 一致 |
| **D5** Hex 双残留偿还 | A 全清 | `rgba(43,127,255,0.12)` → `color-mix(in oklch, ${T.accent} 12%, transparent)` + `#2B7FFF` → `T.accent` + `color="#fff"` Spinner → `T.sendFg`（R-484 sustained 严禁 'white' 字面）|
| **D6** textarea 业务保留 | A sustained | 业务必须 textarea CRUD；不改 pre code block；3 sections（tables/lexicon/business_rules）保 textarea |
| **Q1** TabSystem 6 props 签名 byte-equal | A 严守 | T / catalog / setCatalog / catalogSaving / onSaveCatalogField / onResetCatalogField |
| **Q2** catalog 5 字段 byte-equal | A 严守 | source / tables / lexicon / business_rules / overrides |
| **Q3** 3 sections keys byte-equal | A 严守 | tables / lexicon / business_rules（业务字段不动） |
| **Q4** catalog.overrides?.[key] 业务逻辑 byte-equal | A 严守 | DB 覆盖中 chip 显示 + 恢复默认 button 显示（业务字段不动）|
| **Q5** v0.6 Shared 移植承诺 | A sustained | Section number chip helper inline 第七次复用 — v0.6.0 移入 Shared |

---

## 2. 红线草案（R-551 ~ R-580，30 条）

### 契约守护（R-551 ~ R-555）

| ID | 内容 |
|---|---|
| **R-551** | `TabSystem` 6 props 签名 byte-equal — T / catalog / setCatalog / catalogSaving / onSaveCatalogField / onResetCatalogField |
| **R-552** | catalog 5 字段 byte-equal — source / tables / lexicon / business_rules / overrides |
| **R-553** | 3 sections keys byte-equal — `tables` / `lexicon` / `business_rules`（与 catalog 字段名一致） |
| **R-554** | `catalog.overrides?.[key]` 业务逻辑 byte-equal — DB 覆盖中 chip 显示 + 恢复默认 button 显示 |
| **R-555** | pillBtn from Shared.jsx + Spinner from utils.jsx 调用 byte-equal（R-365 sustained） |

### 视觉重构（R-556 ~ R-565）

| ID | 内容 |
|---|---|
| **R-556** | **Helper banner brand inset 升级** — bg `color-mix(in oklch, ${T.accent} 8%, transparent)`（**R-480 闭环字面第九处扩张**）+ borderLeft 3px `color-mix(in oklch, ${T.accent} 25%, transparent)`（**R-481 第四处闭环扩张**）+ info svg icon 14×14 + radius 8 |
| **R-557** | **Section number chip 22×22** — brandSoft 8% bg + T.accent color + mono + fontWeight 600 + fontSize 11 + ①②③ → 01/02/03 数字（letterSpacing 0.04em）|
| **R-558** | **Section header layout** — flex + gap 12 + number chip + title + Tag source chip + flex spacer + actions |
| **R-559** | Section radius 10 → 12 + padding 升级（与 v0.5.21 Card radius 12 一致） |
| **R-560** | **DB 覆盖中 chip 视觉升级** — hex 全清 → brandSoft 8% bg + T.accent color + padding 2 8 + radius 999 + fontSize 11 + mono + uppercase 装饰豁免（与 R-227.5.1 sustained） |
| **R-561** | textarea 视觉 byte-equal — minHeight key === 'business_rules' ? 220 : 180 + resize vertical + T.inputBg + T.mono/sans + border radius 7 |
| **R-562** | helper text byte-equal — 11.5px + T.muted + marginBottom 8 |
| **R-563** | actions flex right — 恢复默认 ghost btn + 保存 primary btn + 间距 gap 8 |
| **R-564** | `Spinner color="#fff"` → `color={T.sendFg}`（R-484 sustained — 严禁 'white' 字面） |
| **R-565** | Helper banner 文案 byte-equal — "业务目录注入到 schema 检索 + 3 个 Agent prompt..." 字面完全一致（业务文案不动） |

### Hex 偿还 + 通用约束（R-566 ~ R-571）

| ID | 内容 |
|---|---|
| **R-566** | `rgba(43,127,255,0.12)` → `color-mix(in oklch, ${T.accent} 12%, transparent)`（DB 覆盖中 chip bg）|
| **R-567** | `#2B7FFF` → `T.accent`（DB 覆盖中 chip color）|
| **R-568** | grep `#[0-9a-fA-F]{3,6}` tab_system.jsx \| grep -v boxShadow \| grep -v rgba = 0 命中（R-518 sustained）|
| **R-569** | grep `'#fff'\|"#fff"\|#2B7FFF\|2B7FFF` = 0 命中 |
| **R-570** | grep `rgba\(43,127,255` = 0 命中（蓝色 hex 残留偿还） |
| **R-571** | **R-480 闭环字面文件总数 7 → 8 进攻性扩张第九处** — git grep -F 命中 ResultBlock/SavedReports/admin/tab_access/AdminAudit/AdminBudgets/AdminRecovery/admin/tab_resources/**admin/tab_system** 8 文件 |

### 范围 + 契约（R-572 ~ R-580）

| ID | 内容 |
|---|---|
| **R-572** | **R-481 borderLeft 25% 第四处闭环扩张** — `borderLeft: \`3px solid color-mix(in oklch, ${T.accent} 25%, transparent)\`` 字面 byte-equal SavedReports R-356 + AdminBudgets R-465 + AdminRecovery R-481 + **tab_system R-572**（4 文件命中） |
| **R-573** | R-495 字面 byte-equal 严防 sustained — git grep -F 字面无空格/逗号差异 |
| **R-574** | R-550 borderBottom byte-equal sustained — `1px solid ${T.border}` 字面与 admin/* 子模块一致 |
| **R-575** | App/api/index.css/main/utils/Shared/Shell/decor/18 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery/tab_access/tab_resources 0 改 |
| **R-576** | admin/ 其他 3 子模块（tab_access/tab_knowledge/tab_resources/modals）0 改 + chat/ 7 子模块 0 改 |
| **R-577** | App.css 0 行 diff + tab_system 0 新 className |
| **R-578** | KnotLogo R-199.5/222 sustained 仅 Shared+Login+Shell 三文件 |
| **R-579** | 三处版本同步 0.5.21 → 0.5.22 + LIMITS dict 不动（53 → ≤ 120 远低于 250） |
| **R-580** | **R-548 sustained**：Admin.jsx 内 `<TabSystem` 挂载点 0 改（R-548 核爆级 props 守护扩展）|

---

## 3. 子步骤执行顺序（5 步小型 PATCH）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + **R-580 起手前置签名核爆守护**（grep TabSystem 签名 + Admin.jsx 挂载点）+ **R-556 Helper banner brand inset 字面落地**（R-480 第九处 + R-481 第四处）+ **R-568 hex 余效 grep 立即执行** | R-556/R-568/R-571/R-580 |
| 2 | Section header number chip 22×22 brandSoft + ①②③ → 01/02/03 + Tag source chip + actions 右对齐 | R-557/R-558/R-563 |
| 3 | Section radius 10 → 12 + padding 升级 + textarea 视觉 byte-equal + helper text byte-equal | R-559/R-561/R-562 |
| 4 | **R-560/R-566/R-567 DB 覆盖中 chip 视觉升级 + Hex 双残留偿还** — `rgba(43,127,255,0.12)` → color-mix + `#2B7FFF` → T.accent + chip 视觉对齐 | R-560/R-566/R-567 |
| 5 | **R-564 Spinner #fff → T.sendFg** + 三处版本同步 + R-573 字面严防 + 全 hex grep 闭门（R-568/R-569/R-570）+ **R-571 8 文件验证** + **R-572 borderLeft 25% 4 文件验证** | R-564/R-568~571/R-572/R-573/R-579 |

---

## 4. 关键代码意图（提前样化）

### R-556 Helper banner brand inset（R-480 闭环第九处 + R-481 第四处闭环）

```jsx
<div style={{
  display: 'flex', alignItems: 'flex-start', gap: 10,
  padding: '12px 14px',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // R-480 第九处
  border: `1px solid ${T.border}`,
  borderLeft: `3px solid color-mix(in oklch, ${T.accent} 25%, transparent)`,  // R-481 第四处
  borderRadius: 8,
  fontSize: 12, color: T.subtext, lineHeight: 1.55, marginBottom: 16,
}}>
  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke={T.accent} strokeWidth="1.8" style={{ flexShrink: 0, marginTop: 2 }}>
    <circle cx="12" cy="12" r="10"/>
    <line x1="12" y1="16" x2="12" y2="12"/>
    <line x1="12" y1="8" x2="12.01" y2="8"/>
  </svg>
  <div>
    业务目录注入到 schema 检索 + 3 个 Agent prompt。优先级：DB（本面板编辑）→ 仓库 ohx_catalog.py（部署方填）→ ohx_catalog.example.py（仓库默认）。
    当前生效来源：<b style={{ color: T.text }}>{catalog.source || '...'}</b>。任一字段保存即覆盖默认；点"恢复默认"清空 DB 覆盖。
  </div>
</div>
```

### R-557/R-558 Section header — number chip 22×22 + title + actions

```jsx
<div style={{
  display: 'flex', alignItems: 'center', gap: 12,
  padding: '14px 18px', borderBottom: `1px solid ${T.border}`,
}}>
  <span style={{
    width: 22, height: 22, borderRadius: 5,
    background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // R-480 闭环
    color: T.accent,
    fontSize: 11, fontFamily: T.mono, fontWeight: 600,
    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
    letterSpacing: '0.04em',
  }}>{num}</span>
  <div style={{ fontSize: 14, fontWeight: 600, color: T.text, letterSpacing: '-0.01em' }}>{title}</div>
  <div style={{ flex: 1 }}/>
  {/* DB 覆盖中 chip + 恢复默认 + 保存 */}
</div>
```

### R-560/R-566/R-567 DB 覆盖中 chip 视觉升级

```jsx
// 前
<span style={{
  marginLeft: 8, fontSize: 10.5, padding: '2px 7px', borderRadius: 999,
  background: 'rgba(43,127,255,0.12)', color: '#2B7FFF', fontWeight: 600,
}}>DB 覆盖中</span>

// 后
<span style={{
  fontSize: 10.5, padding: '2px 8px', borderRadius: 999,
  background: `color-mix(in oklch, ${T.accent} 12%, transparent)`,  // R-566
  color: T.accent,  // R-567
  fontWeight: 600, fontFamily: T.mono, textTransform: 'uppercase', letterSpacing: '0.02em',
}}>DB 覆盖中</span>
```

### R-564 Spinner color hex 偿还

```jsx
// 前: {catalogSaving[key] ? <><Spinner size={10} color="#fff"/> 保存中</> : '保存'}
// 后: {catalogSaving[key] ? <><Spinner size={10} color={T.sendFg}/> 保存中</> : '保存'}
```

---

## 5. 文件级改动清单（草案）

```
frontend/src/screens/admin/tab_system.jsx  [EDIT 53 → ≤ 120 行]
                                           Step 1: baseline + R-580 props 签名核爆守护 + R-556 Helper banner R-480/R-481 闭环字面 + R-568 hex 余效 grep
                                           Step 2: Section number chip 22×22 brandSoft + ①②③→01/02/03 + Tag source + actions 右对齐
                                           Step 3: Section radius 10→12 + padding 升级 + textarea 视觉 byte-equal + helper text byte-equal
                                           Step 4: DB 覆盖中 chip 视觉升级 + R-566/R-567 hex 双残留偿还（rgba/2B7FFF → color-mix/T.accent）
                                           Step 5: R-564 Spinner #fff→T.sendFg + 三处版本同步
                                                   + 全 hex grep 闭门 + R-571 8 文件验证 + R-572 borderLeft 4 文件验证
                                           守护: 6 props + catalog 5 字段 + 3 sections keys + overrides 业务逻辑 byte-equal [R-551~R-554]
                                                 + pillBtn/Spinner from Shared/utils 调用 byte-equal [R-555 R-365 sustained]
                                                 + inline styles only [R-577 CSS 0 污染]
                                                 + Admin.jsx 内 <TabSystem 挂载点 0 改 [R-580 R-548 sustained]

knot/main.py                            [EDIT version 0.5.21 → 0.5.22]  [R-579]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.21 → 0.5.22]  [R-579]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.21 → 0.5.22]  [R-579]
scripts/check_file_sizes.py             [unchanged]  R-579 LIMITS dict 不动（tab_system 53→≤120 远低于 250）

frontend/src/screens/admin/{tab_access,tab_knowledge,tab_resources,modals}.jsx  [unchanged]  [R-576]
frontend/src/screens/chat/*             [unchanged]  [R-576]
frontend/src/screens/Admin.jsx          [unchanged]  [R-575 + R-580 Admin.jsx 挂载点不动]
frontend/src/screens/{SavedReports,AdminAudit,AdminBudgets,AdminRecovery}.jsx [unchanged]  [R-575]
frontend/src/screens/ (其余 13 屏)       [unchanged]  [R-575]
frontend/src/{App,api,index.css,main,utils,Shared,Shell}.jsx 等  [unchanged]  [R-575 + R-365 sustained]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]  [R-575]
frontend/src/App.css                    [unchanged 0 行 diff]  [R-577]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js  [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.22 段]
                                        - R-551~R-580 偿还表
                                        - ⭐ Inset 8% 铁律进攻性扩张第九处（文件总数 7 → 8；tab_system 加入）
                                        - borderLeft 25% 第四处闭环扩张（设计语言铁律第二维度持续 — 4 文件 byte-equal）
                                        - v0.5.x 第三个 admin tab 子模块复刻（v0.5.16 tab_access + v0.5.21 tab_resources + v0.5.22 tab_system）
                                        - R-484 'white' 字面 + R-286 蓝色 hex 双残留偿还（rgba(43,127,255,0.12) + #2B7FFF + color #fff）
                                        - R-580 R-548 核爆级 props 守护扩展（TabSystem 6 props + Admin.jsx 挂载点）
                                        - Section number chip helper inline 第七次复用 sustained — v0.6 Shared 移植承诺加强
                                        - Loop Protocol v3 第 23 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.22
                                        - v0.5.x 路线图 v0.5.22 ⏳ → ✅
                                        - v0.5.23+ 行更新（剩余 4 屏：tab_knowledge 3 sub-tabs + modals）
docs/plans/v0.5.22-admin-system.md      [当前文件 Stage 1 → LOCKED 后存档]
```

---

## 6. 验收清单（草案）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（CI 干净 env）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS 不变）
- [ ] `npm run build` 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.22**

### Stage 2/3 强化验证

- [ ] **⭐ R-571 闭环字面文件总数 7 → 8**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — **8 文件命中**
- [ ] **R-572 borderLeft 25% 4 文件**：`git grep -F "3px solid color-mix(in oklch, \${T.accent} 25%, transparent)" frontend/src/screens/` — **4 文件命中**（SavedReports + AdminBudgets + AdminRecovery + **tab_system**）
- [ ] **R-568 hex 余效**：`grep -rE "#[0-9a-fA-F]{3,6}" tab_system.jsx | grep -v boxShadow | grep -v rgba` = 0
- [ ] **R-569 '#fff' + #2B7FFF 0 命中**：grep `'#fff'\|"#fff"\|2B7FFF` = 0
- [ ] **R-570 rgba(43,127,255 0 命中**
- [ ] **R-573 字面严防 sustained**：git grep -F 字面 byte-equal 无空格/逗号差异
- [ ] **R-580 核爆守护**：`git diff origin/main HEAD -- tab_system.jsx | grep "^[-+].*export function"` 签名行 byte-equal；Admin.jsx 内 `<TabSystem` 挂载点 byte-equal

### 契约守护（首要红线）

- [ ] **R-551**：TabSystem 6 props 签名 byte-equal — T/catalog/setCatalog/catalogSaving/onSaveCatalogField/onResetCatalogField
- [ ] **R-552**：catalog 5 字段 byte-equal（source/tables/lexicon/business_rules/overrides）
- [ ] **R-553**：3 sections keys byte-equal（tables / lexicon / business_rules）
- [ ] **R-554**：catalog.overrides?.[key] 业务逻辑 byte-equal
- [ ] **R-555**：pillBtn/Spinner 调用 byte-equal（R-365 sustained）

### 范围守护

- [ ] **R-575**：App/api/index.css/main/utils/Shared/Shell/decor/18 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery/tab_access/tab_resources `git diff` = 0
- [ ] **R-576**：admin/ 其他 3 子模块 + modals + chat/ 7 子模块 `git diff` = 0
- [ ] **R-577**：App.css 0 行 diff + tab_system 0 新 className
- [ ] **R-578**：KnotLogo sustained 仅 3 文件

### 端到端业务回归

- [ ] 进 admin → 切 catalog tab → tab_system 加载
- [ ] **Helper banner R-480/R-481 双闭环**（brand inset bg + borderLeft + info svg + DB→仓库→example 文案 + 当前生效来源 highlight）
- [ ] **3 Sections**：表目录 / 业务词典 / 业务规则
- [ ] **Section header**：number chip 01/02/03 + Title + DB 覆盖中 chip（R-560 mono uppercase）+ 恢复默认 + 保存
- [ ] **textarea CRUD**：3 sections textarea 编辑 + 保存 + 恢复默认
- [ ] **R-564 Spinner 偿还**：保存按钮 loading T.sendFg 视觉清晰
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）
- [ ] **light + dark 双模式**
- [ ] **⭐ Inset 8% 8 文件视觉一致性肉眼校验**（v0.5.14~21 + tab_system）
- [ ] **⭐ borderLeft 25% 4 文件视觉一致性**（SavedReports Quote / AdminBudgets Rules / AdminRecovery Rules / tab_system Helper banner）
- [ ] v0.5.0~v0.5.21 既有功能端到端正常

---

## 7. Commit 序列（草案 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.22): tab_system.jsx 视觉重构 — Inset 8% 第九处扩张（7→8 文件）+ borderLeft 25% 第四处闭环 + Hex 双残留偿还 (R-551~R-580)` | npm build + R-571 8 文件验证 + R-572 4 文件验证 + R-568 hex 0 + R-580 核爆守护 + 端到端业务 |
| 2 | `chore(v0.5.22): version bump 0.5.21→0.5.22 三处同步 (R-579) — LIMITS dict 不动` | 全闸门 + version=0.5.22 |
| 3 | `docs(v0.5.22): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + Inset 8% 第九处扩张 + borderLeft 25% 第四处闭环 + Section chip v0.6 Shared 移植` | 全闸门 |

---

## 8. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审待启动（**第 23 次施行**预计）
- ✅ § Visual Replication Protocol 适用（v0.5.8 立约）
- ⏳ 等 Stage 2 资深 + Codex 评审 → Stage 3 v0.4 守护者终审 → LOCKED
- ✅ **v0.5.x 第三个 admin tab 子模块复刻**（v0.5.16 tab_access + v0.5.21 tab_resources + v0.5.22 tab_system）
- ✅ **Inset 8% 闭环字面第九处扩张**（文件总数 7 → 8；tab_system 加入）
- ✅ **borderLeft 25% 第四处闭环扩张**（4 文件 byte-equal — SavedReports + AdminBudgets + AdminRecovery + tab_system；设计语言铁律第二维度持续）
- ✅ **R-484 'white' 字面 + R-286 蓝色 hex 双残留偿还**（rgba/2B7FFF/#fff 三 hex 清零）
- ✅ **R-580 R-548 核爆级 props 守护扩展**（TabSystem 6 props + Admin.jsx 挂载点）
- ✅ **R-365 Shared 0 改动 sustained** — pillBtn / Spinner from Shared/utils 调用 byte-equal
- ✅ **R-520 业务字段不动 sustained** — 6 props + catalog 5 字段 + 3 sections keys + overrides 业务逻辑
- ✅ **D2 双兼模式延伸** — 保 textarea 业务 + 借 demo Helper banner brand inset
- ✅ Section number chip + Helper banner svg info inline 第七次复用 sustained — v0.6 Shared 移植承诺加强
- ✅ R-227.5.1 装饰豁免延伸（DB 覆盖中 chip mono uppercase + number chip mono）
- ✅ R-302.5 emoji 业务豁免 sustained（本 PATCH 无 emoji）
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.22 十 PATCH sustained）
- ✅ R-313 rgba 豁免边界 sustained（本 PATCH 无新增 rgba 豁免 — rgba(43,127,255,0.12) 在 R-566 偿还范围）
- ✅ 范围最小化 — admin/ 其他 3 子模块 + chat/ 7 子模块 + 8 核心非屏 + 18 屏 byte-equal

---

## 9. 风险点 + Stage 2/3 评审建议方向

### 高风险（建议 Stage 2/3 重点关注）

1. **R-551 TabSystem 6 props 签名 byte-equal** — Admin.jsx 内 `<TabSystem` 调用方 byte-equal（R-580 sustained）
2. **R-552/R-553/R-554 业务字段** — catalog 5 字段 + 3 sections keys + overrides 业务逻辑严守
3. **R-566/R-567/R-570 蓝色 hex 双残留** — `rgba(43,127,255,0.12)` + `#2B7FFF` 是 v0.5.16~21 唯一未被偿还的 hex 残留（DB 覆盖中 chip）

### 中风险

4. **D2 Helper banner brand inset** — R-480 第九处 + R-481 第四处闭环扩张；守护者可能质疑两处闭环同时扩张的连贯性？反驳：banner 是设计语言铁律双维度典型应用场景
5. **D3 Section number chip** — 22×22 brandSoft 是新视觉模式；与 v0.5.17/19/20 Avatar 22×22 brandSoft 字面 byte-equal 但 borderRadius 5 vs 50%（语义不同）
6. **D6 textarea 业务保留** — 与 demo pre code block 风格冲突；D2 双兼模式延伸合理化

### 低风险

7. **D4 Section radius 10→12** — 与 v0.5.21 Card radius 12 一致
8. **D5 Hex 偿还 + R-564 Spinner** — v0.5.20/21 模式 sustained

---

📝 **Stage 1 草案完成 — 等待资深架构师 ack 启动 Stage 2/3 评审或简化协议直接 Stage 4。**

> **简化协议候选提示**：本 PATCH 红线 30 + 模式成熟（v0.5.16~21 thead R-480 闭环 + Hex 偿还 + 核爆 props）+ scope 小（53 → ≤ 120）+ 决策无争议 — **可考虑 v0.5.8 简化协议**，但 R-571 第九处扩张 + R-572 第四处闭环 + R-566/R-567/R-570 蓝色 hex 双残留偿还 是设计语言铁律双维度同时扩张，建议守护者评审。资深架构师决定。
