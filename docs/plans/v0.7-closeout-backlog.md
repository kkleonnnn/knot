# v0.7 收尾 backlog — 迭代 0.8 前清零（代码美学 / 无 bug / 高完整度）

> 来源：v0.7.30 全面代码评估（14 agent grounded + 对抗核验）。目标（kk）：**v0.8 前无遗留** + 代码美学 + 功能无 bug + 高完整度。
> 分 6 批，按「风险×收益 / 是否 gated」排序。每批 = 1~N 个 PATCH（走简化协议 or 完整 v3 视是否碰业务逻辑）。语义层激活项（B6）**gated on kk 的 parser 命中率验证**（见 v0.7.30-semantic-validation-runbook.md）。

---

## B1 · 前端韧性 + 真 bug 收敛（🔴 最高优先，do-now，不需部署）

| # | 项 | 来源 | scope | 协议 |
|---|---|---|---|---|
| ✅ B1.1 **(v0.7.33 DONE)** | **App.jsx 加 React ErrorBoundary**（class + getDerivedStateFromError + 降级屏 + error_reporter 上报）；ResultBlock 单独再包一层 —— AppErrorBoundary（main.jsx 包 `<App/>` 全屏降级）+ ResultBlockErrorBoundary（Conversation 仅包 ResultBlock inline 降级）；守护者 R1 App fallback 纯 inline bulletproof；error_reporter +`reportError` 复用节流 | P3 high **confirmed**（历史多次白屏 v0.6.5.2）| 前端 | ✅ 轻量 v3 |
| ✅ B1.2 **(v0.7.35 DONE)** | **SSE live + 同步 /query 按 role 脱敏** —— `scrub_query_payload` 三路单一真相源（pop sql 顶层+嵌套 output + desensitize `_LEAK_TEXT_FIELDS` 含 clarifier 字段 + details walk）+ SSE emit choke-point + sql_step 非 admin suppress + **同步 /query use_agent 旁路封堵**（对抗 review 3/3 REVISE 抓出）+ lexicon 源改 per-user active catalog + get_messages 扩 error/insight。admin byte-equal。tests +13 | P4 medium（信息暴露）| 后端 query.py/desensitize.py/conversations.py | ✅ 轻量 v3 + 对抗 review |
| ✅ B1.3 **(v0.7.34 DONE)** | **3 env 测试隔离修复** —— `KNOT_SKIP_DOTENV` flag（settings.py+deps.py 截断 .env override=False 回读缺失 key，生产默认不设）+ R45 subprocess 空 tmp DB + no-key 测试 mock `get_app_setting`→"" 隔离 DB OR key。**本地全套件首次全绿 1022 passed / 0 failed** + no-key 不再烧 live token | P8 medium（测试非 hermetic）| tests + 2 config 行 | ✅ 简化 |

**done**：整屏白屏爆炸半径收敛到单屏可恢复；非 admin 看不到真实表名；本地全套件 0 env-fail（不再烧 token）。

## B2 · 数据层规模化偿还（before 公测）

| # | 项 | 来源 | scope |
|---|---|---|---|
| B2.1 | **两条热路径补索引**：idx_messages_conv(conversation_id, created_at) + idx_conversations_user(user_id)；_check_conv_owner 改单行查询 | P6 medium | schema + 迁移 |
| B2.2 | **sync SQL 卸载出事件循环**（anyio.to_thread.run_sync，threadpool=32 已预留）+ **Doris statement timeout** | P5 medium overstated（可用性非灾难）| query/execute_query + engine |
| B2.3 | **迁移版本化**：PRAGMA user_version 或轻量 schema_migrations 表，替代 base.py 274 行无版本幂等 ALTER 堆积；_migrate_uploads_db_once 失败改 logger.error 可观测 | 数据层评估 | base.py 重构 |

**done**：老用户数万消息不线性变慢；单条慢查询不阻塞同伴 SSE；迁移每个只跑一次、可观测。

## B3 · 代码美学 / 消歧重构（0 行为变更 —— 直击「代码美学」）

| # | 项 | 来源 | scope |
|---|---|---|---|
| ✅ B3.1 **(v0.7.38 DONE)** | **12 孤儿 dataclass 标 [BLUEPRINT-ONLY]**（backlog「13」订正 12 — Metric/AgentResult/AgentStep 已 v0.7.26/.28 删）：grounded 确认 0 外部实例化 = Go 重写契约锚点（repo 返 dict），在 `models/__init__.py` __all__ 标注 + docstring pattern 说明「一眼分清活契约 vs 蓝图」；保留非删（承 vestigial 精神：真死码删、蓝图锚点标）| 结构评估 | 简化 chore |
| ⏳ B3.2 **(defer → 单独 bugfix)** | ~~抽 _finalize_and_emit helper~~ **不做**（grounded：5call/4分支发散大只省~10行 + 碰 v0.7.35 热区）；但审计挖出真 **latent bug**：query.py L282/316 错误路径（跨源JOIN错/HTTP失败）save_message 漏 `**cost_service.to_save_message_kwargs` → clarifier 成本未记 → 单独 bugfix（=行为变更） | 结构评估 | 独立 bugfix |
| ❌ B3.3 **(关闭 · 声明不成立)** | ~~orchestrator 解环 抽 _shared.py~~ **不做**：grounded 判定**无真实 import 环**（orchestrator 顶层 import clarifier/presenter；后者仅函数体延迟 import orchestrator = 调用时已加载；延迟-import 是 monkeypatch 测试便利非破环）；抽 _shared = 过度工程；延迟-import 注释经核均准确 | 架构评估 | 关闭 |
| ✅ B3.4 **(v0.7.38 DONE)** | **BIAgentError → KnotError**（16 文件 62 处·纯机械 0 行为变更·清 R-79 漏抓的 camelCase 品牌残留）+ `knot.models.__all__` 补全 14 error 全导出（原漏 7）；「更正延迟-import 注释」经核**不成立**（注释均准确）→ 不改 | 命名一致性 | 简化 chore |

**done（B3.1/3.4 v0.7.38）**：models/ 一眼分清活契约 vs 蓝图；品牌改名遗留清零 + __all__ 补全。**B3.3 关闭**（无环）；**B3.2 → 独立 cost-drift bugfix**。

## B4 · 治理归档收口 + 死资产清（hygiene —— 「增量内联不攒批」）

| # | 项 | 来源 | scope |
|---|---|---|---|
| ✅ B4.1 **(v0.7.37 DONE)** | **CLAUDE.md 瘦身 677→473 行（-30%）**：v0.3~v0.7.24 逐 PATCH roadmap 表 + Loop Protocol 施行回顾表（209 行）移至新 `GOVERNANCE-ARCHIVE.md`（移动非删除）；保留全部 live 协议（Loop Protocol + EX 条款/VRP/关键路径/四源点同步/加密/技术债/4 层架构图/v0.7 不变量+前置+交付序/Go）。marker-asserted 切片 + 行级会计核验 + 对抗核验 agent ACCEPT（15 live 规则 0 丢 / 0 misplace）。顺手 6→9 条 contract 精度 | occam simplify | docs |
| ✅ B4.2 **(v0.7.36 DONE)** | **关停 4 幽灵/过期 PR**：#22（远古 main→develop 倒灌 stray）+ #52（v0.5.26 abandoned）+ #130（v0.6.2.2 治理记录已在 CLAUDE.md §R-LP-v3-EX-1）+ #186（superseded by #187）。open PR 4→0 | occam excess | gh 操作（无 PATCH diff） |
| ✅ B4.3 **(v0.7.36 DONE)** | **批量删分支**：develop（184 behind 死快照）+ 62 已合并分支（⚠️ squash-merge 使 `git --merged` 假阴性 0 → 用 gh 按 PR=MERGED 判定）。remote 91→7（main + 6 保留 abandoned）| occam excess | git 操作 |
| ✅ B4.4 **(v0.7.36 DONE)** | **死资产清**：package.json 删 react-window（0 引用；+ npm install 同步 lockfile）；eval-live.yml `EVAL_MODEL` 默认 `google/gemini-2.0-flash-001`(404 下线)→`anthropic/claude-haiku-4.5`（对齐 tests/eval/*.py v0.6.5.4 已修默认，workflow 漏了）。base.py:223 gemini 名是 orphan-cleanup DELETE 目标非死码→留 | occam excess | 简化 chore |

**done（B4.2/4.3/4.4 v0.7.36）**：0 幽灵 PR / remote 91→7 / 死依赖清零 / eval CI 默认活模型。**B4.1 CLAUDE.md 瘦身 → v0.7.37**。

## B5 · 前端测试栈 + bundle（完整度）

| # | 项 | 来源 | scope |
|---|---|---|---|
| B5.1 | **引入 vitest + testing-library**，优先给反复回归的纯逻辑补测：sse_handler.runQueryStream / api.normalizeDetail / ResultBlock layoutHint 归一化 / intent_helpers —— 逐步降级 Python 侧 .jsx 文本 grep 守护 | 前端/测试评估（0 前端测试）| 前端基建 |
| B5.2 | **bundle code-split**：echarts 按需引入（echarts/core + 用到的 chart + SVGRenderer）或 React.lazy；vite manualChunks 切 vendor（当前 1.56MB 单 bundle，Login 也下载 echarts）| occam/前端评估 | 前端 |

**done**：最易白屏/丢消息的纯函数有回归网；首屏不再拖整个 echarts。

## B6 · 语义层激活前置（⚠️ GATED on kk parser 验证 —— 大概率 v0.8 开篇）

| # | 项 | 来源 | scope |
|---|---|---|---|
| B6.1 | **compiler 片段级注入校验**：sqlglot 断言 having/filter/qualify AST 内无 Subquery/Union + 列名白名单（∈ metric.dimensions/caliber）+ 运算符白名单；改「_is_safe_sql 收口」误导注释为「仅保证只读，不防跨表读取」 | P1 high | 后端语义层 · 完整 v3（安全承重）|
| B6.2 | **parser NL→LogicForm 命中率评测集 + ≥90% 门禁**（对标 v0.4.0 intent 门禁）——把 runbook 题库固化成 eval set | P2 high | 后端 eval |
| B6.3 | **Layer C 决断**：语义层/HTTP 持仓表上生产前补 time-window detector 或 UI 明示不可信 | P7 medium | 视验证暴露 |

**gate**：kk 跑完 runbook 拿到三率 → 命中率够高 + 误判≈0 才激活；激活前 B6.1（安全）必修。**B6 本质是 v0.8「语义层激活/上生产」的开篇，非 v0.7 收尾** —— 建议划到 v0.8。

---

## 推荐执行序（v0.7 收尾 = B1→B4→B3→B2→B5；B6 划 v0.8）

1. **B1**（真 bug + 韧性，最高 ROI，do-now）
2. **B4**（hygiene 归档，快、低风险，兑现「不攒批」原则，顺手清 stale PR/branch/死依赖）
3. **B3**（代码美学消歧重构，0 行为变更）
4. **B2**（数据层规模化，公测前）
5. **B5**（前端测试栈 + bundle，完整度）
6. **B6** → **v0.8 开篇**（gated on kk 验证；语义层激活是 MINOR 级里程碑，天然是 v0.8）

> **v0.7 真收尾** = B1-B5 全清 + kk 跑完 parser 验证 → 那时 v0.7 才是「代码美学 + 无 bug + 高完整度 + 旗舰经真实验证」的完整收官，再走 v0.7→v0.8 整体审核仪式定 v0.8（大概率 = 语义层激活/上生产）。

_并行**内容侧**（kk · 纯 DB）：维度中文标签录入（market→交易对 等）+ 派生费率 metric unit=percentage（已本地 set）+ parser 验证。_
