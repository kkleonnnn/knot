# v0.5.8 — Cn+ Chore — CI Boot Smoke 修复 + Visual Replication Protocol — LOCKED

> **状态**：✅ **简化协议 — Stage 1 资深直接 ack → Stage 4 落地**
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（dormant — 简化协议未激活）
> **Loop Protocol v3 第 9 次施行**（**首次简化协议** — 跳 Stage 2/3 直接 Stage 4，资深 ack）

---

## 0. 一句话定调

**两件并一个 chore**：① 修预存 v0.5.0 R-72 留下的 CI boot smoke 硬编 0.5.0 bug（每 PATCH 升版本必挂）；② 在 CLAUDE.md 加 § Visual Replication Protocol 段，提炼 v0.5.7 经验作 v0.5.8+ 屏复刻的协作约束。

## 1. 决议表（D1-D3 — 资深直接 ack）

| ID | 决策 | 锁定 |
|---|---|---|
| **D1** | ci.yml version assertion 修法 | ✅ A 动态读 main.py（regex 提取 `version="X.Y.Z"`） |
| **D2** | § Visual Replication Protocol 在 CLAUDE.md 位置 | ✅ A § v3 协议施行历史回顾表之后独立段（与 Loop Protocol v3 并列） |
| **D3** | Protocol 内容范围 | ✅ A 精炼 v0.5.7 实证经验（路径常量 + 设计系统 + 视觉模型 + byte-equal 红线 + 抗诱惑清单 + 三处同步 + LOCKED 模板复用） |

## 2. 红线最终清单（R-187~R-191，5 条）

| ID | 摘要 |
|---|---|
| **R-187** | ci.yml boot smoke 不再硬编 version 字面；动态读 main.py（v0.5.0 R-72 遗留 bug 偿还） |
| **R-188** | § Visual Replication Protocol ≤ 80 行 CLAUDE.md（精炼提炼）— 实际 59 行 ✓ |
| **R-189** | backend tests 432 严格不变 / 7 contracts KEPT / 72 routes |
| **R-190** | 三处版本同步（main.py + smoke + Login 页脚）0.5.7 → 0.5.8 |
| **R-191** | docs+ci chore — 严禁触碰任何业务 .py/.jsx 逻辑（与 v0.5.4 同规则） |

## 3. 文件改动清单

**修改（5 commit / 7 files）**

| commit | 文件 | 改动 |
|---|---|---|
| 1 | `.github/workflows/ci.yml` | +7 行 / -3 行 — boot smoke version 动态读 |
| 2 | `CLAUDE.md` | +59 行 — § Visual Replication Protocol + v3 协议施行表 +1 行 |
| 3 | `knot/main.py` | version 字面 |
| 3 | `tests/test_rename_smoke.py` | R-72 smoke + docstring 字面 |
| 3 | `frontend/src/screens/Login.jsx` | 页脚字面 |
| 3 | `knot/static/*` | Vite build 产物自动同步 |
| 4 | `CHANGELOG.md` | +1 段 v0.5.8 |
| 4 | `CLAUDE.md` | v0.5.x 路线图 ⏳ → ✅ |
| 4 | `docs/plans/v0.5.8-chore-ci-protocol.md` | NEW LOCKED |

**0 改动（守住 R-191）**

- 任何 .py/.jsx 业务逻辑文件
- 18 屏 + Foundation（Shared.jsx / utils.jsx / App.css / NarrativeMotif.jsx）byte-equal
- requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js

## 4. 验收闸门（全绿）

- [x] backend tests **432 passed** / 112 skipped（R-189 严格不变）
- [x] 7 contracts KEPT
- [x] ruff check All checks passed
- [x] check_file_sizes.py 29 files OK
- [x] FastAPI version 0.5.8 / routes 72 / title KNOT
- [x] R-181 + R-185 sync test 通过（自动跟随新版本）
- [x] R-187 ci.yml `assert app.version == expected`（动态从 main.py 读取）— 本地等价测试 PASS
- [x] R-188 § Visual Replication Protocol 段 59 行 ≤ 80 ✓
- [x] R-191 docs+ci 守护 — `git diff origin/main HEAD` 仅含 .yml/.md/main.py/smoke/Login 页脚字面 + Vite build 产物

## 5. Commit 序列

| # | Hash | 摘要 |
|---|---|---|
| 1 | `2f81600` | chore(v0.5.8): fix ci.yml boot smoke hardcoded version (R-187) |
| 2 | `93d4de7` | docs(v0.5.8): add § Visual Replication Protocol to CLAUDE.md (R-188) |
| 3 | `5032bdb` | chore(v0.5.8): version bump 0.5.7→0.5.8 三处同步 (R-190) |
| 4 | (本 commit) | docs(v0.5.8): CHANGELOG + plan LOCKED + roadmap v0.5.8 ✅ |

## 6. 协议合规备注

- ✅ **Loop Protocol v3 第 9 次施行**（**首次简化协议**）
- ✅ 跳 Stage 2/3 — 资深直接 Stage 1 ack 后 Stage 4（chore 改动极小，5 红线已闭环）
- ✅ 守护者全程 dormant（未激活）
- ✅ 远古守护者 v0.3 dormant
- ✅ 红线总数 5（v0.5.8 区段 R-187~R-191）
- ✅ 历史累积红线 R-1~R-191（共 191 条）

## 7. 简化协议适用规则（v3 协议演化记录）

**v3 协议简化协议**（v0.5.8 首施行）触发条件（满足全部）：

1. PATCH 完全是 docs / ci / chore（0 业务 .py/.jsx 逻辑改动）
2. 红线数 ≤ 8 条（v0.5.4 docs-only 用了 10 条，是不简化的边界）
3. 无新决议争议点（D1-Dn 均为执行细节而非战略选择）
4. 资深架构师明确 ack 简化执行

**不适用简化的场景**（仍走全 v3 三阶段）：
- 任何视觉重构 / 屏复刻 PATCH（v0.5.6 / v0.5.7 都走了全 v3）
- 任何后端业务逻辑修改 PATCH（v0.5.1~v0.5.3 / v0.5.5）
- 任何架构 / 契约 / 红线体系变更 PATCH

**简化后未来回头风险**：低 — 因 chore 改动小且 deterministic，Stage 2/3 即便走也大概率 0 修订。

## 8. 不在 scope（明确推迟）

- v0.5.8+ 屏复刻系列（home / shell / thinking / favorites / 9 admin tabs）— 每屏独立 PATCH 走全 v3
- backend boot smoke matrix v0.5.0 R-72/77 业务逻辑本身评估（仅本 PATCH 修 ci 断言；env 双源逻辑是否还有问题留观察）
- ci.yml 其他遗留（如 lint-test 配置）— 现状已 PASS 不动

---

🛡️ **v0.5.8 chore 收尾。下一步：v0.5.8+ 屏复刻系列正式启动。**
