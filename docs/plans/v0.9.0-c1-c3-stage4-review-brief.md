# v0.9.0 C1-C3 Stage 4 复核 brief（交守护者[v0.8]）

> **性质**：v0.9.0 多租户 MINOR 的前 3 刀（C1 仪式 + C2 机制 + C3 绊线翻转+隔离 CI）已本地落地、全绿、未推。
> 资深选择「先复核 C1-C3 再做 C4 迁移」（高风险数据操作前，在已复核机制上做更稳）。请守护者独立复核。
> **复核对象**：分支 `feat/v0.9.0-multitenant-base`（从 origin/main = v0.8.24 拉），commits C1 `59133c1` / C2 `9b26647` / C3 `e8acaf9`。
> **手册**：`docs/plans/v0.9.0-oos1-ceremony-multitenant-base.md`（Stage 3 守护者已 major-revise → 复核 PASS 放行的 LOCKED 设计）。

---

## §1 交付摘要（C1-C3）

fail-closed 多租户地基（C 方案：平台库 + per-tenant SQLite 文件）。**tenant#1 下全行为 byte-equal**（1329 passed = v0.8.24 的 1319 基线 + 10 新隔离测）。

| Commit | 内容 |
|---|---|
| **C1** `59133c1` | OOS-1→OOS-1v2 红线修订仪式：CLAUDE.md 立约（`## OOS-1v2` 段，数据库段旁）+ 手册落库 + CHANGELOG 修订声明 + 版本 4 源点→0.9.0 + static rebuild |
| **C2** `9b26647` | 机制：`core/tenant_context.py`(fail-closed ctx) + `tenant_repo.py`(get_platform_conn/CRUD/resolve_single) + `platform_schema.sql` + `base.get_conn` 双层 + `main.py` 启动序 ❶-❺ + tenant middleware + copy_context 3 fork + `bi_schedule`/auto-purge/3 scripts ctx + conftest×3 |
| **C3** `e8acaf9` | OOS-1→OOS-1v2 绊线翻转 21 文件（schema 15 注释 + 3 repo _reject 文案 + 11 测 docstring，行为 0 改）+ `tests/test_tenant_isolation.py` 10 测（8 断言 + 线程复用 + production-layout） |

---

## §2 4 处对 LOCKED 手册的偏离（**请守护者重点裁定**）

| # | 偏离 | grounded 理由 | 资深状态 |
|---|---|---|---|
| **D1** | **copy_context vs run_due-set** 修 executor fork | 手册 §0.2 只处理 bi_schedule 1 个 `run_in_executor` fork，但 grounded 发现 **bi_reports.py:263 + bi_share.py:71** 另 2 个 fork（fail-closed tenant ctx 不跨 raw `run_in_executor` → 500，catalog ctx 因有全局回退不炸所以旧代码没暴露）。实测 anyio `to_thread.run_sync` **已 copy ctx**（仅 raw run_in_executor 丢）。→ 3 处统一用 `copy_context().run` 传播（tenant+catalog 一起 + `ctx.run` 天然隔离无线程池残留，反而更干净解决 Stage3 #4 的残留问题）。run_due 回退成无内部 set（调用方给 ctx）。 | **✅ 资深①拍板采纳** |
| **D2** | `.gitignore **/data/tenants/` 提前 C4→C2 | C2 启动序即创建 tenants/ 目录 → 不提前则运行时库裸奔进 git（**实测 `git add -A` 差点提交 `tenants/1/knot.db` 323KB 含 seed admin 哈希**）。`**/data/*.db` 不匹配子目录。必须在 C2。 | 执行者判断（防泄漏） |
| **D3** | 版本 4 源点 bump 提前 C5→C1 | CHANGELOG v0.9.0 [Unreleased] 需 main.py 同步（`test_changelog_top_version_synced_with_main` + `test_changelog_single_unreleased`）；不提前则 C1-C4 每中间 commit doc-invariant 红。C5 只留 tag。 | ✅ 资深确认 |
| **D4** | `assert_tenant_context` 在 core **无 audit** | 手册 §2 iv#1 要「漂移落 audit + raise 镜像 assert_catalog_context」。但 assert_catalog_context 在 services 层（query_helper）因用 audit_service；core/tenant_context.py 若 import audit_service = **破 import-linter「core 不依赖业务层」**（9 contracts）。→ core 版只 raise（tripwire 在位，fail-closed get_conn 是主防线）；audit-on-drift 随 0.1 多租户接线落 services 层（多租户 drift 才是真威胁）。 | **待守护者认** |

---

## §3 执行者已做的 grounded 验证（无单点可信 · 供守护者复核我的验证）

- **Boot-crash 门清除**（关键）：catalog.py:156 模块 import 期 `reload()` 调 get_conn，此时（router import :18 早于 platform bootstrap）无 tenant ctx → fail-closed raise `TenantContextError`。grounded 确认 `catalog_loaders._load_from_db`（:66-87）用 **`except Exception`** 接住（TenantContextError 是 RuntimeError）→ 转 `MetadataError` → `reload(strict=False)` 降级 file/empty（**不崩**）。❺ tenant#1 ctx 补调 reload 修复。手册命门#1 假设成立。
- **fail-closed 对抗核**：v0.8.24 未引入新的启动期/后台 get_conn 消费者（datasource_exists/delete cascade/invalidate_all 全在 request 路径）→ 8 个 ctx set 点仍完备。
- **get_conn 双层 byte-equal**：tenant#1 `db_dir='.'` → `SQLITE_DB_PATH.parent/./knot.db` = 锚点本身 → 32 消费面 + 8+ 直读 SQLITE_DB_PATH 的既有断言 0 改（1329 passed 证）。
- **闸门**：full pytest **1329 passed / 115 skipped / 0 fail** · `ruff check knot/` 绿（CI 范围）· `lint-imports` **9 kept / 0 broken**（core/tenant_context.py 守「core 横切不依赖业务层」）。

---

## §4 Stage 4 watch-item（**请守护者重点 grounded 复核**，含手册 §5 验收）

1. **copy_context 3 fork 正确性**（D1）：`ctx.run` 是否真传播 tenant+catalog ctx 到 worker + 不泄漏进复用线程？测 `test_iso_executor_copy_context_isolation`（worker 内 get_conn 成功 + 同线程复用无 ctx→fail-closed）。守护者可对抗验证并发场景。
2. **fail-closed 无回退**（`current_tenant()` 未 set → raise，**无全局回退**，刻意反 catalog 语义）：测 `test_iso1`。守护者核 serving 期无残留 ctx（各 token finally reset）。
3. **启动序 ❶-❺ chicken-and-egg**（Stage3 #3）：platform bootstrap ctx-free → 逐租户 init_db → tenant#1 ctx seed/totp/reload 补调 + finally reset。守护者核 `KNOT_TOTP_REQUIRED=true` 下 boot 不崩（totp_rollout 在 tenant#1 ctx）。
3. **middleware set-without-reset**（镜像 catalog ctx；uvicorn per-request task 隔离）：手册 §5 要「真 query-stream 测 20 并发 0 泄漏」——**执行者只做了 executor 隔离测 + 依赖 bi_reports/bi_share 通过（走 executor 路径）**；**未做 20 并发 live query-stream 测**（需 LLM key）。⚠️ 这条 guardian 若要硬验须真环境跑（我可备脚手架）。
4. **绊线 vs 隔离 CI 区分**（Stage3 #10）：test_tenant_isolation docstring 显式分「绊线（列缺席 ⑤）」vs「隔离（fail-closed+文件边界 ①-③⑥）」。
5. **flatten==144 精确**（Stage3 #9）：`test_iso8` 用 app_route_paths 精确计数（非 >=80 软下限）。C2 middleware 非 route，144 不变。

---

## §5 未做（C4/C5，本次复核范围外）

- **C4 存量迁移**（下一刀，复核通过后做）：knot.db → platform.db + tenants/1/knot.db；WAL/-shm checkpoint(TRUNCATE)+close 再 rename；强 complete-marker（schema version + 32 表 + 行数 hash + 解密烟测，抗 crash 续跑）；uploads.db 一并迁；DEPLOY.md；失败回滚保旧库。
- **C5 收尾**：CHANGELOG 定稿 + merge 后 tag `v0.9.0`（版本源点已 C1 做）。

---

## §6 给守护者的问题

1. **D1 copy_context** 采纳是否认可（vs 手册 run_due-set；系统性修全 3 fork + 更干净）？
2. **D4 assert_tenant_context 无 audit**（core 纯净，audit 延 0.1）是否接受？还是要求 audit 落 services 层调用点（query 执行前）本 PATCH 就做？
3. **SSE ctx 20 并发 live 测缺失**（§4.3）——接受「executor 隔离测 + bi_reports/bi_share 通过」作代理，还是要求补真 query-stream 测（我备脚手架）？
4. C1-C3 与 LOCKED 手册 / OOS-1v2 仲裁的一致性有无遗漏？
