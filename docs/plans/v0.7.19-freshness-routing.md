# v0.7.19 — 数据新鲜度路由修复（5 源统一轴 + 时间精度）

> **触发**：价值自测发现「今天合约量→ads→空」；Codex 审计（PR #186）+ ultracode workflow 复审确认
> **线上 5 个路由源一致漏「时间窗触达今天→dwd」轴**（few-shot #35 是可执行铁证），系统性少算「本月/本周/今年迄今」双源度量的今天那一天。
> **协议**：完整 v3（含 TimeContext 加字段 R-PA-PB-2 守护者评审 + parser 路由改造）；资深拍「一次性修全 5 源 + 本地改+导出 + 代码」。

## 1. 正确模型（资深确认 ✓✓✓）

路由轴 = **时间窗右边界是否触达今天/现在**（非「今天 vs 历史」二分）：
- `ohx_ads`（日报/汇总）数据到【昨天 T-1】，没有今天；`ohx_dwd`（明细）到【上一小时 T-0】，含今天，可 SUM 出含今天的平台总量。
- 触达今天/迄今（今天/本月[迄今,含今天]/本周/今年迄今/近N天到今天/实时）→ **dwd**。
- 完整过去（昨天/上月/上周/同比去年）→ **ads**（更快更完整）。
- ⚠️「本月/本周/今年」默认迄今含今天 → 双源度量走 dwd（走 ads 漏今天=少算）。ads-only 度量（platform_pnl/active）含今天窗口只能给到昨天。

## 2. 改动

### A. 代码 PATCH（仓库，本 PR）
- **parser.py**：① metric 行加 `表=base_object`（parser 才能按表路由）② 注入 catalog `business_rules`（此前仅 clarifier/sql_planner 有，parser 盲）③ `_LOGICFORM_SYS` 加「库表时效路由第一判据」轴 + 时间词→枚举映射。
- **time_resolver.py**：TimeContext +4 字段（`today`/`yesterday` 日粒度 lag 无关 + `last_month`/`last_year` 完整过去）；resolve 同步（R-PA-PB-2 守护者评审）。
- **compile_helpers `_TIME_KEYS` + parser `_TIME_ENUMS`**：+today/yesterday/last_month/last_year。
- **query_steps.run_semantic_compile_step**：per-metric 新鲜度 —— 按引用 metric 的 `freshness_lag_days` 取 min 解析 time_ctx（dwd lag=0→latest=今天，ads lag=1→昨天）。⚠️ **不用 `or 1`**（dwd lag=0 是 falsy 会被吞成 1 → 漏今天）。
- **测试**：time_resolver 新枚举 + lag=0→今天（防 or-1 陷阱）守护。

### B. DB 内容（kk 的 OHX 数据 —— 本地改 + 导出 `~/Downloads/knot-freshness-fix/`，kk 应用线上）
5 源统一植入轴（一处作者改全防不一致）：
- **catalog business_rules**：库表路由段 axis-first 重写（本月/本周迄今→dwd，仅上月/上周用 ads weekly/monthly）。
- **knowledge chunk #75**：补「迄今含今天」整类。
- **few-shot #35**：`本周注册和活跃→ads 周报最新行`（漏今天）→ `本周注册→dwd_user_reg 含今天`（`sta_time` 非 sta_date）。
- **clarifier / sql_planner prompt**（DB admin 覆盖版）：本周/本月 ≠ 上周/上月；加 dwd 迄今分支。
- **presenter prompt**：今日提醒扩到「含今天窗口走 ads」。
- **metric 注册表**：freshness_lag（ads=1/dwd=0/agent=1/派生=1）+ 加 `user_reg`(dwd) → 41 metric。

## 3. 红线 R-SL-148~

| 红线 | 内容 |
|---|---|
| **R-SL-148** | 路由轴 = 时间窗触达今天否（5 源 + parser 一致）；本月/本周/今年迄今 = 双源走 dwd，仅上月/上周完整过去用 ads weekly/monthly |
| **R-SL-149** | parser 注入 business_rules + 表=base_object（此前 parser 路由盲）；time 词→枚举映射 |
| **R-SL-150** | per-metric 新鲜度：min(freshness_lag_days) 解析 time_ctx；⚠️ 禁 `or 1`（dwd 0 falsy 陷阱）；today/yesterday lag 无关 |
| **R-SL-151** | TimeContext +4 字段（R-PA-PB-2 守护者评审）；prompt_block byte-equal sustained（不注入新字段）|

## 4. 验收（.venv 本地实跑）

- 178 passed（time_resolver + semantic）；新枚举 + lag=0→今天 守护。
- 10 题端到端：今天合约→dwd+today单日 ✓ / **本月合约→dwd+含今天(06-29)** ✓ / 昨天→ads+单日昨天 ✓ / user X 今天→dwd+filter ✓ / 代理→无日期 ✓。
- ⚠️ 待人测（线上应用导出后）：parser 真命中率 + 含今天窗口数据对照。

## 5. commit 序列

- **C1**：parser（business_rules + 表 + 轴）+ time_resolver（4 枚举）+ compile_helpers/_TIME_KEYS + query_steps（per-metric lag）+ 测试。
- **C2**：收官 5 源点 0.7.18→0.7.19 + CHANGELOG + 路线图 + static。

### 自测暴露的后续修复（C2 收官后 live 价值自测逐层暴露 —— 「路由对了才暴露下一层」）

- **C3**：DATETIME 日期列时间窗半开区间。dwd `sta_time`（DATETIME）用 `BETWEEN 'd' AND 'd'` 只匹配 00:00:00 → 全天漏空 NULL；compile_helpers `_date_range_clause` 按列类型分流（`*_time`/含 time → `>= 'd 00:00:00' AND < '(d+1) 00:00:00'` 半开；`*_date` → BETWEEN）。
- **C4**：绝对年份/日期 → 回退 LLM。clarifier 把相对「去年」展开成绝对「2025年全年（dates）」→ parser 把「全年」误映射 this_year（=当前年 2026）错年；双修 = parser 绝对年份→`metrics=[]` 回退 + clarifier prompt（DB/导出）保留相对时间词原样。
- **C5**：尾部散文剥离。opus(sql_planner) 偶在 SQL 后附「注：…」中文说明段 → 污染 sqlglot/HTTP 虚拟表参数提取 → 「安全检查未通过」+ presenter 幻觉「持仓为零」（生产 `futures_admin.futures_position_list` 平台持仓 bug）；`_strip_sql` 增 `_strip_trailing_prose`（空行分隔 + CJK/Note 起手段落丢弃，合法 SQL/CTE 全保留）+ business_rules 输出纪律（导出补一句）。非语义层回归（HTTP 路径），自测暴露 + 疑似 C1 verbose business_rules 触发既存 `_strip_sql` 缺口。

> DB 内容（B）= kk 本地 + 导出，不在 PR。仓库 generic seed `knot/prompts/*.md`（fresh-install fallback）axis 补充 = follow-on（live 用 DB admin 覆盖版，已修）。
