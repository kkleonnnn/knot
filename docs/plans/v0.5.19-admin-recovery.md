# v0.5.19 — (C5+) AdminRecovery 屏复刻 — LOCKED (Stage 1+2+3 整合 → Stage 4 准备)

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **协议**：Loop Protocol v3 — 三阶段评审闭环（**第 20 次施行**）
> **里程碑**：**KNOT admin 顶层屏三部曲收官**（Audit + Budgets + Recovery）+ **Inset 8% 闭环第六处铁律加冕** + **borderLeft 25% 第三处闭环铁律加冕**（设计语言铁律第二维度加冕）+ **R-495 字面 byte-equal 严防死守立约** + **Q2 VRP 局部例外原则** + **技术债登记加强**

---

## 1. 决议表 + 风险项最终裁定（D1-D9 + Q1-Q5 全部锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** 时段 tabs 位置 | ✅ **B 保 content area + 视觉重构**（Stage 2 绝对支持） | R-192 13 props 宪法级严守 |
| **D2** Chart 实现 | ✅ **B 保 LineChart from Shared**（Stage 2 支持） | R-365 Shared 0 改动绝对红线 sustained |
| D3 Top user table → CSS Grid | ✅ A | Stage 1 |
| D4 Emoji 全清 | ✅ A | Stage 1 |
| D5 Hex 全清 R-286 | ✅ A | Stage 1 |
| D6 KPI 第 3 卡 accent | ✅ A | Stage 1 + R-491 加码 transition |
| D7 Rules note brandSoft inset | ✅ A | Stage 1 + R-495 严防字面 |
| D8 LIMIT 380 | ✅ A | Stage 1 |
| D9 Helpers inline 复制 | ✅ A | Stage 1（v0.6 技术债登记加强） |
| **Q1** Chart Tag chip "last 30 days" | ✅ A | Stage 1（推荐用动态 `last ${period_days} days` 体现 7d/30d/90d 切换） |
| **Q2** Medal icon inline svg | ✅ **B 准许 inline trophy path**（Stage 2 VRP 局部例外） | Stage 1 推荐确认 |
| Q3 Avatar 22×22 | ✅ A | Stage 1 |
| Q4 Toast 文案 byte-equal | ✅ A | Stage 1 |
| Q5 v0.6 Shared 移植承诺 | ✅ A 加强 | KpiCard + PeriodTab + medal svg 累计第三次复用确认 |

14/14 锁定；Stage 2 在 D6 加码 R-491（transition）+ D7 加码 R-495（字面严防）+ Codex 加码 R-492（PeriodTab box-shadow）+ Stage 3 加码 R-493（NaN% 守护）+ R-494（chart 高度）；其余 Stage 1 保留。

---

## 2. 红线最终清单（R-466 ~ R-495，30 条）

### Stage 1 草案原 25 条（保留）

| ID | 摘要 |
|---|---|
| R-466 | AdminRecoveryScreen 5 props 签名 byte-equal |
| R-467 | 3 useState slots byte-equal（period / stats / loading） |
| R-468 | api endpoint URL byte-equal |
| R-469 | period 3 values + useEffect dep byte-equal |
| R-470 | stats 业务字段 10 个 byte-equal |
| R-471 | Topbar 删 🛡️ emoji |
| R-472 | 时段 tabs 视觉重构（content area + mono + brandSoft active） |
| R-473 | KPI 3 cards grid + KpiCard inline helper |
| R-474 | KPI 第 3 卡 accent T.accent + fontWeight 700 + 34px |
| R-475 | Chart card brandSoft border + Tag chip header |
| R-476 | LineChart from Shared.jsx props byte-equal（R-365 sustained） |
| R-477 | Top user table HTML 全删 → CSS Grid 5-col |
| R-478 | Top user header medal svg + Tag chip |
| R-479 | Top user row（# rank + Avatar 22 + count/msg_count/rate） |
| R-480 | **brandSoft 8% 闭环第六处** — 字面 byte-equal v0.5.14~19 |
| R-481 | **borderLeft 3px 25% 第三处闭环** — 字面 byte-equal SavedReports R-356 + AdminBudgets R-465 |
| R-482 | grep hex 0 命中 |
| R-483 | emoji 全清 |
| R-484 | Submit btn `'#fff'` → T.sendFg；严禁 'white' |
| R-485 | 范围守护 |
| R-486 | admin/ 4 子模块 0 改 |
| R-487 | chat/ 7 子模块 0 改 |
| R-488 | App.css 0 行 diff |
| R-489 | KnotLogo 3 文件 sustained |
| R-490 | 三处版本同步 0.5.18→0.5.19 + LIMITS 33→34 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-491** | **KPI 动画步调一致性** — KPI 数值 `transition: color 0.2s` — loading 态切换到数据态时 T.accent 变色不生硬 | KpiCard value inline style 加 `transition: 'color 0.2s'`；commit 1 后 grep `transition.*color.*0\.2s` KpiCard 命中 |
| **R-492** | **PeriodTab 交互反馈** — active 状态 `box-shadow: 0 2px 8px color-mix(in oklch, ${T.accent} 20%, transparent)` — 顶层过滤器"选中重感" | PeriodTab active 状态 inline `boxShadow: active ? \`0 2px 8px color-mix(in oklch, \${T.accent} 20%, transparent)\` : 'none'`；commit 1 后手测 active 按钮浮起感视觉 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-493** | **业务字段硬契约扩展（R-470 sustained）** — top_users[i] 处理时必须守护 `count === 0 \|\| msg_count === 0` 边界，防 NaN% 显示 | rate 计算逻辑：`const rate = msg_count ? ((count / msg_count) * 100).toFixed(1) + '%' : '0.0%'`；commit 1 后 mock `msg_count: 0` 实测不显 NaN |
| **R-494** | **Chart 比例守护** — LineChart 高度固定 `280px`；90d 大数据量下折线清晰 | LineChart props 含 `height={280}` 字面；commit 1 后切 90d 实测视觉 |
| **R-495** | **闭环字面 byte-equal 严防死守** — AdminRecovery 所有 color-mix 必须与 SavedReports/AdminBudgets 进行**二进制级别**比对；空格/逗号差异触发 Stage 3 否决 | commit 1 收尾**强制**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` 6 文件命中字面 byte-equal；`git grep -F "3px solid color-mix(in oklch, \${T.accent} 25%, transparent)" frontend/src/screens/` 3 文件命中字面 byte-equal；任何空格/逗号差异 → reset 重写 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §3 验收核心 — 第 6 个 color-mix 闭环 git grep 全站自动化校验

```bash
# 1. brandSoft 8% 闭环第六处（铁律加冕）
git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# 预期 6 文件命中：
#   chat/ResultBlock.jsx (v0.5.14 R-323)
#   SavedReports.jsx (v0.5.15 R-372)
#   admin/tab_access.jsx (v0.5.16 R-386)
#   AdminAudit.jsx (v0.5.17 R-409)
#   AdminBudgets.jsx (v0.5.18 R-444)
#   AdminRecovery.jsx (v0.5.19 R-480) ← 新增

# 2. borderLeft 25% 第三处闭环（设计语言铁律第二维度加冕）
git grep -F "3px solid color-mix(in oklch, \${T.accent} 25%, transparent)" frontend/src/screens/
# 预期 3 文件命中：
#   SavedReports.jsx (v0.5.15 R-356)
#   AdminBudgets.jsx (v0.5.18 R-465)
#   AdminRecovery.jsx (v0.5.19 R-481) ← 新增

# 任何空格/逗号差异 → R-495 否决 → reset 重写
```

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死 — R-480 优先 Step 1）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + LIMIT dict 新增 380 + **R-480 brandSoft 8% 闭环字面率先落地**（第六处铁律加冕）| R-480/R-490 |
| 2 | Topbar 删 🛡️ emoji → "System Recovery 趋势" | R-471 |
| 3 | PeriodTab inline helper + **R-492 box-shadow active 反馈** + minHeight 30 + brandSoft active | R-472/R-492 |
| 4 | KPI 3 cards grid + KpiCard inline helper + **R-491 transition color 0.2s** + 第 3 卡 accent T.accent + R-394 auto-fit | R-473/R-474/R-491 |
| 5 | Chart card svg chart icon header + **Q1 动态 Tag chip "last ${period_days} days"** + LineChart byte-equal + **R-494 height={280}** | R-475/R-476/R-494 |
| 6 | Top user table HTML 全删 → CSS Grid 5-col + thead 视觉应用 R-480 + **Q2 inline trophy svg path**（VRP 局部例外）+ Top Tag chip + **R-493 rate NaN 守护** | R-477/R-478/R-479/R-493 |
| 7 | Rules note 2 条 brandSoft inset + **R-481 borderLeft 3px 25% 第三处闭环** + Tag chip + 📌 删 | R-481 |
| 8 | emoji 🛡️/📈/🏆/📌 全清 → svg icons + hex `'#fff'` → T.sendFg + 全 hex grep | R-483/R-484/R-482 |
| 9 | 三处版本同步 + **R-495 git grep 字面 byte-equal 严防死守**（6+3 文件 byte-equal）+ grep 闭门验证 | R-490/R-495 |

### Stage 2 D1 + D2 双绝对支持落地

- **D1 R-192 13 props 宪法级**：时段 tabs **绝对保留** content area；严禁触碰 Shell.jsx；commit 1 收尾 `git diff frontend/src/Shell.jsx` = 0 行
- **D2 R-365 Shared 0 改动**：LineChart 直接 `import { LineChart } from '../Shared.jsx'`；严禁 inline 重写 svg chart；commit 1 收尾 `git diff frontend/src/Shared.jsx` = 0 行

### Stage 2 Q2 VRP 局部例外纪律

```jsx
// 14×14 trophy/medal svg path — VRP 局部例外（R-365 sustained 但 inline svg 资产化允许）
<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"
     style={{ color: T.accent }}>
  <circle cx="12" cy="8" r="6"/>
  <path d="M8.21 13.89L7 23l5-3 5 3-1.21-9.12"/>
</svg>
```

未来 v0.6 Shared 解锁后纳入 `I.medal`（第四次复用确认 → 偿还触发）。

### Stage 3 R-493 NaN 守护实施

```jsx
// rate 计算 — R-493 守护 msg_count === 0 边界
const rate = msg_count ? ((count / msg_count) * 100).toFixed(1) + '%' : '0.0%';
```

commit 1 后**强制**手测：mock `top_users: [{msg_count: 0, count: 0}]` → rate 显 `0.0%` 而非 `NaN%`。

### Stage 3 R-494 Chart 高度

```jsx
<LineChart
  data={by_day.map(d => ({ date: d.date.slice(5), count: d.count }))}
  stroke={T.accent}
  fill
  labelColor={T.muted}
  gridColor={T.borderSoft}
  width={880}
  height={280}  // ← R-494 固定 — 90d 大数据折线清晰
/>
```

### Stage 2 R-491 + R-492 动效协同

- KpiCard value：`transition: 'color 0.2s'`（加载态→数据态平滑变色）
- PeriodTab active：`boxShadow: '0 2px 8px color-mix(in oklch, ${T.accent} 20%, transparent)'`（顶层过滤器选中浮起感）
- 两者协同形成"加载完成 → KPI 变色 + tab 选中浮起" 视觉序列

### Stage 2 Q1 动态 Tag chip

```jsx
const periodLabel = { '7d': 'last 7 days', '30d': 'last 30 days', '90d': 'last 90 days' }[period];
<Tag chip>{periodLabel}</Tag>
```

或基于 `stats?.period_days`：`<Tag chip>last {stats?.period_days || 30} days</Tag>`

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/AdminRecovery.jsx  [EDIT 152 → ≤ 380 行]
                                        Step 1: baseline + LIMIT 新增 + R-480 闭环字面率先落 [R-480/490]
                                        Step 2: Topbar 删 🛡️ [R-471]
                                        Step 3: PeriodTab helper + R-492 active box-shadow [R-472/492]
                                        Step 4: KPI 3 cards + KpiCard helper + R-491 transition + 第 3 卡 accent [R-473/474/491]
                                        Step 5: Chart card + Q1 动态 Tag + LineChart byte-equal + R-494 height=280 [R-475/476/494]
                                        Step 6: Top user CSS Grid 5-col + Q2 trophy svg + R-493 rate NaN 守护 [R-477/478/479/493]
                                        Step 7: Rules note brandSoft inset + R-481 borderLeft 3px 25% 闭环 [R-481]
                                        Step 8: emoji 全清 + hex 偿还 + 全 hex grep [R-483/484/482]
                                        Step 9: 三处版本同步 + R-495 字面 byte-equal git grep 严防 [R-495/490]
                                        守护: 5 props/3 state/api URL/period 3/stats 10 字段 byte-equal
                                              + inline styles only [R-488 CSS 0 污染]

scripts/check_file_sizes.py             [EDIT LIMITS 33 → 34]
                                        - "frontend/src/screens/AdminRecovery.jsx": 380 [R-490]
knot/main.py                            [EDIT version 0.5.18 → 0.5.19]  [R-490]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.18 → 0.5.19]  [R-490]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.18 → 0.5.19]  [R-490]

frontend/src/screens/admin/*            [unchanged]   [R-486]
frontend/src/screens/chat/*             [unchanged]   [R-487]
frontend/src/screens/{Admin,SavedReports,AdminAudit,AdminBudgets,tab_access}.jsx [unchanged] [R-485]
frontend/src/screens/ (其余 13 屏)       [unchanged]   [R-485]
frontend/src/{App,api,Shell,Shared,utils,main,index.css}.jsx 等 [unchanged]   [R-485 + R-365 sustained]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]   [R-485]
frontend/src/App.css                    [unchanged 0 行 diff]  [R-488]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.19 段]
                                        - R-466~R-495 偿还表
                                        - ⭐ KNOT admin 顶层屏重构完结 — 视觉一致性达到 100% 覆盖（Audit + Budgets + Recovery）
                                        - Inset 8% 闭环第六处铁律加冕（6 屏字面 byte-equal）
                                        - borderLeft 25% 第三处闭环铁律加冕（设计语言铁律第二维度）
                                        - R-495 字面 byte-equal 严防死守立约（git grep 全站自动化校验立约）
                                        - KpiCard + PeriodTab + medal svg 累计第三次复用确认 — v0.6 Shared 移植承诺加强
                                        - Q2 VRP 局部例外（Shared 0 改 sustained 下 inline svg 资产化允许）
                                        - Loop Protocol v3 第 20 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.19（admin 三部曲收官 + Inset 8% 六处铁律加冕）
                                        - v0.5.x 路线图 v0.5.19 ⏳ → ✅
                                        - v0.6 路线图加强 "6+ inline helpers 移入 Shared.jsx 技术债偿还" 承诺
docs/plans/v0.5.19-admin-recovery.md    [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.19**

### Stage 2/3 强化验证

- [ ] **R-480 brandSoft 8% 闭环第六处**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — **6 文件**命中
- [ ] **R-481 borderLeft 25% 第三处闭环**：`git grep -F "3px solid color-mix(in oklch, \${T.accent} 25%, transparent)" frontend/src/screens/` — **3 文件**命中
- [ ] **R-491 KPI transition**：grep `transition.*color.*0\.2s` KpiCard 区域命中
- [ ] **R-492 PeriodTab box-shadow**：grep `boxShadow.*active.*color-mix` PeriodTab 命中
- [ ] **R-493 rate NaN 守护**：grep `msg_count\s*\?\s*\(\(count` 或 `'0.0%'` 命中；mock msg_count=0 实测显 0.0%
- [ ] **R-494 LineChart 高度**：grep `height={280}\|height: 280` LineChart 调用命中
- [ ] **R-495 字面严防**：`git grep -F` 两条命令命中 6+3 文件；无空格/逗号差异
- [ ] **D1 R-192 sustained**：`git diff frontend/src/Shell.jsx` = 0 行
- [ ] **D2 R-365 sustained**：`git diff frontend/src/Shared.jsx` = 0 行
- [ ] **R-482 hex 0 命中**：grep `#[0-9a-fA-F]{3,6}` AdminRecovery \| grep -v boxShadow \| grep -v rgba = 0
- [ ] **R-483 emoji 全清**：grep `🛡️\|📈\|🏆\|📌` AdminRecovery = 0
- [ ] **R-477 HTML table 全删**：grep `<table\|<thead\|<tbody\|<th\|<td\|<tr` AdminRecovery = 0
- [ ] **R-474 KPI accent 第 3 卡**：grep `accent={true}\|color:\s*T\.accent` 自纠正率区命中
- [ ] **Q1 动态 Tag chip**：grep `period_days\|last \${\|periodLabel` 命中

### 契约守护（首要红线）

- [ ] **R-466**：AdminRecoveryScreen 5 props 签名 byte-equal
- [ ] **R-467**：3 useState slots byte-equal（period / stats / loading）
- [ ] **R-468**：api URL byte-equal — `/api/admin/recovery-stats?period=`
- [ ] **R-469**：period 3 values `['7d', '30d', '90d']` + useEffect dep byte-equal
- [ ] **R-470**：stats 业务字段 10 个 byte-equal（含 by_day/top_users 嵌套字段）

### 范围守护

- [ ] **R-485**：App/api/index.css/main/utils/Shared/Shell/decor/13 屏 + Admin/SavedReports/tab_access/AdminAudit/AdminBudgets `git diff` = 0
- [ ] **R-486**：admin/ 4 子模块 0 改
- [ ] **R-487**：chat/ 7 子模块 0 改
- [ ] **R-488**：App.css 0 行 diff + AdminRecovery 0 新 className（除 cb-sb）
- [ ] **R-489**：grep `KnotLogo|KnotMark|KnotWordmark` 仍 3 文件命中

### 端到端业务回归

- [ ] 进 admin → 切 admin-recovery nav → loading → stats 加载
- [ ] **7d/30d/90d 时段切换 → 重新 fetch + KPI 更新 + R-491 transition + R-492 active box-shadow**
- [ ] KPI 3 cards：自纠正总次数 / 覆盖消息数 / 自纠正率（**第 3 卡 accent T.accent**）
- [ ] **LineChart 渲染 by_day 数据**（fill + stroke + R-494 height 280）
- [ ] **R-493 mock msg_count=0 实测 rate=0.0% 非 NaN%**
- [ ] Top user table CSS Grid 5-col + rank # mono + **Q2 trophy svg** + Avatar 22 + username + id + count + msg_count + rate
- [ ] **Q1 动态 Tag chip — 7d/30d/90d 切换体现 last N days**
- [ ] empty state — by_day.length === 0 / top_users.length === 0
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— KPI grid R-394 auto-fit
- [ ] **light + dark 双模式** — KPI / Chart / Table / Rules note 视觉一致
- [ ] **⭐ Inset 8% 6 屏视觉一致性肉眼校验**（ResultBlock / SavedReports / DataSources / AdminAudit / AdminBudgets / **AdminRecovery**）
- [ ] **⭐ borderLeft 25% 3 屏视觉一致性肉眼校验**（SavedReports Quote / AdminBudgets Rules / **AdminRecovery Rules**）
- [ ] v0.5.0~v0.5.18 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.19): AdminRecovery.jsx 视觉重构 — Inset 8% 闭环第六处 + borderLeft 25% 第三处闭环 + KpiCard/PeriodTab/trophy svg + R-491~R-495 (R-466~R-495)` | npm build + R-495 git grep 双闭环 byte-equal + 端到端业务 + R-493 NaN 守护 + R-491/492 动效 + 三档窗宽 + 双模式 | 5 props/3 state/api URL/period 3/stats 10 字段 byte-equal + Step 1→9 顺序 + R-480 优先 + D1/D2 双 sustained + Q2 VRP 例外 |
| 2 | `chore(v0.5.19): version bump 0.5.18→0.5.19 三处同步 + LIMITS 33→34 新增 AdminRecovery 380 (R-490)` | 全闸门 + routes=72 + version=0.5.19 + 34 条 LIMITS | R-490 三处同步 grep ≥ 3 |
| 3 | `docs(v0.5.19): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + ⭐ KNOT admin 顶层屏三部曲完结 + Inset 8% 六处铁律加冕 + borderLeft 25% 三处闭环加冕 + R-495 字面严防立约 + KpiCard/PeriodTab/medal v0.6 技术债加强承诺` | 全闸门 | Loop Protocol v3 第 20 次施行 + 视觉铁律加冕里程碑 + 设计语言铁律第二维度加冕 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→9 顺序不可换；**R-480 闭环字面率先落地**；**R-495 字面 byte-equal 严防死守** 为 commit 1 收尾必经闸门。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 20 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 30（v0.5.19 区段 R-466~R-495）
- ✅ **AdminRecovery = admin 顶层屏三部曲收官**（v0.5.17 Audit + v0.5.18 Budgets + v0.5.19 Recovery）
- ✅ **Inset 8% 闭环字面第六处铁律加冕**（v0.5.14~19 六屏 byte-equal — 视觉一致性 100% 覆盖后端管理资产屏）
- ✅ **borderLeft 25% 第三处闭环铁律加冕**（设计语言铁律第二维度 — SavedReports / AdminBudgets / AdminRecovery byte-equal）
- ✅ **R-495 字面 byte-equal 严防死守立约** — git grep -F 全站自动化校验；任何空格/逗号差异触发 Stage 3 否决；视觉铁律执行机制制度化
- ✅ **D1 R-192 13 props 宪法级 sustained** — Shell 0 改；时段 tabs 不移 topbar
- ✅ **D2 R-365 Shared 0 改动绝对红线 sustained** — LineChart 字面复用
- ✅ **Q2 VRP 局部例外原则** — Shared 无对应资产时 inline svg 允许；v0.6 Shared 解锁后纳入（trophy/medal 累计第四次复用确认 → 偿还触发）
- ✅ **技术债登记加强** — KpiCard + PeriodTab + medal svg 累计第三次复用（v0.5.17 StatusDot + v0.5.18 ActionChip/EnabledChip + v0.5.19 KpiCard/PeriodTab/medal）；v0.6.0 首个 PATCH 偿还承诺加强
- ✅ R-227.5.1 装饰豁免延伸（mono uppercase thead + Tag chip）
- ✅ R-302.5 emoji 业务豁免 sustained
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.19 七 PATCH sustained）
- ✅ R-313 rgba 豁免边界 sustained（无新增豁免）
- ✅ R-394 auto-fit 响应式 sustained
- ✅ D2 双兼模式延伸（v0.5.17 立约）
- ✅ Hero placeholder 模式 sustained（v0.5.18 部分聚合先例延伸）
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 13 屏 + Admin/SavedReports/tab_access/AdminAudit/AdminBudgets byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ **归档锁定手册** → 当前文件；分支 `feat/v0.5.19-admin-recovery`
2. ✅ **R-495 字面严防纪律**：commit 1 收尾**强制**双 `git grep -F` 双闭环 byte-equal — 空格/逗号差异立即 reset 重写
3. ✅ **Stage 3 §2 R-480 优先落地纪律**：Step 1 baseline 后**立即**落 R-480 brandSoft 8% 字面 — 沿用 v0.5.17/18 模式
4. ✅ **D1/D2 双绝对支持严守**：不动 Shell.jsx + Shared.jsx；R-192 + R-365 双红线宪法级
5. ✅ **Q2 VRP 局部例外纪律**：inline trophy svg path；CHANGELOG 显式标注"VRP 局部例外"边界
6. ✅ **R-491 + R-492 动效协同手测**：transition 0.2s + box-shadow 选中浮起感
7. ✅ **R-493 NaN 守护手测**：mock msg_count=0 显 0.0% 非 NaN%
8. ✅ **R-494 LineChart 高度固定**：90d 大数据折线清晰
9. ✅ **Q1 动态 Tag chip 实测**：7d/30d/90d 切换 last N days 体现
10. ✅ **技术债登记加强叙事**：KpiCard + PeriodTab + medal 累计第三次复用确认 + v0.6.0 偿还承诺加强
11. ✅ **里程碑叙事**：⭐ admin 三部曲完结 / Inset 8% 六处铁律加冕 / borderLeft 25% 三处加冕 / R-495 字面严防立约
12. ✅ **6 屏 + 3 屏视觉一致性肉眼校验**：commit 1 收尾强制逐屏切换肉眼对比
13. ✅ **范围抗诱惑**：不抽 KpiCard 入 Shared / 不改 Shell topbar / 不 inline chart / 不动 I.medal — R-485/486/487/489 + D1/D2 双 sustained

---

🛡️ **执行者收到 GO 信号；KNOT admin 顶层屏三部曲收官战正式启动；Inset 8% 闭环第六处铁律加冕 + borderLeft 25% 第三处闭环铁律加冕 + R-495 字面严防死守立约 + Q2 VRP 局部例外原则 + 技术债登记加强五大里程碑同时落地。**
