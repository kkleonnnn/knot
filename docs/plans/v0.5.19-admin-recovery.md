# v0.5.19 — (C5+) AdminRecovery 屏复刻 — Stage 1 草案

> **状态**：📝 **Stage 1 草案**（待 Stage 2 资深+Codex 评审 / Stage 3 v0.4 守护者终审）
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **里程碑**：**admin 顶层屏三部曲完成** (v0.5.17 Audit + v0.5.18 Budgets + **v0.5.19 Recovery**) + **Inset 8% 闭环第六处** + **borderLeft 25% 第三处闭环**（铁律持续扩展）

---

## 0. 关键事实（Stage 1 起手陈述）

**Demo 与现实业务模型相容** — Recovery 视觉重构 + 业务字段 byte-equal 双轨：

| 维度 | Demo 178 行 | 现实业务 |
|---|---|---|
| 数据源 | 静态 mock | `/api/admin/recovery-stats?period=` GET + 后端 baked R-19 legacy filter |
| 字段 | 3 KPI mock + 30d data array + 1 user mock | stats.total_recovery_attempts / total_messages / period_days / by_day[].date+count / top_users[].user_id+username+count+msg_count |
| Chart | custom SVG (gradient + line + area + dots) | `<LineChart>` from Shared.jsx — **R-365 sustained 不 inline 重写** |
| 时段 tabs | topbar right slot | content area 内 — **R-192 13 props 宪法级严守不移 topbar** |

**裁定**：业务字段 + Chart 实现 byte-equal；视觉风格全面对齐 demo（emoji 偿还 / hex 清零 / KPI accent / brandSoft inset rules / CSS Grid table / svg icons）。

---

## 1. 决议表（D1-D9 + Q1-Q5 草案 — 待 Stage 2/3 终审）

| ID | 推荐 | 备注 |
|---|---|---|
| **D1** 时段 tabs 位置 | **B 保 content area + 视觉重构** | R-192 13 props 宪法级红线 sustained；不动 Shell topbar；视觉补偿在 tabs 风格（mono + brandSoft + minHeight 30） |
| **D2** Chart 实现 | **B 保 LineChart from Shared** | R-365 Shared 0 改动绝对红线 sustained；视觉补偿在 chart card 容器（brandSoft border + Tag chip header） |
| **D3** Top user table → CSS Grid | A | v0.5.16/17/18 sustained — HTML `<table>/<thead>/<tbody>/<th>/<td>/<tr>` 全删 → CSS Grid 5-col + Avatar |
| **D4** Emoji 全清 | A | 🛡️ / 📈 / 🏆 / 📌 → svg icons + 文案 |
| **D5** Hex 全清 R-286 | A | `'#fff'` (period btn active) → `T.sendFg`（R-450 sustained — 严禁 'white'）|
| **D6** KPI 第 3 卡 accent | A | 自纠正率使用 `T.accent` color + fontWeight 700（与 demo 一致 — 视觉强调）|
| **D7** Rules note brandSoft inset | A | 2 条 — R-19 守护 + 数据源；borderLeft 3px 25% + bg 8% + Tag chip mono uppercase |
| **D8** 行数预算 ≤ 380 | A | 152 → ≤ 380（与 SavedReports/AdminAudit/AdminBudgets 同 LIMIT）；LIMITS 33 → 34 条 |
| **D9** Helpers 复用模式 | A inline 复制 | KpiCard / Tab helper inline；技术债登记 sustained — v0.6.0 移入 Shared 承诺 |
| **Q1** Chart header Tag chip | A 加 "last 30 days" | demo 有；视觉对齐；纯展示无业务影响 |
| **Q2** Medal icon 来源 | **B inline svg path** | Shared.jsx 无 `I.medal`；inline 14×14 svg path（trophy/medal shape）取代 🏆 emoji |
| **Q3** Avatar size | A 22×22 sustained | 与 v0.5.17 AdminAudit R-410 模式一致；非 demo 24 — 全站一致性优先 |
| **Q4** Toast 业务文案 byte-equal | A sustained | `加载失败` 字面 byte-equal |
| **Q5** v0.6 Shared 移植承诺 | A sustained | CHANGELOG 标注 — KpiCard + medal/chart svg icons 累计第三次复用确认（前两次 v0.5.17/18 ActionChip/StatusDot/EnabledChip） |

---

## 2. 红线草案（R-466 ~ R-490，25 条）

### 契约守护（R-466 ~ R-470）

| ID | 内容 |
|---|---|
| **R-466** | `AdminRecoveryScreen` 5 props 签名 byte-equal（T / user / onToggleTheme / onNavigate / onLogout）|
| **R-467** | 3 useState slots byte-equal（period / stats / loading — 数量+类型+初值）|
| **R-468** | api endpoint URL byte-equal — `api.get(\`/api/admin/recovery-stats?period=${period}\`)` |
| **R-469** | period 3 values byte-equal — `['7d', '30d', '90d']` + useEffect dependency [period] |
| **R-470** | stats 业务字段访问 byte-equal — total_recovery_attempts / total_messages / period_days / by_day[].date+count / top_users[].user_id+username+count+msg_count（10 字段）|

### 视觉重构（R-471 ~ R-480）

| ID | 内容 |
|---|---|
| **R-471** | Topbar `"🛡️ System Recovery 趋势"` → `"System Recovery 趋势"` 删 emoji |
| **R-472** | 时段 tabs 视觉重构 — 内容区保留（R-192 sustained）+ mono + brandSoft active + minHeight 30 + borderRadius 6 |
| **R-473** | **KPI 3 cards grid** — `repeat(auto-fit, minmax(180px, 1fr))` R-394 sustained + KpiCard inline helper（label + value + unit + hint） |
| **R-474** | **KPI 第 3 卡 accent** — 自纠正率 value color = T.accent + fontWeight 700 + 34px（D6） |
| **R-475** | Chart card — header layout：svg chart icon (14×14) + "每日自纠正次数趋势" + Tag chip "last 30 days" + brandSoft border + radius 12 + padding 16 18 |
| **R-476** | `<LineChart>` from Shared.jsx props byte-equal — data/stroke/fill/labelColor/gridColor/width/height（R-365 sustained）|
| **R-477** | Top user table HTML 全删 → CSS Grid 5-col + 表头 brandSoft 8% + mono + 0.06em + uppercase + fontWeight 500 |
| **R-478** | Top user header — svg medal icon (14×14) + "高频自纠正用户" + Tag chip `top ${len}` brand |
| **R-479** | Top user row — `#` rank mono T.accent fontWeight 600 + Avatar 22×22 brandSoft（R-410 sustained）+ username + id mono muted + count/msg_count/rate 全 mono |
| **R-480** | **R-466 brandSoft 8% 闭环第六处** — `color-mix(in oklch, ${T.accent} 8%, transparent)` 字面 byte-equal v0.5.14/15/16/17/18/**19**（6 屏命中）|

### 通用约束守护（R-481 ~ R-484）

| ID | 内容 |
|---|---|
| **R-481** | **R-467 borderLeft 3px 25% 第三处闭环** — `borderLeft: \`3px solid color-mix(in oklch, ${T.accent} 25%, transparent)\`` 与 SavedReports R-356 + AdminBudgets R-465 字面 byte-equal（3 屏）|
| **R-482** | grep `#[0-9a-fA-F]{3,6}` AdminRecovery \| grep -v boxShadow \| grep -v rgba = 0 命中 |
| **R-483** | grep `🛡️\|📈\|🏆\|📌` AdminRecovery = 0 |
| **R-484** | Submit btn `'#fff'` → `T.sendFg`（R-450 sustained — 严禁 'white' 字面）|

### 范围 + 契约（R-485 ~ R-490）

| ID | 内容 |
|---|---|
| **R-485** | App/api/index.css/main/utils/Shared/Shell/decor/13 屏（不含 AdminRecovery）/Admin/SavedReports/tab_access/AdminAudit/AdminBudgets 0 改 |
| **R-486** | admin/ 4 子模块 0 改 |
| **R-487** | chat/ 7 子模块 0 改 |
| **R-488** | App.css 0 行 diff + AdminRecovery 0 新 className（除 `cb-sb`）|
| **R-489** | KnotLogo R-199.5/222 sustained 仅 Shared+Login+Shell 三文件 |
| **R-490** | 三处版本同步 0.5.18 → 0.5.19 + LIMITS 33 → 34（新增 `frontend/src/screens/AdminRecovery.jsx: 380`）|

---

## 3. 9 子步骤执行顺序

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + LIMIT dict 新增 380 + **R-480 brandSoft 8% 闭环字面率先落地**（v0.5.17/18 模式 sustained）| R-480/R-490 |
| 2 | Topbar 删 🛡️ emoji → "System Recovery 趋势" | R-471 |
| 3 | 时段 tabs 视觉重构 — content area 保留 + mono + brandSoft active + minHeight 30 | R-472/D1 |
| 4 | KPI 3 cards grid + KpiCard inline helper + 第 3 卡 accent T.accent + R-394 auto-fit | R-473/R-474 |
| 5 | Chart card — svg chart icon header + Tag chip "last 30 days" + LineChart byte-equal | R-475/R-476 |
| 6 | Top user table HTML 全删 → CSS Grid 5-col + thead 视觉应用 R-480 + svg medal icon + Top Tag chip | R-477/R-478/R-479 |
| 7 | Rules note 2 条 brandSoft inset + **R-481 borderLeft 3px 25% 第三处闭环** + Tag chip + 📌 删 | R-481 |
| 8 | emoji 🛡️/📈/🏆/📌 全清 → svg icons + hex `'#fff'` → T.sendFg + 全 grep | R-483/R-484/R-482 |
| 9 | 三处版本同步 0.5.18→0.5.19 + grep 闭门（hex 0 / emoji 0 / R-480 闭环 6 文件 / R-481 闭环 3 文件）| R-490 |

---

## 4. 关键代码意图（提前样化）

### R-480 brandSoft 8% 闭环第六处字面（KNOT 视觉铁律持续扩展）

```jsx
// thead bg + Avatar bg + Tag chip bg — 多处复用同字面
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // 与 R-323/R-372/R-386/R-409/R-444 byte-equal
```

### R-481 borderLeft 3px 25% 第三处闭环

```jsx
// Rules note borderLeft 字面与 SavedReports R-356 + AdminBudgets R-465 字面 byte-equal
borderLeft: `3px solid color-mix(in oklch, ${T.accent} 25%, transparent)`,
```

### R-473 KpiCard inline helper

```jsx
function KpiCard({ T, label, value, unit, hint, accent }) {
  return (
    <div style={{
      background: T.card, border: `1px solid ${T.border}`,
      borderRadius: 12, padding: '18px 20px',
      display: 'flex', flexDirection: 'column', gap: 6, minWidth: 0,
    }}>
      <div style={{ fontSize: 12, color: T.muted, letterSpacing: '0.01em' }}>{label}</div>
      <div style={{ display: 'flex', alignItems: 'baseline', gap: 6 }}>
        <div style={{
          fontSize: 34, fontWeight: 700, letterSpacing: '-0.02em',
          color: accent ? T.accent : T.text,
          fontFamily: T.sans, lineHeight: 1.05,
        }}>{value}</div>
        {unit && <div style={{ fontSize: 14, color: T.muted, fontWeight: 500 }}>{unit}</div>}
      </div>
      <div style={{ fontSize: 11, color: T.muted, fontFamily: T.mono, letterSpacing: '0.02em' }}>{hint}</div>
    </div>
  );
}
```

### R-472 PeriodTab inline helper

```jsx
function PeriodTab({ T, label, active, onClick }) {
  return (
    <button onClick={onClick} style={{
      height: 30, padding: '0 14px',
      background: active ? T.accent : 'transparent',
      color: active ? T.sendFg : T.muted,
      border: `1px solid ${active ? T.accent : T.border}`,
      borderRadius: 6, fontSize: 12, fontFamily: T.mono,
      fontWeight: 500, cursor: 'pointer',
    }}>{label}</button>
  );
}
```

### R-478 Medal svg icon (Q2 — inline svg path)

```jsx
// 14×14 trophy/medal svg path — Shared 无 I.medal
<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"
     style={{ color: T.accent }}>
  <circle cx="12" cy="8" r="6"/>
  <path d="M8.21 13.89L7 23l5-3 5 3-1.21-9.12"/>
</svg>
```

### R-477 Top user CSS Grid 5-col

```jsx
<div style={{
  display: 'grid', gridTemplateColumns: '64px 1.4fr 1fr 1fr 1fr',
  padding: '8px 18px',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // R-480 闭环
  borderBottom: `1px solid ${T.border}`,
  fontSize: 11, color: T.subtext, fontFamily: T.mono,
  fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase',
}}>
  <span>#</span><span>USER</span><span>自纠正次数</span><span>消息数</span><span>自纠正率</span>
</div>
```

---

## 5. 文件级改动清单（草案）

```
frontend/src/screens/AdminRecovery.jsx  [EDIT 152 → ≤ 380 行]
                                        Step 1-9 顺序锁死（见 §3）
scripts/check_file_sizes.py             [EDIT LIMITS 33 → 34]
                                        - "frontend/src/screens/AdminRecovery.jsx": 380 [R-490]
knot/main.py                            [EDIT version 0.5.18 → 0.5.19]  [R-490]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.18 → 0.5.19]  [R-490]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.18 → 0.5.19]  [R-490]

frontend/src/screens/admin/*            [unchanged]   [R-486]
frontend/src/screens/chat/*             [unchanged]   [R-487]
frontend/src/screens/{Admin,SavedReports,AdminAudit,AdminBudgets,tab_access}.jsx [unchanged] [R-485]
frontend/src/screens/ (其余 13 屏)       [unchanged]   [R-485]
frontend/src/{App,api,Shell,Shared,utils,main,index.css}.jsx 等 [unchanged] [R-485]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]   [R-485]
frontend/src/App.css                    [unchanged 0 行 diff]   [R-488]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.19 段]
                                        - R-466~R-490 偿还表
                                        - **admin 顶层屏三部曲完成里程碑** (v0.5.17 Audit + v0.5.18 Budgets + v0.5.19 Recovery)
                                        - **Inset 8% 闭环第六处** (6 屏 byte-equal)
                                        - **borderLeft 25% 第三处闭环** (3 屏 byte-equal — SavedReports/AdminBudgets/AdminRecovery)
                                        - **KpiCard + PeriodTab helpers + medal svg** 累计第三次复用确认 (v0.6 Shared 移植承诺加强)
                                        - Loop Protocol v3 第 20 次施行
CLAUDE.md                               [EDIT]
                                        - v3 协议施行历史回顾表 +1 行 v0.5.19（admin 三部曲完成）
                                        - v0.5.x 路线图 v0.5.19 ⏳ → ✅
                                        - v0.5.20+ 行更新
docs/plans/v0.5.19-admin-recovery.md    [当前文件 Stage 1 → LOCKED 后存档]
```

---

## 6. 验收清单（草案）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（CI 干净 env）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS）
- [ ] `npm run build` 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.19**

### 视觉强化验证

- [ ] **R-480 brandSoft 8% 闭环第六处**：`git grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — ResultBlock/SavedReports/tab_access/AdminAudit/AdminBudgets/**AdminRecovery** **6 文件**命中
- [ ] **R-481 borderLeft 25% 第三处闭环**：`git grep "borderLeft.*3px solid color-mix(in oklch, \${T\.accent} 25%" frontend/src/screens/` — SavedReports/AdminBudgets/**AdminRecovery** **3 文件**命中
- [ ] **R-482 hex 0 命中**：grep `#[0-9a-fA-F]{3,6}` AdminRecovery \| grep -v boxShadow \| grep -v rgba = 0
- [ ] **R-483 emoji 全清**：grep `🛡️\|📈\|🏆\|📌` AdminRecovery = 0
- [ ] **R-477 HTML table 全删**：grep `<table\|<thead\|<tbody\|<th\|<td\|<tr` AdminRecovery = 0
- [ ] **R-474 KPI accent 第 3 卡**：grep `accent={true}` 或 `color: T.accent` 自纠正率区命中
- [ ] **R-475 Chart card Tag chip**：grep `last 30 days` 或 `last\s.*days` 命中

### 契约守护（首要红线）

- [ ] **R-466** AdminRecoveryScreen 5 props 签名 byte-equal
- [ ] **R-467** 3 useState slots byte-equal（period / stats / loading）
- [ ] **R-468** api URL byte-equal — `/api/admin/recovery-stats?period=`
- [ ] **R-469** period 3 values + useEffect dep byte-equal
- [ ] **R-470** stats 业务字段 10 个 byte-equal（含 by_day/top_users 嵌套字段）

### 范围守护

- [ ] **R-485** App/api/index.css/main/utils/Shared/Shell/decor/13 屏 + Admin/SavedReports/tab_access/AdminAudit/AdminBudgets `git diff` = 0
- [ ] **R-486** admin/ 4 子模块 0 改
- [ ] **R-487** chat/ 7 子模块 0 改
- [ ] **R-488** App.css 0 行 diff
- [ ] **R-489** KnotLogo sustained 仅 3 文件

### 端到端业务回归

- [ ] 进 admin → 切 admin-recovery nav → loading → stats 加载
- [ ] 7d/30d/90d 时段切换 → 重新 fetch + KPI 更新
- [ ] KPI 3 cards：自纠正总次数 / 覆盖消息数 / 自纠正率（第 3 卡 accent T.accent）
- [ ] LineChart 渲染 by_day 数据（fill + stroke）
- [ ] Top user table CSS Grid 5-col + rank # mono + Avatar 22 + username + id + count + msg_count + rate
- [ ] empty state — by_day.length === 0 / top_users.length === 0
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— KPI grid auto-fit
- [ ] **light + dark 双模式**
- [ ] **Inset 8% 6 屏视觉一致** (ResultBlock / SavedReports / DataSources / AdminAudit / AdminBudgets / AdminRecovery)
- [ ] **borderLeft 25% 3 屏视觉一致** (SavedReports Quote / AdminBudgets Rules note / AdminRecovery Rules note)
- [ ] v0.5.0~v0.5.18 既有功能端到端正常

---

## 7. Commit 序列（草案 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.19): AdminRecovery.jsx 视觉重构 — Inset 8% 闭环第六处 + borderLeft 25% 第三处闭环 + KpiCard + PeriodTab + medal svg (R-466~R-490)` | npm build + 全 grep 守护 + 端到端业务 |
| 2 | `chore(v0.5.19): version bump 0.5.18→0.5.19 三处同步 + LIMITS 33→34 新增 AdminRecovery 380 (R-490)` | 全闸门 + version=0.5.19 |
| 3 | `docs(v0.5.19): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + admin 顶层屏三部曲完成里程碑 + Inset 8% 六处 + borderLeft 25% 三处 + KpiCard/PeriodTab v0.6 技术债登记加强` | 全闸门 |

---

## 8. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审待启动（**第 20 次施行**预计）
- ✅ § Visual Replication Protocol 适用（v0.5.8 立约）
- ⏳ 等 Stage 2 资深 + Codex 评审 → Stage 3 v0.4 守护者终审 → LOCKED
- ✅ **AdminRecovery = admin 顶层屏三部曲收官**（Audit + Budgets + Recovery）
- ✅ **Inset 8% 闭环字面第六处扩展**（v0.5.14/15/16/17/18 → v0.5.19 R-480）— 视觉铁律持续扩展
- ✅ **borderLeft 25% 第三处闭环**（SavedReports R-356 / AdminBudgets R-465 / **AdminRecovery R-481**）— 设计语言铁律第二维度扩展
- ✅ **KpiCard + PeriodTab + medal svg 累计第三次复用** — v0.6 Shared 移植承诺加强（v0.5.17/18/19 共 6+ inline helpers）
- ✅ D1 R-192 13 props 宪法级 sustained — 时段 tabs 不移 topbar
- ✅ D2 R-365 Shared 0 改动 sustained — LineChart 字面复用
- ✅ R-394 auto-fit 响应式 sustained
- ✅ R-286 hex 0 命中 sustained（v0.5.13~v0.5.19 七 PATCH）
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 13 屏 + 5 admin 顶层屏 byte-equal

---

## 9. 风险点 + Stage 2/3 评审建议方向

### 高风险（建议 Stage 2/3 重点关注）

1. **R-468 api endpoint byte-equal** — `/api/admin/recovery-stats?period=` URL 字面不能变（后端硬契约）
2. **R-470 stats 10 业务字段 byte-equal** — top_users / by_day 嵌套字段（user_id/username/count/msg_count/date/count）严格守护
3. **D2 Chart 实现裁定** — 是否 inline custom SVG (与 demo 1:1) vs 保 LineChart (R-365 sustained)？守护者重点评审

### 中风险

4. **Q2 medal icon 来源** — inline svg vs 用 I.shield/I.book 等替代？视觉准确度 vs Shared 0 改纪律
5. **D1 时段 tabs 位置** — 守护者可质疑：是否应扩 Shell 13 props 加 topbar right slot？（v0.6 候选）
6. **Q1 Chart Tag chip "last 30 days"** — 是否硬编 30d？应动态 `last ${period_days} days`？

### 低风险

7. **D4 emoji 全清** — v0.5.16~18 模式 sustained
8. **D6 KPI accent** — 与 demo 一致
9. **D8 行数预算** — 152 → ≤ 380 充裕

---

📝 **Stage 1 草案完成 — 等待资深架构师 ack 启动 Stage 2/3 评审或简化协议直接 Stage 4。**
