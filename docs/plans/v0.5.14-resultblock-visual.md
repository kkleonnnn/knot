# v0.5.14 — (C5+) ResultBlock 视觉大重构 — v0.5.x ResultBlock 维度收官之战 — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 15 次施行**）
> **里程碑**：v0.5.x ResultBlock 维度收官之战；**R-306/315 红线撤回首例** + **R-341 v0.5 行数收官** + **R-227.5.1 装饰豁免延伸** 三大设计先例同时落地

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**D3/D8 准许 + Q5 强制 R-342 + Codex 精确指令 + Stage 3 加 R-341/R-342/R-343/R-344**。

**Stage 2 关键加码**：
- **D3 反向修正准许** — v0.5.13 R-306/315 TokenPill **受控撤回**（架构判定：严格复刻 > 局部推测性红线 — **v0.5.x 系列红线撤回首例**）
- **D8 Final Ack + R-341** — LIMIT 420→440 是 v0.5 阶段**最后一次微调**；v0.6 必须开启子组件拆分
- **Codex 精确指令**：①Observation `color-mix(in oklch, T.accent 8%, transparent)` 精确百分比 ②Table thead 删 uppercase **同时补** `letter-spacing: normal` ③SQL accordion 时长 `flex: 1 + text-align: right`

**Stage 3 关键加码**：
- **R-342 SavedReports 渲染一致性守护**（Q5 强制）— 内嵌渲染不塌陷
- **R-343 Suggestion Chips 高度锚点** — height 28 + line-height 1 + center
- **R-344 SQL `<>` Text Icon T.mono** — 几何对称（`<` 与 `>` 等宽）

红线总数 28（**R-317~R-344**）。子步骤增至 **7 步**。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** Observation card brandSoft inset | ✅ A + Codex `color-mix(in oklch, T.accent 8%, transparent)` | Stage 2 Codex 精确指令 |
| **D2** Suggestion chips height 28 + chevron | ✅ A + R-343 高度锚点 | Stage 3 加码 |
| **D3** Token meter Pill→Inline | ✅ **A 反向修正准许** | Stage 2 架构判定：红线服从视觉真理；R-306/315 受控撤回 |
| **D4** agent_costs chip pill 999 | ✅ A | Stage 1 |
| **D5** Table thead T.bgInset + 删 uppercase | ✅ A + Codex `letter-spacing: normal` 补齐 | Stage 2 Codex |
| **D6** SQL accordion `<>` + 时长右侧 | ✅ A + Codex `flex: 1 + text-align: right` + R-344 T.mono | Stage 2/3 |
| **D7** "OBSERVATION" 装饰豁免 | ✅ A R-227.5.1 延伸 | Stage 2 仅 ResultBlock Insight Inset 容器；其他屏"洞察"保中文 |
| **D8** LIMIT 420→440 | ✅ **审慎准许 Final Ack + R-341 v0.5 最后一次** | v0.6 必须开启子组件拆分 |
| **Q1-Q4** | ✅ 沿 Stage 1 提议 | Stage 1 |
| **Q5** SavedReports 守护 | ✅ **R-342 强制加码** | Stage 3 守护者强制 |

---

## 2. 红线最终清单（28 条 R-317~R-344）

### Stage 1 草案原 24 条（保留；R-306/315 标注撤回）

| ID | 摘要 |
|---|---|
| **R-317~R-322** | 6 契约 sustained（7 props / msg 25 字段 / ERROR_KIND_META 7 kind / 7 layout / business helpers / 5 handlers）|
| **R-323** | Observation card 新视觉（D1 + Codex color-mix in oklch 8%） |
| **R-324** | Suggestion chips height 28 + radius 14 + chevron svg + T.content（D2） |
| **R-325** | **Token meter Pill→Inline stat + svg ↑↓ icon**（D3 — v0.5.13 R-306/315 撤回） |
| **R-326** | agent_costs chip pill 999 + bgInset border（D4） |
| **R-327** | Table thead T.bgInset + 删 uppercase + Codex letter-spacing normal（D5） |
| **R-328** | SQL accordion `<>` + 时长 mono 右侧 + Codex flex:1 text-align right（D6） |
| **R-329** | "OBSERVATION" mono 装饰豁免 R-227.5.1 延伸（D7） |
| **R-330** | R-286 hex 0 命中 sustained（v0.5.13 R-298 延续） |
| **R-331** | R-302.5 emoji 业务豁免 sustained（ErrorBanner 7 emoji 保留） |
| **R-332** | ResultBlock.jsx ≤ 440 行（D8 + R-341 v0.5 最后微调） |
| **R-333** | backend 432 tests / 7 contracts / 72 routes |
| **R-334** | App/api/index.css/main/utils/Shared/Shell/decor/17 屏 byte-equal |
| **R-335** | chat/ 其他 6 子模块 byte-equal |
| **R-336** | 三处版本同步 0.5.13→0.5.14 |
| **R-337** | CSS 0 污染（App.css 0 diff） |
| **R-338** | KnotLogo R-199.5/222 sustained — 不蔓延 ResultBlock |
| **R-339** | color-mix in oklch 精度守护（R-312 sustained） |
| **R-340** | rgba 仅 boxShadow（R-313 sustained） |

### Stage 2 新增 1 条（D8 配套）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-341** | **v0.5.x ResultBlock 行数收官** — LIMIT 440 是 v0.5 阶段最后一次微调；v0.6 必须开启子组件拆分（MetricCard.jsx / TableContainer.jsx / InsightCard.jsx / BudgetBanner.jsx / ErrorBanner.jsx / TokenMeter.jsx 候选） | check_file_sizes LIMIT 420→440；CHANGELOG + CLAUDE.md 显式锁定 v0.6 必须拆分子组件 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-342** | **SavedReports 渲染一致性守护**（Q5 强制）— ResultBlock 视觉自动同步到 SavedReports.jsx 内嵌渲染；conditional rendering 不塌陷 | commit 1 后强制手测：① 进 SavedReports ② 预览保存的报告 ③ bgInset 固定宽度容器显示正常 ④ Suggestion chips 不出现（无 onFollowup）⑤ 收藏 star 不出现（无 onPin）⑥ 7 intent layout 全预览 |
| **R-343** | **Suggestion Chips 高度锚点** — height 28 + line-height 1 + align-items center；chevron svg 内部垂直居中 + 左侧 8px | inline `height: 28, lineHeight: 1, display: 'inline-flex', alignItems: 'center', gap: 8` |
| **R-344** | **SQL Accordion `<>` Text Icon T.mono** — 必须 mono 保几何对称（`<` 与 `>` 等宽）；严禁默认字体 | `<span style={{fontFamily: T.mono, ...}}>{'<>'}</span>` |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 7 步锁死）

> 先完成 Observation Inset 样式重写 → 再 Suggestion Chips 尺寸修正

| Step | 动作 | 红线 |
|---|---|---|
| 1 | Observation card 新视觉 — `color-mix(in oklch, T.accent 8%, transparent)` bgInset + svg info icon + "OBSERVATION" mono 装饰 | R-323/329/339 + Codex |
| 2 | Suggestion chips height 28 + radius 14 + chevron svg + T.content + line-height 1 + center + gap 8 | R-324/343 |
| 3 | **Token meter 反向修正** — TokenPill 撤回 → inline stat + svg ↑↓ icon | R-325（v0.5.13 R-306/315 撤回） |
| 4 | agent_costs chip pill 999 + bgInset border | R-326 |
| 5 | Table thead T.bgInset + 删 uppercase + `letter-spacing: normal` 补齐 | R-327 + Codex |
| 6 | SQL accordion `<>` text icon T.mono + 时长 mono `flex: 1 + text-align: right` | R-328/344 + Codex |
| 7 | grep 守护 — R-330 hex / R-331 emoji / R-338 KnotLogo / R-339 color-mix in oklch / R-340 rgba | 全 sustained |
| 收尾 | R-342 SavedReports 内嵌渲染手测 + 7 intent 端到端 + R-335 chat 仅 ResultBlock diff + npm build + Token Meter 1:1 像素对比 | 见 §5 验收 |

### Stage 3 §3 视觉校准要求

```bash
# Token Meter 1:1 像素对比 demo
# 启动 backend + frontend dev server + 登录 + 提问触发任意 intent
# 浏览器 dev tools 测 Token meter 区域尺寸 / 间距 / svg ↑↓ icon 位置
# 与 demo thinking.jsx L115-118 区域 1:1 像素对比
# 偏差立即调 inline style
```

### Stage 2 D3 反向修正纪律

v0.5.13 R-306/315 锁定的 TokenPill 模式正式**撤回**：
- CHANGELOG v0.5.14 段**显式标注**："v0.5.13 R-306/315 TokenPill 受控撤回（架构判定：严格复刻 > 局部推测性红线）"
- **v0.5.x 序列第一次红线撤回操作** — 设计先例
- 未来 PATCH 沿用此判定：**红线服从视觉真理**

### Stage 2 D8 + R-341 行数收官纪律

LIMIT 微调历程：
- 250（v0.5.3 R-111） → 400（v0.5.3 R-111 ack） → 420（v0.5.13 Q3 ack） → **440（v0.5.14 R-341 final ack）**

R-341 是 **v0.5 序列对 ResultBlock 行数最后一次扩张**；v0.6 必须开启子组件拆分：
- 候选 `MetricCard.jsx` / `TableContainer.jsx` / `InsightCard.jsx` / `BudgetBanner.jsx` / `ErrorBanner.jsx` / `TokenMeter.jsx`
- CLAUDE.md `v0.6 路线图`段落显式锁定

### Stage 3 R-342 SavedReports 守护

ResultBlock 通过 SavedReports.jsx 内嵌渲染时常见 props 缺失：
- ❌ `onFollowup` 缺失 → Suggestion chips **应隐藏**
- ❌ `onPin` 缺失 → 收藏 star **应隐藏**
- ❌ `onRetry` 缺失 → ErrorBanner retry **应隐藏**
- 重构**严禁**破坏 conditional rendering — 否则 SavedReports 预览塌陷

### Stage 2 Codex 精确指令汇总

| 项 | Codex 指令 |
|---|---|
| Observation card | `color-mix(in oklch, T.accent 8%, transparent)` 精确百分比 |
| Table thead | 删 uppercase **同时补** `letter-spacing: normal`（防 inherited 残留） |
| SQL accordion | 时长 mono `flex: 1` + `text-align: right`（不同容器宽度都贴右） |

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/chat/ResultBlock.jsx   [EDIT ~420 → ≤ 440 行]
                                            Step 1: Observation card brandSoft inset
                                                    + color-mix(in oklch, T.accent 8%, transparent) [R-323/339]
                                                    + svg info icon + "OBSERVATION" mono [R-329/227.5.1 延伸]
                                            Step 2: Suggestion chips height 28 + radius 14 + chevron svg
                                                    + line-height 1 + align-items center + gap 8 [R-324/343]
                                            Step 3: Token meter 反向修正 Pill→Inline stat + svg ↑↓ [R-325 撤回 R-306/315]
                                            Step 4: agent_costs chip pill 999 + bgInset border [R-326]
                                            Step 5: Table thead T.bgInset + 删 uppercase
                                                    + letter-spacing: normal [R-327 + Codex]
                                            Step 6: SQL accordion `<>` T.mono + 时长 mono flex:1 text-align:right [R-328/344 + Codex]
                                            Step 7: 守护 grep — hex/emoji/KnotLogo/color-mix/rgba
                                            守护: 6 契约 sustained [R-317~R-322]
                                                  + R-286 hex 0 + R-302.5 emoji 业务豁免 sustained [R-330/331]
                                                  + inline styles only [R-337]

scripts/check_file_sizes.py                 [EDIT LIMIT 微调 final]
                                            - "frontend/src/screens/chat/ResultBlock.jsx": 420 → 440 [D8 + R-341]

knot/main.py                                [EDIT version 0.5.13 → 0.5.14]  [R-336]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.13 → 0.5.14]  [R-336]
frontend/src/screens/Login.jsx              [EDIT 页脚字面 0.5.13 → 0.5.14]  [R-336]

frontend/src/screens/SavedReports.jsx       [unchanged 0 行 diff] [R-342 验证内嵌渲染]
frontend/src/screens/chat/ChatEmpty.jsx     [unchanged]              [R-335]
frontend/src/screens/chat/Composer.jsx      [unchanged]              [R-335]
frontend/src/screens/chat/Conversation.jsx  [unchanged]              [R-335]
frontend/src/screens/chat/ThinkingCard.jsx  [unchanged]              [R-335]
frontend/src/screens/chat/intent_helpers.js [unchanged]              [R-335]
frontend/src/screens/chat/sse_handler.js    [unchanged]              [R-335]
frontend/src/screens/ (其余 16 屏)           [unchanged]              [R-334]
frontend/src/App.jsx                        [unchanged]              [R-334]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-334]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]              [R-334]
frontend/src/App.css                        [unchanged 0 行 diff]    [R-337]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                    [unchanged]
backend (knot/) 任何文件除 main.py          [unchanged]              [R-333]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.14 段]
                                            R-317~R-344 偿还表
                                            + R-306/315 受控撤回首例（架构判定：严格复刻 > 局部推测性红线）
                                            + R-341 v0.5 ResultBlock 行数收官（v0.6 必须子组件拆分）
                                            + Loop Protocol v3 第 15 次施行
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.14
                                            - v0.5.x 路线图 v0.5.14 ⏳ → ✅（ResultBlock 维度收官）
                                            - v0.6 路线图新增"ResultBlock 子组件拆分"承诺（R-341 锁定）
docs/plans/v0.5.14-resultblock-visual.md    [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（30 条 LIMITS，ResultBlock 420→440）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.14**

### Stage 2/3 强化验证

- [ ] **D3 反向修正**：grep `TokenPill` ResultBlock.jsx = 0（v0.5.13 引入字面已撤回）；token meter 区为 inline stat + svg ↑↓ icon
- [ ] **R-323 Observation Inset**：grep `color-mix(in oklch[^)]*T\.accent[^)]*8%` 命中
- [ ] **R-327 Table thead**：grep `T\.bgInset` thead 命中；grep `letterSpacing.*['"]?normal['"]?` 命中；thead 区 grep `textTransform.*uppercase` = 0
- [ ] **R-328 SQL accordion**：grep `<>` 命中；时长 `flex: 1` + `text-align: right`（或 `textAlign: 'right'`）
- [ ] **R-329 "OBSERVATION"**：grep `OBSERVATION` ≥ 1（仅 ResultBlock Insight）；其他屏 "洞察" 中文保留
- [ ] **R-341 LIMIT**：check_file_sizes ResultBlock 440；CLAUDE.md v0.6 路线图含"子组件拆分"承诺字面
- [ ] **R-342 SavedReports**：手测 7 intent layout 预览正常；conditional rendering 隐藏 chips/star/retry
- [ ] **R-343 Suggestion 高度锚点**：grep `height.*28` + `lineHeight.*1` 命中
- [ ] **R-344 `<>` T.mono**：`<>` 容器 `fontFamily: T.mono` 命中

### Sustained 红线

- [ ] **R-330 hex 0**：`grep -nE "#[0-9a-fA-F]{3,6}" ResultBlock.jsx | grep -v boxShadow` = 0
- [ ] **R-331 emoji 业务豁免**：ErrorBanner 7 emoji 保留
- [ ] **R-338 KnotLogo 3 文件**：grep `KnotLogo|KnotMark|KnotWordmark` frontend/src/ 仍 3 文件
- [ ] **R-339 color-mix in oklch**：每个 color-mix 含 `in oklch`
- [ ] **R-340 rgba 仅 boxShadow**：rgba 上下文全在 boxShadow

### 契约守护

- [ ] **R-317**：7 props 签名 byte-equal
- [ ] **R-318**：msg 25 字段解构 byte-equal
- [ ] **R-319**：ERROR_KIND 7 kind/icon/title byte-equal
- [ ] **R-320**：7 layout 分支字面 byte-equal
- [ ] **R-321**：业务逻辑 byte-equal
- [ ] **R-322**：5 handlers 调用方式不变

### 范围守护

- [ ] **R-334**：App/api/index.css/main/utils/Shared/Shell/decor/16 屏 `git diff` = 0
- [ ] **R-335**：chat/ 其他 6 子模块 `git diff --stat origin/main HEAD -- frontend/src/screens/chat/` 仅 ResultBlock
- [ ] **R-337**：App.css `git diff` = 0
- [ ] SavedReports.jsx `git diff` = 0（R-342 守护 — 仅手测验证）

### 端到端

- [ ] 7 intent 触发并渲染：metric_card/line/bar/pie/rank_view/detail_table/retention_matrix
- [ ] 收藏 star（v0.5.13 R-303）双态切换 + onPin API
- [ ] ErrorBanner 7 类 kind 渲染（emoji 保留）
- [ ] BudgetBanner svg + 关闭逻辑
- [ ] CSV 下载 + SQL 折叠 `<>` icon + 时长右侧
- [ ] followup chip 新视觉点击触发查询
- [ ] **SavedReports 预览 7 intent 全渲染**（R-342）
- [ ] **Token Meter 1:1 像素对比 demo**（Stage 3 §3）
- [ ] v0.5.0~v0.5.13 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.14): ResultBlock.jsx 视觉大重构 — Observation Inset + Suggestion 28h + Token Inline + Table bgInset + SQL <> (R-317~R-344, R-306/315 受控撤回)` | npm build + 7 intent + R-342 SavedReports + Token Meter 像素对比 + R-335 chat 仅 ResultBlock |
| 2 | `chore(v0.5.14): version bump 0.5.13→0.5.14 三处同步 + ResultBlock LIMIT 420→440 v0.5 final ack (R-336/341)` | 全闸门 + version=0.5.14 + 30 条 LIMITS |
| 3 | `docs(v0.5.14): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + R-306/315 撤回首例存档 + v0.6 子组件拆分承诺` | 全闸门 |

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 15 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 28（v0.5.14 区段 R-317~R-344）
- ✅ **红线撤回首例** — v0.5.13 R-306/315 TokenPill 受控撤回；设计先例（严格复刻 > 局部推测性红线）
- ✅ **R-341 v0.5 ResultBlock 行数收官** — 440 final ack；v0.6 必须开启子组件拆分
- ✅ **R-342 SavedReports 守护** — 视觉自动跟随到内嵌渲染时不塌陷
- ✅ **R-227.5.1 装饰豁免延伸** — "OBSERVATION" 仅 Insight 容器；其他屏"洞察"保中文
- ✅ R-330/331 hex+emoji sustained（v0.5.13 R-298/302.5 延续）
- ✅ R-339/340 color-mix in oklch + rgba 仅 boxShadow sustained
- ✅ R-251 视觉自动跟随设计模式 sustained
- ✅ 范围最小化 — 16 屏 + 11 子模块 + 8 核心非屏 + SavedReports + App.css 全部 byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.14-resultblock-visual`
2. ✅ D3 反向修正纪律 — TokenPill 字面完全撤回；commit 1 后 grep `TokenPill` = 0
3. ✅ R-341 v0.5 final ack 严守 — LIMIT 440 最后一次；超线立即裁剪
4. ✅ R-342 SavedReports 强制手测 — commit 1 收尾必须实际进 SavedReports 预览 7 intent
5. ✅ Stage 3 §3 像素级对比 — Token Meter 与 demo L115-118 1:1 dev tools 实测
6. ✅ Codex 精确指令严守 — color-mix 8% / letter-spacing normal / flex:1 text-align right
7. ✅ R-343 Suggestion 高度锚点 — height 28 + line-height 1 + center + chevron 8px
8. ✅ R-344 `<>` T.mono — 几何对称（`<` 与 `>` 等宽）
9. ✅ R-329 装饰豁免边界 — "OBSERVATION" 仅 Insight；其他屏"洞察"保中文
10. ✅ Step 1→7 顺序锁死
11. ✅ CHANGELOG 红线撤回首例叙事 — v0.5.14 段显式标注
12. ✅ CLAUDE.md v0.6 承诺锁定 — "子组件拆分"承诺字面
13. ✅ 范围抗诱惑 — table 表名 / chart 大改 / KnotMark 行内 / saved_reports 视觉一律不碰

---

🛡️ **执行者收到 GO 信号；v0.5.x ResultBlock 维度收官之战正式启动；R-306/315 红线撤回首例 + R-341 行数收官 + R-227.5.1 装饰豁免延伸三大设计先例同时落地。**
