# v0.6.4.7 — UI v2 favorites（SavedReports card radius 对齐）屏复刻（逐屏复刻第 7 屏 · 用户侧收尾）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + VRP grep-guard + **修正版本同步模型（4 无条件 ★CI 含 Login footer）**
> **基座**：worktree `v0647-fav` @ branch `v0.6.4.7-favorites`（off main `41f71ba` = v0.6.4.6）
> **源稿**：`knot_demo_ui/v0.6/artboards/favorites.jsx`（144 行）
> **目标**：`frontend/src/screens/SavedReports.jsx`（363，LIMIT 380）

## §0 范围 + adversarial 评估（结论：**1 PATCH · radius 对齐**，同 results 模式）

**adversarial finding**：SavedReports v0.5.15 已复刻（bookmark SavedItem + title block 22px/meta │/StatusDot frozen + brandSoft quote R-372 + thead T.bg + hex 0）。真实 v2 delta = **系统性 card radius 对齐**（同 chat-results）。已 grep 全部 ~10 radius 站点逐个分类（吸取 results 教训不漏）。

## §1 radius 全站点枚举映射（分类规则同 results：primary 数据卡 12 / alert banner 10 / inset+utility+input+chip 保）

**改动（3 站点）**：

| 元素 | 现 | → | 类 |
|---|---|---|---|
| 结果 table card L295 | 10 | **12** | primary 数据卡（matches demo L98）|
| warning banner L268 | 8 | **10** | bordered alert banner（同 results ErrorBanner）|
| runResult.error banner L280 | 8 | **10** | bordered alert banner |

**stays（枚举 + 理由）**：

| 元素 | radius | stays 理由 |
|---|---|---|
| SavedItem sidebar L87 | 6 | sidebar list item（matches demo SavedItem L11 = 6）|
| EmptyView 返回按钮 L138 | 6 | control button |
| title edit input L221 | 6 | input |
| truncated notice L275 | 6 | slim 无 border info pill（非 bordered banner；同 results inline error pill stays 6）|
| noteDraft textarea L289 | 6 | input |
| **原始问题 quote L333** | 6 | **brandSoft 8% inset（R-372/R-356 闭环字面），demo quote 亦 6；radius 不动 + R-372 color 字面不动** |
| SQL details L344 | 10 | utility/code collapsible（同 results SQL accordion stays 10）|

## §2 保 byte-equal（3 大不动守护 + 结构）

- **thead `T.bg` 灰底保留**（L308，v0.5.38 资深 established；**非** demo bgInset；同 results 守护）
- **原始问题 quote brandSoft 8% inset 字面不动**（L331-332，R-372/R-356 全站闭环铁律 `color-mix(in oklch, ${T.accent} 8%)` + `borderLeft 3px 25%`；**非** demo bgInset）
- **TopBar 按钮放置不动**（OOS）：demo 把 编辑/重跑/CSV/Excel 放 TopBar right（Btn sm）；产品放 content 内 pillBtnCompact 按钮行（v0.5.15 established）。改 placement = 结构改（动 AppShell topbarTrailing）→ OOS
- **title block / meta │ / StatusDot frozen 不动**（v0.5.15 已匹配 demo）
- 全业务逻辑 byte-equal：SavedReportsScreen 5 props（`{T,user,onToggleTheme,onNavigate,onLogout}`）+ loadReports/handleDelete/handleRun/handleSaveEdit/handleExport + EmptyView/DetailView/safeParseRows + INTENT_EMOJI 7 keys + api 5 endpoint + formatTime
- hex 0 sustained + R-302.5 banner emoji 业务豁免（⚠️/🔍/❌）

## §3 红线

| # | 红线 |
|---|---|
| R-UI2-F-1 | SavedReportsScreen 5 props 签名 byte-equal → App.jsx 0 diff |
| R-UI2-F-2 | thead T.bg 灰底 byte-equal（v0.5.38）|
| R-UI2-F-3 | 原始问题 quote brandSoft 8% inset R-372 字面不动（color + borderLeft）|
| R-UI2-F-4 | 全业务逻辑 byte-equal（5 handlers + EmptyView/DetailView + api 5 endpoint + INTENT_EMOJI 7 keys）|
| R-UI2-F-5 | hex 0（grep 排 rgba = 0 sustained）|
| R-UI2-F-6 | diff 仅 3 radius（table card →12 / 2 banner →10）；无结构 / 无业务 / TopBar 按钮 placement 不动 |
| R-UI2-F-7 | SavedReports ≤ 380 LIMIT（radius 改 0 净行）；其他屏/App/Foundation 0 改 |
| R-UI2-F-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-F-9 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.7；Shell L43 不动（条件式，stays v0.6.4.2）|

## §4 验收
1. 结果 table card radius 12 + warning/error banner 10；thead 灰底 + brandSoft quote + SQL details/sidebar/input 不变
2. `git diff origin/main -- frontend/src/App.jsx frontend/src/Shared.jsx` = 0
3. hex 0 + npm build exit 0 + check_file_sizes OK + R-PA-8 0
4. **4 处版本 → 0.6.4.7**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（收藏屏：sidebar 列表 + 详情 title block + 结果表 12 圆角 + brandSoft quote + 重跑/编辑/导出 + thead 灰底）

## §5 commit 序列
- **commit 1**：SavedReports 3 radius 对齐 + 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §6 协议合规
轻量 v3：执行者 Stage 1（adversarial 全 radius 枚举 = 3 改 + 7 stays，thead/brandSoft/业务/TopBar placement byte-equal）→ 守护者 Stage 3（adversarial 复核 radius 完整性 + thead T.bg + brandSoft R-372 + 契约 + 4 处版本）→ 逐 commit 直读 → 资深 push/merge + :8000 live。用户侧屏收尾（chat flow 6 屏 + favorites）。

## §9 LOCKED 归档（守护者 Stage 3 Accept + commit 1 ACK）

**守护者 Stage 3 = Accept 无修订（最干净一次）+ commit 1 ACK（`f0172d3`）**：diff 精确 3 radius（result card→12 + 2 banner→10）· thead T.bg + quote brandSoft R-372 字面未动 · App.jsx 0 diff · 4 无条件版本（含 Login footer）· Shell L43 stays v0.6.4.2。

**⭐ adversarial 完整性内化（headline）**：
- 执行者 Stage 1 主动枚举全部 radius 站点（11 站点全分类）→ 守护者 grep 确认**完整无漏**（无 FeedbackBar-type 漏）。
- **adversarial Stage 3 模式演进**：Composer noop→hex / thinking 抓 placement / results 抓 radius 漏（FeedbackBar+orchestrator）/ **favorites = 确认完整非抓漏** → 前 3 次抓漏，第 4 次执行者自检到位 = 双层防漏机制成熟正向信号。

**quote brandSoft 8% R-372 有意偏离 demo（记档）**：demo favorites quote 用 `bgInset`；产品守 `brandSoft 8%`（R-372 全站 inset 闭环铁律，与 ResultBlock Observation 字面 byte-equal）。为单屏采 demo bgInset 会裂解 R-372 闭环 → R-UI2-VRP「本地铁律 > artboard 细节」标准应用（同 login error 色、thead T.bg）。资深知悉。

**radius 最终映射（跨屏一致）**：与 results 同 — 数据卡 12 / alert banner 10 / pill·input·utility·inset stays。

**⭐ UI v2 用户侧屏全收尾**：login→shell→home→composer→thinking→results→favorites 7 屏，非 admin 用户全部 v2。下一站转 admin 屏群（~11 屏）。
