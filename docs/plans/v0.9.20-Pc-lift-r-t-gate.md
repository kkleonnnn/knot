# v0.9.20（P-c）—— lift R-T-GATE：放开第二家公司 · **Stage 1**

> 弧的最后一片，也是**唯一一次不可逆的口径变更**。
>
> ⚠️ **本文不是 v0.9.18 那份 lift 草案的复制** —— 那份的 §2 与 §5 **已被实测推翻两处**（见 §1、§2），
> 且它引的行号已漂。本文的每条事实都是 2026-08-10 重测的。
>
> **前置已清**：v0.9.19（P-b）消掉启动序 4 处 `resolve_single_tenant()`
> ⇒ 「第二家一激活，下次重启整个平台起不来」这个**唯一硬阻塞**已不存在。

## §0 grounded 测绘（2026-08-10 实测，全部可复跑）

### 0.1 那道门的全部面

| 事实 | 实测 |
|---|---|
| 生产码**调用点恰 1 处** | `api/tenant_resolution.py:77`（`resolve_for_request` 的**第一行**） |
| 生产码**散文提及** 6 处 | `auth.py:33` · `tenant_resolution.py:23` · `tenant_repo.py:296` · `tenant_provisioning.py:45` · `platform_admin.py:152/203/225` |
| 测里**真调用**它 | `test_tenant_isolation.py:220/223/238`（1/2/0 active 三态） |
| 测里**靠它立论** | `test_tenant_resolution.py:234` · `test_tenant_resolution_api.py:167` · `test_platform_admin.py:262` · `test_file_catalog_owner_gate.py:557/643` |
| **预演已存在** | `tests/api/test_two_tenant_e2e_isolation.py` —— **21 个测函数**（含参数化），靠 `monkeypatch` **仅测内**把门换成 no-op |

### 0.2 ⭐ 实测推翻 v0.9.18 草案 §5 的第 1 步

那份草案写着：「**先**删 `test_two_tenant_e2e_isolation.py` 的 monkeypatch，其余一律不动 → 跑该文件。
全绿 ⇒ 预演可信，继续；**有红 ⇒ 停**」。

**实测结果**（外部 pytest 插件在 `pytest_runtest_call` 把真门装回去 = 等价于删掉那行 monkeypatch，
**仓库零改动**）：

```
18 failed, 3 passed
```

**原因是结构性的、不是环境问题**：门是 `resolve_for_request` 的**第一行**，而该 fixture 造的正是
**2 个 active 租户** ⇒ 门必然触发 ⇒ **每个 HTTP 请求 fail-closed**。
⇒ 那一步**必然红**，然后按它自己的判据「有红 ⇒ 停」把自己停掉。

**仍绿的恰好 3 条，且恰好都是不走 HTTP 请求的**（交叉印证同一个成因）：

| 仍绿 | 为什么 |
|---|---|
| `test_two_tenants_really_use_distinct_db_files` | 直接查库文件 |
| `test_interim_tid_selects_the_tenant_for_2fa_path[1]/[2]` | 走 `interim_session`，不经请求级租户解析 |

⇒ ⭐ **修正**：**门那一行与 monkeypatch 必须一起删** —— 它们是同一件事实的两半，
拆开跑没有任何一步是有意义的。（§4 已按此重排。）

### 0.3 ⭐⭐ 新发现：lift 后「激活第二家」**没有任何代码路径**

AST 实测（属性调用 / 裸名调用 / import 三种形态全扫）：
**`tenant_repo.update_tenant` 生产调用方 = 0**，只有 `tests/test_platform_audit.py` 与
`tests/api/test_platform_admin.py` 在用。

而 `update_tenant` 正是 v0.9.8 建的**单一写口**（审计 INSERT 与动作**同连接、同事务、单次 commit**，
配 4 条哨兵）。

⇒ **lift 之后，「把某家公司改成 active」= 让第二家真的进来 = 整个平台最要紧的那个动作，
只能靠运维直接 `sqlite3 UPDATE`** ⇒ **它不被审计**，
而专门为它建的审计写口**零调用方**。⇒ §7 决策点 1。

### 0.4 ⭐ 新发现：一条**只写在 docstring、零守护**的规则，且它的理由 lift 后会失效

`platform_admin.TenantCreateRequest` **刻意没有 `status` 字段**，docstring 给的理由是：

> 若它可传，一个调用就能造出第二个 active 租户 ⇒ `assert_no_second_active_tenant_served()`
> 让**全站每个请求** fail-closed。

**lift 后这个后果消失** ⇒ 理由蒸发 ⇒ 下一个读代码的人会据此认为「现在可以加了」。
而实测：**全仓没有任何测断言该字段不存在**（`tests/repositories/test_tenant_provisioning.py:335`
断的是 `model_fields["slug"]`，不是 `status` 的缺席）。

⇒ **v3.1-B #5 命中**（「新增规则里哪条只写在 docstring 而无守护」）。§3-B 给守护。

## §1 核心判断：lift 不是「删一行 + 补回归」，是**把一条已存在的预演转正**

`test_two_tenant_e2e_isolation.py` **现在就在**双 active 租户下跑端到端隔离
（token 只读自己家 / 库文件分离 / tid 篡改 401 / 代号选租户 / 审计落对库 / 2FA interim 选租户…）。

⇒ **lift 最强的验收不是新写的测，而是那 21 个函数在门真的没有了之后仍然全绿。**
⇒ 本片的风险因此**不在「双租户能不能跑」**（已答），而在 §2 那个判据问题。

## §2 ⚠️ 本片最容易做错的地方：lift 后「门被误删」与「门按计划删了」在行为上完全一样

⇒ **换判据的原则**：新判据必须回答**新的那个问题** ——

> **两个 active 租户下，A 的 token 能不能读到 B 的数据？**

而**不是**「那行代码还在不在」。这是本仓那条自诊断的又一次应用：
**判据要锚在系统真的产出了什么。**

⚠️ **删掉旧测是错的** —— 它们的**断言**仍值钱，过期的只是**理由**
（v3.1-B #8：「处置通常是改名 + 重写理由，不是删」）。

## §3 逐条处置

### §3-A 三条「判据到期」的测 —— 改名 + 重写理由（不删断言）

| 测 | 现判据 | lift 后的继任判据 |
|---|---|---|
| `test_tenant_resolution.py::test_gate_runs_before_tid_resolution` | 「门在解析之前跑」 | ⭐ **`test_no_credentials_never_falls_back_to_any_tenant`** —— 2 active + **完全没凭证** ⇒ **不得设 ctx 到任何租户**。<br>这正是原测第二段已经在测的那个性质（原话：「连完全没凭证也必须被 gate 挡住」），只是此前**由门代劳**；门没了，它才是那个真正要守的东西 |
| `test_tenant_resolution_api.py::test_two_active_tenants_blocks_every_request` | 「2 active ⇒ 每个请求 fail-closed」 | ⭐ **`test_token_for_suspended_tenant_is_401`** —— 断言从「active 数 > 1 就拒」改为「**tid 指向的那家不可服务就拒**」。<br>⚠️ 不要把它改成「2 active ⇒ 请求成功」——那与 e2e 那 21 条**重复**，等于用一条弱测换掉一条强测 |
| `test_platform_admin.py:262` 那条 | 「2 active 时平台面也 raise」 | 平台面在双租户下**应当可用** ⇒ 改为断言**平台面不依赖租户 ctx**（本来就是平行认证路径） |

⚠️ **`test_tenant_isolation.py::test_iso6_*`（真调用那道门的 3 处）**：
门本身**保留为函数**还是**物理删除**？→ §7 决策点 2。

### §3-B 六处散文推理 —— 逐条重写（**其中一条要配哨兵**）

| # | 位置 | lift 后 | 处置 |
|---|---|---|---|
| 1 | `tenant_resolution.py:23` | 「单租户下二者等价」失去前提 | 只需改措辞 —— `_LEGACY_SINGLE_TENANT_PATHS` **已为空**（v0.9.17），**无活消费者** |
| 2 | `auth.py:33` | 「无代号登录可回退」的唯一理由消失 | 改措辞。**行为不变**：`resolve_single_tenant()` 在 2 active 时 raise ⇒ 无代号登录**全部 401**（kk 已裁为**可用性**问题、产品迁移动作，不是安全洞） |
| 3 | `tenant_repo.py:296` | 注释 | 改措辞 |
| 4 | `tenant_provisioning.py:45` | 「新租户恒 suspended」的理由从「否则全站挂」变成「激活是独立的、要留痕的动作」 | 改措辞（理由**变强**了，不是变弱） |
| 5 | `platform_admin.py:152/203` | 「它不是运维逃生舱」**变成假的** —— 门没了，平台端点在 2 active 下**确实可用** | 改措辞。⚠️ 端点存在的**唯一理由**（R-C3：零消费者的 dependency 无法 revert-to-bad）**不受影响** |
| 6 | ⭐ `platform_admin.py:225`（`TenantCreateRequest` 无 `status`） | 理由**蒸发**（见 §0.4） | **改措辞 + 配哨兵**：断言 `"status" not in TenantCreateRequest.model_fields`。<br>新理由：**激活是不可逆、影响面最大的动作，必须是独立且留痕的一步，不能是 create 的副作用**。<br>⚠️ 顺带把 `db_dir` 一起纳入同一条哨兵 —— 它的理由（路径逃逸）**不会过期**，但同样零守护 |

### §3-C ⭐ 激活 CLI（**kk 2026-08-10 裁定选 B**）

**为什么需要它**：§0.3 实测 —— lift 后「把某家改成 active」是整个平台影响面最大的动作，
而它**没有任何代码路径** ⇒ 只能直接 `sqlite3 UPDATE` ⇒ **不留痕**，
同时 v0.9.8 专门为它建的审计写口 `update_tenant` **零调用方**。

**设计**（照本仓既有纪律，不自创）：

| 项 | 内容 | 依据 |
|---|---|---|
| 载体 | `knot/scripts/set_tenant_status.py` | 与 `reset_admin_password` / `purge_audit_log` 同族 |
| 写路径 | **必须**经 `tenant_repo.update_tenant` | v0.9.8：审计与动作同连接、同事务、单次 commit ⇒ 不存在「做了但没记」 |
| 目标 | `--tenant <id>` **必填、无默认、缺参零写入 + 非 0 退出** | CLAUDE.md v3.1-B #1：**破坏性工具不得有默认目标**（v0.9.15 三个实例的教训） |
| 状态 | `--status active\|suspended` 必填 | 不给默认 —— 「默认激活」正是最危险的那个默认 |
| 预览 | `--dry-run` 可保留回退，但**必须打印它解析到了谁** | 同上条的但书 |
| 起源租户 | 停用起源租户仍由 `update_tenant` 内既有守卫拒绝 | v0.9.6 已立，本片**不动它** |

⚠️ **激活前置校验**（fail-closed，缺一即拒绝激活，**并说明缺哪一项**）：
- `allowed_http_hosts` **必须已配**（`IS NULL` ⇒ 拒）—— v0.9.7 三态：`NULL` 对非起源租户
  没有回退，漏配 ⇒ 该租户 HTTP 数据源**全部静默拒绝**，而那与 bug 不可区分（v0.9.7 M5 已登记）；
- `allowed_webhook_hosts` 同理（v0.9.18 P-a）；
- `db_dir` 目录**真的存在**且库已建（否则首登 500 —— v0.9.5 测绘实测过的形状）。

⚠️ **诚实边界**：运维仍可直接 `sqlite3 UPDATE` 绕过本工具 ⇒
只声称「**经本工具的**激活被审计」，**不声称**「激活一定被审计」。须写进 DEPLOY。

### §3-D 门函数**物理删除**（**kk 2026-08-10 裁定**）

`assert_no_second_active_tenant_served` 连同其**唯一调用点**一并删除。

⚠️ 按 v3.1-B #8「过期的是理由，不是断言」，删之前先把它仍值钱的那条**搬走**：

| 该函数原本守的 | lift 后 | 去处 |
|---|---|---|
| `>1 active` ⇒ raise | **这条才是要删的东西** | 删 |
| ⭐ `0 active` ⇒ **不得** raise（交上层语义） | **仍值钱** —— 0 active 是真实故障态（平台库空/被清），此时必须是 401 而不是 500 | 搬到 `resolve_by_tid` 的 0-active 语义测；`test_tenant_isolation.py::test_iso6_*` 三条按此改写而非整删 |
| `1 active` ⇒ 通过 | 平凡 | 随之消失 |

## §4 验收（顺序**是承重的** · 每条先答**五问②**）

| # | 判据 | 层 | 注入 | 前 → 后 | 五问② |
|---|---|---|---|---|---|
| 0 | ⭐ **先**把门那一行 + e2e 的 monkeypatch **一起删**，其余一律不动 → 跑该文件 | 端到端 | —— | 期望 **21 passed**；有红 ⇒ **停**，那比 lift 本身更要紧 | ✅ 修正自 v0.9.18 草案（实测 18 failed，见 §0.2） |
| 1 | 全量 → 逐条确认红的**都是**「判据到期」类，无行为回归 | 全量 | —— | 预期红：§3-A 三条 + `test_iso6_*` | ✅ 红的名单是**事先列出**的，事后对不上就是回归 |
| 2 | §3-A 三条继任判据各自成立 | 单元/API | 各自 revert | 见 §3-A 表 | ✅ 每条注入都能产生它要排除的那个事件 |
| 3 | ⭐ **A 的 token 读不到 B 的数据**（e2e 21 条转正为**唯一**隔离验收） | 端到端 | 让 `resolve_for_request` 恒返 tenant#1 | 前：各读各家 → 后：跨租户读到 | ✅ 注入真能产生跨租户读 |
| 4 | **`status` / `db_dir` 不在 `TenantCreateRequest`** | 元 | 加回任一字段 | 前：绿 → 后：红并说明**新**理由 | ✅ 此前**零守护**（§0.4） |
| 5 | 单租户（现网形态）**行为不变** | 全量 | 无（刻意） | ⚠️ **本条零判别力**，守的是「没顺手改坏别的」，与 #3 成对才完整 | ✅ 明示它不构成任何安全声称 |

## §5 v3.1-B 承重面枚举（命中：租户隔离 · 鉴权 · 审计 · 出网 · 限流）

1. **fail-open 面**：删门后若 tid 解析有**任何**回退 ⇒ 直接跨租户供数。
   ⇒ 本片**不得**新增任何 `resolve_single_tenant` 到请求路径（AST 哨兵已在 v0.9.19 落地，扫描面需扩到 `api/`）。
   ⭐ **「1 → N」推论自查**：本片把「active 租户数」从**恒 1** 变成 **N** ⇒
   必须扫**所有依赖「只有一家」的回退点**。§0.1 那 6 处散文 + §0.3 的激活路径就是这次扫描的产物。
2. **oracle 能力 + 注入真能产生它吗**：§4 逐条已答；#0 那条**已实测**（这正是 v0.9.18 草案错的地方）。
3. **「那一行」/ 入口**：门只有 1 个调用点（AST 核）。⚠️ **但要问「这个能力有几个入口」**——
   「第二家能不能被服务」的入口 = `tenants.status` 的写路径，实测**代码路径 0 个**（§0.3）⇒ 决策点 1。
4. （并入 #3）
5. **散文规则无守护？** ⇒ ⭐ **命中一条**（§0.4 的 `status` 字段）⇒ §3-B #6 配哨兵。
6. **声明 vs 生产者**：R-T-GATE 清单在 CLAUDE.md ⇒ 本片改为「已 lift」+ 保留历史。
7. **顶班**：删门后 `test_iso6_*` 会红 ⇒ 必须确认红的**理由**是「门没了」而非别的（→ §7 决策点 2）。
8. **既有测的绿是真的吗**：⭐ §4 #0 就是这一问的执行 —— 且 v0.9.19 刚吃过一次
   （两条既有测的绿建立在「A 的口令能进 B」之上）。
9. **契约冲突**：`_LEGACY_SINGLE_TENANT_PATHS` 已空 ⇒ 无路径绕过 tid 解析。
10. **策略题的影子**：`auth.py` 无代号回退保留 —— 不是策略摇摆（它 fail-closed，且 kk 已裁定）。
11. **诚实收窄**：本片**不声称** —— 多副本安全（R10' 在 lift **之后**）· SQL 数据源出网受控（**P-a'**）·
    禁重定向（**P-a''**）· 平台侧无身份/无吊销（v0.9.5 E1 既有边界）·
    运维直改平台库仍绕过写口（若决策点 1 选 A 则**加重**，须写进 DEPLOY）。

## §6 版本

**v0.9.20（PATCH）** —— 理由同 v0.9.8/v0.9.19：**MINOR 是 Agent 生命周期边界**
（换执行者/守护者），与「弧内保持同一执行者的连续上下文」冲突。

## §7 kk 裁定（2026-08-10 · 已落进 §3-C / §3-D）

| 决策点 | 裁定 | 落点 |
|---|---|---|
| 1 —— 激活第二家要不要留痕的路径 | ⭐ **B：加激活 CLI**（走 `update_tenant` ⇒ 同事务审计） | §3-C |
| 2 —— 门函数本身留不留 | ⭐ **物理删除**（但先把 `0 active` 那条断言搬走，不随理由一起丢） | §3-D |
| 评审 | ⭐ **走独立 lens**（不同于 v0.9.19 的「省 lens」） | Stage 2 |

## §8 Stage 2 输入边界（v3.1-A）

给评审者的**只有**：① 本文档 ② 仓库现状。
**不给**：执行者的探索过程、实施期叙述、自辩。

⇒ 本片尚无 diff（Stage 1 = 0 代码）⇒ 包 1 即本文档；
§9 实施记录将在实施后作为**包 2**、且**必须在评审者交出初步发现之后**才送
（v0.9.8 立的排序 —— 否则 §9 会锚定评审者的判断）。
