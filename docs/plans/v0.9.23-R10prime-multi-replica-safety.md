# R10' — 让多副本安全（Stage 1 · 0 代码）

> **来源**：`docs/plans/v0.9-lift-arc-remaining-plan.md` §2 D-E（kk 2026-07-31 拍板「**要真解，不要临时方案**」）。
> **位置**：lift 之后（kk 2026-08-01）—— 它是**容量特性**，不是 lift blocker。
> ## ⭐ kk 2026-08-11 三条裁定（**已锁**）
> **D1 = RWO** ⇒ 存储层实测安全，本片成立（⚠️ 附带同节点共置的运维前提，见 §6 D1）·
> **D2 = (a)** 6 个桶全走共享计数器 · **D3 = A+B+C+E**（`upload_engine` 只登记不改）。
>
> **性质**：承重面命中 **鉴权（限流 = 爆破防护）· 凭据（引擎缓存持 DB 密码）· 删除数据（启动期 purge）** ⇒ 走完整 v3.1-B 枚举。

---

## §0 grounded 测绘

### 0.1 ⚠️⚠️ 锁定文档那张表：**5 行里 4 行要改，另漏 3 项、多 1 项**

那张表（`v0.9-lift-arc-remaining-plan.md` §2 D-E「R10' 的真实作业面」
= `DEPLOY.md` §「多租户运维门」`replicas=1` 条下那张表，**同一份内容抄了两处**）
写于 2026-07-31。逐行实读后：

| 原表行 | 实读结论 |
|---|---|
| 「数据源引擎缓存 ⇒ 别的副本最多 **1 小时用旧凭据**」 | ⭐ **不准确，且方向反了两次** —— 见 §0.2 |
| 「限流桶：模块级 `_Bucket()` 每副本一份」 | ⭐ 是 **6 个桶**不是 1 个，且**性质分三类**（爆破防护 / 成本 / CPU）—— 见 §0.3 |
| 「catalog 槽：每 query 无条件 reload ⇒ 当场自愈」 | ✅ 实读属实（`http_planner.py` 每次 `pick_http_route` reload） |
| 「JWT 吊销版本缓存 `TTLCache(ttl=60)` ⇒ 最多晚 60 秒」 | ✅ 实读属实（`totp_service.py:30`），**有界、可接受** |
| 「数据源健康/统计缓存：cosmetic」 | ✅ 实读属实（`datasources.py:39,44`） |

**表里漏的 3 项**（按 v0.9.18 P-a 那条：**先列「所有需要被管住的行为」，再看谁没被管**）：

| 漏项 | `file:line` | 多副本后果 |
|---|---|---|
| ⭐ **第二个引擎缓存** `upload_engine._upload_engines` / `_uploads_path_owner` | `upload_engine.py:20,22` | 每副本一份、**无 TTL**、指向**共享 SQLite 文件**（`uploads.db`） |
| ⭐ **启动期 audit auto-purge 的 read-then-write 竞态** | `main.py:293-300` + `:326` | N 副本同时启动 ⇒ 同时读到「已过 7 天」⇒ **N 个并发 chunk DELETE 打同一租户库**，异常被 `except` 吞成 WARN |
| ⭐⭐ **SQLite 存储层本身** | `base.py:80-82` / `tenant_repo.py:47` | **表里完全没有这一行** —— 而它比表里任何一项都根本（§0.4） |

**表里多的 1 项 —— 已经安全，且它是其余修法的样板**：
BI 调度器 `claim`（`bi_report_schedule_repo.py:82-96`）是**条件 UPDATE + 返 rowcount**
（`WHERE id=? AND enabled=1 AND next_run_at<=?`，认领与推进**同一条 SQL、无窗口**）
⇒ **多副本重叠 tick 本来就不会双打**。⭐ **R10' 的所有共享计数器都应照它的形状写。**

### 0.2 ⭐ 引擎缓存：真实陈旧面比文档窄，但**有一处比文档说的严重**

三个缓存点的键（`engine_cache.py` 实读）：

| 站点 | 缓存键 | 键里**有**的连接参数 | 键里**没有**的 |
|---|---|---|---|
| `get_user_engine` 多源（:177-183） | `(tid, uid, "{host}:{port}:{user}")` | host · port · user | **password · database** |
| `get_user_engine` legacy（:266-268） | 同上 | 同上 | 同上 |
| ⭐ `get_engine_for_source`（:303-306） | `(tid, "source", source_id)` | **无** | ⭐ **全部** |

⇒ **两处订正**：
1. **改 host / port / user 是当场生效的**（键变了 ⇒ 必然 miss）—— 文档说的「1 小时用旧连接」对这三项**不成立**；
2. ⭐ **但 `get_engine_for_source` 的键里零连接参数** ⇒ 该站点**所有字段**（含密码）都陈旧最多 1 小时
   —— 文档把三个站点当成一个说，**恰好把最严重的那个说轻了**。

⇒ **真实陈旧面 = ①②站点的 `db_password` / `db_database` + ③站点的全部**。
- `db_password` 陈旧 ⇒ 轮换密码后别的副本**继续用旧密码建新连接** ⇒ 认证失败（用户可见）**或**旧密码仍有效（**安全**：撤销无效）；
- `db_database` 陈旧 ⇒ 别的副本查**旧的库清单** ⇒ **数据范围错**（比连接失败更隐蔽）。

⚠️ **另外两条把文档的处方否掉了**：
- ⭐ **`data_sources` 表根本没有 `updated_at` 列**（`schema.sql:91-108` 实读，只有 `created_at`）
  ⇒ 「比对 `updated_at`」**按字面实现不了**；加列 = 加一条「每个写路径都要记得维护」的约束
  = 本仓反复被烧的那种漂移源。
- ⭐ **「**而非**靠 TTL」是错的** —— 缓存条目里还存着 `schema`（**远端库的表结构**，
  可以在数据源行完全不变的情况下变化）⇒ **TTL 是在管 schema 陈旧，指纹管连接参数**，
  **两者都要，不是二选一**。

### 0.3 限流：6 个桶，三类性质（`_rate_limit.py:32-44` 实读）

| kind | 限额 | 键 | 性质 | ×N 后果 |
|---|---|---|---|---|
| `login` | 10 / 60s | IP（**跨租户全局**） | 爆破防护 | 有效限额 ×N |
| `change_pwd` | 5 / 60s | IP | 爆破防护 | ×N |
| ⭐ `totp_verify` | 5 / 60s | `{tid}:{kind}:{uid}` | **第二因子爆破防护** | ×N（**最承重** —— 6 位码空间 10⁶，限额直接决定可猜次数） |
| `totp_enroll` | 3 / 3600s | tid:uid | 防 secret 生成滥用 | ×N |
| `totp_enroll_complete` | 10 / 3600s | tid:uid | **封顶 bcrypt CPU** | ×N |
| `query` | 30 / 60s | tid:uid | **成本**（LLM burn） | ×N |

⚠️⚠️ **锁定文档说「共享计数器写平台库可接受」漏了一件事**：
**限流器要防的那种流量，恰恰是会把共享计数器打爆的流量。**
攻击者每秒 1000 次登录尝试 ⇒ 每次都读写平台库 ⇒ **限流器变成对 SQLite 的 DoS 放大器**。
⇒ 设计必须让「**已超限**」这个状态可以**本地判定**（见 §1-B）。

### 0.4 ⭐⭐ 存储层：本片最根本的一行，而它不在任何表里

- 连接形态：`sqlite3.connect(path, check_same_thread=False)` + `PRAGMA journal_mode=WAL`
  （`base.py:80-82`；平台库 `tenant_repo.py:47` 同形）。**无显式 `timeout=`** ⇒ 走 Python 默认 **5.0s** busy timeout。
  全仓**无 `database is locked` 重试逻辑**（`repositories/` grep = 0 处）。
- ✅ **实测（本机，4 进程 × 200 写同一 WAL 库，逐字照抄 `get_conn` 形态）**：
  `期望 800 / 实际 800 / 零丢失 / 零错误 / 最慢 0.16s`。
- ⛔⛔ **但这条结论我原先写成「同节点多进程共享 WAL SQLite 是安全的」—— 那是错的，已被我自己的第二次实测推翻**
  （详 `v0.9.23-stage2-consolidated.md` §A）：
  **全新库 + 3 副本同时启动 ⇒ 2/3 boot 崩**（`database is locked` @ `init_platform_db`），
  **本地文件系统、与 NFS 无关**；库已建好则 3/3 正常。
  ⇒ **上面那次测的是稳态写（短 INSERT），启动路径是另一种写模式**（建表 / `PRAGMA journal_mode` 要排他锁 / 长事务）。
  ⇒ **正确的、收窄后的声称**：**稳态请求路径**上，同节点多进程共享 WAL SQLite 实测安全；
  **首启建库路径不安全**（处置见 §6 H-2 —— kk 裁定「本片只改 DEPLOY 措辞，代码修法另起一片」）。
- ⚠️ **跨节点未验、且有已知上游限制**：SQLite 的 WAL 依赖 `-shm` 共享内存映射，
  **官方文档明示不支持跨主机的网络文件系统**（此条为**转述上游约束，非本机实测** —— 沙箱内无 NFS 可测）。
  本仓已有旁证：`DEPLOY.md:473`「跨节点共享卷（K8s RWM/NFS PVC）上的 `flock` **不保证跨节点互斥**」
  + `platform_audit_repo.py:28`「多副本 + 共享 PVC 首启并发写 `platform.db` ⇒ `database is locked` ⇒ **全副本 boot 崩循环**」。

⇒ ⭐ **这是 §6 D1 的由来**：若 PVC 是 **RWX/NFS**，那么**修进程内缓存是修错了地方** ——
存储层就不成立，多副本在任何写路径上都不安全。若是 **RWO（多副本被迫同节点）**，
则实测已证明存储层没问题，R10' 的作业面就正好是本文档 §1 那几项。

### 0.5 ✅ 实测：为什么共享计数器**必须**是单条原子 SQL

4 进程 × 150 次并发自增同一行（同一份 WAL 库）：

| 写法 | 期望 | 实际 | 丢失 |
|---|---|---|---|
| **先 `SELECT n` 再 `UPDATE SET n=?`**（= 限流器「读计数→判断→写回」的形状） | 600 | **155** | ⛔ **445（74%）** |
| **单条 `UPDATE SET n=n+1`**（= 调度器 `claim` 的形状） | 600 | 600 | ✅ **0** |

⇒ 「共享计数器」若写成读-判断-写回，**在 4 副本下会漏掉 74% 的计数** ——
限流器会以为没人来过。**这不是理论推演，是上面那张表的实测数字。**

<details><summary>两条实测的可复跑形态（Stage 1 = 0 代码，故不落文件；实施期它们变成 §3 #4/#6 的测）</summary>

```python
# ① 存储层：N 个独立进程并发写同一 WAL 库（逐字照抄 base.get_conn 形态：无显式 timeout）
#    每次都 sqlite3.connect(...) → PRAGMA journal_mode=WAL → INSERT → commit → close
#    4 进程 × 200 次 ⇒ 期望 800 / 实际 800 / 错误 0

# ② 丢更新：同一份 WAL 库，4 进程 × 150 次自增同一行，两种写法对照
#    read_then_write: SELECT n → UPDATE SET n=?      ⇒ 600 期望 / 155 实际 / 丢 74%
#    atomic:          UPDATE SET n=n+1               ⇒ 600 期望 / 600 实际 / 丢 0
```

⚠️ **必须是真子进程，不能用线程** —— 线程共享模块级状态，
「每副本一份」这个事实在线程里**表示不出来**（六问②）。
</details>

### 0.6 派生扫描面（**按行为，不按形态** —— v0.9.18 P-a 的教训）

AST 扫 `knot/` 模块级：容器字面量 **44** · 构造调用 **108** · 装饰器缓存 **1** · `global` 语句 **2**。
逐条判「是否在运行期被改写」后，**真·跨请求可变状态 = 8 处**：

`lark._token_cache`（v0.9.18 已按租户键化，跨副本仅 miss，无害）·
`datasources._DS_STATS_CACHE` / `_DS_STATUS_CACHE`（cosmetic）·
`tenant_context._drift_state`（**诊断计数器**，各副本各算 ⇒ 告警阈值意义变弱，非安全）·
`catalog_state._state`（每 query reload ⇒ 自愈）· `auth_service._dummy_hash`（惰性初始化，只写一次）·
⭐ `engine_cache._engine_cache` · ⭐ `upload_engine._upload_engines` · ⭐ `_rate_limit._bucket`。

#### 0.6 附录：三处特殊形态（**逐个实读，不是靠形态推定**）

| 形态 | 位置 | 多副本后果 |
|---|---|---|
| `@lru_cache(maxsize=1)` | `core/crypto/fernet.py:92` `get_crypto_adapter` | ✅ **无害** —— 各副本从**同一个 env** 建同一个 adapter；主密钥轮换本就需要重启 |
| `global` ×2 | `services/audit_service.py:59,98` `_audit_write_failures_total` | ⚠️ **见下** |
| `global`（同一个名字，两处） | 同上 | 同上 |

⭐⭐ **一个值得升格的发现（我原本要写成「无害」的）**：
全仓**仅有两个**「让安全失败可见」的计数器 ——
`audit_service._audit_write_failures_total`（**审计写入失败**，由 admin metrics 路由读）
与 `tenant_context._drift_state`（**租户漂移**，R-10）。
**两个都是进程内的** ⇒ 多副本下 admin 查到的是**随机某个副本的数**
⇒ **可能读到 0，而另一个副本已经累计了 50**。

⇒ 这不是「cosmetic」：这两个计数器的**存在理由**就是让静默失败变得可见，
而多副本恰好**把它们变回静默**。⚠️ 但它是**可观测性**而非隔离/鉴权 ⇒ **登记，本片不一定修**（§6 D3）。

⚠️ 本清单**必须派生**（本仓「清单会漂」已有 5 个数据点）⇒ §1-E 把它做成哨兵。

---

---

# ⭐ 以下 §1 起为 **Stage 2 后修订版**（三 lens + 执行者自测整合）

> 逐条来源见 [`v0.9.23-stage2-consolidated.md`](v0.9.23-stage2-consolidated.md)；本节只留**结论**。
> **kk 2026-08-11 追加两条裁定**：**H-1 = 进**（可信 IP 来源纳入本片）·
> **H-2 = 只改 DEPLOY 措辞，boot 竞态的代码修法另起一片**。
>
> ## ⭐⭐ kk 2026-08-11 第三次裁定：**B 单独成片** ⇒ 本片（v0.9.23）= **A + C + E**
>
> **为什么切**（我在 Stage 3 后向 kk 提示、kk 拍板）：B 的实际作业面在评审后**膨胀了**——
> B-0（可信 IP）是新加的前置，而它**要推翻一条既有红线 R-限-1 + 一条正断言它的测 + DEPLOY 两处**；
> 加上 MF4（默认值会造成全平台共用一个 login 桶）、MF5（`_MAX_KEYS` 今天已无效）、
> MF6（平台库其他消费者未被保护）⇒ B 自己就是一片的量，且它动的是**鉴权面**。
>
> | 归属 | 项 | 相关 must-fix |
> |---|---|---|
> | **v0.9.23（本片）** | **A** 引擎缓存指纹 · **C** purge 原子认领 · **E** 派生哨兵 | MF7 · MF8 · MF9 |
> | **▶ 另起一片（B 片）** | **B** 限流共享计数器 + B-0 可信 IP | MF1 · MF2 · MF3 · MF4 · MF5 · MF6 + §3 #5~#10 |
>
> ⚠️ **切分不改变任何已裁定的内容** —— B 的 §1-B 全文、§3 #5~#10、MF1~MF6 **原样留在本文档**
> 作为 B 片的 Stage 1 输入（**别重做那轮评审**）。
> ⚠️ **B 片必须带走的两条既有缺陷登记**（本片不修，但不能丢）：
> ① **`_MAX_KEYS` 在活跃喷射下无效**（✅实跑：15000 活跃 key ⇒ 上限 10000 形同不存在
> ⇒ 红线 R-限-4 为假）② **XFF 解析全仓两处且语义已不一致**（`_audit_helpers` 读 `x-real-ip`，`_rate_limit` 不读）。

## §1 修法（修订版）

### R10'-A 引擎缓存：连接指纹进缓存键 **尾部** + 旧条目 `dispose()`

1. **指纹放键的最后一段**（🔒 **红线**）—— `invalidate_user_engine_cache` 与 `get_user_databases`
   **都按位置**解析 `k[0]`/`k[1]`（✅自核）⇒ 插在中间会**静默废掉这两个消费者**。
2. **指纹 = 该「组」全部源行的排序摘要**，不是 `primary` 单行（A-P1-4 / B-O6 独立同结论）——
   `create_engine` 只用 `primary` 而 `databases` 由组内全部源合并 ⇒ 组内增删 secondary / flip `is_active`
   在今天**键不变**，只对 primary 取指纹**修完仍陈旧**。摘要须含 `id` 集合与 `is_active`。
3. **HMAC(进程随机 salt) 而非裸 sha256**（A-P1-3）——`get_datasource` 返**已解密明文口令**，
   `sha256(明文口令)` 是可离线爆破的无盐快哈希。缓存本就进程内 ⇒ 进程 salt 不损语义。
   字段拼接用 `json.dumps(sort_keys=True)` 或长度前缀（消除 `a|b` 边界歧义）。
4. ⭐ **写新指纹条目前，先 pop 同 `(tid, uid, group_key)` 前缀的旧条目并 `engine.dispose()`**（A-P0-3）——
   生产码今天 `.dispose()` **0 处**（✅自核）⇒ 不 dispose 则旧连接池带着**已撤销口令的活连接**长存，
   「凭据轮换」只修了新建连接那一半；顺带消除 A-P0-4（`get_user_databases` 首个命中即返回 ⇒ 返回最老那条）。
5. **`_TTL_SEC` 不动**（管远端 schema 陈旧）。⚠️ 但 **site 3 的条目不含 `schema`**（B-O7，✅自核）
   ⇒ 「TTL 管 schema」这个理由**只对 site 1/2 成立**，文档不得笼统写。
6. **指纹计算抽独立模块**（C-7）—— `engine_cache.py` **356 / ACK 361，余量 5**（✅自核），装不下。

### R10'-B 限流（**修订最多的一项**）

**B-0 前置（kk 裁定 H-1 = 进）：可信 IP 来源。**
`_client_ip` 取 XFF **首段**（`_rate_limit.py:86-90`，✅自核）⇒ key **由攻击者构造**
⇒ 任何**按 key 生效**的机制（上界 / 阻断 / 计数）统统失效，且每请求一行新记录。
⇒ 新增信任跳数配置：**从 XFF 右起第 N 跳取**。

⭐ **MF2 收敛为单一 helper**：XFF 解析全仓**两处**（✅自核）——`_rate_limit._client_ip`
与 `_audit_helpers._get_client_ip:24,28`，**后者还读 `x-real-ip` 而前者不读**
⇒ 两个子系统今天对「谁是客户端」就已不一致。只改前者 ⇒ 限流按 hop-N 计数、审计记 hop-0
⇒ **爆破事件的审计 IP 仍可伪造**。⇒ 两处共用同一 helper，信任跳数规则须覆盖 `x-real-ip`。

⭐⭐ **MF3 —— B-0 是在推翻一条既有红线，不只是改个函数**（✅自核，我此前完全没意识到）：
`_rate_limit.py:12` 的 **R-限-1「IP 提取必走 X-Forwarded-For first（反代场景）」**。
⇒ 按 v3.1-B #8「兑现/推翻一个承诺要**扫两侧**」，必须同时处置：
① 红线 R-限-1 原文改写 · ② `tests/api/test_rate_limit.py:67 test_login_xff_header_used_for_ip`
——它**正断言 B-0 要推翻的行为** ⇒ **改名 + 重写理由，不是删**（断言可能仍值钱，过期的只是理由）·
③ `DEPLOY.md:300`「反代不会污染真实客户端 IP」B-0 后**为假**，须同改（§5 的 DEPLOY 清单原先没含它）。

⚠️⚠️ **MF4 默认值会造成跨租户可用性事故**：现网是 nginx XFF + K8s ingress
⇒ 「未配置 ⇒ 只用 `request.client.host`」= **全平台共用一个 login 桶**
⇒ **10 次失败即全站 60 秒无人能登录**。⇒ 二选一（须写进文档）：
**启动期 fail-fast**（未配置信任跳数即拒绝启动）**或** DEPLOY 列为必配项 + §3 #9 加活性正对照。
⚠️ 这不是加固，是 **B 成立的前提** —— 没有它，最承重的 `login`/`change_pwd` 上 B 是装饰品。

**B-1 ⭐ 保留现有 `_Bucket` 作「本地上界」，只在它放行时才写共享计数**（C-6 —— 三个 lens 里最好的一条）：
- **单副本语义逐字不变**（本地桶先触顶）⇒ 单副本零行为变化、零性能代价；
- ⭐⭐ **MF1 订正（这是我那个错误形状的第 5 次）**：每 key 每窗口的库写次数
  **每副本 ≤ limit ⇒ 全局 ≤ N×limit** —— 我原写「天然 ≤ limit」**只在单副本成立**，
  而**本片的全部意义就是 N>1**。该句此前还被编进 §3 #6 的 oracle 与 §4-1 的 fail-open 论据；
- `_MAX_KEYS` 与 `test_memory_limit_prunes_old_buckets` **不用重写**；
- ⇒ **删掉原方案的「本地阻断缓存」这个新概念** —— 本地桶就是它，少一个会写错的东西。

**B-2 计数是单条原子 SQL**（§0.5 实测 read-then-write 丢 74%），照 `bi_report_schedule_repo.claim` 形状。

**B-3 窗口索引取库端时钟**（A-P1-2）：SQL 内 `strftime('%s','now')`，**不用各副本自己的 `time.time()`**
⇒ 「各副本看到的窗口」结构上不可能不同；本地桶到期判定改 `time.monotonic()`（wall clock 前跳 = fail-open）。

**B-4 ⭐ DB 侧必须有 `_MAX_KEYS` 的等价物**（C-1，**阻断项**）——
本地上界只在「同一 key 重复」时省写；**IP 喷射每请求换 key** ⇒ 仍每请求一行。
【lens C 实跑】4 进程 × 500 新 key = **4167 写/s、65 B/行** ⇒ 10k IP × 10 次 = 10 万行 / ~6.5 MB。
⇒ 必须：`window_start` 列 + **采样触发**的 `DELETE WHERE window_start < ?`（**不能每次 check 都 DELETE** = 第二次写）+ **硬行数上限**。
**不做这条 = 把内存 DoS 换成磁盘 DoS。**

⭐⭐ **MF5 —— 到顶时的行为必须写死：拒绝新 key，但绝不淘汰既有行。**
淘汰活跃行 ⇒ 喷射者可**挤掉别租户的 `totp_verify` 计数器 = 重置其限额**（跨租户 fail-open）。
⚠️⚠️ **且不得引 `_MAX_KEYS` 作先例 —— 它今天就是无效的**（✅**我实跑坐实**）：
prune 只删 `ts[-1] < now - window*2` ⇒ **活跃喷射下一个都不删**；
灌 15000 个活跃 key 后 `len(_bucket._d) == 15000`、而 `_MAX_KEYS == 10000`
⇒ 红线 **R-限-4「内存有上限、防内存膨胀攻击」在活跃喷射下为假**（既有缺陷，登记）。

**B-5 库不可用时降级回本地桶 + 计数 + WARN**（A-P1-1）——
`get_platform_conn` 无显式 `timeout=` ⇒ 默认 5.0s，且这些是 **sync Depends 跑 threadpool**
⇒ 纯 fail-closed 会让登录争用**耗尽线程池、全站 sync 端点一起停**。
⭐ **MF6 —— 还要保护平台库的其他消费者**（C-5 被我只吸收了一半）：
平台库**已在每个已鉴权请求的热路径上**（`tenant_resolution` → `get_tenant`），
而 `get_platform_conn` **无显式 timeout（默认 5s）+ 全仓 0 重试**（✅自核）
⇒ 只给限流器自己短 timeout **保护不了它们**。⇒ 二选一并写进文档：
**(i) 计数表放独立 SQLite 文件**（结构上不可能锁到平台库 —— 合本仓「让两者结构上不可能冲突」的优先级）
**或 (ii)** 给 `get_platform_conn` 显式 timeout + 重试。
⇒ 限流器用**专用连接、显式短 timeout（0.5~1s）**；失败降级回本地桶（最坏 ×N，
**严格优于全放行、也严格优于全拒绝**）。⚠️ **必须计数 + WARN**，并配「正常情况下降级计数 == 0」的测（§D-1）。

**B-6 落位**：新建 `knot/repositories/rate_limit_repo.py`（照 `platform_audit_repo` 先例）——
✅自核：`lint-imports` **9 kept**、`knot/api/` **已有 31 个文件** import `knot.repositories`
⇒ **原 §4-9 的分层顾虑是伪问题，划掉**。⛔ 别塞 `tenant_repo.py`（317/ACK 320，余量 3）。

**B-7 加表只碰 `platform_schema.sql` 一处**（C-4）—— `init_platform_db` 每次启动
`executescript` 且全是 `CREATE TABLE IF NOT EXISTS` ⇒ 对存量库同样生效，**不用走 `run_platform_migrations`**
（那是给**加列**用的）。合「一个性质只允许一个创建点」。

**B-8 key 存 HMAC 摘要 + 定保留期**（A-P2-2 / C-8）——
`login` 桶的 key 含**客户端 IP**，今天只在 RAM 活 60s，落库后成为**平台库里的持久 PII**，
而 `audit_service._PII_BLACKLIST` 管不到它。

### R10'-C 启动期 audit auto-purge：原子认领

1. **SQL 改 upsert**（A-P0-2 / B-O4，✅自核实跑）：原方案那条 `UPDATE … WHERE` 在**行不存在时 rowcount=0**
   ⇒ **全新部署永不 purge**；且 `value IS NULL` 是死条件（schema `DEFAULT ''`）。
   ⇒ `INSERT … ON CONFLICT(key) DO UPDATE SET value=excluded.value WHERE app_settings.value < excluded.value`。
2. **写死「逐租户、在 tenant ctx 内」**——`app_settings` 是**租户库**表（`platform_schema.sql` 命中 0，✅自核）。
3. ⭐⭐ **MF7 订正：认领用独立标记，不得提前 stamp `last_purge_at`** ——
   ✅自核：今天 `last_purge_at` 写在 **purge 成功之后**（`purge_audit_log.py:83-89`）
   ⇒ 若认领即 stamp，而 purge 抛错（被 `main.py` 的 per-tenant `except` 吞成 WARN）
   ⇒ **7 天内不再重试**，审计表无限增长而日志只有一条 WARN。
   ⇒ 用 `audit.purge_claimed_at` 之类**独立标记**认领，或失败时回滚该标记。
   （原 C-9「认领即推进」的动机仍成立 —— 但它要推进的是**认领标记**，不是完成标记。）
   ⚠️ 原文如下（保留以显示我改了什么）：今天读在 `main.py:294`、
   写回在 `purge_audit_log.py:85-89`，**认领与写回分离仍有窗口**；purge 结果只进日志/审计。
4. ⚠️ **逻辑不得写进 `main.py`**（C-7，✅自核 **375 / ACK 375，余量 0**）
   ⇒ 照 `secret_at_rest.warn_all_tenants_at_startup` 先例搬进被调模块，**main.py 净零**。

### R10'-D `upload_engine`：不动（**理由重写**）

⛔ 原理由「多副本并发写 uploads.db = §0.4 那条存储层问题的实例，由 D1 统一决定」**是错的**（C-2，✅自核）：
`create_sqlite_engine`（`doris.py:204-208`）**不设 WAL、不设 timeout** ⇒ uploads.db 是 **rollback-journal**
⇒ **§0.4 的 WAL 实验对它不适用**（六问⑤：两边非同法产出）。
**正确理由**：uploads.db 提交阶段**阻塞读**（lens C 实跑：40 万行上传期间另一进程读最坏延迟
**delete=134.7 ms / WAL=1.1 ms**），5s busy_timeout 是悬崖；**一行 `PRAGMA journal_mode=WAL` 可消除，本片不做**。
⚠️ 另须登记：**`_uploads_path_owner` 是 fail-closed 安全 tripwire、每副本一份**
（副本 A 触发、副本 B 不触发；今天由 `idx_tenants_db_dir` UNIQUE 兜住）—— 不能只说「不存在陈旧语义」。

### R10'-E 哨兵：进程内状态清单派生（**4 支扫描面**）

按行为扫：容器字面量 · **构造调用**（`TTLCache(...)` 这类）· `@lru_cache` · `global`。
⭐⭐ **MF8 —— 哨兵必须有比对对象**：断言「**派生集合 == 具名登记表**」
（我在修订时把「与具名登记表比对」这句丢了 ⇒ 只扫不比 = 扫出来也没人判对错）。
⚠️ 同时把 `v0.9-lift-arc-remaining-plan.md` §2 与 `DEPLOY.md` 那张**抄了两处**的表**改为单一来源**
（§0.1 已实证它 5 行错 4 行 —— 两份副本会同时错）。
⚠️ **诚实声明**：本哨兵扫的是**模块级可变状态**，**不覆盖 DB 层 read-then-write**
（守护者 §III-3 指出这个盲区 —— 那部分对；但它举的例子错了，见 §8 记录）。
⭐ **取材要做成「唯一抓住」**（B-O9）：既有同族扫描器 `test_catalog_carrier_registry.py:138-185`
**只扫 `catalog.py`、且看不见 `TTLCache(...)` 与 `@lru_cache`** ⇒ 注入要**分别覆盖 4 支**，
并记录旧哨兵 `passed`（证明盲区，而盲区才是事故原因）。

## §2 必须处置的既有测（**修订：从 4 条增至 10 条**）

| 测 / fixture | 为什么 |
|---|---|
| `tests/api/test_rate_limit.py:118 / :148 / :180` | ⭐ **不声明任何 DB fixture**（✅自核），而 session 兜底 fixture 只重定向 `SQLITE_DB_PATH`、**从不 `init_platform_db()`**（✅自核）⇒ 无计数表 |
| `tests/test_tenant_cache_isolation.py:185` | 同上 |
| ⭐ `tests/test_tenant_cache_isolation.py:111` | 断言**精确三元组** `(1,7,gk) in _engine_cache`（✅自核）⇒ 追加指纹**必红**；改它时**必须**同时确认指纹在尾部 |
| `_rate_limit._reset_for_tests`（`:209-212`）| conftest **两个** autouse 调用点，其中一个**早于 tenant ctx** ⇒ 清库只能走 ctx-free 的 `get_platform_conn`；且须先调**幂等** `init_platform_db()`（**别 try/except 吞**）。【C 实跑】成本 0.43+0.56 ms × ~3200 次 ≈ **3.2 s** |
| `tests/integration/test_default_source_cascade_api.py:39-56 / 59-75` | **手工塞键、从不调 `get_engine_for_source`** ⇒ A 落地后其理由变假而**仍绿** ⇒ 升级成真调用 |
| `test_memory_limit_prunes_old_buckets`（`:174-193`）| 断言 `len <= _MAX_KEYS + 100` **只能抓「prune 完全没跑」** ⇒ 顺手改成断「真降到 `_MAX_KEYS` 以下」 |
| conftest 其余 autouse reset（**6 处**）| 哪些会从「纯内存」变成「需要 DB」要一并列（C-10） |
| `tests/test_startup_with_multiple_tenants.py` 样板 | 复用时三处坑：`KNOT_MASTER_KEY` **每次新生成**（须提到调用方）· env 全量替换 · **`_PREAMBLE` 自证面必须等于作业面** |

## §3 验收（⭐ **整表重写** —— lens B 判定原 9 条里**只有 2 条**有判别力）

> **两条贯穿规则**：① **负对照内联进每格**（同一测内先证「该命中时真命中」，再证「该重建时真重建」）——
> 取消原来那条散文式的独立正对照 #9；② **oracle 锚在系统真的产出了什么**（对象身份 / SQLite trace / 自报 rowcount），
> 不锚「我以为它走了哪条分支」。

| # | 判据 | 层 | 注入（必须实跑） |
|---|---|---|---|
| 1 | ⭐ **参数化 3 站点 × 7 字段**（host/port/user/password/database/`is_active`/**组成员集合**）：改一格 ⇒ `e_b is not e_c`；同格 no-op ⇒ `e_a is b` | 服务（stub `create_engine=lambda: object()`） | 指纹从键里去掉 / 只对 primary 取指纹 ⇒ 后者**只有组成员那几格会红** |
| 2 | ⭐ TTL 仍管远端 schema：**site 1/2** 行不变 + 跨 TTL ⇒ 返回的 schema 串**真换成 v2** | 服务 | 删 TTL 判断。⚠️ **不可放在 site 3**（其条目无 schema） |
| 3 | ⭐ 旧条目被 `dispose()`：写新指纹后，旧 engine 的 `dispose` 被调用**且**旧键不在缓存里 | 服务 | 去掉 dispose/pop ⇒ 红 |
| 4 | ⭐ `get_user_databases` 返回**新**那条的 databases | 服务 | 同 #3 的注入 ⇒ 红（今天首个命中即返回） |
| 5 | ⭐⭐ **多进程**限流：**前提「共享库可用」下** `passes <= limit`（安全）**且** `passes >= 1`（活性）；**另加降级态一条**：库不可用 ⇒ `passes <= N×limit` **且**降级计数 > 0 | 端到端 `Popen × N` + barrier | 退回纯模块级 `_Bucket` ⇒ `passes` 接近 N×limit ⇒ 红。⚠️ **MF-冲突**：#5 原写成绝对上界，与 B-5 的降级（最坏 N×limit）**互斥** ⇒ 必须写前提 |
| 6 | ⭐⭐ **库写次数 `1 <= writes <= N×limit`**（MF1：上界是 **N×**limit，不是 limit），且请求数 ≫ N×limit；写次数取自 **SQLite `set_trace_callback`**，⚠️ **注入点必须在 `sqlite3.connect` 层** —— `get_platform_conn` **每次新建连接**（✅自核）⇒ 挂在别处探针到不了（六问①） | 端到端 | ①「一律拒绝」⇒ writes=0 ⇒ 红 ② 去掉本地上界 ⇒ writes=请求数 ⇒ 红 |
| 7 | ⭐ **共享路径真的被走到**：本地桶未触顶 ⇒ 计数表里**必须有该 key 的行** | 端到端 | 让共享写恒失败（走降级）⇒ 红 —— **这条防「整片 inert 而全绿」**（§D-1） |
| 8 | ⭐ **降级计数在正常情况下 == 0** | 服务 | 把库路径弄坏 ⇒ 该计数 > 0 ⇒ 红 |
| 9 | ⭐ 可信 IP：XFF 伪造首段**不产生新 key** · **+ MF4 活性正对照**：同一 `client.host` 下用户 A 耗尽限额后**用户 B 仍能登录**（防「全平台共用一个桶」） | 服务 | 退回取首段 ⇒ 每请求新 key ⇒ 红；把 hop-N 改成「恒用 client.host」⇒ 活性那条红 |
| 10 | ⭐ DB 侧行数上界：灌 ≫ 上限个不同 key ⇒ 表行数**有界**，**且受害者 key 的既有计数未被清除**（MF5：淘汰活跃行 = 跨租户 fail-open） | 端到端 | ① 去掉采样清理 ⇒ 无界 ⇒ 红 ② 到顶改成淘汰最老活跃行 ⇒ 受害者计数被重置 ⇒ 红 |
| 11 | ⭐⭐ purge 认领**双态** × N 进程：`missing-row` 与 `stale-row` **各一条**，都断「认领成功的进程**恰 1 个**」（各进程自报 rowcount） | 端到端 `Popen × N` + barrier + `with TestClient(app):` 驱动 lifespan | ① 退回 read-then-write ⇒ >1 个成功 ⇒ 红 ② 用原方案那条 `UPDATE…WHERE` ⇒ **missing 态 0 个成功** ⇒ 红 ③ **MF7 失败路径**：认领成功但 purge 抛错 ⇒ **下次仍可认领**（把标记改成提前 stamp `last_purge_at` ⇒ 红）。⚠️ **不断 DELETE 行数**（审计表空时不可观测） |
| 12 | ⭐ 哨兵 4 支扫描面各注入一次；**断言「派生集合 == 具名登记表」**（MF8）；**并记录旧哨兵 `passed`** | 元（AST） | `_x = {}` · `_c = TTLCache(...)` · `@lru_cache` · `global` · **从登记表删一项而代码仍在** ⇒ 红 |
| 13 | **单副本行为不变**：`_MAX_KEYS` prune 那条测 + 既有 6 桶语义测**全绿且未改判据** | 服务 | 无（刻意） |

⚠️ **#5/#6/#7/#11 必须真子进程 + 共同 barrier**：`subprocess.run` 是**顺序阻塞**（✅自核）
⇒ 造不出竞态（六问②）；且 purge 挂在 `@app.on_event("startup")` + `create_task`
⇒ **只 `import knot.main` 触发不到它**（✅自核）⇒ 必须 `with TestClient(app):` 驱动 lifespan 并等任务落地。

## §4 v3.1-B 枚举（修订版 · 承重面：鉴权 · 凭据 · 删除数据）

1. **fail-open 面**：⭐ 最大的两处 —— ① **key 由攻击者构造**（B-0 解决）② **本地桶只能更严不能更松**
   （B-1 的形状天然保证：它是第一道门，放行才写库）。库写失败 ⇒ 降级本地桶（**不是放行**）+ 计数 + WARN。
   ⭐ **1 → N 自查**：本片让「限流状态」从进程内唯一变成跨副本共享 ⇒ 重扫依赖「桶是进程内唯一」的点：
   `_reset_for_tests`（2 个 autouse 调用点）· `_MAX_KEYS` 守护 · `test_memory_limit_prunes_old_buckets`。
2. **oracle 能力 + 注入**：见 §3 —— 原表 9 条只有 2 条有判别力，已整表重写；
   #6 的两个方向（0 写 / 写=请求数）各对应一个真实失效。
3. **「那一行」/ 入口**：计数在**一条 SQL** 内；限流有 **6 个入口** ⇒ 门在 `_bucket.check` 这个**共同收敛点**。
   ⚠️ 新增入口面：`_client_ip` 是**另一个**收敛点（4 个 IP 键桶共用）。
5. **散文规则无守护？** —— 「同节点共置」「先单副本迁移」都是 DEPLOY 散文、**零强制**；
   本片**不新增**这类散文（§1-E 用哨兵），但 H-2 的 DEPLOY 措辞修正**必须做**。
6. **声明 vs 生产者**：新表有读者（限流）+ **清理者**（B-4，定量）+ retention（B-8）。
7. **顶班**：#13 单副本正对照 + #7 共享路径正对照 —— 缺任一条都可能「功能整个坏掉而绿」。
8. **既有测的绿是真的吗** → §2 十条逐条。
9. ~~分层~~ **已证伪，划掉**（C-3 实跑 `lint-imports` 9 kept + 31 个 api 文件已 import repositories）。
10. **策略题的影子**：原「fail-closed vs fail-open」二选一 —— ⭐ **C-6 + A-P1-1 合起来消掉了它**：
    本地桶既是上界又是降级目标 ⇒ 不需要在「全拒」与「全放」之间选。**这正是「那个失败模式本不该存在」。**
11. **诚实收窄**：本片**不声称** —— 首启并发建库安全（§0.4 已收窄，代码修法另起一片）·
    uploads.db 并发提交不阻塞读（C-2 实测 135 ms）· 两个安全可观测计数器跨副本准确 ·
    跨节点存储安全 · 现网真做了同节点共置（需运维确认）· 预算 TOCTOU（单副本今天就有）。

## §5 版本 / 协议

v0.9.23（PATCH）· 完整 v3.1。

**MF9 —— DoD 清单补全**（原文漏了一半）：
- **4 源点**：`main.py` version · **`tests/test_rename_smoke.py` R-72 字面 + docstring**（原漏）·
  `README.md` 顶部 · `frontend/src/version.js`；**+ CHANGELOG 顶部条目 + DEPLOY 顶部**；
- **⭐ 重建 `knot/static`**（R14）**+ 前端三件恒做：eslint + vitest + 重建**（原只写了重建）；
- **DEPLOY 改动清单**：H-2 措辞修正 · 那张表的 4 处订正 + 3 项漏登记 ·
  **MF3 的 `DEPLOY.md:300`** · **MF4 的信任跳数必配项** · MF8 的「表改单一来源」；
- **§8 Stage 3 终审整合 + §9 实施记录**（偏离理由 / 实施期自己写错的东西）—— 登记位已建（见 §8）。

## §6 决策（**全部已拍板**）

| # | 裁定 |
|---|---|
| D1 | **RWO** ⇒ 稳态路径实测安全；⚠️ 附带「多副本须同节点共置」的运维前提 |
| D2 | **(a)** 6 桶全走共享计数器 ⇒ ⚠️ 要量化的是 **login 桶在喷射下的写入量**（C-5：原来锚在有鉴权的 query 桶上，锚错了） |
| D3 | **A+B+C+E**（D 只登记不改） |
| **H-1** | ⭐ **进** —— 可信 IP 来源纳入本片（B-0） |
| **H-2** | ⭐ **只改 DEPLOY 措辞**；boot 竞态的**代码修法另起一片**（登记见下） |

**▶ 新登记的独立片：首启并发建库竞态**
把 `main.py:76-77`（`init_platform_db` + `seed_default_tenant`）纳入 `.c4-migration.lock`
⇒ 今天那把锁在 `main.py:82` 才获取（✅自核），**没盖住这两步**。
⚠️ 受 C-7 约束（`main.py` 余量 0）⇒ 须同时把逻辑搬进被调模块。
⚠️ 未验：K8s 是否靠 pod 重启自愈 —— **实测它，或别在文档里声称**。

## §7 ⭐ 落地对照表（**任一行「落点」为空即视为未吸收**）

| 来源 | 落点 |
|---|---|
| A-P0-1 攻击者构造 key | §1 B-0 + §3 #9 + §4-1 |
| A-P0-2 / B-O4 CAS SQL 返 0 | §1 C-1 + §3 #11（双态） |
| A-P0-3 `dispose()` 0 处 | §1 A-4 + §3 #3 |
| A-P0-4 首个命中即返回 | §1 A-4 + §3 #4 |
| A-P1-1 fail-closed 全站自伤 | §1 B-5 + §3 #8 + §4-10 |
| A-P1-2 wall clock 窗口 | §1 B-3 |
| A-P1-3 明文口令摘要 | §1 A-3 |
| A-P1-4 / B-O6 同组多源 | §1 A-2 + §3 #1（组成员集合那一格） |
| A-P2-1 / C-3 分层是伪问题 | §1 B-6 + §4-9（划掉） |
| A-P2-2 / C-8 新 PII 落点 | §1 B-8 |
| B-O1 #9 是散文 | §3 表头「两条贯穿规则」① + 删原 #9、改 #13 |
| B-O2 写次数对 0 恒真 | §3 #6（双向有界） |
| B-O3 探针到不了 | §3 表末警示 + #11 的载体要求 |
| B-O5 4 条测无 DB fixture | §2 前两行 |
| B-O7 #3 站点错配 | §1 A-5 + §3 #2「不可放在 site 3」 |
| B-O8 `== limit` 混安全与活性 | §3 #5 拆两条 |
| B-O9 只覆盖 1 支扫描面 | §1 E + §3 #12（含「记录旧哨兵 passed」） |
| B-O10 子进程样板三坑 | §2 末行 |
| B-O11 指纹只能加尾部 | §1 A-1（🔒红线）+ §2 第三行 |
| C-1 DB 侧无 `_MAX_KEYS` 等价物 | §1 B-4 + §3 #10 |
| C-2 uploads.db 不是 WAL | §1 D（理由重写）+ §4-11 |
| C-4 加表只碰 platform_schema | §1 B-7 |
| C-5 D2 锚错了桶 | §6 D2 备注 |
| C-6 保留 `_Bucket` 作本地上界 | §1 B-1（**删掉原「本地阻断缓存」概念**）+ §4-10 |
| C-7 size 闸门卡死 | §1 A-6 + §1 C-4 + §6 新登记片 |
| C-9 认领与写回分离 | §1 C-3 |
| C-10 conftest 6 处 reset | §2 第 7 行 |
| §A-1 boot 竞态（执行者自测） | §6 H-2 + 新登记片 + DEPLOY 措辞 |
| §A-2 / G-3 我的 4 处错 | §0.4 订正 + §1 D + §6 D2 + §1 C-1 |
| MF1 第 5 次外推（≤limit 只在单副本成立） | §1 B-1 + §3 #6 + §4-1 |
| MF2 XFF 解析两处 / audit 读 x-real-ip | §1 B-0（单一 helper） |
| MF3 B-0 推翻既有红线 R-限-1 + 一条测正断言它 | §1 B-0（扫两侧）+ §5 DEPLOY 清单 |
| MF4 默认值 ⇒ 全平台共用一个 login 桶 | §1 B-0 + §3 #9 活性正对照 |
| MF5 到顶不得淘汰活跃行 + `_MAX_KEYS` 今天已无效 | §1 B-4 + §3 #10 |
| MF6 平台库其他消费者没被保护 | §1 B-5（独立文件 or timeout+重试） |
| MF7 认领即 stamp ⇒ purge 失败后 7 天不重试 | §1 C-3 + §3 #11 |
| MF8 哨兵无比对对象 + 那张表抄了两处 | §1 E + §3 #12 |
| MF9 DoD 清单缺 eslint/vitest/R-72 字面/§8-§9 | §5（整段重写） |
| 守护者 §III-3（budget 定性）**不采纳** | §8.2 记录理由（写侧是原子累加，实读+全仓扫描） |
| 守护者 §III-3 下半句（扫描面看不见 DB 层） | §1 E 诚实声明 + §8.2 |
| 守护者 §III-4（#5 与 B-5 互斥） | §3 #5 补前提 + 降级态判据 |

⚠️ **v0.9.22 的教训**：这张表**只覆盖评审 findings，不覆盖「我自己改了方案」**
（v0.9.20/.21/.22 连续三片栽在后者）⇒ **范围一改先 grep 全文旧方案名词**。
本次已执行一次：**「本地阻断缓存」这个词在 C-6 后已从 §1 删除**（原 §1-B 的核心概念）。

---

## §8 Stage 3 终审整合（守护者 · **裁定 `revise-before-implement` · 9 条 must-fix**）

> 守护者在**隔离 worktree**、只拿包 1（修订版 Stage 1 + Stage 2 整合 + 闸门现状；本片 0 代码无 diff）。
> **每条我都自核后才落**；它错的那条我**不采纳并记在此**（本仓有「守护者也会错」的实证）。

### 8.1 九条 must-fix 的落点

| MF | 内容 | 我的自核 | 落点 |
|---|---|---|---|
| **MF1** | ⭐⭐ **我那个错误形状的第 5 次**：「每 key 每窗口库写**天然 ≤ limit**」**只在单副本成立**；N 副本各有本地桶 ⇒ 真值 **≤ N×limit**。而**本片的全部意义就是 N>1**。它还被编进 §3 #6 的 oracle 与 §4-1 的 fail-open 论据 | ✅ 构造上显然成立 | §1 B-1 + §3 #6 + §4-1 |
| **MF2** | XFF 解析全仓**两处**，`_audit_helpers` 还读 `x-real-ip` 而 `_rate_limit` 不读 ⇒ 只改一处 ⇒ 限流按 hop-N、审计记 hop-0，**爆破事件的审计 IP 仍可伪造** | ✅**自核**：`_audit_helpers.py:24,28` vs `_rate_limit.py:86` | §1 B-0（收敛为单一 helper） |
| **MF3** | ⭐⭐ **B-0 在推翻一条既有红线** R-限-1「IP 提取必走 XFF first」，且有一条测**正断言**它；`DEPLOY.md:300` 也会变假 | ✅**自核**：红线原文在 `_rate_limit.py:12`；`test_login_xff_header_used_for_ip` 在 `:67` | §1 B-0（扫两侧：改名+重写理由，不删）+ §5 DEPLOY 清单 |
| **MF4** | ⚠️ B-0 的默认值（只用 `client.host`）在 nginx+ingress 现网 = **全平台共用一个 login 桶** ⇒ 10 次失败全站 60 秒无法登录 | 转述（机理成立；未实测现网拓扑） | §1 B-0（fail-fast 或 DEPLOY 必配）+ §3 #9 活性正对照 |
| **MF5** | ⭐⭐ 到顶行为须写死「**拒绝新 key，绝不淘汰既有行**」（淘汰活跃行 ⇒ 喷射可挤掉别租户 `totp_verify` 计数 = 重置其限额）；**且 `_MAX_KEYS` 今天就是无效的** | ✅**我实跑坐实**：灌 15000 活跃 key ⇒ `len==15000` 而 `_MAX_KEYS==10000` ⇒ **R-限-4 在活跃喷射下为假**（既有缺陷） | §1 B-4 + §3 #10 |
| **MF6** | C-5 我只吸收了一半：平台库**已在每个已鉴权请求热路径上**，只给限流器短 timeout **保护不了其他消费者** ⇒ 计数表放**独立 SQLite 文件**，或给 `get_platform_conn` 加 timeout+重试 | ✅**自核**：`get_platform_conn` 无显式 timeout、全仓 0 重试 | §1 B-5 |
| **MF7** | ⭐⭐ 我采纳 C-9 时**引入了回归**：今天 `last_purge_at` 写在 **purge 成功之后**；认领即 stamp ⇒ purge 抛错（被 per-tenant `except` 吞成 WARN）后**7 天不再重试** | ✅**自核**：`purge_audit_log.py:83-89` 确在末尾 | §1 C-3（改独立认领标记）+ §3 #11 失败路径 |
| **MF8** | 哨兵**没有比对对象**（我修订时把「与具名登记表比对」丢了 ⇒ 只扫不比）；且那张**抄了两处**的表要改单一来源 | ✅**自核**：修订版 §1-E 原文确无「登记表」字样 | §1 E + §3 #12 |
| **MF9** | §5 的 DoD 清单缺 **eslint + vitest**、缺 `test_rename_smoke.py` R-72 字面、缺 §8/§9 登记位 | ✅**自核**（对照 CLAUDE.md v3.1-C） | §5（整段重写） |

### 8.2 ⚠️ 守护者错的一条（**不采纳，记录以免下一个人照抄**）

**§III-3 说 `budget_service` 是「读-判断-然后写成本行，即 §0.5 实测丢 74% 的那个形状」——
写侧的定性是错的。** ✅ 我实读 + 全仓扫描：
`user_repo.py:95-100` 是 **`monthly_tokens = monthly_tokens + ?, monthly_cost_usd = monthly_cost_usd + ?, query_count = query_count + 1`**
= **单条原子累加**，正是安全的那一类。全仓扫描：原子累加形态 **3 处**；
其余 8 处 `SET x=? WHERE` 是 `sort_order` / `title` / `content` 之类的 **last-writer-wins**，不是丢计数。

⇒ **真实性质**：预算是 **check-then-act TOCTOU**（并发请求都通过预算门），
**总额不会算错**（写是原子的），且该竞态**单副本今天就有** ⇒ §4-11 原先的表述是**对的**，保留。

⭐ **但守护者那条的下半句是对的、且价值高**：**我的 §0.6 扫描面（模块级可变状态）
结构上看不见 DB 层的 read-then-write** ⇒ 已在 §1-E 加**诚实声明**，并按行为扫了一遍全仓（结果如上）。
⇒ **这是「扫描面 ≠ 作业面」的又一实例**，而它这次指的方向对、举的例子错。

### 8.3 守护者复核通过的（也要记，否则只剩批评）

C-6 在 §1/§3/§4 **全文一致**（grep「阻断缓存」在 §1/§3/§4 零命中）· A-P0-3 的 `dispose` 0 处
（它自陈**先误算成 5，因为那是 `knot/static` 的 JS bundle** —— 与我第一次犯的「`grep|sed` 吞退出码」同族）·
B-O11 指纹加尾部**且更硬**（`:331` 是 `len(k) >= 2` ⇒ 尾部追加天然兼容）· B-O7 site3 无 schema ·
C-7 size 三个数字 · OOS-1v2 合规（`{tid}` 落**平台库**正是红线允许的方向，`platform_audit.tenant_id` 是先例）·
R-T-GATE 代偿门不受影响（`UPDATE tenants` 恰一处的哨兵不撞 `UPDATE rate_limit_*`）·
§6 新登记片的 root cause 独立复核成立。

### 8.4 它未复核、我也不声称的

`lint-imports` 9 kept（它的 shell 无该命令；但它独立 AST 数得 **31** 个 api 文件 import repositories，与我的数一致）·
lens C 的三组性能数字 · 跨节点 NFS · K8s 重启自愈。

## §9 实施记录（v3.1-C #4/#5）

### 9.1 落地清单（A + C + E；**B 已拆片**）

| 落点 | 内容 |
|---|---|
| `knot/services/source_fingerprint.py`（新） | 组级指纹：HMAC(进程随机 salt) + `json.dumps(sort_keys=True)` + `_norm`（`9030` 与 `"9030"` 同指纹） |
| `knot/services/engine_cache.py` | 3 站点键**尾部**加指纹 + `_put()` 淘汰同前缀旧条目并 `engine.dispose()`；ACK 361→395（带理由） |
| `knot/repositories/settings_repo.py` | `claim_if_newer()` 单条 upsert 原子认领 |
| `knot/services/audit_service.py` | `claim_auto_purge(days)` —— **独立认领标记** `audit.purge_claimed_at` |
| `knot/main.py` | 认领替换 read-then-write ⇒ **净减 12 行**（375→363，余量本来是 0）；顺带订正一条已 stale 的注释 |
| `tests/test_process_state_registry.py`（新） | 四支扫描面 + 具名登记表**双向相等** + 防占位符 + 防白名单逃逸 |
| `tests/services/test_engine_cache_fingerprint.py`（新） | 9 条：3 站点 × 字段矩阵、组内 secondary、TTL 仍管 schema、淘汰+dispose |
| `tests/test_auto_purge_atomic_claim.py`（新） | 4 条：`Popen×N` + barrier、双态、负对照、MF7 失败路径 |
| `tests/test_tenant_cache_isolation.py` | 那条精确三元组断言**改判据形状**并升级成「指纹只能加尾部」的守护 |
| `DEPLOY.md` / `v0.9-lift-arc-remaining-plan.md` | 那张**抄了两处**的表废除 ⇒ 指向单一来源（派生登记表）+ 写明本版处置了什么、什么没处置 |
| 版本 / 文档 | 4 源点 + CHANGELOG + DEPLOY 顶部 + README banner 滚动 + 重建 `knot/static` |

### 9.2 偏离本文档的地方

1. **`is_active` 从字段矩阵移出** —— 实测它在 `engine_cache` 里**分组前就被过滤**，
   到不了指纹；它的效果由「组成员集合变了」表达（见 9.3 #2）。模块 docstring 同步订正。
2. **`_MAX_KEYS` 那条既有缺陷只登记、不修** —— 它属于 B 片（限流）。
3. §1-E 原写「与具名登记表比对」= MF8 要求的形状，实施时**额外加了两条测**
   （防占位符 / 防白名单逃逸）—— 因为「有比对对象」本身可以被「登记表全填 `cache`」或
   「把自己的类塞进纯工厂白名单」绕过。

### 9.3 ⭐ 实施期我自己写错的东西（**5 处**）

1. **字段矩阵里放了个 no-op**：`("is_active", 1)` 而 base 本来就是 `1`
   ⇒ **改动压根没发生**却断言「必重建」⇒ 测自己红（六问②）。
2. ⭐⭐ **我断言「删掉一个源 ⇒ 必重建」，实测代码是对的、判据是错的**：
   `engine_cache` 在分组**之前**按 `is_active` 过滤 ⇒ 删一个**本来就 inactive** 的行不该重建。
   ⇒ 改成一条**负对照**并写明理由（免得下一个人也这么想）。
3. ⭐ **我自己的哨兵判据有盲区，是它自己抓出来的**：按「模块里真有语句在改它」派生
   **抓不到 `_bucket = _Bucket()`**（改写在**对象方法内**，模块级名字从未被赋值）；
   也漏 `_drift_state["count"] += 1`（`AugAssign` 不是 `Assign`）。⇒ 补 ② fail-closed 构造分支 + `AugAssign`。
4. ⭐ **bundle 比对比错了文件**：取 `*.js` 的字母序第一个 ⇒ 拿 `echarts` chunk 跟 `index` chunk 比，
   得出「差 -3665 字节」。比对同一个 chunk 后是**差 0**。（六问⑤：两边非同法产出。）
5. ⭐ **把 ruff 输出的末行读成了结论** —— 末行是 "No fixes available"，而 "Found 6 errors" 在上面
   ⇒ 我以为闸门绿了。**同一个老毛病**（`tail` 吞掉真信号，记忆里已有条目）。
   ⇒ 此后一律 `grep -E "^(All checks|Found)"` 取结论行。
6. **`git checkout` 还原不了未入库的新文件** ⇒ 一次注入残留没清掉，还原后测仍红。手动清除并复验。

### 9.4 revert-to-bad 汇总（**13 组全部真跑**）

| 注入 | 结果 |
|---|---|
| A：指纹从 3 个键里全去掉（= 今天的行为） | **5 failed / 4 passed** —— 且恰好是 password/database/secondary/site3 ⇒ 坐实「只有那几格有判别力」（host/port/user 本就在 group_key 里） |
| A：指纹只喂 primary 那一行 | **恰 1 failed**（组内 secondary 那条 = 唯一抓住者） |
| A：去掉 `_put` 的淘汰 + dispose | **恰 1 failed**（另 12 条 tenant-cache-isolation 仍绿 ⇒ 那些测不覆盖 dispose） |
| A：删 TTL 判断 | **恰 1 failed**（schema 那条） |
| A：指纹插到键**中间** | **1 failed / 11 passed**（既有那条改写后的测抓住 ⇒ 红线有守护） |
| C：换回**原方案**那条纯 `UPDATE … WHERE` | **3 failed**（含 missing-row ⇒ 坐实「全新部署永不 purge」真的存在过） |
| C：认领换回 read-then-write | 3 failed |
| C：认领时提前 stamp `last_purge_at` | **恰 1 failed**（MF7 那条） |
| E：①容器 / ②`TTLCache` 构造 / ③`@lru_cache` / ④`global` 各注入一次 | 各 **1 failed**（②刻意**不改写**那个名字 ⇒ 坐实两支分工） |
| E：登记表留过期条目 | 1 failed |
| E：把 `_Bucket` 塞进纯工厂白名单 | **2 failed** |

### 9.5 闸门

- ruff **All checks passed**（⚠️ 看结论行，见 9.3 #5）· import-linter **9 kept / 0 broken** · size **R-94 OK**
- 前端三件：eslint · vitest · **重建 `knot/static`** ⇒ ⭐ **index chunk 归一后逐字节相同（557054 字节，差 0）**
- README/DEPLOY 版本改动机械核：README 归一后与 HEAD **逐字节相同**；
  DEPLOY `+17/−8` 且 diff 里只有版本 banner + 那张表（逐行看过）
- 前端：eslint 0 warning · vitest **83 passed**
- ⭐ 全量 **1965 passed / 115 skipped / 1 xfailed / 0 failed**（410s）——
  基线 1949 ⇒ **+16 = 9（指纹）+ 4（认领）+ 3（哨兵）**，与新增测数逐一对得上。
