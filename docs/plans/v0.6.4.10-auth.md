# v0.6.4.10 — UI v2 auth 屏收尾（Enroll 色/token 对齐 + ForceChangePassword noop）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + 修正版本同步模型（4 无条件 ★CI 含 Login footer）
> **基座**：worktree `v064a-auth` @ branch `v0.6.4.10-auth`（off main `de4a354` = v0.6.4.9）
> **源稿**：demo auth.jsx（ForcePasswordPage + EnrollPage）
> **目标**：Enroll.jsx（224，LIMIT 240）+ ForceChangePassword.jsx（151）

## §0 范围 + adversarial 评估（结论：Enroll 色/token 对齐 + ForceChangePassword noop）

auth 屏与 Login 同族（含 KnotLogo）。**ForceChangePassword 已 v2**（card radius 12 + boxShadow R-313 + T.warn token + T.sendFg 按钮 + brandSoft inset R-372 + hex 0）→ **noop**。**Enroll 是 delta 主场** —— 用旧硬编 oklch 色字面 + #fff 按钮，需对齐 v2 auth-family（同 Login TOKENS_V2.err + T.warn + T.sendFg）。

## §1 改动（Enroll 色/token 对齐 — 6 类）

| 站点 | 现 | → | 类 |
|---|---|---|---|
| L125/152/187/202 按钮 `color:'#fff'`（×4）| #fff | **T.sendFg** | hex 偿还 R-484（ForceChangePassword 已用 sendFg）|
| L219-221 ErrorBanner `oklch(62% 0.22 27)`（×3 bg/border/color）| 硬编 oklch | **TOKENS_V2.err** + color-mix | v2 auth-family error（同 Login）|
| L166-168 warn banner `oklch(72% 0.15 85)`（×3）| 硬编 oklch | **T.warn** + color-mix | v2 warn token（同 ForceChangePassword 模式）|
| L181 `'color-mix(in oklch, T.accent 12%, transparent)'` | 未插值字面（CSS 失效 bug）| `` `...${T.accent}...` `` | 插值修复（downloaded 按钮 bg）|

**import 追加**：Enroll `import { I, KnotLogo, TOKENS_V2 }`（additive；用 TOKENS_V2.err；同 Login v0.6.4.1）。

**ForceChangePassword**：**noop**（已 v2，0 改）。

## §2 保 byte-equal / 不动守护

- **QR bg `#fff`（L117）= 功能性豁免**（QR 扫描需浅底 + 深模块，theme-independent；demo auth L142/144 亦 #fff）
- **不做 demo 外层 AuthCard wrap**（radius 20 + boxShadow）= 结构改 OOS（产品 Enroll 无边框居中列 + ForceChangePassword 已有 radius12 card；保各自布局）
- **bg T.bg 不动**（Enroll/ForceChangePassword 居中布局；demo radial-gradient at 30% 20% = VRP 陷阱不照搬，同 Login v0.6.4.1.1 教训）
- **card 内卡 radius 不动**（Enroll QR 卡 12 / recovery 卡 10 / ForceChangePassword card 12；非本批焦点 — 色/token 对齐为主）
- KnotLogo R-199.5 sustained（5 文件不增减）+ 业务逻辑（4-step enroll / changePassword）+ props byte-equal → App.jsx 0 diff
- R-227.5.1 步骤标题 mono uppercase 装饰豁免 sustained

## §3 红线

| # | 红线 |
|---|---|
| R-UI2-AU-1 | EnrollScreen 5 props + ForceChangePasswordScreen 4 props 签名 byte-equal → App.jsx 0 diff |
| R-UI2-AU-2 | Enroll hex 偿还：4× `#fff`→T.sendFg；QR bg #fff 功能豁免（grep `#[0-9a-fA-F]` 排 rgba/QR = 0）|
| R-UI2-AU-3 | Enroll 硬编 oklch 清零：ErrorBanner→TOKENS_V2.err + warn→T.warn（grep `oklch(` Enroll 仅剩 color-mix in oklch + QR 无）|
| R-UI2-AU-4 | L181 broken `T.accent` 字面 → `${T.accent}` 插值修复 |
| R-UI2-AU-5 | ForceChangePassword 0 改（noop verified）|
| R-UI2-AU-6 | 不做 AuthCard 结构 wrap（布局/bg/card radius 不动）；KnotLogo 5 文件 sustained |
| R-UI2-AU-7 | 业务逻辑 byte-equal（enroll 4-step / changePassword / api.totp）|
| R-UI2-AU-8 | Enroll ≤ 240 LIMIT；其他屏/App/Foundation 0 改 |
| R-UI2-AU-9 | R-PA-8 Phase B 字面 0 |
| R-UI2-AU-10 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.10；Shell L43 不动（条件式，stays v0.6.4.2）|

## §4 验收
1. Enroll 4 按钮 sendFg + ErrorBanner 红（TOKENS_V2.err）+ warn 琥珀（T.warn）+ downloaded 按钮 bg 生效（插值修复）；ForceChangePassword 不变
2. `git diff origin/main -- frontend/src/screens/ForceChangePassword.jsx frontend/src/App.jsx frontend/src/Shared.jsx` = 0
3. hex 0（QR 豁免）+ 硬编 oklch 0 + npm build exit 0 + check_file_sizes OK + R-PA-8 0
4. **4 处版本 → 0.6.4.10**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（Enroll 4-step：QR + 验证码 + recovery + confirm，按钮/error/warn 色 v2；ForceChangePassword 改密流程不变）

## §5 commit 序列
- **commit 1**：Enroll 色/token 对齐（4×sendFg + TOKENS_V2.err + T.warn + 插值修复 + import）+ 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §6 协议合规
轻量 v3：执行者 Stage 1（adversarial 评估 = Enroll 色/token 对齐 + ForceChangePassword noop，QR 豁免 + 不做结构 wrap）→ 守护者 Stage 3（adversarial 复核 ForceChangePassword noop + Enroll 硬编 oklch/hex 清零 + 不越界结构 + 契约）→ 逐 commit 直读 → 资深 push/merge + :8000 live。**UI v2 全屏收尾**（用户侧 7 + admin 10 + auth 2 = 19 屏/文件）。
