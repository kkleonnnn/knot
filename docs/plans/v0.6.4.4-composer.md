# v0.6.4.4 — UI v2 Composer（R-217 hold 清偿 · 最小 PATCH）

> **协议**：轻量 v3。**特殊**：守护者起草 Stage 1（= 自审，破独立性）→ 守护者 adversarial Stage 3 补偿（查起草未看的盲区）→ 执行者 commit 1。资深拍板「A 最小 PATCH + Stage 3 由守护者做」。
> **基座**：worktree `v0644-composer` @ branch `v0.6.4.4-composer`（off main `257c768` = v0.6.4.3）
> **源稿**：`knot_demo_ui/v0.6/artboards/home.jsx`（composer 块）+ `chat-results.jsx`（活跃态，自审复验无独立 composer）

## §0 核心 finding（守护者 adversarial 自审确认）

**Composer v0.5.11 早已是 v2 artboard spec** —— v0.6.4.3 执行者「Composer 需 radius14 + 双层 shadow 大改」假设被现实推翻：
- radius 14（L47）· 双层 boxShadow（L21-24 base + focus ring color-mix）· focus-within border/shadow（L46/48）· send 32×32（L87）· hint mono（L66-74）· textarea minHeight 48（L60）—— 逐项重合或更 richer。

**唯一真 delta = L94 `Spinner color="#fff"` → `T.sendFg`**（R-484 hex 偿还；Composer 最后一处 hex 清零）。

**守护者 adversarial 自查（补偿自审破独立性）**：
1. `chat-results.jsx`（Conversation 活跃态 composer）→ ✅ 无独立 composer 设计（单一共享组件，两消费者同渲染）→ 无漏 delta
2. Composer 除 L94 外其他 hex → ✅ 仅 L94 1 命中（grep 排 rgba/oklch）
3. rgba 豁免边界 → ✅ 仅 boxShadow L22-23（R-313 sustained）

## §1 改动

| 文件 | 改动 |
|---|---|
| `Composer.jsx` L94 | `<Spinner size={12} color="#fff"/>` → `color={T.sendFg}`（唯一源码 delta；R-484）|
| `main.py` | version 0.6.4.3 → 0.6.4.4 |
| `test_rename_smoke.py` | R-72 字面 0.6.4.3 → 0.6.4.4 |
| `README.md` 顶部 | v0.6.4.3 → v0.6.4.4（KNOW-1）|
| `Login.jsx` footer | v0.6.4.3 → v0.6.4.4（**R-181 无条件 ★CI** — 1 行 carve-out）|

**footer hint 保产品 R-255 版**（`Enter 发送 · Shift+Enter 换行`）——**不采** artboard `enter ↵`/小写（字面分流 byte-equal）。

## §2 红线

| # | 红线 |
|---|---|
| R-UI2-C-1 | Composer 9 props 签名 byte-equal → ChatEmpty + Conversation 0 diff |
| R-UI2-C-2 | `git diff origin/main -- Composer.jsx` = **仅 L94 1 行**（hex）|
| R-UI2-C-3 | hex 0（grep `#[0-9a-fA-F]{3,6}` Composer 排 rgba = 0；L94 `#fff` 清零）|
| R-UI2-C-4 | footer hint R-255 byte-equal（不采 artboard ↵）|
| R-UI2-C-5 | boxShadow rgba R-313 豁免 sustained（无新增 rgba）|
| R-UI2-C-6 | **4 无条件版本同步**（main + smoke + README★ + **Login footer R-181 ★**）→ 0.6.4.4；**Shell L43 不动**（条件式，未改 Shell，stays v0.6.4.2）|
| R-UI2-C-7 | 其他屏/Foundation/App 0 改；Composer ≤100 LIMIT（hex 净 0 行）|
| R-UI2-C-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-C-9 | **R-217 hold（自 v0.5.10 挂 9 PATCH）正式清偿归档**（实证 Composer 已 v2 → hold 解除）|

## §3 验收
1. `git diff origin/main -- Composer.jsx` = 仅 L94；ChatEmpty/Conversation 0 diff
2. hex 0 + VRP grep 0 + npm build exit 0 + check_file_sizes OK
3. **4 处版本 → 0.6.4.4**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
4. **CI 重点盯 R-181（Login footer）+ KNOW-1（README）**（v0.6.4.3 R-181 红教训）
5. 资深 :8000 post-merge live（Composer loading spinner 色 + send/focus 不破）

## §4 commit 序列
- **commit 1**：Composer L94 hex + 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG + R-217 清偿归档

## §5 协议合规
守护者 Stage 1 自审（透明声明）+ adversarial Stage 3（查 chat-results 盲区补偿独立性）→ 执行者 commit 1 → 守护者逐 commit 直读（重点 4 处版本 + diff 仅 hex）→ 资深 push/merge + :8000 live。

## §9 LOCKED 归档（守护者 Stage 3 + commit 1 ACK）

**守护者 commit 1 ACK（`90002b0`）= 干净独立守护**（执行者写 commit，守护者验 — 自审风险止于 Stage 3，实现由独立角度确认）：Composer diff 仅 L94 hex · 契约 8 文件 0 diff · 4 无条件版本 → 0.6.4.4（含 Login footer★）· Shell L43 stays v0.6.4.2 条件式不动。

**⭐ R-217 hold 正式清偿（自 v0.5.10 挂 9 PATCH）**：
- R-217 三方共识：v0.5.10 Home 复刻时 Composer 留独立 PATCH 大改（避免层级断层）→ v0.5.11 复刻 → v0.6.4.3 又假设「Composer 需 v2 大改」留本 PATCH。
- 守护者 adversarial 自审实证 **Composer v0.5.11 早已是完整 v2 spec** → 「需大改」假设被推翻，唯一债 = 1 行 hex（今清零）→ **hold 解除归档**。

**自审/adversarial 流程记录（破独立性 + 补偿）**：
- 守护者起草 Stage 1 = 自审（破 v3 独立性，透明声明）；资深拍板「Stage 3 由守护者做」。
- 守护者 adversarial Stage 3 补偿：专查起草未看的盲区 —— ① chat-results.jsx 活跃态 composer（确认无独立设计，单一共享组件）② Composer 除 L94 外 hex（确认仅 1 处）③ rgba 边界（仅 boxShadow R-313）→ §0 finding 独立角度复验成立。
- commit-1 复核 = 干净独立守护（执行者写、守护者验）。

**⭐ 修正版本同步模型首次完整施行（v0.6.4.3 R-181 CI 红教训）**：
- 4 无条件 ★CI（main.py + smoke + README KNOW-1 + **Login footer R-181**）每 PATCH 必 bump；Shell L43 = 1 条件式无 CI（仅改 Shell 时）→ task #44 待补 CI/动态读。
- v0.6.4.3 误把 Login footer 当条件式留旧值 → R-181 CI 红 → commit 3 补救。本 PATCH Login footer 主动 bump → R-181 预期绿。
