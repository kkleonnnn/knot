# v0.5.6 — Claude Design UI 重构（C5）— 设计 tokens + Shared 视觉重构第一刀 — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 7 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

7/7 决策（D1-D7）**与执行者 Stage 1 提议完全一致**，0 修订。
新增 5 条红线（R-165~R-169），Stage 2 (2) + Stage 3 (3)。
**Stage 2 唯一成功标准**："视觉变了，但逻辑和契约没变" — 转化为 R-156/157/158/159/160 五条契约红线。

## 1. 决议表最终裁定（D1-D7）

| ID | 锁定 | 与执行者提议 |
|---|---|---|
| **D1** Tokens 切换 | ✅ A Shared.jsx 内建 | 一致 |
| **D2** 颜色空间 | ✅ A 原生 OKLCH | 一致 |
| **D3** Brand 色相 | ✅ A 电青色 195° | 一致 |
| **D4** Icon 集合 | ✅ A 保全 36 + 可选扩充 | 一致 |
| **D5** App.css 范围 | ✅ A 仅切字体 + 清残留 | 一致 |
| **D6** utils.jsx 风格 | ✅ A 视觉重写保签名 | 一致 |
| **D7** className 字面 | ✅ A 强制 byte-equal | 一致 |

## 2. 红线最终清单（16 条 R-154~R-169）

### Stage 1 草案原 11 条（保留）

| ID | 摘要 |
|---|---|
| **R-154** | backend tests **430 严格不变** |
| **R-155** | 7 contracts KEPT |
| **R-156** | 18 屏 0 修改（screens/ 字面 byte-equal） |
| **R-157** | Shell.jsx / App.jsx / api.js / index.css / main.jsx 字面 byte-equal |
| **R-158** | Shared.jsx 契约 0 破坏（I 36 names + buildTheme 25 字段 + 函数签名） |
| **R-159** | utils.jsx 契约 0 破坏（8 exports 函数签名） |
| **R-160** | App.css `cb-sb` + `cb-fadein` 字面 byte-equal |
| **R-161** | 视觉迁移：OKLCH + PingFang/HarmonyOS + Icon viewBox 24 stroke 1.6 |
| **R-162** | npm build 通过；vite bundle ≤ +1KB gzip |
| **R-163** | routes=72 / version=0.5.6 |
| **R-164** | 不动 package.json / requirements / pyproject / .importlinter / vite.config |

### Stage 2 新增 2 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-165** | **OKLCH Fallback 兜底** | App.css `:root` + `@supports not (color: oklch(0% 0 0))` hex fallback；仅 base styles，不渗透 buildTheme 25 字段 |
| **R-166** | **WCAG AA 对比度合规** | brand 195° on bg / text on bg ≥ 4.5:1；用在线 OKLCH 对比度计算器（不引 npm dep） |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-167** | **语义色辨识度** | success 145° 翠绿 / warn 85° 琥珀 / error 27° 朱红；远离 brand 195°；故意触发错误手测 |
| **R-168** | **字体抗锯齿** | App.css `body` 加 `-webkit-font-smoothing: antialiased` + `-moz-osx-font-smoothing: grayscale` |
| **R-169** | **CHART_COLORS 8 色 45° 均匀分布** | hue: 195/240/285/330/15/60/105/150；lightness 65~70% / chroma 0.16~0.20 |

## 3. 实施核心顺序（Stage 3 §2 锁定）

**commit 1 子步骤 1→6 顺序锁死**（**严禁跳序**）：

| # | 子步骤 | 守护红线 |
|---|---|---|
| **1** | buildTheme(dark) 25 字段切 OKLCH | R-158 (字段保) + R-166 + R-167 + R-169 |
| **2** | iconBtn / pillBtn 样式重写 | R-158 (函数签名保) |
| **3** | I 36 names path 重绘（viewBox 24 stroke 1.6） | R-158 (names 保) |
| **4** | CHART_COLORS + LineChart/BarChart/PieChart/TypingDots 默认色 OKLCH | R-158 (函数签名 + props 保) + R-169 |
| **5** | utils.jsx Modal/ModalHeader/Input/Select/Spinner/toast 视觉重写 | R-159 (8 exports 函数签名保) |
| **6** | App.css 字体切 + Vite 残留清 + R-165 fallback + R-168 抗锯齿 | R-160 (cb-sb/cb-fadein byte-equal) + R-168 |

**commit 1 收尾强制**：
```bash
git diff frontend/src/screens/ frontend/src/Shell.jsx frontend/src/App.jsx \
         frontend/src/api.js frontend/src/index.css frontend/src/main.jsx
# 输出必须 0 行；任何修改 = 范围溢出立即 reset（R-156/157）
```

## 4. 文件级改动清单

```
frontend/src/Shared.jsx                     [EDIT 重构 ~295 行]
                                            子步骤 1: buildTheme(dark) 25 字段切 OKLCH
                                            子步骤 2: iconBtn / pillBtn 样式重写（保签名）
                                            子步骤 3: I 36 icon names path 重绘
                                            子步骤 4: CHART_COLORS + 4 charts 默认色 OKLCH

frontend/src/utils.jsx                      [EDIT 视觉重写 ~115 行]
                                            子步骤 5: Modal/ModalHeader/Input/Select/Spinner/toast

frontend/src/App.css                        [EDIT ~184 → ~80 行]
                                            子步骤 6: 字体 + 抗锯齿 + fallback + 清 Vite 残留
                                            cb-sb / cb-fadein 字面 byte-equal [R-160]

frontend/src/screens/                       [unchanged]   R-156（18 屏 0 修改）
frontend/src/{App,Shell}.jsx                [unchanged]   R-157
frontend/src/{api.js,index.css,main.jsx}    [unchanged]   R-157

knot/main.py                                [EDIT version 0.5.5 → 0.5.6]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.5 → 0.5.6]

scripts/check_file_sizes.py                 [unchanged]
.github/workflows/ci.yml                    [unchanged]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]   R-164

CHANGELOG.md                                [EDIT [Unreleased] v0.5.6 段]
CLAUDE.md                                   [EDIT path version + § v3 历史 +1 + 路线图 ⏳→✅]
docs/plans/v0.5.6-claude-design-foundation.md   [本锁定手册]
```

## 5. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `feat(v0.5.6): Shared.jsx + utils.jsx + App.css 视觉重构 — OKLCH 蓝青 195° + PingFang/HarmonyOS + Icon viewBox 24 stroke 1.6 (R-154~R-169)` | npm build + 18 屏 0 diff + 契约守护 + R-166/167/168/169 手测 |
| 2 | `chore(v0.5.6): version bump 0.5.5 → 0.5.6` | 全闸门 + routes=72 + version=0.5.6 |
| 3 | `docs(v0.5.6): CHANGELOG + CLAUDE.md + plan LOCKED 归档` | 全闸门 |

## 6. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 7 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 16（v0.5.6 区段 R-154~R-169）
- ✅ 范围最小化 — Out-of-Scope 严守（Shell/Login/18 屏/Logo/design 子目录全部 0 触碰）

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-10）。执行者按本手册实施。
