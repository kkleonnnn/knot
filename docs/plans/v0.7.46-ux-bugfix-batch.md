# v0.7.46 — UX bugfix 批（P0 反馈条崩溃 + 故障伪装空态 + CSV/截断）· Stage 1 执行手册

> **执行者**:v0.7 Agent · **日期**:2026-07-05 · **基线**:main `52f9d40`(v0.7.45)
> **性质**:纯 bugfix PATCH(无新能力 / 无新红线 / 无 schema 改动)→ 按 §MINOR 规则为 **PATCH** 级。
> **来源**:v0.7→v0.8 整体审核执行者产物 §3.1 即修清单([overall-review-executor-2026-07-04](v0.7-to-v0.8-overall-review-executor-2026-07-04.md));6 项全部执行者 grounded 复核到真实代码行。
> **协议**:走完整 v3 三阶段(Stage 1 本文 → Stage 2 辅助 AI 初审 → Stage 3 守护者终审 → 落地)。**本文是 Stage 1 草案,未开始改代码。**

---

## §1 决议与范围

### 目标
清掉整体审核发现的一批"点一下就能碰到"的前端 UX 缺陷,其中 1 项为 P0(负反馈通道完全不可用)。这些缺陷能存活多个版本的根因 = 前端无 UI 冒烟测试(后端 1046 test vs 前端 3),本 PATCH 只修 bug,测试栈补强留 v0.8。

### 范围(IN)
| # | 缺陷 | 文件:行(grounded) | 修法 | 级别 |
|---|---|---|---|---|
| 1 | **反馈条 Modal 崩溃** — 点 👎 打开评论弹窗即 `TypeError: Cannot read 'content' of undefined` → ErrorBoundary 把整条查询结果替换成"渲染出错"卡片 | `FeedbackBar.jsx:45` `<Modal onClose={...}>` 漏传 `T`;`utils.jsx:46` Modal 无条件解引用 `T.content` | 加 `T={T}`(T 已在 prop 作用域,同文件 L46 已在用) | **🔴 P0** |
| 2 | **admin 加载失败伪装成"暂无数据"** — loadAll catch 空吞,失败后 tabLoading=false → 显示"暂无用户/数据源/目录" | `Admin.jsx:109` `catch { /* 由 toast 上层处理 */ }`(注释谎称有上层,实无) | catch 补 `toast('加载失败: '+e.message, true)`(toast 已 import L14) | 高 |
| 3 | **知识库三 tab 加载期显示"暂无"** — TabKnowledge 未接收 loading,加载中直接渲染"暂无文档/示例" | `tab_knowledge.jsx:26` 签名无 `loading`;`Admin.jsx:393` dispatch 未传 `loading={tabLoading}`(对照 `Admin.jsx:373` TabAccess 有传) | ① Admin.jsx:393 加 `loading={tabLoading}` ② 签名加 `loading` ③ 3 处空态 mirror `tab_access.jsx:56` `{loading ? '加载中…' : '暂无…'}` | 高 |
| 4 | **前端错误屏自己加载失败时谎报"暂无错误记录"** | `AdminErrors.jsx:34` `.catch(() => {/* fail-soft */})`;toast 未 import(仅 Spinner) | import 加 `toast` + catch 补 toast | 中 |
| 5 | **Chat CSV 导出中文列名 Excel 乱码** — 无 UTF-8 BOM(对照 `AdminAudit.jsx:37` 有 BOM) | `Chat.jsx:209` `new Blob([csv], …)` | 改 `new Blob(['﻿' + csv], …)` | 中 |
| 6 | **结果表格静默截断** — 显示全量"N 行"但只渲染前 100 行,无提示(对照 SavedReports 有提示) | `TableContainer.jsx:45` 显示 `{rows.length} 行`;L59 `rows.slice(0,100)` | 行数标签 `rows.length>100` 时显示"前 100 行 · 共 N 行" | 低 |

### 范围(OUT — 分叉点,见 §7)
- **item 5-orig(App.jsx 侧栏静默)**:触碰 byte-equal 铁律守护的 App.jsx + onErr 三处共享 + 登录 prefetch 弹 toast 行为变更 → **默认 defer 出本 PATCH**,待 §7 拍板。
- 前端测试栈(jsdom/@testing-library 引入,给 P0 补渲染回归)= 新 npm 依赖,按抗诱惑清单单独 chore,留 v0.8。
- 其余整体审核项(死码清扫 / query.py 抽取 / T Context / sync 链删除)= 各自独立 PATCH。

---

## §2 红线(byte-equal + 抗诱惑)

- **只改上表 6 文件**(FeedbackBar / Admin / tab_knowledge / AdminErrors / Chat / TableContainer)+ 版本 4 源点;**其余 18 屏 + Shell + utils.jsx + api.js + App.jsx + index.css + main.jsx + Shared.jsx git diff 0 行**。
- **不碰 utils.jsx**:P0 只在调用点补 T,**不**给 Modal 加 dev 断言(会破 utils.jsx byte-equal;回归守护改由 v0.8 测试栈承担)。
- 不动视觉设计语言:6 项全部沿用既有 token / toast / 文案模式,**0 新增视觉元素、0 新 buildTheme 字段、0 新 icon**。
- 不顺手 i18n、不引新依赖、不改其他屏。
- 业务链路(api.* / SSE / localStorage / onFeedback 契约)byte-equal —— item 1 只加 prop,不改反馈提交逻辑。
- export name + props 签名:item 3 给 TabKnowledge **加** `loading`(additive,父调用同步更新),不改现有 props 顺序语义;FeedbackBar/TableContainer 签名不变。

---

## §3 文件改动清单(精确到行为)

1. `frontend/src/screens/chat/ResultBlock/FeedbackBar.jsx` — L45 `<Modal onClose=` → `<Modal T={T} onClose=`。
2. `frontend/src/screens/Admin.jsx` — L109 catch 体加 toast(保留"保持当前数据视图"语义,仅加可见提示)。
3. `frontend/src/screens/admin/tab_knowledge.jsx` — L26 签名加 `loading`;knowledge/fewshots/prompts 三处空态渲染加 `{loading ? '加载中…' : …}`(实施时定位精确行,mirror tab_access.jsx:56)。
   `frontend/src/screens/Admin.jsx` — L393 TabKnowledge dispatch 加 `loading={tabLoading}`。
4. `frontend/src/screens/AdminErrors.jsx` — import 行加 `toast`;L34 catch 加 toast。
5. `frontend/src/screens/Chat.jsx` — L209 Blob 前拼 `'﻿'`。
6. `frontend/src/screens/chat/ResultBlock/TableContainer.jsx` — L45 行数标签截断感知。
7. **版本 4 源点 + CHANGELOG**(v0.7.45→v0.7.46):`knot/main.py` version · `tests/test_rename_smoke.py` L131/134 断言 + docstring · `README.md` 顶部 · `frontend/src/version.js` APP_VERSION · `CHANGELOG.md` 顶部条目。
8. `knot/static/`(**`npm run build` 产物**)— 前端改动必重建(VRP 协议)。

---

## §4 验收标准

1. **P0 闭环**:点 👎 → 评论 Modal 正常打开(不再 TypeError)→ 填/空提交均记录 → 整条结果卡片不被 ErrorBoundary 替换。
2. **空态诚实**:admin 任一 tab / 知识库三 tab / 前端错误屏在后端不可达时,显示"加载中…"(加载期)或明确 toast 报错(失败),**不再**出现"暂无 X"伪装。
3. **CSV**:Chat 导出的 CSV 用 Excel 打开中文列名不乱码(与 AdminAudit 导出行为一致)。
4. **截断提示**:结果 >100 行时表头显示"前 100 行 · 共 N 行"。
5. **全闸门绿**:pytest 全套(version 断言更新后)· check_file_sizes · lint-imports 9 kept · ruff · 前端 3 vitest 不回归。
6. **byte-equal 守护**:`git diff origin/main HEAD -- frontend/src/` 仅含 §3 的 6 屏文件 + version.js + static 产物;`App.jsx / utils.jsx / Shared.jsx / api.js` 0 行(除非 §7 拍板纳入 App.jsx)。
7. **doc-invariant CI**:4 源点版本一致 + CHANGELOG 顶 == main version。

---

## §5 commit 序列(建议 4 commit)

1. `fix(chat): 反馈条 Modal 补传 T 修 P0 崩溃`(item 1,单独锁定最紧急项)
2. `fix(admin): 加载失败不再伪装空态`(item 2+3+4,故障可见性组)
3. `fix(chat): CSV 加 UTF-8 BOM + 表格截断提示`(item 5+6)
4. `chore: bump v0.7.46 + CHANGELOG + rebuild static`(4 源点 + npm build)

> commit 序列工期可硬预估(守护者 Stage 3 依据,R-PB-GOV-1 例外):6 项均 1-3 行改动 + 1 次 build,实施半日内可全闸门绿。

---

## §6 协议合规

- Loop Protocol v3 完整三阶段;本 PATCH 有业务代码,**不适用** R-LP-v3-EX-1(方向决策跳 Stage 2 例外)。
- Stage 3 守护者(v0.6 Agent)终审重点:① item 3 additive prop 是否破 R-116 AdminScreen 契约 ② byte-equal 守护是否真收敛 6 文件 ③ §7 App.jsx 分叉点裁定。
- 对抗验证:P0 与空态修复为行为变更,守护者 Stage 3 可跑 Workflow 对抗验证(v0.7.20 教训:无单点可信)。
- 不变量延续:R-PA-PB-V1 视觉铁律 + doc-invariant CI + R-192 AppShell props 全部守护。

---

## §7 待决分叉点(资深/守护者拍板)

**item 5-orig — App.jsx 侧栏静默失败是否纳入本 PATCH?**
- **现状**:`App.jsx:69` `api.get('/api/conversations').then(setConvs).catch(onErr)`,`onErr` 只处理 enroll 403,其余错误静默 → 会话侧栏永久空白无提示。
- **代价**:App.jsx 是 CLAUDE.md byte-equal 铁律守护文件;`onErr` 被 conversations / db-status / datasources 三处 prefetch 共享;修复=登录后 prefetch 失败会弹 toast(行为可见变更,transient 网络抖动也会弹)。
- **选项 A(执行者建议)**:defer 出 v0.7.46,并入 v0.8 前端硬化 PATCH(届时连同 T Context / Admin 拆分一起,App.jsx 本就要动)。理由:本 PATCH 保持"清晰小赢"纯度,不为一项债破 App.jsx byte-equal。
- **选项 B**:纳入本 PATCH,显式声明 App.jsx in-scope,conversations 单独 catch(不改 db-status/datasources 的 onErr),失败弹一次 toast。
- 【假设待验】选项 A 不影响 P0/其余 5 项落地节奏。

---

## §8 自检

- [x] 6 项全部 grounded 到真实代码行(非 subagent 转述定稿)
- [x] P0 执行者亲自复核(FeedbackBar:45 缺 T + utils Modal:46 解引用 T.content)
- [x] 修法依据核实:toast 在 Admin 已 import / AdminErrors 未 import;tabLoading state 存在;tab_access:56 loading 模板确认
- [x] 红线:不碰 utils.jsx/Shared.jsx/api.js;App.jsx 作分叉点摆出
- [x] 版本 4 源点 + CHANGELOG 列全
- [x] Stage 3 守护者终审(执行者-as-guardian,7 并行 grounded 核实 wq17kqq16)

---

## §9 Stage 3 终审整合(LOCKED · 2026-07-05)

**裁决:ACCEPT WITH REVISIONS** — 6 项 bug 全 CONFIRMED,以下修订已整合落码:

| 修订 | 内容 | 落地 |
|---|---|---|
| **R1(必改)** | item 2 draft 写 `e.message` 但 catch 是 bare(无 e 绑定)→ `catch (e) { toast('加载失败: ' + (e?.message || e), true); }` | ✅ Admin.jsx:109 |
| **R4(必改)** | item 4 `.catch(() =>` 无 e 参 → `.catch((e) => { toast(...) })` | ✅ AdminErrors.jsx:34 + import toast |
| **订正(item 3)** | 实为 **2 处空态**(knowledge L100 + fewshots L183),**prompts 段无「暂无」空态**(直渲 PromptsView)→ 只改 2 处 | ✅ tab_knowledge.jsx |
| **建议(item 5)** | Blob type 补 `;charset=utf-8`(对齐 AdminAudit 完整) | ✅ Chat.jsx:211 |
| **排除(item 6)** | 对抗发现 L60 border 逻辑 `ri < rows.length-1` >100 时末行边框语义偏 + 100 硬编 2 处 → **属范围外,勿顺手改**(scope 纪律) | ✅ 未改,仅动 L45 行数标签 |
| **§7 DEFER** | App.jsx 侧栏静默采纳 Option A(DEFER v0.8):Option B 只修 conversations = asymmetric 半修;破 App.jsx byte-equal for 单项债;transient 抖动弹 toast 需 debounce 设计 | ✅ 未纳入本 PATCH |
| **prose 订正** | "后端 1046 test" → 实 **1044**(v0.7.45 prompt-only 未加测) | 记于此,非闸门 |

**守护者提示(merge 前 kk 履约)**:P0 无 jsdom 自动回归(B5.1 vitest 是 node-env 纯逻辑)→ **merge 前人验**:点 👎 → 评论 Modal 正常开 → 填/空提交均记录 → 整条结果卡不被 ErrorBoundary 替换。1-char 修 + 机制 verifier grounded 确认,手验可接受;jsdom 回归留 v0.8。
