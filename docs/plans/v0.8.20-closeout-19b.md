# v0.8.20 — 整体审核收口 19b · 执行手册（Stage 1）

> **性质**：v0.8.19 整体审核收口 PATCH 的**第二刀（19b）**。scope 在 v0.8.19 Stage 1 手册里已随 F1-F8 一起过 **Codex Stage 2 + 守护者 Stage 3 终审**（major-revise，5 blocking must-fix）；19a 已实施+Stage 4 PASS+MERGED（PR #239）。
> **协议**：19b 是**同一 Stage-3-reviewed 计划的后半**（非新 scope）→ 执行者按 Stage 3 终审的 must-fix 实施 → **守护者 Stage 4 final-diff**。若守护者/资深认为 F7（default-admin，安全-部署敏感）需**新一轮 Stage 2/3**，支持补（见 §6）。
> **基线**：main @ `a7709f1`（v0.8.19）。**版本目标**：0.8.19 → **0.8.20**。
> **来源**：[`v0.8.19-overall-review-closeout.md`](v0.8.19-overall-review-closeout.md) §0（Stage 3 LOCKED）+ 守护者 Stage 4 carry-forward。

---

## § 1 决议（范围）

**目标**：清完整体审核收口剩余项（19a 已清 F1/F5/P1）。

| # | 项 | 严重度 | 守护者 must-fix | 锚点 |
|---|---|---|---|---|
| F2 | use_agent 成功公式**源头修 + 残留闭合** | latent-HIGH | **#2** | `sql_planner.py:196`+`:350`+`:191` |
| F4 | 2 SSRF（db/test 授权 + datasources allowlist） | MEDIUM | — | `database.py:33` / `admin/datasources.py:42` |
| F6a | SSE 中断分支成本落账 | low-medium | #4（F6a 部分） | `query.py:530/533` |
| F7 | default-admin 首启竞态缓解（+DEPLOY 重写） | 低-中 | 决策 b / R-LP-v3-EX-3-1 | `base.py:213-225` |
| F8-min | echarts XSS 升级 + doc drift（P0/P1） | MEDIUM/— | #5（余项） | `package.json` / README / DEPLOY / .env.example |

**范围外（defer 独立，非 19b）**：**F3**（busy_timeout **已 ≈5000ms** —— sqlite3.connect 默认 timeout=5.0；须实测锁类型再对症修，非 no-op）· **F6b**（da_asst 进 messages 桶已**否决** → 随 agent_kind「三源合一」Occam chore）· **F8 CLAUDE.md 关键路径表大修**（独立 docs 专项）· **base.py→migrations.py 拆**（v0.9 前，承诺已登记）。

---

## § 2 红线（守护点 + 携带 must-fix 条件）

- **F2 必须闭合残留（守护者 must-fix #2）**：删第二子句**不充分** —— `sql_planner.py:190-191` fallback 路径 `exec_err` 只进 observation 不进 `final_error` → 末步走此路径 + max_steps 耗尽仍 `success=True`。**须同时**让 `:191` 的 `exec_err` 落 `final_error`。sync `:196` + async `:350` 一致。
- **F6b 不做**（守护者 must-fix #4）：da_asst **不进 messages agent_kind 桶**（3 重复源 + 硬编列 + 已双账 + 语义错配）。19b 只做 **F6a**（SSE 中断落账，正交干净）。
- **byte-equal**：F8 echarts = `package.json` 版本 bump + lock + rebuild static，**`frontend/src/**` 0 改**。F2/F6a 不改 SSE 协议字段集 / 契约（只补错误路径记账，镜像既有 `:284-300`）。
- **不碰**：`catalog.py` / LogicForm / `KNOT_SEMANTIC_LAYER` / 多租户结构 / bi_* 前端。import-linter 9 KEPT。
- **F7 立约已在**：R-LP-v3-EX-3-1（CLAUDE.md，19a 随 #238 已合）—— 19b 落**代码缓解 + 文档同步**。

---

## § 3 文件改动（per-fix · grounded）

### F2 — use_agent 成功公式源头修 + 残留
- `sql_planner.py:196`（sync）+ `:350`（async）：删第二子句 → `success = bool(final_sql and not final_error)`。
- `sql_planner.py:191`（fallback re-execute 失败分支）：`observation = f"重新执行失败: {exec_err}"` **之后补** `final_error = exec_err`（使 fallback 路径的 DB 错也进 final_error，闭合残留）。sync + async 双胞胎一致。
- **无回归论据**（守护者）：成功 execute 必 `:190 break` → `final_error` truthy ⟺ 无成功执行 ⟺ rows 空，永不把真成功翻 False。

### F4 — 2 SSRF
- **①** `database.py:33` `POST /api/db/test`：依赖 `get_current_user` → **`require_admin`**；`test_connection` 返回文案收敛（不区分 refused/auth-fail，去 error_msg 原文透传，防内网探测判别）。
- **②** `admin/datasources.py`：`_test_source` HTTP HEAD（:42）+ `admin_create/update_datasource` 存 http_config 前，调 `check_url_allowed`（`url_allowlist`，对齐 `executor.py:97` 单一守卫）。

### F6a — SSE 中断成本落账
- `query.py:530`（`except KnotError`）+ `:533`（`except Exception`）：**镜像 `:284-300` 跨源错误路径** —— `if agent_buckets 有累计成本`（防 pre-LLM 早退产生虚假 error 行）→ `aggregate_agent_costs(agent_buckets)` + `save_message`（错误行）+ `update_user_usage`。防已完成步骤（clarifier/sql_planner）花费在异常时全丢（R-17 聚合一致性 + 看板可见）。

### F7 — default-admin 首启竞态缓解
- `base.py:213-225` seed：`import os` → `env = os.environ.get("KNOT_INITIAL_ADMIN_PASSWORD")`；`env` 有值 → 用之；无 → **随机强口令**（`secrets.token_urlsafe(12)`）+ `logger.warning` **一次性打印**（运维从启动日志取）。`must_change_password=1` 保留。**消除「跨部署同一已知 admin123」**。
- **reset 脚本**：`knot/scripts/reset_admin_password.py`（env / 随机重置 admin 口令 + must_change_password=1），供运维遗失口令时用。
- **文档同步**：`README.md:147` + `DEPLOY.md` **8 处 admin123**（`:35/:259/:274/:275/:277/:340/:519/:520`）改为新首启流程；修 `DEPLOY.md:274` stale ref（引 `base.py:94`，实际 seed 在 `:213`）。

### F8-minimal — echarts XSS + doc drift（P0/P1）
- `frontend/package.json`：`echarts ^6.0.0 → ^6.1.0`（GHSA-fgmj-fm8m-jvvx XSS）；`npm install` 更 lock；`npm run build` 重建 static；`npm audit --omit=dev` 0 moderate+。
- doc drift：`README.md:62` 调度器「在建」→已交付（v0.8.17）· `README.md:150` 数据源夸大（删 ClickHouse/SQLite + 加 http 类型）· `DEPLOY.md:3` 版本 v0.7.36→0.8.20 + IM 分享出境 3 域名白名单说明（`im_egress.py:13`）· `.env.example` 补 `OPENROUTER_API_KEY=` + v0.7/v0.8 env（KNOT_TOTP_REQUIRED / KNOT_SEMANTIC_LAYER / KNOT_HTTP_ALLOWED_HOSTS / KNOT_WEBHOOK_ALLOWED_HOSTS / KNOT_SCHEDULER_TOKEN / STRICT_READONLY_GRANTS + **KNOT_INITIAL_ADMIN_PASSWORD**）。
- **注**：路由数 143 已在 19a 落；CLAUDE.md 关键路径表大修 defer 独立专项（不在 F8-min）。

---

## § 4 验收标准

- **F2**：新增测试 —— use_agent SQL 引用不存在列，**走 fallback re-execute 路径**（非仅 `__FINAL__`）+ max_steps 耗尽 → `success=False` + `error` 非空（sync + async 双验）；流式主链路成功路径结果 byte-equal 无回归。
- **F4**：`/api/db/test` 非 admin → 403（新测）+ 前端调用点确认 admin-only（`admin/modals.jsx` db-test 按钮，收紧无破）；datasources create/update/probe 对非白名单 host → 拒（新测）。
- **F6a**：mock presenter 抛 KnotError → agent_buckets 已累计成本落 messages + update_user_usage（新测）；pre-LLM 早退 → 不产生虚假 error 行（新测）。
- **F7**：无 env → seed 随机口令 + 日志一次性打印 + must_change_password=1（新测）；有 `KNOT_INITIAL_ADMIN_PASSWORD` → 用 env 值（新测）；reset 脚本可跑。
- **F8**：`npm audit --omit=dev` 0 moderate+；README/DEPLOY/.env.example drift 清；DEPLOY 版本 0.8.20。
- **全局闸门**：full pytest 绿 · import-linter 9 KEPT · check_file_sizes OK · frontend lint/build · 4 版本源点 0.8.19→0.8.20 + CHANGELOG + doc-invariant/bridge。

---

## § 5 commit 序列（建议 · Stage 4 前可调）

1. **F2**（`sql_planner.py:196/:350/:191` 删第二子句 + 残留闭合 + fallback 失败测）—— 最小纯正确性。
2. **F6a**（`query.py:530/533` 中断落账 + 测）。
3. **F4**（`database.py` + `admin/datasources.py` + 测）—— 安全，小。
4. **F7**（`base.py` seed env/随机 + `reset_admin_password.py` + README/DEPLOY 8 处 + 测）—— 部署敏感，最大。
5. **F8-minimal**（package.json echarts + static rebuild + README/DEPLOY/.env.example doc drift）。
6. **收尾**：版本 4 源点 0.8.19→0.8.20 + CHANGELOG（demote v0.8.19）+ README banner + full 闸门。

---

## § 6 协议合规（v3）

- 本手册 scope（F2/F4/F6a/F7/F8-min）**已在 v0.8.19 Stage 1 手册随 F1-F8 过 Codex Stage 2 + 守护者 Stage 3**（must-fix 条件已定）→ 19b 按 Stage 3 终审实施 → **守护者 Stage 4 final-diff**。
- **⚠️ 建议资深/守护者裁定**：**F7（default-admin，安全-部署敏感 + 新增 reset 脚本 + 新 env）**是否需**新一轮 Stage 2/3**（而非仅沿用 v0.8.19 Stage 3）？执行者判断 F7 的实现细节（随机口令 UX / reset 脚本）超出原 Stage 3 review 深度，**倾向 F7 单独补一轮 Stage 2/3**，其余 F2/F4/F6a/F8-min 沿用既有 Stage 3 → Stage 4。请资深拍。

---

## § 7 自检（抗诱惑）

- 仅碰 §3 锚点文件；不顺手改语义层 / 多租户结构 / Shared.jsx / 其他屏。
- **F6b 不做**（已否决）；**F3 不做**（defer）；CLAUDE.md 大修不做（defer）。
- 不引新 npm 依赖（echarts 是版本 bump）。
- F7 随机口令用 `secrets`（非 `random`）；env 名 `KNOT_INITIAL_ADMIN_PASSWORD` 同步 .env.example + DEPLOY。
- 落码后验 clean worktree 可复现构建；版本收尾 4 源点 + CHANGELOG 恰 1 `[Unreleased]`。
