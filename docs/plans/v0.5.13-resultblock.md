# v0.5.13 — (C5+) ResultBlock 偿还 — hex 清理 + emoji 偿还 + 局部视觉微调 — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 14 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**Q1 修订**（Stage 1 A useMemo → Stage 2 B helper function `getErrorKindMeta(T, kind)` 更解耦）。

**Stage 2 关键加码 3 条**（含 R-302.5）：
- **R-302.5**：语义级 Emoji 业务豁免（ErrorBanner 7 emoji 保留）— **字面分流体系第三条**
- **R-312**：color-mix 精度守护（必须含 `in oklch`，严禁 srgb / 空缺）
- **R-313**：boxShadow rgba 豁免延续（仅 boxShadow 内允许 rgba）

**Stage 3 关键加码 3 条**：
- **R-314**：收藏按钮双态同步手测
- **R-315**：Token Meter mono 纯度
- **R-316**：msg 25 字段解构精准守护

红线总数 26（**R-292~R-316** 含 R-302.5）。子步骤增至 **9 步**。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** R-286 hex 全清扩展 | ✅ A 15+ 处全清 | Stage 1 |
| **D2** 收藏按钮 emoji→svg | ✅ A inline svg star | Stage 1 |
| **D3** AGENT_KIND_EMOJI 偿还 | ✅ A inline svg dict | Stage 1 |
| **D4** BudgetBanner emoji→svg | ✅ A inline svg | Stage 1 |
| **D5** ErrorBanner emoji 保留 | ✅ A 保留（业务 UX） | Stage 1 |
| **D6** Token meter pill chip | ✅ A mono pill | Stage 1 |
| **D7** Scope 边界 | ✅ A **绝对同意** | Stage 2 强调 |
| **D8** LIMIT 400→420 | ✅ A 微调 ack | Stage 2 |
| **Q1** ERROR_KIND_META 改造 | ⚠️ **B Helper Function**（修订 — Stage 1 推荐 A useMemo） | Stage 2 推荐 `getErrorKindMeta(T, kind)` 更解耦 |
| **Q2** svg path 集中 const 字典 | ✅ A `const RB_SVG = {...}` | Stage 1 |
| **Q3** LIMIT 微调 | ✅ A 接受 | Stage 2 |
| **Q4** R-302.5 emoji 业务豁免 | ✅ A **正式建立 R-302.5** | Stage 2 强制加码 |
| **Q5** Scope 受控 | ✅ A 分两步 | Stage 2 同 D7 |

---

## 2. 红线最终清单（26 条 R-292~R-316 含 R-302.5）

### Stage 1 草案原 20 条（保留）

| ID | 摘要 |
|---|---|
| **R-292** | ResultBlock 7 props 签名 byte-equal |
| **R-293** | msg 25 字段解构 byte-equal |
| **R-294** | ERROR_KIND_META 7 kind 字面 byte-equal（icon/title 不动） |
| **R-295** | 7 layout 分支字面 byte-equal（R-117 sustained） |
| **R-296** | resolveEffectiveHint / exportMessageCsv / MetricCard 业务 byte-equal |
| **R-297** | onCopy/onDownload/onFollowup/onPin/onRetry 调用方式不变 |
| **R-298** | R-286 扩展 — hex 0 命中 |
| **R-299** | hex+alpha 拼接 → color-mix(in oklch, ...) |
| **R-300** | ERROR_KIND_META 字典 hex → T tokens + color-mix |
| **R-301** | `'#fff'` / `'#0001'` 兜底 → T.sendFg / T.bg |
| **R-302** | AGENT_KIND_EMOJI 4 处 emoji → inline svg |
| **R-303** | 收藏 ⭐🌟 → inline svg star（实心/描边态） |
| **R-304** | BudgetBanner 🛑⚠️ → inline svg |
| **R-305** | ErrorBanner 7 emoji 保留（业务 UX） |
| **R-306** | Token meter pill chip |
| **R-307** | ResultBlock.jsx ≤ 420 |
| **R-308** | backend 432 tests / 7 contracts / 72 routes |
| **R-309** | App/api/index.css/main/utils/Shared/Shell/decor/17 屏 byte-equal |
| **R-310** | chat/ 其他 6 子模块 byte-equal |
| **R-311** | 三处版本同步 0.5.12→0.5.13 |

### Stage 2 新增 3 条（含 R-302.5）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-302.5** | **语义级 Emoji 业务豁免** — emoji 是业务状态核心辨识符号且无对应 SVG 视觉标准时允许保留 | 字面分流体系第三条（R-227.5 装饰小写 / R-227.5.1 单字母装饰豁免 / R-302.5 语义级业务豁免）；ErrorBanner 7 emoji 适用 |
| **R-312** | **color-mix 精度守护** — 所有 color-mix 必须含 `in oklch` 插值空间 | grep `color-mix\([^)]` 每次命中必须含 `in oklch`；严禁 srgb / 空缺 |
| **R-313** | **boxShadow rgba 豁免延续** — rgba 仅在 boxShadow 内允许；其他字段严禁 | grep `rgba(` 命中行**全部**在 boxShadow 上下文 |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-314** | **收藏按钮双态同步** — SVG star 与 onPin 完美绑定；点击立即切换实心/描边 + 触发 API | commit 1 后强制手测：① 空心→实心 + API ② 实心→空心 + API ③ 多 message 状态独立 |
| **R-315** | **Token Meter Mono 纯度** — Pill Chip 100% T.mono；数字等宽对齐 | grep `fontFamily.*T\.mono` 命中；多设备实测对齐 |
| **R-316** | **msg 25 字段解构精准守护** — 严禁增减；recovery_attempt 等保留 | `git diff` 检查解构语句字段数 byte-equal |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死）

> 先完成 getErrorKindMeta 函数化改造清除字典 Hex → 再全局 grep 扫描清 15+ 处 Hex

| Step | 动作 | 红线 |
|---|---|---|
| 1 | `const getErrorKindMeta = (T, kind) => {...}` helper（Q1 B 修订） — 字典 hex → T tokens + color-mix in oklch | R-300 + R-312 |
| 2 | `const RB_SVG = { sparkle, search, wrench, chart, star_filled, star_outline, shield, triangle }` svg path 集中字典 | Q2 A |
| 3 | AGENT_KIND_EMOJI 4 处 → svg path 引用 | R-302 |
| 4 | 收藏按钮 ⭐🌟 → svg star 实心/描边 + onPin 双态 | R-303 + R-314 |
| 5 | BudgetBanner 🛑⚠️ → svg shield/triangle | R-304 |
| 6 | ErrorBanner 7 emoji **保留**（R-302.5 豁免） | R-305 + R-302.5 |
| 7 | Token meter 行内 stat → mono pill chip + mono 纯度 | R-306 + R-315 |
| 8 | 全局 hex 扫描清理 — `T.accent + '30'` → `color-mix(in oklch, T.accent 30%, transparent)`；`'#cc6600'` / `'#FF990022'` / `'#FF9900'` / `'#fff'` / `'#0001'` → T tokens | R-298/299/300/301/312 |
| 9 | rgba 边界（仅 boxShadow）+ msg 解构字段数 byte-equal | R-313 + R-316 |
| 收尾 | grep + npm build + 7 intent 端到端业务回归 + R-314 收藏双态手测 | 见 §5 验收 |

### Stage 3 §3 验收关键

```bash
# R-298 hex 0 命中（排除 boxShadow）
grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/screens/chat/ResultBlock.jsx | grep -v boxShadow
# 预期：0 命中

# R-312 color-mix 精度
grep -n "color-mix" frontend/src/screens/chat/ResultBlock.jsx
# 每行必须含 "in oklch"

# R-313 rgba 仅在 boxShadow
grep -nB2 "rgba(" frontend/src/screens/chat/ResultBlock.jsx
# 每命中行上下文必须含 boxShadow

# 7 intent 端到端 — 实际触发 metric_card/line/bar/pie/rank_view/detail_table/retention_matrix
```

### Stage 2 Q1 修订 — getErrorKindMeta helper 实施

```jsx
// 模块顶层（无 T 依赖部分保留 const）
const ERROR_KIND_ICONS = { budget_exceeded: '🛑', config_missing: '🔧', llm_failed: '🤖',
                          sql_invalid: '🚫', sql_exec_failed: '⚠️', data_unavailable: '📡', unknown: '❌' };
const ERROR_KIND_TITLES = { budget_exceeded: '预算超限', config_missing: '配置缺失', llm_failed: 'AI 服务异常',
                            sql_invalid: 'SQL 不合规', sql_exec_failed: 'SQL 执行失败',
                            data_unavailable: '数据源不可用', unknown: '系统错误' };

// Helper function — 组件内调用
const getErrorKindMeta = (T, kind) => {
  const isCritical = kind === 'budget_exceeded' || kind === 'sql_invalid' || kind === 'unknown';
  return {
    icon: ERROR_KIND_ICONS[kind] || ERROR_KIND_ICONS.unknown,
    title: ERROR_KIND_TITLES[kind] || ERROR_KIND_TITLES.unknown,
    color: isCritical ? T.accent : T.warn,
    bg: `color-mix(in oklch, ${isCritical ? T.accent : T.warn} 13%, transparent)`,
  };
};
```

### Stage 2 R-302.5 字面分流体系扩展

**v0.5.x 字面分流体系三条**（CHANGELOG 显式标注）：

| 红线 | 内容 | 落地 |
|---|---|---|
| **R-227.5** (v0.5.10) | 装饰小写豁免 | "knot · ready" |
| **R-227.5.1** (v0.5.12) | 单字母装饰豁免 | K/N/O letter chip |
| **R-302.5** (v0.5.13) | 语义级业务 Emoji 豁免 | ErrorBanner 7 emoji |

### Stage 2 R-312 color-mix 精度

```jsx
// ✅ 正确
background: `color-mix(in oklch, ${T.accent} 30%, transparent)`,
// ❌ 禁止
background: `color-mix(${T.accent} 30%, transparent)`,           // 缺空间
background: `color-mix(in srgb, ${T.accent} 30%, transparent)`,  // 错空间
```

### Stage 2 R-313 rgba 边界

```jsx
// ✅ 允许
boxShadow: '0 1px 3px rgba(15,30,45,0.04)',
// ❌ 禁止
background: 'rgba(15,30,45,0.04)',  // 非 boxShadow
color: 'rgba(0,0,0,0.5)',           // 非 boxShadow
```

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/chat/ResultBlock.jsx   [EDIT 381 → ≤ 420 行]
                                            Step 1: getErrorKindMeta helper [Q1 B + R-300 + R-312]
                                            Step 2: const RB_SVG = {...} svg path 字典 [Q2 A]
                                            Step 3: AGENT_KIND_EMOJI → svg [R-302]
                                            Step 4: 收藏 ⭐🌟 → svg star + 双态 [R-303 + R-314]
                                            Step 5: BudgetBanner 🛑⚠️ → svg [R-304]
                                            Step 6: ErrorBanner 7 emoji 保留 [R-305 + R-302.5]
                                            Step 7: Token meter pill chip + mono 纯度 [R-306 + R-315]
                                            Step 8: 全局 hex 清理 → T tokens + color-mix in oklch [R-298/299/300/301/312]
                                            Step 9: rgba 边界 + msg 解构字段数 byte-equal [R-313 + R-316]
                                            守护: 7 props 签名 byte-equal [R-292]
                                                  + msg 25 字段解构 byte-equal [R-293]
                                                  + 7 kind icon/title byte-equal [R-294]
                                                  + 7 layout 分支字面 byte-equal [R-295]
                                                  + business handlers byte-equal [R-297]
                                                  + inline styles only

scripts/check_file_sizes.py                 [EDIT LIMIT 微调]
                                            - "frontend/src/screens/chat/ResultBlock.jsx": 400 → 420 [Q3 ack]

knot/main.py                                [EDIT version 0.5.12 → 0.5.13]  [R-311]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.12 → 0.5.13]  [R-311]
frontend/src/screens/Login.jsx              [EDIT 页脚字面 0.5.12 → 0.5.13]  [R-311]

frontend/src/screens/chat/* (其余 6 子模块) [unchanged]              [R-310]
frontend/src/screens/ (其余 17 屏)           [unchanged]              [R-309]
frontend/src/App.jsx                        [unchanged]              [R-309]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-309]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]              [R-309]
frontend/src/App.css                        [unchanged 0 行 diff]    [R-309]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                    [unchanged]
backend (knot/) 任何文件除 main.py          [unchanged]              [R-308]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.13 段]
                                            R-292~R-316 偿还表 + R-302.5 字面分流体系扩展 + Loop Protocol v3 第 14 次施行
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.13
                                            - v0.5.x 路线图 v0.5.13 ⏳ → ✅
docs/plans/v0.5.13-resultblock.md           [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（不允许减少）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（30 条 LIMITS，ResultBlock 400→420）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.13**

### Stage 2/3 强化验证

- [ ] **R-298 hex 0 命中**：`grep -nE "#[0-9a-fA-F]{3,6}" ResultBlock.jsx | grep -v boxShadow` = 0
- [ ] **R-302.5 字面分流**：CHANGELOG 显式标注（R-227.5 / R-227.5.1 / R-302.5）
- [ ] **R-312 color-mix**：每次 `color-mix` 命中含 `in oklch`
- [ ] **R-313 rgba 边界**：`rgba(` 命中全部在 boxShadow 上下文
- [ ] **R-314 收藏双态**：手测 ① 空心→实心 + API ② 实心→空心 + API ③ 多 message 独立
- [ ] **R-315 Token Meter mono**：pill chip `fontFamily.*T\.mono` 命中
- [ ] **R-316 msg 解构 byte-equal**：解构语句字段数不变

### 契约守护

- [ ] **R-292**：ResultBlock 7 props 签名 byte-equal
- [ ] **R-293**：msg 25 字段解构 byte-equal
- [ ] **R-294**：ERROR_KIND_META 7 kind 字面 byte-equal（icon/title 不动）
- [ ] **R-295**：7 layout 分支字面 byte-equal
- [ ] **R-296**：resolveEffectiveHint / exportMessageCsv / MetricCard 业务 byte-equal
- [ ] **R-297**：5 handlers 调用方式不变

### 范围守护

- [ ] **R-309**：App/api/index.css/main/utils/Shared/Shell/decor/17 屏 `git diff` = 0
- [ ] **R-310**：chat/ 其他 6 子模块 `git diff --stat origin/main HEAD -- frontend/src/screens/chat/` 仅 ResultBlock
- [ ] App.css `git diff` = 0

### 端到端业务回归

- [ ] 7 intent 触发并渲染正常：metric_card/line/bar/pie/rank_view/detail_table/retention_matrix
- [ ] 收藏按钮 onPin API 调用 + pinned 态切换（R-314）
- [ ] ErrorBanner 7 类 kind 渲染（emoji 业务豁免 R-302.5）
- [ ] BudgetBanner 触发 + 关闭（svg 偿还 R-304）
- [ ] CSV 下载 / SQL 折叠 / followup 点击
- [ ] v0.5.0~v0.5.12 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.13): ResultBlock.jsx 偿还 — R-286 hex 清理 + emoji→svg + token pill + getErrorKindMeta helper (R-292~R-316)` | npm build + 7 intent 端到端 + R-298 hex 0 + R-310 仅 ResultBlock diff + R-314 收藏双态 |
| 2 | `chore(v0.5.13): version bump 0.5.12→0.5.13 三处同步 + ResultBlock LIMIT 400→420 微调 ack (R-307/311, Q3)` | 全闸门 + version=0.5.13 + 30 条 LIMITS |
| 3 | `docs(v0.5.13): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + R-302.5 字面分流体系扩展` | 全闸门 |

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 14 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 26（v0.5.13 区段 R-292~R-316 含 R-302.5）
- ✅ **R-302.5 语义级 Emoji 业务豁免** — 字面分流体系第三条
- ✅ **R-286 全面禁止扩展** — ResultBlock 是 v0.5.x 系列 hex 残留最重组件，本 PATCH 收尾
- ✅ Stage 2 Q1 修订 — Helper function 比 useMemo 更解耦
- ✅ R-251 视觉自动跟随设计模式 sustained
- ✅ Scope 受控分两步 — table+chart+insight 留 v0.5.14

---

## 8. 执行者纪律自检（14 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.13-resultblock`
2. ✅ Q1 修订严守 — Step 1 直接走 helper function `getErrorKindMeta(T, kind)`
3. ✅ R-298 hex 0 命中 — commit 1 收尾强制 grep（排除 boxShadow）
4. ✅ R-312 color-mix 精度 — 每个 color-mix 含 `in oklch`
5. ✅ R-313 rgba 边界 — 仅 boxShadow；其他字段严禁
6. ✅ R-302.5 业务豁免边界 — ErrorBanner 7 emoji 保留；icon 字段不动
7. ✅ R-314 收藏双态手测 — 实际启动 + 实际点击 → 立即 UI 切换 + API 调用 + 多 message 独立
8. ✅ R-315 Token Meter mono — pill chip fontFamily T.mono；数字等宽对齐
9. ✅ R-316 msg 解构精准 — 25 字段全保留（含 recovery_attempt 等未变视觉字段）
10. ✅ D7 scope 受控严守 — table/chart/insight 不动；v0.5.14 独立 PATCH
11. ✅ Step 1→9 顺序锁死
12. ✅ 7 intent 端到端回归 — 实际触发全 7 类查询
13. ✅ Q3 LIMIT 420 资深 ack — 与 v0.5.3/v0.5.7 微调先例同模式
14. ✅ 范围抗诱惑 — 不顺手改 ChatEmpty/Composer/ThinkingCard/Conversation 视觉

---

🛡️ **执行者收到 GO 信号，待资深架构师最终 ack 后启动 Stage 4。**
