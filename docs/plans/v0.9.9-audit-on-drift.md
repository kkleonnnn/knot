# v0.9.9 — 租户漂移写审计（兑现 R-10 · Stage 1 草案 · 一页纸）

> **协议**：Loop Protocol v3 + v3.1。**本文档 0 行业务代码。**
> **触发**：kk 2026-08-01「那你给兑现了呗」。⇒ **不升红线，直接做掉**（原 D-G 待拍项撤回）。
> **前置**：v0.9.8（平台审计落点，`8be8ce1` 已合 main）。

---

## §1 现状（grounded）

| 事实 | 依据 |
|---|---|
| 漂移绊线已有生产调用点，检测到「ctx 非 None 但 tid 对不上」⇒ **拒绝请求** | `core/tenant_context.py:113` ← `api/deps.py:162`（**恰一处**） |
| 但它**只写日志 + 进程计数器**，**刻意不写审计** | `tenant_context.py:127` 原文：「R-10：**刻意不新增 `AuditAction`** —— LOCKED 的 audit-on-drift **仍未结清**」 |
| ⇒ 该安全事件**过后查不到**：日志会滚、计数器重启归零 | —— |
| LOCKED 决议自 v0.9.4 登记，经 .5/.6/.7/.8 **连续 5 片未兑现** | 超 R-LP-v3-EX-3 的「≥3」线（先例 `R-LP-v3-EX-3-1`） |

## §2 ⛔ 两条**先自己撞到**的硬约束（不是评审给的）

### 2.1 绊线**不能自己写数据库**
`.importlinter` Contract `core-no-business`：`knot.core` **禁 import** `knot.repositories`
⇒ `assert_tenant_context` 住在 `core`，**没有任何途径**直接写审计。
（这是 v0.9.8 那条阻断的同形，本片自查时提前撞到。）

### 2.2 「在应用层统一记录」这条路**走不通**
`deps.py:161-164` **在调用点就把 `TenantContextError` 折成 `HTTPException(401)`**，
且那个 `except` 是**刻意**的（注释原文：「运维要能把『租户不可服务』与『坏 token』分开」）
⇒ 异常**永远到不了**应用级 handler。

### 2.3 ⚠️ 而那个 `except` 同时接住**两种**情况
- **真漂移**（ctx 非 None 但对不上）—— 单租户下**不应发生**，发生即事故；
- **中间件没设 ctx**（租户停用/不存在）—— v0.9.4 明写这是**预期路径**。

⇒ **直接在那个 `except` 里记审计会把预期路径也记进去** ⇒ 审计表被噪音刷满、真信号被淹没。

---

## §3 决议

### D1 `core` 新增 `TenantDriftError(TenantContextError)`，**只在真漂移时抛**

判断「什么算漂移」**仍留在 `core`**（单一判据，不复制到调用方）；
调用方只需**能区分**它 —— 靠异常类型，不靠自己重新判断。

### D2 记录点 = `deps.py` 那个 `except`，**先接 `TenantDriftError`（记 + 401），再接 `TenantContextError`（只 401）**

- 记录发生在**漂移被转成拒绝的那一行** ⇒ 满足 v3.1-B #3「记录与被记录的动作是同一个事件」。
- `api → repositories` 顺向合法（v0.9.5/v0.9.8 已在用）。
- ⚠️ 今天**恰一个**调用点；若将来出现第二个，它拿到的是同一个异常类型 ⇒
  **哨兵**（见 D5-③）断言「捕获 `TenantDriftError` 的地方都记审计」。

### D3 写进**平台审计表**（不是租户审计表）—— 并**推翻我上周的判断**

> **串台发生的那一刻，「现在是哪家公司」这个信息本身就是坏的。**
> 写进某一家公司的表，就得先相信那个已经坏掉的信息；
> 平台表**不需要知道是哪家公司**（`get_platform_conn` 是 ctx-free 的）
> ⇒ **它是这件事发生时唯一还可信的落点。**

⚠️ **我上周在 CLAUDE.md 写的「R-10 是租户侧的事」是错的，本片推翻它**
（当时没想到「上下文本身不可信」这一层）。⇒ 也就是说 CLAUDE.md 里**原先**那条
「因平台侧无审计落点所以未解开」**是对的** —— 是我 v0.9.8 的「订正」订错了。文档同步改。

### D4 新 Literal `platform.tenant_ctx_drift`（第 3 条）
它**有生产者**（D2）⇒ 满足「只声明有生产者的 Literal」；
且 v0.9.8 的**精确集合**守护会强制本片同时更新它 —— 机制按预期工作。

记录内容：`tenant_id` = **JWT 声明的 tid**（请求自称的身份）· `success=False`（请求被拒）·
`detail` = `{"expected": <jwt tid>, "actual": <ctx tid>}`（都是内部 id，非凭据 ⇒ 不触 #262）。

### D5 三条守护
1. 真漂移 ⇒ 审计表多一条、`success=0`、`detail` 含 expected/actual；
2. **中间件没设 ctx（预期路径）⇒ 审计表零新增**（防噪音淹没真信号）；
3. **哨兵**：AST 断言「`except TenantDriftError` 的每一处都调了平台审计写口」——
   防将来第二个调用点漏记（避免「N 份清单」）。

### D6 ⚠️ 一个**固有**的策略题（不是自造的，请评审重点看）
审计写失败时怎么办？
- **本片主张：仍返 401，审计失败记 ERROR 日志。** 理由：**保护动作（拒绝请求）已经发生**，
  把它改成 500 会让「审计基础设施故障」看起来像服务器故障，**反而更可能掩盖漂移本身**。
- ⚠️ 与 v0.9.8 那个策略题的**区别**：那次「两个选项都不好」是因为**失败模式是自造的**
  （审计与动作可以同事务）⇒ 消掉本体即可。**这次不行** —— 这里的「动作」是「拒绝请求」，
  它不是一次 DB 写 ⇒ **没有可与之同事务的东西** ⇒ 这个二选一是**固有的**，必须选一个。
- ⇒ 按 v3.1-B #10 的判据自问过了：**这次的影子背后确实有本体。**

---

## §4 验收

1. 真漂移 ⇒ 平台审计 +1 条（action / success=0 / expected+actual 齐）+ 仍 401；
2. 中间件没设 ctx ⇒ **审计零新增** + 仍 401（取材：把 D1 的类型判断去掉 ⇒ 本条红）；
3. 审计写失败 ⇒ **仍 401**（不变成 500）+ ERROR 日志（取材：让写口抛 ⇒ 断言状态码仍 401）；
4. Literal 精确集合守护自动要求同片更新（v0.9.8 已建）；
5. 哨兵 D5-③ 取材：加一个 `except TenantDriftError` 但不记审计的假调用点 ⇒ 红；
6. 四闸门 + 全量（基线 **1718**）。

## §5 commit 序（4 步）
1. `docs(plan)` 本文档
2. `feat(core)` `TenantDriftError` + 只在真漂移抛（**零消费者**，行为不变）
3. `feat(security)` `deps.py` 分两支 + emit + Literal + 验收 1-3
4. `test`+`docs` 哨兵 D5-③ + CLAUDE.md 订正（推翻「租户侧」那句）+ 4 源点 + CHANGELOG

## §6 v3.1-B 枚举表（承重面：审计 + 鉴权）
| # | 答 |
|---|---|
| 1 fail-open 面 | 漂移仍 fail-closed（401 不变）；本片只加记录 |
| 2 oracle | 验收 2 用「审计表零新增」表示「预期路径没被记」—— 能表示要排除的事件 |
| 3 「那一行」族 | 记录在**漂移转成拒绝的那一行**；判据仍在 `core`（单一判据） |
| 5 散文规则 | 「第二个调用点必须也记」⇒ 已配哨兵 D5-③，不留散文 |
| 6 声明 vs 生产者 | 新 Literal 有生产者；精确集合守护强制同片 |
| 9 契约冲突 | ⭐ 命中：`core-no-business` ⇒ 记录点必须在 `api`（本片自查时提前撞到，非评审给出） |
| 10 策略题 | ⭐ 命中但**这次是固有的**（见 D6）—— 与 v0.9.8 的自造式两难不同 |
| 11 不声称 | 不改漂移的**检测**逻辑；不动那个兜底 `except Exception`（既有 backlog）；不做审计限流（见下） |

⚠️ **未做 + 理由**：不给漂移审计加限流/去重。漂移**不是外部可触发的**
（ctx 与 tid 同源于一枚 JWT，正常路径恒相等）⇒ 写入量受「真实 bug」而非攻击者控制。
加限流反而引入「你要的那条恰好是第 101 条」的新失败模式。**请评审确认这个判断。**

---

## §7 Stage 3 六项（M1-M6）—— 已落，**每条先自验**

> 守护者裁定 `minor-revise`，六项落完即开工、不必再给一版。
> ⚠️ 下列每条的事实我都**自己复跑过**（R-137：评审意见是线索，用前必验）。

### M1 给「免限流」的前提配一条守护（否则它会静默过期）

**自验**：`tenant_resolution.py:26-30` —— `_LEGACY_SINGLE_TENANT_PATHS` **恰含一条**
`/api/bi/scheduler/tick`，注释原文「**无 JWT**（调度器共享密钥）⇒ tid 不适用」；
且 `/api/auth/login` **已于 v0.9.4 摘出**该集合。
⇒ 该路径不经 `get_current_user` ⇒ `assert_tenant_context` 在那条路上**永不执行**
⇒ **漂移确实不可外部触发** ⇒ D6 的免限流判断成立。

⚠️ **但这个结论依赖一个没有守护的事实** —— 而 **P4（scheduler tick 租户域化）正是要动这里的那一片**。
⇒ **守护**：断言该集合的**每个成员**在路由策略快照里都是 `PUBLIC_OR_OUT_OF_BAND`
（= 不挂 `get_current_user`）。⇒ 往里加任何带鉴权的路径 ⇒ **撞红**，而不是让推理静默过期。

### M2 新异常子类会不会被既有 `except` 吞掉 —— **靠枚举关闭，具名写下来**

**风险形状**：`TenantDriftError(TenantContextError)` ⇒ 任何既有 `except TenantContextError`
都会**连带吞掉**漂移，而它比「无 ctx」严重得多。

**自验：全仓恰 2 处**
| 位置 | 会不会吞掉漂移 |
|---|---|
| `deps.py:163` | **本片目标** —— 改成先接 `TenantDriftError`（记 + 401）再接父类（只 401） |
| `auth.py:52` | **结构上到不了** —— 登录路径（`_resolve_login_tenant`）**从不调** `assert_tenant_context`；它接的是「按 slug 解析租户」的失败 |

⇒ 安全。**写下来的理由不是形式主义**：「新增异常子类被既有父类 handler 吞掉」是真实危险，
这次是靠**枚举**关闭的；不写下来，下一个人无法判断我是否检查过。

### M3 ⭐ 哨兵重锚（原锚点的 oracle 表示不了要排除的事件）

**原锚**：「`except TenantDriftError` 的每一处都记审计」。
**逃逸形态**：将来有人在新调用点写 `except TenantContextError`（**父类**）⇒ 漂移被吞，
而哨兵**只找子类名** ⇒ **看不见**。

⇒ **改锚到真正的 choke point**：`assert_tenant_context` 的**生产调用点**。
**自验：恰 1 处**（`deps.py:162`；其余命中全是 docstring / 测）。
⇒ **形式：断言调用点数恰为 1，且该处记审计**（exact-count 与 v0.9.8 的 Literal 精确集合同源
—— 逼「第二个调用点」成为一次显式评审改动）。

⭐ **别新写 —— 扩既有那条**：`tests/test_tenant_resolution.py::test_B4_drift_tripwire_has_production_call_site`
（v0.9.4 立）**已经收集了调用点**，但只断言「非空」。⇒ 在它上面加「恰 1 处 + 该处记审计」，
**不加第二份判据**（否则就是本弧反复治的「N 份清单」）。

### M4 `tenant_id` / `tenant_slug` 的口径 —— 选 **(a) NULL**，不放宽既有列语义

**自验**：v0.9.8 的 schema 注释原文 `tenant_id INTEGER,  -- 动作的对象租户（NULL = 非租户维动作）`。

⇒ 漂移事件**没有单一「对象租户」**（有两个互斥声明）⇒ 写 JWT tid 会让平台 admin 读成
「这条是关于租户 X 的动作」= **静默放宽了那条列语义**。
⇒ **裁定 (a)**：`tenant_id = NULL` · `tenant_slug = NULL` · **两个 id 都放 `detail`**
（`{"expected": <jwt tid>, "actual": <ctx tid>}`）。
**理由**：既有列语义原样成立，无需同片改注释；而「两个互斥声明」这件事本身就该体现在
**detail 里有两个值**，而不是在 tenant_id 里挑一个。
（顺带解决了守护者点名的「JWT tid 解析不出租户时 slug 怎么办」—— 本方案下 slug 恒 NULL，不存在该问题。）

### M5 计数器与审计的顺序 —— **结构上已成立**，但仍加验收钉住

**自验**：计数器自增在 `core/tenant_context.py`（`_drift_state["count"] += 1`），**在 WARN 之前**；
而审计写在 `deps.py` 的 `except` 里 ⇒ **`core` 先跑并抛，调用方才可能写审计**
⇒ **顺序不可能被倒置**（除非把计数器搬出 `core`，而 `core-no-business` 不允许它搬去能写库的地方）。

⇒ 守护者的担心（「审计写故障时两个信号一起消失」）**在本设计里结构性不成立**。
但仍按要求**加验收**把这条性质钉住：**审计写口抛 ⇒ 仍 401 且计数器仍 +1**。

### M6 枚举表补全（v3.1-B 明写「可答不适用但不能跳过」）

| # | 答 |
|---|---|
| **4**（已并入 #3） | 失败说明挂在**真会失败的路径**：验收 2/3/5 全用 `try/except/else` 或内容级断言 |
| **7 顶班** | **不适用** —— 本片零门被摘除 |
| **8 既有测的绿是真的吗** | ⚠️ **有一条**：v0.9.4 那三条漂移测用 `pytest.raises(TenantContextError)`，子类化后**仍绿**但对**新类型零判别力**（父类捕获子类）。**这不是缺陷**（它们本就只测「拒绝」这个后果），但正是 #8 要说出来的话 ⇒ 本片**新增**一条断言「抛的是 `TenantDriftError` 而不只是父类」，否则「只抛父类」这个回退**没有测会红** |

⚠️ **本片是 v3.1 的首次施行 ⇒ 枚举表的完整性本身就是被观察的对象**（守护者原话）。

### 守护者补强的两条（记录，非改动）

1. **D3 的理由可以更硬**：写租户库不止是「要相信坏信息」，而是
   **把安全事件披露给错误那家公司的 admin、同时对该知道的那家隐藏它** = **一次跨租户信息披露**。
2. **D6 的辨析是 #10 判据被正确使用的示范**：那次两难自造可消，这次「动作 = 拒绝请求」不是 DB 写、
   **没有可同事务的对象** ⇒ 固有。

## §8 验收（补 M1/M3/M5/M6 后的全集）

1. 真漂移 ⇒ 平台审计 +1（`success=0` · `tenant_id`/`tenant_slug` **均 NULL** · detail 含 expected+actual）+ 仍 401
2. 中间件没设 ctx（预期路径）⇒ **审计零新增** + 仍 401
3. 审计写口抛 ⇒ **仍 401** + ERROR 日志 + **计数器仍 +1**（M5）
4. 抛的是 `TenantDriftError` 而非仅父类（M6-#8）
5. **哨兵**（扩既有 `test_B4_drift_tripwire_has_production_call_site`）：`assert_tenant_context`
   生产调用点**恰 1 处**且该处记审计
6. **哨兵**（M1）：`_LEGACY_SINGLE_TENANT_PATHS` 每个成员在路由快照里都是 `PUBLIC_OR_OUT_OF_BAND`
7. Literal 精确集合守护自动要求同片更新（v0.9.8 已建）
8. 四闸门 + 全量（基线 **1718**）

---

## §9 实施记录

### 9.1 ⭐ 我自己的判据在这一片错了 **4 次**（都被取材逮住，没有一次靠运气）

**全部同一个病：「名字出现过」≠「那条路上用了它」/「注入能产生那个后果」。**

1. **数调用点用了按行文本扫** ⇒ 把 `platform_admin.py:6` 的**docstring 提及**算成了调用点。
   文本匹配能答「有没有」，**答不了「有几处」** —— 讨论一个名字的文件必然含有它（R-SENTINEL-AST）。
   ⇒ 改 AST。**本仓第四次「文本 → AST」**（v0.9.3 载体名 / v0.9.4 test_R17 / v0.9.5 旧名 / 本次）。
2. **五分钟后同一个测里又犯一次**：判「那支真的记了审计」时断的是「文件里出现过这两个名字」。
   故意删掉分支去取材 ⇒ **没红** —— 因为 import 还在、函数**定义**也还在。⇒ 改**结构级**判定
   （AST 找 `except TenantDriftError` handler 且其 body 里真有那次调用）。
3. **验收 2 的注入方式本身不成立**：我用「把两支合并」当取材 ⇒ **没红**。
   原因：父类异常**没有** `expected`/`actual` 属性 ⇒ 写审计时抛 `AttributeError` ⇒ 被兜底吞掉
   ⇒ **根本没写成** ⇒ 测看不见。⇒ 换成真实回退形态（在「无 ctx」那支**也加**一条 emit）才红。
   **注入必须真能产生那个后果，否则「取材证明」是空的。**
4. **我预期「Literal 精确集合守护会红」，它没红 —— 而那是对的**：我同时加了 Literal 和它的 emit
   ⇒ 两边同步 ⇒ 守护本就该绿（它只在**不同步**时红）。**我的预期错了，机制正常。**

### 9.2 ⚠️ 我放松了上一片自己写的一条哨兵（必须被看见）

v0.9.8 那条「平台审计的**调用方模块**零 env 读取」在本片**误报**：新调用方 `api/deps.py`
**合法地**读 env（JWT 密钥 6 处），与 `detail` 毫无关系。
⇒ 判据是**过宽的代理**（它想守「env 值进 detail」，却用「模块碰过 env」近似）
⇒ **收窄到「构造 `detail` 的那个函数」**。
- 收窄后**仍能抓住真事**（实测：在该函数里读 env ⇒ 红），且不误伤别处。
- **丢了什么（诚实）**：同模块里绕一个 helper 读 env 再塞进 `detail` ⇒ 函数级看不见（跨函数数据流）
  ⇒ 那条残余风险由**行为测**（内容级 oracle）兜。
- 哨兵里写了一句给下一个人：**「这次放松是因为判据本身过宽，不是因为它挡了路 ——
  若将来又有人要放松它，请先问：是判据错了，还是我的代码错了？」**

### 9.3 与 Stage 1 的偏离
无设计层偏离。M1-M6 六项按 §7 全落；其中 M5 自验为**结构上已成立**（计数器在 `core`、
审计在调用方 ⇒ 顺序不可倒置），仍按要求加了验收钉住。

### 9.4 v3.1-B 回流一问（Stage 4 强制）
**本片有没有出现表里没有的新失效形状？** —— **没有新形状**，但有一条**旧形状的新变体**值得记：
#2（oracle 能否表示要排除的事件）此前的实例都在**断言侧**；本片 9.1-3 是它在**注入侧**的镜像
——「注入是否真能产生那个后果」。⇒ 建议把 #2 的表述从「判据能表示要排除的事件吗」
扩成「**判据能表示它，且注入真能产生它吗**」。**折进 #2，不新增条目。**
