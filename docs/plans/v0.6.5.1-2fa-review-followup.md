# v0.6.5.1 — 强制 2FA review 跟进（落 #153 独立复审 5 建议）· Stage 1

> **协议**：轻量 v3 + **保 Stage 3**（含 2 test-logic 新增 + main.py 启动逻辑微改 — 守护者亲读）。
> **基座**：worktree `v0651-followup` @ branch `v0.6.5.1-2fa-review-followup`（off main `29bfb30` = v0.6.5.0）
> **触发**：`/review` PR #153 独立对抗性复审（补缺失的 Stage 2）产出 5 建议；资深「按你的建议来」。
> **性质**：测试补强 + ops 告警 + docs。**0 gate 决策逻辑改 · 0 前端改 → 单 commit（无 npm build）**。

## §0 来源（#153 复审 5 建议）
| 严重度 | 建议 | 本 PATCH |
|---|---|---|
| 🔴 | 后门不泄漏给非 admin 无测试（安全边界断言缺失）| ① 补反向测试 |
| 🟡 | rollout：升级即强制存量未 enroll 用户 enroll | ④ CHANGELOG/DEPLOY 分阶段提示 |
| 🟡 | env 拼写 fail-open（`=ture` 静默关 2FA）| ③ 启动期 warn |
| 🟢 | 非 admin 强制 enroll 路径未测 | ② 补正向测试 |
| 🟢 | CLAUDE.md 路线图缺 v0.6.5.x 行 | ⑤ 补行 |

## §1 改动

### ① 🔴 后门不泄漏给非 admin（test_totp_mandatory.py）
- 新 `test_R_2FA_3_backdoor_does_not_leak_to_nonadmin`：建 analyst（admin API + 清 must_change_password，2FA 仍 off 时 setup）→ delenv REQUIRED（默认 on）+ setenv BYPASS_ADMIN=true → analyst 打 `/api/conversations` **断言 403 totp_enroll_required**（后门 admin-only，role 短路 `role=="admin" and` 实证不泄漏）。
- 代码本已正确（gate L156 role 短路）；本测试守护「哪天有人删 role 检查」回归。

### ② 🟢 非 admin 强制 enroll（test_totp_mandatory.py）
- 新 `test_R_2FA_1_nonadmin_forced_enroll`：analyst + delenv 双 env（默认 on，无后门）→ 403 totp_enroll_required（非 admin 路径无 bootstrap，本就强制）。
- 共用 `_make_analyst(client, auth_headers)` helper（POST `/api/admin/users` role=analyst + user_repo 清 must_change_password + login 拿 token）。

### ③ 🟡 启动期 2FA 状态告警（knot/main.py）
- master-key 加载成功 log 后加：`KNOT_TOTP_REQUIRED != "true"` → `logger.warning("⚠️ TOTP enforcement 已禁用…仅 eval/demo")`；`KNOT_TOTP_BYPASS_ADMIN == "true"` → `logger.warning("⚠️ admin 应急后门启用…用完即撤")`。
- **启动期一次性**（非 per-request，避日志洪水）；fail-open 拼写 → 启动日志立即可见。`os` 已 import 验证。

### ④ 🟡 rollout 分阶段提示（CHANGELOG + DEPLOY §5）
- 加一句：「升级到默认-on 前，如不想打断在测的未 enroll 用户 → 先 `export KNOT_TOTP_REQUIRED=false` 分阶段，准备好后再移除」。

### ⑤ 🟢 CLAUDE.md 路线图补行
- v0.6.x 路线图表补 v0.6.5.0（强制 2FA）+ v0.6.5.1（本 review 跟进）2 行（简洁，催更已积压的 v0.6.5.x）。

### 版本 4 源点 → 0.6.5.1
- main.py + version.js + README + smoke；Login/Shell 经 APP_VERSION 自动跟随（**0 前端改 → 0 build**）。CHANGELOG demote v0.6.5.0 + 新 v0.6.5.1（count==1）。

## §2 红线
| # | 红线 |
|---|---|
| R-2FA-9 | 后门 admin-only 反向测试（非 admin + BYPASS=true → 403）+ 非 admin 强制测试 → 双断言绿 |
| R-2FA-10 | gate 决策逻辑 **0 改**（deps.py `_admin_bypass_active` + gate byte-equal）；本 PATCH 仅测试+告警+docs |
| R-2FA-11 | 启动 warn 仅启动期一次（非 per-request）；不改 enforcement 行为 |
| R-2FA-12 | 0 前端改（App.jsx/Shared/utils 0 diff → 0 build，单 commit）；routes 77 不变；7 contracts KEPT |
| R-2FA-13 | CHANGELOG count==1（demote v0.6.5.0）+ 4 源点版本 0.6.5.1 |

## §3 验收
1. `_make_analyst` setup OK（2FA off 时建 analyst + 清改密 + login）。
2. backdoor-no-leak（analyst+BYPASS=true → 403）+ nonadmin-forced（analyst → 403）双绿（CI 实证 — 本机无 pytest）。
3. 启动 warn：`KNOT_TOTP_REQUIRED=false` 启动 → 日志见 warning（部署后 docker logs 肉眼）。
4. gate byte-equal（deps.py git diff 0）+ 0 前端改 + routes 77 + count==1 + 版本 0.6.5.1。

## §4 commit 序列（⚠️ 自纠：version.js bump 需 rebuild bake — 初稿「无 build」误判）
- **commit 1**：2 测试 + main.py 启动 warn + CHANGELOG/DEPLOY rollout + 版本 4 源点 + 本 plan。
- **commit 2（收官）**：npm build 静态（APP_VERSION 0.6.5.1 bake — version.js 是前端 4 源点之一，bump 必 rebuild 否则 Shell/Login 显示 stale 0.6.5.0）。
- **⑤ deferred**：CLAUDE.md 路线图施行回顾表实查落后 ~11 PATCH（停 v0.6.4.1）→ 补 2 行留中间空洞；改单独 routemap 批量 catch-up（task 记账，本 micro 不碰 CLAUDE.md）。

## §5 协议
轻量 v3 + 保 Stage 3（2 test-logic + main.py 启动逻辑微改 — 守护者亲读断言 + 确认 gate 0 改）。执行者 Stage 1 → 守护者 Stage 3 → commit 直读 → 资深 push/merge + CI 终判。
