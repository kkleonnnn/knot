# v0.6.2.0 — TOTP 2FA 强制 enroll v2（Stage 4 LOCKED）

> **协议依据**：Loop Protocol v3 完整三阶段
>
> **触发**：M-1.5 / Phase B 完整版 v4 LOCKED §1 段 1 + v0.6.2.0-pre-governance #114 已 merged
>
> **整合范围**：v0.6 执行者 Stage 1 草案 + Stage 2 独立第三方 AI 评审（4.2/5）+ v0.5 守护者第 12 次 active Stage 3 终审 + 资深 9 项决策（γ1-γ3 + 6 项默认 accept）拍板
>
> **执行者**：v0.6 执行者
>
> **守护者**：v0.5 守护者（第 12 次 active；commit 2 + commit 6 强制 active sustained — γ3 决策）
>
> **远古守护者**：v0.4 远古守护者（**不激活** — 本 PATCH 不涉 v0.4.x 资产扩展；第 4 次激活预约 v0.6.3.0）
>
> **资深架构师**：你
>
> **日期**：2026-05-28
>
> **状态**：**Stage 4 LOCKED**（commit 1 可启动）
>
> **supersedes**：`v0.6.2.0-totp-2fa.md` Stage 1 草案

---

## §0 v1 → v2 修订摘要

| 修订项 | v1 → v2 | 依据 |
|---|---|---|
| 红线数 11 → **13 + 2 子条款** | +R-PB-B1-12 / +R-PB-B1-13 + NRP-1/NRP-2 | Stage 2 Q1/Q4 致命遗漏 + Stage 2 关键独立贡献 |
| R-PB-B1-3 admin 防御 | id=1 24h → **三层（env 变量 + 24h 宽限 + admin enroll 自动失效）** | Stage 2 Q5 + 守护者折中 §III.4 |
| R-PB-B1-7 enroll 二次确认 | 2 个连续动态码 → **1 个动态码 + 强制 recovery codes 下载** | Stage 2 Q4 业界标准（GitHub/Google）+ 守护者撤回 §III.3 |
| R-PB-B1-10 race 守护 | 100× ± 1 → **100× ± 5 + @race mark CI 独立 job** | Stage 2 Q3 CI 稳定 + 守护者保 v0.4 R-27 折中 + γ2 |
| R-PB-B1-11 audit_action | +3 → **+4（含 recovery_code_used）** | Stage 2 Q7 关键审计盲区 |
| **R-PB-B1-12 新增** | valid_window=1 ±30 秒时钟漂移容忍 | Stage 2 Q1 协议级遗漏 |
| **R-PB-B1-13 新增** | user_secret_version JWT 吊销真空期防御三层（schema + reset 触发 + cachetools TTLCache）| Stage 2 关键独立贡献 + γ1 |
| **NRP-1 子条款** | JWT cache 用 cachetools TTLCache **不用 lru_cache** | 守护者 §VII NRP-1 |
| **NRP-2 子条款** | race 100× + valid_window=1 测试 mock 时间锁定单步长 | 守护者 §VII NRP-2 |
| 工期 3-4 周 | → **2.5-3.5 周** | Stage 2 砍二次确认 -0.5 天 + 新红线 +1 天 = 净 +0.5 天 |
| 撤回 Stage 1 提议 | ❌ 二次确认 ❌ LRU 缓存 secret | Stage 2 Q4 + Q6 |
| 守护者强 active 时机 | 无 → **commit 2 + commit 6 强制 active** | γ3 决策 |

### Stage 4 LOCKED 累计 9 项决策

```
默认 accept 6 项：
  R-PB-B1-3 三层防御 / R-PB-B1-7 业界标准 / R-PB-B1-11 +4 audit_action
  R-PB-B1-12 valid_window / NRP-1 cachetools / NRP-2 测试隔离

资深 explicit 拍板 3 项（γ1-γ3）：
  γ1 R-PB-B1-13 全面纳入 / γ2 100×±5 折中 / γ3 commit 2+6 强 active
```

---

## §1 范围（F1-F7 — Stage 2 整合后）

### F1 — 依赖 + Schema（commit 1）
- F1.1 `pyproject.toml` 加 `pyotp>=2.9.0` + `qrcode[pil]>=7.4.2` + `cachetools>=5.3.0`（**γ1 新增 — JWT cache**）
- F1.2 `requirements.txt` 同步
- F1.3 `users` 表 ALTER：
  - `totp_secret TEXT` (Fernet enc_v1:)
  - `totp_enrolled_at TEXT`
  - `totp_last_used_at TEXT`
  - **`token_version INTEGER NOT NULL DEFAULT 1`**（**γ1 新增 — JWT 吊销机制**）
- F1.4 新表 `totp_recovery_codes`（id / user_id FK / code_hash TEXT / used_at TEXT NULL / created_at TEXT）
- F1.5 `knot/repositories/base.py` startup 幂等 ALTER + CREATE

### F2 — Service 层（commit 2 — **守护者强制 active**）
- F2.1 `knot/services/totp_service.py` [NEW ≤ 220 行 — v1 200 +20 行新红线]
  - `enroll_init(user_id) → (secret, qr_payload)` — 仅生成不持久化
  - `enroll_complete(user_id, code) → recovery_codes[10]` — **1 次动态码验证**（R-PB-B1-7 修订）
  - `verify(user_id, code) → bool` — **pyotp.TOTP(secret).verify(code, valid_window=1)**（R-PB-B1-12）+ R-PB-B1-6 rate limit + R-PB-B1-10 race 守护
  - `consume_recovery(user_id, code) → bool` — **单次使用 + audit_log recovery_code_used**（R-PB-B1-11）
  - `reset(user_id, by_admin_id) → recovery_codes[10]` — **UPDATE token_version + 1**（R-PB-B1-13）+ audit
  - `generate_qr_payload(user_id, secret) → str` — otpauth:// URI
- F2.2 Fernet 集成（R-PB-B1-1 / R-PB-B1-8）：透明 encrypt/decrypt enc_v1: 前缀
- F2.3 **R-46-Tx SQLite 事务**（R-PB-B1-9）：secret 写 users + recovery_codes INSERT 同事务
- F2.4 **应用层 Session 验证补充防御**（R-PB-B1-9 修订）
- F2.5 **JWT 吊销机制**（R-PB-B1-13）：
  - `_token_version_cache = cachetools.TTLCache(maxsize=10000, ttl=60)` — **NRP-1 sustained**
  - `get_token_version(user_id) → int` — DB 查询 + cache（reset 时 cache.pop(user_id)）
  - `bump_token_version(user_id) → int` — UPDATE users SET token_version = token_version + 1 + cache invalidate

### F3 — API 层（commit 3）
- F3.1 `knot/api/totp.py` [NEW ≤ 180 行 — v1 150 +30 行 token_version 改造]
  - `POST /api/totp/enroll-init` → 返 secret + QR payload
  - `POST /api/totp/enroll-complete` → 1 次动态码验证 + 持久化 + 返 recovery_codes（**v1 enroll-verify 改名**）
  - `POST /api/totp/verify` → login 后强制验证
  - `POST /api/totp/reset` → admin 重置（**含 bump_token_version**）+ audit_log
- F3.2 `knot/api/_rate_limit.py` 加 `rate_limit_totp_verify`（5/min/user）+ `rate_limit_totp_enroll`（3/hour/user）
- F3.3 `knot/api/auth.py` login 端点改造：
  - 成功后若 user.totp_enrolled → 返 `{"need_totp": true, "interim_token": <短 JWT 仅含 verify 权限>}`
  - login 同时 UPDATE 不 bump token_version（避免每登录 cache miss）
  - **change_password 顺手 bump_token_version**（顺便偿还 v0.4.5 安全债）
- F3.4 `knot/api/deps.py` `get_current_user` 改造：
  - **JWT 验证 +token_version 检查**：`payload.ver == get_token_version(user.id)` → 否则 401 JWT_REVOKED
  - 加 totp_verified 检查（除 `/api/totp/enroll-*` + `/api/totp/verify` 端点豁免）
  - **R-PB-B1-3 三层防御**：
    1. env `KNOT_TOTP_BYPASS_ADMIN=true` → 全跳（admin role）
    2. users 表 0 个 admin enroll + 启动期 24h 内 → admin 跳 + logger.warn
    3. ≥ 1 admin enroll 完成 → 上述两 fallback 自动失效

### F4 — Audit 扩展（commit 4）
- F4.1 `knot/models/audit.py` AuditAction Literal **+4**（33 → 37 — R-PB-B1-11 修订）：
  - `user.totp.enroll`
  - `user.totp.verify_failed`
  - `user.totp.reset`
  - **`user.totp.recovery_code_used`**（**新增 — Stage 2 关键审计**）
- F4.2 audit_service 集成 4 新 action — INSERT-only sustained
- F4.3 PII 防御：TOTP secret + recovery code 明文严禁记 audit

### F5 — 前端 UI（commit 5）
- F5.1 `frontend/src/screens/Login.jsx` 扩展：login 成功后 if need_totp → TOTP 6 位输入框
- F5.2 `frontend/src/screens/Enroll.jsx` [NEW ≤ 170 行 — v1 200 -30 行（**砍二次确认 UX**）]：
  - Step 1: 显示 QR code + secret 字面（用 inline SVG，不依赖 npm `qrcode.react`）
  - Step 2: **1 个 6 位动态码验证**（业界标准 — R-PB-B1-7 修订）
  - Step 3: 显示 10 个 recovery codes + **强制点击下载按钮**（R-PB-B1-7 修订 — 未下载"完成 enroll"按钮 disabled）
  - Step 4: 弹窗确认 "已妥善保存 recovery codes? [是 / 我需要重看]"
  - 确认 "是" → 持久化 totp_enabled=true + 完成
- F5.3 `frontend/src/screens/admin/tab_access.jsx` 用户行加"TOTP 重置"按钮
- F5.4 `frontend/src/api.js` 加 5 endpoint 调用（enrollInit / enrollComplete / verify / reset / status）+ 401 JWT_REVOKED 拦截器 → 自动跳 login
- F5.5 `frontend/src/App.jsx` Enroll 屏路由 + 强制 enroll 拦截

### F6 — 守护测试 + smoke（commit 6 — **守护者强制 active**）
- F6.1 `tests/test_totp_2fa.py` [NEW ≤ 350 行 — v1 300 +50 行新红线]：
  - enroll 成功 + recovery codes 10 个生成
  - **enroll 1 次码失败 → secret 未持久化**（R-PB-B1-7 修订）
  - **R-PB-B1-9 R-46-Tx**：mock recovery_codes INSERT 失败 → secret 事务回滚
  - **R-PB-B1-10 race 100× ± 5**（γ2 折中）：`asyncio.gather(*[verify(user_id, "wrong") for _ in range(100)])` → 命中 ≥ 95 (rate_limit 5/min/user) - **mark `@pytest.mark.race` CI 独立 job**
  - **R-PB-B1-12 valid_window=1**：mock 时间 t-30s / t / t+30s 必通过；t-60s / t+60s 必失败
  - **R-PB-B1-13 token_version**：reset TOTP → 旧 JWT 验证返 401 JWT_REVOKED
  - **R-PB-B1-13 change_password**：同上 — 偿还安全债
  - **R-PB-B1-13 race**：reset 并发 + 旧 JWT 验证 → 100% 失效（无真空期）
  - rate limit 5/min/user 第 6 次返 429
  - recovery code 单次使用 + audit_log INSERT
  - **NRP-2 测试隔离**：race 100× 时 mock 时间锁定单一时间步长（不跨 ±30 秒边界）
  - R-PB-B1-1/8 Fernet enc_v1: 前缀 sqlite3 直读验证
  - R-PB-B1-2 不锁死：enroll 中断 → totp_enrolled_at NULL → 可再次走 enroll
  - **R-PB-B1-3 三层防御**：mock env + 24h 宽限 + admin enroll 完成
- F6.2 `tests/test_rename_smoke.py` R-72 routes count +4（87 → 91 下限）
- F6.3 `tests/test_audit_actions.py` 验证 AuditAction Literal **+4**（37 类完整性）

### F7 — 文档 + 版本同步（commit 7）
- F7.1 **4 处版本同步 0.6.1.12 → 0.6.2.0**：
  - `knot/main.py` FastAPI version
  - `tests/test_rename_smoke.py` R-72 守护字面
  - `frontend/src/screens/Login.jsx` 页脚 R-181
  - `README.md` 顶部 + 技术栈段 R-KNOW-1
- F7.2 CLAUDE.md v0.6.x 路线图表加 v0.6.2.0 行
- F7.3 CHANGELOG.md 加 [Unreleased] - v0.6.2.0 段
- F7.4 README.md 提及 TOTP 2FA

---

## §2 文件改动清单（v2 修订）

### 新建（5 个文件，v1 sustained）
- `knot/services/totp_service.py` (≤ 220 行 — v1 200 +20)
- `knot/api/totp.py` (≤ 180 行 — v1 150 +30)
- `frontend/src/screens/Enroll.jsx` (≤ 170 行 — v1 200 -30 砍二次)
- `tests/test_totp_2fa.py` (≤ 350 行 — v1 300 +50)
- `tests/test_audit_actions.py` (≤ 80 行)

### 修改（v1 22 项 sustained + 5 项调整）
**新增改动**：
- `knot/repositories/user_repo.py` 加 `get_token_version` / `bump_token_version`（**γ1 新增 +30 行**）
- `knot/api/deps.py` JWT 验证 +token_version 检查（**γ1 修改 +15 行**）
- `knot/services/auth_service.py` `change_password` bump_token_version（**γ1 顺手偿还 +5 行**）
- `pyproject.toml` 加 cachetools（**γ1 +1 行**）

**估计总行数**：~1240 行（v1 1170 + Stage 2 整合 +70）

---

## §3 红线（R-PB-B1-1~13 + NRP-1/2 — Stage 4 LOCKED）

| 红线 | 内容 | 状态 |
|---|---|---|
| R-PB-B1-1 | TOTP secret Fernet 加密（仅 SQLite knot.db）| ✅ sustained |
| R-PB-B1-2 | enroll 失败/中断不锁死账号 | ✅ sustained |
| **R-PB-B1-3** | admin 三层防御（env + 24h 宽限 + admin enroll 自动失效）| ⚠️ 修订 |
| R-PB-B1-4 | 公测启动闸门 | ✅ sustained |
| R-PB-B1-5 | TOTP 重置流程 | ✅ sustained |
| R-PB-B1-6 | TOTP 验证 rate limit 5/min/user | ✅ sustained |
| **R-PB-B1-7** | enroll 1 次码 + 强制下载 recovery codes | ⚠️ 修订 |
| R-PB-B1-8 | Fernet 加密路径明示 | ✅ sustained |
| R-PB-B1-9 | R-46-Tx SQLite 事务 + 应用层 Session 验证 | ✅ sustained |
| **R-PB-B1-10** | race 100× ± 5 + @race CI 独立 job | ⚠️ 修订（γ2）|
| **R-PB-B1-11** | audit_action **+4**（含 recovery_code_used）| ⚠️ 修订 |
| **R-PB-B1-12** | TOTP valid_window=1 ±30 秒时钟漂移容忍 | 🆕 新增 |
| **R-PB-B1-13** | user_secret_version JWT 吊销真空期防御三层 | 🆕 新增（γ1）|
| **NRP-1** | JWT cache 用 cachetools TTLCache 不用 lru_cache | 🆕 子条款 |
| **NRP-2** | race 100× + valid_window=1 测试 mock 时间锁定 | 🆕 子条款 |

**v2 立约红线总计**：13 条 + 2 子条款（v1 11 条）

**通用红线 sustained**：R-PA-PB-V1 / R-PB2-1~18 / R-94 / R-100 / R-128 / R-158 / R-192 / R-286 / R-302.5 / R-65 / R-27 / **R-LP-v3-EX-2/2.1/3** / **R-PB-GOV-1**

---

## §4 commit 序列（v2 — 7 commit + 守护强 active 时机）

```
commit 1  F1 — pyproject (含 cachetools) + schema (含 token_version) + migration       (~0.5 天)

commit 2  F2 — totp_service + Fernet + R-46-Tx + Session + valid_window + JWT 吊销
          ⚠️ 守护者强制 active（γ3 — 核心安全模块）                                    (~2.5 天)

commit 3  F3 — API 4 端点 + rate_limit + auth login 改造 + R-PB-B1-3 三层 + deps.py     (~2.5 天)

commit 4  F4 — audit Literal +4                                                       (~0.5 天)

commit 5  F5 — 前端 Login + Enroll 改造（移除二次确认 + 强制下载）+ admin 重置        (~1.5 天)

commit 6  F6 — 守护测试（race 100×±5 + 时钟漂移 + JWT 吊销 + R-46-Tx 回滚 + Fernet）
          ⚠️ 守护者强制 active（γ3 — 红线 100% 覆盖核验）                              (~2.5 天)

commit 7  F7 — 文档 + 4 处版本同步 0.6.1.12 → 0.6.2.0                                 (~0.5 天)
```

**总指导性工期**：**~10.5 天 / 2.5-3.5 周**（R-PB-GOV-1 非硬承诺 sustained）

**commit 依赖**：1 → 2 → 3 → (4 // 5 可并行) → 6 → 7

---

## §5 验收标准（v2 — 含 Stage 2 整合新项）

### 5.1 功能验收
- [ ] 新用户 login → 拦截到 Enroll 屏 → QR 扫描 + **1 个 6 位码**（v2 修订）→ 持久化 + **强制下载 recovery codes**
- [ ] 已 enroll 用户 login → 返 interim_token → /api/totp/verify → 完整 JWT
- [ ] recovery code 单次使用 + **audit_log recovery_code_used INSERT**
- [ ] admin 重置 user TOTP → **该用户旧 JWT 立即 401 JWT_REVOKED** + audit + 重新 enroll
- [ ] **change_password 后旧 JWT 立即失效**（v2 顺手偿还）
- [ ] 月 ≥ 5 次重置 → admin 警报

### 5.2 红线守护测试（R-PB-B1-1~13 + NRP-1/2 全覆盖）
- [ ] **R-PB-B1-9 R-46-Tx**：mock INSERT 失败 → secret 未持久化（事务回滚）
- [ ] **R-PB-B1-10 race 100× ± 5**：误差 ≤ 5（**@pytest.mark.race CI 独立 job 跑** — γ2）
- [ ] **R-PB-B1-12 valid_window=1**：t-30s / t / t+30s 通过；t-60s / t+60s 失败
- [ ] **R-PB-B1-13 token_version**：reset → 旧 JWT 401；change_password 同上；race 100% 失效
- [ ] **R-PB-B1-7 1 次码**：单码验证通过 + recovery codes 必下载才能完成（业界标准）
- [ ] **R-PB-B1-3 三层防御**：env / 24h 宽限 / admin enroll 后自动失效（三场景 mock）
- [ ] **R-PB-B1-11 audit_action**：4 个新 action 全 INSERT（含 recovery_code_used）
- [ ] **NRP-1 cachetools**：reset 时 cache.pop(user_id)；其他 user cache 不受影响
- [ ] **NRP-2 测试隔离**：race 100× + valid_window=1 mock 时间锁定单步长（CI 稳定性）
- [ ] R-PB-B1-1/8 Fernet enc_v1: sqlite3 直读验证
- [ ] R-PB-B1-6 rate limit 5/min/user 第 6 次返 429

### 5.3 协议合规
- [ ] 7 contracts KEPT
- [ ] R-94 文件大小：totp_service.py ≤ 220 / api/totp.py ≤ 180 / Enroll.jsx ≤ 170
- [ ] R-PA-8 leakage check OK
- [ ] LIMITS dict 扩 5 新文件
- [ ] 4 处版本同步
- [ ] CI 全绿（含 @race 独立 job）

### 5.4 人测（demo 录屏）
- [ ] enroll 全流程：QR scan + 1 个 6 位码 + recovery codes 强制下载
- [ ] login 流程（已 enroll 用户）
- [ ] admin 重置 user TOTP → 用户被 logout（旧 JWT 失效）
- [ ] change_password → 用户被 logout（旧 JWT 失效）

---

## §6 协议合规（v3 三阶段 — 已完成）

| Stage | 状态 | 时机 |
|---|---|---|
| Stage 1 | ✅ 2026-05-28 | 执行者草案 |
| Stage 2 | ✅ 2026-05-28 | 独立第三方 AI 评审（4.2/5）|
| Stage 3 | ✅ 2026-05-28 | v0.5 守护者第 12 次 active 整合 |
| **Stage 4** | ✅ **2026-05-28 LOCKED** | 资深 9 项决策（γ1-γ3 + 6 项 accept） |

**守护者强制 active 节奏**（γ3 sustained）：
- commit 2 完成 → 守护者复核（核心安全模块）
- commit 6 完成 → 守护者复核（红线 100% 覆盖核验）
- commit 7 完成 + PR open → 守护者 Stage 3 终审（PATCH 全闸门）

**远古守护者**：本 PATCH 不激活；第 4 次预约 v0.6.3.0 B2 audit V2 启动前 sustained。

---

## §7 风险与依赖（v2 — Stage 2 整合）

| 风险 | 影响 | 缓解 |
|---|---|---|
| **JWT 吊销 cache 实现 bug** | reset 后旧 JWT 仍生效 | NRP-1 cachetools TTLCache + 守护测试 race 100% 失效场景 |
| **valid_window=1 跨边界 race** | 测试间歇性失败 | NRP-2 mock 时间锁定单步长 + race 测试 mark 独立 job |
| pyotp/qrcode/cachetools 三个新依赖 | 维护面扩大 | 业界主流 + 长期维护（pyotp 2014+ / qrcode 2011+ / cachetools 2014+）|
| admin 自锁 | 公测无法启动 | R-PB-B1-3 三层防御（env / 24h / admin enroll 自动失效）|
| change_password 改造影响现有用户 | 上线后批量 logout | 部署 docs 明示；可分批 rollout |
| 业务方 enroll 流程耐心 | 上线节奏 | 1 次码（不是 2 次）+ recovery codes 强制下载 UX 友好 |

### 撤回机制
- commit 2 守护者复核期间发现 R-PB-B1-9 / B1-13 红线冲突 → 撤回该红线 + 重定义 Stage 3
- Stage 4 LOCKED 后无 OVERRIDE — 任何范围修订须新走三阶段评审

---

## §8 自检清单（v2 — 22 项）

- [x] §0 v1 → v2 修订摘要（11 项 + 2 子条款）
- [x] §1 22 子项每项有落地 commit 标识 + Out-of-Scope 明示
- [x] §2 文件改动清单（v1 sustained + 4 项 γ1 新增）
- [x] §3 R-PB-B1-1~13 全 13 条 + NRP-1/2 + 通用红线 sustained
- [x] §4 commit 序列 7 commit + 守护强 active（commit 2/6）
- [x] §5 验收（功能 + 红线守护 + 协议合规 + 人测）
- [x] §6 协议三阶段 + Stage 4 LOCKED
- [x] §7 风险 6 项 + 撤回机制
- [x] **R-PB-B1-12 valid_window=1 协议级修复**（Stage 2 Q1）
- [x] **R-PB-B1-13 JWT 吊销机制全面纳入**（γ1 + Stage 2 关键独立贡献）
- [x] **R-PB-B1-10 race 100× ± 5 折中**（γ2）
- [x] **commit 2 + 6 守护者强制 active**（γ3）
- [x] **R-PB-B1-7 业界标准对齐 1 次码**（Stage 2 Q4）
- [x] **R-PB-B1-11 audit_action +4 含 recovery_code_used**（Stage 2 Q7）
- [x] **R-PB-B1-3 三层防御**（Stage 2 Q5）
- [x] **NRP-1 cachetools TTLCache**（守护者 §VII）
- [x] **NRP-2 race + valid_window 测试隔离**（守护者 §VII）
- [x] R-PB-GOV-1 工期指导性 2.5-3.5 周 sustained
- [x] KNOT 元数据 (SQLite) vs 业务库 (Doris) 架构 sustained
- [x] 撤回 v1 二次确认 + LRU 缓存提议
- [x] change_password 顺手偿还安全债
- [x] 4 处版本同步（main.py + smoke + Login.jsx + README）

---

## §9 commit 1 启动闸门（Stage 4 LOCKED 后）

资深 announce 启动 commit 1 时（R-PB-GOV-1 按节奏）：

1. **F1.1** `pyproject.toml` 加 `pyotp` + `qrcode[pil]` + **`cachetools`**
2. **F1.2** `requirements.txt` 同步
3. **F1.3** `users` 表 ALTER 4 字段（含 **`token_version`**）
4. **F1.4** `totp_recovery_codes` 表创建
5. **F1.5** `repositories/base.py` startup 幂等 migration
6. CHANGELOG 加 commit 1 entry
7. commit `feat(v0.6.2.0)-commit-1` push + PR open

---

**附录 A**：v1 → v2 演进归档
- v1 `docs/plans/v0.6.2.0-totp-2fa.md` — Stage 1 执行者草案
- v2 `docs/plans/v0.6.2.0-totp-2fa-v2.md`（本文档）— **Stage 4 LOCKED**
- Stage 2 context pack `docs/plans/v0.6.2.0-totp-2fa-stage2-context-pack.md`

**附录 B**：关联 Task
- Task #3 v0.6.2.0 TOTP 2FA（in_progress — Stage 4 LOCKED 后 commit 1 启动）
- Task #16 代码结构评估 §1（已交付，本 PATCH 不影响）

**附录 C**：上游 LOCKED 路线图
- `docs/plans/v0.6.2.0-phase-b-roadmap-v4.md` §1 段 1 B1 + §3.2 红线
- `docs/governance/override-cumulative-log.md` — 治理 source-of-truth（本 PATCH 不属 OVERRIDE）
- `CLAUDE.md` § Loop Protocol v3 — 三阶段评审纪律
