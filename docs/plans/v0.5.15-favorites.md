# v0.5.15 — (C5+) Favorites 屏复刻（SavedReports）— v0.5.x 首个新顶层屏复刻 — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 16 次施行**）
> **里程碑**：v0.5.x 首个新顶层屏复刻（v0.5.14 ResultBlock 收官后）；**R-372 brandSoft inset 8% 全站设计语言闭环**

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**Stage 2 D6 强化契约 + Q4 严禁 white 字面 + Q5 frozen 硬编**。

**Stage 2 新增 2 条**：
- **R-369**：Sidebar 布局精度（bookmark icon 14×14 + gap 10）
- **R-370**：Meta Separator `│` U+2502 字符精度

**Stage 3 新增 3 条**：
- **R-371**：4 按钮 hover/disabled 状态机
- **R-372**：Quote inset bg 与 v0.5.14 R-323 brandSoft 8% **字面 byte-equal**（全站设计语言闭环）
- **R-373**：SavedItem time "YYYY.MM.DD" mono 9px textFaint

红线总数 29（**R-345~R-373**）。子步骤增至 **9 步**。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** INTENT_SVG 7 path | ✅ A 7 path | Stage 2 架构判定：保业务分类信息密度 |
| **D2** 4 按钮 emoji→svg | ✅ A inline svg | Stage 1 |
| **D3** Banner emoji 保留 | ✅ A R-302.5 sustained | Stage 1 |
| **D4** Sidebar header 删 📌 | ✅ A | Stage 1 |
| **D5** Sidebar SavedItem bookmark + brandSoft | ✅ A | Stage 1 |
| **D6** Topbar breadcrumb 不动 | ✅ **绝对同意**；视觉补偿在 Title block 加倍补 | Stage 2 强调 Shell 13 props 契约 |
| **D7** Title block 22px + meta mono · separator + StatusDot frozen | ✅ A | Stage 1 + Stage 2 D6 视觉补偿 |
| **D8** R-286 hex 全清扩展 | ✅ A 5 处全清 | Stage 1 |
| **Q1** INTENT_SVG 策略 | ✅ A 7 path（保业务上下文） | Stage 2 |
| **Q2** Sidebar header 字体 | ✅ A 删 📌 改 T.mono | Stage 1 |
| **Q3** LIMIT dict 验证 | ⚠️ 起手前 grep 标定 — **前置已确认 SavedReports 未在 dict** → 新增 380 | Stage 1 |
| **Q4** pillBtn `'#fff'` 偿还 | ✅ **A T.sendFg**（严禁 'white' 字面） | Stage 2 强调暗黑模式对比度 |
| **Q5** StatusDot frozen | ✅ **A 视觉装饰硬编码** | Stage 2 架构判定：收藏 = 历史快照 = 冻结视觉 |

---

## 2. 红线最终清单（29 条 R-345~R-373）

### Stage 1 草案原 24 条（保留）

| ID | 摘要 |
|---|---|
| **R-345** | SavedReportsScreen 5 props 签名 byte-equal |
| **R-346** | EmptyView/DetailView/safeParseRows/pillBtn 4 helpers byte-equal |
| **R-347** | loadReports/handleDelete/handleRun/handleSaveEdit/handleExport 5 handlers byte-equal |
| **R-348** | INTENT_EMOJI 字典名 + 7 keys 字面 byte-equal（仅 value 偿还为 svg path） |
| **R-349** | AppShell 调用 props byte-equal（R-192 sustained） |
| **R-350** | api 5 调用 + endpoint URL 字面 byte-equal |
| **R-351** | INTENT_EMOJI 7 处 → INTENT_SVG dict + SvgPath（D1） |
| **R-352** | 4 按钮 emoji → svg path（D2） |
| **R-353** | Sidebar header 删 📌 + T.mono（D4） |
| **R-354** | Sidebar SavedItem bookmark svg + brandSoft + 删 borderLeft + time mono（D5） |
| **R-355** | Title block 22px + meta mono · separator + StatusDot frozen（D7） |
| **R-356** | Original question quote inset 风格 |
| **R-357** | Table thead 删 uppercase + letter-spacing normal（v0.5.14 R-327 模式） |
| **R-358** | R-286 hex 0 命中（v0.5.13 R-298 sustained） |
| **R-359** | hex+alpha → color-mix in oklch |
| **R-360** | Warning banner #FF990022/#FF9900/#cc6600 → T.warn + color-mix |
| **R-361** | pillBtn `'#fff'` → T.sendFg（严禁 'white' 字面） |
| **R-362** | banner ⚠️/🔍/❌ 保留（R-302.5 sustained） |
| **R-363** | SavedReports.jsx ≤ 380 行 |
| **R-364** | backend 432 tests / 7 contracts / 72 routes |
| **R-365** | 范围守护（App/api/index.css/main/utils/Shared/Shell/decor/16 屏/chat 子模块 byte-equal） |
| **R-366** | 三处版本同步 0.5.14→0.5.15 |
| **R-367** | KnotLogo R-199.5/222 sustained — 不蔓延 SavedReports |
| **R-368** | CSS 0 污染 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-369** | **Sidebar 布局精度** — SavedItem bookmark icon 14×14 + 与文本 gap 10px | inline `gap: 10` + svg `width={14} height={14}`；commit 1 后 dev tools 实测 |
| **R-370** | **Meta Separator 字符精度** — 必须 `│` (U+2502) + textFaint 色；非普通 `\|` 字符 | meta 行 separator inline `<span style={{ color: T.textFaint, fontFamily: T.mono }}>│</span>`；commit 1 后 grep U+2502 确认 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-371** | **4 按钮 SVG 交互反馈** — Hover 状态平滑过渡 T.accent；disabled/loading 走 pillBtn helper 状态机 | inline style `transition: 'color 200ms'`；React state `isHover`；commit 1 后手测编辑/重跑按钮状态机 |
| **R-372** | **Quote inset 全站设计语言闭环** — bgInset 与 v0.5.14 R-323 ResultBlock Insight 完全 byte-equal | `background: \`color-mix(in oklch, ${T.accent} 8%, transparent)\`` 字面与 ResultBlock.jsx 同；未来全站 brandSoft inset 沿用 |
| **R-373** | **SavedItem Time Mono 格式化** — "YYYY.MM.DD" + T.textFaint + 9px | inline `fontSize: 9, color: T.textFaint, fontFamily: T.mono`；format helper `${y}.${m}.${d}` |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死）

> 先 5 处 Hex 全清 (R-358) → 建 INTENT_SVG 字典 → 重构 Sidebar SavedItem

| Step | 动作 | 红线 |
|---|---|---|
| 1 | 起手前 grep `SavedReports.jsx` scripts/check_file_sizes.py 标定 LIMIT dict 现状 | Q3 |
| 2 | 5 处 hex 全清 — pillBtn '#fff' → T.sendFg；Warning #FF990022/#FF9900/#cc6600 → T.warn + color-mix；Error ${T.accent}30 → color-mix | R-358/359/360/361/Q4 |
| 3 | `const INTENT_SVG = {...}` 7 path + `const BTN_SVG = {...}` 4 path 字典 | R-351/352 |
| 4 | Sidebar header 删 📌 + T.mono "收藏报表 N" | R-353/D4 |
| 5 | Sidebar SavedItem 重构 — bookmark svg 14×14 + gap 10 + brandSoft + 删 borderLeft + time mono 9px "YYYY.MM.DD" | R-354/369/373 |
| 6 | Title block 22px + meta `│` U+2502 + StatusDot frozen | R-355/370/Q5 |
| 7 | Original question quote inset — borderLeft 3px brandSoftBorder + bg `color-mix(in oklch, T.accent 8%, transparent)`（**与 R-323 字面闭环**） + "原始问题" mono uppercase | R-356/372 |
| 8 | Table thead 删 uppercase + letter-spacing normal | R-357 |
| 9 | 4 按钮 hover/disabled 状态机 + pillBtn helper 保持 | R-371 |
| 收尾 | grep 守护 + 端到端业务回归 + light/dark 双模式校准 + npm build | 见 §5 |

### Stage 3 §3 验收关键

```bash
# 双模式视觉校准
# light mode: Title block 22px + meta mono · separator + StatusDot frozen
# dark mode: 切换后同样验证
# 重点：Title block "极简感"（D6 视觉补偿到位）

# R-358 hex 0
grep -nE "#[0-9a-fA-F]{3,6}" SavedReports.jsx | grep -v boxShadow
# 预期 0

# Q4 严禁 white
grep -iE "['\"](white|#fff|#FFFFFF)['\"]" SavedReports.jsx
# 预期 0

# R-370 U+2502
grep -P "│" SavedReports.jsx
# 预期 ≥ 1

# R-372 闭环
diff <(grep "color-mix(in oklch.*T\.accent.*8%" ResultBlock.jsx) \
     <(grep "color-mix(in oklch.*T\.accent.*8%" SavedReports.jsx)
# 字面必须一致
```

### Stage 2 D6 视觉补偿纪律

Topbar topbarTitle 仍传简单 string（R-192 Shell 13 props 契约 sustained）；视觉补偿全部在 **Title block (R-355)**：
- 22px fontWeight 600
- meta 行 mono `│` U+2502 separator + StatusDot frozen
- "极简感"作为 D6 不动 Shell 的代价

### Stage 2 Q4 严禁 'white' 字面

```jsx
// ✅ 正确
color: T.sendFg
// ❌ 严禁
color: 'white'
color: '#fff'
color: '#FFFFFF'
```

### Stage 2 Q5 StatusDot frozen 硬编码

```jsx
<span style={{
  display: 'inline-flex', alignItems: 'center', gap: 4,
  fontSize: 11, fontFamily: T.mono, color: T.muted,
}}>
  <span style={{ width: 6, height: 6, borderRadius: '50%', background: T.muted }}/>
  frozen
</span>
```

未来动态状态留 v0.6+。

### Stage 3 R-372 设计语言闭环

```jsx
// ResultBlock.jsx v0.5.14 R-323
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
// SavedReports.jsx v0.5.15 R-372（必须 byte-equal）
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
```

### Stage 3 R-373 time 极简

```jsx
const formatTime = (iso) => {
  const d = new Date(iso);
  return `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`;
};
```

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/SavedReports.jsx       [EDIT 318 → ≤ 380 行]
                                            Step 1: 起手 grep check_file_sizes 标定 Q3
                                            Step 2: 5 处 hex 全清 [R-358/359/360/361/Q4]
                                            Step 3: INTENT_SVG + BTN_SVG dict [R-351/352]
                                            Step 4: Sidebar header 删 📌 + T.mono [R-353/D4]
                                            Step 5: Sidebar SavedItem bookmark 14×14 + gap 10 + brandSoft + time 9px [R-354/369/373]
                                            Step 6: Title block 22px + meta │ U+2502 + StatusDot frozen [R-355/370/Q5]
                                            Step 7: Quote inset color-mix in oklch 8% [R-356/372 闭环 R-323]
                                            Step 8: Table thead 删 uppercase + letter-spacing normal [R-357]
                                            Step 9: 4 按钮 hover/disabled 状态机 [R-371]
                                            守护: 5 props 签名 + 4 helpers + 5 handlers + INTENT_EMOJI 7 keys
                                                  + AppShell props + api 5 调用 byte-equal
                                                  + R-362 banner emoji 保留
                                                  + inline styles only [R-368]

scripts/check_file_sizes.py                 [EDIT LIMITS]
                                            前置 grep 已验证 SavedReports 未在 dict → 新增 380 (30→31 条)

knot/main.py                                [EDIT version 0.5.14 → 0.5.15]  [R-366]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.14 → 0.5.15]  [R-366]
frontend/src/screens/Login.jsx              [EDIT 页脚字面 0.5.14 → 0.5.15]  [R-366]

frontend/src/screens/chat/*                 [unchanged byte-equal]   [R-365]
frontend/src/screens/ (其余 16 屏)           [unchanged]              [R-365]
frontend/src/App.jsx                        [unchanged]              [R-365]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-365]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]              [R-365]
frontend/src/App.css                        [unchanged 0 行 diff]    [R-368]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                    [unchanged]
backend (knot/) 任何文件除 main.py          [unchanged]              [R-364]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.15 段]
                                            R-345~R-373 偿还表
                                            + R-372 全站 brandSoft inset 8% 设计语言闭环（与 v0.5.14 R-323）
                                            + Loop Protocol v3 第 16 次施行
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.15
                                            - v0.5.x 路线图 v0.5.15 ⏳ → ✅
docs/plans/v0.5.15-favorites.md             [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（SavedReports 380）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.15**

### Stage 2/3 强化验证

- [ ] **R-358 hex 0 命中**：`grep -nE "#[0-9a-fA-F]{3,6}" SavedReports.jsx | grep -v boxShadow` = 0
- [ ] **Q4 严禁 'white'**：`grep -iE "['\"](white|#fff|#FFFFFF)['\"]" SavedReports.jsx` = 0；`grep "T\.sendFg"` ≥ 1
- [ ] **R-372 brandSoft 闭环**：`color-mix(in oklch, ${T.accent} 8%, transparent)` 字面与 ResultBlock byte-equal
- [ ] **R-362 emoji 业务保留**：banner ⚠️/🔍/❌ 仍在文件
- [ ] **R-369 Sidebar 精度**：grep `gap: 10` + svg `width={14}` 命中
- [ ] **R-370 Meta U+2502**：`grep -P "│"` SavedReports.jsx ≥ 1
- [ ] **R-371 按钮 hover**：`transition.*color\|isHover` 命中
- [ ] **R-373 time mono 9px**：`fontSize: 9` + `T.textFaint` 命中
- [ ] **Q5 StatusDot frozen**：grep `frozen` 命中

### 契约守护

- [ ] **R-345**：SavedReportsScreen 5 props 签名 byte-equal
- [ ] **R-346**：EmptyView/DetailView/safeParseRows/pillBtn 业务 byte-equal
- [ ] **R-347**：5 handlers 调用方式 byte-equal
- [ ] **R-348**：INTENT_EMOJI 字典名 + 7 keys 字面 byte-equal
- [ ] **R-349**：AppShell 调用 props byte-equal（R-192 sustained）
- [ ] **R-350**：api 5 endpoint URL 字面 byte-equal

### 范围守护

- [ ] **R-365**：App/api/index.css/main/utils/Shared/Shell/decor/16 屏/chat 子模块 `git diff` = 0
- [ ] **R-367**：KnotLogo 仍仅 3 文件
- [ ] **R-368**：App.css `git diff` = 0；0 新 className

### 端到端

- [ ] 进 SavedReports → loading → reports
- [ ] 点击 SavedItem 切换 active
- [ ] 编辑保存 title/note → API put
- [ ] 重跑 → API post
- [ ] CSV/xlsx 导出（R-371 状态机）
- [ ] 删除确认 + 列表更新
- [ ] EmptyView 空状态
- [ ] **light + dark 双模式 Title block 极简感**（Stage 3 §3）
- [ ] **R-372 闭环视觉**
- [ ] v0.5.0~v0.5.14 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.15): SavedReports.jsx 视觉重构 — INTENT_SVG 7 path + Sidebar bookmark + Title 极简 + hex 全清 + brandSoft inset 闭环 (R-345~R-373)` | npm build + 端到端 + R-358 hex 0 + R-365 范围 + R-372 闭环 + light/dark |
| 2 | `chore(v0.5.15): version bump 0.5.14→0.5.15 三处同步 + SavedReports LIMIT 新增 380 (R-363/366)` | 全闸门 + version=0.5.15 + 30→31 条 |
| 3 | `docs(v0.5.15): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + brandSoft 8% 闭环存档` | 全闸门 |

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 16 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 29（R-345~R-373）
- ✅ **首个新顶层屏复刻**（v0.5.14 ResultBlock 收官后）
- ✅ **R-372 brandSoft inset 8% 全站闭环** — 与 v0.5.14 R-323 字面 byte-equal
- ✅ D6 Shell 13 props 契约严守 — 视觉补偿在 Title block
- ✅ R-302.5 emoji 业务豁免 sustained
- ✅ R-227.5.1 装饰豁免延伸（INTENT_SVG / "原始问题" mono uppercase）
- ✅ R-286 hex 0 命中扩展（v0.5.13/14/15 三 PATCH 三处守护）
- ✅ 范围最小化 — 16 屏 + chat 7 子模块 + 8 核心非屏 + App.css byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.15-favorites`
2. ✅ Q3 LIMIT dict 起手验证 — **前置已确认 SavedReports 未在 dict**，新增 380（30→31 条）
3. ✅ Step 1→9 顺序锁死 — 先 hex 清再字典再 sidebar 再 title block
4. ✅ Q4 严禁 'white' 字面 — pillBtn 偿还只能 T.sendFg
5. ✅ R-372 闭环 byte-equal — color-mix 8% 与 ResultBlock R-323 一致
6. ✅ R-370 U+2502 字符精度 — `│` 非普通 `|`
7. ✅ R-373 time 极简 — "YYYY.MM.DD" 9px T.textFaint mono
8. ✅ Stage 3 §3 双模式校准 — light/dark Title block 极简感
9. ✅ D6 视觉补偿到位 — Title block 22px + meta · + StatusDot frozen
10. ✅ Q5 frozen 装饰严守 — 硬编不引入业务字段
11. ✅ R-371 按钮状态机 — useState isHover + pillBtn disabled/loading
12. ✅ R-365 范围抗诱惑 — 不顺手改 chat/ Shell topbar / api helper
13. ✅ 设计先例叙事 — CHANGELOG 标注"首个新顶层屏复刻 + brandSoft inset 8% 全站闭环"

---

🛡️ **执行者收到 GO 信号；v0.5.x 首个新顶层屏复刻正式启动；R-372 brandSoft inset 8% 全站设计语言闭环 + D6 Shell 13 props 契约严守 + Stage 3 §3 双模式极简感校验三大重点协同落地。**
