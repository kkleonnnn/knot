# v0.5.1 — SQL AST 预校验（笛卡尔积硬防御）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

5 项决策（D1-D5）**与执行者 Stage 1 提议完全一致**，无修订。
新增 5 条红线（R-89~R-93），分别来自 Stage 2（2 条）+ Stage 3（3 条）。
3 条隐性指令落地：性能（仅 final_answer 调用）/ TDD 优先（CTE + 恒真 ON）/ 锁定（本手册即归档版）。

## 1. 决议表最终裁定（D1-D5 锁定）

| ID | 锁定结果 | 依据 |
|---|---|---|
| **D1** | ✅ **A 独立 `knot/services/sql_validator.py`** | Stage 2 — 为 v0.5.2 sql_planner 瘦身预留解耦 |
| **D2** | ✅ **B 推迟（无前缀列检测）** | Stage 2 — 无 Schema Cache 误杀风险高 |
| **D3** | ✅ **A Fail-open** | Stage 2 — 安全 guardrail 已在 doris.py 闭环；本验证器是质量守护 |
| **D4** | ✅ **A 旧式逗号 Join 一律拒** | Stage 2 — 维护 prompt/runtime 语义一致性 |
| **D5** | ✅ **B fan-out regex 不动** | Stage 2 — 单 PATCH 单核心问题原则 |

## 2. 红线最终清单（14 条 R-80 ~ R-93）

### Stage 1 草案原 9 条（保留不动）

| ID | 内容 | 验证 |
|---|---|---|
| **R-80** | sqlglot 缺包/解析失败 → fail-open 不阻断 SQL（与 doris.py 安全 guardrail 反向；本验证器是质量守护，非安全 guardrail），但落 logger.warning 留观测线 | 单元测试 mock import 失败 + parse 异常 |
| **R-81** | 笛卡尔积 4 类（C1~C4 旧式逗号 / CROSS JOIN / 缺 ON / 恒真 ON）一律返 `(True, reason)` 拒收；reason 必须含可定位的表名/字段名（便于 ReAct LLM 修正） | 13 条单元测试覆盖 |
| **R-82** | 反馈消息走 `__REJECT_CARTESIAN__:` 前缀（与 `__REJECT_FAN_OUT__:` 同协议）；sync + async 两条 ReAct 循环都加处理；observation 反馈给 LLM 后**不 break**（让 LLM 重生成） | 集成测试 mock LLM 模拟先错后对 |
| **R-83** | CTE / 子查询内部递归检测（sqlglot 树 walk 自带）；不能让用户用 WITH/子查询绕过检测 | 单元测试 +2 case（D-2 优先） |
| **R-84** | 核心代码总量 ≤ 200 行硬约束：validator ≤ 140 行 + sql_planner 接入 ≤ 60 行；超线必须拆功能或推延而不是放过 | 复审 git diff `--stat` |
| **R-85** | 不破坏 v0.4.1.1 `_is_fan_out` 检测：fan-out 测试套（`test_sql_planner_fan_out.py`）保持 100% 绿；fan-out 与 cartesian 优先级互不干涉（同时触发时 cartesian 优先返，理由：cartesian 是更基础错误，fan-out 是细分聚合错误） | 现有 fan-out 测试 + 联合 case 1 条 |
| **R-86** | 不引入新依赖：sqlglot 已是 `requirements.txt` 既有依赖（doris.py 在用）；本 PATCH 仅在 `sql_validator.py` 顶部 `import sqlglot`，import 失败遵 R-80 | `requirements.txt` 不动 |
| **R-87** | 7 contracts 全部 KEPT（不动 `.importlinter`）；`sql_validator.py` 落在 `services/` 层 — 上游可被 `services/agents/sql_planner.py` 调用，不反向 | `lint-imports` 0 broken |
| **R-88** | 测试增量 ≥ 13（v0.5.0 baseline 375 → ≥ 388）：单元 ≥ 13 + 集成 ≥ 5 - 重叠去重 ≈ +13；live LLM eval 反例 case `edge_anti_cartesian_product`（v0.4.1.1 已加）保留并新增 AST 兜底断言 | `pytest tests/ -v` 388+ passed |

### Stage 2 新增 2 条（资深架构师 + Codex）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-89** | **递归深度限制** — `is_cartesian` 入口预检 SQL 长度 / 括号深度，防恶意超深嵌套栈溢出 | sql_validator.py 顶部加 `_MAX_SQL_LEN = 100_000` + `_MAX_PAREN_DEPTH = 100`，超限直接 fail-open + warning |
| **R-90** | **Validator 纯函数禁连接 DB** — 严禁 `db.execute` / `EXPLAIN` 调用 | sql_validator.py 不 import `adapters.db` / `repositories`；contract 隐含守护（services 层不直查 DB 已是常识但显式锁死） |

### Stage 3 新增 3 条（v0.4 守护者）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-91** | **ReAct 无限循环保护** — 连续 3 次 `__REJECT_CARTESIAN__` 强制中断 | sql_planner.py 加 `_cartesian_reject_count` 计数器（与 `max_iterations` **共享**而非独立预算）；≥ 3 次 raise / final answer 报错给前端 |
| **R-92** | **建设性 reason** — 必须明确"表 A 与表 B 缺乏关联条件" | `_format_reject_reason` 必须输出形如 `"Cartesian product: tables [users, orders] joined without ON/USING condition"`；恒真 ON case 输出 `"Tautological ON expression: 1=1"`；C1 case 输出 `"Implicit comma-join: tables [a, b] in FROM without explicit JOIN"` |
| **R-93** | **v0.4.5 加密列兼容** — sqlglot 不能误判 `enc_v1:` 前缀 / 加密相关函数 | 加 1 条单元测试：含 `WHERE api_key = 'enc_v1:xxxxxx'` 的合法 SQL 必须返 `(False, ...)`；如发现 sqlglot 真有问题，validator 仅做检测**不**触碰加密字段值 |

## 3. Stage 2/3 三条隐性指令（落地手段）

### 3.1 性能提醒（Stage 3 末段）

> ⚠️ **`sqlglot` 是重型库** — `is_cartesian` 仅在 `_run_tool` **`final_answer` 分支前夕**执行一次，**严禁**在 ReAct 中间思考步骤（`generate_sql` / `validate_sql` 等中间 tool）调用。

**落地**：sql_planner.py 接入点**只在 `final_answer` 分支调用**（与 `_is_fan_out` 同位置同模式）— Stage 1 §5 文件级改动清单已明确，写入 commit message 提醒。

### 3.2 D-2 测试骨架优先（Stage 3 末段）

> 重点关注 **CTE 递归检测（R-83）** 与 **恒真 ON 检测（C4）**

**落地**：`tests/services/test_sql_validator.py` 测试用例顺序调整 — CTE/子查询 case 与 C4 恒真 ON case **优先编写**（D-2 阶段 TDD red 状态先建立这两组），其余 C1/C2/C3 后续补齐。

### 3.3 锁定执行手册（Stage 3 操作指令 §1）

> 整合 R-80 至 R-93

**落地**：本文档即整合后的 LOCKED 版（DRAFT 已覆盖），Stage 4 commit 4 时归档。

## 4. 文件级改动清单（最终版）

```
knot/
└── services/
    ├── sql_validator.py                 [NEW] ≤ 140 行（R-84）
    │   ├── _MAX_SQL_LEN = 100_000       [R-89]
    │   ├── _MAX_PAREN_DEPTH = 100       [R-89]
    │   ├── def is_cartesian(sql) -> tuple[bool, str]   [入口；R-89 预检 + R-90 纯函数]
    │   ├── def _has_cross_join(node) -> bool
    │   ├── def _has_implicit_comma_join(node) -> bool
    │   ├── def _has_join_without_on(node) -> bool
    │   ├── def _has_tautological_on(on_expr) -> bool
    │   └── def _format_reject_reason(kind, tables, expr) -> str   [R-92 建设性]
    └── agents/
        └── sql_planner.py               [EDIT] +约 60 行（R-84）
            ├── final_answer 分支调 sql_validator.is_cartesian      [性能：仅此一处]
            ├── 返 __REJECT_CARTESIAN__:<reason>                     [R-82]
            ├── _cartesian_reject_count 计数（共享 max_iterations）  [R-91]
            └── 连续 3 次拒收 → 强制终止报错                          [R-91]

tests/
└── services/
    ├── test_sql_validator.py            [NEW] 单元 ≥ 13 case
    │   ├── R-83 CTE 内笛卡尔积                          [D-2 优先]
    │   ├── R-83 子查询内笛卡尔积                        [D-2 优先]
    │   ├── C4 恒真 ON × 3                              [D-2 优先]
    │   ├── 正例 4 + C1×2 + C2×1 + C3×2
    │   ├── R-89 超长 SQL / 超深嵌套 → fail-open + warning
    │   ├── R-80 sqlglot import 失败 → fail-open
    │   └── R-93 含 enc_v1: 加密字段合法 SQL → (False, ...)
    └── test_sql_planner_cartesian.py    [NEW] 集成 ≥ 5 case
        ├── final_answer 笛卡尔积 → __REJECT_CARTESIAN__ 反馈
        ├── ReAct 重生成成功（mock LLM 第二轮合法）
        ├── R-85 fan-out + cartesian 联合 → cartesian 优先
        ├── R-80 sqlglot 失败 → fail-open（业务不阻断）
        └── R-91 连续 3 次拒收 → 强制终止 + 错误返回

CHANGELOG.md           [EDIT] [Unreleased] v0.5.1 段（R-80~R-93 全条偿还表）
CLAUDE.md              [EDIT] v0.5.x 路线图 v0.5.1 ⏳ → ✅
pyproject.toml         [EDIT] version 0.5.0 → 0.5.1（如有 hardcode）
knot/main.py           [EDIT] FastAPI version="0.5.1"
docs/plans/v0.5.1-sql-ast-validator.md   [本锁定手册]
```

## 5. 验收清单（Done Definition）

### 5.1 必须全绿

- [ ] `pytest tests/ -v` ≥ **388 passed**（v0.5.0 baseline 375 + 单元 ≥ 13 + 集成 ≥ 5 - 重叠）
- [ ] `lint-imports` **7 contracts KEPT**, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72 routes, 0.5.1**
- [ ] `git diff --stat` 核心改动 ≤ 200 行（R-84）
- [ ] `npm run build` 通过

### 5.2 端到端手工验证

- [ ] 旧式逗号 join → 拦截 + ReAct 重生成 JOIN ON
- [ ] CROSS JOIN → 拦截 + 重生成
- [ ] live LLM eval `edge_anti_cartesian_product` 跑过，AST 兜底确认拒
- [ ] v0.4.1.1 fan-out 反例继续被拒（R-85）
- [ ] **R-91 — 连续 3 次拒收强制终止**（mock 不收敛 LLM）
- [ ] **R-93 — `WHERE api_key = 'enc_v1:xxxx'` 合法 SQL 不被误判**

### 5.3 不破坏

- [ ] 7 contracts KEPT；core/ adapters/ 不动；routes=72 不变
- [ ] v0.5.0 R-67~R-79 测试仍绿
- [ ] requirements.txt 不动（R-86）
- [ ] **R-90 — sql_validator.py 不 import `adapters.db` / `repositories`**

## 6. Commit 序列（最终锁定 — 4 commit）

1. `feat(v0.5.1): SQL AST validator — sql_validator.py (R-80/R-81/R-83/R-89/R-90/R-92/R-93)`
   含单元测试 ≥ 13 case；D-2 阶段 TDD red → green
2. `feat(v0.5.1): sql_planner ReAct 接入 __REJECT_CARTESIAN__ + 计数器 (R-82/R-85/R-91)`
   含集成测试 ≥ 5 case；sync + async 双路径
3. `chore(v0.5.1): version bump 0.5.0 → 0.5.1`
4. `docs(v0.5.1): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED 归档`

每 commit 独立闸门：pytest + lint-imports + ruff 全过。

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（Stage 1 执行者 + Stage 2 资深 + Codex + Stage 3 v0.4 守护者）
- ✅ 守护者全程**只读**未触碰代码（合规）
- ✅ 远古守护者（v0.3）**dormant**未激活（PATCH 内常规流程）
- ✅ 红线总数 14（v0.5.1 区段 R-80~R-93）

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-09）。执行者按本手册实施。
