# v0.6.4.3 — UI v2 home/ChatEmpty 屏复刻（逐屏复刻第 3 屏）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP（artboard 方向 + 本地细节）+ VRP grep-guard + **五处版本同步**（守护者 v0.6.4.2 教训：显式核 README ★ + Shell L43）
> **基座**：worktree `v0643-home` @ branch `v0.6.4.3-home`（off main `ecba8d1`）
> **源稿**：`knot_demo_ui/v0.6/artboards/home.jsx`（157 行 artboard）
> **目标**：`frontend/src/screens/chat/ChatEmpty.jsx`（92 行，LIMIT 100）

## §0 范围（窄 — 轻量视觉复刻，0 业务 / 0 契约改动）

仅 `ChatEmpty.jsx` 一文件。**诚实定级：本屏 v2 deltas 小** — greeting block（"knot · ready" + h1 + 副标题）现已与 demo 近乎一致（v0.5.10 复刻自同源 demo）；真实变化集中在 suggestion 区 + icon 偿还 + 间距节奏。**Composer v2 styling（radius14 + 双层 boxShadow）留独立 PATCH v0.6.4.4**（沿 v0.5.11 先例；Composer.jsx 是 ChatEmpty + Conversation 共享组件，本 PATCH `<Composer>` 调用 byte-equal = R-217 式 hold）。

## §1 视觉变化（artboard 方向 → 本地 VRP 锚定）

### ① suggestion chips 布局：flex-wrap → grid
- `flexWrap:wrap + flex:'1 1 280px'` → `display:grid` + `gridTemplateColumns:'repeat(auto-fit, minmax(240px, 1fr))'`（artboard 方向 = 2-col grid；本地 auto-fit 保 fluid 窄屏自动 1-col — v0.5.16 R-378/394 既有 VRP pattern，胜 demo 写死 `1fr 1fr`）
- chip 文字色 `T.subtext` → `T.text`（demo 方向，提升可读）；bg `T.content` + border + radius 10 保不变

### ② icon Foundation 偿还（v0.6.4.0 已加 I.spark/I.flow）
- 删 inline `SparkIcon`/`FlowIcon`（v0.5.30 Q2 VRP 例外，立时 Shared 缺）→ `I.spark`/`I.flow`（Foundation；-2 inline def 给行 headroom）
- suggestion 数组 `{ Icon: SparkIcon }` → `{ icon: <I.spark/> }` 渲染调整（其余 I.users/I.db 保）

### ③ greeting 间距节奏（demo gap-based）
- greeting 容器加 `gap: 8`（demo L59）；label `marginBottom 18→4`；副标题 `margin '12px 0 28px'` → `'4px 0 0'`（gap 接管间距）；Composer 前留白由容器/wrapper marginTop 处理（不破 `<Composer>` 调用）

### 保 byte-equal（本地内容/业务铁律）
- **业务 suggestion 文案保留**（合约交易总量 / 平台总盈亏 / 新用户注册 / 查看表 = KNOT 真实域；非 demo GMV/订单 fiction）
- "knot · ready" 装饰小写 + color-mix dot ring（R-227.5 + R-229 严禁 hex alpha；demo `${t.brand}22` 不照搬）
- h1 "Hi {firstName}，今天想<解>哪个结？" + "解" brand span（KNOT 解结双关）
- 副标题文案 + "KNOT 可能出错 · 关键结果请核对原始数据" 大写（R-227.5 声明大写；demo 小写不照搬）
- v0.6.1 R-PA-PB-1 窄场景 hint（"当前支持 SELECT 类查询…"）byte-equal
- firstName 三态兜底逻辑 byte-equal
- `<Composer>` 调用 + ChatEmpty 10 props 签名 byte-equal

## §2 红线

| # | 红线 |
|---|---|
| R-UI2-H-1 | ChatEmpty 10 props 签名 byte-equal（`{ T, user, question, setQuestion, loading, onSubmit, onKeyDown, activeUpload, setActiveUpload, onUpload }`）→ Chat.jsx 消费方 0 diff |
| R-UI2-H-2 | `<Composer>` 调用 byte-equal（Composer v2 留 v0.6.4.4；R-217 式 hold）|
| R-UI2-H-3 | VRP grep-guard：ChatEmpty grep `1280|800|at%|width:'NN%'`（排除合法 100%/maxWidth）= 0 真违规；maxWidth 720/640/560 是合法内容居中 cap |
| R-UI2-H-4 | 业务文案 byte-equal：4 suggestion 文案 + 副标题 + "KNOT 可能出错" 大写 + R-PA-PB-1 hint + h1 + firstName 逻辑 |
| R-UI2-H-5 | hex 0（grep `#[0-9a-fA-F]{3,6}` 排除 boxShadow = 0；R-229 dot ring 保 color-mix 非 hex alpha）|
| R-UI2-H-6 | 其他屏 0 改：`git diff origin/main -- frontend/src/screens/ frontend/src/Shared.jsx frontend/src/primitives.jsx frontend/src/utils.jsx frontend/src/Shell.jsx` 仅 ChatEmpty + version 同步点；App.jsx/api.js/index.css/main.jsx 0 改 |
| R-UI2-H-7 | ChatEmpty.jsx ≤ 100 LIMIT（92 → 删 2 inline icon ≈ -8 / 加 grid ≈ +0 → ~90）|
| R-UI2-H-8 | 字面分流 sustained：R-227.5（knot 小写装饰 vs KNOT 大写声明）|
| R-UI2-H-9 | R-PA-8 Phase B 字面 0；R-185 不涉（ChatEmpty 无 KnotLogo）|
| R-UI2-H-10 | **五处版本同步**（守护者 v0.6.4.2 教训）：main.py + smoke + README ★ + Login footer + Shell L43 → 0.6.4.2→0.6.4.3（本 PATCH 不改 Login/Shell 视觉，但版本字面须四处 CI 强制点 + Shell L43 同步）|

## §3 验收
1. suggestion 2-col grid（宽屏）/ 1-col（窄屏 auto-fit）+ chip T.text + I.spark/I.flow Foundation icon
2. `git diff origin/main -- frontend/src/screens/chat/ frontend/src/screens/Chat.jsx` 仅 ChatEmpty（Composer/Conversation/其余 0 改）
3. npm build 绿 + check_file_sizes OK + hex 0 + VRP grep 0 + R-PA-8 0
4. **五处版本同步**（main.py + smoke + README ★ KNOW-1 + Login footer R-181 + Shell L43）
5. 资深 :8000 post-merge live 终判（home greeting + 2-col suggestion + Composer 不破）

## §4 commit 序列（轻量 v3）
- **commit 1**：ChatEmpty.jsx 视觉（suggestion grid + icon 偿还 + 间距）+ 五处版本同步 0.6.4.2→0.6.4.3 + 本 plan
- **commit 2（收官）**：npm build 静态重建 + CHANGELOG

## §5 协议合规
轻量 v3：执行者 Stage 1（本草案）→ 守护者 Stage 3（VRP 合规 grep + 五处版本同步显式核 + Composer hold 合理性 + R-217 式契约 byte-equal）→ 逐 commit 直读 → 资深 push/merge + :8000 live 终判。
