# v0.5.17 — (C5+) AdminAudit 屏复刻 — Stage 1 草案

> **状态**：📝 **Stage 1 草案**（待 Stage 2 资深+Codex 评审 / Stage 3 v0.4 守护者终审）
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **里程碑**：v0.5.x 第二个顶层屏复刻（v0.5.15 SavedReports → v0.5.17 AdminAudit）+ **Inset 8% 闭环第四处**+ **rgba 豁免 R-313 扩展（modal overlay）**+ **StatusDot 首次抽取**

---

## 1. 决议表（D1-D8 + Q1-Q5 草案 — 待 Stage 2/3 终审）

| ID | 推荐 | 备注 |
|---|---|---|
| **D1** Stat 4-cards 顶部 grid | A 加（总记录/今日/失败/涉及用户） | 4 placeholder + `title="后端聚合 API 对接中 (v0.6+)"` tooltip — v0.5.16 模式 |
| **D2** Filter 5-field demo 风格 | A 视觉 demo + **业务字段名 byte-equal** | label 改"显示名"; placeholder/value 字段 byte-equal `actor_id/action/resource_type/since` |
| **D3** Table HTML `<table>` → CSS Grid | A 替换 | 与 v0.5.15 SavedReports + v0.5.16 DataSources 一致；删 `<thead>`/`<tbody>`/`<tr>`/`<th>`/`<td>` |
| **D4** Emoji 偿还 | A 全清 | `📋` topbar 删；`✓✗` → svg check/x + StatusDot inline；drawer ✕ 保 Unicode（业务标准）|
| **D5** Redacted hex 清理 | A 全清 R-286 sustained | `#FF990033` → `color-mix(in oklch, T.warn 20%, transparent)`；`#cc6600` → `T.warn`；`#fff` → `T.sendFg`；`#2e7d32` → `T.success` |
| **D6** Drawer rgba 处理 | **B R-313 扩展豁免（modal overlay）** | `rgba(0,0,0,0.4)` 保 — 与 boxShadow 同列 rgba 业务豁免；OKLCH 在 backdrop 浏览器 fallback 不一致风险高 |
| **D7** Topbar "导出 CSV" 按钮 | **B 跳过** | 无对应 api endpoint（`/api/admin/audit-log/export.csv` 待 v0.6+）；维持 R-401 api 契约 byte-equal |
| **D8** 行数预算 | A 新增 LIMIT 380 | 264 → ≤ 380（同 SavedReports）；31 → 32 条 LIMITS |
| **Q1** Stat card 真数据 vs placeholder | A 全 placeholder | 后端无 stats endpoint；`title` tooltip 同 v0.5.16 |
| **Q2** Filter "重置" 按钮加不加 | A 加 | demo 有；视觉对称；`onClick={() => setFilter({...empty})}` |
| **Q3** Action color mapping 接入 | A 全接入 | `auth.*` warn / `budget.*+prompt.*+fewshot.*` brand / `export.*` warn / default muted（含 helper 函数）|
| **Q4** Avatar 组件复用 | A inline 22×22 brandSoft（**v0.5.16 I.db 复用模式延伸**）| Shared 无 Avatar；inline 不抽 helper（保 R-393 KnotLogo sustained 3 文件）|
| **Q5** StatusDot 抽 helper | A inline 局部 helper | demo 引用 `StatusDot` window helper；本 PATCH inline 函数 `StatusDot(T, status)` |

---

## 2. 红线草案（R-399 ~ R-425, 27 条）

### 契约守护（R-399 ~ R-403）

| ID | 内容 |
|---|---|
| **R-399** | `AdminAuditScreen` 5 props 签名 byte-equal（T / user / onToggleTheme / onNavigate / onLogout）|
| **R-400** | 5 state slots byte-equal（items / loading / page / size / filter / drawerRow — useState 数量 + 类型 + 初值）|
| **R-401** | `api.get('/api/admin/audit-log?...')` URL byte-equal + 6 query params byte-equal（limit/offset + actor_id/action/resource_type/since）|
| **R-402** | `_PAGE_SIZES = [50, 100, 200]` + `_REDACTED_RE = /••••redacted••••/g` 字面 byte-equal |
| **R-403** | row 13 字段访问 byte-equal（id/created_at/actor_name/actor_role/actor_id/action/resource_type/resource_id/client_ip/user_agent/request_id/success/detail_json）|

### 视觉重构（R-404 ~ R-414）

| ID | 内容 |
|---|---|
| **R-404** | Topbar title `"📋 审计日志"` → `"审计日志"` 删 emoji |
| **R-405** | Stat 4-card grid `repeat(auto-fit, minmax(180px, 1fr))` (R-394 sustained) + 4 placeholder + tooltip |
| **R-406** | Filter strip Field helper mono label + `letterSpacing: '0.06em'` + uppercase + select/input bgInset |
| **R-407** | Filter "查询" + "重置" 双按钮（Q2）；"查询" primary T.accent + T.sendFg；"重置" ghost 边框 |
| **R-408** | Table HTML `<table>` 全删 → CSS Grid 7-col `1.4fr 1fr 1.3fr 2fr 0.7fr 0.6fr 60px`（R-117 SavedReports 模式 sustained）|
| **R-409** | thead bg → `color-mix(in oklch, ${T.accent} 8%, transparent)` + T.mono + 0.06em + uppercase（**R-386 brandSoft 8% 闭环第四处** — ResultBlock/SavedReports/DataSources/AdminAudit 四屏 byte-equal）|
| **R-410** | Avatar 22×22 brandSoft 8% (R-397 sustained) + name[0] uppercase Inter + role chip mono uppercase 10px |
| **R-411** | Action color mapping helper：`auth.*` → T.warn / `budget.*+prompt.*+fewshot.*` → T.accent / `export.*` → T.warn / default → T.muted（Q3）|
| **R-412** | StatusDot inline 函数（Q5）— 6×6 圆 + 颜色 (success/warn) + 文字（"成功" / "失败"）+ flexShrink: 0 |
| **R-413** | Pagination 风格更新 — "1-N / 总数" mono + 上/下页 ghost btn + 50/100/200 select |
| **R-414** | Redacted 高亮 `#FF990033`/`#cc6600` → `color-mix(in oklch, ${T.warn} 20%, transparent)` + `T.warn`（D5）|

### R-313 rgba 豁免扩展 + hex 全清（R-415 ~ R-418）

| ID | 内容 |
|---|---|
| **R-415** | **R-313 rgba 豁免扩展第二处** — modal/drawer overlay rgba(0,0,0,0.4) 业务豁免（首处 v0.5.11 R-254 boxShadow；本处第二处） |
| **R-416** | Submit btn `'#fff'` → `T.sendFg`（v0.5.15 R-352 Q4 sustained — 严禁 'white' 字面）|
| **R-417** | success `'#2e7d32'` → `T.success` + 删 `T.success \|\| '#2e7d32'` fallback hex |
| **R-418** | grep `#[0-9a-fA-F]{3,6}` AdminAudit \| grep -v boxShadow \| grep -v rgba = 0 命中（hex 0 残留）|

### 范围 + 契约（R-419 ~ R-425）

| ID | 内容 |
|---|---|
| **R-419** | App/api/index.css/main/utils/Shared/Shell/decor/16 屏（不含 AdminAudit）/Admin/SavedReports/tab_access `git diff` = 0 |
| **R-420** | admin/ 4 子模块（tab_resources/knowledge/system/modals）0 改 |
| **R-421** | chat/ 7 子模块 0 改 |
| **R-422** | App.css 0 行 diff + AdminAudit 0 新 className（除 `cb-sb`）|
| **R-423** | KnotLogo R-199.5/222 sustained 仅 Shared+Login+Shell 三文件 |
| **R-424** | 三处版本同步 0.5.16 → 0.5.17（main.py + smoke + Login.jsx）|
| **R-425** | scripts/check_file_sizes.py LIMITS 31 → 32（新增 `frontend/src/screens/AdminAudit.jsx: 380`）|

---

## 3. 9 子步骤执行顺序

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + LIMIT dict 新增 380 | R-425 |
| 2 | Topbar title 删 📋 emoji → "审计日志" | R-404 |
| 3 | Stat 4-card grid + R-394 auto-fit + tooltip placeholder | R-405 |
| 4 | Filter strip 5-field — Field helper mono uppercase + 重置/查询 双按钮 | R-406/407 |
| 5 | Table `<table>` → CSS Grid 7-col + thead brandSoft 8% mono | R-408/409 |
| 6 | Avatar 22 brandSoft + role chip mono + action color mapping | R-410/411 |
| 7 | ✓/✗ → StatusDot inline helper + svg check/X polyline | R-412 |
| 8 | Pagination 风格 + redacted hex 清理 + 全 hex grep 守护 | R-413/414/418 |
| 9 | R-313 rgba 豁免扩展叙事 + 三处版本同步 + grep 闭门验证 | R-415/424 |

---

## 4. 关键代码意图（提前样化）

### R-409 brandSoft 8% 闭环字面（第四处）

```jsx
// thead
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // 与 R-323/R-372/R-386 字面 byte-equal
```

### R-411 Action color mapping

```jsx
function actionColor(T, action) {
  if (action.startsWith('auth.')) return T.warn;
  if (action.startsWith('budget.') || action.startsWith('prompt.') || action.startsWith('fewshot.')) return T.accent;
  if (action.startsWith('export.')) return T.warn;
  return T.muted;
}
```

### R-412 StatusDot inline helper

```jsx
function StatusDot({ T, ok }) {
  return (
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, color: ok ? T.success : T.warn, fontSize: 11.5 }}>
      <span style={{ width: 6, height: 6, borderRadius: '50%', background: 'currentColor', flexShrink: 0 }}/>
      {ok ? '成功' : '失败'}
    </span>
  );
}
```

### R-415 rgba 豁免扩展叙事

```jsx
// drawer backdrop — R-313 sustained 扩展豁免 #2（首处 v0.5.11 R-254 boxShadow）
// 理由：modal/drawer overlay OKLCH→sRGB fallback 浏览器不一致风险高，rgba 业务豁免
background: 'rgba(0,0,0,0.4)',  // ← R-415 豁免列表
```

---

## 5. 文件级改动清单

```
frontend/src/screens/AdminAudit.jsx     [EDIT 264 → ≤ 380 行]
                                        Step 1-9 顺序锁死（见 §3）
scripts/check_file_sizes.py             [EDIT 31 → 32 条] LIMITS 加 'frontend/src/screens/AdminAudit.jsx': 380
knot/main.py                            [EDIT 0.5.16 → 0.5.17]  [R-424]
tests/test_rename_smoke.py              [EDIT smoke 0.5.16 → 0.5.17]  [R-424]
frontend/src/screens/Login.jsx          [EDIT 页脚 0.5.16 → 0.5.17]  [R-424]

frontend/src/screens/admin/*            [unchanged]   [R-420]
frontend/src/screens/chat/*             [unchanged]   [R-421]
frontend/src/screens/Admin.jsx          [unchanged]   [R-419]
frontend/src/screens/SavedReports.jsx   [unchanged]   [R-419]
frontend/src/screens/tab_access.jsx     [unchanged]   [R-419]
frontend/src/{App,api,Shell,Shared,utils,main}.jsx 等  [unchanged]   [R-419]
frontend/src/App.css                    [unchanged 0 行 diff]   [R-422]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]   [R-419]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.17 段]
CLAUDE.md                               [EDIT v3 协议历史表 +1 / 主路线图 +1]
docs/plans/v0.5.17-admin-audit.md       [当前文件，Stage 1 → LOCKED 后存档]
```

---

## 6. 验收清单

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（CI 干净 env）
- [ ] `lint-imports` 7 contracts KEPT
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（32 files）
- [ ] `npm run build` 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.17**

### 视觉强化验证

- [ ] **R-409 brandSoft 8% 闭环 byte-equal** — `color-mix(in oklch, ${T.accent} 8%, transparent)` 4 文件命中（ResultBlock/SavedReports/tab_access/AdminAudit）
- [ ] **R-418 hex 0 命中** — grep `#[0-9a-fA-F]{3,6}` AdminAudit \| grep -v boxShadow \| grep -v rgba = 0
- [ ] **R-415 rgba 豁免** — grep `rgba\(` AdminAudit = 1 命中（drawer overlay 唯一）
- [ ] **R-404 topbar emoji 删** — grep `📋` AdminAudit = 0 命中
- [ ] **R-405 stat grid + tooltip** — 4 处 `title="后端聚合 API 对接中 (v0.6+)"`
- [ ] **R-408 HTML table 全删** — grep `<table\|<thead\|<tbody\|<th\|<td\|<tr` AdminAudit = 0
- [ ] **R-411 action color** — helper function `actionColor` 命中

### 契约守护

- [ ] **R-399** AdminAuditScreen 5 props 签名 byte-equal（diff vs origin/main 仅视觉行）
- [ ] **R-400** 5 useState slots byte-equal
- [ ] **R-401** api URL + 4 filter query params byte-equal
- [ ] **R-402** `_PAGE_SIZES` + `_REDACTED_RE` 字面 byte-equal
- [ ] **R-403** row 13 字段 byte-equal

### 范围守护

- [ ] **R-419** 16 屏 + Admin/SavedReports/tab_access + 8 核心非屏 `git diff` = 0
- [ ] **R-420** admin/ 4 子模块 0 改
- [ ] **R-421** chat/ 7 子模块 0 改
- [ ] **R-422** App.css 0 行 diff；AdminAudit 0 新 className（除 cb-sb sustained）
- [ ] **R-423** KnotLogo sustained 仅 3 文件

### 端到端

- [ ] 进 admin → 切 admin-audit nav → loading → items 加载（真后端 audit_log 数据）
- [ ] Filter 输入 4 字段 → 查询 → 列表更新
- [ ] Filter "重置" → 4 字段清空
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Stat grid + Filter strip 自适应
- [ ] 翻页 1→2→1 + 50/100/200 size 切换
- [ ] eye 按钮 → drawer 打开 → 5 KV + DetailJsonView
- [ ] **DetailJsonView redacted 高亮**（含 PII 的 detail_json）— color-mix 闭环视觉
- [ ] Drawer click outside → 关闭（R-415 backdrop 仍生效）
- [ ] **light + dark 双模式** — Stat / Filter / Table / Drawer 视觉一致

---

## 7. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.17): AdminAudit.jsx 视觉重构 — Stat grid + Filter strip + Grid table + StatusDot + brandSoft 8% 第四处闭环 (R-399~R-425)` | npm build + 全 grep 守护 + 端到端 |
| 2 | `chore(v0.5.17): version bump 0.5.16→0.5.17 三处同步 + LIMIT 380 (R-424/425)` | 全闸门 + version=0.5.17 |
| 3 | `docs(v0.5.17): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + Inset 8% 四处闭环里程碑 + R-313 rgba 豁免扩展叙事` | 全闸门 |

---

## 8. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审待启动（**第 18 次施行**预计）
- ✅ § Visual Replication Protocol 适用（v0.5.8 立约 — 顶层屏复刻）
- ⏳ 等 Stage 2 资深 + Codex 评审 → Stage 3 v0.4 守护者终审 → LOCKED
- ✅ 红线总数 27（R-399~R-425）— 高于 v0.5.16 (25)，原因：StatusDot inline helper + rgba 豁免扩展叙事 + Avatar 抽 helper + action color mapping 多新增视觉机制
- ✅ **AdminAudit 是 v0.5.x 第二个顶层屏复刻**（v0.5.15 SavedReports → v0.5.17 AdminAudit）
- ✅ **Inset 8% 闭环字面第四处扩展**（v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386 + v0.5.17 R-409）
- ✅ **R-313 rgba 豁免扩展第二处** — 首处 v0.5.11 R-254 boxShadow / 本处 v0.5.17 R-415 modal overlay
- ✅ **StatusDot 首次抽取**（inline helper；v0.6 候选 → 移入 Shared.jsx 与 ResultBlock/AdminBudgets 等共用）
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 15 屏 + App.css byte-equal

---

## 9. 风险点 + Stage 2/3 评审建议方向

### 高风险（建议 Stage 2/3 重点关注）

1. **R-401 api URL byte-equal** — 视觉重构不得改变 URL 路径或 query 字段名（后端接口契约硬契约）
2. **R-415 rgba 豁免扩展** — modal overlay 是否真有 OKLCH fallback 风险？建议守护者补充 evidence
3. **R-403 row 13 字段全 byte-equal** — 真实 API 返回字段名（actor_name vs actor_id vs created_at）严格逐字段守护

### 中风险

4. **D2 Filter 字段名 vs label** — `actor_id` 业务字段 vs "用户" demo label 风格冲突；推荐 label 改"显示名"保业务字段名
5. **D7 跳过导出 CSV 按钮** — 守护者可质疑是否应同步加 endpoint（推 v0.6+）
6. **Q5 StatusDot inline vs Shared 抽取** — 现 inline；未来 ResultBlock + AdminBudgets 复用时再抽

### 低风险

7. **D4 emoji 偿还** — 与 v0.5.16 模式一致
8. **D8 行数预算** — 264 → ≤ 380 与 SavedReports 318 → 380 同模式

---

📝 **Stage 1 草案完成 — 等待资深架构师 ack 启动 Stage 2/3 评审或简化协议直接 Stage 4。**
