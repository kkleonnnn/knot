# v0.5.17 — (C5+) AdminAudit 屏复刻 — LOCKED (Stage 1+2+3 整合 → Stage 4 准备)

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **协议**：Loop Protocol v3 — 三阶段评审闭环（**第 18 次施行**）
> **里程碑**：v0.5.x 第二个顶层屏复刻 + **Inset 8% 闭环第四处**（视觉规范铁律化）+ **R-313 rgba 豁免扩展第二处**（架构原则确立）+ StatusDot 首次抽取 + D2 双兼模式 + R-428~R-430 复杂业务屏守护

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 全部锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| D1 Stat 4-cards | ✅ A 4 placeholder + tooltip | Stage 1 |
| **D2** Filter 字段平衡 | ✅ A + **业务字段 + Demo 风格双兼**（修订 — Stage 2 强化） | Label "操作人 (Actor ID)" + Placeholder "输入用户 ID..." 防混淆 |
| D3 Table → CSS Grid | ✅ A | Stage 1 |
| D4 Emoji 全清 | ✅ A | Stage 1 |
| D5 Redacted hex 全清 | ✅ A R-286 sustained | Stage 1 |
| **D6** Drawer rgba 豁免 | ✅ **B R-415 架构准许**（Stage 2 evidence 补强） | Chrome < 111 / WebKit backdrop-filter GPU 渲染抖动；rgba 是稳健选择 |
| D7 跳过导出 CSV 按钮 | ✅ B | Stage 1（api endpoint 待 v0.6+） |
| D8 LIMIT 380 | ✅ A 31→32 条 | Stage 1 |
| Q1 Stat placeholder | ✅ A | Stage 1 |
| Q2 "重置" 按钮加 | ✅ A | Stage 1 |
| Q3 Action color mapping | ✅ A | Stage 1 + R-426 加码 |
| Q4 Avatar inline 22×22 | ✅ A | Stage 1（R-419 Shared 0 改 sustained） |
| **Q5** StatusDot inline helper | ✅ **A 局部 inline 准许**（v0.6 候选移入 Shared） | Stage 2 准许 — v0.5.x 冲刺阶段 R-419 优于组件提取 |

13/13 锁定；Stage 2 D2 强化双兼模式 + D6 evidence 补强；其余 Stage 1 保留。

---

## 2. 红线最终清单（R-399 ~ R-430，32 条）

### Stage 1 草案原 27 条（保留）

| ID | 摘要 |
|---|---|
| R-399 | AdminAuditScreen 5 props 签名 byte-equal |
| R-400 | 5 useState slots byte-equal |
| R-401 | api URL + 4 filter query params byte-equal |
| R-402 | `_PAGE_SIZES` + `_REDACTED_RE` 字面 byte-equal |
| R-403 | row 13 字段访问 byte-equal |
| R-404 | Topbar 删 📋 emoji |
| R-405 | Stat 4-card grid + tooltip placeholder |
| R-406 | Filter Field helper mono uppercase 0.06em + bgInset |
| R-407 | Filter 查询 + 重置 双按钮 |
| R-408 | Table HTML 全删 → CSS Grid 7-col |
| R-409 | **thead brandSoft 8% 闭环第四处** — 字面与 R-323/R-372/R-386 byte-equal |
| R-410 | Avatar 22×22 brandSoft + role chip mono |
| R-411 | Action color mapping helper |
| R-412 | StatusDot inline helper |
| R-413 | Pagination mono + ghost btn + size select |
| R-414 | Redacted hex → color-mix(in oklch, T.warn 20%, transparent) |
| R-415 | **R-313 rgba 豁免扩展第二处**（modal overlay） |
| R-416 | Submit btn `'#fff'` → T.sendFg；严禁 'white' |
| R-417 | success hex → T.success + 删 fallback |
| R-418 | grep hex 0 命中（除 boxShadow + rgba 豁免） |
| R-419 | 范围守护（App/api/...）byte-equal |
| R-420 | admin/ 4 子模块 0 改 |
| R-421 | chat/ 7 子模块 0 改 |
| R-422 | App.css 0 行 diff |
| R-423 | KnotLogo 3 文件 sustained |
| R-424 | 三处版本同步 0.5.16→0.5.17 |
| R-425 | LIMITS 31→32 条 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-426** | **Action Chip 语义** — 不仅颜色映射（R-411），必须 `padding: 2px 8px` + `border-radius: 4px` + `font-weight: 500` | inline style `padding: '2px 8px', borderRadius: 4, fontWeight: 500`；与 R-411 actionColor helper 协同 |
| **R-427** | **Redacted 视觉降噪** — color-mix 高亮必须配 `cursor: help` 暗示敏感脱敏 | DetailJsonView redacted 段 inline `cursor: 'help'`；与 R-414 color-mix 协同 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-428** | **13 字段访问安全 (R-403 扩展)** — actor_name 渲染必须 null check：`row.actor_name \|\| row.actor_id \|\| 'System'` | Table cell 渲染 actor 字段时全部走该兜底链；commit 1 后**强制** mock 系统级日志（actor_name = null）实测不崩溃 |
| **R-429** | **DetailJsonView 性能守护** — detail_json 解析 try-catch 包裹；畸形 JSON 显原始字符串 | DetailJsonView 内 `try { JSON.parse(detail_json) } catch { return <pre>{raw}</pre> }`；防主界面卡死；commit 1 后 mock 畸形 JSON 实测 |
| **R-430** | **Pagination 边界逻辑** — disabled 严格基于 `items.length < size` 或后端总数 | "上一页" disabled = `page === 0`；"下一页" disabled = `items.length < size`（无总数 API 时）；commit 1 后实测首页/尾页/中间页按钮态 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死 — R-409 优先）

> 优先 R-409 Inset 8% 闭环字面替换（全站视觉统一里程碑）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + LIMIT dict 新增 380 + **R-409 brandSoft 8% 闭环字面率先落地** | R-409/R-425 |
| 2 | Topbar 删 📋 emoji → "审计日志" | R-404 |
| 3 | Stat 4-card grid + R-394 auto-fit + tooltip placeholder | R-405 |
| 4 | Filter strip Field helper mono uppercase 0.06em + bgInset + 重置/查询双按钮 + **D2 双兼模式** Label "操作人 (Actor ID)" + Placeholder "输入用户 ID..." | R-406/407/D2 |
| 5 | Table HTML 全删 → CSS Grid 7-col + thead 视觉应用 R-409 brandSoft 8%（Step 1 已落字面） | R-408 |
| 6 | Avatar 22×22 brandSoft + role chip mono + ActionChip helper + **R-426 chip padding/radius/fontWeight** | R-410/411/426 |
| 7 | StatusDot inline helper + svg check/X polyline + **R-428 actor 字段 null check**（actor_name \|\| actor_id \|\| 'System'）| R-412/R-428 |
| 8 | Pagination 风格 + **R-430 disabled 边界** + Redacted hex 清理 + **R-427 cursor:help** + 全 hex grep 守护 + **R-429 DetailJsonView try-catch** | R-413/414/418/427/429/430 |
| 9 | R-415 rgba 豁免扩展叙事注释 + 三处版本同步 + grep 闭门验证 | R-415/424 |

### Stage 3 §3 验收关键 — grep 闭门

```bash
# 1. hex 0 命中（除 boxShadow + drawer rgba）
grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/screens/AdminAudit.jsx | grep -v boxShadow | grep -v rgba
# = 0 行

# 2. rgba 仅 1 命中（drawer overlay）
grep -nE "rgba\(" frontend/src/screens/AdminAudit.jsx
# = 1 命中

# 3. brandSoft 8% 闭环四处 byte-equal
git grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# ResultBlock.jsx / SavedReports.jsx / admin/tab_access.jsx / AdminAudit.jsx — 4 文件命中
```

### Stage 2 D2 双兼模式纪律

```jsx
<Field label="操作人 (Actor ID)" placeholder="输入用户 ID..." value={filter.actor_id} ... />
<Field label="操作类型 (Action)" placeholder="如 auth.login..." value={filter.action} ... />
<Field label="资源类型 (Resource Type)" placeholder="如 user / budget..." value={filter.resource_type} ... />
<Field label="起始时间 (Since)" placeholder="YYYY-MM-DD..." value={filter.since} type="date" ... />
```

**严禁**：Label 仅显"用户"等 demo 风格（admin 易混淆 actor_id 和 username）。

### Stage 2 R-415 evidence 补强落地

```jsx
{/* R-415 R-313 sustained 扩展豁免 #2（首处 v0.5.11 R-254 boxShadow）
    理由：Chrome < 111 / WebKit backdrop OKLCH 渲染抖动；rgba 是全平台一致性稳健选择 */}
<div style={{
  position: 'fixed', inset: 0,
  background: 'rgba(0,0,0,0.4)',  // ← R-415 豁免列表
  zIndex: 1000,
}} onClick={onClose} />
```

### Stage 3 R-428 actor null check 实施

```jsx
const displayName = row.actor_name || row.actor_id || 'System';
const displayInitial = (displayName || 'S').charAt(0).toUpperCase();
// 渲染 Avatar 用 displayInitial；name 列用 displayName
```

### Stage 3 R-429 DetailJsonView try-catch 实施

```jsx
function DetailJsonView({ T, raw }) {
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    return <pre style={{ ... mono ... }}>{raw}</pre>;  // 畸形 JSON 兜底
  }
  return <div>...</div>;  // 正常渲染 parsed 树 + redacted 高亮 + R-427 cursor:help
}
```

### Stage 3 R-430 Pagination 边界

```jsx
<button disabled={page === 0} onClick={() => setPage(p => p - 1)}>上一页</button>
<button disabled={items.length < size} onClick={() => setPage(p => p + 1)}>下一页</button>
```

后端无总数 API → 用 `items.length < size` 推断末页；v0.6+ 加总数字段后改为 `(page + 1) * size >= total`。

### Stage 2 R-426 ActionChip 协同 R-411

```jsx
function ActionChip({ T, action }) {
  const color = actionColor(T, action);
  return (
    <span style={{
      color,
      background: `color-mix(in oklch, ${color} 12%, transparent)`,
      padding: '2px 8px',
      borderRadius: 4,
      fontWeight: 500,
      fontSize: 11,
      fontFamily: T.mono,
    }}>{action}</span>
  );
}
```

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/AdminAudit.jsx     [EDIT 264 → ≤ 380 行]
                                        Step 1: baseline + LIMIT 新增 + R-409 brandSoft 8% 字面率先落 [Stage 3 §2 优先]
                                        Step 2: Topbar 删 📋 [R-404]
                                        Step 3: Stat 4-card grid + R-394 auto-fit + tooltip [R-405]
                                        Step 4: Filter strip + R-406/407 + D2 双兼 Label+Placeholder
                                        Step 5: Table → CSS Grid 7-col + thead 闭环视觉 [R-408/409]
                                        Step 6: Avatar + role chip + ActionChip [R-410/411/426]
                                        Step 7: StatusDot inline + R-428 actor null check
                                        Step 8: Pagination + R-430 边界 + redacted hex 清 + R-427 cursor:help
                                                + R-429 DetailJsonView try-catch + 全 hex grep [R-413/414/418/427/429/430]
                                        Step 9: R-415 rgba 豁免注释 + 三处版本同步 + grep 闭门 [R-415/424]
                                        守护: 5 props + 5 useState + api URL/params + _PAGE_SIZES/_REDACTED_RE + 13 字段 byte-equal
                                              + inline styles only [R-422 CSS 0 污染]

scripts/check_file_sizes.py             [EDIT LIMITS 31 → 32]
                                        - "frontend/src/screens/AdminAudit.jsx": 380 [R-425]
knot/main.py                            [EDIT version 0.5.16 → 0.5.17]  [R-424]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.16 → 0.5.17]  [R-424]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.16 → 0.5.17]  [R-424]

frontend/src/screens/admin/*            [unchanged]   [R-420]
frontend/src/screens/chat/*             [unchanged]   [R-421]
frontend/src/screens/Admin.jsx          [unchanged]   [R-419]
frontend/src/screens/SavedReports.jsx   [unchanged]   [R-419]
frontend/src/screens/ (其余 15 屏)       [unchanged]   [R-419]
frontend/src/{App,api,index,main,utils,Shared,Shell}.jsx 等  [unchanged]  [R-419]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]   [R-419]
frontend/src/App.css                    [unchanged 0 行 diff]  [R-422]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.17 段]
                                        R-399~R-430 偿还表
                                        + **Inset 8% 闭环第四处里程碑**（视觉规范铁律化 — 四屏字面 byte-equal）
                                        + **R-313 rgba 豁免扩展第二处**（架构原则 — boxShadow + modal overlay）
                                        + StatusDot inline 首次抽取（v0.6 候选移入 Shared）
                                        + ActionChip helper + DetailJsonView try-catch + Pagination 边界守护
                                        + Loop Protocol v3 第 18 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.17（Inset 8% 第四处 + rgba 豁免第二处）
                                        - v0.5.x 路线图 v0.5.17 ⏳ → ✅
docs/plans/v0.5.17-admin-audit.md       [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（32 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.17**

### Stage 2/3 强化验证

- [ ] **R-409 brandSoft 8% 闭环第四处**：`git grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — ResultBlock/SavedReports/admin/tab_access/AdminAudit **4 文件**命中
- [ ] **R-418 hex 0 命中**：`grep -nE "#[0-9a-fA-F]{3,6}" AdminAudit.jsx | grep -v boxShadow | grep -v rgba` = 0
- [ ] **R-415 rgba 仅 1 命中**：`grep -nE "rgba\(" AdminAudit.jsx` = 1（drawer backdrop） + 含注释说明豁免理由
- [ ] **R-426 Action Chip**：grep `padding.*2px 8px\|padding:\s*'2px 8px'` ActionChip 区命中 + `borderRadius:\s*4` + `fontWeight:\s*500`
- [ ] **R-427 cursor:help**：grep `cursor.*help\|cursor:\s*['"]help['"]` Redacted 段命中
- [ ] **R-428 actor null check**：grep `row\.actor_name\s*\|\|\s*row\.actor_id\s*\|\|\s*['"]System['"]` 命中
- [ ] **R-429 DetailJsonView try-catch**：DetailJsonView 内 `try {.*JSON.parse.*} catch` 命中
- [ ] **R-430 Pagination 边界**：`disabled.*page === 0` + `disabled.*items\.length < size` 命中
- [ ] **D2 双兼**：Label `操作人 (Actor ID)` + Placeholder `输入用户 ID` 命中
- [ ] **R-404 emoji 删**：grep `📋` AdminAudit.jsx = 0
- [ ] **R-405 tooltip placeholder**：grep `title="后端聚合 API 对接中.*v0\.6\+"` ≥ 4
- [ ] **R-408 HTML table 全删**：grep `<table\|<thead\|<tbody\|<th\|<td\|<tr` AdminAudit.jsx = 0

### 契约守护（首要红线）

- [ ] **R-399**：AdminAuditScreen 5 props 签名 byte-equal
- [ ] **R-400**：5 useState slots byte-equal（items / loading / page / size / filter / drawerRow — 数量+类型+初值）
- [ ] **R-401**：`api.get('/api/admin/audit-log?...')` URL + 6 query params byte-equal（limit/offset + actor_id/action/resource_type/since）
- [ ] **R-402**：`_PAGE_SIZES = [50, 100, 200]` + `_REDACTED_RE = /••••redacted••••/g` 字面 byte-equal
- [ ] **R-403**：row 13 字段访问 byte-equal（id/created_at/actor_name/actor_role/actor_id/action/resource_type/resource_id/client_ip/user_agent/request_id/success/detail_json）

### 范围守护

- [ ] **R-419**：App/api/index.css/main/utils/Shared/Shell/decor/15 屏（不含 AdminAudit）/Admin/SavedReports/tab_access `git diff` = 0
- [ ] **R-420**：admin/ 4 子模块 `git diff` = 0
- [ ] **R-421**：chat/ 7 子模块 `git diff` = 0
- [ ] **R-422**：App.css `git diff` = 0；AdminAudit 0 新 className（除 cb-sb）
- [ ] **R-423**：grep `KnotLogo|KnotMark|KnotWordmark` frontend/src/ 仍 3 文件命中

### 端到端业务回归

- [ ] 进 admin → 切 admin-audit nav → loading → items 加载真后端数据
- [ ] Filter 4 字段输入 → 查询 → 列表更新；Label/Placeholder D2 双兼显示正确
- [ ] Filter "重置" → 4 字段清空
- [ ] 翻页 1→2→1（R-430 边界 disabled 正确）+ 50/100/200 size 切换
- [ ] eye 按钮 → drawer 打开 → 5 KV + DetailJsonView
- [ ] **Redacted 高亮 + R-427 cursor:help 实测**（鼠标悬停显帮助光标）
- [ ] **R-429 畸形 JSON 兜底实测**（mock detail_json = "not a json"）— 显原始字符串不崩溃
- [ ] **R-428 actor null check 实测**（mock row.actor_name = null）— 显 actor_id 或 'System'
- [ ] Drawer click outside → 关闭（R-415 backdrop 仍生效）
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Stat grid + Filter strip auto-fit 自适应
- [ ] **light + dark 双模式** — Stat / Filter / Table / Drawer 视觉一致
- [ ] v0.5.0~v0.5.16 既有功能（Shell/Login/ChatEmpty/Composer/ThinkingCard/ResultBlock/SavedReports/DataSources）端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.17): AdminAudit.jsx 视觉重构 — Inset 8% 闭环第四处 + Filter D2 双兼 + ActionChip + StatusDot + R-428~R-430 (R-399~R-430)` | npm build + 全 grep 守护（hex 0/rgba 1/闭环 4 文件）+ 端到端业务 + R-429 畸形 JSON + R-428 actor null + R-430 边界 + 三档窗宽 + 双模式 | 5 props/5 state/api URL/13 字段 byte-equal + Step 1→9 顺序 + R-409 优先落地 + R-415 rgba 豁免注释 |
| 2 | `chore(v0.5.17): version bump 0.5.16→0.5.17 三处同步 + LIMITS 31→32 新增 AdminAudit 380 (R-424/425)` | 全闸门 + routes=72 + version=0.5.17 + 32 条 LIMITS | R-424 三处同步 grep ≥ 3 |
| 3 | `docs(v0.5.17): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + Inset 8% 四处闭环铁律化里程碑 + R-313 rgba 豁免扩展原则 + StatusDot 首次抽取存档` | 全闸门 | Loop Protocol v3 第 18 次施行 + 视觉规范铁律化 + 架构原则确立 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→9 顺序不可换；**R-409 闭环字面率先落地**（Stage 3 §2 明确执行核心）。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 18 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 32（v0.5.17 区段 R-399~R-430）
- ✅ **AdminAudit = v0.5.x 第二个顶层屏复刻**（v0.5.15 SavedReports → v0.5.17 AdminAudit）
- ✅ **Inset 8% 闭环第四处 — 视觉规范铁律化**（v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386 + v0.5.17 R-409 字面 byte-equal）
- ✅ **R-313 rgba 豁免扩展原则确立** — 首处 v0.5.11 boxShadow + 第二处 v0.5.17 modal overlay；evidence 补强（Chrome < 111 / WebKit backdrop GPU 渲染）
- ✅ **StatusDot 首次 inline 抽取** — v0.6 候选移入 Shared.jsx 与 ResultBlock/AdminBudgets 共用
- ✅ **ActionChip helper + DetailJsonView try-catch + Pagination 边界三大守护** — 复杂业务屏复刻新模式
- ✅ **D2 双兼模式** — 业务字段名 + Demo 风格 Label/Placeholder 平衡（防 admin 混淆）
- ✅ R-227.5.1 装饰豁免延伸（mono uppercase thead）
- ✅ R-302.5 emoji 业务豁免 sustained
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.17 五处 sustained）
- ✅ I.db 复用模式延伸（Q4 Avatar inline 22×22 brandSoft）
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 15 屏 + App.css byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ **归档锁定手册** → 当前文件；分支 `feat/v0.5.17-admin-audit`
2. ✅ **Stage 3 §2 执行核心严守**：R-409 brandSoft 8% 闭环字面**率先落地**（Step 1）— 标志全站四处闭环里程碑
3. ✅ **R-415 rgba 豁免 evidence 注释必加**：drawer backdrop 上方注释 Chrome < 111 / WebKit GPU 渲染抖动
4. ✅ **D2 双兼纪律**：4 Filter 字段 Label 显业务字段名（Actor ID 等）+ Placeholder 显输入示例
5. ✅ **R-428~R-430 三大守护手测**：mock actor_name=null / 畸形 JSON / 翻页边界
6. ✅ **R-409 闭环 4 文件 byte-equal**：4 文件 git grep 命中
7. ✅ **R-418 hex 0 命中纪律**：grep `#[0-9a-fA-F]{3,6}` AdminAudit | grep -v boxShadow | grep -v rgba = 0
8. ✅ **R-415 rgba 唯一命中**：grep `rgba(` AdminAudit = 1
9. ✅ **R-426 ActionChip 协同 R-411**：actionColor + color-mix 12% bg + padding/radius/fontWeight 三件套
10. ✅ **R-427 cursor:help 实测**：悬停 redacted 高亮显帮助光标
11. ✅ **Q5 StatusDot inline 守纪律**：v0.5.x 冲刺**仅 inline**；CHANGELOG 显式标 v0.6 候选承诺
12. ✅ **里程碑叙事**：CHANGELOG 显式三条标注（Inset 8% 铁律化 / rgba 豁免架构原则 / StatusDot v0.6 候选）
13. ✅ **范围抗诱惑**：不顺手抽 StatusDot 入 Shared / 不清 tab_access Users hex / 不动其他 admin tab — R-419/420/421/423 四重范围守护

---

🛡️ **执行者收到 GO 信号；v0.5.x 第二个顶层屏复刻正式启动；Inset 8% 闭环铁律化 + R-313 rgba 豁免架构原则确立 + StatusDot 首次抽取 + D2 双兼模式 + R-428~R-430 复杂业务屏守护五大设计先例同时落地。**
