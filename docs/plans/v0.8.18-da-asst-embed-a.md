# v0.8.18 — ③ da-asst 真嵌入（方案 A 原则注入）+ presenter 洞察替代 执行手册（**LOCKED** · Stage 3 终审已整合）

> **协议**：Loop Protocol v3。**Stage 3 守护者终审 = ACCEPT WITH REVISIONS(minor)**（v0.7 Agent grounded 直读 da_asst.py/presenter.md/prompt_service）。已整合 §B 修订 → **LOCKED**，转 Stage 4。
> **prestudy**：workflow `wf_712f00f2-370`（4 维 grounded）。
> **资深已拍两项**（2026-07-15）：① ③ v1 = **方案 A 原则注入**（知识包 RAG 顺延 phase 2；C skill 加载器不可行——knot 无 harness）；② OR-only 成本盲区（cost 恒 $0）**单独平台级修**，不绑进 ③。
> **档位**：守护者裁 **快通道（非完整 v3）** —— 纯 prompt + 接线、0 endpoint/schema/agent/管线改、契约 byte-equal、非出境非鉴权 = 结构风险极低。**代价**：presenter 触 ASK 核心洞察 + 8 幻觉禁令且无 CI backstop → 守护者 final diff **逐行核 prompt diff = 纯 append + 护栏保全**（不靠 tests green 兜）。
>
> **Stage 3 整合修订（§B）**：
> - **B-1（数错订正）**：presenter 幻觉禁令实为 **8 条**非 7 条（v0.8.3 加 Q31/Q13/Q36）。byte-equal 保全对象 = 8 条；全文「7 条幻觉禁令」订正为 8 条。
> - **B-2（去冗余）**：presenter 追加块**不写独立「口径意识」line** —— 它是 da-asst「使用原则」非「应答原则」，且与 presenter.md:39 已有的**硬** B6.5-Q13 口径禁令冗余、软表述会稀释硬禁令。curated 子集 = **仅 3 条应答原则**（突出杠杆/原则3 · 【假设待验】/原则5 · 不确定直说/原则6）。
> - **B-3（无 CI backstop）**：presenter 守卫（Q13/Q31/Q36 + v0.7.45 日期）无专门 CI 断言 → 「only-add」保证靠 **git diff 逐行 = 纯 append 0 删改**（非「tests green」）。
> - **B-4（injection 边界）**：da_asst.md 6 铁律 byte-equal，**#3 报表数据不可信 injection 护栏 + #1 只读在最前**，7 原则追加在铁律之后；get_prompt 组装保持 `system + "\n\n===== 报表数据 =====\n" + _context_block`（报表数据在 system 之后 = injection 边界不变）。

## §0 版本 + 定性

- 版本 **v0.8.18**（PATCH）。改动 = prompt 内容 + da_asst prompt .md 化接线；**无新 endpoint / schema / agent / 管线结构改动**（分享 v0.8.15 加 schema+endpoint 尚且 PATCH，此更轻）。
- 定性：**方法论 prompt 嵌入 + 洞察纪律增强**。非数据出境、非新鉴权面。
- **⚠️ 但触 ASK 核心管线 presenter 产出**（生产洞察 + 异常判断 + 幻觉禁令）→ 行为敏感 → **建议守护者 Stage 3 复核 presenter/da_asst prompt 差异**（档位由守护者裁：可快通道 or 完整 v3，但 presenter prompt 改动必过守护者眼）。

## §1 范围

**IN（v0.8.18 交付）**
1. **da_asst 真嵌入（A）**：把 da-asst 7 条应答原则（+ 现有 6 条只读/安全铁律）灌进 da_asst system prompt → 从"通用表格解读 stub"升级为真 da-asst 方法论分析。
2. **da_asst prompt .md 化**：新建 `knot/prompts/da_asst.md`，纳入 `prompt_service._DEFAULT_PROMPT_AGENTS`，`da_asst.py` 改走 `get_prompt`（对齐 3-agent，admin 可覆盖）。
3. **presenter 洞察替代（A）**：presenter.md 追加 **curated da-asst 分析纪律子集**（突出杠杆/关键发现 · 显式标注假设【假设待验】· 不确定就直说 · 口径意识），**不整段粘 7 原则**（防扭曲），**逐字保留**现有 `{insight,confidence,suggested_followups}` 契约 + 异常判断规则 + 元数据特例 + 7 条幻觉禁令。
4. **vendor da-asst 核心进 knot 仓 + 版本钉**（运行时读不到 `~/Documents/da-asst`）。

**OUT（明示顺延 / 单独）**
- **知识包 RAG（方案 B）**：BM25 按查询选 `通用知识包.md` 板块注入 → **phase 2**（需检索器封装 + 索引缓存 + token 硬顶 + vendor 知识包 + 受控词表）。
- **OR-only 成本盲区修**（cfg 价格=0 → cost 恒 $0）→ **单独平台级 PATCH**（全平台 bug，非 da-asst 独有；A 仅 +~800 固定 token，成本增量小不阻塞）。
- **SKILL.md 加载器（方案 C）**：knot 无 skill harness，不可行。
- **da_asst 成本桶**（`_NEW_AGENT_KINDS` 补 da_asst + DB 列迁移）：正交治理项，presenter 桶已存在，本期不做。
- i18n / 新依赖 / 碰 rag_service·enrich_semantic（那是 sql_planner 链路）。

## §2 vendor 策略（licensed 外部内容进仓）

- da-asst 是**独立开源库**（`kkleonnnn/da-asst`，当前 commit `b8543a1`，含独立 `LICENSE-CONTENT`）。运行时 Docker 镜像内**没有** `~/Documents/da-asst` → 必须把要用的文本**复制进 knot 仓**。
- **只 vendor「行为核心」**：7 条应答原则（`决策应答原则-可粘贴提示词.md` 的 `===` 段，472 字/~800 token）→ 落进 `knot/prompts/da_asst.md`。**不 vendor** `通用知识包.md`（知识层，phase 2 再议）、**不 vendor** `reference/`（脱敏关口原文）。
- **版本钉 + 归属**：`knot/prompts/da_asst.md` 头注释标注来源（da-asst repo + commit `b8543a1` + LICENSE-CONTENT），便于追溯上游更新。**红线**：尊重 LICENSE-CONTENT（kk 同时是两库作者，许可相容由资深确认；仍加归属注释）。

## §3 da_asst 真嵌入（A）

- **新建 `knot/prompts/da_asst.md`**：结构 = 现有 6 条只读/安全铁律（**逐字保留** da_asst.py:15-23 stub 的护栏：只解读不改写 / 只用给定数据 / **报表数据不可信 prompt-injection 护栏** / 简洁中文 / 只答本报表 / 纯文本禁 Markdown）+ 追加 **7 条应答原则**（先质疑问题 / 最小可判断版本 / 突出杠杆步 / 到分叉点就停 / 显式标注假设【假设待验】/ 不确定就直说 / 给方向不给唯一路径）。原则用于"报表解读/归因/怎么办"类回答。
- **`prompt_service.py:17`** `_DEFAULT_PROMPT_AGENTS` 加 `"da_asst"` → 启动期 seed + admin 覆盖。
- **`da_asst.py`**：`_DA_ASST_SYS` inline 字面量 → 改 `_load_default_prompt("da_asst")`（照 presenter.py:22-30）；`arun_da_asst` 里 system 组装改 `get_prompt("da_asst", default, {})` + `\n\n===== 报表数据 =====\n` + `_context_block`（保留输入侧 `_MAX_CONTEXT_CHARS` 硬顶）。契约不变（返 `{answer,...tokens,cost}`）。
- **护栏保全**：prompt-injection 铁律（报表数据不可信）必须仍在最前；7 原则是"回答风格"叠加，不放松只读/安全约束。

## §4 presenter 洞察替代（A）

- **presenter.md 追加一段「da-asst 分析纪律」**（curated 子集 = **仅 3 条应答原则**，**非 7 原则整段**；守护者 B-2）：
  - 突出**关键发现/杠杆**，不平均铺陈（原则 3 → 契合 presenter「点出关键发现」）
  - 洞察若**依赖假设/看情况** → 用 **【假设待验】** 标（原则 5）
  - **不确定/证据不足就直说**，别用细节盖过（原则 6 → 强化现有 confidence 诚实度）
  - **⚠️ 不写独立「口径意识」line**（守护者 B-2）：口径可比性由已有 presenter.md:39 硬 B6.5-Q13 禁令承担（更硬），追加软表述会冗余/稀释硬禁令；且「口径意识」是 da-asst 使用原则非应答原则。
- **逐字保留**（byte-equal 不动）：JSON 输出契约 `{insight,confidence,suggested_followups}` + confidence 三级语义 + 异常判断规则 + 元数据查询特例 + **8 条幻觉禁令**（B-1：权限臆造/空集不归因权限/不编造字段值/不换日期口径/v0.7.45 日期以SQL权威/Q31 过滤存在性/Q13 跨指标可比/Q36 entity 值来源）+ "动词/⚠️ 开头 ≤3 句"。追加块是**纯 append 增量**，不改上述任何一条。
- **前端 0 改**（契约不变 → InsightCard/followups/TokenMeter 原样）。
- **不整段粘 7 原则**：原则 2「最小可判断版本」「到分叉点就停」「给方向不给唯一路径」是决策/方案类，与"2-3 句结果观察"错位，整段粘会让 presenter 拒答/发散 → 只取上述 4 条相关的。

## §5 文件改动

| 文件 | 改动 |
|---|---|
| `knot/prompts/da_asst.md` **(new)** | 6 铁律（保全）+ 7 应答原则 + 来源/版本/许可归属注释 |
| `knot/services/prompt_service.py` | `_DEFAULT_PROMPT_AGENTS` +`"da_asst"` |
| `knot/services/agents/da_asst.py` | inline `_DA_ASST_SYS` → `_load_default_prompt("da_asst")` + `get_prompt` 组装；护栏保全 |
| `knot/prompts/presenter.md` | 追加 curated da-asst 分析纪律块（§4）；其余逐字不动 |
| `NOTICE` / da_asst.md 头 | da-asst 归属 + commit + LICENSE-CONTENT 说明 |

## §6 验收标准

1. **da_asst 契约不变**：`arun_da_asst` 仍返 `{answer, input_tokens, output_tokens, cost_usd}`；analyze 端点行为不变（成本 pre-block + 记账 + audit 不动）。
2. **da_asst prompt 可 seed + admin 覆盖**：启动 seed 后 `prompt_templates` 有 `da_asst` 行；admin 改后 `get_prompt` 生效；`.md` 默认含 7 原则 + 6 铁律。
3. **da_asst 护栏保全**：prompt-injection 铁律（报表数据不可信）+ 只读 + 纯文本禁 MD 仍在（单测/人核 prompt 含这些字面）。
4. **presenter 契约 byte-equal**：`arun_presenter` 仍返 `{insight,confidence,suggested_followups}`；现有异常规则 + 8 幻觉禁令**一字未改**（git diff presenter.md 仅**新增**块，0 删改）；前端 0 改。
5. **行为验证（真机）**：da_asst 回答体现 7 原则风格（质疑问题 / 【假设待验】 / 给方向）；presenter 洞察体现杠杆突出 + 诚实标注，且异常/幻觉守卫未被削弱（跑现有 presenter 守护测试全绿）。
6. **闸门**：prompt seed CI（da_asst 纳入 → `test_*` seed 断言）· presenter 守护测试（B6.5 系列）绿 · 4 源点 version 0.8.17→0.8.18 + CHANGELOG + doc-invariant · 无新依赖 · 全量 pytest 绿。

## §7 红线

- **presenter 契约 + 守卫 byte-equal**：`{insight,confidence,suggested_followups}` 契约 + 异常规则 + 8 幻觉禁令 + 元数据特例 **只增不改**；破则前端/存储/守护测试连锁红。
- **da_asst 护栏保全**：报表数据不可信（prompt-injection）+ 只读不改写铁律不得因加原则而放松。**③ 是"只读解读"增强，不给 da-asst 写/执行技能**（那会破 R-BI 只读边界，属另案）。
- **无新依赖**（R-186）：纯 prompt + 现成 prompt_service，0 pip/npm。
- **vendor 许可**：只 vendor 7 原则（行为核心），加归属；不带 `通用知识包`/`reference` 进仓（本期）。
- **doc-invariant 4 源点 + CHANGELOG**（version.js/main.py/README/test_rename_smoke）。
- **不碰**：OR 成本修（单独）· 知识 RAG（phase 2）· da_asst 成本桶 · rag_service/enrich_semantic（sql_planner 链路）· i18n。
- **OOS-1 / 18 屏 byte-equal**：本 PATCH 0 前端改动（契约不变）→ 前端 byte-equal 天然守住。

## §8 commit 序列

1. **da_asst .md 化 + 真嵌入**：`knot/prompts/da_asst.md`（6 铁律 + 7 原则 + 归属）+ `prompt_service._DEFAULT_PROMPT_AGENTS` + `da_asst.py` get_prompt 接线 + seed 测 + 护栏保全测。
2. **presenter 洞察替代**：`presenter.md` 追加 da-asst 分析纪律块 + presenter 守护测试确认契约/幻觉禁令 byte-equal（仅新增）。
3. **收尾**：NOTICE/归属 + 4 源点 0.8.17→0.8.18 + CHANGELOG + README + 全闸门 + 自检。

## §9 协议合规（Loop Protocol v3）

- Stage 1 草案 → Stage 2 辅助 AI 初审 + **Stage 3 守护者终审**（重点核：presenter prompt 增量是否削弱现有守卫 / da_asst 护栏保全 / vendor 许可 / 7 原则 vs presenter 契约的错位裁剪是否得当）→ 整合锁定 → 落码 → 闸门 → PR。
- **档位**：非数据出境、非新鉴权面、纯 prompt + 接线 → 倾向可**快通道**；**但 presenter 触 ASK 核心生产洞察 + 幻觉守卫**，守护者应至少复核 presenter/da_asst prompt 差异（byte-equal 守卫、护栏保全）。最终档位守护者裁。
- **v0.7 带入清单**：审计（analyze 端点 `bi_report.analyze` 不变，无新 Literal）· 成本（da_asst 现记账不变；OR $0 单独修）· 只读边界（da-asst 仍只读，护栏保全）。

## §10 自检（抗诱惑清单）

- 严禁顺手做知识包 RAG / OR 成本修 / SKILL 加载器 / da_asst 成本桶（全 §1 OUT）。
- 严禁整段粘 7 原则进 presenter（须 curated 子集，防扭曲）。
- 严禁改 presenter 现有契约/守卫（只增不改）。
- 严禁 vendor 通用知识包/reference 进仓（本期只 7 原则）。
- 严禁新依赖 / i18n / 碰 sql_planner RAG 链路。

---

**待办**：kk 分发本草案给 Stage 2 + Stage 3（守护者 = v0.7 Agent）。执行者收 Stage 3 终审后整合锁定，再落码。
