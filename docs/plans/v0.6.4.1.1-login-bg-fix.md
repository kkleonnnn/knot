# v0.6.4.1.1 — login 背景 VRP 修复 + R-UI2-VRP 立约（hotfix）

> **性质**：v0.6.4.1 同 PATCH 周期小修补（version 0.6.4.1 不动，仅 build 时间戳 + 修复 — 版本号纪律）；轻量 v3（守护者复核）
> **触发**：资深本地 :8000 viewing 发现 login 背景「整体偏移」+ 立 UI v2 复刻原则
> **基座**：worktree `v0641-login-fix` @ branch `v0.6.4.1.1-login-bg-fix`（off main `6779653`）

## §0 根因
v0.6.4.1 login 复刻**照搬 artboard 写死值** → 宽 viewport 背景整体偏移：
- 外层 `radial-gradient(ellipse at 22% 18%, ..., transparent 55%)` — farthest-corner ellipse 在宽屏胀开 + 偏移
- motif `width: '70%'`（artboard 写死）
违反 v0.5.7 VRP「底色 fluid + element-anchored」标准。

## §1 修复
- 外层 `background` → `T.chipBg` 实底（fluid 铺满，无 viewport 百分比渐变）
- motif wrapper `right 70%` → `inset 0`（铺满 left panel，v0.5.7 标准）
- 绿光由 motif 自身 `radial-gradient(ellipse at 30% 30%, color-mix(in oklch, T.accent 15%, transparent), transparent 60%)` 提供（element-anchored，锚到 motif，宽屏不偏移）
- footer build 时间戳 bump（version 0.6.4.1 不动）；右侧浮动 card 不变

## §2 ⭐ R-UI2-VRP 立约（CLAUDE.md § 视觉模型 — UI v2 复刻铁律）
**artboard（Claude design）把握「整体设计方向」；本地标准把握「细节」。** artboard 写死尺寸/坐标/百分比严禁照搬，须按本地 VRP（fluid 实底 + element-anchored glow + buildTheme/TOKENS_V2 + 契约 byte-equal）重新锚定。**后续每屏复刻强制套用**。

## §3 红线
- LoginScreen export+props byte-equal（App.jsx 0 diff sustained）· 业务链/文案/契约 0 改（仅背景 + motif 锚定 + footer build）· VRP 标准（本地锚定，0 artboard 写死值）· R-PA-8 0 · 资深已 viewing 批准（:8000）

## §4 验收
1. 宽 viewport 背景不偏移 + 左侧 motif 绿光锚定（资深 :8000 已批准 ✓）
2. App.jsx 0 diff · 其他屏 0 改 · 业务/文案 byte-equal
3. npm build + check_file_sizes + R-PA-8 0

## §5 commit（1 commit · hotfix）
Login.jsx（bg + motif + footer build）+ CLAUDE.md R-UI2-VRP 立约 + 静态重建 + 本 plan。version 0.6.4.1 不动（小修补纪律）。

## §6 协议
轻量 v3：执行者修（资深 viewing 批准）→ 守护者复核（VRP 标准 + R-UI2-VRP 立约 concur）→ push/merge。
