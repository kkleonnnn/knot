# v0.8.3 — B6.5/6.6 叙述层收尾：presenter/clarifier ground 到 SQL/params（语义激活门 🟡）

> **执行者手册 · v2（Stage 3 终审整合后）**（v0.8 Agent）。Loop Protocol v3 完整三阶段。Stage 1 → 守护者
> （v0.7 Agent，workflow `w342zaae2` 2 路 POC + 脱敏链 trace）终审 = **ACCEPT WITH REVISIONS (minor)**
> —— 6 结构断言全 CONFIRMED，**脱敏 crux 3 泄漏假设全 REFUTED（无 blocking 泄漏）**，5 fork 全裁 A。修订 =
> fork-1B caller 计数订正（3→**2**）+ §B pre-existing params-scrub 口径统一（Q36 顺手 1 行，guardian OK）+
> §E 部署强调（prompt fork 须 admin-UI 重applied 否则线上 0 效果）。执行者复核：§C caller=2 + §B params 未 pop 逐行核实 CONFIRMED。§9 = 整合记录（供守护者 light final-diff）。
> **B6 批最低风险条**（🟡 叙述、数字不变、无 design 问题、脱敏无真泄漏）。
> **性质**：v0.7 遗留激活门 B6.5🟡 + B6.6🟡（`v0.7-closeout-backlog.md` L67/68）——叙述层「说了 SQL 没做的事」
> = 数字对、散文误导（🟡 价值损失非危险）。**激活门 收尾（非硬 blocker）**：gate（误判≈0+命中≥90%）已由
> B6.1（安全）+ B6.4（误判）承重；B6.5/6.6 数字对不动误判率（B6.4 plan §46 明示「回退后散文仍说同比 ≠ 激活门失败」）。
> **风险**：多为 prompt-层（B6.6 纯 clarifier.md / B6.5 Q13·Q31 纯 presenter.md），唯 **B6.5-Q36 需一处编排改**（喂 params 给 presenter）→ 整体 ≈ B6.2（低-中）。
> **grounded 基线**：main HEAD `dd2d5db`（v0.8.2）。经 grounding workflow `woovdbroe`（3 路）核实（R-137）。

---

## §0 背景（grounded · 结构关键发现）

### 0.1 ⭐ 两个独立叙述面（backlog「都喂同一 explanation」措辞不精确 — grounding 订正）

| 显示字段 | 来源 | owner |
|---|---|---|
| **`explanation`**（ResultBlock:170） | `clarifier_result["analysis_approach"] or sql_result.explanation`（`query.py:487` save / `:507` SSE / `:378` HTTP）—— **analysis_approach 非空即胜（几乎总非空）→ 显示的是 clarifier 叙述** | **B6.6（clarifier）** |
| **`insight`**（ResultBlock:210 InsightCard，独立字段 `query.py:511`） | presenter.insight | **B6.5（presenter）** |

**二者不竞争同一字段**：`analysis_approach→explanation`、`insight→insight` 是两个 SSE 字段。B6.5 修 `insight`；B6.6 修 `explanation`。
（sync `/query` 路径不跑 clarifier、用 `result.explanation`——B6.5/6.6 explanation-selection 只在 `query-stream` SSE 生产路径。）

### 0.2 B6.5 三例（presenter `insight`，backlog L67）
- **Q13 跨指标比率不校验时间口径**：本月充值（MTD）vs 累计代理费（cumulative 无日期列）→ presenter 叙述「5.4 倍」当可比。SQL 侧正确回退（⚪），纯 **presenter 叙述** overclaim。
- **Q31 声称时间过滤实际无 WHERE**：SQL 无 WHERE 却叙述「本月/近7天」。**largely 已被 v0.7.45 `presenter.md:37`「SQL 无时间条件时不编造时间范围」覆盖** → 或仅需收紧/验证。
- **Q36 幻觉 entity 值**：HTTP 正确查 `user_id=12345`（DB msg264 params 铁证），但 presenter 从裸答案「1」编造「该用户 ID:1」。**根因**：HTTP 路径喂 presenter 的 sql = `f"GET {url_template}"`（`query.py:363`）**不含 resolved params**（params 在 `http_result["params"]` `:321/:377`，**未传** run_presenter_step）→ presenter 无从知 12345。ban 3「引用数字来自查询结果」反而**导致**此 bug。

### 0.3 B6.6 两例（clarifier `analysis_approach`，backlog L68）
- clarifier 是 SQL 前的独立 LLM 调用（只喂 schema/history/date_block/business_rules）→ **读不到权威编译窗** → 自算日期漂移。
- Q28：clarifier 自算日期 vs 权威 SQL 窗不一致。Q3：「近7天」clarifier `06-28~07-04`（不含今天）vs compiler `06-29~07-05`（含今天）= 口径定义分歧。
- ⚠️ **clarifier.md:12 relative-word 规则 scoped 到 `refined_question`、EXPLICITLY EXEMPT `analysis_approach`**（「口径/路由说明可写进 analysis_approach」）→ B6.6 须**把规则扩到 analysis_approach**。

### 0.4 勿一刀切（backlog L67：单指标直陈+空集诚实已好 Q27/30/29）
必须**不回归**：单指标直陈（Q27/30）· 空集诚实（Q29，`presenter.md:21` + should_run_presenter gate）· metadata 特例（`presenter.md:26-30`）· 标量 COUNT/SUM=high（`:24`）。**修须外科**（只收紧 比率/过滤声明/entity 值），blanket「ground 一切」会回归。

### 0.5 B6.4 散文 overclaim 半的重叠（grounding 精确结论）
B6.6（clarifier 相对词）只闭 **date-drift 半**；**同比 semantic-overclaim 半 B6.6 不闭**（相对词与「声称做了没做的同比」正交）。B6.4 guard 已使同比→engine=llm，但 clarifier analysis_approach（上游）仍可写「计算同比变化」promote 到 explanation。**真闭 = explanation 对账 SQL（promotion-seam 编排改）**或接受残余（B6.4 已声明非激活失败）。见 §1.5 fork。

---

## §1 决议（Stage 1 提案）

### 1.1 B6.6：clarifier.md `analysis_approach` 扩相对词 + 不写具体日期（纯 prompt + fork 重applied）
- clarifier.md：**把 :12 relative-word 铁律扩到 `analysis_approach`**（现 exempt）——analysis_approach **不写具体绝对日期/口径窗**，用相对词（今天/本周/近7天…）；具体窗由下游权威 SQL 决定。闭 Q28/Q3 显示 explanation 的日期漂移。
- ⚠️ **部署（承重 grounding）**：live clarifier DB = **2026-05-25 定制 fork ≠ 文件默认**（v0.7.22 memory）→ 幂等 seed **不覆盖既有行** → **改 clarifier.md 只影响新装/re-seed，不影响 live fork**。故 fix：① 改 clarifier.md（默认/新装）；② **kk 须经 admin-UI（有审计）把该规则重applied 到 live fork**（勿裸 DB re-seed 会 clobber fork）。backlog「clarifier DB==文件默认 re-seed 干净」前提**在 fork 上不成立** → 手册明示。

### 1.2 B6.5-Q13 / Q31：presenter.md 收紧规则（纯 prompt）
- **Q13**：presenter.md 加 ban——**跨指标比率/倍数/对比，须时间口径可比**；MTD vs cumulative（无日期列）等口径不一致 → 不出比率或明示口径不匹配。（presenter 已收 SQL，材料足。）
- **Q31**：验证/收紧 `presenter.md:37`——SQL 无 WHERE/时间谓词 → insight **不得声称任何过滤/时间圈定发生**（明示结果为全量/未过滤）。largely 已覆盖，收紧措辞即可。

### 1.3 B6.5-Q36：喂 params 给 presenter（编排改）+ presenter.md entity 规则 + params-scrub 口径统一
- **编排（fork-1A LOCKED）**：query.py:363 HTTP caller 把 `http_result["params"]`（含 user_id=12345）并进喂 presenter 的 sql 串（镜像 message-save `:377`）。
- **presenter.md**：加 ban——**entity 标识符（user_id / 交易对 symbol 等）须从 SQL/params 读，严禁从裸答案值推断**（修 ban 3 导致 Q36 的反效果）。
- **⭐ params-scrub 口径统一（守护者 §B，Q36 顺手）**：`desensitize.py:145` 现只 `out.pop("sql")` **不 pop `output.params`** → 非 admin trace 拿 `{user_id:12345}` 原样（pre-existing v0.7.35，params 非机密 = **非泄漏**，但与「sql 被 pop」口径不一致）。Q36 既已把 params 抬进 pipeline（喂 presenter），**顺手加 1 行 `out.pop("params", None)`** 统一「HTTP 内部形态（sql+params）不发非 admin」+ 非-admin scrub 单测。**注**：presenter insight echo user_id = **自读非泄漏**（守护者 §B：user_id 是用户自己问句抽取值 + get_messages conv-owner 校验），本 fix 不影响 insight，只收紧 trace。

### 1.4 外科红线（勿一刀切）
新规则**只**针对 比率-可比性 / 过滤-存在性 / entity-值来源三类；**不**收紧单指标直陈、空集诚实、metadata 特例、标量 confidence。

### 1.5 ⭐ 待裁 fork（Stage 2/3）
1. **Q36 编排机制** → **✅ A LOCKED**：query.py:363 HTTP caller 把 `http_result["params"]` 并进喂 presenter 的 sql 串（镜像 message-save `:377`）——最小、不改签名、SQL caller `:456` byte-equal、presenter 视 sql 为 opaque 串可单测。（**B** 加 `params` 参数：守护者 §C 订正 run_presenter_step **仅 2 caller**（`:362 HTTP`/`:456 SQL`，非草案「3」——第 3 是 test 打 arun_presenter），B 反逼 SQL 路径传冗余空 params → 不选。）
2. **clarifier fork 部署** → **✅ A LOCKED**：改 clarifier.md（默认）+ 手册明示 kk admin-UI 重applied 到 2026-05-25 fork（审计）；B（re-seed）在 fork 上无效（守护者 §A：seed skip 既有行 + runtime DB-first）。
3. **B6.4 同比 semantic-overclaim 残余** → **✅ A LOCKED**：接受为残余（🟡 + B6.4 已声明非激活失败 + 回退后 engine=llm；守护者 §A 确证正交）；promotion-seam 对账 defer。
4. **Q31** → **✅ A LOCKED**：验证微调 `presenter.md:37`（守护者 §A：largely 已覆盖，非新 ban）。
5. **脱敏** → **✅ Q36 fix 无需改脱敏链**（守护者 §B：3 泄漏假设全 REFUTED，无真泄漏）；**output.params 口径不一致 → Q36 顺手加 1 行 `out.pop("params")` 统一**（§1.3，guardian OK）。验证法：prompt 规则 LLM-behavioral（无确定性 POC → Stage 2/3 + kk live eval）；**Q36 编排 + params-scrub 可单测**。

---

## §2 红线
- **R-8.3-1 数字不变（narrative-only）**：B6.5/6.6 只改叙述文案，**不改任何 SQL/数字/行为**；命中/误判率不受影响（🟡 非 blocker）。
- **R-8.3-2 勿一刀切**：单指标直陈/空集诚实/metadata/标量 confidence 叙述 **不回归**（§1.4）。
- **R-8.3-3 flag off**：`KNOT_SEMANTIC_LAYER` 不动。
- **R-8.3-4 prompt fork 部署**：clarifier.md/presenter.md 是 DB-seeded + live 是 2026-05-25 fork → 改 .md **须明示 kk admin-UI 重applied**（不覆盖 fork 的幂等 seed；admin-UI 有审计=首选）。
- **R-8.3-5 Q36 编排最小**：唯一代码改 = 喂 params 给 presenter step（HTTP 路径）；不碰 compile/parser/guard/语义层。
- **R-8.3-6 版本 4 源点** 0.8.2→0.8.3 + CHANGELOG demote v0.8.2→[Released]。
- **R-8.3-7 sync `/query` 不动**（不跑 clarifier/presenter analysis_approach 路径）。

---

## §3 文件改动清单（提案）
| 文件 | 改动 | 类型 |
|---|---|---|
| `knot/prompts/clarifier.md` | :12 relative-word 铁律扩到 `analysis_approach`（不写具体绝对日期，用相对词）——B6.6 | 改（prompt · fork 需 admin-UI 重applied） |
| `knot/prompts/presenter.md` | +ban：跨指标比率须口径可比（Q13）· 收紧无-WHERE-不声称过滤（Q31）· entity 从 SQL/params 读非裸答案（Q36）——B6.5；守勿一刀切 | 改（prompt · fork 需 admin-UI 重applied） |
| `knot/api/query.py` | `:363` HTTP caller 喂 presenter 的 sql 串并入 `http_result["params"]`（Q36 fork-1A LOCKED；`run_presenter_step` 签名 + SQL caller `:456` 0 改）| 改（编排） |
| `knot/services/desensitize.py` | `:145` +`out.pop("params", None)`——统一 HTTP 内部形态 sql+params 均不发非 admin（守护者 §B，Q36 顺手）| 改（脱敏口径统一） |
| `tests/...`（query/desensitize 测试） | Q36：params 到达 presenter（sql 串含 user_id=12345）单测 + 非-admin output.params 被 pop 单测；prompt 规则 behavioral 无确定性单测 | 改/新增 |
| 版本 4 源点 + CHANGELOG | §6 | 改 |

---

## §4 验收标准
### 4.1 Q36（可确定性单测）
- HTTP 路径 presenter step 收到含 `user_id=12345`（或 params dict）的输入（sql 串含 params / 参数透传）——单测断言 params 到达（修「presenter 无从知 entity」根因）。
### 4.2 prompt 规则（behavioral — Stage 2/3 + kk live eval 验，非确定性单测）
- Q13：口径不一致的跨指标比率 → presenter 不出比率/明示不匹配。
- Q31：SQL 无 WHERE → insight 不声称过滤。
- Q28/Q3：explanation（clarifier analysis_approach）不写具体绝对日期 → 无日期漂移。
- **勿一刀切**：Q27/30 单指标直陈 + Q29 空集诚实 + metadata 特例 **叙述不回归**（kk live 抽验）。
### 4.3 闸门
数字/行为 byte-equal（narrative-only）· 全套件绿（含 Q36 单测）· lint-imports 9 · flag off · 4 源点版本 CI 绿。

---

## §5 commit 序列（提案）
1. **Q36 编排**（query.py HTTP 喂 params + [fork-1B 则 run_presenter_step 参数]）+ presenter.md entity 规则 + params-到达单测。
2. **presenter.md Q13/Q31 规则**（比率口径可比 + 收紧无-WHERE；守勿一刀切）。
3. **clarifier.md B6.6**（analysis_approach 扩相对词不写具体日期）+ 手册/README 明示 kk fork admin-UI 重applied。
4. **版本 4 源点** 0.8.2→0.8.3 + CHANGELOG（demote v0.8.2→[Released]）+ static（version.js 变则重建）。

---

## §6 协议合规 + 版本
- **Loop Protocol v3**：Stage 1 → kk 分发 Stage 2 → 守护者 Stage 3 终审 → 整合 → commit。**prompt 规则 behavioral → 守护者重点评审措辞是否外科（勿回归 Q27/29/30）+ Q36 编排正确性 + fork 部署路径**。
- **版本 4 源点 ★CI** 0.8.2→0.8.3 + CHANGELOG single-unreleased。
- **v0.7 不变量**：脱敏（Q36 params 喂 presenter 须确认非 admin 脱敏链不泄漏 —— presenter insight 走 message 读端点继承脱敏，params 是 HTTP 查询参数如 user_id 非机密，但须核）· OOS-1/crypto/成本/async 不触。**⚠️ 脱敏核**：喂 params 给 presenter 后，params（user_id 等）是否随 insight 泄漏给非 admin？须核 message 读端点脱敏覆盖 insight（R-SL-13/16）。

---

## §7 自检（执行者）
- [x] 两叙述面 grounded（explanation=clarifier analysis_approach 胜 / insight=presenter；非同字段）
- [x] B6.5 三例 + B6.6 两例 + 勿一刀切 grounded
- [x] Q36 根因 grounded（params 未喂 presenter `query.py:363` vs `:377`）+ clarifier.md:12 analysis_approach exempt
- [x] 激活框架 grounded（🟡 收尾非 blocker；B6.4 已声明散文残余非激活失败）
- [x] clarifier live = 2026-05-25 fork → re-seed 不生效、须 admin-UI 重applied（v0.7.22 memory）
- [ ] Stage 2 辅助 AI 初审（待 kk 分发）
- [ ] Stage 3 守护者终审（待）：裁 §1.5 5 fork + 措辞外科性 + Q36 编排 + 脱敏核（params→insight）+ fork 部署
- [ ] 整合 → commit

---

## §8 裁决清单（Stage 3 守护者全 LOCKED — 见 §1.5）
1. Q36 编排 → **✅ A**（sql 串带 params；守护者 §C run_presenter_step 仅 2 caller，fork-1A 最小）。
2. clarifier fork 部署 → **✅ A**（改 .md + kk admin-UI 重applied fork；re-seed 在 fork 无效）。
3. B6.4 同比 semantic-overclaim 残余 → **✅ A 接受+记录**。
4. Q31 → **✅ A 验证微调 `presenter.md:37`**。
5. 脱敏 → **✅ 无真泄漏**（守护者 §B 3 假设全 REFUTED）；**output.params 口径 Q36 顺手 1 行 `out.pop("params")` 统一**。

---

## §9 Stage 3 终审整合记录（供守护者 light final-diff）

守护者裁决 = **ACCEPT WITH REVISIONS (minor)**，B6 批最低风险，无 design 问题。

| 守护者 finding | 执行者复核 | 解（v2 落点） |
|---|---|---|
| §A 6 结构断言（两叙述面独立字段/Q36 根因/clarifier exempt/勿一刀切四锚点/B6.4 重叠/fork 部署） | ✅ 全 CONFIRMED | §0 采信不改 |
| §B 脱敏 crux（3 泄漏假设 REFUTED，无 blocking） | ✅ CONFIRMED（desensitize.py:137/145 pop sql 不 pop params；insight echo user_id=自读+conv-owner 校验非泄漏） | Q36 fix 无需改脱敏链；insight 安全 |
| §B pre-existing CONCERN（output.params 非 admin 未 pop，与 sql pop 口径不一致） | ✅ CONFIRMED | §1.3 Q36 顺手加 `out.pop("params")` 统一 + §3 desensitize.py 行 + 单测 |
| §C fork-1B caller 计数（3→2） | ✅ CONFIRMED（grep run_presenter_step = :362/:456 两 caller） | §1.5 fork-1 订正 + fork-1A LOCKED |
| §D 5 fork 全 A | 采纳 | §1.5/§8 LOCKED |
| §E 部署 crux（prompt fork admin-UI 重applied 否则线上 0 效果，同 v0.7.45 教训） | ✅ 采纳（R-8.3-4 已抓，特别强调） | §1.1 + R-8.3-4 + commit 3 明示 kk fork 重applied |

**守护者 light final-diff 检查点**（§G）：Q36 params 到达 presenter（sql 串含 user_id=12345）单测 + output.params 非-admin pop 单测 + 四锚点未回归 + 数字 byte-equal + flag off + 手册 fork 重applied 提示。

**⚠️ 两 housekeeping（守护者提，kk 需 action）**：
1. **B6.4 v0.8.2 欠一次完整 final diff**（守护者上轮承诺 non-light）——kk 把 v0.8.2 B6.4 merged diff 喂守护者补核（硬误判=0 重构 + regex 精修是否真落 dd2d5db）。
2. **B6.5/6.6 落码后 kk 必须 admin-UI 把新 clarifier/presenter 规则重applied 到 live fork**（否则叙述层修复线上 0 效果 —— 本 PATCH 唯一真操作风险）。
