# v0.6.4.0 — UI v2 Foundation刀（设计系统 v2 · 全站大改起手）

> **Loop Protocol v3 — 全 v3 三阶段**（UI 大改铁律：业务代码走完整评审）
> **资深拍板（2026-06-17）**：① UI v2 先于 v0.7 语义层（新 MINOR 系列 v0.6.4.x）② Foundation刀 **保留 25 token 名只换值**（v0.5.6 R-156 strangler 先例）
> **设计源**：`/Users/kk/Documents/knot_demo_ui/v0.6/`（`tokens.js` + `ui.jsx` + 22 artboards）
> **先例**：v0.5.6 Foundation刀（18屏0改自动换皮 R-156 + buildTheme 契约 R-158）+ v0.6.2.3 additive-only 修改 R-365
> **基座**：worktree `v064-ui-foundation` @ branch `v0.6.4.0-ui-v2-foundation`（off main `0f67b5f`）

---

## §0 实证（当前 Shared.jsx vs 新 tokens.js — 奥卡姆界定真实 delta）

⭐ **调色板已同源**（产品 v0.5.6 即从此 palette 复刻）：
| token | 当前 buildTheme | 新 tokens.js | delta |
|---|---|---|---|
| brand | `oklch(72/58% 0.17 195)` | brand[400]/[600] 同值 | **0 改** |
| bg/content/sidebar/border/text/subtext/muted | ink[950/900/50/200/...] | 同 ink 色阶值 | **0 改** |
| success | `oklch(72% 0.18 145)` | `ok: oklch(72% 0.16 155)` | 微调（chroma/hue）|
| warn | `oklch(82% 0.16 85)` | `warn: oklch(78% 0.16 75)` | 微调（L/hue）|
| err / info | 无（错误用 warn/accent）| `err oklch(66% 0.20 25)` / `info oklch(70% 0.15 240)` | **新增** |

**结论**：UI v2 真实 delta ≠ 颜色（palette 同源），而是 **① 新组件原语**（`Btn` 5-variant / `Tag` 5-tone — 取代 iconBtn/pillBtn 模式）**② 逐屏新布局**（floating inset 侧栏 / 新卡片形态等 — 每屏 artboard）**③ 新增** brand/ink 完整色阶 + radius scale + err/info + 图标扩充（49 vs 38）。

→ **Foundation刀 = 微调 + additive，极小低风险**；18 屏 0 改自动换皮；真正大头在 v0.6.4.1+ 逐屏布局复刻。

---

## §1 范围

### In-scope（Foundation刀 — additive 为主）
- **F1 buildTheme 微调（3 值，守护者预审 Gap 1 补 successSoft）**：success `145°→155° (0.18→0.16)` + **successSoft 同步 `155°/0.16` tint**（L358 原硬编 145°/0.18，不同步则实心绿与 soft tint 不自洽）+ warn `85°→75° (L82→78)`；brand/ink/accentSoft(195°)/其余 0 改；**25 token 名 byte-equal**（R-158 契约 sustained）。App.css `@supports not(oklch)` fallback 仅 base styles不含语义色 → F5 对 success/warn no-op
- **F2 新增设计 token 导出**（additive，不入 buildTheme 25）：`TOKENS_V2`（brand/ink 完整色阶 + radius scale + err + info）供复刻屏引用
- **F3 新组件原语 → 拆 `frontend/src/primitives.jsx`**（资深拍 B — 守护者 §五；Shared 18-21 屏长战役早晚撑爆，UI v2 系列文件形态先例）：`Btn`（default/primary/ghost/soft/danger × sm/md/lg）+ `Tag`（neutral/brand/ok/warn/err）；import TOKENS_V2 from Shared + 接 `T` 对象（§9 映射表为唯一可审契约）。TOKENS_V2 纯数据留 Shared
- **F4 图标扩充**：I 加新增图标（保现有 38 名 byte-equal；新增 node/flow/bolt/catalog/budget/audit/bookmark 等 ui.jsx 有而 I 缺的）
- **F5 App.css / index.css**：:root OKLCH fallback 若 success/warn 变则同步
- **F6 收官**：version 0.6.4.0 三同步 + README/CLAUDE 路线图（UI v2 系列）+ CHANGELOG + 静态

### OOS（明确不做 — 留逐屏复刻）
- ❌ 不改任何屏布局（18 屏 + 子模块 git diff 0 行 — R-156 自动换皮）
- ❌ 不动 Shell.jsx（floating inset 侧栏 = 复刻 PATCH）/ App.jsx / 真实契约
- ❌ 不强制屏迁移到 Btn/Tag（iconBtn/pillBtn 保留共存；屏复刻时逐个换）
- ❌ 不删旧 export（additive only — R-365 sustained；iconBtn/pillBtn/旧 helper 全留）
- ❌ 0 后端 / 0 业务逻辑

---

## §2 决策点（执行者推荐，待 Stage 2/3）

- **D1 TOKENS_V2 形态（守护者收紧 — 仅 theme-无关）**：独立 export `export const TOKENS_V2 = { brand:{50..900}, ink:{0..950}, radius:{xs..pill}, ok, warn, err, info }` 镜像 tokens.js **theme-无关**部分。**严禁**承载 theme-split 结构色（bg/border/text — 那是 buildTheme 的活，否则结构色双真相源）。复刻屏按需取色阶/radius/semantic。
- **D2 Btn/Tag token 源（守护者 Gap 2 — 架构命门，钉死）**：Btn/Tag 与 TOKENS_V2 同住 Shared.jsx → **结构色取 `T`（prop）**（default variant bg=T.content/border=T.border/text=T.text；soft bg=T.accentSoft）+ **semantic/radius 取模块内 `TOKENS_V2`**（danger fg=TOKENS_V2.err；radius=TOKENS_V2.radius.md；module-local 不穿 prop）。签名 `Btn({ T, variant, size, icon, onClick, children })` / `Tag({ T, tone, children })`——**接 T 对象（非 demo theme 字串）；err/radius 走 TOKENS_V2，绝不塞进 buildTheme 破 25 契约**。
- **D3 success/warn 微调要不要做**：推荐做（对齐新设计源）—— 但这会让 18 屏的 success/warn 视觉**轻微变色**（自动换皮，非 0 视觉漂移）。资深确认可接受（这正是 UI v2 的一部分）。
- **D4 图标新增范围**：推荐仅加 ui.jsx 有而产品复刻会用到的（node/flow/bolt/catalog/budget/audit/bookmark/medal/info...）；不动现有 38 名。

---

## §3 红线（R-UI2-1~7 — 执行者立，待终审）

| 红线 | 内容 |
|---|---|
| **R-UI2-1 buildTheme 25 名 byte-equal** | buildTheme 仍导出 25 设计 token 同名（R-158 契约 sustained）；仅 **success + successSoft + warn 3 值**变（守护者 Gap 1 — successSoft 必须随 success 同步 155°/0.16 保内部自洽）；brand/ink/accentSoft/其余值 0 改 |
| **R-UI2-2 18 屏 0 代码 diff（≠ 0 视觉）** | `git diff origin/main -- frontend/src/screens/ Shell.jsx App.jsx` = **0 行**（R-156 strangler 代码层）；**视觉非 0 漂移** — success/warn 微调变色 = **R-PA-PB-V1 有意松绑，资深 2026-06-17 ack 记档：UI v2 sanctioned 演进非违规**（守护者修订，防后续屏复刻拿「0 视觉漂移」当矛盾）|
| **R-UI2-3 additive only** | 不删任何现有 export（iconBtn/pillBtn/I 38 名/KpiCard/StatusDot/... 全留 byte-equal）；新增 = TOKENS_V2 + Btn + Tag + 新图标（R-365 additive sustained）|
| **R-UI2-4 Btn/Tag 接 T 对象** | 新组件签名用产品 T 约定（非 demo theme 字串）；复刻屏可用，旧屏不受影响 |
| **R-UI2-5 utils.jsx / Shell / App 契约** | utils.jsx 8 export byte-equal；Shell.jsx 13 props（R-192 宪法级）0 改；App.jsx 0 改 |
| **R-UI2-6 check_file_sizes（守护者 Gap 3 — 资深拍 B 拆文件）** | Shared 665/720 headroom 55，+TOKENS_V2+图标 ≈+80 → Shared LIMIT 720→**760**（小幅）；Btn/Tag 拆 `primitives.jsx` [NEW] 自有 LIMIT ~150；Stage 2 的 620 作废（< actual 665 会 CI FAIL）；CI 绿 |
| **R-UI2-7 R-PA-8 + 后端 0 改** | R-PA-8 0 命中；knot/**/*.py 0（除 version 串）|

---

## §4 验收标准

1. 18 屏自动换皮：success/warn 新色生效，brand/ink 不变，布局 0 漂移（部署后 knot.0p.oh light/dark 肉眼）
2. `git diff origin/main -- frontend/src/screens/ Shell.jsx App.jsx` = 0 行（R-UI2-2/5）
3. buildTheme 25 token 名 byte-equal（R-UI2-1）+ 旧 export 全留（R-UI2-3）
4. Btn/Tag/TOKENS_V2 新增可用（写个临时引用验编译，commit 前删）
5. npm build exit 0 + check_file_sizes OK + R-PA-8 0
6. version 0.6.4.0 三同步 + README L11 bump（纳收官 checklist）

---

## §5 commit 序列（2 commit）

- **commit 1**：Shared.jsx（buildTheme success/**successSoft**/warn 3 值 + 新增 `TOKENS_V2` 纯数据 + ~16 新图标 additive）+ **`frontend/src/primitives.jsx` [NEW]**（Btn/Tag 按 §9 映射表，import TOKENS_V2 + 接 T）+ check_file_sizes（Shared 720→760 + primitives ~150）+ §9 LOCKED 提交。验证：npm build + diff screens/Shell/App **0 行** + 旧 export(I 38/iconBtn/pillBtn/ghostBtnStyle/...)byte-equal + 新图标 0 撞名 + viewBox 24 + 临时引用验 Btn/Tag/TOKENS_V2 编译后删 + R-PA-8
- **commit 2**：收官 — version 0.6.4.0 三同步 + README L11 bump（描述符 = UI v2 起手）+ CLAUDE 路线图 UI v2 系列段 + CHANGELOG（demote 3.2→Released + 加 0.6.4.0 段 + UI v2 roadmap）+ 静态重建

---

## §6 协议合规

**全 v3 三阶段**（资深拍 — UI 大改业务代码不走简化协议）：Stage 1 草案 → Stage 2 辅助 AI 初审（重点：additive 不破 25 契约 + Btn/Tag API + 自动换皮可行性）→ Stage 3 守护者终审（v0.5 血缘 — 守 v0.5.6 R-156/R-158 设计系统先例）→ Stage 4 落地。**无远古激活**（Foundation 非 admin.py/PII）。

---

## §7 自检（Stage 1 草案）

- ✅ 设计源锚定（knot_demo_ui/v0.6 tokens.js + ui.jsx）
- ✅ 实证 palette 同源（Foundation刀 additive 低风险）
- ✅ 先例复用（v0.5.6 R-156/R-158 + v0.6.2.3 R-365 additive）
- ✅ 25 token 契约 sustained（R-UI2-1）+ 18 屏 0 改（R-UI2-2）
- ⏳ 待 Stage 2 初审 + 守护者 Stage 3（守 v0.5.6 设计系统先例）+ 资深拍板

---

## §8 待人测（部署后 knot.0p.oh）

- 18 屏 light/dark 自动换皮：success/warn 新色 + brand/ink 不变 + 布局 0 漂移
- Btn/Tag/TOKENS_V2 在后续复刻屏验真实渲染

---

## §9 LOCKED（Stage 1 预审 + Stage 2 + Stage 3 终审 + 资深拍板整合 — 2026-06-17）

> **Stage 2 质量信号**：辅助 AI 初审 4.9/5「大师级」但**未读源**（同 v0.6.3.2 模式）— 3 处 ungrounded：① LIMIT 称 560→620（实际 720/actual 665，620 会 CI FAIL）② F4 编造「38 图标重绘 + viewBox 塌陷 + SparkIcon/FlowIcon」（实际 additive + key 小写）③ D3 复述 Gap 2 未解。**降级**，契约由执行者实证 + 守护者 Stage 3 重验。

### §9.1 治理代际定性 = A（资深拍 + 守护者 Stage-1）
v0.6.4 第 2 位仍 6 → **v0.6 同代延续**（代际边界 = v0.6→v0.7）；UI v2 已定先于 v0.7 = 留 v0.6 内 → **守护者续任 v0.5，正常三阶段，不触发整体审核，无远古激活**。草案「新 MINOR 系列」措辞校正为「v0.6 同代新战役系列 v0.6.4.x」。

### §9.2 ⭐ Btn/Tag 落地映射表（守护者读 ui.jsx 实证 — 新组件唯一可审契约）
Btn/Tag 拆 `primitives.jsx`（资深拍 B）；**结构色自 `T`（prop）+ semantic/brand 阶自模块引 `TOKENS_V2`**（同仓 import，不穿 prop）。签名 `Btn({ T, variant='default', size='md', icon, iconRight, onClick, style })` / `Tag({ T, tone='neutral', children })`。

**Btn 5-variant**（sizes sm/md/lg = `{h:28/34/40, px:10/14/18, fs:12/13/14, gap:6/8/8}`；radius **8** 硬编；fontWeight 500；translateY 0.5px press）：
| variant | bg | fg | border |
|---|---|---|---|
| default | `T.content` | `T.text` | `T.border` |
| primary | `T.accent` | `T.sendFg` | `T.accent` |
| ghost | transparent | `T.text` | transparent |
| soft | `T.accentSoft` | `T.accent` | `TOKENS_V2.brand[200]` |
| danger | transparent | `TOKENS_V2.err` | `T.border` |

**Tag 5-tone**（h20；padding 0 8；fs11；radius **4** 硬编；mono；letterSpacing 0.02em；**lowercase**）：
| tone | bg | fg | border |
|---|---|---|---|
| neutral | `T.chipBg` | `T.muted` | `T.border` |
| brand | `T.accentSoft` | `T.accent` | `TOKENS_V2.brand[200]` |
| ok | `color-mix(in oklch, ${T.success} 12%, transparent)` | `T.success`(新155°) | `color-mix(in oklch, ${T.success} 30%, transparent)` |
| warn | `color-mix(in oklch, ${T.warn} 12%, transparent)` | `T.warn`(新75°) | `color-mix(in oklch, ${T.warn} 30%, transparent)` |
| err | `color-mix(in oklch, ${TOKENS_V2.err} 12%, transparent)` | `TOKENS_V2.err` | `color-mix(in oklch, ${TOKENS_V2.err} 30%, transparent)` |

> demo 用 `oklch(L C H / 0.12)` 字面 alpha；**产品改 `color-mix(in oklch, X 12%, transparent)`** 对齐 9 屏 brandSoft inset 铁律（R-286/R-372）。dumb 无硬编 margin（间距由宿主 gap 决定 — Stage 2 Q3 采纳）。执行者照表翻译，禁自揣 variant 配色。

### §9.3 buildTheme 3 值变（successSoft 补 — 守护者 Gap 1）
`success oklch(72% 0.16 155)` + `successSoft oklch(72% 0.16 155 / 0.16|0.10)`（随 success 同步，否则实心绿与 soft tint 异相）+ `warn oklch(78% 0.16 75)`；brand/ink/accentSoft(195°)/其余 0 改；25 键名 byte-equal。App.css fallback 不含语义色 → no-op。

### §9.4 TOKENS_V2 范围（守护者收紧 — 仅 theme-无关）
`export const TOKENS_V2 = { brand:{50..900}, ink:{0..950}, radius:{xs..pill}, ok, warn, err, info }`（镜像 tokens.js theme-无关部分）。**严禁**承载 theme-split 结构色（bg/border/text 是 buildTheme 的活，避免双真相源）。

### §9.5 图标 additive（F4 真红线 — 替 Stage 2 幻觉）
新增 ~16 key（attach/flask/edit/chat/node/flow/arrow/spark/catalog/budget/audit/bookmark/bookmarkFill/medal/info/pin）：**0 撞现有 38 名 + viewBox `0 0 24 24` + stroke 1.6**（与 38 一致）；38 既有 byte-equal 不碰。重命名对（I.gear↔cog / chev↔chevron / dl↔download / x↔close / zap↔bolt）复刻时映射，本刀不动 38 名。Stage 4 grep 实证。

### §9.6 LIMIT（守护者 Gap 3 + 资深拍 B）
Shared 720→**760**（+TOKENS_V2+图标 ~+80）；`primitives.jsx` [NEW] LIMIT ~150（Btn/Tag）。Stage 2 的 620 作废。UI v2 系列文件形态先例（后续屏复刻 helper 优先入 primitives/专属文件，Shared 不无界膨胀）。

### §9.7 R-PA-PB-V1 松绑 ack（资深 2026-06-17 记档）
success/warn/successSoft 微调 = **UI v2 sanctioned 视觉演进，非破 R-PA-PB-V1 铁律**。18 屏自动换皮变色是预期。后续屏复刻不得拿「0 视觉漂移」当矛盾。

### §9.8 红线 verdict
R-UI2-1 修订（3 值含 successSoft）/ R-UI2-2 修订（0 代码 diff ≠ 0 视觉 + VRP 松绑 ack）/ R-UI2-3 保留 / R-UI2-4 保留+补强（§9.2 映射表）/ R-UI2-5 保留（Shell 13 props/App/utils 0 改）/ R-UI2-6 修订（B 拆 primitives + Shared 760）/ R-UI2-7 保留。

### §9.9 待 commit 1（§九放行闸门自证）
npm build + diff screens/Shell/App 0 行 + 25 键名 byte-equal + 3 值新 + 旧 export byte-equal + 新图标 0 撞名/viewBox24 + §9.2 映射表落地 + check_file_sizes 绿 + R-PA-8。**3 项资深拍板全清（B 拆文件 / A 延续守护者 / VRP 松绑记档）→ 起 commit 1。**

### §9.10 commit-by-commit 守护者复核归档（守护者归档义务）
- **commit 1 复核 `3e16c98`**（守护者直读 worktree 亲验，不采信书面）= **ACK + 放行 commit 2**：§九闸门全验（screens/Shell/App/utils/backend 全 0 diff + buildTheme 25 名仅 3 值变 + 旧 export 0 触碰 + I 54 名 54 unique 0 撞名 viewBox24 + 守护者亲跑 check_file_sizes 62 files；build 采信 temp-build 265s + App 还原 diff 0）；**§9.2 映射表 byte-faithful**（Btn 5-variant/Tag 5-tone 逐行吻合 + color-mix in oklch 采纳 + 未误植 demo StatusDot）；Gap 1/2/3 全落 + Stage 2 三错（LIMIT 560/F4 幻觉/接-T-未解）实证绕过 + TOKENS_V2 theme-无关确认。
- **commit 2 收官**（本 commit）= version 0.6.4.0 三同步（main.py + smoke R-72 + Login footer R-181）+ README L11/技术栈 + CLAUDE 路线图 UI v2 系列行 + CHANGELOG（demote 3.2→Released + 0.6.4.0 段 + **§Notes VRP 松绑记档**）+ check_file_sizes stale 注释清（守护者 nit #4）+ §9.10 归档 + 静态重建。
- **守护者前瞻 nit #5（登记）**：TOKENS_V2.ok/warn 与 buildTheme success/warn 当前同值但 primitives Tag 用 T.success/T.warn → 单一同步源纪律（后续屏复刻/调色须两处同改，勿单改 drift）。
- 待守护者 commit 2 收官复核监视清单 5 项 → push/PR → UI v2 Foundation刀完结。

### §9.11 commit 3 CI-fix — 段3 Foundation 冻结守护 re-baseline（全链漏检 → CI 兜底）
**PR #139 CI lint-test FAIL**（push 后）：`tests/test_foundation_additive.py`（段3 v0.6.2.3 立）的 **R-PB-SH-1**（Shared.jsx 既有 decl `I`/`buildTheme` byte-equal 冻结 vs baseline 快照）+ **R-PB-SH-4**（I=38）拦下本刀（buildTheme 3 值微调 = modification 非 additive + I 38→54）。
- **全链漏检**：执行者 Stage 1 + Stage 2 + 守护者 Stage 3 + commit 1 复核 + 资深 — **均盯 R-158/R-156，漏记段3 byte-equal 冻结守护**。CI 干净 env 兜底（印证本机坏 python 下 CI 不可替代）。
- **根因 = 治理冲突非 bug**：UI v2 本质就是演进 Foundation（改 buildTheme + 扩图标），段3 守护把它冻结。**v0.6.4.0 = sanctioned re-baseline 点**（同 v0.6.2.3 retire R-365 时 re-baseline；Foundation 守护"演进非撤回"）。
- **commit 3 修**（资深 2026-06-17 拍 + 守护者复核）：① `tests/foundation/shared_foundation_base.jsx` re-baseline → UI v2 Shared（byte-identical；R-PB-SH-1/2 通过；future v0.6.4.1+ 对此 additive）② `test_foundation_additive.py` R-PB-SH-4 38→54 + docstring 记 UI v2 演进 re-baseline。R-PB-SH-3（26 keys）不动。mirror-verify：SH-1/2 0 drift + SH-4 54 + SH-3 26 全绿。
- **教训登记**：未来重大 Foundation 节点（设计系统演进）须**前置查 test_foundation_additive.py R-PB-SH-1/3/4 冻结契约** + 同 PATCH re-baseline，纳入屏复刻 checklist。
- **⚠️ 守护者更深发现（比"v0.6.4.0 漏记"严重）— baseline 此前已 stale 126 行**：实证 origin/main(0f67b5f) 的 `shared_foundation_base.jsx` vs `Shared.jsx` = **126 行 diff**（PeriodTab/TagChip 等 v0.6.2.4 后期 helper 老基线 0 命中 / main Shared 命中）→ **additive 守护 R-PB-SH-1/2 对这 126 行自 ~v0.6.2.4 起形同虚设**（守护以为在保护，实际盲区）。re-baseline 顺带追平（126 行皆 v0.6.2.4 已过审 merged 合法代码，**不构成洗白** — 反洗白验：新基线 − main Shared = 21 处全 = 已批准 v0.6.4.0 delta）。守护者反洗白三连过 + concur。
- **#3 utils 基线实证 = 0 diff**（与 main utils 同步，未 stale，无需 re-baseline）。
- **守护者系统性建议（3 条，待资深定是否本 PATCH commit 4 落 vs 跟进）**：① 本 §9.11 已补 stale-126 实证 ② **CI 加 baseline==Shared 强制同步守护**（每动 Shared 的 PATCH diff≠0 即红，防 baseline 再 silent drift；⚠️ 此为守护语义决策 — mirror 同步 vs 既有 additive byte-freeze）③ utils 基线已验 0 diff 干净。

---

## §附 UI v2 全系列 roadmap（草案 — 待资深确认排序）

v0.6.4.0 Foundation刀（本 PATCH）→ v0.6.4.1+ 逐屏复刻（1屏1PATCH，镜像 v0.5.7~v0.5.21）：
login / shell（floating inset 侧栏）/ home(ChatEmpty) / thinking / chat-results(ResultBlock) / favorites / datasources / api-model / catalog / knowledge / fewshot / prompts / system / budget / audit / recovery / admin-metrics / admin-history / admin-errors / auth(Enroll) / ForceChangePassword —— 约 18-21 屏。每屏按对应 artboard 复刻布局 + 迁移 Btn/Tag + 保真实契约。收官后 → v0.7.0 语义层。
