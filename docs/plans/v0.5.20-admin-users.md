# v0.5.20 — (Cn+) admin/users 视觉偿还 — LOCKED (Stage 1+2+3 整合 → Stage 4 准备)

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **协议**：Loop Protocol v3 — 三阶段评审闭环（**第 21 次施行**）
> **里程碑**：**R-376 hex 债务正式清偿**（v0.5.16 hold 4 PATCH 偿还 — v0.5.x 最长 hold 历史性纪录）+ **TabAccess 模块正式进入全 OKLCH/T-System 时代** + **Inset 8% 闭环第七处扩展**（6 文件恒定深耕）+ **R-518/519/520 守护立约**

---

## 1. 决议表 + 风险项最终裁定（D1-D6 + Q1-Q5 全部锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** Scope 扩张 thead 同步 | ✅ **准许** | Stage 2 — 防 UI 破窗；视觉割裂不可接受 |
| D2 Avatar 22 brandSoft 8% | ✅ A | Stage 1 |
| D3 thead 视觉升级 | ✅ A | Stage 2 准许同步升级 |
| D4 Status text byte-equal | ✅ A | Stage 1 |
| **D5** 5-col 几何守恒 | ✅ **支持 5-col**（业务字段自然体现） | Stage 2 判定 |
| D6 LIMIT dict 不动 | ✅ A | Stage 1 |
| Q1 Sources 部分绝对不动 | ✅ A | Stage 1 + R-519 强化 |
| Q2 业务字段 byte-equal | ✅ A | Stage 1 |
| Q3 thead 中文 labels | ✅ A | Stage 1 |
| Q4 "正常/已停用" 文案 | ✅ A | Stage 1 |
| Q5 v0.6 Shared 移植承诺 | ✅ A | Stage 1（Avatar inline 第四次复用确认） |

11/11 锁定；Stage 2 在 D1 + R-511/R-495 + D5 强化 + Codex 加码 R-516（Avatar 居中精度）+ R-517（Row hover）+ Stage 3 加码 R-518（余效 grep）+ R-519（绝对零度）+ R-520（roleChip 不动）；其余 Stage 1 保留。

---

## 2. 红线最终清单（R-496 ~ R-520，25 条）

### Stage 1 草案原 20 条（保留）

| ID | 摘要 |
|---|---|
| R-496 | TabAccess 9 props 签名 byte-equal |
| R-497 | u 5 字段 byte-equal（id/display_name/username/role/is_active） |
| R-498 | roleChip(u.role) 调用 byte-equal |
| R-499 | onEditUser(u) / onDeleteUser(u.id) 调用 byte-equal |
| R-500 | Avatar 22×22 brandSoft 8%（与 AdminAudit/AdminRecovery 字面 byte-equal） |
| R-501 | Avatar bg `linear-gradient(...#ff7a3a)` → brandSoft 8%（闭环第七处扩展） |
| R-502 | Avatar 字母 color `'#fff'` → T.accent |
| R-503 | Avatar display `inline-flex` 居中 + flexShrink: 0 |
| R-504 | thead bg → brandSoft 8%（本文件第二处命中） |
| R-505 | thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext |
| R-506 | Row minWidth: 0 + ellipsis 兜底（R-396 sustained） |
| R-507 | Status text "正常"/"已停用" + color byte-equal |
| R-508 | grep hex 0 命中 |
| R-509 | grep `#ff7a3a` = 0 |
| R-510 | grep `'#fff'\|"#fff"` = 0 |
| R-511 | R-495 字面 byte-equal 严防 sustained — 6 文件恒定 |
| R-512 | Sources 部分 L35-87 字面零修改 |
| R-513 | 范围守护（App/api/.../17 屏/Admin/...）byte-equal |
| R-514 | admin/ 其他 4 子模块 + chat/ 7 子模块 0 改 |
| R-515 | 三处版本同步 0.5.19→0.5.20 + LIMITS dict 不动 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-516** | **Avatar 字母垂直居中补偿** — inline-flex 模式下 T.sans 跨系统渲染差异；必须 `lineHeight: 1` + `fontSize: 10.5` 像素级垂直居中 | Avatar inline style 含 `lineHeight: 1, fontSize: 10.5`；commit 1 后 Chrome/Safari 跨浏览器视觉校验字母居中 |
| **R-517** | **Row Hover 状态机** — Users 行 Hover bg 必须与 Sources byte-equal；切换 Tab 触感一致 | commit 1 起手前**强制** grep Sources 行 hover 实现（如有）；如 Sources 现有 hover style → Users 沿用字面 byte-equal；如 Sources 无 hover → Users 不引入新 hover（避免不对称） |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-518** | **R-376 债务清偿余效验证** — Step 1 后额外 grep：`grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/screens/admin/tab_access.jsx`；除 boxShadow 透明度 hex 外，**结果必须 0** | commit 1 Step 1 完成后立即执行；任何残留立即清；这是 v0.5.x 视觉治理的硬指标 |
| **R-519** | **Sources 部分绝对零度** — 编辑时严禁 IDE 自动格式化；防非故意空格/缩进污染 | 关闭 IDE format-on-save；commit 1 收尾 `git diff --unified=0 origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx` 必须显示 L35-87 区间 0 改动 |
| **R-520** | **RoleChip 装饰豁免界限** — 保留 `roleChip(u.role)` 调用；严禁重构 roleChip 内部实现 | grep `roleChip` 调用 byte-equal；不动 Admin.jsx 内 roleChip 定义（R-513 sustained）；维持 v0.5.16 "业务字段不动" 红线 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §3 验收核心 — Avatar 22×22 Retina 清晰度 + hex 清零

```bash
# 1. R-518 R-376 债务清偿余效验证（Stage 3 守护者硬指标）
grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/screens/admin/tab_access.jsx
# 预期：0 行（除 boxShadow 透明度 hex 例外，但本 PATCH tab_access 无 boxShadow）
# 任何残留 = R-376 债务未清偿 = Stage 3 否决

# 2. R-511 6 文件恒定深耕
git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# 预期：仍 6 文件命中（ResultBlock/SavedReports/admin/tab_access/AdminAudit/AdminBudgets/AdminRecovery）
# tab_access.jsx 内部命中数可增加（Avatar + thead）但文件总数恒定 6

# 3. R-519 Sources 绝对零度
git diff --unified=0 origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx
# L35-87 区间必须 0 改动；空格/缩进 0 污染
```

### Stage 3 §2 实施核心顺序（4 步小型 PATCH — commit 1 子步骤锁死）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + **R-501 Avatar brandSoft 8% 字面落地** + **R-504 thead bg 升级** + **R-518 R-376 债务清偿余效 grep 立即执行**（Stage 3 守护者要求）| R-501/R-504/R-518 |
| 2 | Avatar 22×22 + T.accent 文字 + inline-flex 居中 + flexShrink: 0 + **R-516 lineHeight: 1 + fontSize: 10.5** | R-500/R-502/R-503/R-516 |
| 3 | thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext + Row minWidth: 0 + ellipsis 兜底 + **R-517 hover 与 Sources byte-equal** | R-505/R-506/R-517 |
| 4 | 三处版本同步 + R-511 字面严防 grep（6 文件恒定）+ R-508/509/510 hex 0 + **R-519 git diff --unified=0 Sources 绝对零度验证** + **R-520 roleChip 不动** | R-515/R-508~511/R-519/R-520 |

### Stage 2 R-517 hover 实施

```bash
# 探查 Sources 行 hover 实现
grep -nE "onMouseEnter|onMouseLeave|hover|:hover" frontend/src/screens/admin/tab_access.jsx
```

- **场景 A**：Sources 行有 hover state 实现 → Users 行**字面沿用** byte-equal
- **场景 B**：Sources 行无 hover state → Users 行**不引入**新 hover（保持不对称即合理）

如 Sources 当前无 hover state（最可能），R-517 = "保不引入" 而非 "强制对齐"。

### Stage 3 R-519 IDE 格式化纪律

1. 关闭 IDE format-on-save（VSCode: `editor.formatOnSave: false`）
2. 关闭 ESLint auto-fix（如有）
3. 仅手动编辑 L9-33（Users 部分）
4. 严禁触碰 L35-87（Sources 部分）— 即使光标经过也不修改字符

commit 1 收尾**强制** `git diff --unified=0 origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx` 验证 L35-87 区间 0 改动。

### Stage 3 R-520 roleChip 边界

`roleChip` 函数定义在 Admin.jsx（父屏）；本 PATCH 仅作为 prop 接收调用：
- ✅ 允许：`{roleChip(u.role)}` 调用 byte-equal
- ❌ 严禁：触碰 Admin.jsx 内 roleChip 定义
- ❌ 严禁：本 PATCH 内重写 roleChip（即使 inline）

维持 v0.5.16 "业务字段不动" 红线 sustained。

### Stage 2 R-516 Avatar 字母居中精度（与 AdminAudit R-410 + AdminRecovery R-479 字面 byte-equal）

```jsx
<div style={{
  width: 22, height: 22, borderRadius: '50%',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
  color: T.accent,
  display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
  fontSize: 10.5, fontWeight: 600, lineHeight: 1, flexShrink: 0,
}}>
  {(u.display_name || u.username || '?').slice(0, 1).toUpperCase()}
</div>
```

commit 1 后 Chrome/Safari 双浏览器跨平台视觉校验。

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/admin/tab_access.jsx  [EDIT 88 → ≤ 100 行]
                                           Step 1: baseline + R-501 Avatar brandSoft 8% 字面 + R-504 thead bg 升级
                                                   + R-518 R-376 债务清偿余效 grep 立即执行
                                           Step 2: Avatar 22×22 + T.accent + inline-flex + R-516 lineHeight:1 + fontSize:10.5
                                           Step 3: thead mono + 0.06em + 500 + T.subtext + Row minWidth:0 + ellipsis
                                                   + R-517 hover byte-equal Sources
                                           Step 4: 三处版本同步 + R-511 字面严防 grep（6 文件恒定）
                                                   + R-519 git diff --unified=0 Sources 绝对零度验证
                                                   + R-520 roleChip 不动
                                           守护: 9 props + 5 字段 + roleChip + 2 handlers byte-equal [R-496~R-499]
                                                 + Sources L35-87 字面 0 改 [R-512 + R-519]
                                                 + IDE format-on-save OFF [R-519 起手纪律]
                                                 + inline styles only [R-513 CSS 0 污染]

knot/main.py                          [EDIT version 0.5.19 → 0.5.20]  [R-515]
tests/test_rename_smoke.py            [EDIT R-72 smoke 0.5.19 → 0.5.20]  [R-515]
frontend/src/screens/Login.jsx        [EDIT 页脚字面 0.5.19 → 0.5.20]  [R-515]
scripts/check_file_sizes.py           [unchanged]  R-515 LIMITS dict 不动（tab_access 88→≤100 << 250 LIMIT）

frontend/src/screens/admin/{tab_resources,tab_knowledge,tab_system,modals}.jsx  [unchanged]  [R-514]
frontend/src/screens/chat/*           [unchanged]  [R-514]
frontend/src/screens/Admin.jsx        [unchanged]  [R-513 + R-520 roleChip 定义保护]
frontend/src/screens/SavedReports.jsx [unchanged]  [R-513]
frontend/src/screens/AdminAudit.jsx   [unchanged]  [R-513]
frontend/src/screens/AdminBudgets.jsx [unchanged]  [R-513]
frontend/src/screens/AdminRecovery.jsx [unchanged] [R-513]
frontend/src/screens/ (其余 13 屏)     [unchanged]  [R-513]
frontend/src/App.jsx                  [unchanged]  [R-513]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-513]
frontend/src/decor/NarrativeMotif.jsx [unchanged]  [R-513]
frontend/src/App.css                  [unchanged 0 行 diff]  [R-513]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js  [unchanged]
.github/workflows/ci.yml              [unchanged]
backend (knot/) 任何文件除 main.py    [unchanged]

CHANGELOG.md                          [EDIT [Unreleased] v0.5.20 段]
                                      - R-496~R-520 偿还表
                                      - ⭐ KNOT 管理端视觉治理：TabAccess 模块正式进入全 OKLCH/T-System 时代
                                      - R-376 hex 债务正式清偿（v0.5.16 hold 4 PATCH 偿还 — v0.5.x 最长 hold 历史性纪录）
                                      - tab_access 视觉一致性收官（Users + Sources 整屏统一）
                                      - Inset 8% 闭环字面第七处扩展（Avatar bg + thead 本文件双处命中；6 文件恒定深耕）
                                      - R-495 字面 byte-equal 严防 sustained（git grep -F 6 文件恒定）
                                      - R-518 R-376 余效验证立约（v0.5.x 视觉治理硬指标）
                                      - R-519 Sources 绝对零度（IDE format-on-save 关闭纪律）
                                      - R-520 roleChip 装饰豁免界限（业务字段不动红线延续）
                                      - Avatar inline 第四次复用确认 — v0.6 Shared 移植承诺加强（7+ helpers）
                                      - Loop Protocol v3 第 21 次施行
CLAUDE.md                             [EDIT]
                                      - § v3 协议施行历史回顾表 +1 行 v0.5.20（R-376 hex 清偿 + TabAccess 全 OKLCH/T-System 时代）
                                      - v0.5.x 路线图 v0.5.20 ⏳ → ✅
                                      - v0.6 路线图加强 "7+ inline helpers 移入 Shared.jsx 技术债偿还" 承诺
docs/plans/v0.5.20-admin-users.md     [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS 不变）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.20**

### Stage 2/3 强化验证

- [ ] **R-518 R-376 债务清偿余效**：`grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/screens/admin/tab_access.jsx` = 0（除 boxShadow 透明度 hex；本 PATCH tab_access 无 boxShadow → 严格 0）
- [ ] **R-509 #ff7a3a 0 命中**：grep `#ff7a3a` tab_access.jsx = 0
- [ ] **R-510 '#fff' 0 命中**：grep `'#fff'\|"#fff"` tab_access.jsx = 0
- [ ] **R-511 6 文件恒定**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — 仍 **6 文件命中**
- [ ] **R-516 Avatar 居中精度**：grep `lineHeight: 1.*fontSize: 10\.5` Avatar 区命中；Chrome/Safari 跨浏览器视觉字母居中
- [ ] **R-517 Row hover**：起手 grep Sources hover 实现确认场景 A/B
- [ ] **R-519 Sources 绝对零度**：`git diff --unified=0 origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx` L35-87 区间 0 改动
- [ ] **R-520 roleChip 调用 byte-equal**：grep `roleChip(u\.role)` 命中；Admin.jsx git diff = 0
- [ ] **R-500 Avatar 22×22**：grep `width: 22, height: 22.*borderRadius:\s*['"]50%['"]` Users 区命中

### 契约守护（首要红线）

- [ ] **R-496**：TabAccess 9 props 签名 byte-equal — 函数签名行字面 byte-equal
- [ ] **R-497**：u 5 字段 byte-equal（id/display_name/username/role/is_active）
- [ ] **R-498**：roleChip(u.role) 调用 byte-equal
- [ ] **R-499**：onEditUser(u) / onDeleteUser(u.id) 调用 byte-equal

### 范围守护

- [ ] **R-512 + R-519 双重 Sources 严守**：L35-87 区间 0 行 diff（含空格/缩进）
- [ ] **R-513**：App/api/index.css/main/utils/Shared/Shell/decor/17 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery `git diff` = 0
- [ ] **R-514**：admin/ 其他 4 子模块 + chat/ 7 子模块 `git diff` = 0
- [ ] App.css `git diff` = 0
- [ ] grep `KnotLogo|KnotMark|KnotWordmark` 仍 3 文件命中

### 端到端业务回归

- [ ] 进 admin → 切 admin-users tab → users 加载
- [ ] **Avatar 22 brandSoft 8% T.accent 字母 lineHeight:1 居中**（与 AdminAudit / AdminRecovery 视觉一致）
- [ ] **长 username / display_name 不挤出操作按钮**（R-506 ellipsis 兜底）
- [ ] 编辑按钮 → 调 onEditUser(u)
- [ ] 停用按钮 → 调 onDeleteUser(u.id)
- [ ] is_active 切换 → "正常" / "已停用" + color byte-equal
- [ ] roleChip(u.role) → role 装饰渲染（R-520 不动定义）
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Users 列表布局自适应
- [ ] **light + dark 双模式** — Avatar / thead / row 视觉一致
- [ ] **切 Sources tab → 视觉完全 byte-equal**（R-512/R-519 sustained）
- [ ] **⭐ Inset 8% 7 屏 sustained 视觉一致**（v0.5.14/15/16/17/18/19/20 — 6 文件恒定 + tab_access 本文件深耕）
- [ ] **⭐ Retina 屏 Avatar 22×22 清晰度校验**（Stage 3 §3 验收关键）
- [ ] v0.5.0~v0.5.19 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.20): tab_access.jsx Users 视觉偿还 — ⭐ R-376 hex 债务清偿 + Avatar 22 brandSoft 8% + thead Inset 8% 第七处扩展 (R-496~R-520)` | npm build + R-518 余效 grep 0 + R-511 6 文件恒定 + R-519 Sources 绝对零度 + R-516 Avatar 居中 + 端到端业务 + 三档窗宽 + 双模式 | 9 props + 5 字段 + roleChip + 2 handlers byte-equal + Step 1→4 顺序 + IDE format OFF + L35-87 0 改 |
| 2 | `chore(v0.5.20): version bump 0.5.19→0.5.20 三处同步 (R-515) — LIMITS dict 不动` | 全闸门 + routes=72 + version=0.5.20 | R-515 三处同步 grep ≥ 3 |
| 3 | `docs(v0.5.20): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + ⭐ KNOT 管理端视觉治理 TabAccess 全 OKLCH/T-System 时代 + R-376 hex 债务清偿 + Inset 8% 第七处扩展 + R-518/519/520 守护立约 + Avatar v0.6 Shared 移植加强` | 全闸门 | Loop Protocol v3 第 21 次施行 + R-376 历史性偿还 + TabAccess 视觉治理收官 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→4 顺序不可换；**R-501 Avatar 字面 + R-504 thead 字面率先落地**（沿用 v0.5.17~19 模式）；**R-518 + R-519 双 grep 验证**为 commit 1 收尾必经闸门。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 21 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 25（v0.5.20 区段 R-496~R-520）
- ✅ **R-376 hex 债务正式清偿** — v0.5.16 hold 4 PATCH 偿还（最长 hold 历史性纪录）
- ✅ **TabAccess 模块正式进入全 OKLCH/T-System 时代** — Stage 3 §3 里程碑宣告
- ✅ **Inset 8% 闭环字面第七处扩展** — 6 文件恒定深耕（无新增文件；tab_access 内部命中数从 1 处扩至 3 处 — Sources thead + Avatar + Users thead）
- ✅ **R-495 字面 byte-equal 严防 sustained** — 6 文件恒定 git grep -F
- ✅ **R-518 R-376 余效验证立约** — v0.5.x 视觉治理硬指标制度化
- ✅ **R-519 Sources 绝对零度** — IDE format-on-save 关闭纪律确立
- ✅ **R-520 roleChip 装饰豁免界限** — v0.5.16 "业务字段不动" 红线延续
- ✅ **R-516 Avatar 跨浏览器精度** — inline-flex + lineHeight:1 + fontSize:10.5 三件套
- ✅ **D5 几何守恒** — 业务字段自然体现（Users 5-col vs Sources 6-col）
- ✅ **Avatar inline 第四次复用确认** — v0.6 Shared 移植承诺加强（累计 7+ inline helpers）
- ✅ R-227.5.1 装饰豁免延伸（thead 中文 + roleChip）
- ✅ R-302.5 emoji 业务豁免 sustained（本 PATCH 无 emoji）
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.20 八 PATCH sustained）
- ✅ R-313 rgba 豁免边界 sustained（无新增豁免）
- ✅ R-396 列宽稳定性 sustained
- ✅ R-394 auto-fit 响应式 sustained
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 18 屏 + Sources 部分 byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ **归档锁定手册** → 当前文件；分支 `feat/v0.5.20-admin-users`
2. ✅ **R-519 IDE format-on-save 关闭纪律**：commit 1 起手**第一步**关闭 IDE format
3. ✅ **R-518 R-376 余效验证纪律**：Step 1 后**立即**执行 hex grep = 0
4. ✅ **R-517 hover 起手前置探查**：grep Sources hover 实现 — 确认场景 A/B
5. ✅ **R-516 Avatar 跨浏览器实测**：lineHeight:1 + fontSize:10.5
6. ✅ **R-520 roleChip 边界严守**：不动 Admin.jsx 定义
7. ✅ **R-511 6 文件恒定纪律**：commit 1 收尾**强制** git grep -F 6 文件命中
8. ✅ **R-512 + R-519 双重 Sources 严守**：`git diff --unified=0` L35-87 0 改
9. ✅ **里程碑叙事**：⭐ TabAccess 全 OKLCH 时代 / R-376 历史性偿还 / 第七处扩展 / R-518/519/520 守护立约
10. ✅ **技术债登记加强**：CHANGELOG 列 v0.6 Shared 7+ helpers 清单
11. ✅ **范围抗诱惑**：不抽 Avatar 入 Shared / 不动 roleChip / 不改 Sources
12. ✅ **Retina 验收**：fontSize 10.5 锐利无锯齿
13. ✅ **历史性纪录**：v0.5.x 最长 hold 红线偿还；不简化协议

---

🛡️ **执行者收到 GO 信号；v0.5.x 最长 hold 红线 R-376 hex 债务正式清偿；TabAccess 模块全 OKLCH/T-System 时代来临；Inset 8% 闭环第七处 6 文件恒定深耕 + R-518/519/520 守护立约 + Avatar v0.6 Shared 移植承诺加强四大里程碑同时落地。**
