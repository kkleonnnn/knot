# v0.5.20 — (Cn+) admin/users 视觉偿还 — Stage 1 草案

> **状态**：📝 **Stage 1 草案**（待 Stage 2 资深+Codex 评审 / Stage 3 v0.4 守护者终审）
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **里程碑**：**R-376 hex 债务清偿** + **tab_access 视觉一致性收官**（Users + Sources 整屏统一）+ **Inset 8% 闭环第七处扩展**（Avatar bg 命中）+ **R-495 字面 byte-equal 严防 sustained**

---

## 0. 关键事实（Stage 1 起手陈述）

**v0.5.16 R-376 hex 债务正式清偿** — `tab_access.jsx` Users 部分（L9-33）含 v0.5.x 最后两处 hex 残留：

| Hex | 位置 | 偿还方案 |
|---|---|---|
| `linear-gradient(135deg, ${T.accent}, #ff7a3a)` | L17 Avatar bg（橘色渐变） | → `color-mix(in oklch, ${T.accent} 8%, transparent)`（R-480 闭环第七处） |
| `color: '#fff'` | L17 Avatar 字母 | → `color: T.accent`（与 brandSoft 8% bg 形成 brand 单色和谐） |

**裁定**：本 PATCH 是**视觉一致性收官**（非 1:1 demo 复刻 — 无 users.jsx demo），借用 v0.5.17 AdminAudit + v0.5.19 AdminRecovery 已确立的 Avatar 22 brandSoft 8% 模式。

---

## 1. 决议表（D1-D6 + Q1-Q5 草案 — 待 Stage 2/3 终审）

| ID | 推荐 | 备注 |
|---|---|---|
| **D1** 范围 | **B 含 thead 视觉对齐**（含 thead 升级至 brandSoft 8%）| 仅清 hex 但保 thead 旧风格会导致 Users vs Sources 视觉割裂 — 不可接受 |
| **D2** Avatar pattern | A 22×22 brandSoft 8% + T.accent 文字 + `inline-flex` 居中 | 与 v0.5.17 AdminAudit R-410 + v0.5.19 AdminRecovery R-479 模式一致 |
| **D3** thead 视觉升级 | A brandSoft 8% + mono + 0.06em + uppercase + fontWeight 500 | 与 Sources thead 字面 byte-equal — R-480 闭环本文件第二处命中 |
| **D4** Status text 处理 | A 保 current `is_active ? T.success : T.muted` byte-equal | 业务字段不动；不引入 StatusDot（避免范围扩张） |
| **D5** gridTemplateColumns | A `1.5fr 1fr 0.8fr 0.8fr 80px` byte-equal | 不动 — 仅视觉风格升级，不动几何布局 |
| **D6** 行数预算 | A LIMIT 250 不动 | tab_access.jsx 88 → ≤ 100；远低于 250 阈值；LIMITS dict 不动 |
| **Q1** 范围严守 Sources 部分 | A 绝对不动 | v0.5.16 byte-equal sustained；`git diff origin/main -- L35-87` = 0 行 |
| **Q2** 业务字段 byte-equal | A 5 字段（u.id/display_name/username/role/is_active）+ 9 props 签名 + roleChip 调用 | TabAccess 9 props 不动；R-374 sustained |
| **Q3** thead 中文 labels | A 保 byte-equal | "用户/账号/角色/状态" 不动（R-227.5.1 装饰豁免延伸） |
| **Q4** "正常/已停用" 文案 | A byte-equal | 业务文案不动 |
| **Q5** v0.6 Shared 移植承诺 | A sustained | Avatar inline 复制（与 AdminAudit/Recovery 一致）— v0.6 一并移入 Shared |

---

## 2. 红线草案（R-496 ~ R-515，20 条）

### 契约守护（R-496 ~ R-499）

| ID | 内容 |
|---|---|
| **R-496** | `TabAccess` 9 props 签名 byte-equal（T / tab / users / sources / onEditUser / onDeleteUser / onEditSource / onDeleteSource / roleChip） |
| **R-497** | u 业务字段 5 个 byte-equal — id / display_name / username / role / is_active |
| **R-498** | roleChip 函数调用方式 byte-equal — `roleChip(u.role)`（不改 props 签名） |
| **R-499** | onEditUser / onDeleteUser 调用 byte-equal — `onEditUser(u)` / `onDeleteUser(u.id)` |

### Users 视觉重构（R-500 ~ R-507）

| ID | 内容 |
|---|---|
| **R-500** | Avatar 26×26 → 22×22 brandSoft 8%（**与 AdminAudit R-410 + AdminRecovery R-479 字面 byte-equal**） |
| **R-501** | Avatar bg `linear-gradient(...#ff7a3a)` → `color-mix(in oklch, ${T.accent} 8%, transparent)`（**R-480 闭环字面第七处扩展**） |
| **R-502** | Avatar 字母 color `'#fff'` → `T.accent` |
| **R-503** | Avatar display `grid + placeItems: center` → `inline-flex + alignItems: center + justifyContent: center` + `flexShrink: 0` |
| **R-504** | thead bg `T.bg` → `color-mix(in oklch, ${T.accent} 8%, transparent)`（**R-480 闭环字面第二处本文件命中**） |
| **R-505** | thead color `T.muted` → `T.subtext` + fontFamily `T.mono` + letterSpacing `0.03em` → `0.06em` + fontWeight `600` → `500` |
| **R-506** | Row 内 minWidth: 0 + textOverflow ellipsis 兜底（**R-396 sustained 列宽稳定**） |
| **R-507** | Status text 字面 byte-equal "正常"/"已停用" + color `is_active ? T.success : T.muted` byte-equal（业务字段不动） |

### 通用约束守护（R-508 ~ R-511）

| ID | 内容 |
|---|---|
| **R-508** | grep `#[0-9a-fA-F]{3,6}` tab_access.jsx \| grep -v boxShadow \| grep -v rgba = 0 命中（**v0.5.16 R-376 hex 债务正式清偿**） |
| **R-509** | grep `linear-gradient.*#ff7a3a` tab_access.jsx = 0 命中 |
| **R-510** | grep `'#fff'\|"#fff"` tab_access.jsx = 0 命中 |
| **R-511** | **R-495 字面 byte-equal 严防 sustained** — `git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` 命中数 6 文件不变（Avatar 字面 byte-equal R-480 闭环；可以新增更多命中但 byte-equal 严守） |

### 范围 + 契约（R-512 ~ R-515）

| ID | 内容 |
|---|---|
| **R-512** | **Sources 部分（L35-87）字面零修改** — `git diff origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx` 中 Sources 段 0 diff（v0.5.16 byte-equal sustained） |
| **R-513** | App/api/index.css/main/utils/Shared/Shell/decor/17 屏（含 SavedReports/AdminAudit/AdminBudgets/AdminRecovery）/Admin/Admin.jsx 0 改 |
| **R-514** | admin/ 其他 4 子模块（tab_resources/knowledge/system/modals）0 改 + chat/ 7 子模块 0 改 |
| **R-515** | 三处版本同步 0.5.19 → 0.5.20（main.py + smoke + Login.jsx）；LIMITS dict **不动**（88 → ≤ 100 远低于 250 LIMIT） |

---

## 3. 子步骤执行顺序（4 步 — 小型 PATCH）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + **R-501 brandSoft 8% 字面落地**（Avatar bg）+ **R-504 thead bg 升级** | R-501/R-504 |
| 2 | Avatar 22×22 + T.accent 文字 + inline-flex 居中 + flexShrink: 0 | R-500/R-502/R-503 |
| 3 | thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext + Row minWidth 0 + ellipsis 兜底 | R-505/R-506 |
| 4 | 三处版本同步 + R-495 字面严防 grep（6 文件 sustained）+ R-508/509/510 hex 0 + R-512 Sources 零改 | R-515/R-508~512 |

---

## 4. 关键代码意图（提前样化）

### R-501/R-504 brandSoft 8% 闭环字面（第七处扩展）

```jsx
// Avatar bg + thead bg — 同字面 byte-equal v0.5.14/15/16/17/18/19 sustained
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
```

### R-500~R-503 Avatar 升级（与 AdminAudit R-410 字面 byte-equal）

```jsx
// L17 替换 — 26→22 + 渐变 → brandSoft 8% + '#fff' → T.accent
<div style={{
  width: 22, height: 22, borderRadius: '50%',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
  color: T.accent,
  display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
  fontSize: 10.5, fontWeight: 600, flexShrink: 0,
}}>
  {(u.display_name || u.username || '?').slice(0, 1).toUpperCase()}
</div>
```

### R-504/R-505 thead 视觉升级（与 Sources thead 字面 byte-equal）

```jsx
// L11 替换 — bg T.bg→brandSoft 8% + mono + 0.06em + fontWeight 500
<div style={{
  display: 'grid', gridTemplateColumns: '1.5fr 1fr 0.8fr 0.8fr 80px',
  padding: '9px 16px',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
  fontSize: 11, color: T.subtext, fontFamily: T.mono,
  fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase',
  borderBottom: `1px solid ${T.border}`,
}}>
  <div>用户</div><div>账号</div><div>角色</div><div>状态</div><div></div>
</div>
```

### R-506 Row minWidth: 0 + ellipsis 兜底

```jsx
// 长 username / display_name 不挤出操作按钮
<span style={{ color: T.text, fontWeight: 500, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
  {u.display_name || u.username}
</span>
```

---

## 5. 文件级改动清单（草案）

```
frontend/src/screens/admin/tab_access.jsx  [EDIT 88 → ≤ 100 行]
                                           Step 1: baseline + R-501/R-504 brandSoft 8% 字面落地
                                           Step 2: Avatar 22 brandSoft + T.accent + inline-flex
                                           Step 3: thead mono+0.06em + Row ellipsis 兜底
                                           Step 4: 三处版本同步 + grep 闭门
                                           守护: 9 props + 5 字段 + roleChip + 2 handlers byte-equal
                                                 + Sources 部分 L35-87 字面 0 改 [R-512]
                                                 + inline styles only [R-513 CSS 0 污染]

knot/main.py                          [EDIT version 0.5.19 → 0.5.20]  [R-515]
tests/test_rename_smoke.py            [EDIT R-72 smoke 0.5.19 → 0.5.20]  [R-515]
frontend/src/screens/Login.jsx        [EDIT 页脚字面 0.5.19 → 0.5.20]  [R-515]

scripts/check_file_sizes.py           [unchanged]  R-515 LIMITS dict 不动（tab_access.jsx 既有 250 LIMIT 远未触达）
frontend/src/screens/admin/{tab_resources,tab_knowledge,tab_system,modals}.jsx  [unchanged]  [R-514]
frontend/src/screens/chat/*           [unchanged]  [R-514]
frontend/src/screens/{Admin,SavedReports,AdminAudit,AdminBudgets,AdminRecovery}.jsx  [unchanged]  [R-513]
frontend/src/screens/ (其余 13 屏)     [unchanged]  [R-513]
frontend/src/{App,api,index.css,main,utils,Shared,Shell}.jsx 等  [unchanged]  [R-513]
frontend/src/decor/NarrativeMotif.jsx  [unchanged]  [R-513]
frontend/src/App.css                  [unchanged 0 行 diff]  [R-513]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js  [unchanged]
.github/workflows/ci.yml              [unchanged]
backend (knot/) 任何文件除 main.py    [unchanged]

CHANGELOG.md                          [EDIT [Unreleased] v0.5.20 段]
                                      - R-496~R-515 偿还表
                                      - **R-376 hex 债务正式清偿** (v0.5.16 hold 4 PATCH 后偿还)
                                      - **tab_access 视觉一致性收官** (Users + Sources 整屏统一)
                                      - **Inset 8% 闭环字面第七处扩展** (Avatar bg + thead 双处命中)
                                      - **R-495 字面 byte-equal 严防 sustained**（git grep -F 6 文件不变）
                                      - Loop Protocol v3 第 21 次施行
CLAUDE.md                             [EDIT]
                                      - § v3 协议施行历史回顾表 +1 行 v0.5.20（R-376 hex 清偿 + 第七处闭环）
                                      - v0.5.x 路线图 v0.5.20 ⏳ → ✅
                                      - v0.5.21+ 行更新
docs/plans/v0.5.20-admin-users.md     [当前文件 Stage 1 → LOCKED 后存档]
```

---

## 6. 验收清单（草案）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（CI 干净 env）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS 不变）
- [ ] `npm run build` 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.20**

### 视觉强化验证

- [ ] **R-501 brandSoft 8% 闭环字面 sustained**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — 仍 **6 文件命中**（无新增文件 — 本 PATCH 仅扩展 tab_access 内命中数）
- [ ] **R-509 #ff7a3a 0 命中**：grep `#ff7a3a` tab_access.jsx = 0
- [ ] **R-510 '#fff' 0 命中**：grep `'#fff'\|"#fff"` tab_access.jsx = 0
- [ ] **R-508 hex 0 命中**：grep `#[0-9a-fA-F]{3,6}` tab_access.jsx \| grep -v boxShadow \| grep -v rgba = 0
- [ ] **R-500 Avatar 22**：grep `width: 22, height: 22, borderRadius: '50%'` Users 区命中
- [ ] **R-512 Sources 部分零改**：`git diff origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx` L35-87 段 0 行 diff

### 契约守护（首要红线）

- [ ] **R-496** TabAccess 9 props 签名 byte-equal
- [ ] **R-497** u 5 字段 byte-equal（id/display_name/username/role/is_active）
- [ ] **R-498** roleChip(u.role) 调用 byte-equal
- [ ] **R-499** onEditUser(u) / onDeleteUser(u.id) 调用 byte-equal

### 范围守护

- [ ] **R-513** App/api/index.css/main/utils/Shared/Shell/decor/17 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery `git diff` = 0
- [ ] **R-514** admin/ 其他 4 子模块 0 改 + chat/ 7 子模块 0 改

### 端到端业务回归

- [ ] 进 admin → 切 admin-users tab → users 加载
- [ ] **Avatar 22 brandSoft 8%**（与 AdminAudit / AdminRecovery 视觉一致）
- [ ] **长 username / display_name 不挤出操作按钮**（R-506 ellipsis 兜底）
- [ ] 编辑/停用按钮 → 调 onEditUser / onDeleteUser
- [ ] is_active 切换 → "正常" / "已停用" 颜色正确
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）
- [ ] **light + dark 双模式**
- [ ] **Sources tab 切换视觉完全 byte-equal**（R-512 sustained）
- [ ] **Inset 8% 7 屏 sustained 视觉一致**（v0.5.14/15/16/17/18/19/20）
- [ ] v0.5.0~v0.5.19 既有功能端到端正常

---

## 7. Commit 序列（草案 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.20): tab_access.jsx Users 部分视觉偿还 — R-376 hex 债务清偿 + Avatar 22 brandSoft + thead R-480 闭环 (R-496~R-515)` | npm build + 全 grep 守护 + 端到端业务 + R-512 Sources 零改 |
| 2 | `chore(v0.5.20): version bump 0.5.19→0.5.20 三处同步 (R-515) — LIMITS dict 不动` | 全闸门 + version=0.5.20 |
| 3 | `docs(v0.5.20): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + R-376 hex 债务清偿 + tab_access 视觉一致性收官 + Inset 8% 第七处扩展` | 全闸门 |

---

## 8. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审待启动（**第 21 次施行**预计）
- ✅ § Visual Replication Protocol — **本 PATCH 无 demo 锚点**（users.jsx 不存在）；视觉模式借用 v0.5.17/19（Avatar 22 brandSoft 8%）
- ⏳ 等 Stage 2 资深 + Codex 评审 → Stage 3 v0.4 守护者终审 → LOCKED
- ✅ **R-376 hex 债务正式清偿**（v0.5.16 R-376 hold 4 PATCH 后偿还 — v0.5.x 最后两处 hex 残留清零）
- ✅ **tab_access 视觉一致性收官**（v0.5.16 Sources + v0.5.20 Users 整屏统一）
- ✅ **Inset 8% 闭环字面第七处扩展**（Avatar bg + thead bg — 6 文件 sustained，无新文件，仅本文件内命中数扩展）
- ✅ **R-495 字面 byte-equal 严防 sustained** — git grep -F 6 文件命中数不变
- ✅ R-227.5.1 装饰豁免延伸（thead 中文 + roleChip mono 装饰）
- ✅ R-302.5 emoji 业务豁免 sustained（本 PATCH 无 emoji）
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.20 八 PATCH sustained）
- ✅ R-313 rgba 豁免边界 sustained（本 PATCH 无 modal/drawer）
- ✅ R-396 列宽稳定性 sustained
- ✅ 范围最小化 — 仅 tab_access.jsx Users 部分 + 三处版本同步；admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 18 屏 + Sources 部分 byte-equal

---

## 9. 风险点 + Stage 2/3 评审建议方向

### 高风险（建议 Stage 2/3 重点关注）

1. **R-512 Sources 部分严守** — `git diff` L35-87 段必须 0 行；任何意外触碰都是 v0.5.16 byte-equal 回归
2. **R-496 TabAccess 9 props 签名** — Admin.jsx 调用 byte-equal（不动 Admin.jsx）
3. **R-497 u 5 字段** — display_name 与 username 兜底链不动（`u.display_name || u.username`）

### 中风险

4. **D3 thead 视觉升级**：守护者可能质疑是否过度扩 scope？反驳：Users vs Sources 视觉割裂不可接受
5. **D5 gridTemplateColumns 不动** — 几何布局 byte-equal vs 可能与 Sources 6-col 不一致；但 Users 5-col 业务字段少于 Sources 6-col，几何不一致是合理的
6. **Q3 thead 中文 labels** — 装饰豁免延伸；不改"用户/账号/角色/状态"

### 低风险

7. **D2 Avatar pattern** — v0.5.17/19 已验证；inline 复制纪律
8. **D6 LIMITS dict 不动** — 88 → ≤ 100 远低于 250 LIMIT

---

📝 **Stage 1 草案完成 — 等待资深架构师 ack 启动 Stage 2/3 评审或简化协议直接 Stage 4。**

> **简化协议候选**：本 PATCH 红线 20 条 + 决议无争议 + 模式已成熟（v0.5.17/19 Avatar pattern + v0.5.16 thead pattern）+ scope 小 — 可考虑 v0.5.8 简化协议（跳 Stage 2/3 直 Stage 4），但 R-376 hex 清偿是历史性偿还，建议资深架构师决定。
