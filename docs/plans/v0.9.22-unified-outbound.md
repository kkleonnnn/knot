# v0.9.22 —— 出网禁令**不可遗漏** + 补上最后一处 · **Stage 1**

> ## ⭐ kk 2026-08-11 裁定：**最小方案**（不做统一 helper）
>
> **理由（我自己在 §6 提出、kk 采纳）**：禁重定向已 6/7 完成 ⇒ helper 的边际价值只剩
> 「第 8 个出网点自动带禁令」+「收拢三份重复」+「补 `or_catalog`」。
> 而**「不可遗漏」这条性质是哨兵提供的，不是 helper** —— helper 额外带来的是可维护性收益，
> 不是安全收益。⇒ 用哨兵拿到安全那半，**不付 helper 那份代价**
> （7 处生产码 + 4 条既有测处置 + 一条必须改名重写理由的哨兵）。
>
> ### 本片范围（**三件，就这三件** · Stage 2 后已二次收敛）
> 1. ⭐ **补上 `or_catalog` 那唯一无禁令的出网点** —— 取 **(a)** 三行 `_NoRedirect` opener（§1.1）；
> 2. ⭐ **哨兵：两级并存**（import 级禁换库 + 调用/值级要求字面 `False` + 属性级封 prepare-then-send，§1.4），
>    并**扩既有那条**而不是新写一份 banned 清单（§1.5）；
> 3. **代理面启动期 WARN**（⚠️ 理由已订正：代理面**今天就存在**，`urlopen` 同样走 `HTTP_PROXY`
>    —— 不是换形态引入的）。
>
> ### ⛔ 本片**明确不做**
> 统一 helper `outbound.py` · 收编那 6 处已有禁令的 sender · 收拢三份重复的 3xx 判断
> ⇒ 那三份重复**留在原地**（各自都是对的，只是没有单一真相源）
> ⇒ **§0.2 从「本片要修的」降级为「已登记的可维护性债」**。
> 另**不做**：既有哨兵的**别名盲**（登记独立 backlog）· LLM SDK 出网（独立一片，§4.11 显式豁免）。
>
> ### ⚠️⚠️ 二次裁定：**(a) 而非 (b)**（kk 2026-08-11，Stage 2 后）
> 我原先把「最小方案」解释成 **(b) 换 `requests`** —— **两个 lens 独立判它净收益为负，我复跑确认**：
> (b) 引入 **4 处 fail-open**（含 302 的响应体会被当模型目录 upsert）+ **一个不可关闭的
> `.netrc` 凭据出境面** + 异常层级重写；而 (a) 让**那三处连带处置全部归零**（3xx 抛 `HTTPError`
> ⇒ 与 4xx/5xx 共用既有 `except` ⇒ 照旧 503）。
> ⇒ **原「§1.3 三处连带处置」整节作废**，勿再照它推断。

> ⚠️⚠️ **本片不是 P-a'' 那份手册的照抄** —— v0.9.21 已经把它的**一半**做掉了。
> 照那份做会重复劳动 + 写出已经为假的理由。本文每条事实为 **2026-08-11 重测**。

## §0 grounded 重测（v0.9.21 合入后）

### 0.1 ⭐ 禁重定向**已完成 6/7** —— 本片性质随之改变

AST 实测（按 import 解析限定名，含别名）：

| 出网点 | 禁令 | 形态 |
|---|---|---|
| `adapters/http/executor.py:126/129` | ✅ | `requests.get/post` |
| `adapters/notification/lark.py:45` | ✅ | `requests.post` |
| `adapters/notification/telegram.py:52` | ✅ | `requests.post` |
| `adapters/notification/webhook.py:179` | ✅ | `requests.post` |
| `api/admin/datasources.py:72` | ✅ | `_rq.head`（别名） |
| ⛔ **`api/admin/or_catalog.py:37`** | **无** | **`urllib.request.urlopen`** |

⇒ **本片不再是「加禁令」，而是两件事**：
1. **补上最后那一处** —— 它之所以漏，正因为**形态不同**（P-a'' Stage 1 就是这么发现它的）；
2. ⭐ **让禁令变成结构性的** —— 今天 6 处各写一遍，**第 8 个出网点默认无禁令**。

### 0.2 三处 3xx 判断**逐字重复**（实测）

`telegram.py:62` · `webhook.py:190` · `lark.py:48` 都是 `if 300 <= resp.status_code < 400:`。
⚠️ 它们**必须存在**（v0.9.21 M2：`raise_for_status()` 对 3xx **不抛**
⇒ webhook 会把「没投递」写进「已发送」审计）—— 但**没有理由写三遍**。

### 0.3 ⚠️ 新出现的设计皱褶：`url_canon` 也 import `requests`

v0.9.21 新增的 `adapters/http/url_canon` 用 `requests.Request(...).prepare()` **算 host，从不发送**。
⇒ 「**恰好一个文件可持有 HTTP 客户端**」这个哨兵口径**按文件算会误伤它**。
⇒ 判据必须能区分「**用 requests 解析**」与「**用 requests 发送**」：
**只有 `outbound.py` 可以调用 `requests.<verb>()`**；`requests.Request(...).prepare()` 不算发送。
⚠️ 这与 v0.9.21 已立的先例**同源**（「解析器不是客户端」）——
只是判据从 **import 级**下沉到**调用级**（import 级挡不住这个区分）。

## §1 修法（**Stage 2 后整段重写** —— 原文以 helper 为对象，已 moot）

> ⚠️⚠️ **kk 二次裁定（Stage 2 后）：取 (a) 保留 `urlopen` + 不跟随重定向的 opener。**
> 我原先解释成 (b) 换 `requests`，**两个 lens 独立判它净收益为负，我复跑确认**。

### 1.1 ⭐ `or_catalog`：三行 opener（**这就是全部**）

```python
class _NoRedirect(urllib.request.HTTPRedirectHandler):
    def redirect_request(self, *a):   # 返 None ⇒ 不跟随
        return None

_OPENER = urllib.request.build_opener(_NoRedirect())
```
调用改 `_OPENER.open(req, timeout=30)`。

⭐ **机理（✅ 执行者复跑确认）**：`redirect_request` 返 `None` ⇒ `http_error_302` 直接 return
⇒ 落到 `http_error_default` ⇒ **抛 `HTTPError(code=302)`**、第二跳零发生
⇒ **3xx 与 4xx/5xx 共用既有那条 `except (URLError, JSONDecodeError, TimeoutError)`** ⇒ 照旧 **503**。

⇒ ⭐⭐ **原 §1.3 那三处「换形态的连带处置」全部归零** —— 异常层级不变 · 500 仍 fail-closed · UA 不变。

### 1.2 ⛔ 为什么**不**选 (b) 换 `requests`（三条实测，全部是我原方案的错）

| # | 实测 |
|---|---|
| 1 | **fail-open 是四处不是一处**：4xx / 5xx / **以及 302 的响应体** —— ✅ 复跑 `302 + {"data":[{"id":"evil/model"}]}` 在 `requests` 下**不抛**，而 `or_catalog` 紧接着 `json.loads` ⇒ **该体被当模型目录 upsert** |
| 2 | **带一个不可关闭的凭据出境面**：`requests` 把 `.netrc` 凭据变成 `Authorization: Basic` 发出（`urlopen` 不会），且 `default` 条目匹配**任意 host**；而 `Session()` 被 kk 裁定②禁用 ⇒ `trust_env` **关不掉** |
| 3 | **异常层级重写**：`RequestException` **非** `URLError` 子类 ⇒ 三个 `except` 全落空、超时/DNS 失败从 503 变 **500** |

⇒ (b) 的唯一实质收益（单一形态让哨兵简单）值约 5 行哨兵代码，**不成比例**。

⚠️ **顺带订正我方案里一处定性反了**（lens A）：我把「设不了 `trust_env=False`」记成**换形态的代价**
—— 实测 `urlopen` **同样**走 `HTTP_PROXY` ⇒ **代理面今天就存在**，不是 (b) 引入的。
⇒ item ③ 的 WARN 仍该做，但**理由要改对**。

### 1.3 UA 版本字面

`or_catalog.py:35` 显式设 `knot/0.6.0.6` —— ⚠️ 该版本字面**已漂 30+ PATCH**，顺手修。

### 1.4 ⭐ 哨兵（**本片安全价值全押这里** —— 故判据必须够硬）

> ⚠️⚠️ **两个 lens 都判我原来的措辞「必须显式**声明**重定向策略」没有判别力。**
> ✅ 我复跑坐实：那个判据对 `allow_redirects=True` 与 `=变量` **两个 mutant 全部放行**。

**两级并存**（⚠️ 原方案写「下沉到调用级」是**错的一半** —— ✅ 实测 `httpx` / `urllib3` /
`http.client` 的惯用形态是**实例方法**，调用级**结构上认不出**，0 命中）：

| 级别 | 判据 | 管什么 |
|---|---|---|
| **import 级** | `knot/` 内只允许 `requests` / `urllib.request`；`httpx` / `urllib3`（非 `.util`）/ `http.client` / `aiohttp` / `socket` / `pycurl` **禁** | **换库**这一整类（含 `getattr` / 变量间接 / `partial` / `Session()` —— 它们都需要本文件先 import） |
| ⭐ **调用 + 值级** | `requests.<verb>()` 必须带 **`allow_redirects=ast.Constant(False)`**；`urllib.request.urlopen` **全仓禁**（`or_catalog` 走 `_OPENER`） | **参数被改成 `True` / 变量 / 漏写** |
| **属性级** | `url_canon.py` 内 `requests` 的属性**只允许 `{Request}`` | 封死「白名单文件里 prepare-then-send」（✅ 实测该文件 `requests.*` 属性**只有 `Request`** 一个） |

⚠️ **参数名按库映射**，不能一个集合通吃（✅ 实测：在 httpx 上写 `allow_redirects` 而非
`follow_redirects` 会让强判据也放行）：`requests→allow_redirects` / `httpx→follow_redirects` /
`urllib3→redirect`。**本片 import 级已禁 httpx/urllib3 ⇒ 该映射是为将来解禁时准备的，写进 docstring。**

⚠️ **`tests/` 用文件白名单，不是目录排除**（✅ 实测 `tests/` 调用级命中仅 **3 处 / 1 文件**）——
目录排除会成为逃逸口（把出网代码写进 `tests/` 下的 helper 再被生产码 import；
今天 `knot/` import `tests/` = **0 处**，但**无守护** ⇒ 顺手加一条）。

### 1.5 ⭐ 扩既有那条，**不是**新写一条替代它（lens B 的建议，我采纳）

`test_no_second_http_client_in_http_adapter` 的 **banned set 与 `parser_only_from` 豁免形状已经是对的**
（v0.9.21 刚校准过），**只是作用域太小**（`adapters/http/**`）⇒ **扩到 `knot/`**（✅ lens 实测零假红）。
⇒ 新哨兵**只负责**「调用+值级」与「属性级」那两条。
**理由**：重写一份 banned 清单等于制造**第二份会漂的清单**。

⚠️ 且实测既有那条 `test_all_outbound_requests_calls_live_inside_execute` 是**别名盲**
（`import requests as _rq; _rq.post(...)` 在 `adapters/http/` 内新增会**静默通过**）——
⚠️ 但它与本片**互补不重叠**（它说「能力没有兄弟」，新哨兵说「每处出网都禁重定向」）
⇒ 同 commit 双红可接受；**别名盲那条登记为独立 backlog**，不在本片修（scope）。

## §2 必须处置的既有测（**Stage 2 订正：原表两行为假**）

| 测 | 最小方案下会怎样 |
|---|---|
| `test_all_outbound_requests_calls_live_inside_execute` | ⚠️ **不会被触发**（✅ 实测扫描面 = `adapters/http/**`，而 `or_catalog` 在 `api/admin/`）⇒ **原表说「必然违反、须改名重写理由」是假的** |
| `test_no_second_http_client_in_http_adapter` | 本片**主动扩它的作用域**到 `knot/`（§1.5）⇒ 要同片改它的 docstring（作用域变了） |
| `tests/api/test_model_catalog.py:114` | patch `urllib.request.urlopen` ⇒ 改用 `_OPENER.open` 后**变空操作** ⇒ **只需改 patch 目标一行**（`URLError` 仍是正确的模拟）—— 这正是 (a) 比 (b) 省的地方 |
| `no_network` 探针 | ⚠️ 本片**不改** `requests` 侧 ⇒ 那 3 条反向断言不受影响；**但可顺手给探针加 `urlopen`**，让本端点首次进入全局探针视野（lens A 指出这是 (b) 的收益，而它**不必换形态就能拿到**） |

## §3 验收（**整表重写** —— 原 8 条里 5 条是 helper-only，已 moot）

| # | 判据 | 层 | 注入 | 前 → 后 |
|---|---|---|---|---|
| 1 | ⭐ **真 302 ⇒ `_OPENER.open` 抛，且第二跳零发生** | 端到端（真服务 ×2） | 去掉 `_NoRedirect` | 前：抛 `HTTPError(302)` + 第二监听器零收到 → 后：**跟到第二跳** |
| 2 | ⭐ **端点层：上游 302 ⇒ 503 且模型表不变** | API | 同上 | ⚠️ **oracle 不能只用「行数不变」**（见下） |
| 3 | ⭐ **哨兵：`allow_redirects` 值必须是字面 `False`** | 元（AST） | 任一处改 `True` / 改成变量 | ✅ 已实测强弱判据的差别（弱判据对两个 mutant 全放行） |
| 4 | ⭐ **哨兵：import 级禁替代客户端** | 元（AST） | 任一文件 `import httpx` | ✅ lens 实测今天 `knot/` 恰 7 处命中、替代客户端 0 ⇒ **零假红** |
| 5 | **哨兵：`url_canon` 的 `requests` 属性只许 `{Request}`** | 元（AST） | 在该文件加 `requests.Session()` | 封死白名单文件里 prepare-then-send |
| 6 | **哨兵：全仓禁 `urlopen`**（`or_catalog` 走 `_OPENER`） | 元（AST） | 改回直调 `urlopen` | —— |
| 7 | 正对照：**200 路径照常同步** | API（真服务） | 无（刻意） | 防「一律拒绝」式假通过 |

### ⚠️ §3-1 #2 的 oracle 必须换（lens A 的 P0-2，✅ 我认同）

原写「后：200 + 错误 JSON 被 upsert」。**但 OpenRouter 式错误体 `{"error":{…}}` 经
`payload.get("data") or []` 得空 ⇒ 0 行 upsert ⇒「行数不变」前后皆真 = 判据恒绿。**
⇒ 可区分的 oracle 是 **HTTP 状态码（503 vs 200）**，或注入**带 `data` 数组的错误体**。
⇒ **本片取「状态码 + `upserted_count`」双断言。**

### ⚠️ §3-2 lens A 还指出一处我完全没想到的下游

`Admin.jsx:166-174` 只在 catch 里报失败，200 走 `已同步 ${r.upserted_count} 条`
⇒ 若上游失败被吞成 200，前端会显示**绿色 toast「已同步 0 条」**，而 `upsert` 从不删行
⇒ **admin 无法区分「同步成功无变化」与「上游挂了」**。
⇒ (a) 下 3xx/5xx 仍抛 ⇒ 该问题**不存在**；本条作为「为什么必须保住 fail-closed」的理由记录在案。

## §4 v3.1-B 枚举（命中：出网 · 凭据）

> ⚠️⚠️ **本节在 (a) 裁定后被整段重写** —— 原文写的是 (b)「统一出网 helper」方案下的答案
> （「禁令在 helper 唯一一行」/「helper 有 7 个消费者」/「新契约 `adapters/http/outbound.py`」），
> 而**那个 helper 不存在了**。⇒ 留着就是**在治理表里描述一个不存在的系统**。
> **这是我连续第四片把「范围变了」漏传到正文** —— §7 落地对照表就是为此加的，它这次抓住了 §1/§2/§3，
> **没抓住 §4**（因为对照表只覆盖 Stage 2 findings，不覆盖「我自己改了方案」）。
> ⇒ **教训**：范围一改，**先扫全文出现旧方案名词的地方**（这次是 grep `helper`），再动手。

1. **fail-open 面**：⭐ **本片的 fail-open 恰恰是判据自己** —— 原措辞「必须显式**声明**重定向策略」
   对 `=True` 与 `=变量` **全部放行**（✅ 已复跑坐实）⇒ 已改为「值必须是**字面 `False`**」。
   `or_catalog` 侧结构上 fail-closed：3xx → `HTTPError` → 既有 `except` → 503 不写表。
   ⭐ **1 → N 自查**：本片让「出网点」从 6 变 7（形态从 1 种变 2 种）
   ⇒ 重扫所有**依赖「只有 requests 一种形态」**的判据 —— 命中 **2 处**：
   ① `test_no_second_http_client_in_http_adapter` 的**豁免面**（`urllib.request` 在 banned set 里
   ⇒ 扩面后当场假红，⚠️ **评审给的「扩面零假红」在这一点上是错的**）·
   ② `no_network` 探针（只 patch `requests.*` ⇒ urllib 路径**零覆盖**）。
2. **oracle 能力 + 注入真能产生后果**：#1 起**两个真监听器** ⇒ 「跟随了重定向」这个事件
   在 oracle 里**可表示**（第二监听器的 `received` 非空）。⚠️ 配可达性正对照 + 清 8 个代理 env
   （六问⑥第二形态）。#2 的 oracle **不能只用「模型表行数不变」**（错误体经 `.get("data") or []`
   得空 ⇒ 前后皆真 = 恒绿）⇒ 改断**状态码 + `upserted_count` 不在响应里**。
3. **「那一行」/ 入口**：禁令在 `_OPENER` 这一个对象上；**入口不止一个** ⇒ 哨兵**两级**
   （import 级管换库、调用+值级管改参数）+ 全仓禁直调 `urlopen`。
   ⚠️ `pytest.raises` **不当主 oracle**（✅ 实测：换回默认 opener 时它停在 `DID NOT RAISE`
   ⇒ 「第二跳零收到」这条真属性的断言**根本不执行**）⇒ 已改为无条件断真属性、最后断可诊断性。
4. （并入 #3）
5. **散文规则无守护？** —— v0.9.21 在三处写了「3xx 必须显式判」的注释，**本片没有把它变成哨兵**
   （三处重复的 3xx 判断已登记为可维护性 backlog）⇒ **诚实记：该条仍只是散文。**
6. **声明 vs 生产者**：`_OPENER` 有 1 个消费者（同文件）；`_PROXY_ENVS` 有 2 个
   （生产 WARN + 交叉核对测）；出网哨兵的**测文件白名单**有断言保证路径真存在。
7. **顶班**：⚠️ 若 `or_catalog` 端点本来就打不通，「禁令生效」与「本来就失败」不可区分
   ⇒ **200 正对照必须存在**（✅ 实测有用：注入「一律拒绝」时正对照红，而 302 那条**照绿**）。
8. **既有测的绿是真的吗** → §2 逐条；另 `tests/api/test_model_catalog.py:114` 的 patch 目标
   若不改会**静默变成空操作**并真打 openrouter.ai。
9. **契约冲突**：无新契约（(a) 不引入 `adapters/http/outbound.py`）；`or_catalog` 在 api 层、
   只 import 标准库 ⇒ 分层不变，import-linter 9 条 kept。
10. **策略题的影子**：代理面出现「关不掉就只能喊」⇒ 选「喊出来」，理由是**失效方式可见**
    （而 `trust_env=False` 那条**在 (a) 下压根不适用** —— urlopen 没有那个开关）。
11. **诚实收窄**：本片**不声称** —— 端口受控 · headers 不受检 · DNS rebinding / TOCTOU ·
    **LLM 厂商 SDK 的出网**（`openai`/`anthropic` 走 `httpx`，实测 `follow_redirects=True`）·
    代理面**受控**（只 WARN）· 三处 3xx 判断**未去重**。


## §5 版本 / 协议

**v0.9.22（PATCH）**。⚠️ 本片动 **7 处出网点 + 换一处形态 + 新增哨兵** ⇒ 建议走 lens。

## §6 待 kk 裁定

1. ⭐ **本片还值得做吗？** 禁令已 6/7 完成 ⇒ helper 的**边际安全价值**是
   「**第 8 个出网点自动带禁令**」+ 收拢三份重复 + 补上 `or_catalog`。
   ⇒ 我认为**值得**，但**理由变了**（不是「加禁令」而是「让禁令不可遗漏」）。
   ⚠️ 若你觉得性价比不够，**最小替代方案** = 只做 `or_catalog`（补禁令 + 状态码分流 + 修 UA 版本字面）
   + 一条**调用级哨兵**（新出网点必须显式声明重定向策略），**不做 helper**
   —— 那样 diff 小得多，而「不可遗漏」这条性质**由哨兵而非 helper 提供**。
2. **`trust_env` 那条**：启动期 WARN 放哪？我倾向与 v0.9.7 那两条 egress WARN 同处
   （`main.py` 启动钩子），理由是运维看启动日志的习惯已经在那里。

---

## §7 ⭐ 落地对照表（v0.9.21 立的纪律 · **任一行「落点」为空即视为未吸收**）

> ⚠️ **为什么必须有这张表**：v0.9.20 / v0.9.21 / **本片** —— 连续三次被评审抓到
> 「**整合了但没落进正文**」。本片的形态最典型：kk 裁定「最小方案」之后，
> §1/§2/§3 **整段仍以 helper 为对象**（§3 八条里五条 moot、§2 表里两行为假），
> 而**两个 lens 都点了这条**。⇒ 散文提醒无效，只有可机械检查的表有效。

| # | Stage 2 的发现（A/B） | 落点 |
|---|---|---|
| 1 | ⭐ **(b) 净收益为负 ⇒ 取 (a)** | 头部二次裁定框 + §1.1 + §1.2 |
| 2 | (b) 的 fail-open 是**四处**不是一处（含 302 响应体被 upsert） | §1.2 表 第 1 行 |
| 3 | (b) 带**不可关闭的 `.netrc` 凭据出境面** | §1.2 表 第 2 行 |
| 4 | 异常层级重写（`RequestException` 非 `URLError` 子类） | §1.2 表 第 3 行 |
| 5 | ⭐ **我对代理面的定性反了**（`urlopen` 同样走 `HTTP_PROXY`） | §1.2 末段 + 头部范围 第 3 条 |
| 6 | ⭐ **哨兵判据无判别力**（「声明」≠「声明成 `False`」） | §1.4 警示框 + 表「调用+值级」+ §3 #3 |
| 7 | ⭐ **「下沉到调用级」是错的一半**（httpx 等在调用级 0 命中） | §1.4「两级并存」表 |
| 8 | 参数名**按库映射**（httpx 写错名会让强判据也放行） | §1.4 末段 |
| 9 | `url_canon` 判据应下沉到**属性级**（只许 `{Request}`） | §1.4 表「属性级」+ §3 #5 |
| 10 | ⭐ **扩既有那条，别新写一份 banned 清单** | §1.5 |
| 11 | 既有 `test_all_outbound_...` **别名盲** ⇒ 登记 backlog、不在本片修 | §1.5 末段 + 头部「明确不做」 |
| 12 | `tests/` 用**文件白名单**不是目录排除（+ 加一条「`knot/` 不得 import `tests/`」） | §1.4 末段 |
| 13 | ⭐ **§2 原表两行为假**（那两条哨兵扫描面 = `adapters/http/**`，`or_catalog` 不在其内） | §2 表 第 1 行 |
| 14 | `test_model_catalog.py:114` **只需改 patch 目标一行** | §2 表 第 3 行 |
| 15 | `no_network` 探针可**顺手加 `urlopen`**（不必换形态就能拿到那个收益） | §2 表 第 4 行 |
| 16 | ⭐ **#2 的 oracle 恒绿**（错误体经 `.get("data") or []` 得空 ⇒ 行数前后皆不变） | §3-1 |
| 17 | 前端把 fail-open 显示成绿色「已同步 0 条」 | §3-2（作为「必须保住 fail-closed」的理由记录） |
| 18 | §3 原 8 条里 5 条 helper-only ⇒ **整表重写** | §3（已重写为 7 条） |

---

## §9 实施记录（v3.1-C #4/#5 —— 偏离 + 我自己写错的东西）

### 9.1 落地清单（三件范围 + 连带）

| 落点 | 内容 |
|---|---|
| `knot/api/admin/or_catalog.py` | `_NoRedirect` handler + `_OPENER`（`build_opener`）· `_OPENER.open` 替代 `urlopen` · UA 去掉版本字面 · docstring 同步 |
| `tests/test_outbound_redirect_policy.py`（新） | **6 条**：值级禁令 · 全仓禁 `urlopen` · `url_canon` 属性级 · `knot/` 不得 import `tests/` · 白名单路径存在 · ⭐ **行为级 e2e**（真 302 两监听器） |
| `tests/test_file_catalog_owner_gate.py` | 既有 import 级哨兵扫描面 `adapters/http/**` → **`knot/`** + **窄豁免**（按 (文件, 模块)） |
| `knot/adapters/http/url_canon.py` | `_PROXY_ENVS` + `warn_if_proxy_env_may_bypass_allowlist()` + stdlib logger |
| `knot/main.py` | 代理 WARN 接线（与既有两条 egress WARN 同一 startup hook） |
| `tests/conftest.py` | `no_network` 补 urllib 路径（patch **`OpenerDirector.open`**） |
| `tests/adapters/test_egress_allowlists_shared_properties.py` | 代理 WARN 2 条（点名不打值 / 无代理时静默） |
| `tests/adapters/test_url_canon_single_parser.py` | 交叉核对：生产 `_PROXY_ENVS` ⊆ fixture 清空集 |
| `tests/api/test_model_catalog.py` | patch 目标改 `_OPENER.open` + **端点层 302 fail-closed** + **200 正对照** |
| 版本 / 文档 | 5 源点 + 重建 `knot/static` + CHANGELOG + README banner 滚动 + DEPLOY 顶部 |

### 9.2 偏离 Stage 1 的地方（逐条理由）

1. **代理 WARN 落 `url_canon` 而不是 `url_allowlist`**（§6-2 原倾向「与那两条 egress WARN 同处」）。
   ⚠️ **诚实说明触发顺序**：是 **size 闸门**（`url_allowlist` 加完 304 > 300）让我去重看归属的。
   但理由独立成立：`url_allowlist` 是**数据源那一份**名单（webhook 有自己的一份），
   而代理面对**四个出网点全部成立** ⇒ 放那里会被读成「只跟数据源有关」。
   `url_canon` 是四处共用的规范化口径，且本条说的正是**它自己的诚实边界**：
   它算出一个 host，它不控制字节去哪。（搬家后三条 revert 判别力**逐条重验**，不变。）
2. **`no_network` 的 patch 目标 = `OpenerDirector.open`，不是 `urlopen`** ——
   ⭐ **§7 表第 15 条（「顺手加 `urlopen`」）按字面做是个 no-op**：生产走自建 opener
   ⇒ ✅ 实测 patch `urlopen` 时探针命中 `[]`、异常是真 `URLError`（= 真出网了）；
   patch `OpenerDirector.open` 才拦住（两者的共同收敛点，`urlopen` 内部也调它）。
3. **e2e 测不用 `pytest.raises`** —— 见 9.3 #3。
4. **§4 整段重写** —— 见 9.3 #1。

### 9.3 ⭐ 我自己写错 / 漏掉的东西（本节价值最高）

1. ⭐⭐ **§4 治理表停留在 (b) 方案，描述了一个不存在的系统**（「禁令在 helper 唯一一行」/
   「helper 有 7 个消费者」/「新契约 `outbound.py`」）。**这是我连续第四片把「范围变了」漏传到正文**
   （v0.9.20 / .21 / .22-Stage1 / 本次）。§7 落地对照表这次**抓住了 §1/§2/§3、没抓住 §4** ——
   因为对照表只覆盖 **Stage 2 findings**，不覆盖「**我自己改了方案**」。
   ⇒ **可操作补丁**：范围一改，先 `grep` 全文的旧方案名词（这次是 `helper`），再动手。
2. ⭐ **扩哨兵作用域时忘了同步豁免面** —— `urllib.request` 本就在 banned set 里，
   而 `or_catalog` **必须** import 它 ⇒ 扩面当场假红。
   ⚠️ **评审给的「扩面零假红」在这一点上是错的**（它没把 banned set 与新扫描面**交叉核**）。
   处置 = 按 **(文件, 模块)** 精确豁免（不是整文件豁免），并**双向验**：同文件 `import httpx` 仍红。
3. ⭐ **e2e 用 `pytest.raises` 当主 oracle 是假证明** —— revert（换回默认 opener）时它停在
   **DID NOT RAISE**，于是「第二跳零收到」这条**真属性的断言根本不执行**，
   失败消息也**不提重定向**。⇒ 改 `try/except` + **无条件**断真属性，最后才断可诊断性；
   重跑 revert 后失败消息变成 `⛔ **跟随了重定向** —— 第二跳收到 ['/redirected']`。
   （CLAUDE.md 早有这条：**安全属性是「什么没发生」，不是「抛了异常」**。）
4. ⭐ **我第一版注入的正则形状就是错的**（`except urllib.error.URLError as e:`，
   实际是三元组 `(URLError, JSONDecodeError, TimeoutError)`）⇒ 注入没生效而测「2 passed」
   —— **我差点把它读成「判据无判别力」**。第二次注入前先 `assert t.count(anchor)==1`。
   （六问②：**注入不成立时，「取材证明」是空的**。）
5. **改 UA 时把自己的 docstring 弄成了假的** —— 函数 docstring 仍写
   `User-Agent 显式 "knot/X.Y.Z"`，而代码已改成 `knot`。自查时发现并修。
6. **搬家漏带 `import os`** —— 响亮失败（ruff F821 + `NameError`），不是静默；记在此作对照：
   **同一个搬家动作若漏带的是一条 WARN 的调用点，就是静默的** ⇒ 这次运气好，不是纪律好。
7. **误读 pytest 输出一次** —— grep 上下文里出现了「可达性正对照」那行源码，我先读成
   「红在可达性上」；实际 `>` 标记在 `pytest.raises`。⇒ 判失败位置要看 `>` 标记，不看 grep 邻近行。

### 9.4 revert-to-bad 汇总（全部真跑）

| 注入 | 结果 |
|---|---|
| `allow_redirects=False` → `True` | 1 failed / 4 passed |
| → `allow_redirects=<变量>` | 1 failed / 4 passed |
| 整个参数删掉 | 1 failed / 4 passed |
| `or_catalog` 改回直调 `urlopen` | 1 failed / 4 passed |
| 任意生产文件 `import httpx`（扩面后） | 1 failed |
| 同上，但在**已豁免**的 `or_catalog` 里（验豁免没写宽） | 1 failed |
| `_OPENER` 去掉 `_NoRedirect`（e2e） | 1 failed，消息指名「跟随了重定向 —— 第二跳收到 `['/redirected']`」 |
| 代理 WARN 打出 env 值 | 1 failed / 1 passed |
| 代理 WARN 改成无条件打 | 1 failed / 1 passed |
| 生产 `_PROXY_ENVS` 加一个 fixture 没清的名字 | 1 failed |
| 端点把上游异常吞成 200 | 2 failed / 1 passed |
| 端点「一律拒绝」（验正对照） | 2 failed（含 200 正对照）/ 1 passed —— ⭐ 302 那条**照绿** ⇒ 正对照必须存在 |

### 9.5 闸门

- 全量**两轮**（第二轮是加完端点层两条测之后的收尾轮）：
  - 第一轮 **1945 passed / 2 failed**（410s）—— 2 failed 均为 `test_dockerignore_context`
    「新文件未入库会被烤进镜像」，`git add` 后 18 passed（**该测按设计工作**，不是缺陷）；
  - ⭐ 收尾轮 **1949 passed / 115 skipped / 1 xfailed / 0 failed**（427s；2065 collected）。
- 前端：eslint 0 warning · vitest **83 passed**。
- ruff `All checks passed` · import-linter **9 kept / 0 broken** · size `R-94 OK`。
- 前端：重建 `knot/static` ⇒ ⭐ **bundle 归一后逐字节相同（557054 字节，差 0）**
  ⇒ 「前端除版本字样外零改动」是**实证**。`index.html` 指新 chunk、无孤儿 chunk。
- README / DEPLOY 版本改动**机械核**：把新文件里的 `0.9.22` 换回 `0.9.21` 后与 `HEAD` 版**逐字节相同**，
  且改的那处上下文是「**当前版本**」banner，不是「上版 / 更早」历史条目
  （CLAUDE.md 记着 v0.9.3 曾用 `head.replace` 误改上一条发布记录而 4 条 CI 全绿）。
