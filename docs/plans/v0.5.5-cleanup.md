# v0.5.5 — Cleanup（Cn）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 6 次施行**；v0.5.x 序列**首个减法 PATCH** — Negative Delta）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

5/5 决策（D1-D5）**与执行者 Stage 1 提议完全一致**，0 修订。
新增 5 条红线（R-149~R-153），分别来自 Stage 2（2 条）+ Stage 3（3 条）。

**LOCKED 手册 §4 文件级清单 2 处与 Stage 1 探查事实不一致，整合时按 Stage 1 实际路径执行**（精神类似 v0.5.3 D4 内部 mapping 调整条款）：
1. **路径笔误**：LOCKED §4 写 `knot/services/notification/...`，实际是 `knot/adapters/notification/...`；测试同样从 `tests/services/` 改 `tests/adapters/`
2. **sync API 8 处分布**：LOCKED §4 写"`llm_client.py [EDIT 8 处]`"暗示集中，实际**分散在 7 个文件**（llm_client.py 2 处 + _llm_invoke.py / sql_planner.py / sql_planner_llm.py / clarifier.py / presenter.py / orchestrator.py 各 1）；R-152 grep 验证扩展为 `grep -rcn "DEPRECATED v0.5.5" knot/services/`

## 1. 决议表最终裁定（D1-D5 全部锁定）

| ID | 锁定 | 与执行者提议 |
|---|---|---|
| **D1** sync API 标注形式 | ✅ A docstring 仅注释 | 一致 |
| **D2** sync API 删除时间表 | ✅ A v1.0 删除目标 | 一致 |
| **D3** notification/ 目录残留 | ✅ A 保留 base + __init__ | 一致 |
| **D4** 红线编号 | ✅ A 接续 R-139~R-153 | 一致 |
| **D5** Commit 序列 | ✅ A 3 commit | 一致 |

## 2. 红线最终清单（15 条 R-139~R-153）

### Stage 1 草案原 10 条（保留）

| ID | 摘要 |
|---|---|
| **R-139** | backend tests 432 → **430**（受控降级 — 删 2 lark 测试） |
| **R-140** | 7 contracts KEPT |
| **R-141** | `notification/{base,__init__}.py` 字面 byte-equal |
| **R-142** | sync API 业务行为零变更（仅 docstring） |
| **R-143** | 非流式路径（`query_steps.run_generate_sql_with_fix_retry` + `run_agent_step_sync`）继续工作 |
| **R-144** | routes=72 / version=0.5.5 |
| **R-145** | 0 修改 frontend / requirements.txt / package.json / pyproject.toml / .importlinter |
| **R-146** | scripts/check_file_sizes.py LIMITS 不动（无新建模块） |
| **R-147** | live LLM eval 不影响 |
| **R-148** | CLAUDE.md `lark.py(stub)` 字面去除（除 v0.5.x 路线图历史回顾） |

### Stage 2 新增 2 条（资深架构师 + Codex）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-149** | **依赖幽灵扫描** — 物理删 `lark.py` 后必须 `grep -rn "notification\.lark\|from \.lark\|import.*lark" knot/ tests/` 全局扫描；严禁残留隐性 import 致 ImportError | commit 1 删除后立即跑 grep；命中数必须 = 0；如 `notification/__init__.py` 有 `from .lark import ...` 必须同 commit 内移除 |
| **R-150** | **非流式路径完整性** — sync API docstring 修改必须手测 `/api/conversations/{cid}/query` 非 SSE 模式 | commit 2 docstring 修改后**实际启动**后端跑一次非流式 query — 防止误伤缩进 / 三引号闭合错误致函数解析异常 |

### Stage 3 新增 3 条（v0.4 守护者）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-151** | **测试基准受控降级存档** — 432→430 的删除必须在 CHANGELOG 显式注明 Test Case 名称 | CHANGELOG v0.5.5 段加一行「删除 lark 相关测试 2 个：`test_lark_satisfies_protocol` + `test_lark_send_raises_not_implemented`（位于 `tests/adapters/test_notification.py`）— 防未来审计误判测试丢失」；起手前 grep 已标定（保留 `test_notification_dataclass_defaults`） |
| **R-152** | **统一 Deprecation 话术** — 8 处 sync API docstring 必须**完全一致**格式 | **锁定模板**：`[DEPRECATED v0.5.5; target removal in v1.0] Use async equivalent (a*) instead.`（首行；现有 docstring 内容保留在下方）；8 处字面 byte-equal 这一行 |
| **R-153** | **路径索引一致性** — CLAUDE.md 关键路径表 `notification` 描述从"飞书/Slack 提醒"修正为"通知接口抽象层" | 同 commit 3 一并改；如关键路径表无 notification 行则新增（保持索引完整性） |

## 3. Stage 2/3 三条隐性指令（落地手段）

### 3.1 Stage 3 §3：Negative Delta 里程碑

**落地**：CLAUDE.md L113-114 v3 协议施行历史回顾表（v0.5.4 已扩展到 5 行）→ 加第 6 行：

| PATCH | 主题 | 红线 | 关键决策 / 施行特征 |
|---|---|---|---|
| v0.5.5 | Cn cleanup（遗留清理） | R-139~R-153 (15) | **首次净行数减少（Negative Delta -18）**；物理删 `lark.py` stub；8 处 sync API 标 `[DEPRECATED v0.5.5; target removal in v1.0]`；测试受控降级 432→430 |

v0.5.0~v0.5.4 全部是**加法 PATCH**，v0.5.5 是 v0.5.x 序列**首个减法 PATCH**。

### 3.2 Stage 3 §2：起手前置守护（已完成）

**落地**：commit 1 起手前 grep 已完成 — `tests/adapters/test_notification.py` 含 3 测试：
- 保留：`test_notification_dataclass_defaults`（L9）
- 删除：`test_lark_satisfies_protocol`（L16）
- 删除：`test_lark_send_raises_not_implemented`（L20）

### 3.3 Stage 2 R-149 grep 命令 + R-150 手测命令

```bash
# R-149 commit 1 后立即跑（预期 0 命中）
grep -rn "notification\.lark\|from \.lark\|import.*lark" knot/ tests/

# R-150 commit 2 后启动后端非流式实测
python3 -m uvicorn knot.main:app --port 8000 &
curl -X POST http://localhost:8000/api/conversations/{cid}/query \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"question":"...","use_agent":false}'
```

## 4. 文件级改动清单（最终版 — 含 LOCKED §4 路径整合修正）

```
knot/adapters/notification/                       [实际路径，非 services/]
├── lark.py                         [DELETE -29 行]                    [R-149 grep 0 残留]
├── base.py                         [unchanged byte-equal]              [R-141]
└── __init__.py                     [unchanged byte-equal]              [R-141; R-149 已确认无 .lark 引用]

# sync API 8 处 docstring 加 [DEPRECATED v0.5.5; target removal in v1.0] Use async equivalent (a*) instead.
# 实际分散在 7 个文件（非集中在 llm_client.py）：
knot/services/llm_client.py                     [EDIT 2 处] generate_sql + fix_sql
knot/services/_llm_invoke.py                    [EDIT 1 处] _invoke_via_adapter
knot/services/agents/sql_planner.py             [EDIT 1 处] run_sql_agent
knot/services/agents/sql_planner_llm.py         [EDIT 1 处] _call_llm
knot/services/agents/clarifier.py               [EDIT 1 处] run_clarifier
knot/services/agents/presenter.py               [EDIT 1 处] run_presenter
knot/services/agents/orchestrator.py            [EDIT 1 处] _llm

tests/adapters/test_notification.py             [EDIT -2 lark 测试 -约 13 行]    [实际路径]
                                                保留 test_notification_dataclass_defaults [R-151 已 grep 标定]

knot/main.py                                    [EDIT version 0.5.4 → 0.5.5]
tests/test_rename_smoke.py                      [EDIT R-72 smoke 0.5.4 → 0.5.5]

CHANGELOG.md                                    [EDIT [Unreleased] v0.5.5 段]
                                                R-139~R-153 偿还表
                                                + R-151 删除测试名称显式注明
                                                + Loop Protocol v3 第 6 次施行存档（首次 Negative Delta）

CLAUDE.md                                       [EDIT]
                                                - 关键路径表去除 `notification/lark.py(stub)` 字面 [R-148]
                                                - 关键路径表 notification 描述改"通知接口抽象层" [R-153]
                                                - v3 协议施行历史扩第 6 行（Negative Delta 里程碑） [Stage 3 §3]
                                                - v0.5.x 路线图 v0.5.5 ⏳ → ✅
                                                - main.py path version 0.5.4 → 0.5.5

requirements.txt / package.json / pyproject.toml / .importlinter   [unchanged]   [R-145]
scripts/check_file_sizes.py                     [unchanged]                       [R-146]
.github/workflows/ci.yml                        [unchanged]
frontend/                                       [unchanged]                       [R-145]
docs/plans/v0.5.5-cleanup.md                    [本锁定手册]
```

**净增减**：-29（lark.py）-13（lark 测试）+~24（8 处 docstring 首行 ≈ 8 行 + CHANGELOG/CLAUDE.md +~16）= 净 **-18 行** ✅

## 5. 验收清单（最终版）

### 5.1 必须全绿

- [ ] `pytest tests/ -v` = **430 passed / 112 skipped**（R-139 严格 432 → 430，**不可多不可少**）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.5**
- [ ] `npm run build` 通过

### 5.2 Stage 2/3 强化验证

- [ ] **R-149**：`grep -rn "notification\.lark\|from \.lark\|import.*lark" knot/ tests/` → 0 命中
- [ ] **R-150**：commit 2 后实际启动后端 + 跑非 SSE `/api/conversations/{cid}/query` 闭环成功
- [ ] **R-151**：CHANGELOG 显式列被删测试名称 2 个；保留的 dataclass 测试仍跑过
- [ ] **R-152**：8 处 sync API docstring 首行字面 byte-equal `[DEPRECATED v0.5.5; target removal in v1.0] Use async equivalent (a*) instead.` — `grep -rcn "DEPRECATED v0.5.5" knot/services/` ≥ 8
- [ ] **R-153**：CLAUDE.md 关键路径表 notification 描述 = "通知接口抽象层"（非"飞书/Slack 提醒"）

### 5.3 不破坏

- [ ] R-141 `notification/{base,__init__}.py` 字面 byte-equal
- [ ] R-142 sync API 8 处函数体零修改（grep diff 仅 docstring）
- [ ] R-143 非流式 query 路径手测通过
- [ ] R-148 CLAUDE.md `lark.py(stub)` 字面去除（路线图历史段例外）
- [ ] frontend / requirements.txt / pyproject.toml / package.json / .importlinter 不动
- [ ] v0.5.0~v0.5.4 测试全绿（除 R-139 受控降级 2 lark）

## 6. Commit 序列（最终锁定 — 3 commit；D5 单文件单职责）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `chore(v0.5.5): 删 lark.py stub + 清理幽灵 import + 删 2 lark 测试 (R-139/141/148/149/153)` | pytest 430 + lint-imports + ruff + grep 0 命中 + check_file_sizes | R-149 grep 守护 + R-141 base.py byte-equal |
| 2 | `chore(v0.5.5): sync LLM API 8 处 docstring 标注 [DEPRECATED v0.5.5] (R-142/150/152)` | 全闸门 + R-150 非流式手测 | R-152 字面统一 + R-150 非 SSE 实测 |
| 3 | `docs(v0.5.5): version 0.5.4→0.5.5 + CHANGELOG + CLAUDE.md 关键路径修正 + 路线图 + plan LOCKED` | 全闸门 + routes=72 + version=0.5.5 | R-151 测试名显式 + Stage 3 §3 Negative Delta 里程碑 |

每 commit 独立全闸门绿；commit 1 删除前先 grep 标定测试名（R-151 已完成）；commit 2 修改完手测非 SSE（R-150）；commit 3 一并提 version + docs。

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 6 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 15（v0.5.5 区段 R-139~R-153）
- ✅ **首次 Negative Delta PATCH**（v0.5.x 序列里程碑）
- ✅ 范围最小化 — Out-of-Scope 严守（不删 sync API 实现 / 不删 base.py / 不动 frontend）

## 8. 给执行者（本 Agent）的执行注意

1. **归档锁定手册** → 本文件覆盖 DRAFT；commit 3 一起提
2. **commit 顺序绝对不可换**：lark 删除 → docstring 标注 → version + docs；R-149/R-150/R-151 各自绑死对应 commit
3. **R-151 起手前置已完成**：保留 `test_notification_dataclass_defaults`；删除 `test_lark_satisfies_protocol` + `test_lark_send_raises_not_implemented`
4. **R-149 grep 守护**：commit 1 删除 lark.py 后**立即**跑 `grep -rn "notification\.lark\|from \.lark\|import.*lark" knot/ tests/`；命中 = 0 才能 commit
5. **R-152 字面统一**：8 处 docstring 首行**byte-equal** — `grep -rcn "DEPRECATED v0.5.5" knot/services/` ≥ 8
6. **R-150 非 SSE 手测**：commit 2 后**强制**启动后端跑非流式 query 一次；docstring 缩进 / 三引号闭合错会立刻 IndentationError
7. **R-153 关键路径表**：CLAUDE.md `notification` 描述硬性改为"通知接口抽象层"
8. **Negative Delta 里程碑**：CLAUDE.md v3 协议施行历史第 6 行**必须**含"首次净行数减少（Negative Delta -18）"字眼
9. **范围抗诱惑**：8 处 sync API 看着想顺手删 — 但 query_steps 非流式仍依赖；本 PATCH 仅标注。任何"顺手删 sync API"/"删 base.py"/"清理 query_steps 非流式" = 范围溢出立即停手

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-10）。执行者按本手册实施。
