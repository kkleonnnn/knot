# v0.5.16 — (C5+) DataSources 屏复刻（tab_access Sources 部分）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 17 次施行**）
> **里程碑**：首个 admin tab 子模块复刻 + **全站 Inset 8% 设计语言三处闭环**（v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386）+ **I.db 复用先例**

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**Q3 修订**（Stage 1 B inline svg → Stage 2 A I.db 复用 — v0.5.x 资产复用首例）+ **Q1 加 tooltip** + **Q5 强制叙事** + **D6 双重强制 out-of-scope**。

**Stage 2 新增 2 条**：
- **R-394**：Summary Grid 响应式契约（auto-fit grid 替代 media query）
- **R-395**：Type Chip 字号 11px + letterSpacing 0.02em 工业感

**Stage 3 新增 3 条**：
- **R-396**：Sources Grid 列宽稳定性（min-width 0 + ellipsis 兜底）
- **R-397**：Name DB Icon 视觉重心（28×28 居中 + brandSoft 8% byte-equal）
- **R-398**：StatusDot 语义粘性（s.status → color 映射 byte-equal）

红线总数 25（**R-374~R-398**）。Step 1→9 子步骤。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** Summary grid 加 | ✅ A 4 卡片 + placeholder + tooltip | Stage 2 Q1 加码 tooltip |
| **D2** 表数列 placeholder | ✅ A 加 6 列（含 '—' 占位） | Stage 1 |
| **D3** Name 列 brandSoft icon | ✅ A 28×28 + I.db 复用 | Stage 1 + Q3 修订 |
| **D4** Type 列 inline chip | ✅ A brandSoft bg | Stage 1 |
| **D5** thead bg + mono + 0.06em | ✅ A | Stage 1 |
| **D6** Users 部分 out-of-scope | ✅ **A 绝对 Out-of-Scope**（Stage 2 强制）| tab === 'users' 0 改 R-376 严守 |
| **D7** brandSoft 8% 闭环 | ✅ A 第三处字面 byte-equal | Stage 1 |
| **D8** 行数预算 ≤ 110 | ✅ A LIMIT 不动 | Stage 1 |
| **Q1** Summary '—' + tooltip | ✅ A `title="后端数据对接中 (v0.6+)"` | Stage 2 强制 |
| **Q2** 表数列占位 | ✅ A 加 tooltip 同 Q1 模式 | Stage 1 |
| **Q3** I.db 复用 vs inline | ✅ **A I.db 复用**（修订 Stage 1 推荐 B）| Stage 2 推荐既有资产复用 |
| **Q4** Users hex out-of-scope | ✅ A 绝对分离 | Stage 2 强制 |
| **Q5** Inset 8% 闭环叙事 | ✅ A **显式标注里程碑** | Stage 2 强制 |

---

## 2. 红线最终清单（25 条 R-374~R-398）

### Stage 1 草案原 20 条（保留；Q3 修订 R-383）

| ID | 摘要 |
|---|---|
| **R-374** | TabAccess 8 props 签名 byte-equal |
| **R-375** | users/sources 数据流 + 5 handlers + roleChip byte-equal |
| **R-376** | **Users 部分（tab === 'users' 分支 L9-33）字面零修改**（**含 #ff7a3a 残留保留** — Stage 2/3 双重强制）|
| **R-377** | Sources 部分字面 byte-equal — `tab === 'sources'` 条件 + sources.map + handlers + 业务字段 byte-equal |
| **R-378** | Summary grid 4 卡片 + brandSoft 8% bg + radius 10 + padding 14 16 |
| **R-379** | Summary 字段：已连接 = sources.length / 总 schema/总表数/上次心跳 = '—' + tooltip |
| **R-380** | Sources Table 容器 radius 10 → 12 |
| **R-381** | thead bg 等价 brandSoft + mono + letterSpacing 0.06em + 保 uppercase + fontWeight 500 |
| **R-382** | Grid 列数 5 → 6（gridTemplateColumns）+ 表数列 '—' placeholder |
| **R-383** | **Name 列 brandSoft 28×28 + `<I.db size={14}/>` 复用**（Q3 修订 — 原 inline svg 改 Shared 资产复用）|
| **R-384** | Type 列 inline chip — brandSoft + brand color + padding 2 8 + radius 999 + mono |
| **R-385** | R-286 hex 0 命中 sustained — Sources 部分严禁 hex；**Users 部分 #ff7a3a 残留豁免** |
| **R-386** | brandSoft 8% 闭环字面与 v0.5.14 R-323 + v0.5.15 R-372 byte-equal（**全站第三处**）|
| **R-387** | tab_access.jsx ≤ 110 行；LIMIT dict 不动 |
| **R-388** | backend 432 tests / 7 contracts / 72 routes |
| **R-389** | App/api/index.css/main/utils/Shared/Shell/decor/16 屏/Admin.jsx/SavedReports.jsx byte-equal |
| **R-390** | admin/ 其他 4 子模块（tab_resources/knowledge/system/modals）byte-equal |
| **R-391** | chat/ 7 子模块 byte-equal |
| **R-392** | 三处版本同步 0.5.15→0.5.16 |
| **R-393** | KnotLogo R-199.5/222 sustained + CSS 0 污染 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-394** | **Summary Grid 响应式契约** — `gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))'` 替代 media query | inline style 限制下的最佳实践；commit 1 后浏览器三档窗宽（1024/1280/1920）实测自适应 |
| **R-395** | **Type Chip 字体精度** — `fontSize: 11` + `letterSpacing: '0.02em'` + `fontFamily: T.mono` | demo 工业感呼应 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-396** | **Sources Grid 列宽稳定性** — 每列 `min-width: 0` + `overflow: hidden` + `textOverflow: ellipsis` + `whiteSpace: nowrap` | 防超长主机名挤出操作按钮；commit 1 后实测 |
| **R-397** | **Name DB Icon 视觉重心** — 28×28 容器 `display: flex, alignItems: center, justifyContent: center` + bg `color-mix(in oklch, ${T.accent} 8%, transparent)` byte-equal R-386 | I.db 14×14 绝对居中 |
| **R-398** | **StatusDot 语义粘性** — s.status 颜色映射 byte-equal（online → T.success / offline → T.warn）— 不动现有 inline 实现 | grep 确认 origin/main 同字面 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死）

> 先插入 Summary Grid 占位结构 → 再重构 Sources Table 6 列 Grid 布局

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline diff 标定 Users (L9-33) / Sources (L35-57) 边界 | R-376 准备 |
| 2 | 插入 Summary grid 4 卡片占位（Sources 部分顶部）+ tooltip + R-394 auto-fit | R-378/379/394 |
| 3 | Sources Table 容器 radius 10 → 12 | R-380 |
| 4 | thead brandSoft + mono + 0.06em + 保 uppercase + fontWeight 500 | R-381 |
| 5 | Grid 5→6 列 + 表数列 '—' placeholder + tooltip | R-382 + Q2 |
| 6 | Name 28×28 brandSoft + `<I.db size={14}/>` 复用 | R-383 + Q3 + R-397 |
| 7 | Type inline chip — brandSoft + 11px + 0.02em + mono | R-384 + R-395 |
| 8 | 每列 min-width 0 + ellipsis 兜底 | R-396 |
| 9 | StatusDot 颜色映射 byte-equal 守护 | R-398 |
| 收尾 | grep + 端到端双 tab + 三档窗宽 + npm build | 见 §5 |

### Stage 3 §3 验收关键

```bash
# R-376 Users 部分严守
git diff origin/main HEAD -- frontend/src/screens/admin/tab_access.jsx
# L9-33 段 0 行 diff（含 #ff7a3a 渐变残留保留）

# R-386 闭环 byte-equal
grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# 应至少 3 文件命中：ResultBlock / SavedReports / tab_access

# R-394 响应式
# 浏览器 dev tools: 1024 / 1280 / 1920 三档窗宽实测 Summary grid 自适应换行
```

### Stage 2 Q3 修订 — I.db 复用纪律

**关键修订**（Stage 1 推荐 B inline svg → Stage 2 推荐 A I.db 复用）：

```jsx
import { I, iconBtn } from '../../Shared.jsx';
// ...
<div style={{ width: 28, height: 28, ... brandSoft 8% container ... }}>
  <I.db width="14" height="14"/>
</div>
```

**架构判定**：v0.5.x 第一个资产复用先例；未来如 Shared.jsx I.* 已有图标**优先复用**。

### Stage 2 R-394 auto-fit 实现

```jsx
gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))'
```

宽屏自动 4 列、≤720px 2 列、≤360px 1 列 — 完全替代 media query。

### Stage 2 Q5 闭环里程碑叙事

CHANGELOG v0.5.16 段**显式标注**：

> 视觉语言闭环：ResultBlock (v0.5.14 R-323) / SavedReports (v0.5.15 R-372) / DataSources (v0.5.16 R-386) 全站 **Inset 8% 语义统一** — `color-mix(in oklch, ${T.accent} 8%, transparent)` 字面 byte-equal 三处闭环。

### Stage 3 R-376 Users 严守

tab === 'users' 分支（L9-33）**字面零修改**：
- `linear-gradient(${T.accent}, #ff7a3a)` 渐变残留保留
- avatar + name + email + role chip + handlers 调用方式
- 任何 inline style / className / props 传递字面

commit 1 收尾**强制** L9-33 段 0 行 diff。

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/admin/tab_access.jsx   [EDIT 60 → ≤ 110 行]
                                            Step 1: baseline diff 标定 Users/Sources 边界
                                            Step 2: Summary grid 4 卡片 + tooltip + R-394 auto-fit
                                            Step 3: Table 容器 radius 12
                                            Step 4: thead brandSoft + mono + 0.06em + uppercase + fw 500
                                            Step 5: Grid 5→6 列 + 表数 '—' + tooltip
                                            Step 6: Name brandSoft 28×28 + I.db size={14} 复用
                                            Step 7: Type chip brandSoft + 11px + 0.02em mono
                                            Step 8: 每列 min-width 0 + ellipsis
                                            Step 9: StatusDot 语义粘性
                                            守护: Users L9-33 字面零修改 [R-376]
                                                  + 8 props + 5 handlers + 业务字段 byte-equal [R-374/375/377]
                                                  + inline styles only [R-393]

scripts/check_file_sizes.py                 [unchanged]   LIMIT 250 远未触达
knot/main.py                                [EDIT 0.5.15 → 0.5.16]  [R-392]
tests/test_rename_smoke.py                  [EDIT R-72 smoke 0.5.15 → 0.5.16]  [R-392]
frontend/src/screens/Login.jsx              [EDIT 页脚 0.5.15 → 0.5.16]  [R-392]

frontend/src/screens/admin/ (其他 4 子模块) [unchanged]  [R-390]
frontend/src/screens/chat/*                 [unchanged]  [R-391]
frontend/src/screens/Admin.jsx              [unchanged]  [R-389]
frontend/src/screens/SavedReports.jsx       [unchanged]  [R-389]
frontend/src/screens/ (其余 16 屏)           [unchanged]  [R-389]
frontend/src/App.jsx                        [unchanged]  [R-389]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-389]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]  [R-389]
frontend/src/App.css                        [unchanged 0 行 diff]  [R-393]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.16 段]
                                            R-374~R-398 偿还表
                                            + **全站 Inset 8% 语言闭环里程碑** (v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386)
                                            + **I.db 复用先例**（v0.5.x 资产复用 first）
                                            + Loop Protocol v3 第 17 次施行
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.16（Inset 8% 第三处闭环 + I.db 复用先例）
                                            - v0.5.x 路线图 v0.5.16 ⏳ → ✅
docs/plans/v0.5.16-datasources.md           [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.16**

### Stage 2/3 强化验证

- [ ] **R-376 Users 0 改 + 渐变残留保留**：`git diff origin/main HEAD -- tab_access.jsx` L9-33 段 0 diff；grep `#ff7a3a` 仍在
- [ ] **R-385 Sources hex 0**：Sources 部分 grep `#[0-9a-fA-F]{3,6}` | grep -v boxShadow = 0
- [ ] **R-386 闭环 byte-equal**：grep `color-mix(in oklch, \${T.accent} 8%, transparent)` 三文件命中
- [ ] **R-383 Q3 I.db 复用**：grep `<I\.db` ≥ 1
- [ ] **R-394 响应式**：grep `auto-fit\|minmax(180` 命中；三档窗宽实测
- [ ] **R-395 Type chip**：grep `fontSize: 11` + `letterSpacing: '0.02em'`
- [ ] **R-396 列宽稳定**：每列 grep `minWidth: 0` + `textOverflow: 'ellipsis'`
- [ ] **R-397 Icon 居中**：28×28 `display: 'flex', alignItems: 'center', justifyContent: 'center'`
- [ ] **R-398 StatusDot 语义粘性**：status 字段颜色映射逻辑 byte-equal
- [ ] **Q1/Q2 tooltip**：grep `title="后端数据对接中 \(v0\.6\+\)"` ≥ 3

### 契约守护

- [ ] **R-374**：TabAccess 8 props 签名 byte-equal
- [ ] **R-375**：users/sources 数据流 + 5 handlers + roleChip byte-equal
- [ ] **R-377**：Sources 业务字段 byte-equal

### 范围守护

- [ ] **R-389**：App/api/index.css/main/utils/Shared/Shell/decor/16 屏/Admin/SavedReports `git diff` = 0
- [ ] **R-390**：admin/ 其他 4 子模块 0 改
- [ ] **R-391**：chat/ 7 子模块 0 改
- [ ] **R-393**：App.css 0 行 diff；KnotLogo 仍 3 文件

### 端到端

- [ ] 进 Admin → 切 admin-sources tab → Summary grid + 6 列 + db icon + chip
- [ ] 切 admin-users tab → 视觉**完全原始**（含橘色渐变残留）
- [ ] 编辑/删除按钮调用 onEditSource/onDeleteSource
- [ ] **三档窗宽实测**（1024/1280/1920）
- [ ] **超长主机名 ellipsis 兜底实测**
- [ ] v0.5.0~v0.5.15 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.16): tab_access.jsx Sources 部分视觉重构 — Summary grid + 6 列 + I.db 复用 + type chip + brandSoft 8% 第三处闭环 (R-374~R-398)` | npm build + 端到端双 tab + 三档窗宽 + Users 渐变残留保留 + R-386 闭环 |
| 2 | `chore(v0.5.16): version bump 0.5.15→0.5.16 三处同步 (R-392)` | 全闸门 + version=0.5.16 |
| 3 | `docs(v0.5.16): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + Inset 8% 三处闭环里程碑存档 + I.db 复用先例` | 全闸门 |

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 17 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 25（R-374~R-398）
- ✅ **首个 admin tab 子模块复刻** — Users out-of-scope 模式（R-376）
- ✅ **全站 Inset 8% 设计语言三处闭环** — v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386 字面 byte-equal
- ✅ **I.db 复用先例** — v0.5.x 资产复用模式（前几 PATCH 用 inline svg 字典；本 PATCH 起优先复用 Shared）
- ✅ R-394 响应式契约 — auto-fit grid 替代 media query
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 16 屏 + App.css byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.16-datasources`
2. ✅ R-376 Users 严守 — 绝对不碰 L9-33；Stage 2 + 3 双重强制
3. ✅ Q3 I.db 复用纪律 — v0.5.x 第一次资产复用先例；未来 Shared 有图标优先复用
4. ✅ R-394 auto-fit 替代 media query — inline style 限制下最佳实践
5. ✅ R-386 闭环 byte-equal — 字面与 v0.5.14 R-323 + v0.5.15 R-372 严格一致
6. ✅ Step 1→9 顺序锁死
7. ✅ Q1/Q2 tooltip 落地 — 4 处 `title="后端数据对接中 (v0.6+)"`
8. ✅ R-396 ellipsis 实测 — 超长主机名兜底
9. ✅ R-397 brandSoft 28×28 居中
10. ✅ R-398 StatusDot 语义粘性 — 颜色字段映射不动
11. ✅ D6 Users out-of-scope 严守 — 未来 admin tabs 系列覆盖 Users 时独立 PATCH
12. ✅ 里程碑叙事 — CHANGELOG/CLAUDE.md 显式标注 Inset 8% 三处闭环 + I.db 复用先例
13. ✅ 范围抗诱惑 — 不顺手改 admin 其他 tab / chat / Users 渐变

---

🛡️ **执行者收到 GO 信号；首个 admin tab 子模块复刻正式启动；全站 Inset 8% 设计语言三处闭环 + I.db 复用先例 + R-376 Users 绝对 out-of-scope 三大设计先例同时落地。**
