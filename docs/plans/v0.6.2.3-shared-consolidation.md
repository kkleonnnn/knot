# v0.6.2.3 + v0.6.2.4 — Shared.jsx inline helper 整合（段 3）· Stage 1 执行者草案

> **性质**：Stage 1 **执行者草案**，**非** LOCKED。下一步 → Stage 2 辅助 AI 初审 → Stage 3 守护者终审 → Stage 4 落地。
> **协议**：业务代码重构（前端）→ **完整 Loop Protocol v3 三阶段**，**守护强 active**，**不适用简化协议**（最高风险 PATCH）。
> **偿还承诺**：v0.5.17→v0.5.22 累积「8+ inline helpers 移入 Shared.jsx」技术债（CLAUDE.md 多处登记，累计第六次复用确认）。
> **日期**：2026-06-12 · 执行者 v0.6 Agent。
> **拆 2 PATCH**：PATCH-1 = v0.6.2.3（加法机械搬运·证明模式）；PATCH-2 = v0.6.2.4（drift 调和·守护逐条 sign-off）。版本号为 PATCH 级（纯前端重构，无新屏/新 endpoint），待资深确认 `.3 / .4` 命名。

---

## 0. 背景 + 探查方法论（为何此草案可信）

本草案的 helper 普查经历**两轮**，第二轮推翻第一轮——这本身是 Stage 1 严谨性的证据：

- **第一轮**：4 路并行探查 agent + 综合 + **对抗式 critic**。critic 发现**致命漏查**：探查遗漏 3 个 v0.6.x 新增屏（`AdminMetrics.jsx` / `AdminQueryHistory.jsx` / `AdminErrors.jsx` + `ForceChangePassword.jsx`），导致三大旗舰 helper（KpiCard / PeriodTab / TagChip）副本数被低估、PATCH 分配错误。
- **第二轮**：执行者**亲自直读 + brace-matched md5** 复核每个跨屏 helper 函数体（不信任 agent 提取），定论如下表。

**md5 复核定论**（执行者直读 frontend/src/screens/，brace-matched 函数体）：

| Helper | 副本 | byte-equal 状态（md5）| 修正后 PATCH |
|---|---|---|---|
| **KpiCard** | 2（AdminMetrics:12 + AdminRecovery:14）| **byte-equal** `7058f47b…` | PATCH-1（双屏同改）|
| **PeriodTab** | 3 | AdminMetrics==AdminRecovery `031b07f5…`；AdminQueryHistory **DRIFTED** `a01ff634…`（28px/r6/无 shadow vs 30px/r8/shadow）| **PATCH-2** |
| **TagChip** | 3 | AdminMetrics==AdminRecovery `af637ebc…`；AdminQueryHistory **DRIFTED superset** `b603561…`（多 `kind=` 参数 + colors map）| **PATCH-2** |
| **statLabelStyle** | 2 | AdminAudit `c5145147…`（fn, 10.5）≠ AdminBudgets `e78d00ac…`（arrow, 11）**DRIFTED** | PATCH-2 |
| **SvgPath** | 5 | 全 byte-equal（critic md5 `16ab3d0d…`）| PATCH-1（5 文件同改）|
| **KV / DetailRow** | 1+1 | sibling：AdminAudit KV `{T,k,v,mono}` vs AdminQueryHistory DetailRow `{T,k,v,kind}` | PATCH-2（合并调和）|
| **Avatar** | 3+ | **无命名 def** = inline JSX 圆形（22×22 brandSoft + initial）跨 AdminAudit/AdminRecovery/tab_access | PATCH-2（提取 inline → 组件）|

**critic 8 点整改全部吸收**（详 §1 / §3 / §5）。`AdminErrors.jsx` 仅注释提及 KpiCard，**无自有 def**（不增副本）；`ForceChangePassword.jsx` 含 `fieldStyle(T)`（并入 input-style family → PATCH-2）。

---

## 1. 决议（scope）

### 1.1 总目标
把散落 18+ 屏的 inline helper 整合进 `frontend/src/Shared.jsx`（视觉 Foundation 单一 import 入口），偿还跨 6 个 PATCH 的技术债。**自 v0.5.6 Foundation 冻结以来首次受准修改 Shared.jsx**。

### 1.2 PATCH-1（v0.6.2.3）— 加法机械搬运（证明模式）
**仅** single-copy 或 md5-byte-equal 多副本 helper，每条**纯机械**（加 export → 删 local → 换 import），守护者用 git-diff 逐条验证「删除体 == 新 export 体 byte-equal」，**0 reconciliation 判断**。

| # | Helper | 来源 | Shared 命名 | 备注 |
|---|---|---|---|---|
| 1 | **SvgPath** | 5 文件 byte-equal | `SvgPath` | 旗舰；5 文件同 PATCH in-scope |
| 2 | **KpiCard** | AdminMetrics + AdminRecovery byte-equal | `KpiCard` | **双屏同改**（R-PB-SH-5）|
| 3 | StatusDot | AdminAudit | `StatusDot` | 源注释已标 R-412 v0.6 候选 |
| 4 | NumChip | tab_system | `NumChip` | R-557 候选 |
| 5 | OverrideChip | tab_system | `OverrideChip` | |
| 6 | SourceTag | tab_system | `SourceTag` | 仅 formal lift；tab_knowledge inline span 合并推 PATCH-2 |
| 7 | FileIcon | tab_knowledge | `FileIcon` | |
| 8 | LetterChip | ThinkingCard | `LetterChip` | |
| 9 | DoneCheck | ThinkingCard | `DoneCheck` | |
| 10 | Row → **FormRow** | AdminBudgets | `FormRow` | 改名避免泛 `Row` 碰撞 |
| 11 | Field → **FilterField** | AdminAudit | `FilterField` | 改名避免泛 `Field` 碰撞 |
| 12 | statCardStyle | AdminAudit | `statCardStyle` | `(T)=>{}` 工厂 |
| 13 | statValueStyle | AdminAudit | `statValueStyle` | **恢复**（critic 发现被综合漏掉）；保 `color:T.muted` 怪癖 byte-equal |
| 14 | preStyle | AdminAudit | `preStyle` | |

→ **PATCH-1 新增 14 export**（12 → 26）。

### 1.3 PATCH-2（v0.6.2.4）— drift 调和（守护逐条 sign-off）
每条需 **canonical-body 决策**，非机械搬运：

| Helper | 调和决策 |
|---|---|
| **PeriodTab** | 2 byte-equal + AdminQueryHistory drifted → 定 canonical body（保 30px/r8/shadow？）+ AdminQueryHistory 调用点视觉位移 sign-off OR 参数化 |
| **TagChip** | 2 byte-equal + AdminQueryHistory superset(`kind=`) → 采用 superset API 并迁移 2 个简单调用点 OR 保两份 |
| **statLabelStyle** | fontSize 10.5(fn) vs 11(arrow) → 定 canonical OR 参数化 `statLabelStyle(T, fontSize=11)` |
| **KVRow + DetailRow** | `{T,k,v,mono}`(AdminAudit) vs `{T,k,v,kind}`(AdminQueryHistory) → 合并为统一 KVRow 或保 sibling（critic 点 4：KV 不可单独 PATCH-1 提取）|
| **input-style family** | inpStyle(AdminBudgets) + inputStyle(AdminAudit) + fieldStyle(ForceChangePassword) → 统一 vs 保 distinct + 命名 |
| **button-style family** | ghostBtnStyle + primaryBtnStyle + pageBtnStyle(AdminAudit) → lift-as-is vs fold into pillBtn variants（防 Foundation 内 button 工厂泛滥）|
| **theadStyle** | tab_knowledge formal + tab_access L32/L83 inline + tab_resources L92 inline + **tab_access summary labels L64-77**（critic 点 10 扩面）→ 抽 typography 子集 spread，保每表 gridTemplateColumns inline |
| **actionColor + ActionChip** | audit-domain 耦合（auth./budget./export. 前缀）→ 进 Foundation vs 留 AdminAudit |
| **trailingChip → FilledChip** | 唯一 in-body closure（TabResources 内）→ 转 `({T,value})` prop 形态（refactor 非 copy）|
| **Avatar** | 验证 22×22 brandSoft initial 圆 byte-equal 子集（AdminAudit/AdminRecovery/tab_access）→ 提取 inline JSX 为组件（注意 `50%` radius 在 8 文件出现，非全是 avatar，需筛真 avatar 子集）|
| **SavedReports.pillBtn 碰撞** | local `pillBtn(T,primary)` 与 Shared 冻结 `pillBtn` **同名异体** → 改名 `pillBtnCompact` OR 迁 7 调用点（像素位移）|
| ~~SparkIcon / FlowIcon~~ | **不进 Shared**（资深 2026-06-12 拍板）— 实证仅 ChatEmpty 单屏单用、非重复；按整合「只 DRY 重复项」原则保留 inline，strokeWidth 1.8 不动，**I dict 恒 38**。绕开 R-158 contract-touch + strokeWidth 之争。详 §8 |
| **path 常量** | ARROW_UP/DOWN/SHIELD/TRIANGLE/INFO：SHIELD 复用既有 `I.shield`（I.db 复用先例）；其余 v0.6.0.2 故意解耦，**倾向 DEFER**（集中 SVG_PATHS dict 会逆转既定设计）|

→ **PATCH-2 新增约 8-12 export**（含 dedup 决策），落 ~26→34-38。

### 1.4 DEFER（两 PATCH 均不动 — 越出 Shared 整合范围）
- **3 modals（UserFormModal/SourceFormModal/FewShotModal）+ FeedbackBar** — 依赖 utils.jsx，进 Shared 会造 **Shared→utils 循环**（R-PB-SH-8）；若整合 → utils.jsx 独立 PATCH
- **屏/域 feature 组件**（PromptsView/DetailView/EmptyView/MetricCard/TableContainer/InsightCard/BudgetBanner/ErrorBanner/TokenMeter）— 已在各自文件，非 Foundation primitive
- **屏数据 dict**（_AGENTS/PERIOD_LABELS(EN/ZH drifted)/_PAGE_SIZES/_REDACTED_RE/SAVED_SVG/INTENT_EMOJI/RB_SVG/AGENT_KIND_EMOJI/CHART_BTNS/ERROR_KIND_*）
- **纯 JS format/util**（_relativeTime/_heartbeatRelative/_relTime/_sinceFromPreset/formatTime/safeParseRows/exportAuditCsv/exportMessageCsv/fmt/fmtCost）→ 非视觉，未来 format-util 另议（R-PB-SH-12）
- **roleChip**（Admin.jsx:283 in-body）— R-520 sustained 冻结在原位

---

## 2. 红线（R-PB-SH-1 ~ R-PB-SH-12）

| 红线 | 内容 |
|---|---|
| **R-PB-SH-1** | **R-365 正式退役**。Shared.jsx/utils.jsx 从「git-diff=0 绝对红线」转为「既有 exports byte-equal + diff 仅含新增」。守护测试同步从 0-diff 改 additive-only 校验。**首次受准修改 Foundation**。|
| **R-PB-SH-2** | 既有 **12 Shared + 8 utils** export 的**名 + props 签名 + 函数体 byte-equal**（18 屏 import 不破）。KnotMark/KnotWordmark/KnotLogo 仍限 Shared+Login+Shell 3 文件（R-199.5/222 sustained）。|
| **R-PB-SH-3**（资深口径锁定 2026-06-12）| buildTheme **26 个 runtime key** = **25 设计 token + 1 `dark` 布尔透传**（node 实测 `Object.keys(buildTheme(true)).length===26`，含 line 55 `dark,` shorthand）。**CLAUDE.md/header「25 字段」正确**（指 25 设计 token，不含 dark — **非陈旧**）。自验证测试 assert `===26`（守护者 §V.5 原拟 `==25` 按 Object.keys 实现会 CI 红，已纠正）。新 helper 严禁加 token，需新色用 `color-mix(in oklch)` inline。保留怪癖（statValueStyle `color:T.muted` 不"修正"）。|
| **R-PB-SH-4**（资深修订 2026-06-12）| I icon dict **38 names 契约恒定**（PATCH-1 + PATCH-2 **均不动**）。SparkIcon/FlowIcon **不进 I dict**（资深拍板 — 单屏单用非重复留 inline），故无 38→40 contract-touch、无 strokeWidth 1.6/1.8 夹带货之争（R-186 抗诱惑 sustained）。|
| **R-PB-SH-5** | **多屏 helper 一次性提取铁律**：跨屏 helper 提取必须同 PATCH 编辑其**所有 def + 调用屏**。KpiCard=AdminMetrics+AdminRecovery 双屏 in-scope；SvgPath=5 文件 in-scope。**严禁单屏阶段化提取**（防 Shared-AND-inline 双真源——critic 核心警告）。|
| **R-PB-SH-6** | **PATCH-1 = 纯加法机械搬运**：仅 single-copy 或 md5-byte-equal 多副本；守护者逐条 git-diff 验证「删除 local body == 新 Shared export body byte-equal」；0 reconciliation。|
| **R-PB-SH-7** | **PATCH-2 = drift 调和**：每条 canonical-body 决策守护者逐条 sign-off（§1.3 全表）。|
| **R-PB-SH-8** | **cycle 守护**：Shared.jsx 严禁 import utils.jsx。3 modals + FeedbackBar 排除出 Shared。|
| **R-PB-SH-9** | **新增 CI LIMIT**：check_file_sizes.py 纳入 `frontend/src/Shared.jsx`(~520) + `frontend/src/utils.jsx`(~200)（此前两文件无 cap；R-365 解除后防无界增长）。注意路径在 `frontend/src/` 非 `frontend/src/screens/`。|
| **R-PB-SH-10** | **in-scope 屏预声明**：AdminMetrics.jsx + AdminQueryHistory.jsx 当前 branch **clean**（仅 modals/tab_access dirty）；编辑它们是 intentional in-scope，非意外破 byte-equal。本草案 §3 预声明全部受影响屏。|
| **R-PB-SH-11**（资深修订 + 守护者 §V.5 强化）| **stale-doc 修正 + 自验证**：仅 I icon count 陈旧（**36→38**）；**buildTheme「25 字段」不改**（指 25 设计 token，正确）。改 header 前先 grep 确认无守护测试匹配字面。守护测试动态比对替代人工维护：`assert Object.keys(I).length===38` + `assert Object.keys(buildTheme(true)).length===26`。|
| **R-PB-SH-12** | **纯 JS format/util 排除**：非视觉函数不进视觉 Foundation。|

**Sustained**：R-286 hex 全禁（新 helper 用 T token / color-mix in oklch）；R-313 rgba 豁免边界（boxShadow/overlay）；R-PA-PB-V1 视觉设计语言铁律；字面分流体系（R-227.5 / R-227.5.1 / R-302.5）。

---

## 3. 文件改动（in-scope 预声明）

### PATCH-1（v0.6.2.3）受影响文件
- **frontend/src/Shared.jsx** — +14 export（加法）+ header count 修正（R-PB-SH-11 前置校验后）
- **frontend/src/screens/AdminMetrics.jsx** — 删 KpiCard，import Shared【R-PB-SH-5/10】
- **frontend/src/screens/AdminRecovery.jsx** — 删 KpiCard，import Shared【R-PB-SH-5】
- **frontend/src/screens/AdminAudit.jsx** — 删 StatusDot/Field→FilterField/statCardStyle/statValueStyle/preStyle，import
- **frontend/src/screens/AdminBudgets.jsx** — 删 Row→FormRow，import
- **frontend/src/screens/admin/tab_system.jsx** — 删 NumChip/OverrideChip/SourceTag，import
- **frontend/src/screens/admin/tab_knowledge.jsx** — 删 FileIcon，import
- **frontend/src/screens/chat/ThinkingCard.jsx** — 删 LetterChip/DoneCheck，import
- **frontend/src/screens/SavedReports.jsx + chat/ResultBlock.jsx + chat/ResultBlock/{BudgetBanner,InsightCard,TokenMeter}.jsx** — 删 SvgPath（5 处），import【R-PB-SH-5 5 文件同改】
- **scripts/check_file_sizes.py** — 加 Shared.jsx/utils.jsx LIMIT【R-PB-SH-9】
- **knot/main.py + tests/test_rename_smoke.py** — version 0.6.2.2→0.6.2.3（三处同步）
- **CLAUDE.md** — R-158 修正：I icon **36→38**；buildTheme **25 设计 token 不动**（加口径注「26 keys = 25 token + dark」消歧）+ 路线图 v0.6.2.3 行
- **守护测试** — Shared/utils byte-equal 测试改 additive-only【R-PB-SH-1】

### PATCH-2（v0.6.2.4）受影响文件
+ AdminQueryHistory.jsx【R-PB-SH-10 in-scope】、tab_access.jsx、tab_resources.jsx、ForceChangePassword.jsx、各 drift 屏。逐条 §1.3。（ChatEmpty.jsx **不在 PATCH-2** — SparkIcon/FlowIcon 留 inline，资深拍板）

### 绝对不动（byte-equal 守护）
App.jsx / Shell.jsx / main.jsx / index.css / App.css / Login.jsx / Chat.jsx / Conversation.jsx / Composer.jsx / Admin.jsx（roleChip 在内）/ modals.jsx / FeedbackBar.jsx / 后端全部。

---

## 4. 验收

### PATCH-1
1. **既有 12 Shared + 8 utils export** 名/签名/体 byte-equal（`git diff` 仅显新增 export 行）【R-PB-SH-2】
2. 每条搬运：删除的 local body 与新 Shared export body **byte-equal**（守护者 git-diff 逐条核）【R-PB-SH-6】
3. KpiCard：AdminMetrics + AdminRecovery **均**改为 import，无残留 inline def（grep `function KpiCard` 仅 Shared 1 命中）【R-PB-SH-5】
4. SvgPath：5 文件均 import，grep `const SvgPath` 仅 Shared 1 命中
5. buildTheme 仍 26 keys（25 token + dark）；I 仍 38 names（PATCH-1 不动）【R-PB-SH-3/4】
6. Shared.jsx/utils.jsx 进 check_file_sizes 且未超 LIMIT【R-PB-SH-9】
7. `npm run build` 绿；前端 lint 绿；boot smoke 绿（version 0.6.2.3）
8. **18 屏 + 子模块视觉肉眼无变化**（dark+light；机械搬运 = 0 视觉漂移）
9. 后端测试数不变（前端重构 0 后端改动）；7 contracts KEPT；92 routes 不变

### PATCH-2
逐条 canonical-body 决策守护者 sign-off；drift 屏视觉位移**显式确认**（如 PeriodTab AdminQueryHistory 28→30px）；I dict 若扩 header count 同步。

---

## 5. commit 序列（PATCH-1 草案 · 待 Stage 3 终审）

> 原则：**Shared 加 export 与删 local 同 commit**（避免中间 import 失败）；按 helper 簇分 commit，守护者逐 commit git-diff 验 byte-equal。

- **commit 1**：R-PB-SH-11 前置 grep 校验（无 guard 匹配 header）→ 守护测试改 additive-only + check_file_sizes 加 LIMIT（红线基础设施先行）
- **commit 2**：SvgPath 旗舰（Shared +1 / 5 文件删 import）— 证明加法-搬运模式 + 守护者 git-diff 验 5 处 byte-equal
- **commit 3**：KpiCard 双屏（Shared +1 / AdminMetrics + AdminRecovery）【R-PB-SH-5 试金石】
- **commit 4**：chip/badge 簇（StatusDot/NumChip/OverrideChip/SourceTag/FileIcon/LetterChip/DoneCheck）
- **commit 5**：layout/style 簇（FormRow/FilterField/statCardStyle/statValueStyle/preStyle）
- **commit 6**：version 三处同步 0.6.2.3 + CLAUDE.md R-158 count 修正 + 路线图行 + CHANGELOG
- （PATCH-2 commit 序列待 PATCH-1 落地 + 守护者复盘后另起）

---

## 6. 协议合规

- **完整 Loop Protocol v3**：Stage 1（本稿）→ Stage 2 辅助 AI 初审 → Stage 3 守护者终审 → Stage 4 落地。**守护强 active**，**不走简化协议**（业务代码 + 多新红线 + 最高风险，对比 v0.6.2.2 扩展适用三条件**不满足**「范围窄单 bug」）。
- **非 OVERRIDE**：本 PATCH 在 v0.6.x LOCKED 路线图 段 3 范围内（既定计划兑现），不计入 §1 OVERRIDE 累计表（sustained 3 次）。
- **R-365 退役**属红线**演进**（非撤回）：从绝对冻结转 additive-only，由偿还承诺驱动；守护者 Stage 3 须 explicit ack「R-365 退役合理 + R-PB-SH-1 替代契约充分」。
- 关联 Task #33（主）/ #16（代码结构评估）。

---

## 7. 自检（执行者 Stage 1 完成度）

- ✅ helper 普查两轮（对抗式 critic + 执行者 md5 复核），3 漏查屏已补
- ✅ 跨屏 helper 副本数 byte-precise 定论（KpiCard 2 eq / PeriodTab 3:2eq+1drift / TagChip 3:2eq+1drift / statLabelStyle 2 drift）
- ✅ PATCH-1/PATCH-2 修正分配（PeriodTab/TagChip 从 PATCH-1 移 PATCH-2；statValueStyle 恢复；Avatar 纳入 PATCH-2；KV+DetailRow 合并）
- ✅ R-365 退役契约 + 12 新红线
- ✅ 多屏一次性提取铁律（R-PB-SH-5）防 Shared-AND-inline 双真源
- ✅ cycle 守护 + LIMIT 新增 + stale-doc 前置校验 + in-scope 预声明
- ⏳ **待 Stage 2**：辅助 AI 初审（红线粒度 / 命名 / commit 序列 / Avatar 提取风险 / I dict contract-touch 时机）
- ⏳ **待 Stage 3**：守护者终审（R-365 退役 ack / PATCH-1·2 边界 / drift canonical-body 预意见）

---

**附：探查产物**（workflow `wf_f1e2d282-98f`，6 agent / ~953k tokens）落 `/private/tmp/.../tasks/wyln59c5c.output`，含完整 73-helper 原始普查 + 综合 + critic 8 点整改。本草案 §0 md5 复核为执行者亲验，优先级高于 agent 提取。

---

## 8. Stage 3 守护者终审整合 + 资深 LOCKED（2026-06-12）

> 本节为**权威 LOCKED delta**，整合 Stage 2 第三方初审（4.2/5 修订后批准）+ v0.5 守护者 Stage 3 终审（4.5/5 修订后批准，3 硬前置）+ 资深 4 项拍板。**§5 commit 序列以本节 §8.4 为准**。

### 8.1 资深 4 项拍板（2026-06-12）

| # | 议题 | 拍板 |
|---|---|---|
| 1 | **R-365 退役 ack** | ✅ **ack** — Foundation 自 v0.5.6 冻结以来首次受准修改；红线**演进非撤回**（git-diff=0 绝对 → 静态+运行时 additive-only）；条件 = Shadow Run 运行时双层落地 |
| 2 | **buildTheme 口径** | ✅ **26 总 key** = 25 设计 token + 1 `dark`（node 实测 `Object.keys===26`）；自验证测试 assert `===26`；CLAUDE.md「25 字段」=25 设计 token **正确不动**（详 §8.2 守护者纠错反转）|
| 3 | **红线 12 不精简** | ✅ **保 12 + 4 组分类**（守护者裁定）— 真冗余仅 SH-1↔SH-2 一对，Stage 2 砍到 8 过度（揉合契约/执行两维度）；4 组降心智负担 |
| 4 | **Icon strokeWidth** | ✅ **SparkIcon/FlowIcon 不进 I dict** — 实证仅 ChatEmpty 单屏单用非重复，按整合「只 DRY 重复项」原则保留 inline，1.8 不动，**I dict 恒 38**，绕开 contract-touch + strokeWidth 之争（比守护者方案 A/B 更优 — 质疑了前提）|

### 8.2 守护者 §II.1 纠错的执行者复核反转（治理诚实度）

守护者 awk 列举 buildTheme = 25，裁定「草案 26 错 / CLAUDE.md 25 对」并设为 commit-1 硬前置。**执行者按守护者「不接受单方陈述」原则亲自 node 实测**：`Object.keys(buildTheme(true)).length===26`（含 line 55 `dark,` shorthand）。

**定论（资深拍板）**：两边各对一半 —— 对象有 **26 个 runtime key**（草案数对），其中 **25 个是设计 token**（CLAUDE.md 语义对）。守护者 awk 因匹配 `key:` 冒号**结构性漏掉无冒号的 `dark,` shorthand**（awk 盲区，非判断错）。**守护者 §V.5 钦定的 `assert ==25` 若按 `Object.keys` 实现会 CI 红 → 改 `===26`**。治理意义：执行者用守护者自己的「不接受单方陈述」原则反向纠正了守护者的实证盲区 — 双向独立验证。

### 8.3 守护者补充立约全盘整合（9 项）

| 立约 | 整合动作 |
|---|---|
| **R-PB-SH-1 强化** | additive-only **双层守护**：① 静态 git-diff byte-equal ② **Shadow Run 运行时**（import `Shared.old.jsx` 备份 + 修改后，双 theme 实例，全 12 旧 export `react-test-renderer` toJSON `deepEqual`，0 行为漂移）。**commit-1 硬前置 + R-365 退役条件** |
| **R-PB-SH-8 强化** | cycle 守护**工具化**：commit 1 前 AST 依赖扫描（dependency-cruiser 或等效）断言 Shared 0 import utils/screens/api + 14 搬运 helper 0 隐式访问原文件顶层全局 / 0 间接 format 调用。CI 纳入（前端依赖契约首次工具化，对标后端 import-linter）。**commit-1 硬前置** |
| **R-PB-SH-7 强化** | drift 调和**绝对 0 位移**（撤回草案 §1.3「视觉位移 sign-off」）：一律 Optional Props 参数化 — PeriodTab 默认 30px/r8/shadow + AdminQueryHistory 显式 `height={28} radius={6} noShadow`；statLabelStyle 默认 11 + AdminAudit 显式 `(T,10.5)`；TagChip 采 superset `kind=` + 简单调用点显式默认。视觉归一另由资深/UI 发起（不在本 PATCH）|
| **R-PB-SH-11 强化** | 自验证动态比对：`assert Object.keys(I).length===38` + `assert Object.keys(buildTheme(true)).length===26`（替代人工字面维护）|
| **NRP-SH-1 新立** | **md5 免疫黑天鹅守护**：PATCH-1 前全屏 grep 正则扫含 oklch/color-mix 的 inline style 节点（匿名 lambda 无函数名对 md5 免疫）→ 未走 Shared import 的标为潜在隐形副本，纳入范围决策。**commit-1 硬前置（与 AST 同步）** |
| **NRP-SH-2 新立** | **Avatar AST 三条件子集筛选**（PATCH-2）：严禁仅按 `borderRadius:'50%'` 提取（8 文件命中非全 avatar）；须 ① 有宽高 ② 含 initial 文本/占位 ③ 边框 brandSoft 三条件全满足；其余 50% 节点不误伤 |
| **Object.freeze 测试** | 样式工厂传入 `Object.freeze(buildTheme(true))` 验只读性（防深度解构 undefined 字段静默崩塌）|
| **R-PB-SH-3 修正** | 见 §8.1/8.2（26 keys 口径）|
| **R-PB-SH-4 修正** | I dict 恒 38（SparkIcon/FlowIcon 不进 — §8.1.4）|

### 8.4 commit 序列修订（守护者 §VI.2 — 取代 §5）

> 守护者澄清：**R-PB-SH-5 是 PATCH 级非 commit 级** — 防的是 PATCH 结束时双真源，中间 commit 短暂双真源在 PATCH 边界内收敛不违反。故 add/delete 可分 commit（降 Stage 2 revert 成本）。

- **commit 1（红线基础设施 + 3 硬前置）**：① NRP-SH-1 inline lambda grep 扫描 ② R-PB-SH-8 AST 依赖扫描落地 ③ R-PB-SH-1 Shadow Run 测试脚手架（`Shared.old.jsx` 备份机制）④ 守护测试改 additive-only ⑤ check_file_sizes 加 Shared.jsx(~520)/utils.jsx(~200) LIMIT ⑥ buildTheme `===26` + I `===38` 自验证测试 ⑦ R-PB-SH-11 前置 grep（无 guard 匹配 header）→ **守护者 active 复核 3 硬前置后方可 commit 2**
- **commit 2a**：Shared 加 `SvgPath` export（纯加法，5 文件 local 仍在 — 短暂双真源不 import 失败）
- **commit 2b**：5 文件同 commit 删 local SvgPath + 换 import（byte-equal 同源 → 守护者一次 git-diff 验 5 处）
- **commit 3a**：Shared 加 `KpiCard` export（纯加法）
- **commit 3b**：AdminMetrics + AdminRecovery 同 commit 删 local + 换 import【R-PB-SH-5 试金石】
- **commit 4**：chip/badge 簇（StatusDot/NumChip/OverrideChip/SourceTag/FileIcon/LetterChip/DoneCheck）— add/delete 同簇 commit
- **commit 5**：layout/style 簇（FormRow/FilterField/statCardStyle/statValueStyle/preStyle）
- **commit 6**：version 三处同步 0.6.2.3 + CLAUDE.md R-158 修正（I 36→38；buildTheme 25 token 不动 + 口径注）+ 路线图 + CHANGELOG
- **PATCH-1 收官守护**：`grep 'const SvgPath' / 'function KpiCard'` 全 frontend = 仅 Shared 各 1 命中（R-PB-SH-5 PATCH 级单真源验证）→ 守护者 active

### 8.5 PATCH-2（v0.6.2.4）守护者预锁 3 雷区

| 雷区 | 预锁 |
|---|---|
| Icon | ✅ **已解** — SparkIcon/FlowIcon 不进 I dict（§8.1.4），雷区消失 |
| drift 调和 | 绝对 0 位移参数化（R-PB-SH-7 强化 §8.3）|
| SavedReports pillBtn 碰撞 | 实证确认 L371 local `function pillBtn(T,primary=false)`（守护者 §II.7）— 改名 `pillBtnCompact` OR 验 body byte-equal 后迁 7 调用点。PATCH-2 守护逐条 sign-off |

### 8.6 守护者激活时机（执行者遵循）

- ✅ Stage 3 终审（已完成）
- ⏳ **commit 1 后 3 硬前置复核**（Shadow Run + AST + 自验证 — 守护者 active 确认 → 放行 commit 2）
- ⏳ **commit 2/3 add-delete git-diff 逐条验 byte-equal**（强制 active）
- ⏳ **PATCH-1 收官单真源 grep 验证**（强制 active）
- ⏳ **PATCH-2 全程强制 active**（drift 逐条 canonical-body sign-off）

### 8.7 LOCKED 状态

**PATCH-1 (v0.6.2.3) 修订后批准 → Stage 4 可启动**。commit 1 = 3 硬前置（Shadow Run / AST+lambda grep / 自验证测试）先行，守护者 active 复核后放行 commit 2。PATCH-2 (v0.6.2.4) 待 PATCH-1 收官 + 守护者复盘后另起。

**签字**：执行者 v0.6 / 守护者 v0.5（4.5/5 修订后批准）/ 资深 2026-06-12 LOCKED（4 拍板）。
