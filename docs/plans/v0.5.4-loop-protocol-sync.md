# v0.5.4 — Loop Protocol v3 路线图同步（C4）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 5 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

5/5 决策（D1-D5）**与执行者 Stage 1 提议完全一致**，0 修订。
新增 5 条红线（R-134~R-138），分别来自 Stage 2（2 条）+ Stage 3（3 条）。
**特别意义**：本 PATCH 既是 Loop Protocol v3 路线图同步也是协议施行第 5 次 —— 用 v3 协议同步 v3 协议（自我引用闭环）。

## 1. 决议表最终裁定（D1-D5 全部锁定）

| ID | 锁定 | 与执行者提议 |
|---|---|---|
| **D1** README 形式 | ✅ A 内嵌轻量简介 | 一致 |
| **D2** 历史扩展粒度 | ✅ A 回顾摘要表 | 一致 |
| **D3** README 简介深度 | ✅ A 4 级角色 + 三阶段 + 滚动规则 | 一致 |
| **D4** 红线编号 | ✅ A 接续 R-129~R-133 | 一致 |
| **D5** Commit 序列 | ✅ A 3 commit | 一致 |

## 2. 红线最终清单（10 条 R-129~R-138）

### Stage 1 草案原 5 条（保留）

| ID | 内容 | 验证 |
|---|---|---|
| **R-129** | backend 432 pytest passed 严格不变（docs-only） | `pytest tests/ -v` |
| **R-130** | 7 import-linter contracts KEPT；不动 .importlinter | `lint-imports` 0 broken |
| **R-131** | 0 修改 backend / frontend / scripts / tests / requirements / package / pyproject | `git diff --stat` 仅 README.md / CLAUDE.md / CHANGELOG.md / docs/plans/ / knot/main.py(version) / tests/test_rename_smoke.py(R-72) |
| **R-132** | README 既有 7 段标题字面零修改（角色 / 3-Agent / 快速开始 / Docker / 部署 / 技术栈 / 项目结构 / 版本记录） | grep README.md 既有 § 标题字面零修改 |
| **R-133** | CLAUDE.md Loop Protocol v3 段落字面准确性（不引入与 § 角色定义 / § 三阶段评审 / § 角色滚动规则 矛盾的描述） | 字面交叉对比：v3 协议条款不变，仅"施行历史"段扩展 |

### Stage 2 新增 2 条（资深架构师 + Codex）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-134** | **严禁修改协议核心逻辑** — 本 PATCH 仅"同步"与"回顾"；严禁擅自修改 CLAUDE.md 已锁定的 v3 协议条款（评审天数 / 角色滚动触发点 / 整体审核仪式定义等） | 编辑 CLAUDE.md 时仅触碰 L110-114 "v3 协议施行历史"段 + 路线图段 + version 字段；§ 角色定义 / § 三阶段评审 / § 角色滚动规则 / § 远古守护者激活原则 字面 byte-equal |
| **R-135** | **URI 完整性校验** — README 内部跳转链接（指向 CLAUDE.md 的锚点）须手动验证，严禁死链 | 推荐**不带锚点**直接指向 `CLAUDE.md`（避免 GitHub URL-encoded anchor 死链 + 锚点漂移），让读者自行 Ctrl+F 定位 |

### Stage 3 新增 3 条（v0.4 守护者）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-136** | **协议仪式感视觉化** — README 三阶段评审段必须含一行 ASCII 流程图（Stage 1 → Stage 2 → Stage 3 → 执行） | 复用 CLAUDE.md L46-60 ASCII 流程图的简版（不照搬全图避免冗余）；如：`执行者 (Stage 1) → 辅助 AI 初审组 (Stage 2) → 守护者 (Stage 3 终审) → 执行者落地` |
| **R-137** | **角色演化透明度** — 明确标注"执行者/守护者"身份随 MINOR 版本滚动；防止新人误以为存在不可动摇的"技术层级" | README 段须含一句：「角色按 MINOR 滚动 — 当前 MINOR 的执行者 → 下一 MINOR 自动转为守护者；守护者 → 转为远古守护者」；强调"规则治权"而非"人治层级" |
| **R-138** | **docs-only 零漂移** — 除 `knot/main.py` 的 `version` 字符串与 `tests/test_rename_smoke.py` 的 R-72 smoke 字符串，严禁触碰任何 `.py / .js / .jsx` 逻辑行 | `git diff --ignore-space-change knot/ frontend/ tests/ scripts/` 输出仅含 `version="0.5.4"` 与 `0.5.4` smoke 两行变化；任何 `import` / 函数体 / 数据结构变化 = 范围溢出 |

## 3. Stage 2/3 三条隐性指令（落地手段）

### 3.1 Stage 3 §2：回顾表填坑指引

> 重点突出 v0.5.2 (Backend Slim) 的 27 文件行数压制成就

**落地**：CLAUDE.md L113-114 v3 协议施行历史段扩展为 5 行回顾摘要表（v0.5.0~v0.5.3 + 本 PATCH v0.5.4）：

| PATCH | 主题 | 红线 | 关键决策 / 施行特征 |
|---|---|---|---|
| v0.5.0 | KNOT rename + Foundation | R-67~R-79 (13) | 包名 / env 双源 / DB migration / Loop Protocol v3 首次完整施行 |
| v0.5.1 | SQL AST 笛卡尔积硬防御 | R-80~R-93 (14) | sqlglot AST + ReAct `__REJECT_CARTESIAN__` + R-91 计数器 |
| v0.5.2 | 后端代码瘦身 | R-94~R-110 (17) | **27 文件行数压制（4 主 ≤ 350/300/220/220 + 8 新建 ≤ 250）**；sync/async 双胞胎保守不合并；scripts/check_file_sizes.py CI 核验 |
| v0.5.3 | 前端代码瘦身 | R-111~R-128 (18) | Chat.jsx 925 → ≤ 350；Admin tab 7→4 文件按职责合并；className 0 diff 守护 |
| v0.5.4 | Loop Protocol v3 路线图同步 | R-129~R-138 (10) | docs-only；第 5 次 v3 施行；README 加 protocol 简介对外公开治理 |

**Stage 3 明确指示**：v0.5.2 行须显式提"27 文件行数压制"成就（不能只写"后端瘦身"）。

### 3.2 Stage 2 R-135 URI 校验细节

**落地**：README 指向 CLAUDE.md 的链接形式 — 选择**不带锚点**直接指向 `CLAUDE.md`（避免 GitHub URL-encoded anchor 死链 + 锚点漂移），让读者 Ctrl+F 自行定位。

### 3.3 Stage 3 §3：3 commit 单职责严守

**落地**：D5 commit 序列每个内部独立全闸门绿。commit 1 触碰 README + CLAUDE.md 不混 version；commit 2 仅 version + smoke 不混文档（便于 git bisect）；commit 3 仅 CHANGELOG + plan 归档。

## 4. 文件级改动清单（最终版）

```
README.md                        [EDIT] 79 → ~120 (+约 40 行)
                                       新增 § Loop Protocol v3 段（位于 § 项目结构 后 / § 版本记录 前）
                                       含：1 段开场 + 4 级角色简表 + R-136 ASCII 流程图
                                           + R-137 角色滚动透明声明 + 指向 CLAUDE.md 深挖
CLAUDE.md                        [EDIT] L110-114 v3 协议施行历史段扩展
                                       → 5 行回顾摘要表（含 v0.5.2 27 文件行数压制特别提及）
                                       + v0.5.x 路线图 v0.5.4 ⏳ → ✅
                                       (R-134 严守：仅触碰施行历史段 + 路线图 + version；
                                        § 角色定义 / § 三阶段评审 / § 角色滚动规则 字面不动)
knot/main.py                     [EDIT] FastAPI version="0.5.3" → "0.5.4"
tests/test_rename_smoke.py       [EDIT] R-72 smoke 0.5.3 → 0.5.4

scripts/check_file_sizes.py      [unchanged]   (无新建模块)
.github/workflows/ci.yml         [unchanged]
requirements.txt / package.json / pyproject.toml / .importlinter   [unchanged]

CHANGELOG.md                     [EDIT] [Unreleased] v0.5.4 段
                                       (R-129~R-138 偿还表 + Loop Protocol v3 第 5 次施行存档)
docs/plans/v0.5.4-loop-protocol-sync.md   [本锁定手册]
```

## 5. 验收清单（最终版）

### 5.1 必须全绿

- [ ] `pytest tests/ -v` = **432 passed / 112 skipped**（R-129 严格不变）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken（R-130）
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（既有 LIMITS 不动）
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.4**
- [ ] `npm run build` 通过（前端无改动 — 增量构建无效但不破坏）

### 5.2 Stage 2/3 强化验证

- [ ] **R-134**：`git diff CLAUDE.md` 中 § 角色定义 / § 三阶段评审 / § 角色滚动规则 / § 远古守护者激活原则 段落字面 byte-equal（仅 § v3 协议施行历史 + § v0.5.x 路线图变动）
- [ ] **R-135**：README 内部链接手动点击验证（IDE 或 GitHub preview）— 0 死链
- [ ] **R-136**：README 含 ASCII 三阶段流程图（一行）
- [ ] **R-137**：README 含"角色按 MINOR 滚动"明确声明
- [ ] **R-138**：`git diff --ignore-space-change knot/ frontend/ tests/ scripts/` 仅含 `version="0.5.4"` + `0.5.4` smoke 两行；任何函数体 / import / 数据结构 diff = 范围溢出

### 5.3 不破坏

- [ ] R-132 README 既有 7 段标题（角色 / 3-Agent / 快速开始 / Docker / 部署 / 技术栈 / 项目结构 / 版本记录）字面零修改
- [ ] R-133 CLAUDE.md v3 协议条款无矛盾
- [ ] backend / frontend 代码 0 触碰（除 version + smoke）
- [ ] requirements.txt / pyproject.toml / package.json / .importlinter 不动

## 6. Commit 序列（最终锁定 — 3 commit；D5 单文件单职责原则）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `docs(v0.5.4): README + CLAUDE.md 同步 Loop Protocol v3 + v0.5.0~v0.5.3 4 PATCH 回顾 (R-129~R-137)` | pytest + lint-imports + ruff + check_file_sizes + npm build | R-132/133/134 字面守护 + R-135 URI + R-136 ASCII + R-137 滚动声明 |
| 2 | `chore(v0.5.4): version bump 0.5.3 → 0.5.4 (R-138)` | 全闸门 + routes=72 + version=0.5.4 | R-72 smoke 同步；`git diff --ignore-space-change` 仅 2 行 |
| 3 | `docs(v0.5.4): CHANGELOG + plan LOCKED 归档` | 全闸门 | R-129~R-138 偿还表 + Loop Protocol v3 第 5 次施行存档 |

每 commit 独立全闸门绿；commit 1 触碰 README + CLAUDE.md 不混 version（D5 单职责）；commit 2 仅 version + smoke 不混文档（便于 git bisect）。

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 5 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 10（v0.5.4 区段 R-129~R-138）
- ✅ docs-only — backend/frontend 0 修改（除 version + smoke）
- ✅ 范围最小化 — 5 项 Out-of-Scope 明确推迟（独立 PROTOCOL.md / 英文版 / 协议本身变更等）

## 8. 给执行者（本 Agent）的执行注意

1. **归档锁定手册** → 本文件覆盖 DRAFT；commit 3 一起提
2. **commit 顺序绝对不可换**：README + CLAUDE.md → version bump → docs/plan + CHANGELOG；D5 单文件单职责严守
3. **R-134 守护**：编辑 CLAUDE.md 前先 `git diff CLAUDE.md` 自查 — 只允许 § v3 协议施行历史 + § v0.5.x 路线图 + version 变动；§ 角色定义 / § 三阶段评审 / § 角色滚动规则 / § 远古守护者激活原则 任何字面变化 = 违规立即 reset
4. **R-135 URI 守护**：建议**不带锚点**直接指向 `CLAUDE.md`（避免 GitHub URL-encoded anchor 死链风险）
5. **R-136 ASCII 流程图**：复用 CLAUDE.md L46-60 简版，不照搬全图（README 重在概览不重深度）
6. **R-137 角色滚动声明**：一句话明确"角色按 MINOR 自动滚动 — 不存在不可动摇的技术层级"，强调规则治权
7. **R-138 zero drift 守护**：每 commit 后**强制**跑 `git diff --ignore-space-change knot/ frontend/ tests/ scripts/ requirements.txt package.json` — 输出超过 2 行（version + smoke）= 范围溢出立即 stash 排查
8. **回顾表填坑**：CLAUDE.md 回顾表 v0.5.2 行**必须**显式含"27 文件行数压制"字眼（Stage 3 §2 明确指示）；不可仅写"后端瘦身"
9. **范围抗诱惑**：本 PATCH 是**第 5 次 v3 施行 + docs-only** — 看到任何"顺手优化代码"/"补充协议条款"/"翻译 README"/"加新章节" 一律不碰，留 v0.5.5+ 评估

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-10）。执行者按本手册实施。
