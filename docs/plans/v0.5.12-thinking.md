# v0.5.12 — (C5+) Thinking 屏复刻（AgentThinkingPanel 右 rail）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 13 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

13/13 决策点（D1-D8 + Q1-Q5）锁定；**0 修订**（所有 D + Q 推荐被采纳，但 Stage 2/3 在 D4/D5/D8 + Q1/Q4/Q5 全面加码新红线）。

**Stage 2 关键加码 4 条**：
- **R-227.5.1**：单字母装饰豁免（K/N/O 不构成完整 KNOT 字面）— 字面分流体系扩展
- **R-286**：hex 全面禁止（不止 #09AB3B）— v0.5.9 R-211 残留色清理模式扩展
- **R-287**：Step count transition cubic-bezier(0.4, 0, 0.2, 1) 0.3s 平滑无闪
- **R-288**：响应式契约 — Panel 272→320 与 Conversation.jsx 0 改兼容

**Stage 3 关键加码 3 条**：
- **R-289** 跨浏览器 letter chip 居中
- **R-290** SSE 鲁棒性兜底（空 events / 中断 / 字段缺失三场景）
- **R-291** Conversation.jsx 调用点字节码对齐

红线总数 27（**R-266~R-291** 含 R-227.5.1）。子步骤增至 **9 步**。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 — 13/13 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** 保 3 agent | ✅ A **绝对同意** | Trace 留 v0.6+；尊重后端现状 |
| **D2** emoji → letter chip K/N/O | ✅ A | Stage 1 |
| **D3** Agent name 字面新增 | ✅ A mono uppercase | Stage 1 |
| **D4** Panel 272→320 | ✅ A + R-288 main margin-right 自适应 | Stage 2 加码响应式契约 |
| **D5** Header step count 实时 | ✅ A + R-287 transition cubic-bezier | Stage 2 加码动效 |
| **D6** 卡片 bg T.content + radius 10 | ✅ A | Stage 1 |
| **D7** svg checkmark done 态 | ✅ A 保 TypingDots + ○ | Stage 1 |
| **D8** substeps slice 80→120 | ✅ A + `text-overflow: ellipsis` 兜底 | Stage 2 |
| **Q1** K/N/O 装饰豁免 | ✅ A **R-227.5.1 单字母装饰豁免** | Stage 2 强制加码 |
| **Q2** tagline 4 字母 vs 屏 3 字母 | ✅ A 接受现状 | Stage 1 |
| **Q3** sqlSteps 120 字符 | ✅ A + ellipsis 兜底 | Stage 2 加码 |
| **Q4** step count 闪烁 | ✅ A + R-287 transition 0.3s cubic-bezier | Stage 2 强制加码 |
| **Q5** #09AB3B 残留偿还 | ✅ A **R-286 严禁任何 hex 硬编** | Stage 2 强制加码 |

---

## 2. 红线最终清单（27 条 R-266~R-291 含 R-227.5.1）

### Stage 1 草案原 20 条（保留）

| ID | 摘要 |
|---|---|
| **R-266** | ThinkingCard + AgentThinkingPanel 2 exports 签名 byte-equal |
| **R-267** | AGENT_LABELS / AGENTS 3 entries key 字面 byte-equal |
| **R-268** | getStatus / getDoneOutput / sqlSteps 业务逻辑 byte-equal |
| **R-269** | output 字段访问 byte-equal |
| **R-270** | ThinkingCard.jsx ≤ 160 行 |
| **R-271** | backend 432 tests / 7 contracts / 72 routes |
| **R-272** | App / api / index.css / main / utils / Shared / Shell / NarrativeMotif byte-equal |
| **R-273** | 三处版本同步 0.5.11→0.5.12 |
| **R-274** | KnotLogo R-199.5/222 sustained — 不蔓延 ThinkingCard |
| **R-275** | Conversation + chat/ 其他 5 子模块 0 改 |
| **R-276** | Panel 272 → 320（D4） |
| **R-277** | Agent emoji → letter chip K/N/O 22×22 brand bg + Inter 800（D2） |
| **R-278** | Agent name 字面新增 mono uppercase letterSpacing 0.08em（D3） |
| **R-279** | 卡片 bg T.content + radius 10 + padding 12（D6） |
| **R-280** | Header step count "N/3 STEPS" mono 右侧（D5） |
| **R-281** | Status indicator done 态 svg checkmark 11×11 stroke 2.5（D7） |
| **R-282** | sqlSteps substeps tag chip + text；slice 80→120（D8） |
| **R-283** | ThinkingCard 行内卡片 radius 8→10 |
| **R-284** | CSS 0 污染 |
| **R-285** | check_file_sizes.py LIMIT ThinkingCard 250→160 |

### Stage 2 新增 4 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-227.5.1** | **单字母装饰豁免** — K/N/O 单字母 Chip 认定为"导航标识/图标替代物"，豁免 R-126/R-227 全大写字面强制 | 仅 ThinkingCard.jsx letter chip 场景；如未来出现完整 "KNOT" 字面仍守 R-126；与 v0.5.10 R-227.5 共属字面分流体系 |
| **R-286** | **Hex 硬编全面禁止** — ThinkingCard.jsx 严禁任何 `#09AB3B` 或其他 hex；状态色必须用 T.success / T.error / T.accent | grep `#[0-9a-fA-F]{3,6}` ThinkingCard.jsx = 0；boxShadow rgba() 例外（v0.5.11 豁免延续） |
| **R-287** | **Step Counter transition 动效** — `transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)` 防 SSE 频繁推送视觉跳变 | Header step count span inline；commit 1 后手测 3 agent 顺序触发数字平滑过渡无闪烁 |
| **R-288** | **Right Rail 响应式契约** — Panel 272→320 + Conversation.jsx 0 改 + 1280/1440/1920 三档窗宽实测 | 起手前 grep `marginRight\|margin-right\|272\|320` Conversation.jsx 标定；如硬编 272 走 Panel 内部自补；不动 Conversation |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-289** | **Letter Chip 视觉对齐** — K/N/O 22×22 内绝对居中，跨浏览器重心一致 | inline `display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: "'Inter', sans-serif", fontWeight: 800`；Chrome / Safari / Firefox 实测 |
| **R-290** | **SSE 状态映射鲁棒性** — events 空 / 中断 / 字段缺失三场景兜底无 undefined 崩溃 | optional chaining + `\|\|` 默认值；commit 1 后强制手测三场景 |
| **R-291** | **2 exports 调用点字节码对齐** — Conversation.jsx 调用 ThinkingCard / AgentThinkingPanel 处 byte-equal | commit 1 收尾 `git diff origin/main HEAD -- frontend/src/screens/chat/Conversation.jsx` = 0 行 |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死）

> 先 K/N/O Letter Chip → 右 rail 宽度 → SSE step count + checkmark 逻辑绑定

| Step | 动作 | 红线 |
|---|---|---|
| 1 | letter chip helper K/N/O 22×22 brand bg + Inter 800 + flex 居中 | R-277 + R-289 |
| 2 | emoji → letter chip + Agent name 字面新增（Knowledge/Nexus/Objective mono uppercase） | R-277 + R-278 + R-227.5.1 装饰豁免 |
| 3 | Panel 272→320 + R-288 main margin-right 自适应实测（3 档窗宽） | R-276 + R-288 |
| 4 | 卡片 bg T.content + radius 10 + padding 12 + ThinkingCard radius 8→10 | R-279 + R-283 |
| 5 | Header step count "N/3 STEPS" + R-287 transition cubic-bezier(0.4, 0, 0.2, 1) 0.3s | R-280 + R-287 |
| 6 | Status indicator done 态 svg checkmark 11×11 stroke 2.5 + 保 TypingDots/○ | R-281 |
| 7 | sqlSteps substeps tag chip + slice 80→120 + textOverflow ellipsis 兜底 | R-282 + D8 |
| 8 | hex 硬编清理 `#09AB3B` → `T.success`；grep 0 命中 | R-286 |
| 9 | events 空/异常兜底 + optional chaining；三场景手测 | R-290 |
| 收尾 | grep + R-291 Conversation diff=0 + R-288 三档窗宽 + npm build + 端到端真实 SSE 手测 | 见 §5 验收 |

### Stage 3 §3 验收关键 — 端到端 SSE 真实手测

```bash
# R-291 字节对齐校验
git diff origin/main HEAD -- frontend/src/screens/chat/Conversation.jsx
# 预期：0 行

# R-286 hex 清理
grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/screens/chat/ThinkingCard.jsx
# 预期：0 命中

# R-287 transition 精确
grep "cubic-bezier(0.4, 0, 0.2, 1)" frontend/src/screens/chat/ThinkingCard.jsx
# 预期：≥ 1 命中
```

### Stage 2 R-287 transition 实施

```jsx
<span style={{
  fontFamily: T.mono, fontSize: 11, letterSpacing: '0.04em',
  transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
}}>{doneCount}/3 STEPS</span>
```

### Stage 2 R-288 main margin-right 验证

**起手第一步** — commit 1 之前先 grep Conversation.jsx：

```bash
grep -nE "marginRight|margin-right|272|320|width.*272" frontend/src/screens/chat/Conversation.jsx
```

如硬编 272 字面 → **本 PATCH 不动 Conversation**（R-275），改 Panel 内部自补 48px 视觉宽度。

### Stage 3 R-290 SSE 兜底实施

```jsx
const output = getDoneOutput(key)?.refined_question || null;  // optional chaining
const sqlSteps = events?.filter(e => e?.type === 'sql_step') || [];  // 防 events undefined
```

三场景手测：
1. 刚开 chat 屏（无 SSE event） — visible=false 路径 OR 空 events Empty State
2. SSE 中断（dev tools 禁用网络） — 不应崩溃
3. 后端返字段缺失 mock — output.refined_question 为 undefined 时 ?.

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/chat/ThinkingCard.jsx  [EDIT 110 → ≤ 160 行]
                                            Step 1: letter chip helper K/N/O [R-277/R-289]
                                            Step 2: emoji → chip + name 字面 [R-277/R-278/R-227.5.1]
                                            Step 3: Panel 272→320 + R-288 验证 [R-276/R-288]
                                            Step 4: 卡片 bg T.content + radius 10 [R-279/R-283]
                                            Step 5: Header step count + transition [R-280/R-287]
                                            Step 6: done svg checkmark [R-281]
                                            Step 7: substeps tag + slice 120 + ellipsis [R-282/D8]
                                            Step 8: hex 清理 #09AB3B → T.success [R-286]
                                            Step 9: SSE 兜底 + optional chaining [R-290]
                                            守护: 2 exports 签名 byte-equal [R-266]
                                                  + AGENTS 3 keys + 业务逻辑 byte-equal [R-267~269]
                                                  + inline styles only [R-284]
                                                  + 无 version 字面（仅 Login）

scripts/check_file_sizes.py                 [EDIT LIMIT 250→160] [R-285]
knot/main.py                                [EDIT 0.5.11 → 0.5.12]  [R-273]
tests/test_rename_smoke.py                  [EDIT R-72 字面 0.5.11→0.5.12]  [R-273]
frontend/src/screens/Login.jsx              [EDIT 页脚 0.5.11→0.5.12]  [R-273]

frontend/src/screens/chat/Conversation.jsx  [unchanged 0 行 diff]  [R-275 + R-291]
frontend/src/screens/chat/ChatEmpty.jsx     [unchanged]              [R-275]
frontend/src/screens/chat/Composer.jsx      [unchanged]              [R-275]
frontend/src/screens/chat/ResultBlock.jsx   [unchanged]              [R-275]
frontend/src/screens/chat/intent_helpers.js [unchanged]              [R-275]
frontend/src/screens/chat/sse_handler.js    [unchanged]              [R-275]

frontend/src/screens/ (其余 17 屏)           [unchanged]              [R-275]
frontend/src/App.jsx                        [unchanged]              [R-272]
frontend/src/{api.js,index.css,main.jsx,utils.jsx,Shared.jsx,Shell.jsx}  [unchanged]  [R-272]
frontend/src/decor/NarrativeMotif.jsx       [unchanged]              [R-272]
frontend/src/App.css                        [unchanged 0 行 diff]    [R-284]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                    [unchanged]
backend (knot/) 任何文件除 main.py          [unchanged]              [R-271]

CHANGELOG.md                                [EDIT [Unreleased] v0.5.12]
                                            R-266~R-291 偿还表 + R-227.5.1 装饰豁免 + R-286 hex 清理
CLAUDE.md                                   [EDIT]
                                            - v3 协议施行历史回顾表 +1 行 v0.5.12
                                            - v0.5.x 路线图 v0.5.12 ⏳ → ✅
docs/plans/v0.5.12-thinking.md              [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（不允许减少）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（30 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.12**

### Stage 2/3 强化验证

- [ ] **R-227.5.1**：letter chip K/N/O 单字母装饰豁免认定
- [ ] **R-286 hex 0 命中**：`grep -nE "#[0-9a-fA-F]{3,6}" ThinkingCard.jsx` = 0
- [ ] **R-287 transition**：grep `cubic-bezier(0.4, 0, 0.2, 1)` ≥ 1
- [ ] **R-288 响应式契约**：1280/1440/1920 三档窗宽实测
- [ ] **R-289 letter chip 居中**：跨浏览器 K/N/O 视觉重心一致
- [ ] **R-290 SSE 鲁棒性**：三异常场景无崩溃
- [ ] **R-291 调用点字节码**：`git diff origin/main HEAD -- Conversation.jsx` = 0 行

### 契约守护

- [ ] **R-266**：2 exports 函数签名行 byte-equal
- [ ] **R-267**：AGENTS / AGENT_LABELS 3 keys byte-equal
- [ ] **R-268**：getStatus / getDoneOutput / sqlSteps 逻辑 byte-equal
- [ ] **R-269**：output 字段访问 byte-equal

### 范围守护

- [ ] **R-272**：App/api/index.css/main/utils/Shared/Shell/NarrativeMotif `git diff` = 0
- [ ] **R-274**：KnotLogo grep 仍仅 3 文件
- [ ] **R-275**：chat/ 其他 6 子模块 `git diff` = 0
- [ ] **R-284**：`git diff frontend/src/App.css` = 0
- [ ] 17 屏 + 其他 11 子模块 byte-equal

### 端到端

- [ ] 真实 SSE 提问 → 3 agent (K/N/O) 顺序 pending→thinking(TypingDots)→done(checkmark) 平滑
- [ ] Step count "0/3" → "3/3" R-287 transition 平滑
- [ ] sqlSteps S1/S2/S3 tag chip + 120 字符 + ellipsis
- [ ] v0.5.0~v0.5.11 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.12): ThinkingCard.jsx 视觉重构 — K/N/O letter chip + 320 panel + svg checkmark + step count transition + hex cleanup + SSE 兜底 (R-266~R-291)` | npm build + 端到端 3 agent + R-275/R-291 + R-286 hex 0 + R-288 三档窗宽 + R-290 三异常 |
| 2 | `chore(v0.5.12): version bump 0.5.11→0.5.12 三处同步 + ThinkingCard LIMIT 250→160 (R-273/285)` | 全闸门 + version=0.5.12 + 30 条 LIMITS |
| 3 | `docs(v0.5.12): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED 归档 + R-227.5.1 装饰豁免存档` | 全闸门 |

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 13 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 27（v0.5.12 区段 R-266~R-291 + R-227.5.1）
- ✅ **R-227.5.1 单字母装饰豁免** — 字面分流体系扩展
- ✅ **R-286 hex 全面禁止** — v0.5.9 R-211 残留色清理模式扩展
- ✅ **R-288 响应式契约** — Panel 宽度变更与 R-275/R-291 0 改红线兼容
- ✅ R-251 视觉自动跟随设计模式 sustained
- ✅ 范围最小化 — 17 屏 + 11 子模块 + 8 核心非屏 + App.css 全部 byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ LOCKED 手册落地 → 当前文件；分支 `feat/v0.5.12-thinking`
2. ✅ R-288 起手前置探查 — commit 1 起手第一步 grep Conversation.jsx 标定布局假设
3. ✅ R-286 hex 清理 — commit 1 后强制 grep `#[0-9a-fA-F]{3,6}` = 0；特清 `#09AB3B`
4. ✅ R-287 transition 严守 — `cubic-bezier(0.4, 0, 0.2, 1)` 精确字面
5. ✅ R-289 跨浏览器 — Chrome 主测；Safari/Firefox 实测或 dev tools 模拟
6. ✅ R-290 SSE 兜底 — 三场景手测
7. ✅ R-291 调用点字节码 — Conversation.jsx `git diff` = 0
8. ✅ R-227.5.1 装饰豁免边界 — K/N/O OK；完整 "KNOT" 仍守 R-126
9. ✅ Q2 接受现状 — tagline 4 字母 vs 屏 3 字母不强行对齐
10. ✅ D1 严守 3 agent — 不加 Trace；后端业务改造留 v0.6+
11. ✅ Step 1→9 顺序锁死
12. ✅ 端到端真实 SSE 手测 — commit 1 收尾强制实际提问
13. ✅ 范围抗诱惑 — 不顺手 ResultBlock / table card / 收藏 / KnotMark 行内

---

🛡️ **执行者收到 GO 信号，待资深架构师最终 ack 后启动 Stage 4。**
