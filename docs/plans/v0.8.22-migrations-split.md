# v0.8.22 — base.py → migrations.py 拆分 · 执行手册（Stage 1 草案 · 已过执行者对抗自核 · v0.9.0 物理硬前置）

> **性质**：纯重构 chore PATCH，0 行为改动。兑现 R-LP-v3-EX-3 承诺登记（check_file_sizes.py:32 明示「v0.9 前拆 migrations.py」）。
> **物理动因**：base.py 373 行 / ACK 376，headroom 仅 3 行；v0.9.0 get_conn 双层解析必动 base.py → 不拆必爆 size-gate。资深 2026-07-17 拍板「独立 v0.8.22 先行」。
> **协议**：建议**快通道**（纯重构 / 全被既有测试钉住 — 待资深 explicit 授权留痕；否则走简化三阶段[守护者单终审]）。注：协议核指出无「纯后端启动路径重构」快通道先例，且被搬块含 must_change_password schema 基座（deps.py:135 403 硬门地基）——「0 安全承重」宣称已撤回，验收补对抗复核项对齐快通道既有配方。
> **基线**：main @ v0.8.21。**版本目标**：0.8.22。

## § 1 范围

- **新 `knot/repositories/migrations.py`**（搬 ~302 行 = 内联块 ~200 + 具名函数 ~102）：
  - **三段式入口（对抗自核订正——原「pre/post 二分」与 init_db 实况不符：seed admin L213-238 与 uploads 迁移调用 L244 夹在两段迁移块之间）**：
    1. `run_pre_schema_migrations(conn)` ← base.py L29-36（bi_permissions 重建；executescript 在 :37）
    2. `run_post_schema_migrations(conn)` ← L39-211（**17 个独立注释头块**，逐块保序）
    3. `run_startup_cleanup(conn)` ← L246-264（OR-only 孤儿清理；在 seed + uploads 迁移调用**之后**调）
  - 内联块计数口径：以段落注释头为界共 **~19 块**（L29-36 ×1 + L39-211 ×17 + L246-264 ×1）。
  - 2 个具名函数原样搬：`_migrate_uploads_to_isolated_db_once`（L270-332 活跃）+ `_migrate_uploads_db_once`（L335-373 RETIRED，退役 banner 随搬，**严禁重新接线**）。
- **base.py 保留**：get_conn + _SCHEMA_SQL/executescript + init_db 编排（序列：pre → executescript → post → seed admin → uploads 迁移调用 → cleanup，**与现执行序逐块等位**）+ seed default admin（L213-238，R-LP-v3-EX-3-1 承重代码留 base 减少搬动面 — 裁量点，Stage 3 可调）。目标 ≤300 行，**删除 check_file_sizes ACK 条目**。
- **base.py:53 自标注释处理**（协议核）：「default admin/admin123 是 1.0 公测前必清的安全债」自标在被搬的 L39-211 块内（v0.6.0.20 must_change_password 块），是 CLAUDE.md R-LP-v3-EX-3-1 红线的 grounded 引文锚点——搬迁时**在 base.py seed 块旁保留/复制该自标**，PR 注明红线引文锚点变更。
- **check_file_sizes.py**：删 base.py ACK；migrations.py 若 >300 加 ACK 条目并注明由来（争取 <300 免 ACK）。

## § 2 红线

- **0 行为改**：迁移执行顺序逐块 byte-equal 保序（三段式 + init_db 编排序列即现执行序）；块内逻辑不改一行。
- **SQLITE_DB_PATH 模块副本问题（对抗自核 REFUTED 后定案 — 方案 a）**：被搬函数体内 4 处直读 `SQLITE_DB_PATH`（base.py:277/294/300/340）读的是 base import 期模块副本，而全测试栈靠 `monkeypatch.setattr(base_mod, "SQLITE_DB_PATH", …)` 打点——若 migrations.py 顶层自行 import 该常量，monkeypatch 失灵，`test_upload_isolation` 会 ATTACH **真实** knot/data/uploads.db 写测试表。**定案**：4 处读取点改为**调用期延迟读** `from knot.repositories import base as _b; _b.SQLITE_DB_PATH`（base 顶层 import migrations、migrations 调用期反读——无环）；红线相应定为「**禁模块级 import base**，调用期反读允许」+「除此 4 处路径符号读取外块内逻辑 0 改」。monkeypatch 语义零改、测试面最小。
- 找全解包点（v0.7 教训 1，grep 清单对抗自核补全）：
  - `grep -rn "from knot.repositories.base import\|repositories.base\."`
  - **`grep -rn "from knot.repositories import base"`**（别名形态，实测 12 文件命中：3 个 conftest + test_migration_observability + test_upload_isolation + test_or_only_migration + test_totp_2fa + test_knot_catalog + test_closeout_19b + test_saved_report_service + 2 个 scripts）
  - **`grep -rnE "\bbase(_mod)?\.(SQLITE_DB_PATH|_migrate_|init_db|get_conn|logger)"`**
  - **全套件兜底非 targeted**。
- import 方向：migrations.py 同层（repositories），依赖 stdlib + core(logging) + 调用期反读 base；base import migrations —— 无环，import-linter 9 KEPT。
- 不顺手：不加 busy_timeout（F3 defer 有独立裁定）、不动 seed 逻辑、不清 RETIRED 函数、不碰 schema.sql。

## § 3 验收

- full pytest 绿；**测试改动仅限 `test_migration_observability`**：其 mock 目标是**模块属性 3 类**（setattr SQLITE_DB_PATH :30/:50 + setattr logger :35/:58 + 直调 `base_mod._migrate_uploads_db_once` :38/:61）——随搬迁改指 migrations 模块，PR 注明清单（对抗自核订正：非单一 import 路径改动）。其余测试 0 改（含 test_upload_isolation，经 §2 方案 a 保 monkeypatch 语义）。
- base.py ≤300 无 ACK；三闸门绿（ruff / lint-imports 9 KEPT / check_file_sizes）。
- **对抗复核项（快通道配方必备件，协议核补）**：① 迁移块保序 byte-equal 证据（搬迁前后按块 diff，块内 0 改、序 0 变）② must_change_password 块（原 L112 附近 users TOTP/改密列块）搬迁前后执行序不变证据 ③ mock 目标 3 类核对清单。由独立 subagent 对抗复核后附 PR。
- 4 版本源点 0.8.22 + CHANGELOG + static rebuild + clean worktree 复现构建。

## § 4 commit 序列 + 协议

1. 拆分搬家 + import 接线 + test_migration_observability mock 目标改指 + check_file_sizes 更新（单 commit — 原子性：半搬状态无意义）。
2. 版本收尾 4 源点 + CHANGELOG。

**协议执行**：若资深授权快通道 → 授权须 explicit 留痕（announce 或 PR 描述引用），执行 = 建 + 自验 + §3 对抗复核 + CI；否则走简化三阶段（Stage 1 本文件 → 守护者[v0.8] Stage 3 单终审 → 执行）。
