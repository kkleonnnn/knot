# v0.6 → v0.7 整体审核 — 多方独立评审意见归档

> **仪式**：Task #23 MINOR 滚动整体审核（v0.6→v0.7）
> **评审对象**：`v0.6-to-v0.7-overall-review-2026-06-20.md`（执行者 Stage 1 草案，固定 git hash 评审）
> **协议**：v3 仪式多方独立评审（v0.5 守护者 + v0.4/v0.3 远古守护者各自独立 → 资深仲裁 → LOCKED）
> **本档**：归档各方独立意见原文，供资深仲裁 + 历史存证（仿 `v0.5.0-overall-review.md` 多方归档先例）

---

## 意见 1 — v0.5 守护者（独立 grounded 评审）

> **方法**：9-agent workflow 对 main 0.6.5.7 实测，不信执行者扫描。
> **总立场：`MAJOR-REVISE`** —— 4 产物测量全 verified-true、方向全对（admin.py 拆分作 v0.7 硬前置成立）；revise 针对 (A) v0.7 骨架丢 5/7 守护不变量 (B) 三巨头拆分复杂度严重低估 (C) 剃刀剃了储备资产 (D) 3 产物维度缺失，**非推翻方向**。
> **角色滚动**：本意见 LOCKED 后 v0.5 守护者转远古（v0.7 dormant）；最重交接物 = 「5/7 不变量带入清单」。

### ⭐ 最重发现：v0.7 骨架（产物 4）丢 5/7 守护不变量

| 守护不变量 | 带入? | gap |
|---|---|---|
| gate 鉴权（require_admin + get_current_user）| ❌ | v0.7 新 metric/dimension repo endpoint 必经 gate，骨架未声明 |
| R-2FA（强制 enroll + token_version 吊销）| ❌ | §2.1 仅「2FA surface 保留」一行，未作 v0.7 不变量 |
| 视觉铁律（brandSoft 8% + borderLeft 25% + R-PA-PB-V1）| ❌ | §4 未点名；v0.7.3 审计 UI 必有新屏 → 9 屏 byte-equal 须延续 |
| doc-invariant CI（5 源点 + count==1）| ❌ | §3.3 提 tag CI 但未纳入既有 test_doc_invariants 框架 |
| R-192 AppShell 13 props 宪法级 | ❌ | 草案 0 提；v0.7 新屏接 AppShell 须 0 改 13 props |
| OOS-1 单租户死线 | ⚠️ partial | Q4 框为「是否重构」有险 → 须钉死：metric 注册表归 catalog_id，严禁顺手引 tenant_id |
| R-LP-v3 治理 | ✅ | EX-1/2/2.1/3 + GOV-1 + override-log 均已立约（累计 3 次距 EX-2.1 双锁 1 次之差）|

→ **硬要求：产物 4 必须新增「v0.7 不变量带入清单」段**，逐条声明上 5 项的载体/守护点。

### 4 产物逐条

**① 代码结构 = REVISE**（测量 verified，落地缺护栏）
- ✅ admin.py 907/30 无 LIMITS / http_planner 508 + catalog 460 无闸门 / 前端拆分债真还 / check_file_sizes explicit-dict 非 glob — 全 verified。
- 🔴 admin.py 拆分会静默回归 PR#157 flaky：conftest L60-63 autouse 直绑 `_admin_mod._DS_STATS_CACHE` 且 `except (ImportError,AttributeError): pass` 吞错零报错 → datasources-stats 一旦拆出 admin.py，清污变 NO-OP，v0.6.5.3 flaky 无声复发。`totp_service._TOKEN_VERSION_CACHE` 同型。
- 🔴 agents/catalog.py 是真高危拆分区（§1.2 仅标「P1 无 LIMITS」严重低估）：5 可变全局 + import-time reload() + ContextVar(R-PB-A1-15) + 9 importer 直读 → from-import 别名陷阱 → reload 重绑子模块、外部读旧空绑定 → 静默生产 catalog 永空 / schema 注入失效 bug，pytest 未必抓。

**② 奥卡姆剃刀 = REVISE**（剃了储备资产 + 判定标准不一致）
- 🔴 anthropic_native「以为死实则活」：命中 6 测试文件（非草案 4）+ factory dispatch + `_KNOWN_PROVIDERS` + async prompt-cache 分支 `enable_prompt_cache=(provider=="anthropic")` + base.py 字段契约 + R-31 协议守护。剃 = test-改造 PATCH 非 cleanup 顺手剃。
- 🔴 判定标准不一致：multi-catalog（冻结=留载体）vs anthropic_native（剃=删）同类「暂不用留载体」却一删一留。**铁律：剃刀=真 0 消费者（含测试/import/协议）；冻结=有载体+复活路径+耦合。anthropic_native 应归冻结资产（OR-only 回退储备）非剃刀。**
- http_planner：pick_http_route 是 query.py 每查询热执行（非死代码）；redact_pii/truncate_rows 是 domain-agnostic infra（可复用），仅 `_COIN_TO_MARKET` + `_USER_ID_RE` 是 futures 耦合 → 下沉须精确到 futures regex，一刀切误伤 infra + 破 23 处 test + 4 monkeypatch；且这批方言零 CI 守护。

**③ 重构/协议 = ENDORSE**（admin.py 7 文件方向对 + tag 纪律 + type hint），唯 **catalog.py 必须单独高守护 PATCH，绝不与 admin/http_planner 同批拆**（§3.1 把三巨头当单一工作项 = 复杂度严重低估）。

**④ v0.7 骨架 = REVISE**：见上 5/7 不变量清单 + 钉死 OOS-1 no-tenant_id + tag CI 纳入既有 doc-invariant 框架。

### 3 产物维度缺失（完整性批判）
- 前端体量：§1 对前端只记功未扫红区；v0.7 metric 编辑屏必扩前端 → 须第 5 维度。
- 测试债：测试增幅最大（11,192 LOC/739 函数）却 0 评估；conftest module-path 硬绑定本身就是结构债。
- 依赖 pin：pyotp/qrcode/cachetools + FastAPI 0.137 未 pin（route 90/91 漂移正是此因）→ 1.0 前 lockfile/上游漂移防护是奥卡姆 + 结构双维度。

### §5 Q1-Q5 守护者立场
- **Q1**：admin.py 拆分 = v0.6.x 收官独立 PATCH（非 v0.7.0 起手）；须先解耦 conftest 缓存绑定（前置）。赞成 `knot/api/*.py` 默认行数上限 CI，但纳入既有 check_file_sizes + 补全路径。
- **Q2**：anthropic_native 不剃，归冻结资产（OR-only 回退储备，6 测试 + R-31）。非流式 /query + sync 链可剃但须先 grep 确认无 CLI/启动/测试消费者 + test-改造，v1.0 前独立 cleanup PATCH。
- **Q3**：http_planner v0.7 前精确处理 — futures regex 下沉 catalog 配置 + domain-agnostic infra 保留；领域逻辑窄是对的，但须补 CI 守护或登记 OOS-debt。
- **Q4**：认同冻结留作 v0.7 语义层载体；反对 Q4 框法「是否重构 tenant 归属」— OOS-1 单租户死线已坐实，v0.7 metric 归 catalog_id 严禁引 tenant_id（真 multi-tenant 推 v1.x+）。
- **Q5**：赞成立时效条款（#16 自己 stale 是实证），定位为流程增量非不变量；挂靠既有 doc-invariant CI。

---

## 执行者 R-137 反向核验（不盲信守护者 verdict）

> **方法**：6-agent 对抗式 workflow 独立 ground-verify 守护者 6 大承重主张（confirm/refute）。
> **结论**：守护者**实质方向 6/6 全 confirmed**，执行者**接受 MAJOR-REVISE**；但 R-137 双向实证抓到守护者**具体数字 3 处不准** + 执行者**自身草案 framing 2 处错**。真相以实测为准。

| 主张 | verdict | 数字 / framing 校正（实测） |
|---|---|---|
| conftest 致 admin.py 拆分 flaky 复发 | confirmed | 补强：`test_isolation_hardening.py` + `test_datasources_stats.py` 也硬绑 `_DS_STATS_CACHE` → 拆分会**显式 FAIL**（非全静默，有安全网）；唯 conftest L63 `except: pass` 静默；**安全拆法 = admin.py 保留 re-export（R-100 先例）或同步改 3 文件 import 路径** |
| catalog.py 高危拆分严重低估 | confirmed **(更严重)** | importer **实测 11**（守护者说 9，**守护者自己低估**）；4 可变全局 + reload() + ContextVar + 自动首次 reload；from-import 别名陷阱当前是**靠 docstring 纪律规避未触发**，非语言机制兜底 → 拆分/改 importer 读法即破「模块属性 live 读」不变量 → 静默 catalog 永空 |
| anthropic_native「以为死实则活」 | partial | 守护者「6 测试文件」**实为 3 文件 / 11 引用**；**执行者草案「0 可达死代码」是范畴错误** —— 静态可达（factory dispatch + `enable_prompt_cache=(provider==anthropic)` 4 生产调用点 + R-31 守护全活），运行时冷（OR-only 配置下不被选中）。**结论：应冻结（OR 不享有的 anthropic cache_control 能力分支储备），非剃；若剃 = 能力裁撤 + test 改造 PATCH，非 cleanup 顺手** |
| http_planner 伪通用应简化 | partial | 守护者「23 refs / 4 monkeypatch 分散」**实为 49 ref 行 / 7 文件；4 monkeypatch 集中 1 文件**；**执行者草案「futures 专用伪通用」过简** —— `pick_http_route` 是 query.py:248 每查询热路径；`redact_pii/truncate_rows/humanize/apply_field_mapping/resolve_spec` 是 domain-agnostic infra（0 外部非测试调用）。**精确下沉边界 = 仅 `_COIN_TO_MARKET`+`_USER_ID_RE`+`_MARKET_RE`+`_SHORT_MARKET_RE`+`_SIDE_*`+`_EXCLUSION_RE`+extract_params futures 层；保 infra；一刀切误伤通用脱敏/截断/映射** |
| 完整性 3 维缺失 | partial | 测试债**证据最硬**（11,192 LOC 单类最大资产 0 评估 + conftest module-path 硬绑 + import-timing `os.environ.setdefault` 陷阱 = 真结构债）；前端红区机制对精度待补；依赖 pin「失控致 route 90/91 漂移」证据偏弱（route 漂移另有 FastAPI 0.137 _IncludedRouter 懒包装根因） |
| **v0.7 骨架丢 5/7 不变量** | **confirmed** | 严格只看 §4 正文骨架则 **6/7 丢**（仅 R-LP-v3 在 §4 L105 点名；OOS-1 在 §2.3/§5 承接算 credit 才 5/7）→ 守护者「5/7」成立甚至略乐观。**附带校正**：doc-invariant 现为 **4~5 源点**（task #44 v0.6.4.11 改 version.js 单一真相源），非守护者所述「5 源点」。7 不变量在 live CLAUDE.md 全部 verified 真实存在。 |

**执行者立场**：全盘接受守护者 MAJOR-REVISE 的实质修订（含「5/7 不变量带入清单」硬补 + catalog.py 单独高守护 PATCH + anthropic_native 改判冻结 + http_planner 精确下沉 + conftest 解耦前置 + 3 维补全）。整合留至**远古 v0.4/v0.3 意见齐 + 资深仲裁后**一次性出 LOCKED v2（守「固定 artifact 避免边评边改漂移」纪律，草案 v1 此前字节不动）。

---

## 意见 2 — v0.4 远古守护者（待独立评审）

_（资深召集后补入）_

---

## 意见 3 — v0.3 远古守护者（待独立评审）

_（资深召集后补入）_

---

## 资深仲裁（待 LOCKED）

_（多方意见齐备后资深拍板，逐产物 endorse/revise/否决 → LOCKED）_
