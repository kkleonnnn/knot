# v0.6.4.5 — UI v2 thinking（ThinkingCard / AgentThinkingPanel）屏复刻（逐屏复刻第 5 屏）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + VRP grep-guard + **修正版本同步模型（4 无条件 ★CI 含 Login footer，默认每 PATCH bump）**
> **基座**：worktree `v0645-thinking` @ branch `v0.6.4.5-thinking`（off main `45d5874` = v0.6.4.4）
> **源稿**：`knot_demo_ui/v0.6/artboards/thinking.jsx`（ThinkingStep L299-349）
> **目标**：`frontend/src/screens/chat/ThinkingCard.jsx`（210 行，LIMIT 240）

## §0 范围（窄 — 卡片级视觉 deltas，0 业务 / 0 契约改动）

仅 `ThinkingCard.jsx`。**诚实定级：中等偏轻** — AgentThinkingPanel 卡片 v0.5.12/v0.5.39/v0.6.1.4 已多次演进（K/N/O/T letter chip + step count + done check + tag chip + HTTP 分支），核心结构早接近 v2；真实 deltas = 卡片 active ring + title 字号 + pending icon 3 处。

## §1 视觉变化（artboard 方向 → 本地 VRP）

### ① active 卡片 boxShadow ring（demo L319）
- 卡片 `isThinking` 时加 `boxShadow: '0 0 0 3px color-mix(in oklch, ${T.accent} 15%, transparent)'`（与 Composer focus ring 同 pattern；R-286 color-mix oklch / R-313 boxShadow 豁免）；非 thinking 时 `none`
- active border 保本地 `color-mix(in oklch, ${T.accent} 38%, transparent)`（v0.5.12 R-286 established）— ring + border 共同构成"思考中"辉光

### ② title 字号/字重（demo L331）
- agent label `fontSize 12.5→13` + `fontWeight 500→600`

### ③ pending icon 收敛（demo L312）
- `<span>○</span>` 字符 → `<span>` circle（`width:13 height:13 borderRadius:'50%' border:'1.5px solid ${T.muted}' opacity:0.5 flexShrink:0`）
- 卡片 pending `opacity 0.45→0.55`（demo fidelity）

### 保 byte-equal（结构/业务/Foundation）
- **AgentThinkingPanel borderLeft 右 rail 结构保留**（OOS：改 floating inset 需动 Chat.jsx 布局；main 已 floating panel，rail 在其圆角内裁剪 = 正确）
- **LetterChip / DoneCheck / TypingDots from Shared 0 改**（Foundation；demo inline 版仅 artboard mock）
- ThinkingCard（小 loading 卡 L5-17）保不变（demo 无 1:1 对应，StreamingAnswer 是 skeleton 不同概念）
- 业务逻辑 byte-equal：`_deriveTrace` / `_shouldHideClarifierApproach` / `getStatus` / `getDoneOutput` / `AGENTS` 4 keys (K/N/O/T) / `sqlSteps` / `isAdmin` gating / HTTP 路径分支 / Panel width 320 / header step count `{doneCount}/{AGENTS.length} STEPS` / 4 body 条件渲染（clarifier/sql_planner/presenter/trace）
- R-227.5.1 letter chip 装饰豁免 sustained · R-286 hex 0（全 color-mix）· R-313 rgba/boxShadow 豁免

## §2 红线

| # | 红线 |
|---|---|
| R-UI2-T-1 | ThinkingCard + AgentThinkingPanel export + props 签名 byte-equal（`ThinkingCard({T,agentEvents})` + `AgentThinkingPanel({T,events,visible,user})`）→ Chat.jsx + Conversation.jsx 0 diff |
| R-UI2-T-2 | 业务逻辑 byte-equal（_deriveTrace/_shouldHideClarifierApproach/getStatus/getDoneOutput/AGENTS 4 keys/sqlSteps/isAdmin/HTTP 分支/4 body 渲染）|
| R-UI2-T-3 | Foundation 0 改：LetterChip/DoneCheck/TypingDots from Shared 不碰（R-PB-SH 守护）|
| R-UI2-T-4 | AgentThinkingPanel borderLeft 右 rail 结构保留（不改 floating inset；Chat.jsx 0 diff）|
| R-UI2-T-5 | hex 0（grep `#[0-9a-fA-F]{3,6}` 排 rgba = 0；active ring 用 color-mix oklch 非 hex）|
| R-UI2-T-6 | VRP grep 0 写死 viewport（Panel width 320 固定 rail 宽 = 合法元素值，非 viewport%）|
| R-UI2-T-7 | 其他屏/Foundation/App 0 改；ThinkingCard ≤ 240 LIMIT（210 + ring/pending ≈ +5 → ~215）|
| R-UI2-T-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-T-9 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.5；Shell L43 不动（条件式，未改 Shell，stays v0.6.4.2）|

## §3 验收
1. 思考 rail 卡片：active 蓝青 ring 辉光 + title 13/600 + pending circle（非 ○）
2. `git diff origin/main -- frontend/src/screens/Chat.jsx frontend/src/screens/chat/Conversation.jsx frontend/src/Shared.jsx` = 0
3. hex 0 + VRP grep 0 + npm build exit 0 + check_file_sizes OK
4. **4 处版本 → 0.6.4.5**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（提问触发 → 4 agent K/N/O/T 状态流转 + active ring + done check + HTTP/SQL 双路径 trace + 非 admin 隐藏 sql_step）

## §4 commit 序列
- **commit 1**：ThinkingCard.jsx 卡片视觉（active ring + title + pending icon）+ 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §5 协议合规
轻量 v3：执行者 Stage 1（本草案，adversarial delta 评估 = 卡片 3 deltas，rail 结构/Foundation/业务 byte-equal）→ 守护者 Stage 3（VRP + 契约 byte-equal + Foundation 0 改 + 4 处版本显式核）→ 逐 commit 直读 → 资深 push/merge + :8000 live。
