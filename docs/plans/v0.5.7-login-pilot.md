# v0.5.7 — Claude Design UI 重构（C5+）— Login 屏首屏复刻 pilot — LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环（**第 8 次施行**）
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

8/8 决策（D1-D8）+ 4/4 风险项（Q1-Q4）**与执行者 Stage 1 提议完全一致**，0 修订。
新增 5 条红线（R-182~R-186），Stage 2 (2) + Stage 3 (3)。
**Stage 2 关键加码**：Q3 加 checkbox `title` 提示防误导 + Q4 强制 `T.dark` boolean（严禁 theme 字串）。
**Stage 3 关键加码**：R-186 严格 1 屏限制（Shell.jsx 严守 0 改）+ R-185 DOM 哨兵测试 + R-184 焦点蓝青手测。

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q4 — 12/12 锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| **D1** NarrativeMotif 位置 | ✅ A 独立 `decor/NarrativeMotif.jsx` | Stage 1 |
| **D2** KnotLogo 三件套位置 | ✅ A 进 Shared.jsx | Stage 1 |
| **D3** 7 天 remember-me | ✅ A 仅 UI + localStorage flag | Stage 1 |
| **D4** 忘记密码链接 | ✅ A 占位 `<a>` 无 handler | Stage 1 |
| **D5** 删 3-features list | ✅ A 删 | Stage 1 |
| **D6** error banner 颜色 | ✅ A oklch 红 | Stage 1 |
| **D7** 主按钮文案 | ✅ A demo 文案 "进入 KNOT" | Stage 1 |
| **D8** 登录页版本号 | ✅ A 硬编码 `v0.5.7` + R-181 守护 | 资深 ack |
| **Q1** Motif 对比度 | ✅ A `color-mix(in oklch, ...)` 内部模拟 | Stage 2 — 严禁破 R-158 25 字段契约 |
| **Q2** SVG viewBox | ✅ A KnotMark 100×100（Logo 与 Icon 语义不同） | Stage 2 |
| **Q3** 7 天 UX 不误导 | ✅ A + checkbox `title` 提示 | Stage 2 — `title="目前仅作为偏好记录，Token 有效期受服务器策略控制"` |
| **Q4** T.dark 适配 | ✅ A **强制** `T.dark` boolean（严禁 theme 字串传子组件） | Stage 2 强制 |

---

## 2. 红线最终清单（17 条 R-170~R-186）

### Stage 1 草案原 12 条（保留）

| ID | 摘要 |
|---|---|
| **R-170** | LoginScreen export + props 签名 0 改动（App.jsx 调用 byte-equal） |
| **R-171** | api.login + cb_token localStorage 0 改动（backend 0 改动） |
| **R-172** | error 文案 "用户名或密码错误" byte-equal |
| **R-173** | NarrativeMotif ≤ 120 行 pure SVG func |
| **R-174** | Shared.jsx 总 export ≤ 12（9 → 12） |
| **R-175** | Login.jsx ≤ 200 行 |
| **R-176** | check_file_sizes.py LIMITS +2 → 29 条 |
| **R-177** | backend routes=72 / tests=432 / 7 contracts 全程 KEPT |
| **R-178** | 17 屏 byte-equal（Chat / Admin / Database / Knowledge / Conv / Saved / Catalog / SqlLab / Settings + 12 子模块） |
| **R-179** | App.jsx / api.js / index.css / main.jsx / utils.jsx / Shell.jsx byte-equal |
| **R-180** | version 0.5.6 → 0.5.7 三处同步（main.py + test_rename_smoke.py + Login 页脚） |
| **R-181** | Login 页脚 `v0.5.7` 字面 + 同步守护测试 |

### Stage 2 新增 2 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-182** | NarrativeMotif 必须 `React.memo` 包裹 — 防 Login 屏状态变化（错误 / 主题切换）触发 SVG 巨量 path 重绘 | `decor/NarrativeMotif.jsx` 末尾用 `export default React.memo(NarrativeMotif)`；props 仅 `{ T }`，T 引用稳定（Login 内 `useMemo`）则 memo 命中 |
| **R-183** | KnotLogo 响应式 size props — 严禁 Shared.jsx 内写死像素宽度；必须接 `size` 或 `width`/`height` props | `KnotMark({ T, size = 32 })` / `KnotWordmark({ T, size = 24 })` / `KnotLogo({ T, size = 32 })`；调用处可覆盖；严禁 `width: '32px'` 字面常量 |

### Stage 3 新增 3 条

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-184** | Input Focus T.accent 蓝青 — Tab 键切换焦点视觉反馈必须用 v0.5.6 蓝青 brand；严禁旧蓝/红焦点环 | utils.jsx Input 组件 `:focus` 状态 `outlineColor: T.accent` / `borderColor: T.accent`（v0.5.6 应已如此 — 本 PATCH 复用）；commit 3 后**强制** Tab 键手测 |
| **R-185** | DOM 存在性哨兵测试 — 加 `tests/test_login_smoke.py`（或并入 `test_login_version_sync.py`）断言 KnotLogo 生成内容存在；确保资产重构未"逻辑蒸发" | 用 grep 而非 DOM 渲染（前端无 jest）：`assert 'KnotMark' in login_src or 'KnotLogo' in login_src`；同时校验 Login.jsx 含 `<KnotLogo` 或 `<KnotMark` JSX 调用字面 |
| **R-186** | 严格 1 屏限制 — 严禁在 Shell.jsx 提前引入 KnotLogo；Shell 更新属 v0.5.8 范畴 | `git diff frontend/src/Shell.jsx` 必须 0 行；commit 3 后强制校验；KnotLogo 三件套虽进 Shared.jsx，但本 PATCH **仅 Login.jsx 引用** |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 序列严守）

> 先注入 Shared.jsx Logo 资产 → 再创建 NarrativeMotif → 最后重构 Login.jsx

**落地**：commit 1 注 Logo 后**先验** `npm run build` 通过 + Shared.jsx export 数 9→12 + Login.jsx **暂未引用**（不破坏现有 Login 渲染），再开 commit 2/3。

### Stage 3 §3 验收关键

```bash
pytest tests/test_login_version_sync.py -v   # R-181 + R-185（合并）
cd frontend && npm run build                  # 0 警告 0 error
git diff frontend/src/Shell.jsx | wc -l       # = 0（R-186）
```

### Stage 2 Q1 color-mix 实施

```jsx
// NarrativeMotif.jsx 内部
const tint = `color-mix(in oklch, ${T.accent} 15%, transparent)`;
// 用于 radial-gradient(ellipse at 30% 20%, var(tint), transparent)
```

**严禁**扩 buildTheme 加新字段（违 R-158 25 字段契约）。

### Stage 2 Q4 T.dark 强制

```jsx
const fg = T.dark ? '#f4f6f8' : '#0d1014';   // ✅
// 严禁：const fg = theme === 'light' ? '#0d1014' : '#f4f6f8';   // ❌
```

### Stage 2 R-182 React.memo 实施

```jsx
function NarrativeMotif({ T }) { ... }
export default React.memo(NarrativeMotif);
```

Login.jsx 内 T 引用须稳定（依赖 App.jsx 既有 `useMemo` 提供 T prop）确保 memo 命中。

### Stage 2 R-183 size props 实施

```jsx
export function KnotMark({ T, size = 32 }) {
  return <svg viewBox="0 0 100 100" width={size} height={size}>...</svg>;
}
```

### Stage 2 Q3 title 提示实施

```jsx
<input type="checkbox" title="目前仅作为偏好记录，Token 有效期受服务器策略控制" />
```

---

## 4. 文件级改动清单（最终版）

```
frontend/src/decor/
└── NarrativeMotif.jsx              [NEW] ≤ 120 行
                                    - pure SVG func 接 { T } prop
                                    - color-mix(in oklch, T.accent, transparent) 替代 brand[100]  [Q1]
                                    - 内部用 T.dark boolean 分支配色  [Q4]
                                    - export default React.memo(NarrativeMotif)  [R-182]

frontend/src/Shared.jsx              [EDIT +~60 行]
                                    - 新增 export KnotMark / KnotWordmark / KnotLogo
                                    - 各接 { T, size = 32 } props（严禁写死像素）  [R-183]
                                    - 内部用 T.dark boolean 替代 theme 字串  [Q4]
                                    - viewBox=100×100 不缩  [Q2]
                                    - 总 export 9 → 12  [R-174]

frontend/src/screens/Login.jsx       [EDIT 116 → ≤ 200 行]
                                    - 视觉重构 1:1 复刻 demo
                                    - import KnotLogo from Shared / NarrativeMotif from decor
                                    - <input checkbox title="..."> remember-me  [Q3]
                                    - <input> Focus 状态用 T.accent 蓝青  [R-184]
                                    - 页脚字面 "v0.5.7 · KNOT 内部系统"  [R-181]
                                    - export name + props { T, onLogin, onToggleTheme } byte-equal  [R-170]
                                    - api.login + cb_token 链路 byte-equal  [R-171]
                                    - error 文案 "用户名或密码错误" byte-equal  [R-172]

tests/test_login_version_sync.py     [NEW] ≤ 60 行（合并 R-185 哨兵）
                                    - test_R181_login_footer_version_synced_with_main
                                    - test_R185_login_renders_knot_logo

scripts/check_file_sizes.py          [EDIT LIMITS +2]
                                    - "frontend/src/decor/NarrativeMotif.jsx": 120
                                    - "frontend/src/screens/Login.jsx": 200
                                    - 27 条 → 29 条  [R-176]

knot/main.py                         [EDIT version 0.5.6 → 0.5.7]
tests/test_rename_smoke.py           [EDIT R-72 smoke 0.5.6 → 0.5.7]

frontend/src/screens/                [unchanged 17 屏 byte-equal]   R-178
frontend/src/{App,Shell}.jsx         [unchanged]                    R-179 + R-186 严格守护
frontend/src/{api,utils}.{js,jsx}    [unchanged]                    R-179
frontend/src/{index.css,main.jsx}    [unchanged]                    R-179

requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml             [unchanged]

CHANGELOG.md                         [EDIT [Unreleased] v0.5.7 段]
                                     R-170~R-186 偿还表 + Loop Protocol v3 第 8 次施行存档

CLAUDE.md                            [EDIT]
                                     - § v3 协议施行历史回顾表 +1 行 v0.5.7
                                     - v0.5.x 路线图 v0.5.7 ⏳ → ✅

docs/plans/v0.5.7-login-pilot.md     [LOCKED 当前文件]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（v0.5.5 baseline 430 + R-181 + R-185 = 2）
- [ ] `lint-imports` 7 contracts KEPT
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（29 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.7**

### Stage 2/3 强化验证

- [ ] **R-181**：Login.jsx 含字面 `v0.5.7`；同步测试通过
- [ ] **R-182**：NarrativeMotif.jsx 末尾含 `React.memo(NarrativeMotif)`
- [ ] **R-183**：KnotMark/Wordmark/Logo 函数签名含 `size` props；Shared.jsx 内 grep `width:\s*['"]?\d+px` 无新增
- [ ] **R-184**：手测 Tab 键切换 username/password 输入框焦点 — outline/border 显蓝青 T.accent
- [ ] **R-185**：哨兵测试通过；Login.jsx grep `<KnotLogo\|<KnotMark` ≥ 1
- [ ] **R-186**：`git diff frontend/src/Shell.jsx` = 0 行
- [ ] **Q1**：NarrativeMotif.jsx 内 grep `color-mix(in oklch` ≥ 1
- [ ] **Q3**：Login.jsx remember checkbox 含 `title="目前仅作为偏好记录，Token 有效期受服务器策略控制"`
- [ ] **Q4**：所有新组件内 grep `theme ===` = 0；grep `T\.dark` ≥ 1

### 契约守护

- [ ] **R-170**：App.jsx 调用 LoginScreen 的 props 字面 byte-equal
- [ ] **R-171**：api.login + cb_token 链路 byte-equal（端到端登录手测通过证明）
- [ ] **R-172**：error 文案 byte-equal grep 命中
- [ ] **R-178**：`git diff frontend/src/screens/` 仅含 Login.jsx；其他 17 屏 + 12 子模块 0 改
- [ ] **R-179**：App/Shell/api/index.css/main/utils 字面 byte-equal
- [ ] **R-186**：Shell.jsx 严格 0 修改（即使 KnotLogo 已可用）

### 视觉对照

- [ ] light + dark 双主题各 1 张截图，与 demo login.jsx 视觉对齐
- [ ] NarrativeMotif 在 light 模式下背景对比度可见（Q1 color-mix 兜底有效）

### 不破坏

- [ ] backend 432 tests + 72 routes + 7 contracts 不变（除 R-180 main.py + R-72 smoke + R-181/R-185 新测试）
- [ ] requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js 不动
- [ ] v0.5.0~v0.5.6 既有功能端到端正常（cartesian / fan-out / R-91 / 7 intent layout / CSV / 错误边界 + v0.5.6 蓝青皮）

---

## 6. Commit 序列（最终锁定 — 5 commit；Stage 3 §2 顺序内嵌）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `feat(v0.5.7): add KnotMark/KnotWordmark/KnotLogo to Shared.jsx (R-174/183, Q2/Q4)` | npm build + 17 屏 byte-equal + Shared exports 9→12 | size props + T.dark + viewBox 100×100 |
| 2 | `feat(v0.5.7): add NarrativeMotif decor SVG with color-mix tint (R-173/182, Q1/Q4)` | npm build + decor/ 新增 + React.memo 包裹 | color-mix OKLCH + T.dark + memo |
| 3 | `refactor(v0.5.7): rebuild Login.jsx to demo visual — pilot 1 屏 (R-170~R-172/175/181/184/185, Q3)` | npm build + 端到端登录手测 + Tab 键 focus + remember title | api.login byte-equal + 焦点蓝青 + title 提示 |
| 4 | `chore(v0.5.7): version bump 0.5.6→0.5.7 + check_file_sizes LIMITS +2 (R-176/180)` | 全闸门 + routes=72 + version=0.5.7 + 29 条 LIMITS | 三处同步：main.py + smoke + Login 页脚 |
| 5 | `docs(v0.5.7): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED 归档` | 全闸门 | Loop Protocol v3 第 8 次施行存档 |

每 commit 独立全闸门绿。commit 1 完成后 Login.jsx 暂未引用 KnotLogo（仅 Shared 注入），既有 Login 渲染不破坏；commit 3 才真正切换 Login 视觉。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 8 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 17（v0.5.7 区段 R-170~R-186）
- ✅ Pilot 模式确立 — 1 屏 1 PATCH 为 v0.5.8+ 提供执行模板
- ✅ 范围最小化 — 17 屏 + Shell + App + api 字面 byte-equal；R-186 严格 1 屏限制

---

## 8. 执行者纪律自检（10 条 — Stage 3 守护者建议照单收）

1. ✅ 锁定手册落地 → 当前文件 LOCKED
2. ✅ commit 顺序绝对不可换：Logo → Motif → Login → version+LIMITS → docs
3. ✅ R-186 Shell.jsx 严格守护 — 即使 KnotLogo 已可用本 PATCH 一律不碰；commit 3 后强制 `git diff frontend/src/Shell.jsx` = 0
4. ✅ R-184 焦点蓝青手测 — commit 3 后强制启动 dev server + Tab 键切换两个输入框
5. ✅ R-181 三处同步 — commit 4 同时改 main.py + smoke + Login 页脚；写完一处立即 `grep "0.5.7" knot/main.py tests/test_rename_smoke.py frontend/src/screens/Login.jsx` ≥ 3 命中
6. ✅ R-185 哨兵测试合并入 `test_login_version_sync.py`（避免 30+30 文件碎片化）
7. ✅ R-182 React.memo 命中 — Login.jsx 引用 T 须稳定（依赖 App.jsx 既有 useMemo）
8. ✅ Q1 color-mix `@supports` 兜底（如不支持简化为半透明 T.accent）
9. ✅ Q4 T.dark 严守 — 复制 demo logo.jsx 时全文替换 `theme === 'light'` → `!T.dark` / `theme === 'dark'` → `T.dark`；commit 1/2/3 各跑一次 `grep "theme ===" frontend/src/{decor,Shared,screens/Login}*` = 0
10. ✅ 范围抗诱惑 — 看到任何"顺手 Home/Shell/i18n/buildTheme 扩字段"想法一律拒；本 PATCH 建立的"1 屏 1 PATCH"洁癖将成为 v0.5.8+ 的执行模板

---

🛡️ **执行者收到 GO 信号，待资深架构师最终 ack 后启动 Stage 4。**
