# v0.6.3.2 — C5 AdminAudit.jsx 子组件拆分（Phase B 段 7 · 纯重构）

> **Loop Protocol v3 第 41 次施行 — 全 v3 三阶段**（资深拍板 — 纯重构不走简化协议）
> **范围权威**：`docs/plans/v0.6.2.0-phase-b-roadmap-v4.md` §1 C5（行 111）+ §2 段 7（行 169）
> **基座**：worktree `v0632-audit-split` @ branch `v0.6.3.2-adminaudit-split`（off main `3d38df8`）
> **先例**：v0.6.0.2 ResultBlock.jsx 449→279 + 6 子组件拆分（R-PA-PB-V0.1~V0.6 守护模板）
> **版本**：v0.6.3.2（下一顺序 PATCH；roadmap 暂定 D3 指标屏未做，版本号顺延 — 版本号教训）

---

## §0 现状实证（origin/main 3d38df8）

- `frontend/src/screens/AdminAudit.jsx` = **388 行**（LIMIT 490）— 单文件含 4 模块 helper + AdminAuditScreen（state/effects/4 handlers + 6 视觉段）+ 底部 3 helper
- **C5 = 纯结构重构**（无业务变更）：把 6 视觉段抽成子组件，AdminAuditScreen 仅留编排 → 提升可读性 + 单文件瘦身
- **0 后端 / 0 视觉 / 0 业务逻辑变更** — 纯 React 组件提取（与 v0.6.0.2 ResultBlock 同性质）

**文件内部结构**（拆分锚点）：
| 段 | 行 | 抽成 |
|---|---|---|
| actionColor / ActionChip | 15-32 | → AuditTable.jsx（仅 Table 用）|
| _sinceFromPreset / exportAuditCsv | 34-57 | 留 AdminAudit.jsx（load/topbar 编排用）|
| Stat 4-card grid | 156-175 | → AuditStatGrid.jsx |
| Retention + purge banner | 177-199 | → AuditRetentionBar.jsx |
| Filter strip | 201-232 | → AuditFilterStrip.jsx |
| Table（grid + rows）| 234-290 | → AuditTable.jsx |
| Pagination | 292-306 | → AuditPagination.jsx |
| Drawer + DetailJsonView + KV | 313-388 | → AuditDetailDrawer.jsx |

---

## §1 范围

### In-scope
- **F1** 抽 6 子组件到新目录（D1 决策）：AuditStatGrid / AuditRetentionBar / AuditFilterStrip / AuditTable / AuditPagination / AuditDetailDrawer
- **F2** AdminAudit.jsx 瘦身为编排层（保 state + 4 effects + 4 handlers + load + AppShell + 子组件组合）；保留 _sinceFromPreset / exportAuditCsv 模块 helper
- **F3** check_file_sizes LIMITS dict 加 6 新文件 + AdminAudit.jsx LIMIT 下调（490 → 收紧后实际值，资深 ack）
- **F4** 收官：version 0.6.3.2 三同步 + README/CLAUDE 路线图 + CHANGELOG + 静态（Login footer 改）

### OOS（明确不做）
- ❌ 0 视觉变更（VRP — 渲染输出 byte-equal；inline style 原样平移）
- ❌ 0 业务逻辑变更（api / filter / 状态机 / handler 行为 byte-equal）
- ❌ 0 后端 / 0 schema / 0 路由
- ❌ 不动 Shared.jsx（子组件 import Shared 既有 export，不新增/改 Shared）
- ❌ 不动 App.jsx 挂载点（import L12 + render L121 `<AdminAuditScreen {...commonProps}/>`；export 名 + props 签名 byte-equal）
- ❌ 不顺手改其他 admin 屏（AdminBudgets/Recovery/Errors/Metrics/QueryHistory）
- ❌ 不抽 helper 进 Shared（本 PATCH 仅 AdminAudit 内部拆分；跨屏 helper 共享留未来）

---

## §2 决策点（执行者推荐，待 Stage 2/3 + 资深）

- **D1 子组件目录**：✅ **资深拍 B = `frontend/src/screens/audit/`**（守护者 §四 + critique 修订 — admin/ 是 tab dumb 组件语义，AdminAudit 是顶层屏；audit/ 与 v0.5.3 `chat/` family 先例一致；定 C5 系列拆分先例）。AdminAudit.jsx 仍留 `screens/AdminAudit.jsx`（export 名 byte-equal）。
- **D2 ActionChip + actionColor 去向**：推荐随 **AuditTable.jsx**（仅 Table 渲染用）。
- **D3 DetailJsonView + KV 去向**：推荐随 **AuditDetailDrawer.jsx**（仅 Drawer 用）。
- **D4 handler 归属**：推荐 triggerPurge/saveRetention/_relPurge/load **留 AdminAuditScreen**（依赖多个 state setter；子组件经 props 收 callback + 数据）— 子组件做 dumb presentational（同 v0.5.3 D4 admin tab dumb component 模式）。
- **D5 AdminAudit.jsx 新 LIMIT**：拆后预估 ~180-210 行 → LIMIT 下调（490 → 230 缓冲）；6 子组件各 LIMIT（StatGrid ~50 / RetentionBar ~60 / FilterStrip ~70 / Table ~120 / Pagination ~40 / Drawer ~110）。

---

## §3 红线（R-PB-C5-1~7 — 执行者立，待终审）

| 红线 | 内容 |
|---|---|
| **R-PB-C5-1 契约 byte-equal** | `AdminAuditScreen` export 名 + 5 props 签名 `{ T, user, onToggleTheme, onNavigate, onLogout }` byte-equal → **App.jsx 挂载点（import L12 + render L121 `<AdminAuditScreen {...commonProps}/>`）git diff 0 行**（Stage 2 critique 校正 — 实际挂载在 App.jsx 非 Admin.jsx；grep AdminAudit Admin.jsx = 0）|
| **R-PB-C5-2 业务逻辑 byte-equal** | api 5 endpoint（audit-log/audit-stats/audit-config/audit/purge/audit/purge-status）+ filter params + `_PAGE_SIZES`/`_REDACTED_RE`/`_sinceFromPreset`/`exportAuditCsv`/`actionColor` + 4 handlers（triggerPurge/saveRetention/_relPurge/load）行为 byte-equal |
| **R-PB-C5-3 VRP 0 视觉漂移** | R-PA-PB-V1 — 渲染输出 byte-equal；**VRP 基线 = 当前渲染态**（守护者 §二 钉死）：thead `background: T.bg` 灰底（**非** v0.5.17 R-409 brandSoft 8% — v0.5.38 已回退；严禁"恢复"规范）；AdminAudit 无 brandSoft 8% 内联（仅 Shared Avatar 内部）；唯一实际 color-mix = redacted `T.warn 20%`（L371）；inline style + **注释原样平移**（含 L243 v0.5.38 回退注释 + 陈旧 L271 ActionChip 注释，不借机修缮）；R-415 rgba drawer overlay 豁免 sustained |
| **R-PB-C5-4 0 后端 0 业务屏** | git diff 仅 frontend/src/screens/AdminAudit.jsx + 新 admin/audit/*.jsx + check_file_sizes + 收官 docs/version；knot/**/*.py 0（除 version 串）；其他屏 byte-equal |
| **R-PB-C5-5 Shared.jsx 0 改** | 子组件 import Shared 既有 export（I/iconBtn/pillBtn/StatusDot/FilterField/statCardStyle/Avatar/inputStyleField/ghostBtnStyle/primaryBtnStyle/pageBtnStyle/preStyle/statValueStyle/statLabelStyle）；不新增/改 Shared export |
| **R-PB-C5-6 check_file_sizes** | LIMITS dict +6 文件 + AdminAudit LIMIT 下调；CI 行数核验绿 |
| **R-PB-C5-7 R-PA-8 sustained** | 0 命中（AdminAudit 无 OOS 字面）；R-PA-PB-V1 sustained |

---

## §4 验收标准

1. AdminAudit 屏渲染 + 交互 byte-equal（Stat/Retention/Filter/Table/Pagination/Drawer 全功能）— 部署后 knot.0p.oh 人测
2. `git diff origin/main -- frontend/src/App.jsx` = 0 行（R-PB-C5-1 — App.jsx 是真实挂载点）
3. AdminAudit.jsx 显著瘦身（388 → ~200）+ 6 子组件各 dumb presentational
4. npm build exit 0 + 0 新 lint 错
5. 业务逻辑 byte-equal（R-PB-C5-2）+ Shared.jsx 0 改（R-PB-C5-5）
6. check_file_sizes OK（+6 文件）+ R-PA-8 0 命中
7. version 0.6.3.2 三同步 + R-72/R-181 + README L11 bump（纳入收官 checklist — v0.6.3.1 补强 sustained）

---

## §5 commit 序列（2 commit）

- **commit 1**：抽 6 子组件 + AdminAudit.jsx 改编排层（一次性原子重构 — 保证中间态可编译）+ check_file_sizes LIMITS + Stage 1 草案 §9 LOCKED 提交。验证：npm build + diff App.jsx 0 行 + 业务 byte-equal + VRP
- **commit 2**：收官 — version 0.6.3.2 三同步 + README L11 bump（+ 描述符）+ CLAUDE 路线图 v0.6.3.2 行 + CHANGELOG v0.6.3.2 段（demote 3.1→Released）+ 静态重建（Login footer）+ hub 0 死链 sustained

---

## §6 协议合规

**全 v3 三阶段**（资深拍板 — 纯重构含前端业务屏拆分，不适用简化协议；Stage 2 辅助 AI 初审对组件边界/契约 byte-equal 有价值）：Stage 1 草案 → Stage 2 辅助 AI 初审 → Stage 3 守护者终审 → Stage 4 落地。**无远古激活**（C5 纯重构，非 F4/§2/§3）。

---

## §7 自检（Stage 1 草案）

- ✅ 范围权威锚定（roadmap v4 C5 + 段 7）
- ✅ 先例复用（v0.6.0.2 ResultBlock 6 子组件 R-PA-PB-V0.* 模板）
- ✅ 纯重构 0 视觉/业务/后端变更立约（R-PB-C5-3/4）
- ✅ 契约 byte-equal（R-PB-C5-1 App.jsx 0 改 — Stage 2 critique 校正挂载点）
- ✅ Shared.jsx 0 改（R-PB-C5-5）
- ⏳ 待 Stage 2 初审 + 守护者 Stage 3 终审 + 资深拍板

---

## §8 待人测（部署后 knot.0p.oh）

- AdminAudit 屏：Stat 卡 / Retention 配置 + 立即清理 / Filter 查询重置 / Table 渲染 + Avatar/ActionChip/StatusDot / 翻页边界 / Drawer 详情 + DetailJsonView 脱敏高亮 / CSV 导出
- light + dark 双模式视觉一致（VRP）

---

## §9 LOCKED（Stage 2 整合 + critique 校正 — 2026-06-17；Stage-3-ready）

> **Stage 2 质量信号**：辅助 AI 初审给 4.9/5「滴水不漏」但**未读源文件** — ① ratify 了 Stage 1 的 Admin.jsx 挂载点错误（false green）② 编造不存在的细节（Tailwind 类名 / `logs`/`activeLog` props）。**降级**：Stage 2 不可视为验证了 R-PB-C5-1；契约由执行者实证 + Stage 3 守护者重验。critique（第三方 grounded 复核）抓出错误 + 6 缺口，执行者实证采纳。

### §9.1 ⭐ R-PB-C5-1 挂载点校正（App.jsx 非 Admin.jsx）
执行者实证（grep）：`AdminAudit` 在 Admin.jsx = **0 hits**；真实挂载 = **App.jsx**（L12 `import { AdminAuditScreen } from './screens/AdminAudit.jsx'` + L121 `if (screen === 'admin-audit' && user.role === 'admin') return <AdminAuditScreen {...commonProps}/>`）。
- **R-PB-C5-1 锁定**：AdminAuditScreen export 名 + 5 props 签名 byte-equal → **`git diff origin/main -- frontend/src/App.jsx` = 0 行**（`{...commonProps}` spread 传参 → 只要组件仍解构同 5 props，App.jsx 0 改）。Admin.jsx 全程不涉及（非挂载点）。

### §9.2 ⭐ 逐子组件 props 契约（byte-equal 审计的真正 crux — Stage 1/2 均 hand-wave，本节钉死）
6 子组件 = dumb presentational（D4 — 同 v0.5.3 admin-tab dumb 模式；handler 留 parent）：

| 子组件 | props 签名 | const/helper 随迁 | utils import |
|---|---|---|---|
| **AuditStatGrid** | `{ T, stats }`（stats readonly）| — | — |
| **AuditRetentionBar** | `{ T, relPurgeText, retention, setRetention, purging, onPurge, onSaveRetention }`（守护者 §三 — 父算好 `_relPurge` 字符串传 `relPurgeText`，子保纯展示，不传 purgeStatus）| — | — |
| **AuditFilterStrip** | `{ T, filter, setFilter, onReset, onQuery }` | — | — |
| **AuditTable** | `{ T, items, loading, onOpenDrawer }`（items readonly）| `actionColor` + `ActionChip` | `Spinner` |
| **AuditPagination** | `{ T, items, page, size, setPage, setSize }` | `_PAGE_SIZES` | — |
| **AuditDetailDrawer** | `{ T, drawerRow, onClose }`（drawerRow readonly；`!drawerRow` 内部返 null）| `DetailJsonView` + `KV` + `_REDACTED_RE` | — |

**AdminAuditScreen 保留**：10 state + 3 effects + 3 handler（triggerPurge / saveRetention / load；`_relPurge` 移入 RetentionBar）+ `_sinceFromPreset` + `exportAuditCsv`（load + topbar 编排）+ AppShell + topbarTrailing（导出 CSV button）+ 6 子组件组合 + onReset/onQuery callback（含默认 filter 对象 + setPage/load）。

### §9.3 const 放置（随消费者 — critique gap 2）
- `_PAGE_SIZES` → AuditPagination · `_REDACTED_RE` → AuditDetailDrawer（DetailJsonView 用）· `actionColor`+`ActionChip` → AuditTable · `_sinceFromPreset`+`exportAuditCsv` → **留 AdminAudit**（load/topbar 编排用）。**不静默留 parent**（R-PB-C5-2 refine）。

### §9.4 ⭐ VRP root-element 红线（R-PB-C5-3 强化 — critique gap 3）
每个子组件**必须返回原段的 root element，禁止额外 wrapper/fragment** — 否则父层内 maxWidth column `flex gap:16`（L153）布局漂移：
- StatGrid 返 `<div grid>` · RetentionBar 返 `<div banner>` · FilterStrip 返 `<div filter>` · Pagination 返 `<div pagination>`（4 者 = column 直接子）
- **AuditTable 是陷阱**：返 `{loading ? <Spinner div> : items.length===0 ? <empty div> : <grid div>}` 三元 conditional（单 root/分支，无 wrapper）— 边界**逐字保留**
- AuditDetailDrawer 返 `drawerRow ? <fixed overlay> : null`（position:fixed 不影响布局；R-415 rgba overlay 豁免 sustained）

**既有守护原样平移（守护者 §二 — 不得借机修缮）**：`✕` 字面关闭键（L327，不换 I.x）/ rgba drawer overlay + R-415 豁免注释（连注释搬，L316）/ R-428 actor null 链（L256-257）/ R-429 DetailJsonView try-catch（L351-360）/ R-430 翻页边界 disabled（L302-305）/ 灰 thead T.bg（L247，**严禁回退 R-409 brandSoft**）。

### §9.5 utils.jsx import 分布（critique gap 4 — R-PB-C5-5 扩展）
`Spinner` → AuditTable；`toast` → **留 AdminAudit**（triggerPurge/saveRetention/load 用）。R-PB-C5-5 = Shared.jsx 0 改 **+** utils import 正确分布（不漏迁 / 不错置）。

### §9.6 D5 LIMITs 饱和收紧（采纳 Stage 2）
AdminAudit.jsx LIMIT **490 → 210**（拆后预估 ~190 + ~20 buffer）；6 子组件 ceiling：StatGrid 50 / RetentionBar 60 / FilterStrip 70 / Table 120 / Pagination 40 / Drawer 110。check_file_sizes LIMITS +6 + AdminAudit 下调（资深 ack）。

### §9.7 D1 子组件目录（critique gap 5 — 修订推荐 + 待资深确认）
critique 指出 `admin/` 是 **tab-component 文件夹**（tab_access/resources/...），AdminAudit 是顶层屏非 tab，且姊妹顶层屏（Errors/Recovery/Metrics/QueryHistory）**无子文件夹**（皆 flat 单文件）。**修订推荐 A' = `frontend/src/screens/audit/`**（专属子目录，唯一需拆的顶层屏自包含；不污染 admin/ tab 语义）。备选 flat `screens/AdminAudit*.jsx`（6 文件平铺嫌乱）。**⚠️ 待资深确认**（critique 列为 senior 开放项）。

### §9.8 D3/sequencing（critique gap 6 — 已有资深裁量）
资深 2026-06-17 AskUserQuestion 已选「C5 只拆 AdminAudit」入段 7；v0.6.3.2 = 下一顺序 PATCH（roadmap 暂定 D3 指标屏 = v0.6.3.2 superseded → 顺延；版本号教训：roadmap 指导性，PATCH 号 tag 时顺序定）。**sequencing 已 senior-nodded**。

### §9.9 保留（Stage 2 + Stage 1 合理项）
D4 dumb-topology（accept）· no-mutation 重述真 props（AuditTable items readonly / AuditDetailDrawer row readonly — 非编造的 logs/activeLog）· Q4 Shared.jsx 0 改 · 原子 2-commit · R-PB-C5-2/4/6/7 sustained。

### §9.10 ~~待 Stage 3~~ → Stage 3 守护者终审已整合（见 §9.11）

### §9.11 Stage 3 守护者终审整合（2026-06-17 · Accept with revisions · 8 节）
守护者（v0.5 血缘 — 本屏原作者）直读 c45bef2 后 8 节修订，执行者全实证采纳：
- **§一 契约纠错（必修）**：R-PB-C5-1 守护目标 = **App.jsx 0 diff**；删除 Admin.jsx 空守护（从不引用 AdminAudit，对错都绿 = 空守护）。已落 §9.1 + §3。
- **§二 VRP 真实基线（必修 · 守护者血缘钉死）**：thead = **灰 T.bg 非 R-409 brandSoft**（v0.5.38 回退）；AdminAudit 无 brandSoft 8% 内联；唯一 color-mix = redacted T.warn 20%（L371）；既有守护（✕/rgba+注释/R-428/R-429/R-430/灰 thead）原样平移不修缮。已落 §3 R-PB-C5-3 + §9.4。**执行者实证确认**（grep 命中 L243/265/271 皆注释；ActionChip L271 注释陈旧但 byte-equal 平移）。
- **§三 逐子组件 props（命门）**：守护者表 = 执行者 §9.2 表，2 处细化采纳 — RetentionBar 收 `relPurgeText`（父算 _relPurge 字符串，子纯展示，不传 purgeStatus）+ Drawer 用 `drawerRow`（非 row）；root-element 铁律 + 常量随消费者迁移确认。已落 §9.2/§9.3/§9.4。
- **§四 决策**：D1 修订（上交资深 — 见 §9.7 + §七）；D2/D3/D4/D5 保留（D5 LIMIT 守护者核过 parent ~150-190 / Table ~78 / Drawer ~78，210+20 缓冲合理）；_relPurge 格式化器非 handler（不破 D4）。
- **§五 Stage 2 吸收**：方向对、specifics 不可信（未读源 — 虚构 Tailwind/logs/activeLog + 带进 App.jsx 错）；no-mutation 精神保留、对象名正 items/drawerRow。
- **§六 红线 verdict**：C5-1 修订（App.jsx）/ C5-2 保留+补强（props 表）/ C5-3 修订（灰 thead 基线 + root-element）/ C5-4/5/6/7 保留+补强。
- **§八 放行闸门**：commit 1 须自证 npm build exit 0 + `git diff App.jsx`=0 + check_file_sizes 绿（6 子 + AdminAudit 210）+ 渲染抽查灰 thead 未回退 → 四项齐放行 commit 2。

### §9.12 ⚠️ 上交资深拍板（守护者 §七 — 不单裁）
1. ✅ **D1 子组件目录 = B `screens/audit/`**（资深 2026-06-17 拍 — 与 v0.5.3 `chat/` family 子文件夹先例一致；不污染 admin/ tab 语义）。**定 C5 系列（后续 5 同类 admin 屏拆分）目录先例**。AdminAudit.jsx 仍留 `screens/AdminAudit.jsx`。
2. ✅ **D3 让位 C5**（资深 C5 选择确认）：v0.6.3.2 = C5；D3 指标屏顺延。
3. ✅ **P3「可选」现在做**（资深 C5 选择确认）：主动可读性拆分。

**✅ 3 项资深拍板全清 + 守护者 Accept with revisions → 进 Stage 4，起 commit 1（§八 四闸门自证：build + App.jsx 0 diff + check_file_sizes 6 子+210 + 灰 thead 未回退）。**
