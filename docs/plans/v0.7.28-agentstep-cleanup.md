# v0.7.28 — AgentStep vestigial dataclass 清理（承 v0.7.26 · chore）

> **简化协议**（资深 2026-07-02 拍板「AgentStep 死代码清理（先清尾）」；chore + 0 行为变更 + 0 新红线 + grounded 无争议 → 跳 Stage 2/3；镜像 v0.7.26 AgentResult 清理同类同文件先例）。非阻塞 follow-on 池首项。**0 新 R-SL**。

## 1. 背景（承 v0.7.26）

v0.7.26 删了 `models/agent.py` 的 vestigial `AgentResult`（query 路径实用 `sql_planner.py:42` 自定义版），但**同文件的 `AgentStep` 是同款孤儿**（sql_planner.py:32 有自己的 AgentStep），v0.7.26 出 scope 留评估。本刀清尾 —— 消除 models vs sql_planner 的 AgentStep 双定义混淆。

## 2. grounded 死代码确认（R-137 · 彻底 sweep 含 path-string + mock）

`models/agent.py` `AgentStep`（ReAct 单步快照）：
- `grep "AgentStep"` 排除 sql_planner → 仅 `models/__init__.py:16` import + `:71` __all__（re-export plumbing）+ `models/agent.py:42` 类定义本身。**0 真消费者**（0 `from ... import AgentStep` 外部、0 `AgentStep(` 构造器 outside sql_planner、0 path-string、0 test 引用）。
- `models/agent.py` 内部 0 自引用（引用它的 AgentResult 已于 v0.7.26 删）。
- query 路径 SSE thinking 用 `sql_planner.py:32` 自定义 AgentStep（`_run_tool` 构造），非 models 版。

**保留**（在用）：`AgentKind`/`VALID_AGENT_KINDS`（conversation.py + test_message_repo）/`ClarifierOutput`/`PresenterOutput`。`test_models_are_leaf` 只 iter 子模块 import，不 assert 类存在性 → 不受影响。

## 3. 改动

- **删** `knot/models/agent.py` `AgentStep` 类（+ 扩 v0.7.26 消歧注释：AgentResult + AgentStep 均在 sql_planner.py，勿在此重建）。
- **改** `knot/models/__init__.py`：移除 `AgentStep` import + `__all__` 项（`__all__` 29→28）。

## 4. 验收

- ✅ `import knot.main` + `import knot.models` OK；`AgentStep ∉ knot.models.__all__` + `not hasattr(knot.models, 'AgentStep')`。
- ✅ 全测试套件无回归（models/__init__ 广泛被 import → 全跑；v0.7.26 lesson）。
- ✅ import-linter **9 KEPT**（models 仍纯叶子）；ruff check knot/ 0；check_file_sizes OK。
- ✅ 5 源点 0.7.27→0.7.28 + CHANGELOG + static rebuild。
- ✅ 0 行为变更（纯删死代码 + 注释）。

## 5. commit 序列

- **C1 清理**：agent.py 删 AgentStep（+注释）+ __init__.py 移除 import/__all__。
- **C2 收官**：5 源点 0.7.27→0.7.28 + static rebuild + CHANGELOG + plan §6。

## 6. 协议合规 / 收官记录

- **简化协议**（资深 ack）：跳 Stage 2/3；无守护者终审（chore + 0 行为 + 0 红线）。镜像 v0.7.26 先例。
- **package.json 冻结 0.0.0 不碰**。
- R-137 彻底 sweep（import/构造器/path-string/test — 内化 v0.7.26 path-string + v0.7.27 test-mock 教训；本刀 sweep 0 遗漏，AgentStep 无 mock/path 引用）。
- **AgentStep + AgentResult vestigial 双清收官**（models/agent.py 现仅留在用的 AgentKind/VALID_AGENT_KINDS/ClarifierOutput/PresenterOutput）。

_（C1/C2 commit hash + 验证结果 落地后填）_
