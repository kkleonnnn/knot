# v0.7.47 — 死码清扫 chore（整体审核 §3.2-5 · grounded 分层）· Stage 1 执行手册

> **执行者**:v0.7 Agent · **日期**:2026-07-05 · **基线**:main `f2a2362`(v0.7.46)
> **性质**:死码清扫 chore（删代码=业务代码变更 → 走完整 v3 三阶段,**不适用** R-LP-v3-EX-1）。
> **grounded 依据**:8 路并行核实 workflow `w00xz6ugi`(每候选独立找 caller:import+调用+路径字符串+动态引用+__all__/getattr,全 repo 含 tests/frontend/scripts）。
> **来源**:v0.7→v0.8 整体审核 §1.3 死码 + §3.2-5 chore 打包。

---

## §0 核实修正原审计 3 处(grounded 推翻,写入留痕)

| 审计原判 | grounded 真相 | 处置 |
|---|---|---|
| `query_helper.release`/`reset_active_catalog_ctx` = 未兑现治理承诺,接近 R-LP-v3-EX-3 连续 3 PATCH 线 | **STALE**。中间件方案已在 v0.6.2.6（0c76bbf）判定技术不可行并**撤回**(改 asyncio task 隔离,CHANGELOG:1584 + GOVERNANCE-ARCHIVE:149,@race 100× 测试佐证)。EX-3 不成立 | **纯删死码残骸**(非治理决策) |
| `KnotWordmark` export 是死的 | **半错**。函数体 LIVE（KnotLogo:669 内部调用);仅 export 关键字无外部 import,但被 test_R185 + test_foundation_additive + R-185/R-199.5 治理红线锚定 | **保留,不删** |
| `audit_service.get_failure_count` 死码可删 | **半错**。是 R-64 prometheus hook 预留;底层 counter `_audit_write_failures_total` 仍 log():117 live 递增 | **保留（低成本预留资产）** |

---

## §1 Tier A — 纯删/纯改(建议纳入 v0.7.47;零 byte-equal/foundation/治理阻断)

### 后端
- **A1 删 `query_helper.release`**（query_helper.py:110-117）+ **`reset_active_catalog_ctx`**（agents/catalog.py:72）+ 同步 `tests/services/test_catalog_context.py:29-42`（其 finally 调 reset;改测直接 `_active_catalog_ctx.reset(token)` 或删该断言）。`set_active_catalog_ctx` 返回的 Token 已无消费者（可选退化返回 None,非必须）。
- **A2 删 `user_repo` 4 死方法**（user_repo.py:109/119/128/140:get_user_monthly_usage / get_monthly_cost / get_user_agent_model_config / set_user_agent_model_config,全 TESTS_ONLY）+ trim `tests/repositories/test_user_repo.py`（test_get_monthly_cost_aggregates / test_user_agent_model_config_roundtrip / _empty_default;test_update_user_usage_accumulates 只删 get_user_monthly_usage 读回断言,**保留** update_user_usage 覆盖——它 LIVE）。**严禁碰** `settings_repo.get_agent_model_config/set_...`（命名撞车,app 级 LIVE）。`agent_model_config` 列保留（schema 决定另议）。
- **A3 修 `bulk_insert_few_shots` 返回值**（few_shot_repo.py:53）:comprehension 过滤缺 question/sql 行后仍 `return len(items)`（过滤前总数）→ 物化过滤后 list 变量,`return len(params)`。few_shots.py:83 当前靠 API 预过滤（:71-72）数字恰好对,但 repo 本身错 + 任何其他 caller 得虚高。同步 test 加返回值断言（==2）。

### 前端（screen 文件,非 byte-equal 铁律清单）
- **A4 删 `primitives.jsx` Tag**（L51-72,含 5-tone 注释块）。全 repo 0 import/0 `<Tag`/0 dynamic;Btn 不动 → Login.jsx import 0 破坏。**不改** Shared.jsx:400 注释（保 Shared byte-equal;陈旧注释可 Tier B 随手）。
- **A5 删 4 处幽灵 props**（接收方签名全不含,纯噪音）:Chat.jsx:318 `onSend/onNewChat/hasConv`→ChatEmpty（**保留** newChat 定义,只删传参）· SavedReports.jsx:110 `hideSidebarNewChat`→AppShell · Admin.jsx:369 `onNewChat={()=>{}}`→AppShell · AdminErrors.jsx:43 `convs/setConvs/dbOk/sourceCount`→AppShell（只删 `<AppShell>` 传参,不动组件自身变量）。
- **A6 删 `frontend/src/App.css`**（1-29 行,0 importer + 0 shipped rule → 纯删零行为变更;非 ironclad）。⚠️ **仅纯删**——把内容折进 index.css 是 Tier B（触 ironclad + 补当前缺失的 fallback = 行为变更）。
- **A7 删 Login.jsx:245 陈旧 build 字面** ` · build 202606181000`（2026-06-18 硬编,旁 `v{APP_VERSION}` 已动态=0.7.46）。R-181 test 只验 `v{APP_VERSION}` → 删 build 部分 test 仍绿。改后须 npm build 同步 static。

### 文档（纯改 CLAUDE.md,零风险）
- **A8 CLAUDE.md 漂移 4 处**:① notification L321"接口预留"→ 实况（webhook.py WebhookNotificationAdapter v0.7.7 落地,monitors.py:103/112/121 生产调用;保 R-SL-69 egress allowlist + R-SL-77 flag-gate 安全描述不弱化)② I 库 L192/227"38 names"→"54 names（v0.6.4.0 UI v2 +16）"③ Admin L318"14 handlers+11 state"→"22 handlers + 23 useState + 3 ref（v0.6.2.5 多 catalog 扩容）"④ **R-165 陈述订正**（设计系统段"OKLCH fallback：R-165 …已兜底"）→ 如实陈述:`@supports not` fallback 原在 App.css、当前**未进产物**（随 A6 删 App.css;index.css 仅 3 行 reset,视觉靠各屏 inline fontFamily）。实修 R-165 折进 index.css 留 v0.8 前端硬化（kk 2026-07-05 决策）。**实施前 grounded 读 index.css/App.css 定稿准确措辞**。

**版本**:v0.7.46→v0.7.47（4 源点 main.py/version.js/README/test_rename_smoke + CHANGELOG）+ npm build（A4-A7 前端改动）。

---

## §2 Tier B — 触 foundation/行为（kk 2026-07-05 拍板 LOCKED）

- **B1 删 17 个死 icon → 单独 foundation PATCH（不纳入 v0.7.47）**。理由:re-baseline 仪式（改 Shared.jsx + re-baseline `tests/foundation/shared_foundation_base.jsx` + 递减 R-PB-SH-4 常量 54→37 + 更 CLAUDE.md icon count）+ I.medal 治理债值得独立专注守护,不混进机械 chore。候选清单（grounded 0 使用,部分功能走 SAVED_SVG/RB_SVG/INFO_PATH 路径字面绕过）:history/key/plug/chart/more/collapse/sparkle/robot/attach/edit/chat/node/bookmark/bookmarkFill/medal/info/pin。触 3 guard:R-PB-SH-4(len==54)/ R-PB-SH-1(additive-only)/ R-PB-SH-12(baseline mirror)。
  - **B1 子决策(留给该 foundation PATCH)— I.medal 治理债**:docs/plans/v0.5.19 承诺把 AdminRecovery inline trophy svg 迁移到 I.medal（至今未迁）。删 I.medal = 违该承诺 → 二选一:(a) 删（撤回承诺,记留痕)(b) 履约迁移（则保留）。
- **B2 R-165 fallback 折进 index.css → 推迟 v0.8 前端硬化（LOCKED）**。v0.7.47 只做 A6 纯删 App.css + A8④ CLAUDE.md 如实陈述"fallback 未 ship";真修（折进 ironclad index.css 补回 fallback,行为变更）留 v0.8。

---

## §3 Tier C — 修正/推迟(不纳入本 chore)

- **C1 保留 KnotWordmark / audit_service.get_failure_count**（§0 修正,非死码)。
- **C2 models [BLUEPRINT-ONLY] 标注偿还 + ProviderKind** — **推迟**（需独立一致性 pass）。**冲突未决**:models-dataclass 组确认 dead 的是 {Conversation/User/SavedReport/Catalog/ModelConfig/AgentName + Budget};doc-drift 组称 __all__ 里假 LIVE 的是 {User/DataSource/LLMRequest/LLMResponse/ModelConfig/AgentName}——**两组对 DataSource/LLMRequest/LLMResponse 是否 LIVE 判断冲突**（doc-drift 称 LLMRequest/LLMResponse 实际 resolve 到 adapters.llm.base 本地副本、DataSource 0 构造;原审计 §1.3 称这 3 个是真 LIVE）。ProviderKind 又硬绑 ModelConfig:58 标注（不可脱离单删）。→ 标注是 Go 蓝图锚点、零 runtime 风险,但须一次 grounded 定清哪些真 LIVE 再改 __all__ 标注,不塞进机械 chore。
- **C3 knowledge delete 缺 audit** — **推迟**（非删,是审计契约扩展)。补 audit 须新增 AuditAction `knowledge.*` Literal + AuditResourceType `knowledge`;且 knowledge_upload 同样无 audit → 对称补 create 侧。属契约/行为变更,单独小 PATCH。

---

## §4 红线（byte-equal + 抗诱惑）

- Tier A 只改列明文件;**Shared.jsx / utils.jsx / api.js / Shell.jsx / main.jsx / index.css / App.jsx git diff 0 行**（A6 删 App.css 是删文件非改 ironclad;A4 primitives.jsx 非 ironclad;A5 改 4 screen 非 ironclad;A7 改 Login.jsx screen）。
- 严禁碰 settings_repo.get/set_agent_model_config（命名撞车 LIVE）· 严禁碰 KnotWordmark/get_failure_count（§0 保留)· 严禁把 App.css 折进 index.css（除非 B2 拍板）。
- 不引新依赖 · 不顺手 i18n · 不扩范围到 Tier B/C 未拍板项。

---

## §5 验收标准

1. Tier A 全落 + 全闸门绿:pytest 全套（trim 后的 test 通过）· check_file_sizes · lint-imports 9 kept · ruff · 前端 vitest（删 icons 前 22 passed 不变;A4-A7 不动 foundation → foundation guard 仍绿）。
2. byte-equal:`git diff main HEAD` 不含 7 铁律文件（A6 显示为 delete App.css）。
3. 版本 4 源点 + CHANGELOG 顶 == main。
4. import-linter 9 contract 仍 kept（删 user_repo 方法不破层次）。
5. Tier B 若纳入:R-PB-SH-4 常量同步递减 + baseline re-mirror + foundation guard 重新绿。

---

## §6 commit 序列（Tier A 建议 4 commit;Tier B 若纳入 +1）

1. `chore(be): 删 query_helper.release + user_repo 4 死方法 + 修 bulk_insert 返回值`（A1+A2+A3 + test）
2. `chore(fe): 删 primitives.Tag + 4 幽灵 props + App.css + Login 陈旧 build 字面`（A4-A7）
3. `docs: CLAUDE.md 漂移修正（notification/icon 54/Admin count）`（A8）
4. `chore: bump v0.7.47 + CHANGELOG + rebuild static`

（Tier B1 17 icons 已 LOCKED 单独 foundation PATCH,不在本序列)

---

## §7 协议合规 + scope LOCKED

- Loop Protocol v3 完整三阶段;有业务代码,不适用 EX-1。
- **scope LOCKED（kk 2026-07-05）**:v0.7.47 = **Tier A only**（A1-A8,含 A6 纯删 App.css + A8④ R-165 陈述订正）。Tier B1（17 icons)→ 单独 foundation PATCH;Tier B2（R-165 fold)→ v0.8;Tier C 全推迟。
- Stage 3 守护者终审重点:§0 三处修正是否认可 · A1 governance framing 订正（R-PB-A1-22 非 EX-3)· A2 test trim 不破覆盖契约 · A3 bulk_insert 修法 · byte-equal 7 铁律 0 行。
- 下一步:待 Stage 2 辅助 AI 初审 + Stage 3 守护者终审（可按 v0.7.46 先例由 kk 跑对抗 workflow),终审回来执行者整合落码。

---

## §8 自检

- [x] 8 候选组全 grounded 核实（workflow w00xz6ugi,逐条 import+调用+路径字符串+动态引用)
- [x] 审计 3 处误判 grounded 修正（§0）
- [x] byte-equal/foundation/治理 caveat 逐候选标注
- [x] 命名撞车逐个排除（settings_repo / 语义 catalog / User-Agent / DataSource 冲突记 C2）
- [ ] Stage 2 辅助 AI 初审（待分发）
- [ ] Stage 3 守护者终审（待分发 + Tier B 拍板）
