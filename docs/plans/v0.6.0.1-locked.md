# v0.6.0.1 micro PATCH — LOCKED 手册

> **协议依据**：Loop Protocol v3（v0.5.22+ 资深 ack 自审简化协议 sustained）
> **触发**：资深架构师 announce「v0.6.0.1 micro PATCH 启动」
> **时序**：R-PA-5 Day 0 + 0（2026-05-14 — 提前于 LOCKED 路线图 "Day 7+" 建议窗口；资深 announce 拍板提前）
> **范围**：5 in 1 hotfix + 守护者立约归档
> **执行者**：v0.6 执行者
> **守护者**：v0.5 守护者（PR #74 §9 终审已 explicit 列 G-4/G-5/G-6 范围 → Stage 3 实质已含）

---

## §0 背景

PR #74（v0.6.0 Phase A）merge 后，守护者 §9 终审建议 + 已规划候选合并为 5 in 1 micro PATCH：

1. **原 v0.6.0.1 候选 1**：`SavedReports.jsx:133` ⭐ → inline svg star（v0.5.13 R-303 遗漏的视觉债）
2. **原 v0.6.0.1 候选 2**：`scripts/check_phase_b_leakage.py` R-PA-8 守护工具落地
3. **G-4 立约归档**：R-PA-9 / R-PA-10 / R-PA-11 → LOCKED §3 附录 + CHANGELOG byte-equal
4. **G-5 部署级守护**：`.github/workflows/ci.yml` docker build 自动闸门
5. **G-6 字面单元测试**：`tests/scripts/test_dockerfile_copy.py` R-PA-7 三守护

## §1 In-Scope（5 in 1）

| # | 主题 | 范围 | 守护者依据 |
|---|---|---|---|
| F1 | ⭐ emoji 视觉债偿还 | `frontend/src/screens/SavedReports.jsx:133` ⭐ → `<SvgPath d={SAVED_SVG.star} ...>`；SAVED_SVG 加 `star:` 字面 byte-equal `RB_SVG.star` | v0.6.0.1 路线图候选 1 |
| F2 | R-PA-8 守护工具 | `scripts/check_phase_b_leakage.py` 新建 — 扫 main 分支 Phase B 准备性字面（OOS-1~15 列表 + `docs/plans/v0.6.1-*.md` + `multi-tenant` + `users.project_id`）| v0.6.0.1 路线图候选 2 |
| F3 | R-PA-9/10/11 立约归档 | LOCKED §3 附录新增 R-PA-9 + R-PA-10 + R-PA-11 全文 + CHANGELOG v0.6.0.1 段 | 守护者 §3 G-4 |
| F4 | G-5 CI docker build 闸门 | `.github/workflows/ci.yml` 新增 `docker-build` job + R-PA-8 守护工具运行 | 守护者 §6 G-5 |
| F5 | G-6 R-PA-7 字面单元测试 | `tests/scripts/test_dockerfile_copy.py` 新建 — grep Dockerfile 含 R-PA-7.1/7.2/7.3 字面 | 守护者 §6 G-6 |
| F6 | 版本宣告 | `knot/main.py` / `tests/test_rename_smoke.py` / `frontend/src/screens/Login.jsx` 页脚 0.6.0 → 0.6.0.1 | 三处版本同步 R-181 sustained |

## §2 Out-of-Scope（明确推迟）

- ✘ Phase B 提案启动（`docs/plans/phase-b-proposal-draft.md` 仍 untracked；等 Day 28+ 三方会议）
- ✘ 5 层语义建模任何层（指标/对象/事件/规则/动作）
- ✘ LogicForm 中间层任何雏形
- ✘ v0.6.1+ MINOR PATCH 准备性内容
- ✘ 校验段 / 时间引擎 / 长期记忆 markdown / 写vs跑预演（这些在 phase-b-proposal-draft 中作为 v0.6.1~v0.6.4 候选规划）

## §3 红线 R-PA-12 ~ R-PA-17

### R-PA-12（micro PATCH 范围红线）

v0.6.0.1 严格 5 in 1 — 严禁本 PATCH 超范围 commit（如顺手改其他屏 / 顺手 refactor / 顺手加新功能）。

### R-PA-13（R-PA-9/10/11 字面 byte-equal 立约）

LOCKED §3 附录立约文本与 commit 8 / commit 9 commit message 中字面化的 R-PA-9/10/11 描述保持 byte-equal — 守护者已字面化的立约文本是权威，本 PATCH 仅做"归档动作"不修订。

### R-PA-14（R-PA-8 守护工具自指环避免）

`scripts/check_phase_b_leakage.py` 自身脚本含 Phase B 字面列表 — 必须用字面分割构造（如 `"multi" + "-tenant"`）避免被自己扫描命中（同 audit_ohx_leakage.py R-PA-1 模式）。

### R-PA-15（G-5 docker build CI 非阻塞性）

CI docker-build job 仅作 smoke 验证 — 不阻塞 PR merge（避免 docker hub rate limit 等环境噪声误杀 PR）。`continue-on-error: true` 或 `if: always()` — 待 Stage 4 决议。

### R-PA-16（G-6 R-PA-7 字面 grep 守护精度）

`test_dockerfile_copy.py` 用 regex grep — 不解析 Dockerfile AST（轻量级 + 跨 docker 版本稳定）：
- 必含 `COPY knot/prompts/ /app/knot/prompts/`
- 必含 `COPY knot/services/agents/_template_catalog.py /app/knot/services/agents/_template_catalog.py`
- 必含 `COPY --from=frontend-builder /knot/static ./knot/static`（commit 9 修复后字面）

### R-PA-17（版本同步三处守护）

`knot/main.py` + `tests/test_rename_smoke.py` + `frontend/src/screens/Login.jsx` 页脚 — 全部 0.6.0 → 0.6.0.1（R-181 sustained）。tests/test_login_version_sync.py 自动断言三处一致。

## §4 验收闸门

| # | 闸门 | 期望 |
|---|---|---|
| 1 | pytest 全套 | 418 → 420+ passed（含 G-6 + 可能扩展守护测试）/ 112 skipped |
| 2 | import-linter 7 contracts | KEPT × 7 / 0 broken |
| 3 | ruff check knot/ | clean |
| 4 | npm run build | 0 errors |
| 5 | check_file_sizes | 0 violations（新增 LIMITS：scripts/check_phase_b_leakage.py 100 / tests/scripts/test_dockerfile_copy.py 80）|
| 6 | audit_ohx_leakage --mode=all | exit 0 sustained |
| 7 | **R-PA-8 check_phase_b_leakage** | exit 0（main 分支当前无 Phase B 准备性字面）|
| 8 | bi_agent / BIAGENT_MASTER_KEY 字面 0 命中 | sustained |
| 9 | docker build CI smoke | exit 0（G-5 新增）|
| 10 | tests/scripts/test_dockerfile_copy.py | 3 测试 PASS（G-6 新增）|
| 11 | 闸门 9 端到端 SavedReports 空态 visual 验证 | ⭐ 已偿还 → svg star + "在对话里**收藏**任意结果..." 文案改 |
| 12 | 三处版本同步 grep | 0.6.0.1 字面 byte-equal × 3 处 |
| 13 | GitHub Actions CI 全绿 | 6+ checks PASS（含新 docker-build job）|

## §5 Commit 序列

```
[commit 0]  docs(v0.6.0.1): LOCKED 手册归档（本手册）
   │
commit 1   feat(v0.6.0.1): R-PA-8 守护工具落地（F2）
   │       · scripts/check_phase_b_leakage.py [NEW]
   │       · Phase B 字面检测（OOS + docs/plans/v0.6.1-* + multi-tenant 等）
   │       · R-PA-14 自指环避免（字面分割构造）
   │
commit 2   fix(v0.6.0.1): SavedReports ⭐ → inline svg star（F1）
   │       · frontend/src/screens/SavedReports.jsx
   │       · SAVED_SVG +star 字面 byte-equal RB_SVG.star
   │       · "在对话里点 ⭐ 把任意结果钉成报表" → "在对话里收藏任意结果..."
   │
commit 3   ci+test(v0.6.0.1): G-5 docker build CI + G-6 字面单元测试（F4+F5）
   │       · .github/workflows/ci.yml +docker-build job + R-PA-8 检查
   │       · tests/scripts/test_dockerfile_copy.py [NEW] 3 测试
   │       · R-PA-16 字面 grep 守护精度
   │
commit 4   docs(v0.6.0.1): R-PA-9/10/11 立约归档（F3）
   │       · docs/plans/v0.6.0-phase-a-sanitize.md §3 附录加 R-PA-9/10/11 全文
   │       · CHANGELOG.md v0.6.0.1 段
   │       · CLAUDE.md v0.6.x 路线图 update（v0.6.0.1 已完成 sustained）
   │       · scripts/check_file_sizes.py LIMITS dict +2 新文件
   │
commit 5   chore(v0.6.0.1): 版本宣告 0.6.0 → 0.6.0.1（F6）
           · knot/main.py FastAPI version
           · tests/test_rename_smoke.py 守护字面
           · frontend/src/screens/Login.jsx 页脚字面
           · CHANGELOG release 段标 [v0.6.0.1] 替代 [Unreleased]
```

## §6 协议合规性

### 6.1 自审简化协议适用性

按 v0.5.22 起授权"自审简化协议"（资深 ack）— 本 PATCH 适用条件：
1. ✅ 5 in 1 全部内容**已在 PR #74 守护者 §3 G-4 + §6 G-5/G-6 中 explicit 列范围**
2. ✅ R-PA-9/10/11 立约**已字面化** in commit 8/9 测试代码 + commit message
3. ✅ 守护者 §9 终审"批准 PR merge + 等 commit 10 LOCKED §3 附录补丁"= 实质包含 Stage 3 评审
4. ✅ 资深架构师 announce「v0.6.0.1 micro PATCH 启动」= explicit 拍板触发

→ Stage 2/3 跳过 / Stage 4 直接落地（Stage 1 草案 = 本手册作为执行者归档）

### 6.2 不适用全 v3 三阶段评审的理由

- 5 in 1 全部是守护者已 ack 的内容（不是新提案）
- R-PA-12 红线严守 micro PATCH 范围（不引入争议）
- 内测期严禁 Phase B 准备性 commit — 本 PATCH 是 v0.6.0 Phase A 收尾 hotfix，**非** Phase B 准备

### 6.3 守护者轻量召集（推荐）

PR 开后建议召集 v0.5 守护者轻量复核（5-10 分钟）：
- 验证 5 in 1 范围严守
- 验证 R-PA-9/10/11 立约字面 byte-equal
- 验证 R-PA-12 ~ R-PA-17 红线无破坏

不需要全 v3 三阶段（守护者 PR #74 §9 已含 G-4/5/6 预约）。

## §7 自检清单

合入前执行者自检：

- [ ] 5 in 1 全部 commit 已按 §5 顺序落地
- [ ] §3 红线 R-PA-12~R-PA-17 全过
- [ ] §4 验收闸门 1-13 全过
- [ ] CI GitHub Actions 全绿（含新 docker-build job）
- [ ] R-PA-8 工具自身字面分割 OK（自不命中）
- [ ] 三处版本同步 grep 0.6.0.1 byte-equal × 3
- [ ] PR 描述含 §6 简化协议依据声明 + 守护者 §3 G-4 / §6 G-5/G-6 引用
- [ ] phase-b-proposal-draft.md untracked 状态 sustained（不污染 PATCH）

## §8 状态

| 字段 | 值 |
|---|---|
| 起草日期 | 2026-05-14 |
| 起草人 | v0.6 执行者 |
| 协议 | Loop Protocol v3 + v0.5.22 自审简化协议 sustained |
| 触发 | 资深架构师 announce「v0.6.0.1 micro PATCH 启动」 |
| 归档位置 | `docs/plans/v0.6.0.1-locked.md`（commit 0 进 PR）|
| 守护者参与 | PR #74 §9 G-4/5/6 预约已含；PR open 后建议轻量复核 |
| 完成判定 | §4 闸门 1-13 全过 + 资深架构师拍板 merge |
