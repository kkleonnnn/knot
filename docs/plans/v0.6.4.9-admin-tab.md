# v0.6.4.9 — UI v2 admin tab 群批（radius 归一化）（admin 屏群批 2/2）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + VRP grep-guard + **修正版本同步模型（4 无条件 ★CI 含 Login footer）**
> **基座**：worktree `v0649-admintab` @ branch `v0.6.4.9-admin-tab`（off main `ea7d723` = v0.6.4.8）
> **源稿**：demo datasources/api-model/knowledge/fewshot/prompts/catalog/system.jsx
> **目标**：admin/tab_access/tab_resources/tab_knowledge/tab_system/modals

## §0 范围 + adversarial 评估（合批 2 PATCH 之批 2；结论：**1 delta + 4 文件 noop**）

预判「delta 主场」被全枚举 + demo 交叉核对**推翻** —— admin tab 群也近 noop（同顶层屏批）。原因：① tab 复刻 v0.5.16-22 时 content 卡已用 12；② stat 卡（10）**正确 match demo stat-10**（命门，非 bug）。

**命门解（demo 实锤）**：demo datasources L59=10（stat）+ L70=12（table）；demo knowledge L66=10（stat）+ L98=12（table）→ 产品 tab_access/tab_knowledge 的 stat 卡（10）按 demo 正确，**抵抗盲归一化 stat→12**。

## §1 radius 全站点枚举（demo 交叉核对）

| 文件 | radius 站点 | 动作 |
|---|---|---|
| **tab_access** | L30 **Users table 10** / L65-78 4 stat 10 / L83 Sources table 12 / icon 6 / chip 999 / dot 50% | **⭐ L30 10→12**（唯一 delta：Users table 与同屏 Sources table 12 不一致 → 归一 table-card 12）；stat 10 stay（demo datasources L59）；其余 stay |
| **tab_resources** | L13/35/77 cards 12 / select 7 / chip 3 | **noop**（cards 12 match demo api-model；select 7 control stay）|
| **tab_knowledge** | L51-63 4 stat 10 / L84 icon 10 / 多 content cards 12 / chips 4 / L293 letterchip 5 / inset 6 | **noop**（stat 10 stay demo knowledge L66；content 12 already；icon/chip stay）|
| **tab_system** | L44/86 cards 12 / L29 inset note 8 / L59 catalog 行 8 / select 7 | **noop**（cards 12 match demo catalog/system；inset note 8 stay brandSoft；selector 行 8 stay；select 7 control stay）|
| **modals** | L70 option 7 / L75 checkbox 3 / L218 test banner 6 / L269 input 7 | **noop**（全 modal 控件 7/6/3，非 card）|

**改动（1 站点）**：tab_access.jsx L30 Users table card `borderRadius: 10 → 12`（`T.card + border + overflow hidden` table 容器；match 同屏 Sources table L83=12 + 全站 table-card 标准）。

**stays（枚举 + 理由）**：
- **stat 卡 10**（tab_access L65-78 + tab_knowledge L51-63）：demo datasources L59 / knowledge L66 实锤 stat=10（密集 summary 卡），按自身 demo 正确
- **content/table 卡 12**（tab_resources/tab_knowledge/tab_system 多处）：已 v2
- **inset 类 8**：tab_system L29 业务目录 brandSoft inset note（R-372 8% + border）+ L59 catalog 选择行（brandSoft active 8）— inset/list-row 非 data card
- **控件杂散 radius**：select/input/option 7（tab_resources L47 / tab_system L78/115 / modals L70/269）· checkbox 3 · letterchip 5 · icon 6/8/10 · chip 4/999 — 非 card，控件/chip 类 stay（demo 控件 radii 无统一 v2 标准，非本 card-radius 批范围）

## §2 保 byte-equal（不动守护）

- **stat 卡 radius 10 不动**（demo 实锤 stat=10，抵抗盲归一化 stat→12）
- **thead T.bg 灰底铁律 v0.5.38 不动**（tab_access/knowledge/system table thead；严禁照搬 demo bgInset；守护者补强）
- **brandSoft R-372 inset 不动**（tab_system L29 note 8% + tab_access stat/icon brandSoft 8%）
- 全业务逻辑 + 各 tab props 签名 byte-equal（Admin.jsx tab dispatch 0 diff）
- hex 0 sustained + 控件 radius 不动（本批仅 card-radius 归一）

## §3 红线

| # | 红线 |
|---|---|
| R-UI2-AB-1 | 5 tab 文件 export + props 签名 byte-equal → Admin.jsx 0 diff |
| R-UI2-AB-2 | diff = **仅 tab_access L30 1 行 radius**（10→12）；4 文件 0 改（noop verified）|
| R-UI2-AB-3 | stat 卡 10 不动（demo 实锤；抵抗盲归一化）；inset/控件/chip 类不动 |
| R-UI2-AB-4 | thead T.bg + brandSoft R-372 inset 字面不动 |
| R-UI2-AB-5 | hex 0（grep 排 rgba = 0 sustained）|
| R-UI2-AB-6 | 全业务逻辑 byte-equal（各 tab handlers/state/api）|
| R-UI2-AB-7 | 各文件 ≤ LIMIT（radius 改 0 净行）；其他屏/App/Admin/Foundation 0 改 |
| R-UI2-AB-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-AB-9 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.9；Shell L43 不动（条件式，stays v0.6.4.2）|

## §4 验收
1. tab_access Users table radius 12（与 Sources table 一致）；其余 4 tab 视觉不变（已 v2 / stat-10 正确）
2. `git diff origin/main -- frontend/src/screens/admin/` = 仅 tab_access；Admin.jsx 0 diff
3. hex 0 + npm build exit 0 + check_file_sizes OK + R-PA-8 0
4. **4 处版本 → 0.6.4.9**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（admin 4 tab：tab_access Users/Sources table 一致 12 + stat 卡 10 + 其余不变）

## §5 commit 序列
- **commit 1**：tab_access L30 radius 10→12 + 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §6 协议合规
轻量 v3：执行者 Stage 1（adversarial 全枚举 + demo 交叉核对 = 1 delta + 4 noop，stat-10 命门守护 + 完整枚举防漏改）→ 守护者 Stage 3（adversarial 复核 noop 真实性 + demo 交叉核对 + stat 分类 + 完整性）→ 逐 commit 直读 → 资深 push/merge + :8000 live。合批 2 PATCH 之批 2（admin tab 群）；admin 屏群批收官。
