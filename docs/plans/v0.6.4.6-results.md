# v0.6.4.6 — UI v2 chat-results（ResultBlock card radius 对齐）屏复刻（逐屏复刻第 6 屏 · chat flow 收官）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + VRP grep-guard + **修正版本同步模型（4 无条件 ★CI 含 Login footer）**
> **基座**：worktree `v0646-results` @ branch `v0.6.4.6-results`（off main `0eb25bb` = v0.6.4.5）
> **源稿**：`knot_demo_ui/v0.6/artboards/chat-results.jsx`（309 行 gallery — 7 intent + states）
> **目标**：`ResultBlock.jsx`（285，LIMIT 295）+ 7 子组件（MetricCard/TableContainer/InsightCard/BudgetBanner/ErrorBanner/TokenMeter/FeedbackBar）

## §0 范围 + adversarial delta 评估（拆分结论：**1 PATCH**）

**adversarial finding**：ResultBlock 已大部分 v2 —— v0.5.13/14 hex 全清 + brandSoft inset（R-372）+ svg icons；v0.6.0.2 拆 6 子组件架构就位。真实 v2 delta = **系统性 card radius 对齐**（主题单一、低风险、~8 行跨 4-5 文件）→ **不拆多 PATCH**。

「ResultBlock 复杂」是**逻辑复杂**（7 intent + 业务）；**视觉 delta 已被 v0.5.x 做完**，仅剩 card radius chrome 对齐。同 Composer/thinking「假设大改、实则已 v2」。

## §1 视觉变化 — ⭐ radius 全站点枚举映射（守护者 §二 补全；分类规则：primary 数据卡 12 / alert banner 10 / 次级 utility+inset 10 / chip-pill 保 / control 保）

**改动（6 站点）**：

| 元素 | 现 | → | 类 |
|---|---|---|---|
| MetricCard `_SingleMetric` L30 | 10 | **12** | primary 数据卡 |
| MetricCard `_MultiStatGrid` L52 | 10 | **12** | primary 数据卡 |
| TableContainer chart card L22 | 10 | **12** | primary 数据卡 |
| TableContainer table card L42 | 10 | **12** | primary 数据卡 |
| BudgetBanner 容器 L12 | 8 | **10** | alert banner |
| ErrorBanner 容器 L32 | 8 | **10** | alert banner |

**stays（枚举 + 理由 — 守护者要求显式）**：

| 元素 | radius | stays 理由 |
|---|---|---|
| ResultBlock clarification L58 | 10 | alert/state 类，已匹配 demo state 10 |
| ResultBlock error-no-sql L74 | 10 | alert/state 类，已匹配 |
| ResultBlock inline error L170 | 6 | 极简 inline 错误 pill，非卡片 |
| ResultBlock followup chip L194 | 14 | **chip-pill（h28），v0.5.14 established，非 card（守护者疑「异常」实为 chip）；demo suggestion chip 同 pill-radius** |
| ResultBlock SQL accordion L208 | 10 | 次级 utility — admin-only 折叠代码查看器（非 result 数据卡）；demo 无 equiv → VRP local stays |
| ResultBlock agent_costs chip L248 | 999 | chip-pill |
| TableContainer chart toggle btn L28 | 5 | control button（非卡片）|
| InsightCard L13 | 10 | brandSoft inset 软卡（R-372 字面闭环），次级 callout，radius 保 |
| **FeedbackBar** | 无 card | **无 card 容器 = inline flex 行（text + 2 thumb iconBtn）**；radius 8/6/6 全在 Modal 评论框/按钮（modal context 非反馈条）；demo card-wrap = 结构改超 radius scope → 不动 |

**分类一致性保证**（守护者核心关切）：4 个 primary 数据卡统一 12（无 metric=12/table=10 混杂）；alert banner 统一 10；次级/chip/control 按 element-type 保各自 radius（hierarchy：primary 12 / secondary 10 / pill 14·999）。**⚠️ 2 处 borderline 标注待守护者 commit-1 复核挑战**：SQL accordion（stays 10 vs →12）+ FeedbackBar（stays inline vs →card 12）。

## §2 保 byte-equal（adversarial 确认不动）

- **charts ECharts 0 改**：`LineChart/BarChart/PieChart` from Shared（demo inline SVG 仅 artboard mock；Foundation R-PB-SH）
- **detail_table thead `T.bg` 灰底保留**（v0.5.38 资深「底色改成灰色」established；**非** demo bgInset / **非** brandSoft；R-227.5.1 mono uppercase 装饰豁免 sustained）
- **metric delta% 不引入**（demo `+12.4%` ok/err 是电商 fiction；产品 metric 驱动真实 SQL 无 delta 字段）
- **全业务逻辑 byte-equal**：7 intent 分发（resolveEffectiveHint/autoType/showChart）+ ResultBlock 8 props 签名（`{T,msg,user,onCopy,onDownload,onFollowup,onPin,onRetry,onFeedback}`）+ 子组件 props + exportMessageCsv + handlePin/handleBudgetDismiss + sessionStorage 降噪
- hex 0 sustained（v0.5.13/14 全 color-mix/T tokens）+ brandSoft inset R-372/R-323 字面不动 + R-302.5 banner emoji 业务豁免

## §3 红线

| # | 红线 |
|---|---|
| R-UI2-R-1 | ResultBlock 8 props 签名 byte-equal + 7 子组件 export+props byte-equal → Conversation.jsx + SavedReports（若 import）0 diff |
| R-UI2-R-2 | charts ECharts 0 改（LineChart/BarChart/PieChart from Shared）；Foundation 0 改 |
| R-UI2-R-3 | detail_table thead T.bg 灰底 byte-equal（v0.5.38 sustained）|
| R-UI2-R-4 | 全业务逻辑 byte-equal（7 intent 分发 + handlers + exportMessageCsv + sessionStorage）|
| R-UI2-R-5 | hex 0（grep 排 rgba = 0 sustained）+ brandSoft inset R-372 字面不动 |
| R-UI2-R-6 | diff 仅 card radius（result →12 / banner →10）；无结构 / 无业务 / 无 chart 改 |
| R-UI2-R-7 | 各文件 ≤ LIMIT（radius 改 0 净行）；其他屏/App/Foundation 0 改 |
| R-UI2-R-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-R-9 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.6；Shell L43 不动（条件式，stays v0.6.4.2）|

## §4 验收
1. 结果卡片 radius 12（metric/chart/table）+ banner radius 10（budget/error）；charts/thead/business 不变
2. `git diff origin/main -- frontend/src/screens/chat/Conversation.jsx frontend/src/Shared.jsx` = 0
3. hex 0 + npm build exit 0 + check_file_sizes OK + R-PA-8 0
4. **4 处版本 → 0.6.4.6**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（7 intent 端到端：metric/line/bar/pie/rank/retention/detail + clarification/error/budget/feedback states + radius 视觉）

## §5 commit 序列
- **commit 1**：ResultBlock + 子组件 card radius 对齐（result →12 / banner →10）+ 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §6 协议合规
轻量 v3：执行者 Stage 1（adversarial 评估 = 1 PATCH radius 对齐，charts/thead/业务 byte-equal）→ 守护者 Stage 3（adversarial 复核 delta 完整性 + 契约 byte-equal + charts/thead 0 改 + 4 处版本）→ 逐 commit 直读 → 资深 push/merge + :8000 live。chat flow 6 屏收官（login→shell→home→composer→thinking→results）。

## §9 LOCKED 归档（守护者 Stage 3 + commit 1 ACK + 2 borderline 裁定）

**守护者 commit 1 ACK（`975c9c7`）= 干净独立守护**（执行者写、守护者验）：6 处 radius 精确（4 数据卡→12 + 2 banner→10）· diff 仅 radius 0 结构/业务/chart 改 · 契约 5 文件 0 diff · thead T.bg + charts 守住 · 4 无条件版本（含 Login footer）· Shell L43 stays v0.6.4.2。

**⭐ radius 映射完整性 adversarial 链（headline）**：
- 执行者 Stage 1 列 5 子组件 radius delta。
- 守护者 adversarial Stage 3 grep 全部 borderRadius 抓出 **映射不完整** —— 漏 FeedbackBar（第 7 子组件，红线却写「7 子组件」自相矛盾）+ orchestrator 4 内联卡（L58/74/194/208）。
- 执行者补全**全站点枚举映射**（6 改 + 9 类 stays 逐个理由）+ 主动标注 2 borderline。
- 守护者裁定：① **SQL accordion stays 10**（utility/debug 层，从属 12 数据卡合理）② **FeedbackBar stays inline**（无 card 容器，加 wrap = 结构改 OOS）③ **L194 = chip-pill 非异常**（v0.5.14 followup pill h28 radius 14）。
- **adversarial Stage 3 第三次价值兑现**：Composer noop→hex / thinking placement / results radius 完整性。

**radius 最终映射（一致性闭环）**：数据卡 12 / alert banner 10 / chip-pill 14·999 / brandSoft inset 10（R-372）/ utility accordion 10 / control 5 / FeedbackBar inline。无两数据卡 radius 混杂。

**3 大不动守护（adversarial 确认）**：① charts ECharts from Shared 0 改（demo inline SVG 仅 mock）② detail_table thead T.bg 灰底（v0.5.38 established）③ metric delta% 不引入（demo 电商 fiction）。

**⭐ chat flow 6 屏 v2 收官**：login→shell→home→composer→thinking→results 整条「问→答」核心链路全 v2。
