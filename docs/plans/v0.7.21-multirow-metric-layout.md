# v0.7.21 — 多行结果误渲染 MetricCard 修复（图文不一致）（E · 前端 bugfix）

> **触发**：价值自测 problem 3 / E 具体实例。kk 重测「当前平台 BTC 多头持仓」（HTTP 返 5 个持仓）→
> presenter 文字分析全 5 行（重点 row[1] 持仓ID 46284/用户ID 502 大仓），但数据卡片只显 row[0]（持仓ID 83128/
> 用户ID 1001544）→ **图文不一致**。
> **协议**：前端 1 行 bugfix（前端零测试栈 → 无单测，靠 grounded 逻辑 + manual 重测）。

## 1. 根因（grounded · msg 146）

- msg 146：q=「当前平台 BTC 多头持仓」，**intent='metric'**（clarifier 把「持仓汇总」判成单值 metric），**rows=5 行**。
- 前端 `ResultBlock.jsx:88` `isMetric = layoutHint === 'metric_card'` → True → `<MetricCard rows={5行}/>`。
- `MetricCard`（MetricCard.jsx）：`numericCols>1 → _MultiStatGrid` / else `_SingleMetric`；两者都用 **`rows[0]`** → 渲染 row[0]（持仓ID 83128）。
- presenter 文字分析全 5 行 → 重点 row[1]（46284/502）。→ 卡片(row[0]) ≠ 文字(全行) = **图文不一致**。
- **本质**：多行结果（5 持仓 = 明细列表）被当**单值 metric** 渲染（应 detail_table）。

## 2. 范围

**In-scope**：`ResultBlock.jsx` `isMetric` 加 `&& rows.length === 1` —— metric_card 仅用于单行结果；多行（intent 误判 metric）落 TableContainer（detail_table，显全行）→ 图文一致。

**Out-of-scope**：
- clarifier intent 分类（「列出持仓」应判 detail_table）= 内容/prompt 侧，前端 gate 已 robust 兜底（任何误判 multi-row metric 都落表），不依赖 clarifier 修对。
- 「用户问『汇总』但 API 返明细」（汇总 vs 明细，summary aggregation）= 更深内容层，presenter 文字已做汇总分析，本刀只修图文一致。
- problem 2（合约盈亏 by 交易对 → metric seed）+ b（merge file-http 权威）= 各自独立。

## 3. 红线 R-SL-153~（草案 · 沿用 v0.7.x 编号）

- **R-E-1（核心 · 归一化一处修两症状）**：`if (layoutHint === 'metric_card' && rows && rows.length > 1) layoutHint = 'detail_table'`（归一后 `isMetric=False` + `isDetail=True`）。修两症状：① MetricCard 只渲染 rows[0]（图文不一致）② `showChart = chartable && !isMetric && !isDetail` 经 isDetail=True → **False** → 不把 ID/类别数值列（持仓ID/用户ID/方向）当 metric 画无意义折线图。归一后 isMetric/isDetail/showChart 既有逻辑自然流。
- **R-E-2（单行复合 metric byte-equal）**：单行多值复合 metric（如「今日充值和提现」1 行 2 numericCols → 仍 metric_card → MetricCard → _MultiStatGrid，v0.6.2.2）**不受影响**（rows.length===1 → 不触发归一）。`line/bar/pie/rank_view` intent 不经此归一 → 合法趋势图正常出。
- **R-E-3（单值 metric 不变）**：单值 metric（1 行 1 值）→ rows.length===1 → MetricCard。
- **R-E-4（0 行不变）**：rows.length===0 由既有 `rows.length > 0 && isMetric` 守护（0 行两分支都不进，空态另处理）。
- **R-E-5（5 源点）**：0.7.20→0.7.21（main.py/version.js/README/CHANGELOG count==1/smoke）+ static 重建（前端改）。
- **R-E-6（范围）**：仅 ResultBlock.jsx 一处逻辑 + 注释；0 后端 / 0 schema / 0 其它前端组件。

## 4. 验收

- ✅ 「当前平台 BTC 多头持仓」（5 行）→ detail_table 显全 5 行 → 与 presenter 5 行叙事一致（无图文不一致）。
- ✅ 单值 metric（「今天总充值」1 行）→ MetricCard（不变）。
- ✅ 复合 metric（「今日充值和提现」1 行 2 值）→ _MultiStatGrid（v0.6.2.2 不破）。
- ✅ ruff（无后端改）+ check_file_sizes + 5 源点 0.7.21 + static 重建 + CI 绿。
- 前端零测试栈 → 无单测；manual 重测（kk）+ grounded 逻辑 4 case 推演。

## 5. commit 序列

- **C1**：ResultBlock.jsx isMetric 多行 gate（1 行 + 注释）+ static 重建 + 5 源点 0.7.21 + CHANGELOG + plan。

## 6. 协议合规 / 自检

- 前端 bugfix（零测试栈）→ 简化（资深 ack 起 E）；逻辑 4 case grounded 推演替代单测。
- 自检：gate 严格 `rows.length === 1`（非 `<=1`，0 行已被 `rows.length > 0` 守）；只收窄 metric 到单行，不动 detail/chart/retention 分支。
