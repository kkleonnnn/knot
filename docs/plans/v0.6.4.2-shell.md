# v0.6.4.2 — UI v2 shell 屏复刻（逐屏复刻第 2 屏）

> **协议**：轻量 v3（跳 Stage 2 — 已实证 3 PATCH 无地基；保守护者 Stage 3 + 逐 commit 直读复核）
> **铁律首次强制套用**：R-UI2-VRP（artboard 把握整体方向，本地把握细节；写死尺寸/坐标/百分比严禁照搬）
> **基座**：worktree `v0642-shell` @ branch `v0.6.4.2-shell`（off main `46ac76b`）
> **源稿**：`knot_demo_ui/v0.6/artboards/shell.jsx`（198 行 artboard）
> **目标**：`frontend/src/Shell.jsx`（211 行，LIMIT 220）

## §0 范围（窄 — 纯视觉复刻，0 业务 / 0 契约改动）

仅 `frontend/src/Shell.jsx` 一文件视觉重构 + version 四处同步（含本 PATCH 新立第 4 点 Shell L43）。**R-192 AppShell 13 props 宪法级签名 byte-equal**（9 屏 + Shell 消费方 0 diff）；SideHeading/SideNavRow 签名 byte-equal（Chat.jsx 消费方 0 diff）。

## §1 视觉变化（artboard 方向 → 本地 VRP 锚定）

### ① ⭐ floating inset 双面板（headline · R-UI2-VRP 首次实战）
- **artboard 写死值**：外层 `width:1280 height:800`（artboard 尺寸）→ **本地 `100vw/100vh` fluid**（VRP 铁律；严禁照搬 1280×800）
- 外层容器加 `gap:10, padding:10`（固定元素间距，非 viewport% → VRP-safe 保留），bg 保 `T.bg`（10px 留白框）
- sidebar：删 `borderRight` → 全 `border:1px solid T.border` + `borderRadius:14` + `overflow:hidden` + `boxShadow`（light: `0 1px 3px rgba(15,30,45,0.04)` / dark: `none`；R-313 rgba 豁免 — boxShadow）
- main：加 `border:1px solid T.border` + `borderRadius:14` + `overflow:hidden` + `boxShadow`（同上）；bg 保 `T.content`

### ② logoArea + TopBar 高度对齐（52→56）
- logoArea `height 52→56`；KnotLogo `size 20→16`；version letterSpacing `0.02em→0.06em`（demo byte-equal）
- TopBar header `height 52→56`、`padding '0 22px'→'0 24px'`

### ③ NavItem（SideNavRow 内部视觉）
- 显式 `height:34`、`borderRadius 8→6`、`gap 9→10`
- active bg **保本地 `color-mix(in oklch, ${T.accent} 12%, transparent)`**（v0.5.26 资深 ack「bg 填充足够辨识」；demo `brandSoft` 概念等价 → 本地既有标准胜出 = R-UI2-VRP「细节本地把握」）

### ④ userArea 2 行化（demo 方向）
- 删行内 ADMIN badge → 2 行布局：line1 `name`（fontSize13 fontWeight500 lineHeight1.2）+ line2 `role` mono uppercase（`user.role` 实值 ADMIN/USER；fontSize10 T.muted letterSpacing0.04em）
- avatar 圆保 28px T.accent；**hex 偿还 `color:'#fff'` → `T.sendFg`**（R-484/R-286 sustained）

### ⑤ Foundation icon 偿还（v0.6.4.0 已加 → 清 4 inline icon + 1 收敛）
- 删 `FlaskIcon/FolderIcon/WalletIcon/ClipboardCheckIcon` 4 个 Q2 VRP 局部例外 inline svg（v0.5.26 立时 Shared 缺；v0.6.4.0 已补 → 偿还触发）
  - Few-shot `FlaskIcon` → `I.flask` · 业务目录 `FolderIcon` → `I.catalog` · 预算 `WalletIcon` → `I.budget` · 审计 `ClipboardCheckIcon` → `I.audit`
- 内测指标 `I.zap` → `I.spark`（解 zap/zap 与 API 撞名；demo 命名 `spark` byte-equal）
- 其余 icon（db/users/zap[API]/book/pencil/shield/x/search）保不变

## §2 红线

| # | 红线 |
|---|---|
| R-UI2-S-1 | **R-192 AppShell 13 props 签名 byte-equal**（`{ T, user, active, sidebarContent, topbarTitle, topbarTrailing, showConnectionPill, connectionOk, connectedCount, onToggleTheme, onNavigate, onLogout, children }` 一字不动）→ 9 屏 + Shell 消费方 git diff 0 行 |
| R-UI2-S-2 | SideHeading `{T, children}` + SideNavRow `{T, icon, label, active, onClick}` 签名 byte-equal → Chat.jsx 消费方 0 diff |
| R-UI2-S-3 | **R-UI2-VRP 铁律**：0 artboard 写死 viewport 值（无 `1280`/`800`/viewport%）；`100vw/100vh` fluid；`gap/padding/224/14` 固定元素值允许 |
| R-UI2-S-4 | 业务链 0 改：`onNavigate`/`onLogout`/`onToggleTheme` 调用 + `'admin-'` 前缀分流 + `isAdmin` 判定 + initials 计算 byte-equal（仅 initials 现保留，userArea 渲染调整不动计算逻辑）|
| R-UI2-S-5 | admin nav 13 项顺序 + active key（admin-sources…admin-history）byte-equal；仅 icon 组件替换（4 inline → Foundation I + zap→spark）|
| R-UI2-S-6 | hex 0 命中（grep `#[0-9a-fA-F]{3,6}` Shell.jsx 排除 boxShadow rgba = 0；含 L121 `#fff` 偿还）|
| R-UI2-S-7 | 其他屏 0 改：`git diff origin/main -- frontend/src/screens/ frontend/src/Shared.jsx frontend/src/primitives.jsx frontend/src/utils.jsx` = 0；App.jsx/api.js/index.css/main.jsx 0 改 |
| R-UI2-S-8 | KnotLogo 仅 Shared+Login+Shell 三文件命中（R-199.5 sustained）|
| R-UI2-S-9 | Shell.jsx ≤ 220 LIMIT（预估 211→~213；净增 floating+userArea ≈ +8 / 净减 4 inline icon ≈ -6）；超则资深 ack 微调 |
| R-UI2-S-10 | R-PA-8 Phase B 字面 0 命中 |

## §3 验收
1. Shell.jsx 视觉：双 floating 面板（radius14 + border + light boxShadow）· logoArea/TopBar 56 高 · NavItem 34 高 radius6 · userArea 2 行
2. `git diff origin/main -- frontend/src/App.jsx frontend/src/screens/ frontend/src/Shared.jsx frontend/src/primitives.jsx` = 0 行
3. npm build 绿 + check_file_sizes OK + hex 0 + R-PA-8 0
4. 宽 viewport 不偏移（VRP fluid；资深 :8000 post-merge viewing 终判）
5. 433 tests 不变（前端纯视觉，0 后端）；CI 全绿（R-72 0.6.4.2 + docker build + frontend-lint + lint-test）

## §4 commit 序列（轻量 v3 · 1 commit 主体 + 收官静态）
- **commit 1**：Shell.jsx 视觉重构（floating inset + logoArea/TopBar 56 + NavItem + userArea 2 行 + 4 inline icon → Foundation I + hex 偿还）+ **version 四处同步** 0.6.4.1→0.6.4.2（main.py + test_rename_smoke + Login 页脚 R-181 carve-out 1 行 + **Shell L43** 新增第 4 同步点）+ CLAUDE.md VRP §四处同步立约 + check_file_sizes LIMIT（若需）+ 本 plan
- **commit 2（收官）**：npm build 静态重建 + CHANGELOG

## §4.1 ⭐ 资深拍板（Stage 3 §六 两项 LOCKED）
1. **轻量 v3 继续**（资深显式 ack；跳 Stage 2 — Stage 2 本系列历来低质 + 守护者严格 Stage 3 已做 + 强制资深 :8000 post-merge live-viewing 兜底；R-192 半径已认知）
2. **修 L43 + 立约**（守护者挖到 Shell L43 stale v0.6.1.4 drift 3 MINOR；「四处同步」纪律静默断了）→ L43→v0.6.4.2 + CLAUDE.md VRP 段「三处同步」升「四处同步」补 Shell L43 守护

## §4.2 守护者 Stage 3 放行条件（commit 1 闸门 — 全部已满足）
- ✅ R-192 13 props + SideHeading/SideNavRow 签名 byte-equal（仅内部视觉变）
- ✅ VRP grep-guard：final Shell.jsx grep `1280|800|at%|width:'NN%'` = 0（floating chrome 是固定值非 viewport%）
- ✅ icon 逐项对 artboard ADMIN_NAV（flask/catalog/budget/audit/spark；demo ui.jsx path byte-equal Foundation I.*）
- ✅ hex 0（L121 `#fff`→T.sendFg）
- ✅ 四处版本同步（main.py + smoke + Login footer + Shell L43）
- ✅ check_file_sizes ≤220 + R-PA-8 0 + R-185 KnotLogo 哨兵 sustained

## §5 协议合规
- 轻量 v3：执行者 Stage 1（本草案）→ 守护者 Stage 3 终审（VRP 合规 + R-192 byte-equal + Foundation 偿还合理性）→ 逐 commit 直读复核 → 资深 push/merge
- 守护者 v0.6.4.1.1 承诺：显式核 VRP 合规（grep 写死坐标/百分比/尺寸）→ 本屏首次受检

## §9 LOCKED 归档（守护者 Stage 3 + commit 1 ACK + 3 stale 发现）

**守护者 commit 1 ACK（`6dd2fae`）**：R-192 13 props + SideHeading/SideNavRow byte-equal（App.jsx + 9 消费屏 + Chat.jsx + Shared/primitives/utils 0 diff）✓ · VRP grep-guard 0 真违规（守护者承诺的 VRP 闸门首次制度化）✓ · 四处版本同步修 stale L43 ✓ · hex 0 ✓ · icon 对 artboard ✓。

**3 个「doc 宣称 vs 现实」stale 发现（元模式）**：
1. v0.6.4.0 c3：foundation baseline stale 126 行（自 ~v0.6.2.4）— 已修
2. v0.6.4.2：Shell L43 版本号 stale `v0.6.1.4` drift 3 MINOR — 本 PATCH 修 + R-181 四处同步立约
3. v0.6.4.2：R-199.5「KnotLogo 仅三文件」stale（实 5，Enroll + ForceChangePassword 自 ~v0.6.2.0）— 守护者独立验证执行者透明上报属实非回归；裁定更新文档为 5 文件

**元模式共因**：doc 宣称的不变量（文件集 / 版本字面 / baseline）无 CI 强制 → 跨 PATCH 静默 drift，全靠人肉发现。**登记（非阻断）**：建议某 chore PATCH 集中立「doc-不变量 CI 守护」一揽子（版本字面四处一致 + KnotLogo 文件集 + foundation baseline）。

**§0 governance 解决**：资深「shell 开始」+ 拍板「轻量 v3 继续」= 显式 ack 跳 Stage 2 覆盖 v0.5.9 全-v3 先例（本 shell 重构特例，资深 prerogative）。

## §9.1 commit 3 — CI 修 + 五处同步自省（元模式第 6 数据点）

commit 2 CI 红：`test_readme_top_version_synced_with_main`（KNOW-1）—— README 顶部仍 `v0.6.4.1`。
**根因**：本 PATCH 立「四处同步」时**漏了 README 顶部**（实为第 5 同步点，且本就 CI 强制）。
**讽刺/印证**：修 stale-doc 的立约自身漏一个 CI-enforced 点 = 元模式第 6 数据点 → 强化 task #44「doc-不变量 CI 守护一揽子」必要性（连人肉立的立约都漏，必须 CI 兜底）。
**修**：README 顶部 v0.6.4.1→v0.6.4.2 + 当前版本行改述 shell 复刻；CLAUDE.md §四处→§五处同步（README 第 3 点 ★CI 强制 + 标注 Shell L43 未 CI 待 task #44 补）。
commit 3 纯 docs（README + CLAUDE.md），0 源码 0 静态 re-touch。
