# v0.6.2.4 — Shared drift 调和（段 3 · PATCH-2）· Stage 1 执行者草案

> **性质**：Stage 1 **执行者草案**，**非** LOCKED。→ Stage 2 辅助 AI 初审 → Stage 3 守护者终审 → Stage 4 落地。
> **协议**：业务代码重构（前端）→ **完整 Loop Protocol v3**，**守护者全程强制 active**（drift 逐项 canonical-body sign-off）。**不适用简化协议**。
> **基座**：worktree `v0624-drift` @ `f6f03e3`（v0.6.2.3 PATCH-1 已并入 main；Shared **26 exports**；LOCKED 手册 + foundation guard 在）。
> **承接**：段 3 PATCH-1（v0.6.2.3）只搬 single-copy / byte-equal 机械项；**PATCH-2 处理需 canonical-body 决策的 drift**。详 [v0.6.2.3 手册](v0.6.2.3-shared-consolidation.md) §1.3 / §8.5（3 雷区预锁）。
> **日期**：2026-06-13 · 执行者 v0.6 Agent。

---

## 0. drift 现状（执行者直读 f6f03e3 重验 — md5 brace-matched）

| Helper | 站点 | drift 状态（md5）|
|---|---|---|
| **PeriodTab** | AdminMetrics:13 / AdminRecovery:15 / AdminQueryHistory:22 | 2 byte-equal `031b07f5`；AdminQueryHistory **drifted** `a01ff634`（28px/r6/无 shadow vs 30px/r8/shadow）|
| **TagChip** | AdminMetrics:27 / AdminRecovery:30 / AdminQueryHistory:35 | 2 byte-equal `af637ebc`；AdminQueryHistory **drifted superset** `b603561`（多 `kind=` + colors map）|
| **statLabelStyle** | AdminAudit:397(fn) / AdminBudgets:19(arrow) | **drifted** `c5145147` vs `e78d00ac`（fontSize 10.5 vs 11）|
| **KV / DetailRow** | AdminAudit:386 `{T,k,v,mono}` / AdminQueryHistory:202 `{T,k,v,kind}` | sibling（第 4 参不同）|
| **input 族** | inpStyle(AdminBudgets:11) / inputStyle(AdminAudit:401) / fieldStyle(ForceChangePassword:145) | 3 个异体 input 工厂 |
| **btn 族** | ghostBtnStyle / primaryBtnStyle / pageBtnStyle(AdminAudit:409/419/428) | 与 Shared `pillBtn(T,primary=false)` 重叠 |
| **theadStyle** | tab_knowledge:11(formal) + tab_access:32/87(inline) + tab_resources:94(inline) | typography 子集 3 站点重复 |
| **actionColor + ActionChip** | AdminAudit:16/25 | audit-domain 耦合（auth./budget./export. 前缀）|
| **trailingChip** | tab_resources:11（in-body closure）| 闭包 → prop 形态转换 |
| **Avatar** | AdminAudit:268 / AdminRecovery:179 / tab_access:39（inline JSX 22×22 brandSoft+initial）| 无命名 def；`50%` 圆在 8 文件出现，**仅这 3 个是真 avatar** |
| **SavedReports.pillBtn** | SavedReports:366 `function pillBtn(T,primary=false)` | **同名异体**碰撞（body ≠ Shared.pillBtn compact 尺寸）|

**PATCH-1 14 helper 已在 Shared（26 exports），上述 drift set PATCH-1 未碰 — 全部仍 inline，PATCH-2 处理。**

---

## 1. 范围 — drift 调和清单（per-helper canonical-body 决策）

> 总原则（守护者 §8.3 R-PB-SH-7 强化）：**drift 调和绝对 0 位移**——一律 Optional Props 参数化，默认值 = canonical 屏现值，drift 屏显式传参；migrate 后逐屏 render byte-equal。视觉归一另由资深/UI 发起，**不在本 PATCH**。

### 组 A — drift 参数化（0 位移）
| Helper | Shared 形态 | 调用点处理 |
|---|---|---|
| **PeriodTab** | `({T,label,active,onClick, height=30, radius=8, shadow=true})` 默认=AdminMetrics/Recovery canonical | AdminMetrics/Recovery 无参（默认）；AdminQueryHistory 显式 `height={28} radius={6} shadow={false}` → 0 位移 |
| **TagChip** | 采 AdminQueryHistory **superset** `({T,children, kind='accent'})` + colors map | AdminMetrics/Recovery 2-arg 调用（kind 默认 accent = 原行为）；AdminQueryHistory `kind=` 调用 → 全 0 位移 |
| **statLabelStyle** | `(T, fontSize=11)` 默认=AdminBudgets canonical | AdminBudgets 无参；AdminAudit 显式 `statLabelStyle(T, 10.5)` → 0 位移 |

### 组 B — sibling 合并 →（Stage 3 实证 + 资深 2026-06-13 裁定）**DEFER 两者**
| Helper | 决策（LOCKED）|
|---|---|
| ~~KVRow（KV + DetailRow 合并）~~ | **DEFER 两者** — 实证：KV(AdminAudit) 与 DetailRow(AdminQueryHistory) k 标签 + v 值**两处都 drift**（非守护者预判的 mono/kind 单轴正交：KV k plain / DetailRow k mono-uppercase；KV v mono-toggle / DetailRow v kind-color+lineHeight+wordBreak）。合并需 5-6 参（过度工程）；且**各单屏使用、非跨屏 byte-equal 重复** → 按 SparkIcon/fieldStyle「单用不 globalize」先例，KV 留 AdminAudit、DetailRow 留 AdminQueryHistory。详 §8 |

### 组 C — style/btn 族（lift-vs-fold · 守护者逐条 sign-off · 架构判断）
| Helper | 执行者倾向（待 Stage 2/3 定）|
|---|---|
| **input 族** | inpStyle(reused) + inputStyle(reused) → lift 为 2 distinct export（`inputStyleMono` / `inputStyleField`，体异不强行统一）；**fieldStyle 单用非重复** → 按 PATCH-1 SparkIcon 先例**不 globalize，留 ForceChangePassword inline** |
| **btn 族** | （Stage 3 实证 + 守护者强制）**全 lift 3 distinct**（ghostBtnStyle/primaryBtnStyle/pageBtnStyle），**0 fold**。实证：`pillBtn` `6px11px`/r8/gap6 ≠ btn 族 `8px14px`/r6/fontSize13 + pageBtnStyle 带 `disabled` 参 → fold 必破 0 位移。执行者原"等价则 fold"倾向**撤回**。详 §8 |

### 组 D — 提取 / 转换
| Helper | 决策 |
|---|---|
| **theadStyle** | lift typography 子集（fontSize 11 / mono / 0.06em / uppercase / fontWeight 500 / color T.muted / bg T.bg / borderBottom）→ Shared；spread 入 tab_access:32/87 + tab_resources:94 grid header（**保各表 gridTemplateColumns + padding inline**）。**tab_access summary labels(fontSize 10.5) 不并入**（不同 size，非 thead）|
| **Avatar** | NRP-SH-2 AST 三条件子集：仅 22×22 + initial(charAt) + brandSoft 的真 avatar（AdminAudit:268/AdminRecovery:179/tab_access:39）→ `Avatar({T,name,size=22})`；其余 5 文件 `50%` 圆**不动**。三屏若有 fontSize 微 drift（tab_access 10.5）→ 参数化 0 位移 |
| **trailingChip** | tab_resources in-body closure → `FilledChip({T,value})` prop 形态（R-529 闭包转 prop）|
| **actionColor + ActionChip** | **执行者倾向 DEFER（留 AdminAudit）** — audit-domain 逻辑（auth./budget./export. 前缀映射）非通用视觉 primitive；进 Foundation 会把 domain 知识带入。守护者定 进/留 |
| **SavedReports.pillBtn**（雷区 3）| body ≠ Shared.pillBtn（compact 尺寸）→ lift 为 `pillBtnCompact` distinct export；migrate SavedReports 调用点 0 位移（仅改名）|

### DEFER（PATCH-2 不动 — sustained）
- **roleChip**（Admin.jsx:292 in-body）— R-520 冻结在原位
- **SparkIcon / FlowIcon** — PATCH-1 裁定单屏单用不进 dict，留 ChatEmpty inline（I 恒 38）
- **fieldStyle**（单用）— SparkIcon 先例不 globalize

---

## 2. 红线

### PATCH-2 新立 R-PB-SH-13~20
| 红线 | 内容 |
|---|---|
| **R-PB-SH-13** | **drift 绝对 0 位移参数化**（R-PB-SH-7 实施）：PeriodTab/TagChip/statLabelStyle 默认值 = canonical 屏现值，drift 屏显式传参；migrate 后逐屏 render byte-equal（grep 调用点核 + 人测复核）|
| **R-PB-SH-14** | **KVRow 合并**：{mono}+{kind} 双 optional；KV/DetailRow 两屏调用点行为 byte-equal；合并前 body 差异确认可统一 |
| **R-PB-SH-15** | **style/btn 族 lift-vs-fold 守护者逐条 sign-off**；单用非重复（fieldStyle）按 SparkIcon 先例不 globalize；fold 项删 local 后调用点 0 位移 |
| **R-PB-SH-16** | **actionColor/ActionChip domain 边界**：守护者定进 Foundation vs 留 AdminAudit；执行者倾向留（视觉 primitive ≠ domain logic）|
| **R-PB-SH-17** | **Avatar AST 三条件子集**（NRP-SH-2 实施）：仅 width+height+initial+brandSoft 真 avatar 提取；其余 5 文件 50% 圆不动；三屏 fontSize 微 drift 参数化 0 位移 |
| **R-PB-SH-18** | **theadStyle typography 子集抽取**：lift 子集 spread 入 inline grid header，保各表 gridTemplateColumns；summary labels(10.5) 不并 |
| **R-PB-SH-19** | **SavedReports.pillBtn 碰撞**：lift `pillBtnCompact` distinct（body 异于 Shared.pillBtn）；7 调用点改名 0 位移 |
| **R-PB-SH-20** | **snapshot rebaseline + DEFER**：PATCH-2 commit 1 重生 foundation snapshot 到 post-PATCH-1（26 exports）；roleChip/fieldStyle/SparkIcon-FlowIcon DEFER sustained |

### Sustained（PATCH-1 R-PB-SH-1~12）
- R-PB-SH-1 additive-only（既有 26 export byte-equal + 仅新增）/ R-PB-SH-2 export 名+签名 byte-equal
- R-PB-SH-3 buildTheme 26 keys 不动 / R-PB-SH-4 I 38 names 不动
- R-PB-SH-5 多屏一次性提取铁律（drift helper 的所有屏同 PATCH 改，禁单屏残留）
- R-PB-SH-8 cycle 守护（Shared 0 import utils/screens/api）/ R-PB-SH-9 LIMIT / R-PB-SH-11 自验证
- R-286 hex 全禁 / R-313 rgba 豁免边界 / R-PA-PB-V1 视觉铁律

---

## 3. 文件改动（in-scope 预声明）

**Shared.jsx**（+约 8-12 export，视 fold/lift 决策）：PeriodTab / TagChip / statLabelStyle / KVRow / Avatar / theadStyle / FilledChip / pillBtnCompact (+ input族 lift项 + btn族 lift项)
**screen 改（删 local + import + drift 屏显式传参 / 改名调用点）**：
- AdminMetrics / AdminRecovery / AdminQueryHistory（PeriodTab + TagChip）
- AdminAudit（statLabelStyle + KV→KVRow + Avatar + input/btn 族 + actionColor/ActionChip 视决策）
- AdminBudgets（statLabelStyle + inpStyle）
- tab_access（Avatar + theadStyle inline + summary labels 守护不动）/ tab_resources（theadStyle inline + trailingChip）/ tab_knowledge（theadStyle formal）
- ForceChangePassword（fieldStyle DEFER — 0 改 OR lift 视决策）
- SavedReports（pillBtn → pillBtnCompact）
**基础设施**：tests/foundation/snapshot 重生（26 baseline）+ scripts/check_file_sizes（Shared 增长，560 LIMIT 复核）
**收官**：knot/main.py + test_rename_smoke + Login 页脚 + README（version 0.6.2.4）+ CLAUDE.md 路线图 + CHANGELOG + 静态重建

**绝对不动**：App/Shell/main/index.css/App.css/Chat/Conversation/Composer/Admin(roleChip)/modals/FeedbackBar/后端全部 + PATCH-1 已整合的 14 helper（byte-equal sustained）

---

## 4. 验收
1. drift helper 全屏单真源：grep 各 helper 名全 frontend = 仅 Shared 1 命中（多屏一次性 R-PB-SH-5）
2. **drift 0 位移**：PeriodTab/TagChip/statLabelStyle 参数默认=canonical；drift 屏显式传参；**人测逐屏 byte-equal**（这次"有变化"才是 bug）
3. Avatar：3 真 avatar 提取，其余 5 文件 50% 圆 0 改
4. theadStyle：inline grid header spread 后 gridTemplateColumns 保留；视觉 byte-equal
5. 既有 26 export byte-equal（additive guard）+ buildTheme 26 / I 38 不动
6. `npm run build` exit 0 + 守护测试绿 + check_file_sizes OK
7. 7 contracts KEPT（前端重构 0 后端改）+ R-PA-8 0 命中

---

## 5. commit 序列（草案 · 待 Stage 3 终审）
- **commit 1**：snapshot rebaseline 到 26 baseline + check_file_sizes LIMIT 复核（基础设施）
- **commit 2**：组 A drift 参数化（PeriodTab/TagChip/statLabelStyle add Shared + 3+2+2 屏 migrate 0 位移）
- **commit 3**：组 B KVRow 合并（KV+DetailRow）
- **commit 4**：Avatar 提取（AST 三条件，3 屏）
- **commit 5**：theadStyle typography 抽取（3 站点）
- **commit 6**：组 C style/btn 族（守护者 fold/lift sign-off 后）
- **commit 7**：trailingChip→FilledChip + SavedReports pillBtnCompact + actionColor/ActionChip（视决策）
- **commit 8**：收官（version 0.6.2.4 三处 + README + CLAUDE.md 路线图 + 静态重建 + CHANGELOG）
- 守护者逐 commit add/delete git-diff 验 byte-equal + drift 屏 0 位移核

> **规模提示**：PATCH-2 helper 多（10+），commit 序列较长。若 Stage 3 守护者/资深认为过大，可拆 v0.6.2.4（drift 参数化 + sibling + Avatar）+ v0.6.2.5（style/btn 族 + theadStyle）两 PATCH — **但默认按段 3「2 PATCH」计划一个 PATCH 收完**。Stage 2/3 决定。

---

## 6. 协议合规
- 完整 Loop Protocol v3：Stage 1（本稿）→ Stage 2 → Stage 3 → Stage 4。**守护者全程强制 active**（drift 逐条 canonical-body sign-off — 收官 §8.6）。
- 非 OVERRIDE（v0.6.x LOCKED 路线图 段 3 范围内）。
- 关联 Task #34（主）；承接 v0.6.2.3 §1.3/§8.5。

## 7. 自检 + Stage 2/3 open questions
- ✅ drift set 在 f6f03e3 直读重验（md5 一致）
- ✅ 0 位移参数化总原则（R-PB-SH-7 强化实施）
- ✅ Avatar 真子集 3 屏定位（其余 5 个 50% 圆排除）
- ⏳ **Stage 2/3 待定**：① btn 族 fold-vs-lift（pillBtn 折叠可行性需 body 对比）② actionColor/ActionChip 进 Foundation vs 留 ③ KVRow 合并 body 差异范围 ④ PATCH-2 是否拆 2（规模）⑤ input 族统一 vs 保 distinct
- ⏳ **人测重点**：drift 屏 0 位移（PeriodTab AdminQueryHistory 28px / TagChip kind 色 / statLabelStyle 10.5）— 这次"看着不变"仍是对的，但 drift 屏要逐一确认参数生效

---

## 8. Stage 3 守护者终审整合 + 资深 LOCKED（2026-06-13）

> 权威 LOCKED delta。整合 Stage 2 第三方初审（4.6/5）+ 守护者 Stage 3 终审（4.6/5 修订后批准）+ 守护者判断题实证 + **执行者复核 + 资深拍板**。§1 组 B/C 以本节为准。

### 8.1 五判断题最终裁定（全部收敛为确定性执行）

| # | 判断题 | LOCKED 裁定 | 依据 |
|---|---|---|---|
| 1 | btn 族 fold-vs-lift | **强制 lift 3 distinct，0 fold** | 守护者实证 + 执行者复核：pillBtn `6px11px`/r8/gap6 ≠ ghost/primary `8px14px`/r6/fs13 + pageBtnStyle `disabled` 参 → fold 必破 0 位移 |
| 2 | actionColor/ActionChip | **DEFER 留 AdminAudit** | 三方共识：audit-domain（auth./budget./export. 前缀）≠ 视觉 primitive；进 Foundation 破 R-PB-SH-8 cycle + R-PB-SH-12 精神 |
| 3 | KVRow 合并 | **DEFER 两者**（KV 留 AdminAudit / DetailRow 留 AdminQueryHistory）| 执行者实证质疑守护者"正交"：k+v 两处都 drift（非 mono/kind 单轴）+ 各单屏单用非跨屏重复 → SparkIcon 先例不 globalize。**资深 2026-06-13 拍板 DEFER** |
| 4 | SavedReports.pillBtn | **lift pillBtnCompact distinct** | 实证异体 5 处（gap4/`6px12px`/r6/border 双/color text）≠ Shared.pillBtn → 不 fold |
| 5 | 规模拆分 | **不拆**（一个 PATCH 收完）| 判断题经终审收敛为确定性执行 → commit 6/7 原子化即可；拆 2 反造中间态 drift 债。守护者采纳 Stage 2 + 修正自身倾向 |

### 8.2 执行者复核（不接受单方实证陈述）

守护者 btn 族 / pillBtn / KVRow 实证经**执行者直读 f6f03e3 body 复核**：btn 族 + SavedReports.pillBtn 实证**确认**（守护者对，fold/异体成立）；**KVRow 实证守护者"mono/kind 正交"不准确** —— 实测 KV(plain k + mono-toggle v) vs DetailRow(mono-uppercase k + kind-color v + lineHeight + wordBreak) 两轴都 drift，执行者反向修正 → 资深裁定 DEFER。延续 PATCH-1 双向独立验证传统（守护者三度修正自身：buildTheme 25→26 → 双维度 → 规模倾向；执行者两度反向修正：buildTheme 口径 + KVRow 正交）。

### 8.3 红线最终态

| 红线 | 最终 |
|---|---|
| R-PB-SH-13 | drift 0 位移参数化（PeriodTab/TagChip/statLabelStyle）sustained |
| ~~R-PB-SH-14（KVRow 合并）~~ | **撤回** → DEFER 两者（§8.1 #3）|
| R-PB-SH-15 强化 | **btn 族强制 lift 3 distinct，0 fold**（实证锁定）；input 族 inpStyle/inputStyle lift 2 distinct + fieldStyle 单用 DEFER inline |
| R-PB-SH-16 锁定 | actionColor + ActionChip 永留 AdminAudit（禁导出 Foundation）|
| R-PB-SH-17 | Avatar AST 三条件子集（真 avatar 3 屏；其余 5 个 50% 圆不动）sustained |
| R-PB-SH-18 | theadStyle typography 子集 spread（禁 grid/flex 进 Shared；各表 gridTemplateColumns inline）sustained |
| R-PB-SH-19 | SavedReports.pillBtn → pillBtnCompact distinct sustained |
| R-PB-SH-20 | snapshot rebaseline 26 baseline + roleChip/fieldStyle/SparkIcon-FlowIcon/KV/DetailRow DEFER sustained |

### 8.4 commit 序列（LOCKED · 取代 §5 · 不拆 · 守护者全程 active 逐条）

- **commit 1** snapshot rebaseline 26 + check_file_sizes LIMIT 复核 → 守护者验
- **commit 2** 组 A drift 参数化（PeriodTab/TagChip/statLabelStyle add Shared + 3+2+2 屏 migrate）→ 守护者验 0 位移 + 参数生效
- **commit 3** Avatar AST 三条件（3 真 avatar 屏；5 个 50% 圆不动）→ 守护者验子集
- **commit 4** theadStyle typography 子集（禁 grid/flex；各表 grid 列 inline）→ 守护者验
- **commit 5** btn 族 lift 3 distinct + input 族 lift 2 distinct（fieldStyle DEFER inline）→ 守护者逐条 git-diff（实证强制 lift）
- **commit 6** trailingChip→FilledChip + SavedReports pillBtnCompact + actionColor/ActionChip DEFER 确认（留 AdminAudit 不动）→ 守护者验
- **commit 7** 收官（version 0.6.2.4 三处 + README + CLAUDE.md 路线图 + 静态重建 + CHANGELOG）→ PATCH-2 收官终审
- **（KVRow commit 已删 — KV/DetailRow DEFER 不上移）**

### 8.5 LOCKED 状态
**PATCH-2 (v0.6.2.4) 修订后批准 → Stage 4 可启动**。5 判断题全裁定明确 → 确定性执行。守护者全程强制 active（7 commit 逐条 add/delete git-diff + drift 屏 0 位移 + 人测逐屏参数生效）。

**签字**：执行者 v0.6 / 守护者 v0.5（4.6/5 修订后批准）/ 资深 2026-06-13 LOCKED（5 裁定 + KVRow DEFER 拍板）。
