# v0.6.4.12 — 内测视觉修复 4 处 + doc/CI 微收尾 2 项（合并 PATCH）

> **协议**：轻量 v3 + **保 Stage 3 复核**（含 2 处 test 逻辑改 + 1 处立约范围校正 — 守护者亲读）。
> **基座**：worktree `v064c-docpolish` @ branch `v0.6.4.12-doc-polish`（off main `bc39a81` = v0.6.4.11）
> **触发**：资深 2026-06-19 :8000 实测反馈 5 点（#2 = 设计如此非 bug）+ 同屏字体一致性 sweep（全 19 屏）+ task #44 残线（knotlogo false-red + CHANGELOG 约定 drift）
> **版本**：PATCH v0.6.4.12（首次走 v0.6.4.11 新 4 源点版本同步模型）
> **性质**：4 处前端视觉（0 业务 / 0 后端）+ 2 项 doc/CI chore

## §0 范围

### A. 内测视觉修复（资深 :8000 实测）
- ① 登录页脚版本号字体 ≠ knot.local（一行 sans 一行 mono）
- ③ 首页左上 KNOT logo 偏小
- ④ 内测指标「指标定义」框多余左边框（drift — demo 无）
- ⑤ 查询历史 user_id 框尺寸+字体与左侧 2 个 select 不一致 + 不对齐

> **#2（登录无 2FA）= 设计如此非 bug**：`KNOT_TOTP_REQUIRED` env 内测期默认 off（deps.py:150）；不改码。
> **预算屏 Hero（预计花费 sans / 结算日 mono）= demo byte-equal**（artboard budget.jsx:70/74 故意；金额 sans/日期 mono）；资深 ack **不动**。

### B. doc/CI 微收尾（task #44 残线）
- ⑥ knotlogo guard 收紧（消除注释 false-red）
- ⑦ CHANGELOG `[Unreleased]` demote 一揽子 + count==1 CI 守护

## §1 改动

### ① Login 页脚版本号 → mono（[Login.jsx:245](frontend/src/screens/Login.jsx#L245)）
- `<span>v{APP_VERSION} · build 202606181000</span>` → 加 `style={{ fontFamily: T.mono }}`，与 L246 knot.local 统一。
- **VRP**：artboard login.jsx:93 版本本是 sans → **资深 override（细节统一优先）**。text byte-equal（R-181 sustained：`v{APP_VERSION}` + "version.js" 仍在）。

### ③ Shell logo 16 → 20（[Shell.jsx:42](frontend/src/Shell.jsx#L42)）
- `<KnotLogo T={T} size={16}/>` → `size={20}`。
- **VRP**：artboard shell.jsx:52 = 16 → **资深 override（放大）**。logoArea height 56 不变无溢出；R-199.5 sustained（KnotLogo 仍仅 5 文件：Shared 定义 + Shell/Login/Enroll/ForceChangePassword 渲染）。

### ④ AdminMetrics 删 borderLeft（[AdminMetrics.jsx:42-43, 100](frontend/src/screens/AdminMetrics.jsx#L100)）
- 删 `borderLeft: rulesBorderLeft` + unused `const rulesBorderLeft` + 更新注释。
- 结果 = `border: 1px solid insetBorder` + `borderRadius: 8` → **对齐 demo admin-metrics.jsx:67**（1px 全框 brandSoftBorder，无左 accent）。
- **守护者 Stage 3 更正（已独立 ground-verify）**：AdminMetrics 3px borderLeft 是误挂 R-481「第四处」的**非 demo drift**。**不存在「3 文件 3px byte-equal 闭环」可退回** —— 实查 SavedReports L331=**3px** / AdminBudgets L173=**2px** / AdminRecovery L170=**2px**（v0.5.33 软化）；R-481 borderLeft 本就是 v0.5.x 产品设计语言发明、**非 demo-grounded**。
- 本 PATCH **仅删 AdminMetrics drift**；**不碰 SavedReports/Budgets/Recovery**（各自 locked 独立决策，OOS）；**不声称闭环退回**。

### ⑤ AdminQueryHistory user_id 框对齐（[AdminQueryHistory.jsx:3, 90-93](frontend/src/screens/AdminQueryHistory.jsx#L90)）
- `<div style={{width:160}}><Input ... mono/></div>` → raw `<input>` 与左侧 2 个 `<select>` 同款：
  `width:160, height:28, padding:'0 10px', borderRadius:6, background:T.content, border:1px T.border, color:T.text, fontSize:12, fontFamily:T.sans, outline:'none'`。
- 删 L3 `Input` import（该文件唯一消费方 → 清 unused）。
- 解决三处：尺寸（高 28）+ 字体（mono→sans 与 select 一致）+ 对齐（去 Input wrapper marginBottom:12）。
- 其余 filter strip / table / drawer / 业务逻辑 byte-equal。

### ⑥ knotlogo guard 收紧（test_doc_invariants.test_knotlogo_file_set）
- 子串 `"KnotLogo"` → `"<KnotLogo"`（渲染引用）；expected 集合 **5 → 4 消费方**（Shell/Login/Enroll/ForceChangePassword）。
- Shared 定义（`export function KnotLogo`）由 **R-185（test_login_version_sync L44-46，已 ground-verify 断言三件套 export）** 覆盖 → 不丢守护，仅分离「定义 vs 渲染」，消除注释/字符串提 "KnotLogo" 的 false-red。
- **守护者 Stage 3 小条件（doc 自洽）**：更新 test 注释/docstring → 「R-199.5 = 5 文件 = 4 渲染（本 guard）+ 1 Shared 定义（R-185）」，避免「R-199.5 说 5 / guard 期望 4」语义不一致。

### ⑦ CHANGELOG demote + count CI
- `sed 's/^## \[Unreleased\]/## [Released]/'` 全部 41 个 → `[Released]`（`[Archived]` / 已 `[Released]` 不动）。
- 顶部加 `## [Unreleased] - v0.6.4.12 …`（描述本 PATCH 4 UI + 2 doc/CI；唯一 in-flight）。
- 新 CI `test_doc_invariants.test_changelog_single_unreleased`：`## [Unreleased]` 计数 == 1。
- **不动历史正文**（仅 marker 翻转 + 顶部新增）。

## §2 保 byte-equal / 不动守护
- **Shared.jsx + utils.jsx 0 改**（foundation snapshot sustained；Input 仍存在 utils，仅 AdminQueryHistory 不再 import）。
- **App.jsx 0 diff**；4 UI fixes 全在各自屏内联。
- 0 后端 / 0 业务逻辑 / 0 路由。
- 现有 doc-不变量守护 sustained：R-72 / R-181 / R-185 / KNOW-1 / foundation / bridge / shell-render / changelog-top + 新 changelog-single。

## §3 红线

| # | 红线 |
|---|---|
| R-UI2F-1 | #1 Login 版本 span `fontFamily:T.mono`；text byte-equal（R-181：`v{APP_VERSION}`+"version.js" 在） |
| R-UI2F-2 | #3 KnotLogo 16→20（资深 VRP override）；R-199.5 仍 5 文件；logoArea 56 无溢出 |
| R-UI2F-3 | #4 删 borderLeft（+unused const+注释）→ 回正 demo；**R-481 闭环校正回 3 处**，其余 3 屏不动；mutation：保留 borderLeft → 视觉回退 |
| R-UI2F-4 | #5 user_id raw input 与 2 select 同款（含 fontFamily:T.sans）；删 Input import；filter/table/drawer byte-equal |
| R-UI2F-5 | 预算屏 Hero（预计花费 sans/结算日 mono）**不动**（demo byte-equal · 资深 ack） |
| R-DP-1 | knotlogo 收紧 `<KnotLogo` 精确 4 集；Shared 定义 R-185 覆盖；mutation：6th 渲染/任一蒸发 → 红 |
| R-DP-3 | CHANGELOG `## [Unreleased]` count==1（新 CI 真抓：留 2 个 → 红）；历史正文 byte-equal |
| R-DP-7 | **4 源点版本同步** 0.6.4.12：main.py + smoke + README★ + version.js★（Login 页脚 + Shell sidebar 版本经 APP_VERSION **自动跟随 0 手改**） |
| R-UI2F-6 | 范围：Shared/utils/App.jsx 0 diff · 0 后端 · R-PA-8 0 · check_file_sizes OK（4 屏均 ≤ LIMIT） |

## §4 验收
1. #1 mono / #3 size20 / #4 删边 / #5 三件套对齐 —— 重 build+重启实例肉眼验（资深侧；我不碰 :8000）light+dark 双模式
2. R-181 / R-185 / foundation / bridge / shell-render sustained（UI fixes 不触守护断言）
3. knotlogo 收紧 PASS（4 集）+ R-185 sustained（Shared 定义守护不丢）
4. CHANGELOG 41→[Released] + 顶部 v0.6.4.12 + count==1 PASS；mutation 自验（6th `<KnotLogo` 红 / 2 个 [Unreleased] 红）
5. 4 源点版本 0.6.4.12（**Login/Shell 版本号 0 手改 = 验证新模型**）+ App.jsx 0 diff + R-PA-8 0
6. 本机 python 坏 → 新/改 CI 走 CI 验证（clean PASS + 逻辑 trace mutation 红）

## §5 commit 序列
- **commit 1**：4 UI fixes（Login/Shell/AdminMetrics/AdminQueryHistory）+ knotlogo 收紧 + CHANGELOG demote + count CI + 4 源点版本同步 + 本 plan（CHANGELOG 在 commit 1 → test_changelog 自洽；version.js bump → Login/Shell 版本 0 改自动跟随）
- **commit 2（收官）**：npm build 静态（APP_VERSION 0.6.4.12 + 4 UI fixes bake 进 bundle）

## §6 协议合规
轻量 v3 + **保 Stage 3**（3 处需守护者亲读：⑥ knotlogo test 收紧 + ⑦ count CI 新增 + ④ R-481 闭环范围校正退订一处立约）。执行者 Stage 1 → 守护者 Stage 3 → 逐 commit 直读 → 资深 push/merge。task #44 残线清偿 + 内测视觉反馈闭环。v0.6→v0.7 整体审核前最后清债。
