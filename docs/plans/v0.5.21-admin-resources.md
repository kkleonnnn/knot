# v0.5.21 — (C5+) admin tab_resources 屏复刻 — LOCKED (Stage 1+2+3 整合 → Stage 4 准备)

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **协议**：Loop Protocol v3 — 三阶段评审闭环（**第 22 次施行**）
> **里程碑**：**Inset 8% 铁律从"防御性稳固"转向"进攻性扩张"** + **文件总数 6 → 7 正式扩张** + **项目单色化一致性进入 80% 深度** + **R-484 'white' 字面残留偿还** + **R-546~R-550 五大守护立约**

---

## 1. 决议表 + 风险项最终裁定（D1-D8 + Q1-Q5 全部锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| D1 范围 整个 tab_resources | ✅ A | Stage 1 |
| **D2** thead R-480 闭环字面 | ✅ **绝对支持** | Stage 2 — 设计语言的必然归宿；文件总数锁定 7 |
| D3 KeyInput trailing mono | ✅ A | Stage 1 + R-547 加码 |
| **D4** Agent allocation Plain Span | ✅ **支持保 Plain**（Stage 2） | 管理端指导文案可读性优先；过度 TagChip 稀释视觉权重 |
| **D5** Hex 偿还 Spinner | ✅ **强制执行**（R-536） | Stage 2 — R-484 精神彻底贯彻 |
| D6 Model 默认 chip 保 T.accentSoft | ✅ B 保守 | Stage 1 |
| D7 LIMITS dict 不动 | ✅ A | Stage 1 |
| D8 Card helper inline 保 | ✅ B | Stage 1 |
| Q1 12 props 签名 byte-equal | ✅ A | Stage 1 + R-548 加码 |
| Q2 业务字段 byte-equal | ✅ A | Stage 1 + R-549 加码 |
| Q3 Input from utils byte-equal | ✅ A R-365 sustained | Stage 1 |
| Q4 pillBtn from Shared byte-equal | ✅ A R-365 sustained | Stage 1 |
| Q5 v0.6 Shared 移植承诺 | ✅ A | Stage 1（第六次复用确认） |

13/13 锁定；Stage 2 在 D2/D5 强化 + Codex 加码 R-546（Model ID mono）+ R-547（API Key 安全感）+ Stage 3 加码 R-548（核爆级 props 守护）+ R-549（价格单位保留）+ R-550（borderBottom byte-equal）；其余 Stage 1 保留。

---

## 2. 红线最终清单（R-521 ~ R-550，30 条）

### Stage 1 草案原 25 条（保留）

| ID | 摘要 |
|---|---|
| R-521 | TabResources 12 props 签名 byte-equal |
| R-522 | apiKeys 2 keys byte-equal |
| R-523 | agentCfg 3 keys byte-equal |
| R-524 | model 8 字段 byte-equal |
| R-525 | Input from utils.jsx props byte-equal（R-365 sustained） |
| R-526 | pillBtn from Shared.jsx 调用 byte-equal（R-365 sustained） |
| R-527 | API Keys Card padding 16→20 + radius 10→12 |
| R-528 | Card header 14/600/-0.01em + desc lineHeight 1.55 |
| R-529 | KeyInput trailing mono uppercase + 0.06em + T.success/T.muted |
| R-530 | Agent allocation Card padding/radius + 3-col `120px 1fr 90px` |
| R-531 | Models Table thead bg → brandSoft 8%（闭环字面第八处） |
| R-532 | Models Table thead mono + 0.06em + uppercase + 500 + T.subtext |
| R-533 | Models Table grid `1.5fr 0.7fr 1.4fr 1fr 0.7fr 100px` + minWidth 0 + ellipsis |
| R-534 | Models Row 默认 chip 保 T.accentSoft byte-equal（D6 保守） |
| R-535 | Models Row 状态 byte-equal（业务字段不动） |
| R-536 | `color="#fff"` Spinner 2 处 → `color={T.sendFg}`（R-484 sustained） |
| R-537 | grep hex 0 命中（R-518 sustained） |
| R-538 | grep `'#fff'\|"#fff"` = 0 |
| R-539 | **R-480 闭环字面文件总数 6 → 7**（铁律进攻性扩张） |
| R-540 | R-495 字面 byte-equal 严防 sustained |
| R-541 | 范围守护 18 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery byte-equal |
| R-542 | admin/ 其他 3 子模块 + modals + chat/ 7 子模块 0 改 |
| R-543 | App.css 0 行 diff + tab_resources 0 新 className |
| R-544 | KnotLogo 3 文件 sustained |
| R-545 | 三处版本同步 0.5.20→0.5.21 + LIMITS dict 不动 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-546** | **Model ID 字体守护** — Models Table 中 `m.model_id` 列必须 `fontFamily: T.mono` — Model ID（如 openai/gpt-4o）属技术元数据，Mono 识别度更好 | Model ID cell inline style `fontFamily: T.mono`；commit 1 后 grep 命中 |
| **R-547** | **API Key 输入框安全感** — `apiKeys.openrouter_api_key` 存在时 trailing 展示"已配置"状态；用户无需点开遮罩感知配置态 | KeyInput trailing 状态机已含此逻辑 — 验证 trailing 在 input type='password' 遮罩状态下仍清晰可见 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-548** | **业务字段 12 Props 签名"核爆级"守护** — 严禁调整 export function 参数顺序；手动核对 Admin.jsx 挂载点 100% byte-equal | commit 1 起手前**强制** `git diff origin/main HEAD -- tab_resources.jsx \| grep "^[-+].*export function"` 签名行 byte-equal；同步 grep Admin.jsx 中 `<TabResources` 调用点 byte-equal |
| **R-549** | **价格显示业务标签保留 (R-533 扩展)** — input_price/output_price 显示必须保留 `$` 或单位 | grep 命中；commit 1 后实测价格列显示格式 byte-equal |
| **R-550** | **闭环字面 byte-equal 严防死守 (R-540 加强)** — borderBottom 粗细+颜色必须与 TabAccess 一致（`1px solid ${T.border}`） | grep `borderBottom:\s*\`1px solid \${T\.border}\`` 命中；字面与 tab_access.jsx 同字面 byte-equal |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §3 验收核心 — 7 文件命中标志性 color-mix 字面量

```bash
# 1. R-539 闭环字面文件总数 6 → 7（铁律进攻性扩张验证）
git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# 预期 7 文件命中：
#   chat/ResultBlock.jsx (v0.5.14 R-323)
#   SavedReports.jsx (v0.5.15 R-372)
#   admin/tab_access.jsx (v0.5.16 R-386 + v0.5.20 R-501/504)
#   AdminAudit.jsx (v0.5.17 R-409)
#   AdminBudgets.jsx (v0.5.18 R-444)
#   AdminRecovery.jsx (v0.5.19 R-480)
#   admin/tab_resources.jsx (v0.5.21 R-531) ← 新增 第 7 文件

# 2. R-537 R-518 hex 余效（v0.5.x 视觉治理硬指标 sustained）
grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/screens/admin/tab_resources.jsx | grep -v boxShadow | grep -v rgba
# 预期 0 行（hex 清零）

# 3. R-538 'white' 字面残留偿还
grep -E "'#fff'|\"#fff\"" frontend/src/screens/admin/tab_resources.jsx
# 预期 0 行（R-484 sustained）

# 4. R-548 12 props 签名 byte-equal 核爆级
git diff origin/main HEAD -- frontend/src/screens/admin/tab_resources.jsx | grep "^[-+].*export function"
# 预期：签名行字面 byte-equal（无 +/- 改动行）
```

### Stage 3 §2 实施核心顺序（commit 1 子步骤 5 步锁死 — R-548 前置 + R-531 优先 + R-539 收尾）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + **R-548 起手前置签名核爆守护** + **R-531 thead bg R-480 闭环字面率先落地**（铁律进攻性扩张第八处）+ **R-537 hex 余效 grep 立即执行** | R-531/R-537/R-539/R-548 |
| 2 | API Keys Card padding 16→20 + radius 10→12 + Card header 14/600/-0.01em + desc 1.55 + **R-529 KeyInput trailing mono uppercase 0.06em** + **R-547 安全感验证** | R-527/R-528/R-529/R-547 |
| 3 | Agent allocation Card padding/radius 升级 + 3-col grid `120px 1fr 90px` + **D4 plain span hint byte-equal** | R-530/D4 |
| 4 | Models Table thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext + Row minWidth: 0 + ellipsis 兜底 + **R-546 Model ID 列 fontFamily T.mono** + **R-549 价格业务标签 $ 单位保留** + **R-550 borderBottom 1px T.border byte-equal** | R-532/R-533/R-546/R-549/R-550 |
| 5 | Hex 偿还 `color="#fff"` Spinner 2 处 → `color={T.sendFg}`（R-536）+ 三处版本同步 + **R-540/R-550 字面严防 grep**（闭环字面 byte-equal + borderBottom byte-equal）+ 全 hex grep 闭门 + **R-539 7 文件命中验证** | R-536/R-538/R-540/R-545/R-550 |

### Stage 3 R-548 核爆级 props 守护

```bash
# 1. 检查 tab_resources.jsx 当前 export function 签名
grep -n "export function TabResources\|export default function TabResources\|const TabResources" frontend/src/screens/admin/tab_resources.jsx
# 2. 检查 Admin.jsx 内 TabResources 调用挂载点
grep -n "<TabResources" frontend/src/screens/Admin.jsx
```

确认 12 props 顺序 + Admin.jsx 调用方 props 字面 byte-equal。任何参数顺序调整 = R-548 否决。

### Stage 3 R-549 价格业务标签

```bash
grep -n "input_price\|output_price\|\$\|/M tokens\|每百万" frontend/src/screens/admin/tab_resources.jsx
```

确认现有格式 — 重构时**业务标签字面 byte-equal**；严禁为视觉简洁删除 $ 或单位。

### Stage 3 R-550 borderBottom 闭环

```jsx
// tab_resources.jsx Models Table thead
borderBottom: `1px solid ${T.border}`,  // ← R-550 byte-equal
// tab_access.jsx Sources thead (v0.5.16) + Users thead (v0.5.20)
borderBottom: `1px solid ${T.border}`,  // 字面完全一致
```

### Stage 2 R-546 Model ID Mono

```jsx
<div style={{
  fontFamily: T.mono,  // ← R-546 Model ID 技术元数据 mono 识别度
  fontSize: 11,
  color: T.muted,
  overflow: 'hidden',
  textOverflow: 'ellipsis',
  whiteSpace: 'nowrap',
}}>{m.model_id}</div>
```

### Stage 2 R-547 API Key 安全感

- Input from utils.jsx 已支持 `type='password'` + `trailing` props（R-525 sustained 不动）
- KeyInput trailing "已填写" 文字在遮罩状态下**可见**（trailing 独立于 input 字符遮罩）
- commit 1 后**实测**：填入 OpenRouter Key → input 字符遮罩 + trailing 显 "已填写"（mono uppercase 0.06em T.success）

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/admin/tab_resources.jsx  [EDIT 85 → ≤ 150 行]
                                              Step 1: baseline + R-548 props 签名核爆守护 + R-531 R-480 字面 + R-537 hex 余效 grep
                                              Step 2: API Keys Card padding/radius + header 14/600/-0.01em + R-529 KeyInput mono trailing + R-547 安全感
                                              Step 3: Agent allocation Card padding/radius + 3-col grid + D4 plain span hint byte-equal
                                              Step 4: Models Table thead mono+0.06em+500+T.subtext + Row ellipsis
                                                      + R-546 Model ID fontFamily T.mono + R-549 价格 $ 单位保留 + R-550 borderBottom 1px T.border
                                              Step 5: Hex 偿还 (Spinner color #fff→T.sendFg) + 三处版本同步
                                                      + R-540/R-550 字面严防 grep + 全 hex grep 闭门 + R-539 7 文件验证
                                              守护: 12 props + apiKeys 2 + agentCfg 3 + model 8 字段 byte-equal [R-521~R-524 + R-548]
                                                    + Input/pillBtn from utils/Shared 调用 byte-equal [R-525/R-526 R-365 sustained]
                                                    + inline styles only [R-543 CSS 0 污染]

knot/main.py                            [EDIT version 0.5.20 → 0.5.21]  [R-545]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.20 → 0.5.21]  [R-545]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.20 → 0.5.21]  [R-545]
scripts/check_file_sizes.py             [unchanged]  R-545 LIMITS dict 不动（tab_resources 85→≤150 远低于 250）

frontend/src/screens/admin/{tab_access,tab_knowledge,tab_system,modals}.jsx  [unchanged]  [R-542]
frontend/src/screens/chat/*             [unchanged]  [R-542]
frontend/src/screens/Admin.jsx          [unchanged]  [R-541 + R-548 Admin.jsx 内 TabResources 12 props 调用不动]
frontend/src/screens/{SavedReports,AdminAudit,AdminBudgets,AdminRecovery}.jsx [unchanged]  [R-541]
frontend/src/screens/ (其余 13 屏)       [unchanged]  [R-541]
frontend/src/{App,api,index.css,main,utils,Shared,Shell}.jsx 等  [unchanged]  [R-541 + R-365 sustained]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]  [R-541]
frontend/src/App.css                    [unchanged 0 行 diff]  [R-543]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js  [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.21 段]
                                        - R-521~R-550 偿还表
                                        - ⭐ 视觉闭环扩张：Inset 8% 铁律成功覆盖 admin/tab_resources，项目单色化一致性进入 80% 深度（Stage 3 §3 里程碑宣告）
                                        - Inset 8% 铁律从"防御性稳固"转向"进攻性扩张"（v0.5.20 R-511 6 文件恒定 → v0.5.21 R-539 文件总数 6→7 正式扩张）
                                        - v0.5.x 第二个 admin tab 子模块复刻（v0.5.16 tab_access + v0.5.21 tab_resources）
                                        - R-484 'white' 字面残留偿还（Spinner color="#fff" 2 处 → T.sendFg）
                                        - R-518 hex 余效验证 sustained — v0.5.x 视觉治理硬指标
                                        - R-546 Model ID Mono 守护立约（技术元数据识别度）
                                        - R-547 API Key 输入框安全感立约（trailing 状态可视）
                                        - R-548 12 Props 核爆级守护（签名 byte-equal Admin.jsx 挂载点）
                                        - R-549 价格业务标签保留（$ 单位严守）
                                        - R-550 borderBottom byte-equal 闭环（与 tab_access 字面一致）
                                        - Card/TagChip helpers inline 第六次复用 sustained — v0.6 Shared 移植承诺加强
                                        - Loop Protocol v3 第 22 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.21（Inset 8% 进攻性扩张第八处 + 文件总数 6→7）
                                        - v0.5.x 路线图 v0.5.21 ⏳ → ✅
                                        - v0.6 路线图加强"Card/TagChip 累计第六次复用，v0.6.0 移入 Shared 偿还"
docs/plans/v0.5.21-admin-resources.md   [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS 不变）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.21**

### Stage 2/3 强化验证

- [ ] **⭐ R-539 闭环字面文件总数 6 → 7**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — **7 文件命中**
- [ ] **R-537 R-518 hex 余效**：`grep -rE "#[0-9a-fA-F]{3,6}" tab_resources.jsx | grep -v boxShadow | grep -v rgba` = 0
- [ ] **R-538 '#fff' 0 命中**：grep `'#fff'\|"#fff"` tab_resources.jsx = 0
- [ ] **R-540 字面严防 sustained**：git grep -F 字面 byte-equal 无空格/逗号差异
- [ ] **R-546 Model ID Mono**：grep Model ID 列 `fontFamily.*T\.mono` 命中
- [ ] **R-547 API Key 安全感**：实测填入 OpenRouter Key → input 遮罩 + trailing "已填写" mono uppercase 0.06em T.success 清晰可见
- [ ] **R-548 12 Props 核爆守护**：`git diff origin/main HEAD -- tab_resources.jsx | grep "^[-+].*export function"` 签名行 byte-equal；Admin.jsx 内 `<TabResources` 挂载点 byte-equal
- [ ] **R-549 价格业务标签**：grep `\$.*input_price\|input_price.*\$` 业务标签 byte-equal
- [ ] **R-550 borderBottom byte-equal**：grep `borderBottom:\s*\`1px solid \${T\.border}\`` 命中；与 tab_access 字面一致

### 契约守护（首要红线）

- [ ] **R-521**：TabResources 12 props 签名 byte-equal — T/models/apiKeys/setApiKeys/apiKeysSaving/onSaveApiKeys/agentCfg/setAgentCfg/agentSaving/onSaveAgentCfg/onToggleModel/onSetDefaultModel
- [ ] **R-522**：apiKeys 2 keys byte-equal（openrouter_api_key / embedding_api_key）
- [ ] **R-523**：agentCfg 3 keys byte-equal（clarifier / sql_planner / presenter）
- [ ] **R-524**：model 8 字段 byte-equal（id/name/provider/model_id/input_price/output_price/enabled/is_default）
- [ ] **R-525**：Input from utils.jsx 调用 byte-equal（type/placeholder/mono/trailing/label/value/onChange）
- [ ] **R-526**：pillBtn from Shared.jsx 调用 byte-equal

### 范围守护

- [ ] **R-541**：App/api/index.css/main/utils/Shared/Shell/decor/18 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery `git diff` = 0
- [ ] **R-542**：admin/ 其他 3 子模块 + modals + chat/ 7 子模块 `git diff` = 0
- [ ] **R-543**：App.css 0 行 diff + tab_resources 0 新 className
- [ ] **R-544**：grep `KnotLogo|KnotMark|KnotWordmark` 仍 3 文件命中

### 端到端业务回归

- [ ] 进 admin → 切 models tab → tab_resources 加载
- [ ] **API Keys**：OpenRouter + Embedding 双 KeyInput + R-529 trailing "已填写/未填写" mono uppercase 0.06em + R-547 遮罩+trailing 共存 + 保存
- [ ] **3 个 Agent 模型分配**：clarifier/sql_planner/presenter 各选模型 + D4 hint plain span + 保存
- [ ] **Models Table**：R-531 thead R-480 闭环 + R-532 mono 0.06em + 5 字段列 + R-546 Model ID Mono + R-549 价格 $ 单位 + R-534 默认 chip + R-535 启用/禁用 + 操作按钮
- [ ] **R-536 Spinner 偿还**：保存按钮 loading 状态 Spinner 视觉清晰（T.sendFg 替代 #fff）
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Card grid 自适应
- [ ] **light + dark 双模式** — API Keys / Agent / Models / Spinner 视觉一致
- [ ] **⭐ Inset 8% 7 文件视觉一致性肉眼校验**（v0.5.14~20 + tab_resources）
- [ ] v0.5.0~v0.5.20 既有功能端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.21): tab_resources.jsx 视觉重构 — ⭐ Inset 8% 铁律进攻性扩张第八处（文件总数 6→7）+ thead R-480 + KeyInput mono + Model ID Mono + Hex 偿还 + 核爆级 props 守护 (R-521~R-550)` | npm build + R-539 7 文件验证 + R-537 hex 0 + R-548 核爆 props + R-547 安全感实测 + 端到端业务 + 三档窗宽 + 双模式 | 12 props + apiKeys 2 + agentCfg 3 + model 8 字段 byte-equal + Step 1→5 顺序 + R-548 前置签名守护 + R-540/R-550 字面严防 |
| 2 | `chore(v0.5.21): version bump 0.5.20→0.5.21 三处同步 (R-545) — LIMITS dict 不动` | 全闸门 + routes=72 + version=0.5.21 | R-545 三处同步 grep ≥ 3 |
| 3 | `docs(v0.5.21): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + ⭐ 视觉闭环扩张里程碑（项目单色化一致性 80% 深度）+ Inset 8% 铁律进攻性扩张 + R-546/547/548/549/550 守护立约 + Card/TagChip v0.6 Shared 移植加强` | 全闸门 | Loop Protocol v3 第 22 次施行 + 铁律进攻性扩张里程碑 + 80% 深度宣告 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→5 顺序不可换；**R-548 核爆级 props 守护**为 Step 1 起手前置闸门；**R-539 7 文件验证**为 Step 5 收尾闸门。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 22 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 30（v0.5.21 区段 R-521~R-550）
- ✅ **⭐ Inset 8% 铁律从"防御性稳固"转向"进攻性扩张"**（v0.5.20 R-511 "6 文件恒定" → v0.5.21 R-539 "文件总数 6→7 正式扩张"）
- ✅ **项目单色化一致性进入 80% 深度** — Stage 3 §3 里程碑宣告
- ✅ **v0.5.x 第二个 admin tab 子模块复刻**（v0.5.16 tab_access + v0.5.21 tab_resources）
- ✅ **R-484 'white' 字面残留偿还** — Spinner color="#fff" 2 处偿还
- ✅ **R-518 hex 余效验证 sustained** — v0.5.x 视觉治理硬指标
- ✅ **R-546 Model ID Mono 守护立约** — 技术元数据识别度
- ✅ **R-547 API Key 输入框安全感立约** — trailing 在 password 遮罩下可视
- ✅ **R-548 12 Props 核爆级守护** — 签名 byte-equal + Admin.jsx 挂载点 byte-equal
- ✅ **R-549 价格业务标签保留** — $ 单位严守
- ✅ **R-550 borderBottom byte-equal 闭环** — 与 tab_access 字面一致（设计语言铁律第三维度候选）
- ✅ **R-365 Shared 0 改动 sustained** — Input / pillBtn from utils/Shared 调用 byte-equal
- ✅ **R-520 业务字段不动 sustained** — 12 props + apiKeys 2 + agentCfg 3 + model 8
- ✅ R-227.5.1 装饰豁免延伸（thead 中文 + KeyInput trailing mono）
- ✅ R-302.5 emoji 业务豁免 sustained（本 PATCH 无 emoji）
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.21 九 PATCH sustained）
- ✅ R-396 列宽稳定性 sustained
- ✅ R-394 auto-fit 响应式 sustained
- ✅ **Card/TagChip helpers inline 第六次复用 sustained** — v0.6 Shared 移植承诺加强（累计 8+ inline helpers）
- ✅ 范围最小化 — admin/ 其他 3 子模块 + chat/ 7 子模块 + 8 核心非屏 + 18 屏 byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ **归档锁定手册** → 当前文件；分支 `feat/v0.5.21-admin-resources`
2. ✅ **R-548 核爆级 props 起手前置守护**：grep TabResources 签名 + Admin.jsx 挂载点双向标定
3. ✅ **R-539 7 文件验证收尾纪律**：git grep -F **7 文件命中** 验证
4. ✅ **R-537 R-518 hex 余效纪律**：Step 1 后立即 grep hex = 0
5. ✅ **R-549 价格业务标签起手前置探查**：业务标签字面 byte-equal
6. ✅ **R-550 borderBottom 字面闭环**：与 tab_access.jsx 字面一致
7. ✅ **R-547 API Key 安全感实测**：input password 遮罩 + trailing 共存清晰
8. ✅ **R-546 Model ID Mono 视觉校验**：mono vs sans 字体对比
9. ✅ **D4 plain span hint 守纪律**：诱惑很强但不碰
10. ✅ **里程碑叙事**：⭐ 视觉闭环扩张 / Inset 8% 进攻性扩张 / 80% 深度宣告
11. ✅ **技术债登记加强**：8+ inline helpers 移入 Shared 承诺
12. ✅ **范围抗诱惑**：不抽 Card / 不改 modals / 不动 Agent allocation TagChip / 不改 Model 默认 chip
13. ✅ **Stage 3 §3 80% 深度宣告**：CHANGELOG 必须标注

---

🛡️ **执行者收到 GO 信号；v0.5.x 第二个 admin tab 子模块复刻正式启动；Inset 8% 铁律进攻性扩张（6→7 文件）+ 80% 深度宣告 + R-484 'white' 残留偿还 + R-546/547/548/549/550 五大守护立约 + Card/TagChip v0.6 Shared 移植加强六大里程碑同时落地。**
