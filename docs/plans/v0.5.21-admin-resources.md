# v0.5.21 — (C5+) admin tab_resources 屏复刻 — Stage 1 草案

> **状态**：📝 **Stage 1 草案**（待 Stage 2 资深+Codex 评审 / Stage 3 v0.4 守护者终审）
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **里程碑**：**v0.5.x 第二个 admin tab 子模块复刻**（v0.5.16 tab_access + v0.5.21 tab_resources）+ **Inset 8% 闭环字面第八处扩展 — 文件总数 6 → 7**（铁律持续扩展深耕）+ **R-484 'white' 字面 残留偿还**（Spinner color="#fff" 2 处）

---

## 0. 关键事实（Stage 1 起手陈述）

**Demo 与现实业务模型相容** — tab_resources 视觉重构 + 业务字段 byte-equal 双轨：

| 维度 | Demo api-model.jsx (192 行) | 现实 tab_resources.jsx (85 行) |
|---|---|---|
| 业务 | 静态 mock (API Keys + 3 Agent + Models) | 真后端 (apiKeys CRUD + agentCfg 3 keys CRUD + models list) |
| 字段 | 静态 mock | apiKeys 2 keys + agentCfg 3 keys + model 8 字段 |
| Props | N/A | **12 props**（含 4 saving state + 4 handlers + 2 setters） |

**裁定**：业务字段 + 12 props + 9 handlers/setters byte-equal；视觉风格全面对齐 demo（hex 偿还 + thead R-480 闭环 + KeyInput mono trailing + Agent allocation TagChip）。

---

## 1. 决议表（D1-D8 + Q1-Q5 草案 — 待 Stage 2/3 终审）

| ID | 推荐 | 备注 |
|---|---|---|
| **D1** 范围 | A 整个 tab_resources.jsx | 单 tab 单文件；不动 Admin.jsx 内 props 传递 |
| **D2** thead R-480 闭环字面 | A | bg `T.bg` → `color-mix(in oklch, ${T.accent} 8%, transparent)`（**第八处扩展 — 文件总数 6 → 7**） + mono + 0.06em + uppercase + fontWeight 500 + T.subtext |
| **D3** KeyInput trailing 视觉升级 | A | "已填写/未填写" → mono uppercase letterSpacing 0.06em + T.success/T.muted |
| **D4** Agent allocation hint 视觉 | **B 保 plain span byte-equal** | demo 用 Tag chip；但现实 hint 是辅助说明非装饰；视觉一致性 vs 业务清晰度选 plain；TagChip 保留 sustained 但不强制用于此处 |
| **D5** Hex 偿还 | A `color="#fff"` → `color={T.sendFg}` | R-484 sustained — 严禁 'white' 字面；Spinner 2 处 |
| **D6** Model 默认 chip 视觉 | **B 保 T.accentSoft byte-equal** | 业务无影响；若改 brandSoft 8% 需 D5 决议；保守不动避免 chip 视觉密度过大 |
| **D7** LIMITS dict | A 不动（250 LIMIT 远未触达） | 85 → ≤ 150；远低于 250 |
| **D8** Card helper 抽取 | **B 保 inline byte-equal** | demo 抽 Card helper；现实 3 instances inline 简洁；admin/* 子模块不抽 Shared（R-365 sustained） |
| **Q1** TabResources 12 props 签名 byte-equal | A 严守 | T/models/apiKeys/setApiKeys/apiKeysSaving/onSaveApiKeys/agentCfg/setAgentCfg/agentSaving/onSaveAgentCfg/onToggleModel/onSetDefaultModel |
| **Q2** apiKeys 2 keys + agentCfg 3 keys + model 8 字段 byte-equal | A 严守 | openrouter_api_key/embedding_api_key + clarifier/sql_planner/presenter + id/name/provider/model_id/input_price/output_price/enabled/is_default |
| **Q3** Input from utils byte-equal | A R-365 sustained | type / placeholder / mono / trailing props 不动 |
| **Q4** pillBtn from Shared byte-equal | A R-365 sustained | pillBtn(T, true) primary 调用不动 |
| **Q5** v0.6 Shared 移植承诺 | A sustained | Card/TagChip helpers inline 第六次复用 — v0.6.0 一并移入 Shared |

---

## 2. 红线草案（R-521 ~ R-545，25 条）

### 契约守护（R-521 ~ R-526）

| ID | 内容 |
|---|---|
| **R-521** | `TabResources` 12 props 签名 byte-equal — T/models/apiKeys/setApiKeys/apiKeysSaving/onSaveApiKeys/agentCfg/setAgentCfg/agentSaving/onSaveAgentCfg/onToggleModel/onSetDefaultModel |
| **R-522** | apiKeys 2 keys byte-equal — `openrouter_api_key` / `embedding_api_key` |
| **R-523** | agentCfg 3 keys byte-equal — `clarifier` / `sql_planner` / `presenter` |
| **R-524** | model 8 字段 byte-equal — `id` / `name` / `provider` / `model_id` / `input_price` / `output_price` / `enabled` / `is_default` |
| **R-525** | Input from utils.jsx props byte-equal — `type / placeholder / mono / trailing / label / value / onChange`（R-365 sustained） |
| **R-526** | pillBtn from Shared.jsx 调用 byte-equal — `{ ...pillBtn(T, true), padding: '6px 14px' }`（R-365 sustained） |

### 视觉重构（R-527 ~ R-535）

| ID | 内容 |
|---|---|
| **R-527** | API Keys Card padding 16→20 + radius 10→12（与 demo 一致） |
| **R-528** | API Keys Card header — fontSize 14 + fontWeight 600 + letterSpacing -0.01em + desc lineHeight 1.55（与 demo Card 一致） |
| **R-529** | KeyInput trailing mono uppercase — fontSize 10 + letterSpacing 0.06em + T.success/T.muted（"已填写/未填写"） |
| **R-530** | Agent allocation Card padding/radius 一致 + 3-col `120px 1fr 90px` |
| **R-531** | Models Table thead bg `T.bg` → `color-mix(in oklch, ${T.accent} 8%, transparent)`（**R-480 闭环字面第八处扩展 — 文件总数 6 → 7**） |
| **R-532** | Models Table thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext |
| **R-533** | Models Table grid `1.5fr 0.7fr 1.4fr 1fr 0.7fr 100px` byte-equal（5 字段列 + 操作列） + minWidth 0 + ellipsis 兜底 |
| **R-534** | Models Row 默认 chip 保 `background: T.accentSoft, color: T.accent, fontSize: 9.5, padding: '1px 5px', borderRadius: 3, fontWeight: 600` byte-equal（D6 保守 — 业务无影响） |
| **R-535** | Models Row 状态 byte-equal — `m.enabled ? '启用' : '禁用'` + color `T.success / T.muted`（业务字段不动） |

### Hex 偿还 + 通用约束（R-536 ~ R-540）

| ID | 内容 |
|---|---|
| **R-536** | `color="#fff"` Spinner 2 处 → `color={T.sendFg}`（R-484 sustained — 严禁 'white' 字面） |
| **R-537** | grep `#[0-9a-fA-F]{3,6}` tab_resources.jsx \| grep -v boxShadow \| grep -v rgba = 0 命中（R-518 sustained — v0.5.x 视觉治理硬指标） |
| **R-538** | grep `'#fff'\|"#fff"` tab_resources.jsx = 0 命中 |
| **R-539** | **R-480 brandSoft 8% 闭环字面文件总数 6 → 7** — git grep -F 命中 ResultBlock/SavedReports/admin/tab_access/AdminAudit/AdminBudgets/AdminRecovery/**admin/tab_resources** 7 文件（铁律持续扩展深耕） |
| **R-540** | R-495 字面 byte-equal 严防 sustained — `git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)"` 字面无空格/逗号差异 |

### 范围 + 契约（R-541 ~ R-545）

| ID | 内容 |
|---|---|
| **R-541** | App/api/index.css/main/utils/Shared/Shell/decor/18 屏（含 Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery/tab_access）0 改 |
| **R-542** | admin/ 其他 3 子模块（tab_access/tab_knowledge/tab_system + modals）0 改 + chat/ 7 子模块 0 改 |
| **R-543** | App.css 0 行 diff + tab_resources 0 新 className |
| **R-544** | KnotLogo R-199.5/222 sustained 仅 Shared+Login+Shell 三文件 |
| **R-545** | 三处版本同步 0.5.20 → 0.5.21 + LIMITS dict 不动（85 → ≤ 150 远低于 250） |

---

## 3. 子步骤执行顺序（5 步小型 PATCH）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + **R-531 thead bg R-480 闭环字面率先落地**（文件总数 6 → 7）+ **R-537 hex 余效 grep 立即执行** | R-531/R-537/R-539 |
| 2 | API Keys Card padding/radius 升级 + Card header 14/600/-0.01em + KeyInput trailing mono uppercase | R-527/R-528/R-529 |
| 3 | Agent allocation Card padding/radius 升级 + 3-col grid 120/1fr/90px | R-530 |
| 4 | Models Table thead mono + 0.06em + uppercase + fontWeight 500 + T.subtext + Row minWidth: 0 + ellipsis 兜底 | R-532/R-533 |
| 5 | Hex 偿还 `color="#fff"` 2 处 → `color={T.sendFg}` + 三处版本同步 + R-540 字面严防 grep + 全 hex grep 闭门 | R-536/R-538/R-540/R-545 |

---

## 4. 关键代码意图（提前样化）

### R-531 thead R-480 闭环字面（第八处扩展 — 文件总数 6 → 7）

```jsx
// thead bg byte-equal v0.5.14/15/16/17/18/19/20 + 新增 admin/tab_resources
background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,
```

### R-529 KeyInput trailing mono uppercase

```jsx
trailing={
  <span style={{
    fontSize: 10, color: apiKeys.openrouter_api_key ? T.success : T.muted,
    fontFamily: T.mono, letterSpacing: '0.06em', textTransform: 'uppercase',
  }}>
    {apiKeys.openrouter_api_key ? '已填写' : '未填写'}
  </span>
}
```

### R-536 Spinner color hex 偿还

```jsx
// 前
<Spinner size={11} color="#fff"/>
// 后
<Spinner size={11} color={T.sendFg}/>
```

### R-531/R-532 Models Table thead 升级

```jsx
<div style={{
  display: 'grid', gridTemplateColumns: '1.5fr 0.7fr 1.4fr 1fr 0.7fr 100px',
  padding: '9px 16px',
  background: `color-mix(in oklch, ${T.accent} 8%, transparent)`,  // R-480 闭环第八处
  fontSize: 11, color: T.subtext, fontFamily: T.mono,
  fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase',
  borderBottom: `1px solid ${T.border}`,
}}>
  <div>名称</div><div>提供方</div><div>Model ID</div><div>单价(入/出)</div><div>状态</div><div></div>
</div>
```

---

## 5. 文件级改动清单（草案）

```
frontend/src/screens/admin/tab_resources.jsx  [EDIT 85 → ≤ 150 行]
                                              Step 1: baseline + R-531 thead R-480 字面落 + R-537 hex 余效 grep
                                              Step 2: API Keys Card padding/radius + header 14/600/-0.01em + KeyInput trailing mono
                                              Step 3: Agent allocation Card padding/radius + 3-col grid
                                              Step 4: Models Table thead mono+0.06em+500+T.subtext + Row ellipsis
                                              Step 5: Hex 偿还 (color #fff → T.sendFg) + 三处版本同步 + grep 闭门
                                              守护: 12 props + apiKeys 2 keys + agentCfg 3 keys + model 8 字段 byte-equal
                                                    + Input/pillBtn from utils/Shared 调用 byte-equal [R-525/R-526]
                                                    + inline styles only [R-543 CSS 0 污染]

knot/main.py                            [EDIT version 0.5.20 → 0.5.21]  [R-545]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.20 → 0.5.21]  [R-545]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.20 → 0.5.21]  [R-545]

scripts/check_file_sizes.py             [unchanged]  R-545 LIMITS dict 不动（tab_resources 85→≤150 远低于 250）
frontend/src/screens/admin/{tab_access,tab_knowledge,tab_system,modals}.jsx  [unchanged]  [R-542]
frontend/src/screens/chat/*             [unchanged]  [R-542]
frontend/src/screens/Admin.jsx          [unchanged]  [R-541 + Admin.jsx 内 TabResources 12 props 调用不动]
frontend/src/screens/{SavedReports,AdminAudit,AdminBudgets,AdminRecovery}.jsx [unchanged]  [R-541]
frontend/src/screens/ (其余 13 屏)       [unchanged]  [R-541]
frontend/src/{App,api,index.css,main,utils,Shared,Shell}.jsx 等  [unchanged]  [R-541 + R-365 sustained]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]  [R-541]
frontend/src/App.css                    [unchanged 0 行 diff]  [R-543]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js  [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.21 段]
                                        - R-521~R-545 偿还表
                                        - v0.5.x 第二个 admin tab 子模块复刻（v0.5.16 tab_access + v0.5.21 tab_resources）
                                        - Inset 8% 闭环字面第八处扩展（文件总数 6 → 7，admin/tab_resources 加入）
                                        - R-484 'white' 字面 残留偿还（Spinner color="#fff" 2 处）
                                        - R-518 hex 余效验证 sustained
                                        - Card/TagChip helpers inline 第六次复用 sustained — v0.6 Shared 移植承诺加强
                                        - Loop Protocol v3 第 22 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.21
                                        - v0.5.x 路线图 v0.5.21 ⏳ → ✅
                                        - v0.5.22+ 行更新（剩余 5 屏）
docs/plans/v0.5.21-admin-resources.md   [当前文件 Stage 1 → LOCKED 后存档]
```

---

## 6. 验收清单（草案）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**（CI 干净 env）
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（34 条 LIMITS 不变）
- [ ] `npm run build` 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.21**

### Stage 2/3 强化验证

- [ ] **R-539 闭环字面文件总数 6 → 7**：`git grep -F "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — **7 文件命中**（新增 admin/tab_resources）
- [ ] **R-537 hex 余效**：`grep -rE "#[0-9a-fA-F]{3,6}" tab_resources.jsx` = 0
- [ ] **R-538 '#fff' 0 命中**：grep `'#fff'\|"#fff"` tab_resources.jsx = 0
- [ ] **R-540 字面严防 sustained**：git grep -F 字面 byte-equal 无空格/逗号差异
- [ ] **R-531 thead bg byte-equal**：grep `background: \`color-mix(in oklch, \${T\.accent} 8%, transparent)\`` thead 区命中
- [ ] **R-529 KeyInput trailing mono uppercase**：grep `letterSpacing: '0.06em'.*textTransform: 'uppercase'` 命中 ≥ 2

### 契约守护（首要红线）

- [ ] **R-521** TabResources 12 props 签名 byte-equal
- [ ] **R-522** apiKeys 2 keys byte-equal（openrouter_api_key / embedding_api_key）
- [ ] **R-523** agentCfg 3 keys byte-equal（clarifier / sql_planner / presenter）
- [ ] **R-524** model 8 字段 byte-equal（id/name/provider/model_id/input_price/output_price/enabled/is_default）
- [ ] **R-525** Input from utils.jsx 调用 byte-equal
- [ ] **R-526** pillBtn from Shared.jsx 调用 byte-equal

### 范围守护

- [ ] **R-541** App/api/index.css/main/utils/Shared/Shell/decor/18 屏 + Admin/SavedReports/AdminAudit/AdminBudgets/AdminRecovery `git diff` = 0
- [ ] **R-542** admin/ 其他 3 子模块 + modals.jsx + chat/ 7 子模块 0 改
- [ ] **R-543** App.css 0 行 diff
- [ ] **R-544** KnotLogo sustained 仅 3 文件

### 端到端业务回归

- [ ] 进 admin → 切 admin tab → models tab → tab_resources 加载
- [ ] **API Keys**：OpenRouter + Embedding 双输入 + 已填写/未填写 mono uppercase trailing + 保存
- [ ] **3 个 Agent 模型分配**：clarifier/sql_planner/presenter 各选模型 + 保存
- [ ] **Models Table**：thead R-480 闭环 + 5 字段列 + 默认 chip + 启用/禁用 + 操作按钮（设默认 + 切换启用）
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Card grid 自适应
- [ ] **light + dark 双模式**
- [ ] **⭐ Inset 8% 7 文件视觉一致性肉眼校验**（v0.5.14~20 + tab_resources）
- [ ] v0.5.0~v0.5.20 既有功能端到端正常

---

## 7. Commit 序列（草案 — 3 commit）

| # | 内容 | 闸门 |
|---|---|---|
| 1 | `refactor(v0.5.21): tab_resources.jsx 视觉重构 — Inset 8% 闭环第八处扩展 (文件总数 6→7) + thead R-480 + KeyInput mono + Hex 偿还 (R-521~R-545)` | npm build + 全 grep 守护（hex 0/闭环 7 文件） + 端到端业务 |
| 2 | `chore(v0.5.21): version bump 0.5.20→0.5.21 三处同步 (R-545) — LIMITS dict 不动` | 全闸门 + version=0.5.21 |
| 3 | `docs(v0.5.21): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + Inset 8% 闭环字面第八处扩展（6→7 文件）+ R-484 'white' 残留偿还` | 全闸门 |

---

## 8. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审待启动（**第 22 次施行**预计）
- ✅ § Visual Replication Protocol 适用（v0.5.8 立约）
- ⏳ 等 Stage 2 资深 + Codex 评审 → Stage 3 v0.4 守护者终审 → LOCKED
- ✅ **v0.5.x 第二个 admin tab 子模块复刻**（v0.5.16 tab_access + v0.5.21 tab_resources）
- ✅ **Inset 8% 闭环字面第八处扩展 — 文件总数 6 → 7**（铁律持续扩展深耕；v0.5.20 R-511 "6 文件恒定" 是当时无新增；本 PATCH 自然扩至 7 是设计预期）
- ✅ **R-484 'white' 字面 残留偿还** — Spinner color="#fff" 2 处偿还
- ✅ **R-518 hex 余效验证 sustained** — v0.5.x 视觉治理硬指标
- ✅ **R-365 Shared 0 改动 sustained** — Input / pillBtn from utils/Shared 调用不动
- ✅ **R-520 业务字段不动 sustained** — 12 props + apiKeys 2 + agentCfg 3 + model 8 byte-equal
- ✅ R-227.5.1 装饰豁免延伸（thead 中文 + KeyInput trailing mono）
- ✅ R-302.5 emoji 业务豁免 sustained（本 PATCH 无 emoji）
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.21 九 PATCH sustained）
- ✅ R-396 列宽稳定性 sustained
- ✅ R-394 auto-fit 响应式 sustained
- ✅ 范围最小化 — admin/ 其他 3 子模块 + chat/ 7 子模块 + 8 核心非屏 + 18 屏 byte-equal

---

## 9. 风险点 + Stage 2/3 评审建议方向

### 高风险（建议 Stage 2/3 重点关注）

1. **R-521 TabResources 12 props 签名 byte-equal** — Admin.jsx 调用方 byte-equal（不动 Admin.jsx）
2. **R-522/R-523/R-524 业务字段** — apiKeys 2 + agentCfg 3 + model 8 严守；R-520 业务字段不动 sustained
3. **R-536 Spinner color 偿还** — color="#fff" → T.sendFg；T 已在 scope 内可直接使用

### 中风险

4. **D4 Agent allocation hint TagChip vs plain span** — demo 用 Tag chip；现实用 plain span。决策"保 plain"保守，守护者可能质疑视觉一致性？反驳：hint "推荐..." 是辅助说明非装饰，不需 chip 修饰
5. **D6 Model 默认 chip 保 T.accentSoft vs brandSoft 8%** — 保守不动，避免 chip 视觉密度过大；守护者可能质疑视觉一致性？

### 低风险

6. **D2 thead R-480 闭环** — v0.5.16/17/18/19/20 已验证模式
7. **D3 KeyInput trailing mono** — 与 v0.5.17 AdminAudit Filter Label 视觉一致
8. **D7/D8 LIMITS / Card helper** — 保守不动 sustained

---

📝 **Stage 1 草案完成 — 等待资深架构师 ack 启动 Stage 2/3 评审或简化协议直接 Stage 4。**

> **简化协议候选提示**：本 PATCH 红线 25 + 模式成熟（v0.5.16~20 thead R-480 闭环 + KeyInput mono + Hex 偿还）+ scope 小（85 → ≤ 150）+ 决策无争议 — **可考虑 v0.5.8 简化协议**（跳 Stage 2/3 直 Stage 4），但 R-539 文件总数 6 → 7 扩展是 v0.5.20 R-511 "6 文件恒定" 立约后首次扩展，建议守护者评审。资深架构师决定。
