# v0.5.18 — (C5+) AdminBudgets 屏复刻 — LOCKED (Stage 1+2+3 整合 → Stage 4 准备)

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行准备
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent
> **远古守护者**：v0.3 Agent（dormant）
> **协议**：Loop Protocol v3 — 三阶段评审闭环（**第 19 次施行**）
> **里程碑**：v0.5.x 第三个顶层屏复刻 + **Inset 8% 闭环第五处（铁律化覆盖 100% 后端管理资产屏）** + **borderLeft 3px 25% 第二处闭环** + **ActionChip/StatusDot/EnabledChip v0.6 技术债登记** + **Q1 部分聚合先例** + **D9 WarnNote 模式**

---

## 1. 决议表 + 风险项最终裁定（D1-D9 + Q1-Q5 全部锁定）

| ID | 锁定 | 备注 |
|---|---|---|
| D1 Hero usage card | ✅ A + **Q1 修订部分聚合** | Stage 2 强化 |
| D2 Form Label 双兼 | ✅ A | Stage 1 |
| D3 Table → CSS Grid | ✅ A | Stage 1 |
| D4 Emoji 全清 | ✅ A | Stage 1 |
| D5 Hex 全清 R-286 | ✅ A | Stage 1 |
| D6 Rules note brandSoft inset | ✅ A | Stage 1 + R-465 强化 |
| D7 LIMIT 380 | ✅ A | Stage 1 |
| **D8** ActionChip/StatusDot inline | ✅ **A 维持 inline + v0.6 技术债登记** | Stage 2 — R-365 绝对红线 |
| **D9** Warning UX 替代 | ✅ **brandSoft Warn 内嵌**（修订 Stage 1 推荐）| Stage 2 — T.warn 文字 + 14×14 inline svg 感叹号 |
| **Q1** Hero placeholder 数据源 | ✅ **部分聚合修订**（修订 Stage 1 推荐 A 全 —）| Stage 2 — 第 1 卡用 `budgets.length`；其余 3 卡 `—` + tooltip |
| Q2 Progress bar | ✅ A color-mix OKLCH | Stage 1 |
| Q3 Toast 文案 | ✅ A byte-equal | Stage 1 + R-464 强化 |
| Q4 R-23 实时性 | ✅ A 4 处 reload | Stage 1 |
| **Q5** v0.6 Shared 移植承诺 | ✅ A **技术债正式登记** | Stage 2 + Codex 记录 |

14/14 锁定；Stage 2 修订 Q1（全 — → 部分聚合）+ D9（emoji 删 → brandSoft Warn 内嵌 + svg 感叹号）+ 强化 D8 v0.6 偿债承诺；其余 Stage 1 保留。

---

## 2. 红线最终清单（R-431 ~ R-465，35 条）

### Stage 1 草案原 30 条（保留；Q1/D9 修订对应 R-440/R-448）

| ID | 摘要 |
|---|---|
| R-431 | AdminBudgetsScreen 5 props 签名 byte-equal |
| R-432 | 4 useState slots byte-equal |
| R-433 | api 4 endpoint URL byte-equal |
| R-434 | SCOPE_TYPES + BUDGET_TYPES + ACTIONS 3 常量 byte-equal |
| R-435 | SCOPE_HINT 3 keys byte-equal |
| R-436 | budget 7 字段 + draft 5 字段 byte-equal |
| R-437 | isLegacyScope/isBlockMisuse/canSubmit 业务守护 byte-equal |
| R-438 | 4 handlers 调用方式 byte-equal |
| R-439 | Topbar 删 💰 emoji |
| **R-440** | Hero usage card 4-stat grid + R-394 auto-fit + **第 1 卡 `budgets.length` 部分聚合**（Q1 修订）+ 3 卡 placeholder + tooltip + 进度条 + 0/50%/100% ticks |
| R-441 | Form labels D2 双兼 |
| R-442 | Form Submit + Reset 双按钮 |
| R-443 | Table HTML 全删 → CSS Grid 7-col |
| R-444 | **brandSoft 8% 闭环第五处** — 字面 byte-equal（5 屏命中铁律） |
| R-445 | BudgetActionChip inline helper |
| R-446 | EnabledChip inline helper |
| R-447 | Rules note 4 条 brandSoft inset + R-409 闭环字面 sustained + Tag chip |
| **R-448** | **Warning UX → brandSoft Warn 内嵌**（D9 修订）— T.warn 文字 + 14×14 inline svg 感叹号；emoji ⚠️ 删 |
| R-449 | Pagination 不加 |
| R-450 | submit btn `'#fff'` → T.sendFg；严禁 'white' |
| R-451 | action chip hex → color-mix(in oklch, T.warn 13%, transparent) |
| R-452 | grep hex 0 命中 |
| R-453 | emoji 全清 |
| R-454 | 范围守护（App/api/...）byte-equal |
| R-455 | admin/ 4 子模块 0 改 |
| R-456 | chat/ 7 子模块 0 改 |
| R-457 | App.css 0 行 diff |
| R-458 | KnotLogo 3 文件 sustained |
| R-459 | 三处版本同步 0.5.17→0.5.18 |
| R-460 | LIMITS 32→33 条 |

### Stage 2 新增 2 条（Codex 视觉对齐）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-461** | **Progress Bar 动效补偿** — 进度条虽 placeholder 0%，必须预留 `transition: width 0.3s ease-in-out` — v0.6 接入数据时平滑动效立即可用 | Hero usage progress bar inline style `transition: 'width 0.3s ease-in-out'`；commit 1 后 grep `transition.*width.*0\.3s` Hero 区命中 |
| **R-462** | **Form Grid 间距契约** — 5 字段 `gap: 16px`；auto-fit 换行时 Threshold + Action 尽可能同一行 | Form grid `gap: 16, gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))'`；Threshold + Action 字段加 `minmax` 约束 |

### Stage 3 新增 3 条（守护者强化）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-463** | **R-21 守护逻辑视觉不可见** — `canSubmit` 严禁被样式逻辑短路误伤；legacy/block misuse 时按钮**硬性** disabled + R-448 警告文案触发 | commit 1 后**强制**手测：① 输入 scope_value='legacy' → canSubmit=false + disabled + 警告显 ② action='block' + scope_type='global' → canSubmit=false + disabled + 警告显 |
| **R-464** | **CRUD 幂等性视觉反馈** — R-18 幂等"覆盖式更新"必须显 `'已更新'` 而非 `'已创建'`；handleSubmit 响应处理与后端同步 | 检查 handleSubmit 响应判断逻辑 byte-equal；commit 1 后**强制**手测：相同三元组(scope_type/scope_value/budget_type)二次提交 → toast 显 `'已更新'`；新三元组 → `'已创建'` |
| **R-465** | **Inset 8% 层级一致性** — Rules note borderLeft 固定 `3px` + 颜色严格 `color-mix(in oklch, T.accent 25%, transparent)` | grep `borderLeft.*3px solid color-mix(in oklch, \${T\.accent} 25%, transparent)` 命中；与 Original question quote（SavedReports v0.5.15 R-356）字面 byte-equal |

---

## 3. Stage 2/3 落地手段（关键执行纪律）

### Stage 3 §2 实施核心顺序（commit 1 子步骤 9 步锁死 — R-444 优先 Step 1）

| Step | 动作 | 红线 |
|---|---|---|
| 1 | baseline grep 标定 + LIMIT dict 新增 380 + **R-444 brandSoft 8% 闭环字面率先落地** | R-444/R-460 |
| 2 | Topbar 删 💰 emoji → "预算配置" | R-439 |
| 3 | Hero usage card 4-stat grid + R-394 auto-fit + **Q1 修订** 第 1 卡 `budgets.length` + 3 卡 `—` placeholder + tooltip + 进度条 + **R-461 transition 0.3s 动效补偿** + 0/50%/100% mono ticks | R-440/R-461 |
| 4 | Form labels D2 双兼 + **R-462 Form Grid gap 16 + minmax 约束** + Submit/Reset 双按钮 + emoji ✏️/➕ 删 | R-441/R-442/R-453/R-462 |
| 5 | Table HTML 全删 → CSS Grid 7-col + thead 视觉应用 R-444 brandSoft 8% | R-443/R-444 |
| 6 | BudgetActionChip inline helper（v0.5.17 R-411/R-426 模式 + 三件套 padding 2px 8px / radius 4 / fontWeight 500）| R-445 |
| 7 | EnabledChip inline helper（StatusDot pattern 6×6 圆 + on/off 文字）| R-446 |
| 8 | Rules note 4 条 brandSoft inset + **R-465 borderLeft 3px + color-mix 25%** + R-447 Tag chip + emoji 📌 删 + **D9 修订 R-448 brandSoft Warn 内嵌**（T.warn 文字 + 14×14 inline svg 感叹号 — 取代 emoji ⚠️）+ 全 hex grep | R-447/R-448/R-451/R-452/R-465 |
| 9 | 三处版本同步 + grep 闭门验证（hex 0 / rgba 0 / emoji 0 / R-444 闭环 5 文件） + **R-463 R-21 守护手测** + **R-464 CRUD 幂等 toast 手测** | R-459/R-463/R-464 |

### Stage 3 §3 验收关键 — Hero card 进度条视觉"呼之欲出"

```bash
# R-444 brandSoft 8% 闭环 5 文件 byte-equal
git grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/
# ResultBlock / SavedReports / admin/tab_access / AdminAudit / AdminBudgets — 5 文件命中

# R-465 borderLeft 3px 25% 闭环 2 文件
git grep "borderLeft.*3px solid color-mix(in oklch, \${T\\.accent} 25%" frontend/src/screens/
# SavedReports.jsx (R-356) + AdminBudgets.jsx (R-465) — 2 文件命中

# R-461 progress bar transition
grep "transition.*width.*0\.3s.*ease-in-out" frontend/src/screens/AdminBudgets.jsx

# hex 0 命中（除 boxShadow + rgba）
grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/screens/AdminBudgets.jsx | grep -v boxShadow | grep -v rgba
# = 0 行
```

### Stage 2 Q1 修订 — 部分聚合落地

```jsx
const stats = [
  { label: '已配置预算项', value: budgets.length, tooltip: null },  // ← Q1 修订：真实聚合
  { label: '本月已用 token', value: '—', tooltip: '后端聚合 API 对接中 (v0.6+)' },
  { label: '预计花费', value: '—', tooltip: '后端聚合 API 对接中 (v0.6+)' },
  { label: '本月使用率', value: '—', tooltip: '后端聚合 API 对接中 (v0.6+)' },
];
```

**架构判定**：第 1 卡提供有效信息（不误导）；其余 3 卡保后端契约预期；视觉信息密度提升。

### Stage 2 D9 修订 — brandSoft Warn 内嵌

```jsx
function WarnNote({ T, children }) {
  return (
    <div style={{
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      color: T.warn,
      fontSize: 12,
      lineHeight: 1.4,
    }}>
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
        <path d="M12 9v4M12 17h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
      </svg>
      {children}
    </div>
  );
}
```

用于 R-21 legacy / R-21 block misuse 警告显示。

### Stage 2 D8 + Q5 技术债登记

**CHANGELOG v0.5.18 段显式标注**：
> **技术债登记（v0.6 偿还承诺）**：BudgetActionChip + EnabledChip + StatusDot（v0.5.17 起累计第二次复用确认）将在 v0.6.0 首个 PATCH 移入 Shared.jsx，统一三处 inline 实现（AdminAudit StatusDot / AdminBudgets BudgetActionChip + EnabledChip）。
>
> 当前保留 inline 复制是 **R-365 (Shared 0 改动) 绝对红线 sustained** 所致；v0.6 起 Shared 解锁后即偿还。

### Stage 3 R-465 字面闭环

```jsx
// AdminBudgets v0.5.18 R-465
borderLeft: `3px solid color-mix(in oklch, ${T.accent} 25%, transparent)`,
// SavedReports v0.5.15 R-356（必须 byte-equal）
borderLeft: `3px solid color-mix(in oklch, ${T.accent} 25%, transparent)`,
```

**全站第二处 25% borderLeft 闭环** — 与 brandSoft 8% bg 闭环（R-444 5 处）并列两大设计语言铁律。

### Stage 3 R-463/R-464 双手测纪律

**R-463 R-21 守护**：commit 1 后**强制**：
- 输入 scope_type='global', scope_value='legacy' → 警告显 + Submit button disabled
- 输入 action='block', scope_type='global' → 警告显 + Submit button disabled
- 视觉重构后 canSubmit 逻辑**完全 byte-equal** — disabled 状态机硬性生效

**R-464 CRUD 幂等**：commit 1 后**强制**：
- 创建新预算（如 user/alice/monthly_cost_usd/10/warn）→ toast 显 `'已创建'`
- 提交相同三元组(user/alice/monthly_cost_usd)但不同 threshold → toast 显 `'已更新'`（R-18 幂等）
- 删除 → toast 显 `'已删除'`
- toggle enabled → 视觉切换 + 状态机正确

---

## 4. 文件级改动清单（最终版）

```
frontend/src/screens/AdminBudgets.jsx   [EDIT 232 → ≤ 380 行]
                                        Step 1: baseline + LIMIT 新增 + R-444 brandSoft 8% 闭环率先落 [R-444/460]
                                        Step 2: Topbar 删 💰 [R-439]
                                        Step 3: Hero 4-stat grid + Q1 部分聚合 + R-461 transition 动效 [R-440/461]
                                        Step 4: Form D2 双兼 + R-462 gap 16 + minmax + 双按钮 [R-441/442/462]
                                        Step 5: Table → CSS Grid 7-col + thead 闭环视觉 [R-443/444]
                                        Step 6: BudgetActionChip helper 三件套 [R-445]
                                        Step 7: EnabledChip helper StatusDot pattern [R-446]
                                        Step 8: Rules note brandSoft inset + R-465 borderLeft 3px 25%
                                                + D9 修订 R-448 WarnNote inline svg 感叹号
                                                + 全 hex grep [R-447/448/451/452/465]
                                        Step 9: 三处版本同步 + grep 闭门 + R-463 R-21 守护手测 + R-464 幂等 toast 手测
                                        守护: 5 props/4 state/api URL/3 常量/SCOPE_HINT/7+5 字段/3 守护/4 handlers byte-equal
                                              + inline styles only [R-457 CSS 0 污染]

scripts/check_file_sizes.py             [EDIT LIMITS 32 → 33]
                                        - "frontend/src/screens/AdminBudgets.jsx": 380 [R-460]
knot/main.py                            [EDIT version 0.5.17 → 0.5.18]  [R-459]
tests/test_rename_smoke.py              [EDIT R-72 smoke 0.5.17 → 0.5.18]  [R-459]
frontend/src/screens/Login.jsx          [EDIT 页脚字面 0.5.17 → 0.5.18]  [R-459]

frontend/src/screens/admin/*            [unchanged]   [R-455]
frontend/src/screens/chat/*             [unchanged]   [R-456]
frontend/src/screens/{Admin,SavedReports,AdminAudit,AdminRecovery}.jsx   [unchanged]  [R-454]
frontend/src/screens/ (其余 14 屏)       [unchanged]   [R-454]
frontend/src/{App,api,index.css,main,utils,Shared,Shell}.jsx 等   [unchanged]  [R-454]
frontend/src/decor/NarrativeMotif.jsx   [unchanged]   [R-454]
frontend/src/App.css                    [unchanged 0 行 diff]   [R-457]
requirements.txt / package.json / pyproject.toml / .importlinter / vite.config.js   [unchanged]
.github/workflows/ci.yml                [unchanged]
backend (knot/) 任何文件除 main.py      [unchanged]

CHANGELOG.md                            [EDIT [Unreleased] v0.5.18 段]
                                        - R-431~R-465 偿还表
                                        - **KNOT 视觉铁律宣告：Inset 8% 设计语言正式覆盖 100% 后端管理资产屏**（5 处闭环 — ResultBlock/SavedReports/DataSources/AdminAudit/AdminBudgets 字面 byte-equal）
                                        - **R-465 borderLeft 3px 25% 第二处闭环**（与 SavedReports R-356 字面 byte-equal）
                                        - **技术债登记**：ActionChip + StatusDot + EnabledChip v0.6.0 首个 PATCH 移入 Shared.jsx 偿还承诺
                                        - **Q1 部分聚合修订**：Hero 第 1 卡 budgets.length 真实信息；3 卡 — placeholder
                                        - **D9 brandSoft Warn 内嵌**：R-21 警告 emoji 删 → svg 感叹号 + T.warn 文字
                                        - Loop Protocol v3 第 19 次施行
CLAUDE.md                               [EDIT]
                                        - § v3 协议施行历史回顾表 +1 行 v0.5.18（Inset 8% 第五处铁律化覆盖）
                                        - v0.5.x 路线图 v0.5.18 ⏳ → ✅
                                        - v0.6 路线图新增"ActionChip + StatusDot + EnabledChip 移入 Shared.jsx 技术债偿还"承诺
docs/plans/v0.5.18-admin-budgets.md     [当前文件 LOCKED]
```

---

## 5. 验收清单（最终版）

### 必须全绿

- [ ] `pytest tests/ -v` ≥ **432 passed**
- [ ] `lint-imports` 7 contracts KEPT, 0 broken
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0（33 条 LIMITS）
- [ ] `npm run build` 通过 0 警告 0 error
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.18**

### Stage 2/3 强化验证

- [ ] **R-444 brandSoft 8% 闭环第五处**：`git grep "color-mix(in oklch, \${T.accent} 8%, transparent)" frontend/src/screens/` — ResultBlock/SavedReports/admin/tab_access/AdminAudit/**AdminBudgets** **5 文件**命中（铁律化覆盖）
- [ ] **R-461 Progress Bar 动效**：grep `transition.*width.*0\.3s.*ease-in-out` Hero 区命中
- [ ] **R-462 Form Grid 间距**：grep `gap.*16\|gap:\s*16` Form 区 + `minmax` 命中
- [ ] **R-463 R-21 守护手测**：legacy/block-misuse 场景下 Submit button disabled 硬性触发
- [ ] **R-464 CRUD 幂等 toast**：相同三元组提交 → `'已更新'`；新三元组 → `'已创建'`；删除 → `'已删除'`
- [ ] **R-465 borderLeft 3px 25% 闭环**：grep `borderLeft.*3px solid color-mix(in oklch, \${T\.accent} 25%, transparent)` 命中 + 与 SavedReports R-356 字面 byte-equal
- [ ] **R-452 hex 0 命中**：`grep -nE "#[0-9a-fA-F]{3,6}" AdminBudgets.jsx | grep -v boxShadow | grep -v rgba` = 0
- [ ] **R-453 emoji 全清**：grep `💰\|✏️\|➕\|⚠️\|📌\|✓\|○` AdminBudgets = 0
- [ ] **D9 修订 R-448**：WarnNote inline svg 感叹号 + T.warn 文字命中；R-21 警告区域无 emoji
- [ ] **Q1 修订 Hero**：第 1 卡显 `budgets.length` 真实数值；3 卡 `—` + tooltip
- [ ] **D2 双兼**：grep `作用范围 (Scope Type)` + `范围值 (Value)` + `预算类型 (Budget Type)` + `阈值 (Threshold)` + `超阈值动作 (Action)` 5 标签命中

### 契约守护（首要红线）

- [ ] **R-431** AdminBudgetsScreen 5 props 签名 byte-equal
- [ ] **R-432** 4 useState slots byte-equal
- [ ] **R-433** api 4 endpoint URL byte-equal
- [ ] **R-434** SCOPE_TYPES/BUDGET_TYPES/ACTIONS 3 常量字面 byte-equal
- [ ] **R-435** SCOPE_HINT 3 keys byte-equal
- [ ] **R-436** budget 7 字段 + draft 5 字段 byte-equal
- [ ] **R-437** isLegacyScope/isBlockMisuse/canSubmit 业务守护 byte-equal
- [ ] **R-438** 4 handlers 调用方式 byte-equal

### 范围守护

- [ ] **R-454** App/api/index.css/main/utils/Shared/Shell/decor/14 屏 + Admin/SavedReports/tab_access/AdminAudit/AdminRecovery `git diff` = 0
- [ ] **R-455** admin/ 4 子模块 0 改
- [ ] **R-456** chat/ 7 子模块 0 改
- [ ] **R-457** App.css 0 行 diff + AdminBudgets 0 新 className（除 cb-sb）
- [ ] **R-458** grep `KnotLogo|KnotMark|KnotWordmark` 仍 3 文件命中

### 端到端业务回归

- [ ] 进 admin → 切 admin-budgets nav → loading → budgets 加载
- [ ] Hero usage card：第 1 卡 budgets.length 真实数值 + 3 卡 `—` + tooltip + 进度条 0% transition 预留
- [ ] Form D2 双兼 Label/Placeholder 显示正确
- [ ] **R-463 R-21 'legacy' scope_value → WarnNote 显 + Submit disabled**
- [ ] **R-463 'block' + scope_type='global' → WarnNote 显 + Submit disabled**
- [ ] **R-464 创建 → '已创建' toast + list 更新**
- [ ] **R-464 相同三元组覆盖 → '已更新' toast（R-18 幂等）**
- [ ] **R-464 删除 → confirm → '已删除' toast + list 更新**
- [ ] EnabledChip toggle → on/off 状态机切换 + 视觉反馈
- [ ] **三档窗宽实测**（1024 / 1280 / 1920）— Hero grid + Form grid R-462 minmax 自适应
- [ ] **light + dark 双模式** — Hero / Form / Table / Chip 视觉一致
- [ ] **Inset 8% 5 屏视觉一致性肉眼校验**（ResultBlock / SavedReports / DataSources / AdminAudit / AdminBudgets）
- [ ] **R-461 进度条动效预留**：dev tools 改 width → 平滑 transition 立即生效
- [ ] v0.5.0~v0.5.17 既有功能（Shell/Login/ChatEmpty/Composer/ThinkingCard/ResultBlock/SavedReports/DataSources/AdminAudit）端到端正常

---

## 6. Commit 序列（最终锁定 — 3 commit；Stage 3 §2 子步骤内嵌 commit 1）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.18): AdminBudgets.jsx 视觉重构 — Inset 8% 闭环第五处铁律化 + Hero 部分聚合 + BudgetActionChip/EnabledChip + WarnNote + R-461~R-465 (R-431~R-465)` | npm build + 全 grep 守护（hex 0/emoji 0/闭环 5 文件）+ R-463 R-21 双场景 + R-464 CRUD 幂等三态 + R-461 进度条动效 + 三档窗宽 + 双模式 | 5 props/4 state/api URL/3 常量/SCOPE_HINT/7+5 字段/3 守护/4 handlers byte-equal + Step 1→9 顺序 + R-444 优先落地 + Q1 部分聚合 + D9 WarnNote |
| 2 | `chore(v0.5.18): version bump 0.5.17→0.5.18 三处同步 + LIMITS 32→33 新增 AdminBudgets 380 (R-459/460)` | 全闸门 + routes=72 + version=0.5.18 + 33 条 LIMITS | R-459 三处同步 grep ≥ 3 |
| 3 | `docs(v0.5.18): CHANGELOG + CLAUDE.md 路线图 + plan LOCKED + KNOT 视觉铁律宣告（Inset 8% 100% 覆盖后端资产屏）+ ActionChip/StatusDot/EnabledChip v0.6 技术债登记` | 全闸门 | Loop Protocol v3 第 19 次施行 + 视觉规范铁律化覆盖里程碑 + 技术债正式登记 |

每 commit 独立全闸门绿。commit 1 是 PATCH 重头戏 — Step 1→9 顺序不可换；**R-444 闭环字面率先落地**（沿用 v0.5.17 模式）。

---

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整（**第 19 次施行**）
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 35（v0.5.18 区段 R-431~R-465）
- ✅ **AdminBudgets = v0.5.x 第三个顶层屏复刻**（v0.5.15 / v0.5.17 / v0.5.18）
- ✅ **Inset 8% 闭环第五处 — 视觉铁律化覆盖 100% 后端管理资产屏**（v0.5.14 R-323 + v0.5.15 R-372 + v0.5.16 R-386 + v0.5.17 R-409 + v0.5.18 R-444 字面 byte-equal — 5 文件）
- ✅ **borderLeft 3px 25% 第二处闭环**（v0.5.15 R-356 + v0.5.18 R-465 字面 byte-equal）— 设计语言铁律第二维度
- ✅ **技术债正式登记** — ActionChip + StatusDot + EnabledChip v0.6.0 首个 PATCH 移入 Shared.jsx 偿还承诺
- ✅ **Q1 部分聚合先例** — Hero placeholder 模式进化（v0.5.16/17 全 — → v0.5.18 1 真+3 占位）；未来 PATCH 沿用
- ✅ **D9 WarnNote 模式** — emoji ⚠️ 偿还的新通用方案（inline svg 感叹号 + T.warn 文字 + brandSoft 内嵌）
- ✅ R-227.5.1 装饰豁免延伸（mono uppercase thead + Tag chip）
- ✅ R-302.5 emoji 业务豁免 sustained
- ✅ R-286 hex 0 命中扩展（v0.5.13~v0.5.18 六处 sustained）
- ✅ R-313 rgba 豁免边界 sustained（v0.5.11 boxShadow + v0.5.17 modal overlay；本 PATCH 无新增豁免）
- ✅ R-394 auto-fit 响应式 sustained
- ✅ D2 双兼模式延伸（v0.5.17 立约）
- ✅ 范围最小化 — admin/ 4 子模块 + chat/ 7 子模块 + 8 核心非屏 + 14 屏 + Admin/SavedReports/tab_access/AdminAudit/AdminRecovery byte-equal

---

## 8. 执行者纪律自检（13 条 — Stage 3 守护者建议照单收）

1. ✅ **归档锁定手册** → 当前文件；分支 `feat/v0.5.18-admin-budgets`
2. ✅ **Stage 3 §2 R-444 优先落地纪律**：Step 1 baseline 后**立即**落 R-444 brandSoft 8% 字面 — 沿用 v0.5.17 模式
3. ✅ **Q1 部分聚合修订严守**：第 1 卡用 `{budgets.length}` 真实数值；严禁 `—`；其余 3 卡 `—` + tooltip
4. ✅ **D9 WarnNote 模式落地**：inline `<WarnNote T={T}>` 组件 — 14×14 inline svg 感叹号 + T.warn 文字；用于 R-21 legacy + R-21 block misuse 两处
5. ✅ **R-461 progress bar transition 必加**：即使 0% 也必须含 `transition: 'width 0.3s ease-in-out'`
6. ✅ **R-463/R-464 双手测纪律**：legacy/block disabled + 创建/已更新/已删除 toast 三态
7. ✅ **R-465 borderLeft 闭环 byte-equal**：与 SavedReports R-356 字面严格一致；`git grep` 2 文件命中
8. ✅ **D8 技术债登记纪律**：CHANGELOG 显式标注 + CLAUDE.md v0.6 路线图同步
9. ✅ **R-452 hex 0 命中严守**：grep 0 命中（除 boxShadow + rgba）
10. ✅ **R-462 Form gap 16 + minmax**：三档窗宽实测 Threshold + Action 同行
11. ✅ **里程碑叙事**：CHANGELOG 显式 5 条标注（视觉铁律 / 25% 第二处闭环 / Q1 部分聚合 / D9 WarnNote / 技术债登记）
12. ✅ **范围抗诱惑**：不抽 BudgetActionChip 入 Shared / 不改 AdminRecovery / 不聚合后端 API — R-454/455/456/458 + D8 R-365 sustained
13. ✅ **5 屏 Inset 8% 视觉一致性肉眼校验**：5 处 brandSoft 8% 视觉感知一致

---

🛡️ **执行者收到 GO 信号；v0.5.x 第三个顶层屏复刻正式启动；Inset 8% 视觉铁律化 100% 覆盖后端管理资产屏 + borderLeft 3px 25% 第二处闭环 + 技术债正式登记 + Q1 部分聚合 + D9 WarnNote 模式五大设计先例同时落地。**
