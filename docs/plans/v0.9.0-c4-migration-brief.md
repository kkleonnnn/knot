# v0.9.0 C4 — pre-tenancy 存量迁移（守护者 Stage 4 复核包）

> 执行者 v0.9 Agent · 2026-07-22 · 承接 C1-C3 PASS（守护者已放行 C4，铁律「C4 必先于现网 rollout」）
> 本刀 = 把旧单库锚点 `data/knot.db` 迁进 tenant#1 目录 `data/tenants/1/knot.db` + 新建 `data/platform.db`。

## 0. TL;DR

- **载体**：新模块 `knot/repositories/tenancy_migration.py`（289 行 < cap 300）—— 从 tenant_repo 拆出（tenant_repo 回落 89 行纯 CRUD）。
- **接线**：`main.py` 启动序 ❷.5（`seed_default_tenant` 后、per-tenant `init_db` 前）；`KNOT_SKIP_STARTUP_MIGRATION` gate（测试期跳，防误迁 dev 库）。
- **策略**：`.backup()`（WAL-safe）COPY 锚点→target → 强校验（非空 + 表集合 + 关键表行数 + decrypt 烟测）→ 过才删锚点（备份 `.pre-tenancy.bak`）。COPY 非 move，校验不过 → 删 target 保锚点 last-good + raise 中止启动（fail-closed）。
- **对抗评审**：4 视角证伪 workflow（data-loss / state-machine / concurrency / verify-marker）+ synthesis，12 raw → **5 confirmed**，全部整合修复。5 rejected（针对旧稿，已被本稿 marker/.backup 化解）。
- **闸门**：ruff ✅ · import-linter 9/0 ✅ · check_file_sizes ✅ · 迁移专测 14 + 隔离 10 + doc-invariant 5 + core_purity 3 ✅ · 全量套件（见 §4）。

## 1. 迁移状态机（`_migrate_locked`，持 flock）

| 锚点 | target | 判据 | 动作 |
|---|---|---|---|
| — | — | db_dir='.'（target==锚点，测试） | `skip:same-path` |
| 无 | 无 | 全新部署 | `skip:fresh`（per-tenant init_db 建新库） |
| 无 | 有(完好) | 已迁完 | `skip:migrated` |
| 无 | 有(空/损坏) + 有 .bak | 疑似掉电未落盘 | **raise**（拒服务空库，人工从 .bak 恢复） |
| 有 | 无 | 首迁 | `migrated` |
| 有 | 有(残片/空 或 有 `.c4-migrating` 标记) | 本迁移残留 | `resumed`（覆盖重迁） |
| 有 | 有(用户数据 且 无标记) | 疑似违反铁律先上现网 | **raise**（拒覆盖，人工处置） |

- **完成信号** = 锚点消失（非 marker 存在性）；**`.c4-migrating` 标记** = 消歧「本迁移 crash 残留的 target」vs「prod 独立写入」。
- **`.pre-tenancy.bak`** = 回滚源（锚点的 WAL-safe 原子快照）。`uploads.db` 一并备份**不迁**（engine import 期绑数据根，relocation=v0.9.1）。

## 2. 对抗评审 → 5 confirmed → 修复对照

| # | 严重度 | 发现 | 修复 |
|---|---|---|---|
| 1 | high（部署相关） | 无跨进程串行 → 并发启动可服务**空** tenant#1（`_backup_db` connect 会把被删锚点重建为空库；empty-vs-empty 假过校验）。**synthesis 校准**：`if not bak.exists()` 保住 `.bak` → 真数据存活可恢复，非「全丢」 | **flock**（数据根 `.c4-migration.lock`）串行同节点；`_backup_db` src 不存在即 raise（不重建空库）；`_verify` 拒空表库；DEPLOY 补多副本「先单副本/init-container 迁移」 |
| 2 | medium（须先违反铁律） | `_target_has_real_data` 仅判 `users>1/conversations>0` → 配了数据源但只有 seed admin 的租户库被误判残片而覆盖 | **广判据** `_USER_CONTENT_TABLES`（conversations/data_sources/knowledge_docs/saved_reports/bi_reports/metrics/messages/user_sources）+ users>1；排除 seed 非空表（semantic_layer/prompt_templates/few_shots） |
| 3 | medium | `_decrypt_smoke` 取首个非空值 → legacy 明文/空占位让**错 master key false-pass**（fernet 对非 enc_v1 passthrough 不动 key） | **只探 `is_encrypted`（enc_v1:）真密文**，遍历所有加密列所有行 |
| 4 | low（测试/dev） | gate `setdefault`+`!="1"` → 外部预设非"1"值使 pytest 迁移真实 dev 库 | conftest **无条件直写** `="1"`（同 JWT_SECRET pin） |
| 5 | low（回滚源） | 备份重生按存在性非有效性 → crash 中途留半成品 `.bak` 永不重生 | `_backup_db_atomic`（temp + `os.replace`）—— `.bak`/uploads 均原子 |

**5 rejected（synthesis 判定 stale/无害）**：crash-after-copy 触发假 raise（已被 marker 化解）· 无 fsync（已用 `.backup()` + fsync）· copy2 漏 WAL（已弃 copy2）· verify 对 byte-copy 恒真（无独立危害，加空库拒收后闭合）· 「并发全丢」被 §1 校准为可恢复。

**未采纳的 synthesis 可选建议**：resume 前把 target renamed 到 `.pre-resume.bak`。理由：resume 只在「有标记（本迁移写的，源=锚点仍在）」或「无真数据残片」下 unlink → target ⊆ 锚点恒成立，unlink 不丢唯一数据；额外 `.pre-resume.bak` 徒增文件堆积。**请守护者裁定是否仍需**。

## 3. 铁律（守护者 Stage 4 立，已入 DEPLOY.md）

**C4 迁移必先于现网 rollout**。生产 tenant#1 `db_dir='tenants/1'` ≠ 锚点；裸 rollout = per-tenant init_db 起空库 + 旧数据锚点孤儿。自动迁移已内建闭合正常升级；DEPLOY 补首次升级「先单副本迁移再扩容」+ 回滚步骤（`.pre-tenancy.bak` 复位）。

## 4. 闸门证据

- ruff `knot/ tests/test_tenancy_migration.py` = All checks passed
- import-linter = **9 kept, 0 broken**（`core.crypto 仅 repositories/scripts/自身` KEPT —— tenancy_migration 在 repositories 层，合规）
- check_file_sizes = OK（169 backend auto-discover；tenancy_migration 289 < 300 无需 ACK；main.py 315 ACK bump 312→315）
- 迁移 14 + 隔离 10 + doc-invariant 5 + core_purity 3 = **32 passed**
- 全量套件：见 commit 前最后一次 `pytest tests/`（预期与 C1-C3 基线 1337 passed / 115 skipped 同量级 + 新增迁移测）

## 5. 请守护者裁定（首轮，已由 §6 回应）

## 6. Stage 4 守护者终审整合（revise-before-C5 → 已修，请复核）

守护者 6-agent 数据安全对抗（crash 注入矩阵 / Q2 unique-data / 状态机 / flock-fsync / verify-decrypt / 测覆盖，high effort）+ 亲读。裁定 **revise-before-C5**：crash-safety 硬不变量成立（AT1 穷举 10 死点全 REFUTED），但 3 must + 4 should。**全部已修**：

### Must-fix（block C5）

- **must#1 安全阀不全（AT5 blocking）**：`_target_has_real_data` 由 allowlist → **denylist** —— 检查除 seed-baseline `{users, semantic_layer, prompt_templates, catalogs, app_settings}`（**经验实测**：全新 tenant#1 仅这 5 表非空，2026-07-22 grounded）外**所有**表 + `users>1` + `users.totp_secret IS NOT NULL`。修的正是守护者的场景：admin 登录一次写 totp_recovery_codes/audit_log 而 users 仍=1 → 旧 allowlist 漏 → 覆盖丢 2FA/审计。**denylist 结构上不可能「漏表」**（schema 加新用户表自动纳入）。**grounded 修正守护者建议**：守护者列的 `catalogs`/`app_settings` **不能**加进「用户内容表」——实测 catalogs 迁移 seed id=1、app_settings 存 TOTP-rollout 标志，二者 fresh 即非空，纳入会误判全新 target → 阻断合法迁移；改置于 **排除集**。few_shots 确无 seed（守护者对，已含进 denylist 覆盖）。
- **must#2 Q2 marker 旁路（AT2 推翻执行者论证）**：接纳。resume 覆盖既有 target 前**先 `_backup_db_atomic` → `.pre-resume[.N].bak`**（`_next_free_bak` 绝不覆盖前次保全）。不再依赖被推翻的「marker ⟹ target⊆锚点」——回滚到 C1-C3 在 target 写入的唯一数据现在**可恢复**。
- **must#3 fsync 强制化（AT1+AT4）**：`_fsync_file` 严格（失败即 raise → 传播中止迁移、不删锚点）；删锚点前 `_backup_db` 的 target fsync 走此路径，非耐久卷（NFS/overlay/EIO）掉电 → fail-closed。`_fsync_dir` 对不支持目录 fsync 的 FS 容忍良性 errno、对 EIO/ENOSPC 仍 raise。

### Should-fix（并入本 revision）

- **#4 marker 生命周期**：`_clear_orphan_marker` 在 anchor-absent 分支清孤儿标记；叠加 must#2 保全 → 即便孤儿标记旁路阀，覆盖亦可恢复。
- **#5 损坏 target 无 bak**：skip:migrated 前 `_db_wellformed`（quick_check）**无论 bak 是否在**都 raise（去掉 `and bak.exists()` 短路）——绝不服务空/损坏库。
- **#6 bak 耐久屏障**：`_backup_db_atomic` os.replace 后 fsync 目录；删锚点后 fsync 数据根目录 → 非 ordered FS 掉电不丢 bak。
- **#7 测覆盖**：+9 测（14→23）—— 错 master key 中止 / 损坏 target 无 bak raise / flock 真并发 2 线程串行 / verify 表集+行数不符 / `_backup_db` src-missing / denylist 抓 2FA+审计（users=1，参数化 3 例）/ resume 保全 .pre-resume.bak / 孤儿标记清除。

### Notes（守护者承认、未强制）
decrypt-smoke 仅扫 data_sources（docstring 标注）· verify 对 byte-copy 近乎恒真（非空 guard 承重，docstring 标注）· 锚点迁移期只读（用 `.backup()` 非 checkpoint，docstring「只读」准确）· 跨节点 flock 不互斥（DEPLOY 已「先单副本/init-container 迁移」）。

### 闸门（revision 后）
ruff ✅ · import-linter 9/0 ✅ · check_file_sizes（tenancy_migration 363>300 → ACK：13 fn 单一内聚迁移关注点+防御纵深，拆分反损可读性）· 迁移 23 + 隔离 10 + doc 5 + purity 3 = 41 ✅ · 全量套件（commit 前跑）。

**请守护者复核 must#1-3 + should#4-7 是否闭合 → 放 C5（CHANGELOG + tag v0.9.0）。**
