# v0.8.0 — B6.1 compiler 片段级注入校验（语义激活门 · 安全必修）

> **执行者手册 · v2（Stage 3 终审整合后）**。Loop Protocol v3 完整三阶段：Stage 1 草案 → Stage 3 守护者
> （v0.7 Agent，7 路 grounded 核实 workflow `w5m7w45ka`）终审 = **ACCEPT WITH REVISIONS (major-revise)** →
> **本 v2 = 执行者整合终审意见 + 独立复核**（workflow `wm60u3ilp`，5 路 sqlglot 30.11.0 POC 逐条 CONFIRM 守护者
> 承重发现，含推翻执行者自己 v1 的 2 处 rationale）。待守护者 **diff 级 second pass** → 落 commit。
> **v0.8 = 业务模型重构级 MINOR，不适用简化协议 / R-LP-v3-EX-1。**
>
> **性质**：v0.7 遗留语义激活门 B6.1（`docs/plans/v0.7-closeout-backlog.md` L63）。安全承重。
> **grounded 基线**：main HEAD `46f1852`（v0.7.48）。所有 file:line 经 v1 起草 + Stage 3 守护者 + 执行者
> 复核**三轮**核实（R-137）。§9 = 逐条终审整合记录（供 diff review）。

---

## §0 背景与问题定义（grounded，行号已 Stage 3 订正）

### 0.1 flag 与激活门现状

- `KNOT_SEMANTIC_LAYER` 无 config 常量；查询管道 gate `_semantic_enabled()` 逻辑体在
  [`query_steps.py:123-125`](../../knot/services/query_steps.py)（`:122` 是签名行）+ monitor-fire gate
  [`monitors.py:99`](../../knot/api/admin/monitors.py)。**默认 off。**
- 激活门核心 = **误判≈0 + 命中≥90%**，v0.7.44/.45 重点 12 题**已达标**。离激活差：**B6.1（唯一安全必修）**
  + B6.2（eval 门禁）+ B6.4🔴（同比）+ B6.5/6.6🟡（叙述层）。

### 0.2 注入面 = **7 类 LLM 逐字 splice 片段**（Stage 3 补第 7 类 as_name）

| # | 片段 | splice / join 点 | provenance |
|---|---|---|---|
| 1 | `having` | [`compiler.py:108`](../../knot/services/semantic/compiler.py) `" AND ".join(str(h) ...)` | LLM |
| 2 | `qualify` | `compiler.py:154` `" AND ".join(str(q) ...)` | LLM |
| 3 | `lf.filters`（WHERE） | **build** `:202-205`(单)/`:252-255`(多) → **join** `:216`(单)/`:266`(多)；标量/派生路径另 [`compile_helpers.py:204`](../../knot/services/semantic/compile_helpers.py) + [`multi_base.py:74`](../../knot/services/semantic/multi_base.py) | LLM（lf.filters）+ admin（metric.filters 同 list） |
| 4 | window `partition_by` | `compiler.py:129` `", ".join(str(p) ...)` | LLM |
| 5 | window `arg` | `compiler.py:126,136` `str(w.get("arg"))` | LLM |
| 6 | window 内层 `order_by[].field` | `compiler.py:130-131`（v0.7.32 只护顶层 `lf.order_by`，未护窗口内层） | LLM |
| 7 | **⭐ window `as_name`** | **`compiler.py:136` 右半 `AS {w.get('as_name') or w.get('func')}`** | **LLM（v1 漏检；critical）** |

**第 7 类 as_name（Stage 3 critical 漏检，执行者 POC CONFIRMED）**：`as_name` 是 LLM 自由文本
（`parser.py:42` schema 无约束；`parser.py:56` alias 约束**只管 partition_by/order_by/arg，独漏 as_name**），
str() 逐字拼进外层 SELECT 投影列（`_finalize` `compiler.py:150-151`），且被 `compile_helpers.py:127` 下游**信任**。
POC 铁证（真 `_window_col`+`_finalize`+`_is_safe_sql`）：`as_name='rn, (SELECT MAX(bal) FROM otherdb.wallets)'`
→ 全句 `_is_safe_sql` 返 `(True,'')` **放行跨库子查询 exfil**；`as_name='rn -- '` 同样放行（注释截断）。
唯 stacked `rn; DROP` 被单语句规则拦。

**admin 信任面**（require_admin gated，scope 外 §1.4）：metric `caliber`（`compiler.py:194,236`）+ metric `filters`
（与 lf.filters 混入同一 where list `:197-201`/`:246-251`）。

### 0.3 既有已白名单片段（不动，byte-equal）

window func `_WINDOW_FUNCS`（`compiler.py:112`）· outer 聚合 `_outer_expr`（`:277-286`）· derived op
`_OP_SQL`（`compile_helpers.py:209`）· frame 边界 `_frame_bound` · frame func+日粒度 `_frame_clause` ·
**顶层** `lf.order_by` field（`_order_limit` `compile_helpers.py:126-134`，v0.7.32）· 笛卡尔守护 `_guard`/`is_cartesian`。

### 0.4 唯一现有兜底 = 全句 `_is_safe_sql`（[`doris.py:297`](../../knot/adapters/db/doris.py)）的真边界

sqlglot AST 只读校验（单语句 + 根 ∈ Select/Subquery/Union/With/Show/Describe + 拒写/DDL）。**三轮 POC 确认**
它 by-design 放行片段内嵌**只读**子查询 / `SLEEP()` / `UNION`-in-subquery / 跨库限定列 —— 这不是逻辑漏洞，
而是**只读闸门不保证 bounded-cost / scope**（设计空白，非 bug）。B6.1 补的正是这层。

---

## §1 决议（Stage 3 整合后）

### 1.1 核心设计：**保留**（守护者 §A grounded 背书 + 执行者复核）

结构化拦截为主（全片段，假阳性≈0）+ 别名白名单为辅（仅别名类片段）+ **放弃物理列白名单**（三依据全证实：
语义编译路径无列源；parser 对 filters 0 约束 → func 白名单必误杀；结构化拦截才是真价值）。admin 信任面 scope 外。
fail-closed = LLM 回退（`query_steps.py:199-201` handler，见 §1.7）。

### 1.2 校验器规则集（Stage 3 + 执行者 POC 重塑；fail-closed → 回退非报错）

新纯 leaf `knot/services/semantic/fragment_guard.py`。**抛自有异常 `FragmentUnsafe`**（不 import `CompileError`，
见 §1.6 / B-6），compiler catch 翻译成 `CompileError`。compiler 在各 LLM-片段 splice 前调用。

| 规则 | 适用 | 内容 | 依据（POC verdict） |
|---|---|---|---|
| **G0 注释 prescan** | 所有 LLM 片段 | **原始字符串**含 `--`/`#`/`/*`/`*/` → raise | **rationale 订正**：sqlglot 30.11.0 **不剥注释**，roundtrip 成 `/* */` 存 `node.comments`（v1 Finding-1「剥离」**REFUTED**）；真机制 = 逐字拼串后注释截断编译器自身 GROUP BY/LIMIT，故须 parse **前**扫原始串 |
| **G1 standalone parse** | 所有 LLM 片段 | sqlglot(dialect=mysql) **孤立解析**（**不包裹**）；parse 失败 → raise | **v1 G1「包裹解析」删除**：POC 证所有合法片段孤立解析 0 失败（B-3 CONFIRMED）；且 `SELECT..WHERE(<frag>)` 包裹会引入**自身外层 Select** → 100% 误杀 G2（self-FP 陷阱 CONFIRMED） |
| **G2 结构（子查询/UNION）** | 所有 LLM 片段 | walk AST 现 `exp.Select`/`exp.Subquery`/`exp.Union`/`exp.Intersect`/`exp.Except` → raise | 核心安全价值；孤立解析下注入子查询暴露为 Select/Subquery 节点（`(...)` 单纯括号是 `exp.Paren` 非 Subquery → 不误杀） |
| **G3 未识别函数** | 所有 LLM 片段 | `exp.Anonymous` → raise，**例外一个 version-pinned benign-Anonymous 白名单（含 `UNIX_TIMESTAMP`）** | POC：`SLEEP/BENCHMARK/LOAD_FILE/USER/CONNECTION_ID` = Anonymous（G3 抓）；但 **`UNIX_TIMESTAMP` 亦 Anonymous → 纯「拒 Anonymous」误杀合法过滤**（§E1 CONFIRMED）→ 须 benign 白名单 |
| **G4 typed 危险 denylist** | 所有 LLM 片段 | 拒 typed 危险函数：`CURRENT_USER`(CurrentUser)/`VERSION`(CurrentVersion)/`DATABASE`(CurrentSchema)/`SESSION_USER`(SessionUser)/`GROUP_CONCAT`(GroupConcat) | POC：这 5 个是 typed 节点、G3 抓不到（§E CONFIRMED）；`LOAD_FILE` 是 Anonymous 已归 G3（守护者原列 typed = 订正） |
| **G5 别名 + 限定符** | 别名类（having/qualify/window partition·arg·order·**as_name**） | Column：`.table==.db==.catalog==""`（**拒任何限定符**）且 name ∈ 作用域可见别名集；**as_name 更严 = 纯标识符 `^[A-Za-z_]\w*$`**（别名声明非表达式） | Finding 2（`otherdb.users.revenue` 末段命中 name 白名单）+ B-1（as_name 须标识符校验） |
| **G6 算子/函数（别名类）** | 别名类 | 比较 + 逻辑 + **算术 `+ - * /`** + 极小已识别 scalar 白名单；余 raise | Finding C（`having gmv/dau>0.5` 算术合法，compiler 逐字放行且 DQL 合法 → G6 须放行免误杀） |

**`lf.filters`（物理 WHERE）：仅 G0+G1+G2+G3+G4**，**不施 G5/G6**（物理列无 catalog 列源；parser 对 filters 0 func
约束 → 白名单必误杀 `DATE/CAST/LIKE`）。**外科作用域**：只校验 `lf.filters` 切片（build loop 内），**严禁**碰
metric-filter 切片 / caliber（否则误杀信任口径如 `o.status='paid'`/`COUNT(DISTINCT..)`）。

**G5 window 内层可见别名集**：按**内层子查询作用域** = `{lf.dimensions ∪ metric 别名}` 独立推导，不照抄
`_order_limit` 顶层规则（Finding 11：窗口内层跑内层子查询，列可见性不同）。

### 1.3 splice 外科作用域

WHERE list（`compiler.py:195-216` 单 / `:244-266` 多）交错信任 metric filters（`:197-201`）与 LLM `lf.filters`
（`:202-205`）→ 校验器只作用 `lf.filters` 切片。**多 base / 派生路径**：`lf.filters` presence 已被
`multi_base.py:34/:95` 硬 raise 回退 → guard 对 lf.filters content 只在单对象/多对象维度两 path 生效（不与 presence gate 冲突）；
但 metric.filters 仍经 `multi_base.py:74` + `compile_helpers.py:204` splice（Option A 括号须覆盖，§1.5 / B-5）。

### 1.4 admin 信任片段残余风险（接受 + 明文记录 — Stage 3 §C ratify）

caliber / metric filters 经 `require_admin`（`metrics.py:62,75`）+ `_reject_forbidden` OOS-1 锁 → 信任、scope 外。
残余：粗心/被盗 admin caliber（如 `SUM(o.x)+(SELECT MAX(bal) FROM otherdb.wallets)`）可注入跨表只读子查询，
G0–G6 与 `_is_safe_sql` 均不覆盖。**决议：接受 + 写进 fragment_guard docstring + CHANGELOG**；未来 admin 面开放
非资深角色 → 另开 metric-write-time caliber shape 校验 PATCH。

### 1.5 AND-join 优先级 seam + 括号（**Stage 3 裁定 Option A LOCKED**）

`lf.filters=["1=1 OR o.user_id IN (1,2,3)"]` 过 G0–G6，但 splice 成 `WHERE o.status='paid' AND 1=1 OR ...`
→ 优先级使信任 metric filter 被 OR 旁路（安全承重，sqlglot 实证）。逐片段校验器结构上看不到跨片段优先级。
**Option A（LOCKED）**：splice 时**括起每个片段**，覆盖**全 4 splice 点 + 3 子句类型**：
`compiler.py:216`(单WHERE)/`:266`(多WHERE) + `multi_base.py:74` + `compile_helpers.py:204` + having `:108` + qualify `:154`。
- 对无内部 OR 合法片段**语义恒等**（paren-strip 相等）= 受控 byte-equal 再基线，非行为变更。
- **blast radius（执行者 POC 精确）**：仅括 WHERE filter = **9 断言**（2 exact-`==` + 7 substring，跨 6 test 函数）；
  若一并括 having/qualify（推荐，一致性）≈ **11**（守护者原估）。其余 ~177 semantic+api 测试 0 破、无 checked-in golden。
- commit 3 独立提交使再基线 diff 隔离可审。

### 1.6 依赖 pin + CompileError 归属（Stage 3 裁）

- **sqlglot 上限 pin（§C LOCKED，纳本 PATCH）**：`requirements.txt` `sqlglot>=25.0.0` → `>=25,<31`（当前 30.11.0）。
  G3/G4 的 Anonymous-vs-typed 划分 + 注释 roundtrip 行为**版本相关 = 安全承重**，不拖独立 chore。
- **CompileError 归属（B-6 解）**：`CompileError` 定义在 `compile_helpers.py:31`。fragment_guard **抛自有
  `FragmentUnsafe`**，compiler splice 点 catch 翻译成 `CompileError` → fragment_guard **0 semantic-sibling import**
  → R-8.0-4 字面成立。

### 1.7 fail-closed = 回退（handler 行订正）

校验失败 → compiler raise `CompileError` → **`query_steps.py:199-201`**（`except compiler.CompileError as e:
return None, {...compile_error_reason...}`，v1 误引 `query.py:421-432`）→ LLM ReAct 回退。用户侧无错。
**「0 能力损失」限定为「相对当前 flag-off baseline」**（flag on 后若 guard 误杀「仅语义能编」题 → 回退 LLM 可能失败
= 相对 flag-on 有损失；故 §4.2 假阳性控制承重）。

### 1.8 fragment_guard 放置（Stage 3 裁：新 semantic leaf）

新建 `knot/services/semantic/fragment_guard.py`（fail-**closed** raise）。**不塞进 `sql_validator.py`**：后者是
fail-**open**（解析失败 return `False,''` 放行）+ tuple 返回 + fan-out 专用 + R-90/R-80 契约 —— posture 相反。
（驳 v1「跨层」措辞：二者同属 `knot.services` = intra-layer sibling；拒绝理由是**内聚 + 失败姿态冲突**非跨层。）

### 1.9 附带安全承重（同激活门，Stage 3 §E4 CONFIRMED，纳本 PATCH）

**auto-LIMIT substring bypass**（非片段校验器，但同激活门安全承重）：`doris.py:160-162`
`if "LIMIT" not in sql_upper` = **substring 判断** → LIMIT token 出现在 literal/comment/identifier/内层子查询
即跳过外层 `LIMIT 500`（POC 4 例全 UNBOUNDED）。**修**：`_has_top_level_limit(sql)` 用 sqlglot AST
`tree.args.get("limit") is not None`（复用已有 parse）。适用**所有** execute_query 路径（非仅语义）。

---

## §2 红线（byte-equal 与不变量）

- **R-8.0-1 byte-equal**：除 §1.5 Option A 括号再基线（~9-11 断言）外，所有查询编译 byte-equal（99 `test_compiler.py`
  + 12 验证 + 七刀）。校验器是**门**，永不改写 SQL；括号是 compiler splice 变更（已 §1.5 ratify）。
- **R-8.0-2 fail-closed = 回退非报错**：`CompileError` → `query_steps.py:199-201` → LLM 回退；严禁冒泡 5xx。
- **R-8.0-3 信任片段免验**：metric filters / caliber 切片严禁进校验器。
- **R-8.0-4 Contract 9 无环（CI 强制，B-7 修）**：commit 1 把 `knot.services.semantic.fragment_guard` **加进
  `.importlinter` Contract 9 `semantic-compile-acyclic` 的 `source_modules`**（现仅 compile_helpers+multi_base）→ 从
  docstring/人肉升为 CI 守护；fragment_guard 抛 `FragmentUnsafe`（不 import CompileError）保 0 sibling import。
- **R-8.0-5 crypto contract**：fragment_guard 仅 import sqlglot+stdlib，0 `core.crypto`。
- **R-8.0-6 OOS-1**：0 tenant_id；本 PATCH **强化** OOS-1（G5 拒 3 段跨库/跨 catalog 列引用）。
- **R-8.0-7 gate 鉴权**：不动 require_admin/get_current_user；不新增路由。
- **R-8.0-8 依赖**：sqlglot 上限 pin（既有依赖收紧非新增）；0 npm。
- **R-8.0-9 纯后端**：0 前端 → 不重建 `knot/static`。
- **R-8.0-10 审计/成本/async**：校验器同步纯函数（0 LLM/DB 调用），不触 metric CRUD 审计 / cost_service / async。

---

## §3 文件改动清单（Stage 3 补 multi_base + importlinter + doris auto-LIMIT）

| 文件 | 改动 | 类型 |
|---|---|---|
| `knot/services/semantic/fragment_guard.py` | **新建** leaf：`assert_safe_fragment(frag, kind, visible_aliases)` G0–G6 + `FragmentUnsafe` | 新增 |
| `knot/services/semantic/compiler.py` | 各 7 类 splice 前调 guard（having:108 / qualify:154 / lf.filters slice / window partition:129 / arg:126 / order:130-131 / **as_name:136**）；catch→CompileError；splice 加括号(:216/:266 + having/qualify)；修 `:107` 注释 | 改 |
| `knot/services/semantic/multi_base.py` | metric.filters splice `:74` 加括号（Option A 一致性） | 改 |
| `knot/services/semantic/compile_helpers.py` | `_scalar_subquery` splice `:204` 加括号；可能抽 window 内层可见别名集 helper | 改 |
| `knot/adapters/db/doris.py` | `_is_safe_sql` docstring(`:298`) 订正；**auto-LIMIT `:160-162` → `_has_top_level_limit` AST**（§1.9） | 改 |
| `.importlinter` | Contract 9 `source_modules` += `knot.services.semantic.fragment_guard`（B-7） | 改 |
| `requirements.txt` | `sqlglot>=25.0.0` → `>=25,<31`（§1.6） | 改 |
| `tests/services/semantic/test_fragment_guard.py` | **新建**：攻击语料全 raise/中和 + 合法语料全 pass（§4） | 新增 |
| `tests/services/semantic/test_compiler.py` | §1.5 括号再基线 ~9-11 断言 + guard-wired 回归 | 改 |
| `tests/.../test_semantic_byte_equal_corpus.py` | **新建 CI**：12 验证 LogicForm + 七刀 byte-identical 断言（防不可见回归） | 新增 |
| `tests/adapters/.../test_doris_limit.py`（或既有） | auto-LIMIT AST fix 回归（4 bypass 例 + 真 LIMIT/UNION LIMIT skip） | 新增/改 |
| 版本 4 源点 + CHANGELOG | §6 | 改 |

---

## §4 验收标准

### 4.1 安全（攻击语料全部被拦或中和 — 三轮 POC 实证）

| 攻击 | 载体 | 期望 |
|---|---|---|
| 内嵌只读子查询 / UNION | having/qualify/filters/**as_name** | G2 raise |
| 注释截断 `... -- ` / `# ` | 任意 LLM 片段 | G0 raise |
| 3 段跨库列 `otherdb.users.revenue` | 别名类 | G5 raise（限定符非空） |
| **as_name 携子查询/逗号逃逸** `rn, (SELECT..)` | window as_name | G5 raise（非纯标识符） |
| `SLEEP`/`BENCHMARK`/`LOAD_FILE`/`USER`/`CONNECTION_ID` | 任意 | G3 raise（Anonymous） |
| `CURRENT_USER`/`VERSION`/`DATABASE`/`SESSION_USER`/`GROUP_CONCAT` | 任意 | G4 raise（typed denylist） |
| 优先级 seam `1=1 OR ...` | filters/having/qualify | §1.5 A 括号中和（trusted 不被旁路） |
| auto-LIMIT bypass（LIMIT in literal/comment/subquery） | filters | §1.9 `_has_top_level_limit` 仍 append LIMIT 500 |
| 写/DDL/stacked | 任意 | G3/G2 + `_is_safe_sql` 兜底 |

### 4.2 byte-equal / 无假阳性（合法语料全 pass）+ 已知权衡明示

全 pass：`having=["gmv>10000"]`/`["gmv/dau>0.5"]`(算术)、`qualify=["rk<=3"]`、`partition_by=["city"]`、
window `arg="gmv"`、`as_name="rn"`(纯标识符)、`filters=["o.status='paid'"]`(物理列仅结构校验)、含
`DATE()/CAST()/LIKE/IN/COALESCE/**UNIX_TIMESTAMP**`(benign-Anonymous 白名单)、caliber `SUM(CASE..)`/`COUNT(DISTINCT..)`(信任免验)。
**已知假阳性权衡（docstring 明示，均 → LLM 回退非用户错）**：① 合法字符串字面量含 `--`/`#`（如 `o.note LIKE '%--%'`）
→ G0 误杀；② benign-Anonymous 白名单外的罕见合法 Anonymous 函数 → G3 误杀（版本相关，§1.6 pin 承重）。
`test_semantic_byte_equal_corpus` 断言 12 验证 + 七刀 byte-identical（除 §1.5 再基线）。

### 4.3 闸门

全套件绿（含新语料）· import-linter 9 contracts 绿（Contract 9 含 fragment_guard）· 4 源点版本 CI 绿 ·
`KNOT_SEMANTIC_LAYER` **仍 off**（本 PATCH 不开 flag；开 flag 待 B6.2/6.4/6.5/6.6 全清后独立评估）。

---

## §5 commit 序列

1. **`fragment_guard.py` leaf + `FragmentUnsafe` + 单测** + **Contract 9 source_modules 加 fragment_guard**（B-7）。0 compiler 接线 → 0 行为变更。
2. **接线 compiler + multi_base + compile_helpers**（7 类 splice slice-only，catch→CompileError）+ 修注释（`compiler.py:107`/`doris.py:298`）。
3. **§1.5 括号 + 再基线**（全 4 splice + having/qualify；~9-11 test 断言）——独立 commit 隔离再基线 diff。
4. **auto-LIMIT AST fix**（`doris.py` `_has_top_level_limit`，§1.9）+ 回归。
5. **CI 语料**（`test_semantic_byte_equal_corpus` byte-identical + 攻击语料）+ **sqlglot pin `<31`**（§1.6）。
6. **版本 4 源点**（`main.py` 0.7.48→0.8.0 · `version.js` · `README` 顶 · `test_rename_smoke` R-72）+ **CHANGELOG 顶**。

---

## §6 协议合规 + 版本

- **Loop Protocol v3 完整三阶段**：Stage 1 → Stage 3 守护者终审（major-revise）→ **本 v2 执行者整合 + 独立复核** →
  守护者 **diff 级 second pass**（守护者 §G 提议，无需再走完整 Stage 1）→ commit 序列。
- **Hybrid 起手（kk 2026-07-05 LOCKED）**：B6.1 与跨-MINOR 整体审核仪式并行。
- **版本 4 源点 ★CI**（§5 commit 6）+ CHANGELOG 顶 == main version。
- **v0.7 不变量带入**：crypto contract(R-8.0-5) / OOS-1(R-8.0-6 强化) / gate 鉴权(R-8.0-7) / 审计·成本·async(R-8.0-10)
  直接承重；视觉铁律 N/A（0 前端）。

---

## §7 自检（执行者）

- [x] 注入面 grounded 逐点核实 + **Stage 3 补第 7 类 as_name（POC CONFIRMED）**
- [x] `_is_safe_sql` 边界 grounded（放行子查询/SLEEP/跨库列/as_name exfil）
- [x] catalog 无列源 grounded → 砍物理列白名单
- [x] **执行者独立复核 workflow `wm60u3ilp`（5 路 sqlglot POC）逐条 CONFIRM 守护者承重发现**（含推翻 v1 自身 G0/G1 rationale）
- [x] fail-closed handler 行订正（`query_steps.py:199-201`）
- [ ] 守护者 diff 级 second pass（待）
- [ ] 落 commit 序列 → 全闸门绿 → PR

---

## §8 待 Stage 2/3 裁决清单（**Stage 3 已全裁，记录在此**）

1. §1.5 括号再基线 → **✅ Option A LOCKED**（blast radius 9-11）。
2. §1.6 sqlglot 上限 pin → **✅ 纳本 PATCH**（`<31`）。
3. §1.4 admin caliber 残余 → **✅ 接受 + 明文记录**。
4. fragment_guard 放置 → **✅ 新 semantic leaf**（posture 冲突，非跨层）。
5. catalog 结构化 `columns`（真列白名单前置）→ **defer v0.8.x**（系统内有列源 `doris.get_schema_structured`，仅
   未接入编译路径；HTTP 虚拟表天然 N/A）。
6. B6.2 eval ≥90% 门禁 → **v0.8.1 独立 PATCH**。

---

## §9 Stage 3 终审整合记录（供守护者 diff review — 逐条 finding → 执行者复核 verdict → 解）

| 守护者 finding | 执行者独立复核（POC） | 解（本 v2 落点） |
|---|---|---|
| **B-1** as_name 第 7 类漏检（critical） | ✅ CONFIRMED（V1：`_is_safe_sql` 放行跨库子查询 + 注释截断） | §0.2 补第 7 行 + G5 纯标识符校验 + §4.1 攻击语料 |
| **B-2** G0 机制（sqlglot 不剥注释、roundtrip `/* */`） | ✅ CONFIRMED；**v1 Finding-1「剥离」REFUTED** | §1.2 G0 rationale 重写 |
| **B-3** G1 无实证 + 包裹 self-FP 陷阱 | ✅ CONFIRMED（合法片段孤立解析 0 失败；包裹引入外层 Select 100% 误杀） | §1.2 **G1 改「standalone parse，不包裹」** |
| **B-4** 三处引用错行 | ✅ CONFIRMED（handler `query_steps.py:199-201`；splice `:216/:266`；flag `:123-125`） | §0.1/§0.2/§1.7 全订正 |
| **B-5** 漏 multi_base.py + compile_helpers WHERE splice | ✅ CONFIRMED（`multi_base.py:74`/`compile_helpers.py:204`；presence raise `:34/:95`） | §0.2/§1.3/§1.5/§3 补 |
| **B-6** CompileError 归属 → R-8.0-4 矛盾 | ✅ CONFIRMED（定义 `compile_helpers.py:31`） | §1.6 fragment_guard 抛 `FragmentUnsafe`，compiler 翻译 |
| **B-7** Contract 9 不覆盖 fragment_guard | ✅ CONFIRMED（source_modules 仅 compile_helpers+multi_base） | §2 R-8.0-4：commit 1 加进 source_modules |
| **§C fork ×4** | 采纳 | §8 全 LOCKED（A / pin / 记录 / 新 leaf） |
| **§D 措辞 ×4** | 采纳 | §0.4「只读闸门缺 bounded-cost/scope」· §1.2 G6「compiler 放行非 parser 祝福」· §1.7「相对 flag-off」· §8-5「有列源未接入」 |
| **§E1** UNIX_TIMESTAMP=Anonymous 误杀 | ✅ CONFIRMED（V3） | §1.2 G3 加 benign-Anonymous 白名单（含 UNIX_TIMESTAMP） |
| **§E** typed 危险函数集 + LOAD_FILE=Anonymous 订正 | ✅ CONFIRMED（V3：LOAD_FILE 归 G3；G4=CURRENT_USER/VERSION/DATABASE/SESSION_USER/GROUP_CONCAT） | §1.2 G3/G4 分工订正 |
| **§E2/E3** G0 字面 `--` 误杀 + G4 denylist 不完备 | ✅ 采纳 | §4.2 已知权衡明示 |
| **§E4** auto-LIMIT substring bypass | ✅ CONFIRMED（V4：4 例全 UNBOUNDED；修 = AST `_has_top_level_limit`） | §1.9 纳本 PATCH（commit 4） |
| **§F** 建议不同 sqlglot 版本再验 | 执行者复核用同 30.11.0（与守护者一致）；§1.6 pin `<31` 锁版本漂移 | 版本漂移升为 CI-pin 承重项 |

**执行者对守护者的一处 refinement（供 second pass 核）**：§1.5 blast radius 守护者估「11(3==+8substr)」——
执行者 POC 严格「WHERE filter 片段」读数 = **9(2==+7substr)**；差额 2 = HAVING/QUALIFY 断言，仅当括号也覆盖
having/qualify 时才变（本 v2 §1.5 推荐覆盖 → 回到 ~11）。非设计分歧，仅 scope 口径；已在 §1.5 写明两读数。
