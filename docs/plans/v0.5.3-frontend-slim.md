# v0.5.3 — 前端代码瘦身（C3）— LOCKED

> **状态**：✅ **Stage 1 & 2 & 3 APPROVED (GO)** — Stage 4 执行中
> **执行者**：v0.5 Agent
> **守护者**：v0.4 Agent（active 只读，PATCH 内 Stage 3 终审已完成）
> **远古守护者**：v0.3 Agent（dormant，PATCH 内未激活）
> **Loop Protocol**：v3 — 三阶段评审已闭环
> **整合源**：Stage 1 草案 + Stage 2（资深架构师 + Codex）+ Stage 3（v0.4 守护者）

---

## 0. 终审修订摘要（先看）

6/7 决策（D1/D2/D3/D5/D6/D7）与执行者 Stage 1 提议一致。
**D4 修订** — Admin tab 7 文件 → **4 文件按职责合并**（Stage 2 修订；执行者修订建议：归类按功能内聚，总数保 4 不变，内部 mapping 按 Admin.jsx 实际 tab 调整）。
新增 5 条红线（R-124~R-128），分别来自 Stage 2（2 条）+ Stage 3（3 条）。
3 条隐性指令落地：Chat.jsx 优先 / 每 commit 手测闭环 / D7 CI file-sizes 扩展。

## 1. 决议表最终裁定（D1-D7 全部锁定）

| ID | 锁定 | 与执行者提议 |
|---|---|---|
| **D1** 目录结构 | ✅ A 子目录化（`screens/chat/` + `screens/admin/`） | 一致 |
| **D2** ResultBlock 处理 | ✅ A 整体抽出（避免 prop drilling） | 一致 |
| **D3** SSE Handler 抽出 | ✅ A 独立纯函数文件 | 一致 |
| **D4** Admin Tab 粒度 | ⚠️ **B 按职责合并 4 文件**（Stage 2 修订） | 执行者原提 7 文件 → 改 4 文件 |
| **D5** 行数硬约束 | ✅ A 严格执行 ≤ 350 主 / ≤ 250 子 | 一致 |
| **D6** re-export 兼容 | ✅ 强制 ChatScreen / AdminScreen export 名 + props 0 修改 | 一致 |
| **D7** 检测工具链 | ✅ 扩展既有 `check_file_sizes.py` | 一致 |

**D4 落地映射建议**（4 文件不变，内部 mapping 按 Admin.jsx 实际 tab 内聚）：
- `tab_access.jsx` — Users + Sources（访问与连接）
- `tab_resources.jsx` — Models + Budgets（资源与预算）
- `tab_knowledge.jsx` — Knowledge + FewShots + Prompts（知识库三件套）
- `tab_system.jsx` — Catalog + Audit + Recovery（系统与治理）

## 2. 红线最终清单（18 条 R-111~R-128）

### Stage 1 草案原 13 条（保留）

| ID | 摘要 |
|---|---|
| R-111 | Chat.jsx ≤ 350；Admin.jsx ≤ 250；新建模块 ≤ 250 |
| R-112 | 不动 backend / package.json / .importlinter / Shell.jsx / SavedReports.jsx |
| R-113 | 7 contracts KEPT（前端不入 contract，但 import-linter 不破坏） |
| R-114 | `npm run build` 每 commit 后绿（替代前端无 unit test） |
| R-115 | 手测端到端：NL 查询 → SSE → 7 intent layout 全渲染 |
| R-116 | re-export ChatScreen / AdminScreen 名 + props 签名 0 修改 |
| R-117 | 7 intent layout 分支（MetricCard/Chart/RankView/RetentionMatrix/DetailTable 等）零行为变更 |
| R-118 | SSE handler 纯函数化，不引入新副作用 |
| R-119 | 432 backend tests 不受影响（前端纯改） |
| R-120 | routes=72 / version=0.5.3 |
| R-121 | live LLM eval 不影响 |
| R-122 | 新文件顶部 docstring 注 "v0.5.3: extracted from X" |
| R-123 | commit message 含源行号区间（archeology） |

### Stage 2 新增 2 条（资深架构师 + Codex）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-124** | **严禁全局状态变更** — 本 PATCH 仅结构重组；禁止把现有 props 传递改为 Redux / Context / Zustand 等全局存储 | grep 新建子模块**禁含** `useContext(` / `Provider` / `createContext` / `redux` 关键字（除非主文件原本就在用） |
| **R-125** | **强制构建校验** — 每个 commit 必须 `npm run build` 通过（前端无 unit test 替代闸门） | commit 节奏纪律：每 commit 后跑 `cd frontend && npm run build`，0 error |

### Stage 3 新增 3 条（v0.4 守护者）

| ID | 内容 | 落地动作 |
|---|---|---|
| **R-126** | **KNOT 品牌资产平移** — v0.5.0 修改的 CSV 导出文件名前缀 / Banner 文字等 brand assets 完整平移；**手测必含"导出 CSV 检查文件名"** | grep `knot[-_]` / `KNOT` 在拆分前后命中数一致；手测清单加一项 CSV 导出 + 文件名肉眼校验（如 `knot-report-*.csv`） |
| **R-127** | **错误边界平移** — v0.4.4 ErrorBanner / BIAgentError → 7 类 kind 映射的 UI 渲染逻辑严禁"逻辑蒸发"；ResultBlock / ThinkingCard 收到后端错误时仍正确渲染对应组件 | 拆分前 grep `BIAgentError` / `ErrorBanner` / `error.kind` 在 Chat.jsx 内的命中位置（标定迁移点）；手测清单加一项故意触发后端错误 → 前端 banner 正确显示 |
| **R-128** | **样式零修改** — 严禁拆分过程中改 CSS / Tailwind className | `git diff` 校验 className 字符串字面 diff = 0：拆分前后跑 `git diff --stat | grep -E '\.(css|jsx)$'` 后 review className 行；如必须 split JSX 行，确保 className 字面值 byte-equal |

## 3. Stage 2/3 三条隐性指令（落地手段）

### 3.1 Stage 3 §2：Chat.jsx 优先

> SSE 逻辑（D3）是实时交互心脏，最易受损 — 优先拆 Chat.jsx

**落地**：commit 顺序锁死 — **Chat.jsx 先于 Admin.jsx**。理由：SSE 出错前端立刻挂；Admin.jsx 是配置面板，挂了影响小。

### 3.2 Stage 3 §3：每 commit 手测闭环

> 每个 commit 后必须手动完成【提问 → 思考 → 结果渲染 → CSV 导出】闭环

**落地**：4 commit 中 2 个 refactor commit 后**强制手测端到端**（启动 backend + frontend dev server，浏览器跑一次 NL → SQL → 7 intent 渲染 → CSV 导出 → 关闭）。**自动化测试 mock 不到 SSE 稳定性**（与 v0.5.2 R-109 同精神）。

### 3.3 Stage 2 D7：CI file-sizes 扩展

**落地**：v0.5.2 已建脚本；本 PATCH 在 LIMITS dict 增加：

```python
LIMITS.update({
    "frontend/src/screens/Chat.jsx": 350,
    "frontend/src/screens/Admin.jsx": 250,
})
NEW_FRONTEND_MODULE_LIMIT = 250
```

## 4. 文件级改动清单（最终版 — D4 修订 + R-124~R-128）

```
frontend/src/screens/
├── Chat.jsx                     [EDIT] 925 → ≤ 350
│   - 主屏作"调度员"；保留 ChatScreen export 名 + props
│   - re-export ResultBlock 给可能的外部 import
└── chat/
    ├── ResultBlock.jsx          [NEW] ≤ 250  整体抽出 7 intent 分支（D2 整体不细分）
    ├── intent_helpers.js        [NEW] ≤ 80   intent → layout 映射纯函数
    ├── sse_handler.js           [NEW] ≤ 150  SSE 解析纯函数（D3，R-118 无副作用）
    ├── ChatEmpty.jsx            [NEW] ≤ 60   空状态
    ├── Conversation.jsx         [NEW] ≤ 100  会话视图
    └── ThinkingCard.jsx         [NEW] ≤ 50   思考卡片（R-127 错误边界平移点）
├── Admin.jsx                    [EDIT] 773 → ≤ 250
│   - 主屏作 tab 调度
│   - re-export AdminScreen
└── admin/
    ├── tab_access.jsx           [NEW] ≤ 250  Users + Sources
    ├── tab_resources.jsx        [NEW] ≤ 250  Models + Budgets
    ├── tab_knowledge.jsx        [NEW] ≤ 250  Knowledge + FewShots + Prompts
    ├── tab_system.jsx           [NEW] ≤ 250  Catalog + Audit + Recovery
    └── modals.jsx               [NEW] ≤ 200  UserFormModal + SourceFormModal

knot/main.py                     [EDIT] version 0.5.2 → 0.5.3
tests/test_rename_smoke.py       [EDIT] R-72 smoke 0.5.2 → 0.5.3
scripts/check_file_sizes.py      [EDIT] LIMITS +前端主 + frontend 新建 ≤ 250 阈值
CHANGELOG.md                     [EDIT] [Unreleased] v0.5.3 段（R-111~R-128 偿还表）
CLAUDE.md                        [EDIT] 关键路径表 +前端新建模块 + v0.5.x 路线图 v0.5.3 ⏳ → ✅
docs/plans/v0.5.3-frontend-slim.md [本锁定手册]
```

> **D4 注**：草案原 7 admin tab 文件 → Stage 2 修订为 **4 文件按职责合并**（D4 B 选项）。如实测 Admin.jsx 当前 tab 与 Access/Resources/Knowledge/System 归类不完全对应，**总数保 4 不变**，内部 mapping 按内聚原则调整。

## 5. 验收清单（最终版）

### 5.1 必须全绿

- [ ] `npm run build` 通过（**每 commit 后**，R-125）
- [ ] `pytest tests/ -v` = **432 passed / 112 skipped** 不变（前端纯改不影响后端）
- [ ] `lint-imports` 7 contracts KEPT
- [ ] `ruff check knot/` All checks passed
- [ ] `python3 scripts/check_file_sizes.py` exit 0
- [ ] `python3 -c "from knot.main import app; print(len(app.routes), app.version)"` → **72, 0.5.3**

### 5.2 Stage 3 强化手测（每 refactor commit 后）

- [ ] **R-115/121 端到端**：NL 查询 → 思考卡 → 7 intent 渲染（至少跑 MetricCard / Chart / DetailTable 3 类）
- [ ] **R-126 CSV 导出**：导出文件名前缀含 `knot` brand 关键字
- [ ] **R-127 错误边界**：故意触发后端错误（如断网） → 前端 ErrorBanner 正确按 kind 渲染
- [ ] **R-128 样式零差**：`git diff` 中 className 字符串字面 byte-equal

### 5.3 不破坏

- [ ] v0.5.0 R-67~R-79 + v0.5.1 R-80~R-93 + v0.5.2 R-94~R-110 测试全绿
- [ ] backend 0 修改（除 main.py version + smoke）
- [ ] requirements.txt / pyproject.toml / package.json / .importlinter 不动
- [ ] R-124 全局状态零变更（grep 子模块禁含 useContext/Provider/redux 等关键字 — 除非主文件原本就用）
- [ ] R-118 SSE handler 纯函数化无新副作用

## 6. Commit 序列（最终锁定 — 4 commit；Stage 3 §2 Chat.jsx 优先）

| # | 内容 | 闸门 | 关键守护 |
|---|---|---|---|
| 1 | `refactor(v0.5.3): Chat.jsx 拆 ResultBlock + intent_helpers + sse_handler + 视图组件 (R-111/116/117/118/124/125/126/127/128)` | npm build + 手测端到端 + CSV 导出 + 错误边界 | **优先拆 SSE 心脏**；className 0 diff |
| 2 | `refactor(v0.5.3): Admin.jsx 拆 4 tab 按职责合并 + modals (R-111/116/124/125/128)` | npm build + 手测 admin 配置面板 7 tab 切换 | D4 B 选项；4 文件不超 250 |
| 3 | `chore(v0.5.3): version bump 0.5.2 → 0.5.3 + check_file_sizes.py 加前端阈值 (D7)` | 全闸门 | scripts exit 0 |
| 4 | `docs(v0.5.3): CHANGELOG + CLAUDE.md 关键路径 + plan LOCKED 归档 (R-110 精神)` | 全闸门 | 路线图 ⏳→✅ |

每 commit 内部独立全闸门绿才进下一；**Chat.jsx 拆出错必须本 commit 内 amend 修**，严禁带入 commit 2。

## 7. 协议合规备注

- ✅ Loop Protocol v3 — 三阶段评审完整
- ✅ 守护者全程**只读**未触碰代码
- ✅ 远古守护者（v0.3）**dormant**未激活
- ✅ 红线总数 18（v0.5.3 区段 R-111~R-128）
- ✅ 重构原子性 — D6 强制 re-export，外部 import 0 修改
- ✅ 范围最小化 — 不动 backend / package.json / 公用组件库 / 全局状态

## 8. 给执行者（本 Agent）的执行注意

1. **归档锁定手册** → 本文件覆盖 DRAFT；commit 4 一起提
2. **commit 顺序绝对不可换**：Chat.jsx → Admin.jsx → version+CI → docs
3. **手测纪律**：每个 refactor commit 后实际启动 frontend dev server + backend 跑一次完整闭环
4. **R-128 className 守护**：每 commit 后 `git diff` review className 行
5. **R-124 全局状态守护**：拆分诱惑很强 — 本 PATCH 一律不碰
6. **D4 admin tab 归类核对**：Stage 4 起手前 grep Admin.jsx 当前 tabs，按内聚原则映射 4 文件
7. **R-127 错误边界标定**：Chat.jsx 拆分前 grep `BIAgentError | ErrorBanner | error\.kind` 标定迁移点
8. **范围抗诱惑**：拆分中可能看到很多"顺手优化"的诱惑（prop drilling 改 Context / 重复代码合并 / 给前端加 unit test）— 本 PATCH 一律不碰

---

**📋 状态**：本文档 **LOCKED**（Stage 4 起手归档，2026-05-10）。执行者按本手册实施。
