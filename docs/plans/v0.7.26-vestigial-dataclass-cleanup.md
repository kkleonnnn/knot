# v0.7.26 — vestigial dataclass 消歧清理（models/agent.py AgentResult + models/metric.py Metric）

> **简化协议**（资深 2026-07-02 ack：chore + 0 行为变更 + 0 新红线 + grounded 无争议 → 跳 Stage 2/3；镜像 v0.6.5.6 docs chore 简化先例）。整体审核 v0.6→v0.7 #23 spawn 的 follow-on。
> **0 新 R-SL**（纯删死代码；R-SL-187 仍留给 v0.7.27 维度中文标签）。

## 1. 背景 + 根因（grounded）

v0.7 语义层期间浮现**双定义混淆**：两个 dataclass 是从未被消费的 vestigial 副本，与真正使用的同名/同层定义并存，制造「哪个是真的」困惑。

- **`knot/models/agent.py` `AgentResult`**：query 路径实用的是 `knot/services/agents/sql_planner.py:42` 自定义的 `AgentResult`（`agent_module=sql_planner`，且 v0.7.23/.25 演进出 column_labels/dimension_cols/column_formats 呈现字段）。models 版是孤儿——v0.7.23 加字段时误改本副本 → `test_routing` 报 `AgentResult.__init__() got unexpected keyword argument` 才发现走错文件。
- **`knot/models/metric.py` `Metric`**：metric_repo 全程用 dict（schema.sql + base.py + `_COLS`/`_UPDATABLE`），dataclass 从没被构造过；v0.7.17 date_column + v0.7.25 unit 都没同步进这个 dataclass（stale）。**且从未 wire 进 `models/__init__.py`**（生来即死）。

## 2. grounded 死代码确认（R-137 — 删前核实）

| 死代码 | 确认证据 |
|---|---|
| `models/metric.py` `Metric` | `grep "\bMetric("` = **0 构造器**；`grep "models.metric"` = **0 引用**；不在 `models/__init__.py` import/`__all__`；文件仅含此一类 |
| `models/agent.py` `AgentResult` | 仅 `models/__init__.py` re-export（import + `__all__`），**0 真消费者**；`-> AgentResult` @ sql_planner.py:80/221 是 sql_planner **自己的**（自定义 L42）；sql_planner 0 import from models.agent |

- **保留**（未动）：`models/agent.py` 的 `AgentKind`/`VALID_AGENT_KINDS`（conversation.py + test_message_repo 用）/`ClarifierOutput`/`AgentStep`/`PresenterOutput`。
- **观察未动（出范围）**：`models/agent.py` 的 `AgentStep` 也无 `from ...import` 消费者（sql_planner 有自己的 AgentStep）—— 但本刀只清 AgentResult + Metric（资深 scope），AgentStep 留后续评估。
- `test_models_are_leaf.py` 只 `pkgutil.iter_modules` import 各子模块，不 assert 类存在性/`__all__` 内容 → 删 metric.py + 移 AgentResult 不受影响。

## 3. 改动

- **删** `knot/models/metric.py`（整文件，仅 `Metric`）。
- **删** `knot/models/agent.py` `AgentResult` 类（+ 加消歧注释：SQL Planner 结果形状在 sql_planner.py，勿在此重建）。
- **改** `knot/models/__init__.py`：移除 `AgentResult` import（agent import 块）+ `__all__`（`__all__` 项 -1）。
- **改** `tests/services/test_metric_cost_async_carriers.py`：`_METRIC_FILES` 去 `models/metric.py`（async-native 守护现覆盖 metrics.py + metric_repo.py；dataclass 纯数据 0 LLM，删之不失守护）。⚠️ **路径字符串引用（`Path(...).exists()`）— import-grep 未捕获、全套件跑出（R-137：sweep 靠 import/构造器/`models.metric`，漏 path-string；全跑兜住）**。

## 4. 验收

- ✅ `import knot.main` + `import knot.models` OK；`AgentResult ∉ knot.models.__all__`。
- ✅ 全测试套件 993 passed（models/__init__ 广泛被 import → 全跑）；4 failed 全非本刀：**#2 metric.py exists 已修**（本刀 test 更新）+ **#1 jwt failfast / #3 sql_planner no-api-key / #4 master-key failfast = 预存在 env-dependent**（git-stash baseline 复核 = 同 3 failed；本地 .venv 有 JWT_SECRET/KNOT_MASTER_KEY/api key → failfast 不触发 + 真 LLM 跑；干净 CI env 全绿）。
- ✅ import-linter **9 contracts KEPT**（models 仍纯叶子）。
- ✅ check_file_sizes OK（backend auto-discover 148→147，metric.py 删）。
- ✅ 5 源点 0.7.25→0.7.26 + static rebuild + CHANGELOG。
- ✅ 0 行为变更（纯删死代码 + 消歧注释；无 metric_repo/query 路径/sql_planner 逻辑改）。

## 5. commit 序列

- **C1 清理**：git rm metric.py + agent.py 删 AgentResult（+消歧注释）+ __init__.py 移除 import/__all__ + test_metric_cost_async_carriers.py `_METRIC_FILES` 去 metric.py（path-string 引用）。
- **C2 收官**：5 源点 0.7.25→0.7.26（main.py / version.js / README L16 / CHANGELOG demote+新[Unreleased] / test_rename_smoke R-72）+ static rebuild + plan §6。

## 6. 协议合规 / 收官记录

- **简化协议**（资深 ack）：跳 Stage 2/3；无守护者终审（chore + 0 行为 + 0 红线）。
- **版本排序**（资深拍）：本清理插队取 **v0.7.26**（ready 先 merge）；维度中文标签重编号 → v0.7.27（plan 文档已同步 rename `docs/plans/v0.7.27-dimension-labels.md`）。
- **package.json 冻结 0.0.0 不碰**。
- R-137：删前 grep 三查（构造器/引用/re-export）+ 读 sql_planner 确认自定义 + 读 test_models_are_leaf 确认不 assert 存在性。

_（C1/C2 commit hash + 验证结果 LOCKED 后填）_
