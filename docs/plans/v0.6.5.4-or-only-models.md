# v0.6.5.4 Stage 1 执行手册 — OR-only 模型目录清理

> **协议**：轻量 v3 + 保 Stage 3（删共享代码 cfg.MODELS + query.py + 测试 + migration；安全/回归敏感留守护者终审）。
> **基座**：worktree `or-only` @ branch `v0.6.5.4-or-only-models`（off main `dcc4de3` = v0.6.5.3）
> **产出方式**：执行者 workflow 4-agent 穷尽审计（引用扫 + 解析链 + DB/UI + 合成；542k tok）grounded；资深 2 scope 决策（加 migration + 改悬空串）。所有结论 file:line 实证。
> **触发**：内测验收发现 admin 选了"直连 Anthropic" Claude 但只有 OpenRouter key → 代码回退死掉的 `google/gemini-2.0-flash-001` → 404。资深裁定项目 **OpenRouter-only**：删非 OR 模型 + fallback 必须活 OR。

---

## §0 背景 — 根因（grounded）

`cfg.MODELS` 同时含**直连 provider**（无 "/"，如 `claude-haiku-4-5-20251001` provider=anthropic）与 **OpenRouter**（带 "/"，如 `anthropic/claude-haiku-4.5` provider=openrouter）两套。admin 选了直连套，但部署只配 OpenRouter key（无直连 ANTHROPIC_API_KEY）→ `query.py:_resolve_model` 命中 OR-only fallback → 硬编死模型 `google/gemini-2.0-flash-001`（OpenRouter 已下线，日志 "No endpoints found"）→ 404。**与 2FA / 重启无关**（重启 env 是另一回事，已查清）。

---

## §1 范围（资深拍板）

| # | 改动 | 文件 | 性质 |
|---|------|------|------|
| 1 | cfg.MODELS 删 6 直连 + 1 死 OR | `knot/config/settings.py` | 核心 |
| 2 | query.py fallback 死 OR → `cfg.DEFAULT_MODEL` | `knot/api/query.py:44` | 核心 |
| 3 | 5 处必破测试修复 | `tests/api/test_model_catalog.py` + `tests/services/test_sql_planner_cartesian.py` ×3 + `tests/services/test_sql_planner_async.py` | 测试 |
| 4 | 2 eval 默认死 OR → 活 OR | `tests/eval/test_eval.py:162` + `test_intent_accuracy.py:94` | 测试 |
| 5 | **孤儿幂等 migration**（资深拍：加） | `knot/repositories/base.py` init_db | DB |
| 6 | **悬空串治理**（资深拍：改） | `knot/api/admin.py:778` + `frontend/src/screens/AdminBudgets.jsx:44` | 治理 |
| 7 | 版本 v0.6.5.3→v0.6.5.4（5 源点 + CHANGELOG + build） | main.py / version.js / README / smoke / CHANGELOG | 版本 |

**OOS**：adapter 层死代码（anthropic_native/openai_compat 直连分支 + provider=='anthropic' prompt-cache 特判）—— OR-only 后不可达，但物理删风险高（factory 还服务 ollama/vllm；OR 走独立 OpenRouterAdapter）→ 不删，另开 PATCH 评估（守护者注意：勿顺手删）。

---

## §2 逐项精确改动

### ① cfg.MODELS 删 7 行（settings.py）
删 L26-31 六直连（`claude-haiku-4-5-20251001`/`claude-sonnet-4-6`/`gpt-4o-mini`/`gpt-4o`/`gemini-2.0-flash`/`deepseek-chat`）+ L40 死 OR `google/gemini-2.0-flash-001` + L25 "# 直接 provider 通道" 注释 + 改 L16-23 块注释（"6 direct 保留等 Day 28+"已被 OR-only 推翻）。
**保留 13 纯 OR**（全 provider=openrouter）：anthropic/claude-opus-4·sonnet-4·haiku-4.5 / openai/gpt-4o·gpt-4o-mini·o3-mini / deepseek/deepseek-chat·deepseek-r1 / qwen/qwen-2.5-72b-instruct·qwen-plus / z-ai/glm-4.5·glm-4.5-air / minimax/minimax-01。
**DEFAULT_MODEL(L51)** = `anthropic/claude-haiku-4.5` 已 OR + 在保留集 → 0 改。**PROVIDER_BASE_URLS/PROVIDER_API_KEYS 全不动**（OR 读 `["openrouter"]`；ollama/私有部署依赖）。
**无 KeyError**：业务全走 `MODELS.get()` 带 None 兜底；唯一裸下标 `orchestrator.py:90 cfg=MODELS[model_key]` 仅在 L88-89 先 `model_key=DEFAULT_MODEL`（保留集）后执行 → 安全。无代码按直连 key 名硬编分支（分支判 `model_cfg["provider"]`，与 key 名解耦）。

### ② query.py:44 fallback → cfg.DEFAULT_MODEL
`model_key = pref if "/" in pref else "google/gemini-2.0-flash-001"` → `model_key = pref if "/" in pref else cfg.DEFAULT_MODEL`（活 OR `anthropic/claude-haiku-4.5`，单一真相源，不播第二个孤立字面）。分支可达（OR-only 部署正命中，非死代码）。

### ③ 5 处必破测试
- `test_model_catalog.py:62` 硬断言 `"claude-haiku-4-5-20251001" in cfg.MODELS` → **反向断言** `assert "claude-haiku-4-5-20251001" not in cfg.MODELS`（OR-only 不变量守护，test 数不变；docstring 同步改 OR-only 语义）。
- `test_sql_planner_cartesian.py` 三处（L81/123/173）+ `test_sql_planner_async.py:97`：model_key `claude-haiku-4-5-20251001` → `anthropic/claude-haiku-4.5`。**关键坑**（守护者重点核入参名）：OR 分支不消费 `api_key` 参数（取 `openrouter_api_key`/`_app_key`/`PROVIDER_API_KEYS["openrouter"]`，sql_planner.py:95）→ cartesian 三处必须把 `api_key="fake"` 同步改 `openrouter_api_key="fake"`（否则 sql_planner.py:96 无 key 提前 return "未设置 OpenRouter API Key" 又破）；async:97 `api_key=""` 保持（断言 "未设置" 仍命中 OR 分支文案）。

### ④ 2 eval 默认
`test_eval.py:162` + `test_intent_accuracy.py:94` `EVAL_MODEL` os.getenv 默认 `"google/gemini-2.0-flash-001"` → `"anthropic/claude-haiku-4.5"`（被 _REQUIRES_KEY skipif 不破 CI；治 live opt-in 404）。

### ⑤ 孤儿幂等 migration（base.py init_db）
现存 DB 若有 model_settings 行 is_default=1 指向被删直连 key（资深本地用过）→ 删后 admin "默认"chip 消失（纯 UI；推理走 cfg.DEFAULT_MODEL + agent_model_config 不受影响）。
**幂等 migration**（init_db 末，schema 建表后）：
```python
conn.execute("DELETE FROM model_settings WHERE model_key IN (<7 删除 key>)")
row = conn.execute("SELECT COUNT(*) c, COALESCE(SUM(is_default),0) d FROM model_settings").fetchone()
if row[0] > 0 and row[1] == 0:   # 仅当有配置且无默认 → 兜底设 OR 默认
    conn.execute("INSERT INTO model_settings(model_key,enabled,is_default) VALUES('anthropic/claude-haiku-4.5',1,1) "
                 "ON CONFLICT(model_key) DO UPDATE SET is_default=1")
```
**测试隔离守护**：fresh/test DB 的 model_settings 为空 → `c==0` → 兜底**不触发**（绝不给 test DB 加行，防破测试隔离）；DELETE 在空表 no-op。幂等（重复 init 无副作用）。不动 v0.6.5.3 WAL 锁/rollout 逻辑。**重启行为透明（守护者 Stage 3 记录）**：若 admin 故意配模型但全 is_default=0（无默认），兜底会每次重启 re-assert OR 默认（与"无默认"意图相争）；接受 —— 应用本需默认（cfg.DEFAULT_MODEL fallback 在）、"无默认"非真实用例、属 UX 默认恢复非破坏。

### ⑥ 悬空串治理
`admin.py:778` budget seed 默认 `'claude-haiku-4-5'`（无时间戳、不在 MODELS、pre-existing 悬空）+ `AdminBudgets.jsx:44` 前端 reset 默认同串 → 均改 `'anthropic/claude-haiku-4.5'`（防 OR-only 后播种不可解析种子）。前端改 → commit 含 build。

---

## §3 红线
- **R-OR-1**：cfg.MODELS 删后 = 13 项纯 OR（grep settings.py MODELS 块 0 个 provider ∈ {anthropic,openai,gemini,deepseek}）；13 活 OR 白名单 byte-equal 保留，不误删。
- **R-OR-2**：DEFAULT_MODEL `anthropic/claude-haiku-4.5` + PROVIDER_BASE_URLS/PROVIDER_API_KEYS 6 项 byte-equal 不动。
- **R-OR-3**：全仓 `google/gemini-2.0-flash-001` 在 .py 业务+测试 = 0 命中（query.py:44 + 2 eval 清零；CHANGELOG/docs 历史散文豁免）。
- **R-OR-4**：5 必破测试全修 + 本地 pytest 绿；cartesian 3 处 `api_key→openrouter_api_key` 入参名同步（守护者重点核）。
- **R-OR-5**：migration 幂等 + fresh/test DB 兜底不触发（c==0 跳）；不破 v0.6.5.3 测试隔离修复。
- **R-OR-6**：adapter 层 + provider=='anthropic' prompt-cache 特判 0 改（OOS，勿顺手删）。
- **R-OR-7**：7 contracts KEPT（.importlinter 0 改）；77 routes 不变。
- **R-OR-8**：5 版本源点 0.6.5.4 + CHANGELOG count==1（demote v0.6.5.3）+ build bake。

---

## §4 验收闸门
- pytest 全绿（重点：test_model_catalog 全 8 + test_sql_planner_cartesian 全 7 + test_sql_planner_async 全 + test_llm_client_async L130/142 + test_orchestrator_async 全）。
- test_rename_smoke（app.version=='0.6.5.4'）+ test_doc_invariants（version.js===main.py + Shell 渲染 + CHANGELOG count==1）。
- grep 闸门：`google/gemini-2.0-flash-001` 0 命中 .py；settings.py MODELS 块 0 非-OR provider。
- check_file_sizes（settings.py/query.py 只减）+ ruff + lint-imports 7 KEPT + eslint（前端改）+ build。
- migration 守护测试：fresh DB init_db 后 model_settings 仍空（兜底不触发）+ 含 7 删 key 的行被 DELETE。

---

## §5 commit 序列
- **c1（后端核心）**：settings.py cfg.MODELS 删 7 + query.py:44 fallback + base.py migration + admin.py:778 悬空串。
- **c2（测试）**：5 必破测试修 + 2 eval 默认 + migration 守护测试。
- **c3（前端 + version.js）**：AdminBudgets.jsx:44 悬空串 + version.js 0.6.5.4。
- **c4（main.py + 版本）**：main.py version 0.6.5.4。
- **c5（build + docs）**：npm build + README + CHANGELOG（新 [Unreleased] v0.6.5.4 + demote v0.6.5.3）+ test_rename_smoke + 本 plan。

---

## §6 协议
轻量 v3 + 保 Stage 3（删共享代码 + 回归敏感）。资深授权跳 Stage 2。守护者重点核：① cartesian 入参名 api_key→openrouter_api_key ② migration 幂等 + test 隔离 ③ 13 OR 白名单不误删 ④ adapter 死代码不顺手删（OOS）⑤ 5 版本源点。整改后逐 commit `git show` 直读复核。
