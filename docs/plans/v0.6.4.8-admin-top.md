# v0.6.4.8 — UI v2 admin 顶层屏批（radius 归一化）（admin 屏群批 1/2）

> **协议**：轻量 v3（资深 ack 跳 Stage 2；守护者严格 Stage 3 + 逐 commit 直读 + 资深 :8000 live 终判）
> **铁律**：R-UI2-VRP + VRP grep-guard + **修正版本同步模型（4 无条件 ★CI 含 Login footer）**
> **基座**：worktree `v0648-admintop` @ branch `v0.6.4.8-admin-top`（off main `35f2d92` = v0.6.4.7）
> **源稿**：demo budget/recovery/admin-metrics/admin-errors/admin-history.jsx
> **目标**：AdminBudgets/AdminRecovery/AdminMetrics/AdminQueryHistory/AdminErrors（AdminAudit noop 跳过）

## §0 范围 + adversarial 评估（合批 2 PATCH 之批 1；结论：**1 真 delta + 4 noop**）

**摸底**（资深拍板「合批归一化」+「先 AdminAudit 摸底」）：AdminAudit 已完全 v2（noop，跳过）。本批 = admin 5 顶层屏。**逐屏 demo 交叉核对**（每屏对自身 artboard radius），发现 admin 顶层屏群**几乎已全 v2**（v0.5.17-19/v0.6.1.0 复刻时 radii 已匹配 v2 demo）。

**关键澄清（非 bug）**：产品 stat 卡 radius 看似不一致（`statCardStyle`=10 vs `KpiCard`=12），实为**各屏按自身 demo 正确**：demo errors/audit 用 stat-card 10（密集小卡）、demo metrics/recovery/budget 用 12（大卡/内容卡）。**无需统一**。

## §1 radius 全屏枚举（demo 交叉核对）

| 屏 | demo card radius | 产品现状 | 动作 |
|---|---|---|---|
| **AdminBudgets** | hero/form 12（budget.jsx L58/90）| L63/L113 cards 12 · chip 4 · bar 99 | **noop**（match demo）|
| **AdminRecovery** | KPI/chart/table 12（recovery L34/93/135）| L85/L109 cards 12 | **noop**（match demo）|
| **AdminMetrics** | KPI 12（admin-metrics L57）| KpiCard 12（Shared）+ rules note L100 = 8（inset）| **noop**（KPI match；rules note = brandSoft inset 8 stays）|
| **AdminErrors** | **KPI 10** + tables 12（admin-errors L45/52/64）| L47/L51 KPI 10 + L59/L73 tables 12 | **noop**（**demo 确认 stat 卡=10 正确**）|
| **AdminQueryHistory** | table 12（admin-history L56）| table card L105 = **10** | **⭐ 10→12（唯一真 delta）** |

**改动（1 站点）**：AdminQueryHistory.jsx L105 table card `borderRadius: 10 → 12`（`T.content + border + overflow hidden`，match demo admin-history L56 = 12）。

**stays（枚举 + 理由）**：
- AdminBudgets/Recovery cards 12（match demo）· chip 4/99（pill）
- AdminMetrics KpiCard 12（Shared，match demo）· rules note 8（brandSoft inset callout + borderLeft，非 data card，inset 类 stays — 同 InsightCard/quote 不动）
- AdminErrors KPI 10（**demo 确认 stat 卡=10**）· tables 12 · chip 999 · pagination btn 6
- AdminQueryHistory filter strip L71 = 8（insetBg 无 border filter 容器，inset 类非 bordered card；改成 bordered card = 结构 OOS）· control btn 6 · pre/code 6

## §2 保 byte-equal（不动守护）

- **stat 卡 radius 不统一保留**（errors 10 / metrics 12 各按自身 demo 正确，非 bug）
- **inset 类不动**（AdminMetrics rules note 8 + AdminQueryHistory filter strip 8 — brandSoft/inset 容器，非 data card）
- **thead T.bg 灰底 sustained**（全 admin 屏 v0.5.38；如有 table thead 不碰）
- 全业务逻辑 + 各屏 props 签名 byte-equal（5 屏 export name + props → App.jsx 0 diff）
- hex 0 sustained + brandSoft inset R-372/borderLeft 25% 字面不动

## §3 红线

| # | 红线 |
|---|---|
| R-UI2-AT-1 | 5 屏 export + props 签名 byte-equal → App.jsx 0 diff |
| R-UI2-AT-2 | diff = **仅 AdminQueryHistory L105 1 行 radius**（10→12）；4 屏 0 改（noop verified）|
| R-UI2-AT-3 | stat 卡 radius 不统一保留（errors 10 / metrics 12 各按 demo）；inset 类（rules note/filter strip 8）不动 |
| R-UI2-AT-4 | thead T.bg + brandSoft inset R-372 字面不动 |
| R-UI2-AT-5 | hex 0（grep 排 rgba = 0 sustained）|
| R-UI2-AT-6 | 全业务逻辑 byte-equal（各屏 handlers/api/state）|
| R-UI2-AT-7 | 各文件 ≤ LIMIT（radius 改 0 净行）；其他屏/App/Foundation/Shared 0 改 |
| R-UI2-AT-8 | R-PA-8 Phase B 字面 0 |
| R-UI2-AT-9 | **4 无条件版本同步**（main + smoke + README★ + Login footer R-181★）→ 0.6.4.8；Shell L43 不动（条件式，stays v0.6.4.2）|

## §4 验收
1. AdminQueryHistory table card radius 12；其他 4 屏视觉不变（已 v2）
2. `git diff origin/main -- frontend/src/screens/` = 仅 AdminQueryHistory + Login footer
3. hex 0 + npm build exit 0 + check_file_sizes OK + R-PA-8 0
4. **4 处版本 → 0.6.4.8**（main/smoke/README★/Login footer★）；Shell L43 stays v0.6.4.2
5. 资深 :8000 post-merge live（5 admin 顶层屏 radius 一致 — 卡片 12 / stat 卡 errors-10 / 内容卡 12）

## §5 commit 序列
- **commit 1**：AdminQueryHistory L105 radius 10→12 + 4 处版本同步 + 本 plan
- **commit 2（收官）**：npm build 静态 + CHANGELOG

## §6 协议合规
轻量 v3：执行者 Stage 1（adversarial demo 交叉核对 = 1 delta + 4 noop，stat 卡分类 + inset 守护）→ 守护者 Stage 3（adversarial 复核 noop 真实性 + demo 交叉核对 + 契约）→ 逐 commit 直读 → 资深 push/merge + :8000 live。合批 2 PATCH 之批 1（顶层屏）；批 2 = admin tab 群（v0.6.4.9）。
