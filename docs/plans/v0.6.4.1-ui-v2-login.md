# v0.6.4.1 — UI v2 login 屏复刻（逐屏复刻首屏）

> **Loop Protocol v3 — 全 v3 三阶段**（UI 复刻业务屏不简化）；守护者续任 v0.5（v0.6 同代）
> **设计源**：`/Users/kk/Documents/knot_demo_ui/v0.6/artboards/login.jsx`（设计代理）
> **先例**：v0.5.7 login pilot（R-170~186 模板）+ v0.6.4.0 Foundation刀（TOKENS_V2 + Btn/Tag）
> **基座**：worktree `v0641-login` @ branch `v0.6.4.1-ui-v2-login`（off main `8c5aec3`）

---

## §0 视觉 delta 实证（当前 Login.jsx v0.5.7 vs artboard v0.6）

当前 Login.jsx 已含 2-col grid + 叙事 panel（Knowledge·Nexus·Objective·Trace + headline + desc 已 byte 一致）+ sign in/欢迎回来 + TOTP 2 阶段 + footer。**delta 集中在右表单容器 + 背景**：

| 元素 | 当前 v0.5.7 | artboard v0.6 | 动作 |
|---|---|---|---|
| 背景 | 左 `T.bg` / 右 `T.content` 分屏 | **共享 `radial-gradient(ellipse at 22% 18%, brandSoft, transparent 55%), bgInset`** 跨两列、无分屏 | 改背景 |
| 右表单 | flat field 列 on T.content，maxWidth 380 | **浮动 card**（`border` + `radius 20` + `bgElev` + boxShadow），maxWidth 440，padding 52 | 包 card |
| 提交按钮 | 内联 `<button>` T.accent | **Btn primary lg** iconRight arrow（新 primitive）| 采纳 Btn |
| Field bg | `T.inputBg` | `bgInset`（= T.bg/codeBg 等价）| 微调 |
| NarrativeMotif | `inset 0` 满 panel | right 70% | 调位置 |
| 左 panel padding | 48px 60px | 56 | 微调 |

叙事 panel 内容（label/headline/desc）**已 byte-equal**，不动。

---

## §1 范围

### In-scope（视觉复刻 — 真实契约不动）
- **F1** 背景改共享 radial-gradient（brandSoft，TOKENS_V2/buildTheme 取色；跨两列）
- **F2** 右表单包浮动 card（border + radius 20 + bgElev + boxShadow light/dark 双值，per artboard）；maxWidth 380→440；Field bg → bgInset 等价
- **F3** 提交按钮采纳 **Btn primitive**（primary lg + iconRight arrow）——（D1：Btn 需 disabled/loading 支持）
- **F4** TOTP verify step 同步 re-skin（同 card 视觉 + Btn）
- **F5** NarrativeMotif 位置 + 左 panel padding 对齐 artboard
- **F6** footer R-181 → v0.6.4.1 + 收官三同步

### OOS（保留不动 — 真实契约/业务）
- ❌ LoginScreen export 名 + props `{ T, onLogin, onToggleTheme }` byte-equal（R-170；App.jsx mount 0 改）
- ❌ api.login + `need_totp` 2 阶段 + interim_token → totp.verify 业务链 byte-equal（R-171）
- ❌ error 文案（"用户名或密码错误"/"验证码错误"/"验证过于频繁…"/"验证失败"）byte-equal（R-172）
- ❌ remember localStorage + REMEMBER_TIP + Q3 title byte-equal
- ❌ 忘记密码 = 既有 toast「内测期间请联系管理员重置密码」（**已诚实非死链 — 保留**，R-PB-D-1）
- ❌ showPw 切换 / focused tracker / 主题切换 button / cb_token+knot_remember localStorage key
- ❌ 不动其他屏 / Shared / utils / App.jsx / Shell

---

## §2 决策点（执行者推荐，待 Stage 2/3）

- **D1 ⭐ Btn 提交按钮 disabled/loading**：当前 submit 需 `disabled={loading}` + loading spinner（"登录中…"/"验证中…"）。Btn primitive（v0.6.4.0 新建）签名 `{T,variant,size,icon,iconRight,onClick,style,children}` **无 disabled/loading**。推荐 **A 扩 Btn**（additive props `disabled`/`loading`，loading 时显 Spinner + 禁用 — primitives.jsx 改，benefits 全屏；form 内 Btn 默认 type=submit 可提交）。备选 B 自定义 button 匹配 Btn primary lg 视觉（不用 Btn 组件 — 但破"采纳 Btn"初衷 + 视觉漂移风险）。**推荐 A**。
- **D2 忘记密码**：保留既有 toast 版（诚实，非 artboard 装饰死链）。
- **D3 Btn iconRight**：Btn primitive iconRight 接 node → 传 `<I.arrow/>`（v0.6.4.0 新增图标）。当前用 `<I.send/>`；artboard 用 arrow → 改 arrow（视觉对齐 artboard）。
- **D4 LIMIT**：当前 270；card 包裹 + Btn 采纳后预估持平/略增 → 评估（资深 ack）。

---

## §3 红线（R-PB-L-1~7 — 执行者立，待终审）

| 红线 | 内容 |
|---|---|
| **R-PB-L-1 契约 byte-equal** | LoginScreen export 名 + 3 props 签名 `{ T, onLogin, onToggleTheme }` byte-equal → **App.jsx 挂载点 git diff 0 行** |
| **R-PB-L-2 业务链 byte-equal** | api.login + need_totp 2 阶段 + interim→totp.verify + cb_token/knot_remember localStorage + onLogin(user) 行为 byte-equal |
| **R-PB-L-3 文案 byte-equal** | error 4 文案 + REMEMBER_TIP + 忘记密码 toast + 标题（sign in/欢迎回来/verify/验证身份）+ placeholder byte-equal |
| **R-PB-L-4 VRP per artboard** | 视觉对齐 artboard v0.6（card + radial-gradient + Btn primary lg + bgInset field）；色彩走 buildTheme/TOKENS_V2（0 hex 裸值，沿用 R-286/R-372；error oklch 红字面保留）|
| **R-PB-L-5 Btn 采纳** | 提交用 primitives.jsx Btn（D1 扩 disabled/loading）；iconRight `<I.arrow/>` |
| **R-PB-L-6 范围隔离** | 其他屏 + Shared + utils + Shell + App.jsx 0 改（除 D1 primitives.jsx Btn 扩 additive）；R-PB-SH-12 若动 Shared 则同步（本 PATCH 不动 Shared）|
| **R-PB-L-7 R-181 + R-PA-8** | footer v0.6.4.1 三同步（main.py+smoke+Login）；R-PA-8 0；R-185 KnotLogo DOM 哨兵 sustained |

---

## §4 验收标准

1. login 屏视觉对齐 artboard（card 浮动 + radial-gradient + Btn primary lg）— 部署后 knot.0p.oh light/dark
2. `git diff origin/main -- frontend/src/App.jsx` = 0（R-PB-L-1）
3. 登录链路：正常登录 + need_totp 2 阶段 + error + remember + showPw + 忘记密码 toast 全 byte-equal（R-PB-L-2/3）
4. Btn primitive 采纳（disabled/loading 生效）+ npm build exit 0
5. 其他屏 + Shared + utils + Shell 0 改（R-PB-L-6）
6. check_file_sizes OK + R-PA-8 0 + R-181 v0.6.4.1 三同步

---

## §5 commit 序列（2 commit）

- **commit 1**：login 复刻 — D1 扩 Btn（primitives.jsx disabled/loading additive）+ Login.jsx 视觉复刻（背景 radial-gradient + card 包裹 + Btn 采纳 + TOTP re-skin + Field bgInset + motif/padding）+ check_file_sizes（Login LIMIT + primitives）+ §9 LOCKED。验证：build + diff App.jsx 0 + 业务/文案 byte-equal + 其他屏 0 改
- **commit 2**：收官 — version 0.6.4.1 三同步 + README L11/CLAUDE 路线图 UI v2 复刻进度 + CHANGELOG + 静态重建

---

## §6 协议合规

**全 v3 三阶段**（UI 业务屏复刻）：Stage 1 → Stage 2 辅助初审（重点 D1 Btn 扩 + 业务/契约 byte-equal + VRP per artboard）→ Stage 3 守护者终审（v0.5 续任，守 R-170~186 login pilot 先例 + R-PB-SH-12 若动 Shared）→ Stage 4。无远古。

---

## §7 自检（Stage 1 草案）
- ✅ 设计源 + 先例锚定（artboard login.jsx + v0.5.7 pilot）
- ✅ 视觉 delta 实证（叙事 panel 已 byte-equal，delta 集中 card + 背景 + Btn）
- ✅ 契约/业务/文案 byte-equal 立约（R-PB-L-1/2/3）
- ✅ Btn 采纳（首个真实消费 v0.6.4.0 primitive）
- ⏳ 待 Stage 2 + 守护者 Stage 3 + 资深拍板（D1 Btn 扩）

## §8 待人测（部署后 knot.0p.oh）
login light/dark：card 浮动 + radial-gradient + Btn primary lg + 正常登录 + TOTP 2 阶段 + error + remember + showPw + 忘记密码 toast + footer v0.6.4.1

## §9 LOCKED（Stage 2 + Stage 3 终审 + 资深拍板整合 — 2026-06-17）

> **Stage 2 进步**（抓到 type=submit 不破 form / LIMIT 295 合理 / Q2 共享 card / Q3 z 层级 / R-185 哨兵）**但 3 处纠**：① D3 复用 v0.6.4.0 "viewBox 锁死" 旧幻觉（实为 additive 0 重绘）② Q4 "0 oklch 裸值" 过头（color-mix in oklch 是 R-286/R-372 铁律非违规；boxShadow rgba R-313 豁免）③ `_active_totp_step` 编的变量名（实为 `step`）。降级，契约由执行者实证 + 守护者钉死。

### §9.1 治理 = A 续任 v0.5 守护者（v0.6 同代；无整体审核/远古）

### §9.2 ⭐ D1 Btn 扩展规范（守护者命门 — primitives.jsx additive +3 props）
Btn 扩 **3 additive prop**（守护者修订 — 非草案 2 个）：
- `disabled`（true → `cursor:not-allowed` + `opacity:0.5` + 原生 disabled）
- `loading`（true → 内部 `<Spinner size={14} color={当前 fg}>` 替 children + disabled；**新增 `import { Spinner } from './utils.jsx'` additive 依赖边**）
- **`type`（默认 `'button'` 安全 — 防全站 Btn 默认 submit 潜伏 bug；login 传 `'submit'`）**

**login 落地**：保 `<form onSubmit={submit}>` + `<Btn T={T} variant="primary" size="lg" type="submit" loading={loading} iconRight={<I.arrow/>}>进入 KNOT</Btn>`，**Btn 不 wire onClick**（form onSubmit 驱动）→ **Enter 键 + 点击都走 form，提交 0 篡改**。TOTP step 同（submitTotp form + 同款 Btn）。现有 5-variant/5-tone 映射 byte-equal（grep 既有 variant 字面 0 改）。

### §9.3 ⭐ artboard→产品 token 映射表（守护者硬交付，照翻禁揣测）
| artboard token | 产品落地 |
|---|---|
| `brandSoft`（radial-gradient 起色）| `color-mix(in oklch, ${T.accent} 8%, transparent)`（R-372 inset 字面）|
| `bgInset`（gradient 基底 + field bg）| `T.chipBg`（= ink800/ink50 = bgInset 等价）|
| `bgElev`（card bg）| `T.content` |
| `border`（card + field）| `T.border` |
| card `boxShadow` light/dark | rgba theme-conditional（**R-313 豁免**，per artboard L57，非 hex 违规）|
| `borderStrong`（artboard 自定义 checkbox）| **N/A — 不采纳 artboard 自定义 checkbox**；保现有原生 `<input type=checkbox accentColor={T.accent}>`（OOS 内）；borderStrong ∉ 产品 T/TOKENS_V2，**别引** |

### §9.4 error 色 + race guard（资深 2026-06-17 拍板）
- **(a) ✅ error 迁 TOKENS_V2.err**：两处 banner（L164/L229，各 fg + bg + border 3 行）`oklch(62% 0.22 27)` → `TOKENS_V2.err`（fg = TOKENS_V2.err；bg/border = `color-mix(in oklch, ${TOKENS_V2.err} 12%/25%, transparent)`）。login = 首个 err 采纳点；27°→25° = **VRP 视觉演进非 byte-equal，资深 ack 记档**（同 v0.6.4.0 success/warn 松绑精神）。停用 ad-hoc 红，全站 err 单一源。
- **(b) ✅ race guard 2 行**：submit/submitTotp 首行各加 `if (loading) return;` — 闭 Enter-spam 重入真 gap（disabled Btn 挡点击但 Enter-submit 不挡）。**破 R-PB-L-2 严格 byte-equal → 显式 carve-out 记档**（byte-equal **除**此 sanctioned 2 行）。

### §9.5 D3 iconRight = node 非 string（守护者 §五 — Stage 2 又漏）
login 传 **`iconRight={<I.arrow/>}`**（node，I.arrow v0.6.4.0 新增 ✓），**非 `"arrow"` 字串**（否则渲染字符串）。两 step 的 `<I.send/>` → `<I.arrow/>` 同步换（视觉对齐 artboard）。

### §9.6 Q2/Q3 VRP 锁
- **Q2**：`step==='login'` 与 `'totp_verify'` 共享同 card 容器 + maxWidth 440（card 包在 step 三元外 或两分支 card 字面 byte-equal）→ 切 step 无横向抖/高度塌。
- **Q3**：radial-gradient 最外层基底（z 最底）；NarrativeMotif（left `position:absolute inset:0`）+ 文本 `zIndex:1`（L105/L110 已有，保）层级在渐变上。R-185 KnotLogo 在 left panel（L111，右 card 化不影响；收官验哨兵命中）。

### §9.7 红线 verdict + 登记
R-PB-L-1 保留（export+3 props+App.jsx 0 diff）/ L-2 **修订**（byte-equal 除 §9.4(b) 2 行 carve-out + 含 Enter 键提交不断）/ L-3 保留（文案不变，仅 error 容器色 §9.4(a) 变）/ L-4 **修订**（§9.3 映射 + error→TOKENS_V2.err；color-mix 铁律非违规 + boxShadow R-313）/ L-5 **保留+补强**（Btn 扩 disabled/loading/**type** + iconRight node）/ L-6 保留（其他屏/Shared/utils/Shell/App 0 改；唯 primitives.jsx Btn additive 扩）/ L-7 保留（footer v0.6.4.1 + R-PA-8 0 + R-185）。
- **登记（守护者 §九.3，本 PATCH 不强求）**：primitives.jsx 仍无 additive 守护（v0.6.4.0 systemic建议）；本刀首次扩 Btn → drift 风险登记，后续可加 primitives additive guard（类 R-PB-SH-12）。

### §9.8 commit 1 闸门（§十）
build + App.jsx 0 diff + 17 屏/Shared/utils/Shell 0 + 业务链 byte-equal + **Enter 键提交实测不断** + 文案 byte-equal + Btn 扩 additive（既有 variant 映射 0 改）+ iconRight node + §9.3 映射照翻（0 裸 hex/裸 oklch；color-mix/rgba 合规）+ error→TOKENS_V2.err + 两 step 共享 card 无抖 + motif z + KnotLogo 哨兵 + check_file_sizes(Login 295+primitives) + R-PA-8 0 + footer v0.6.4.1。**2 项资深拍板全清（error 迁 + race guard carve-out）→ 起 commit 1。**

### §9.9 commit-by-commit 守护者复核归档 + carve-out 记档
- **carve-out 记档（守护者监视 #1 — R-PB-L-2/L-4「除…外」依据）**：① **error→TOKENS_V2.err**（Login.jsx errBox；27°→25° = R-PB-L-4 VRP 视觉演进非 byte-equal，资深 2026-06-17 ack）② **race guard 2 行**（submit/submitTotp 首行 `if(loading)return`；R-PB-L-2 byte-equal 例外，资深 ack）。代码注释均带「§9.4 资深 ack」溯源。
- **commit 1 复核 `d63a8d1`**（守护者直读 worktree 亲验）= **ACK + 放行 commit 2**：范围仅 primitives+Login+check_file_sizes+plan（App/Shell/Shared/utils/17屏/backend 全 0 diff）；**Btn 严格 additive**（5-variant grep 空 byte-equal，+17 纯加 type/disabled/loading+Spinner import）；Enter 键提交不断（form onSubmit+Btn 无 onClick）；业务/文案 byte-equal；error→TOKENS_V2.err 0 残留 27°；§9.3 映射照翻（radial-gradient/card R-313/chipBg/不引 borderStrong 保原生 checkbox/iconRight node）；Q2 共享 card + motif right70% + R-185 哨兵；守护者亲跑 check_file_sizes 62 files。
- **commit 2 收官**（本 commit）= version 0.6.4.1 三同步（main.py + smoke R-72 + Login footer R-181）+ README L11/技术栈 + CLAUDE 路线图 v0.6.4.1 行 + CHANGELOG（demote 0.6.4.0→Released + 0.6.4.1 段含 Btn 扩/error 迁/race carve-out）+ §9.9 归档 + 静态重建。
- **登记（守护者监视 #4 前瞻，非阻断）**：primitives.jsx 本刀首次被扩（Btn +3 prop）仍无 foundation additive 守护（test_foundation_additive 只守 Shared+utils）→ drift 风险随屏复刻累积，建议某 PATCH 纳入 primitives 守护（类 R-PB-SH-12）。
- 待守护者 commit 2 收官复核监视 5 项 → push/PR → login 复刻完结 → 续屏（shell/home/...）。
