# v0.6.4.11 — doc-不变量 CI 守护一揽子（task #44 · PATCH chore）

> **协议**：轻量 v3（资深 ack）；守护者建议**保 Stage 3 复核测试逻辑**（CI 自身可能 false-green/red — R-181 翻车教训）。性质 = docs/CI chore + version 单一真相源小重构（0 业务逻辑）。
> **基座**：worktree `v064b-docinv` @ branch `v0.6.4.11-doc-invariant-ci`（off main `07b4d5a` = v0.6.4.10）
> **触发**：资深拍板 task #44；守护者 grounded 缺口清单作 Stage 1 目标（8 stale-doc 数据点元教训：doc-不变量无 CI 则静默 drift）
> **版本定级**：CI/docs chore + 小重构（无新业务/屏/schema）→ **PATCH v0.6.4.11**（非 MINOR）

## §0 守护者 grounded 缺口清单（已区分真缺口 vs 已守 vs 一次性）

**现有守护（勿重造 — 奥卡姆）**：test_rename_smoke（R-72 main.py）/ test_login_version_sync（R-181 Login footer + R-185 KnotLogo DOM）/ test_foundation_additive（snapshot 已同步）/ KNOW-1（README 顶部）。

**真缺口（本 PATCH 补）**：① **PRIMARY: Shell L46 version 0 CI**（实证 stale `v0.6.4.2`，drift **8 PATCH**）② KnotLogo 精确 5 文件集（R-199.5）无 CI ③ CHANGELOG 顶部 version 无 CI。

**一次性已修 ≠ 常驻守护**（防过度工程）：plan §2 矛盾 / R-181 误分类 / foundation 126 stale 是历史事件，已修，不建 CI。

## §1 ⭐ 核心设计抉择（守护者 §移交 flag）— 根治而非二选一

守护者问 Shell L46 CI「无条件（每 PATCH = main，但 Shell 非每 PATCH 改 → 误红）vs 条件式（git diff 含 Shell 才校验 → CI 脆弱）」。

**执行者裁定：两者皆非 → APP_VERSION 单一真相源**（守护者原 task #44 desc「让 Login/Shell 动态读 main.py 版本（单一真相源）而非硬编字面」即此）：

- 新建 `frontend/src/version.js`：`export const APP_VERSION = '0.6.4.11';`（**前端版本唯一源**）
- Shell.jsx L46 `}}>v0.6.4.2</span>` → `}}>v{APP_VERSION}</span>` + import（**drift 根治** — 不再硬编）
- Login.jsx footer `<span>v0.6.4.10 · build …</span>` → `<span>v{APP_VERSION} · build …</span>` + import（build 时间戳保留字面）
- **消解两难**：APP_VERSION 是**1 个常量、无条件 == main.py**（bridge CI），Login/Shell **读它**（无需 per-file bump，drift 不可能）。version 同步点：前端 2 字面 → 1 常量。

## §2 改动（version 单一真相源 + 3 CI 守护 + R-181 适配）

### 重构（version 单一真相源）
| 文件 | 改动 |
|---|---|
| `frontend/src/version.js` [NEW] | `export const APP_VERSION = '0.6.4.11';`（前端版本唯一源）|
| `Shell.jsx` L46 | 硬编 `v0.6.4.2` → `v{APP_VERSION}` + import（**drift 8 PATCH 根治**）|
| `Login.jsx` footer L244 | 硬编 `v0.6.4.10` → `v{APP_VERSION}` + import（build 时间戳保留）|

### 3 CI 守护（新增 `tests/test_doc_invariants.py`）
| 测试 | 守的不变量 | 防 |
|---|---|---|
| `test_app_version_synced_with_main` | `version.js` APP_VERSION == `main.py` app.version（**bridge — 单一真相**）| 前端版本 drift（PRIMARY 根因）|
| `test_knotlogo_file_set` | `grep -rl KnotLogo frontend/src` == 精确 5 文件集（Shared/Login/Shell/Enroll/ForceChangePassword）| 第 6 文件混入 / KnotLogo 蒸发 |
| `test_changelog_top_version` | CHANGELOG 首个 `## [` 条目含 main.py version | CHANGELOG 顶部 stale / 漏条目 |

### R-181 适配（test_login_version_sync）— ⚠️ 守护者必修条件 1（防弱化）
- R-181 原 grep `v{app.version}` 字面 in Login.jsx → 重构后 Login.jsx footer 渲染 `{APP_VERSION}` → **R-181 改为断言渲染引用** `v{APP_VERSION}`（JSX footer body，**非仅 `含 APP_VERSION`** —— 后者 import 行即满足、挪出 footer 也绿）。
- **组合更强**：R-181-adapted（footer 渲染 `v{APP_VERSION}`）+ bridge（version.js APP_VERSION == main）⟹ footer = main version；原 grep 不验位置，适配后**不弱反强**。
- Shell L46 同加渲染哨兵 `v{APP_VERSION}`（test_doc_invariants）。
- R-185 KnotLogo DOM 哨兵 + KNOW-1 README 不动。

## §3 版本同步新模型（本 PATCH 立约 — 替代「五处」）

| 源点（★CI） | 显示点（读源，哨兵 CI）|
|---|---|
| ★ `main.py`（R-72）· ★ `version.js` APP_VERSION（bridge）· ★ `README` 顶部（KNOW-1）· ★ `test_rename_smoke` | Login footer + Shell L46（读 `{APP_VERSION}`，sentinel 不含硬编）|

→ 未来 bump：main.py + smoke + version.js + README（4 源点，全 ★CI）；Login/Shell **自动跟随**（drift 不可能）。**Shell L43 条件式同步规则废除**（CLAUDE.md §五处同步 → 更新）。

## §4 红线

| # | 红线 |
|---|---|
| R-DI-1 | version.js APP_VERSION == main.py（bridge CI 真能抓 drift — 改一不改二即红）|
| R-DI-2 | Shell.jsx L46 + Login.jsx footer 读 `{APP_VERSION}`（无硬编 version 字面）；Shell drift 根治 |
| R-DI-3 | R-181 适配断言**渲染引用** `v{APP_VERSION}`（JSX footer，非仅 import）+ bridge → 组合不弱反强（守护者必修 1）|
| R-DI-11 | 静态重建（commit 2 npm build，changeset 含 knot/static；version bake 进 bundle）（守护者必修 2）|
| R-DI-12 | R-DI-9 顺带 `grep -rn "Shell L43\|五处版本同步"` 全 repo doc 无旧模型残留（防修一处漏一处 — 元教训自指）|
| R-DI-4 | KnotLogo 5 文件集 CI 真能抓增/减（mutation 自验：假设第 6 文件 → 红）|
| R-DI-5 | CHANGELOG top CI 真能抓 stale（假设 top ≠ main → 红）|
| R-DI-6 | 3 新 CI **不误杀**（绿态在干净 repo 验证；R-181 翻车教训 — CI false-red 防范）|
| R-DI-7 | 0 业务逻辑改；Shell R-192 13 props + Login props byte-equal → App.jsx 0 diff（仅 version 显示行 + import 改）|
| R-DI-8 | check_file_sizes（version.js 新增 LIMIT）+ R-PA-8 0 |
| R-DI-9 | CLAUDE.md §五处同步段更新为新模型（APP_VERSION 单一真相源 + Shell 条件式废除）|
| R-DI-10 | 4 源点版本同步 → 0.6.4.11（main + smoke + README★ + version.js★）；Login/Shell 读源不再单列 |

## §5 验收
1. version.js APP_VERSION = 0.6.4.11；Shell L46 + Login footer 渲染 v0.6.4.11（drift 修）
2. 3 新 CI 测试 PASS（干净 repo）+ mutation 自验（改 version.js ≠ main → bridge 红；加第 6 KnotLogo 文件 → red；CHANGELOG top 改 → red）
3. R-181 适配后 PASS（不弱化）；test_foundation_additive / R-72 / KNOW-1 sustained
4. App.jsx 0 diff（Shell/Login props byte-equal，仅 version 显示行）+ R-PA-8 0 + check_file_sizes OK
5. 本机 python 坏 → 3 新 CI + R-181 走 CI 验证

## §6 commit 序列
- **commit 1**：version.js + Shell/Login 读 APP_VERSION + 3 CI 测试（test_doc_invariants）+ R-181 渲染断言适配 + CLAUDE.md §同步模型更新 + 4 源点版本同步 + **CHANGELOG**（**本 PATCH 引入 test_changelog_top_version → CHANGELOG 须移入 commit 1 使其自洽绿**，偏离「CHANGELOG 在收官 commit」惯例，有据）+ 本 plan（源码 + docs，静态 revert）
- **commit 2（收官）**：⚠️ **守护者必修条件 2 — npm build 静态重建**（Login/Shell `{APP_VERSION}` Vite build-time bake 进 bundle；不重建则 :8000 serve stale bundle，源码修了部署没修）；changeset 仅 `knot/static/` 资产 hash 变更

## §7 协议合规
轻量 v3 + **保 Stage 3 复核测试逻辑**（守护者建议 — CI 自身 false-green/red 风险，R-181 翻车教训）：执行者 Stage 1（本草案，核心抉择 = APP_VERSION 单一真相源根治）→ 守护者 Stage 3（复核 3 CI 断言真能抓 drift + 不误杀 + R-181 适配不弱化 + 单一真相源设计）→ 逐 commit 直读 → 资深 push/merge。task #44 清偿 8 stale-doc 元教训。
