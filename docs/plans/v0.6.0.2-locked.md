# v0.6.0.2 micro PATCH — ResultBlock 6 子组件拆分（v0.5.14 R-341 承诺偿还）

> **协议依据**：Loop Protocol v3 + v0.5.22 自审简化协议 sustained
> **触发**：Phase B 评估预 LOCKED 决议 B 修订版（2026-05-14 双守护者共识 + 资深架构师拍板）— v0.5 守护者 §2 强制前置
> **前置原因**：v0.5.14 R-341 锁定 "ResultBlock 460 final ack；v0.6 必须开启子组件拆分 6 候选"。Phase B v0.6.1~v0.7.2 多个 PATCH 需要给 ResultBlock 加 props（onFeedback / dryRunMode / validatorStep）— 不拆分则不可控
> **执行者**：v0.6 执行者
> **守护者**：v0.5 守护者（PR #74 §9 G-4/5/6 + Phase B 预评估 §6 修订建议 5）

---

## §0 背景

v0.5.14 R-341 LOCKED 立约（commit message 字面）：

> v0.5 ResultBlock 行数收官（440 final；v0.6 必须开启子组件拆分 6 候选：MetricCard / TableContainer / InsightCard / BudgetBanner / ErrorBanner / TokenMeter）

当前 ResultBlock.jsx **449 行**（v0.5.39 R-111 LIMIT 460 内）。Phase B v0.6.1+ 多个 PATCH 计划给 ResultBlock 加 props：
- v0.6.2 长期记忆：`onFeedback` prop（用户反馈按钮）
- v0.6.3 预演模式：`dryRunMode` prop（SQL 预览确认）
- v0.7.2 校验段：`validatorStep` prop（数据合理性反检）

不拆分继续加 props → v0.5.14 R-341 + R-318 25 字段 props 契约破裂 + R-548 核爆级 props 守护失效 + 行数突破 460 LIMIT。

## §1 In-Scope（6 子组件拆分）

### F1 — MetricCard 子组件（已在 ResultBlock 内 line 404）

```
frontend/src/screens/chat/ResultBlock/MetricCard.jsx [NEW]
```
- 从 ResultBlock.jsx line 404-428 抽出
- props 签名 byte-equal（T / rows / cols / numericCols）
- 不动业务逻辑

### F2 — TableContainer 子组件

```
frontend/src/screens/chat/ResultBlock/TableContainer.jsx [NEW]
```
- 从 ResultBlock.jsx detail/rank/distribution intent layout 分支抽出表格容器
- props: T / rows / cols / intent
- v0.5.14 R-327 thead 删 uppercase + letter-spacing normal sustained
- v0.5.13 R-302.5 banner emoji 业务豁免 sustained

### F3 — InsightCard 子组件（Observation / brand inset）

```
frontend/src/screens/chat/ResultBlock/InsightCard.jsx [NEW]
```
- 从 ResultBlock.jsx Insight 渲染抽出
- v0.5.14 R-323 brandSoft 8% inset 字面 byte-equal
- v0.5.14 R-227.5.1 OBSERVATION 装饰豁免延伸 sustained
- props: T / insight / confidence

### F4 — BudgetBanner 子组件

```
frontend/src/screens/chat/ResultBlock/BudgetBanner.jsx [NEW]
```
- 从 ResultBlock.jsx line 165 抽出
- v0.4.3 R-16~23 budget 状态映射逻辑 byte-equal
- v0.5.13 R-304 SvgPath shield/triangle svg 替代 🛑⚠️ sustained
- props: T / budget_status / budget_meta / onDismiss

### F5 — ErrorBanner 子组件（含 ERROR_KIND_META）

```
frontend/src/screens/chat/ResultBlock/ErrorBanner.jsx [NEW]
```
- 从 ResultBlock.jsx line 211 抽出 + getErrorKindMeta helper
- v0.4.4 R-28 banner 优先级（ErrorBanner > BudgetBanner）sustained
- v0.5.13 R-302.5 emoji 业务豁免 7 类 kind sustained
- props: T / error / error_kind / user_message / is_retryable

### F6 — TokenMeter 子组件（inline stat + svg ↑↓）

```
frontend/src/screens/chat/ResultBlock/TokenMeter.jsx [NEW]
```
- 从 ResultBlock.jsx Token meter 渲染抽出
- v0.5.14 R-325 inline stat + svg ↑↓ 字面 byte-equal
- v0.5.13 R-306/315 已撤回（v0.5.14 红线撤回首例）sustained — 不复用 TokenPill helper
- props: T / input_tokens / output_tokens / cost_usd / confidence / recovery_attempt

### F7 — ResultBlock.jsx 主文件瘦身

- 449 行 → ≤ 250 行（仅 layout 调度 + intent 分支 + 6 子组件 import + 业务 helpers）
- 保留：`AGENT_KIND_EMOJI` / `ERROR_KIND_ICONS` / `ERROR_KIND_TITLES` / `RB_SVG` / `resolveEffectiveHint` 引用
- 保留：`exportMessageCsv` async helper（业务函数 — 不拆 component）

## §2 Out-of-Scope（明确推迟）

- ✘ ResultBlock.jsx 主文件 props 签名修改（R-318 25 字段契约 sustained）
- ✘ Phase B 新功能（onFeedback / dryRunMode / validatorStep）— v0.6.1+ 各 PATCH 落地
- ✘ ResultBlock.jsx 业务逻辑改动（resolveEffectiveHint / exportMessageCsv / 7 intent layout 分支）
- ✘ 任何视觉规范改动（v0.5.6~v0.5.21 视觉契约全 sustained）

## §3 红线 R-PA-PB-V0（v0.6.0.2 拆分守护）

### R-PA-PB-V0.1 — props 签名 byte-equal
- ResultBlock 主文件 7 props（msg / onAgentEvent / onClarification / onError / onFinal / onException / onFollowup）byte-equal
- Conversation.jsx 调用点 0 改动（git diff = 0 行）

### R-PA-PB-V0.2 — 视觉规范 byte-equal
- v0.5.14 R-323 brandSoft 8% inset / R-325 inline stat / R-327 thead 删 uppercase sustained
- v0.5.13 R-302.5 emoji 业务豁免（7 类 ErrorBanner kind icons 保留）
- v0.5.6 buildTheme 25 字段不扩

### R-PA-PB-V0.3 — 业务逻辑 byte-equal
- resolveEffectiveHint / exportMessageCsv / getErrorKindMeta 业务函数 byte-equal
- 7 intent layout 分支（metric_card / line / bar / rank_view / pie / retention_matrix / detail_table）字面 byte-equal
- AGENT_KIND_EMOJI / ERROR_KIND_ICONS / ERROR_KIND_TITLES / RB_SVG 字典内容 byte-equal

### R-PA-PB-V0.4 — 测试 0 regression
- 现有 R-292~R-322（PR #74 commit 8 sql_planner_guard 守护）sustained
- 闸门 9 / 闸门 10 端到端流程 byte-equal
- pytest 421 → 421 sustained（拆分不改测试覆盖）

### R-PA-PB-V0.5 — 行数压制
- ResultBlock.jsx 主文件 ≤ 250 行（450 → 250 净 -200）
- 6 子组件每个 ≤ 120 行（除 ErrorBanner 含 7 kind 元数据可 ≤ 150）
- scripts/check_file_sizes.py LIMITS dict +6 新文件

### R-PA-PB-V0.6 — Preview MCP 浏览器 visual 验证
- Login / Chat 空 / Conversation 详情 3 屏（含 ResultBlock）双主题 visual 0 漂移
- 7 intent 端到端验证（mock 数据触发 each intent）
- hex 残留 0 命中扫描（Preview MCP eval）

## §4 验收闸门

| # | 闸门 | 期望 |
|---|---|---|
| 1 | pytest 全套 | 421 → 421 sustained |
| 2 | import-linter 7 contracts | KEPT × 7 |
| 3 | ruff check knot/ + frontend | clean |
| 4 | npm run build | 0 errors |
| 5 | check_file_sizes | 主 ≤ 250 + 6 子 ≤ 120/150 |
| 6 | audit_ohx_leakage + R-PA-8 check_phase_b_leakage | exit 0 |
| 7 | tests/scripts/test_dockerfile_copy.py 3 测试 | PASS |
| 8 | Conversation.jsx 调用点 git diff | 0 行 |
| 9 | 闸门 9 端到端 SavedReports 收藏 + CSV 导出 + 重跑 | PASS |
| 10 | Preview MCP 视觉验证 3 屏 × 2 主题 | 0 漂移 |
| 11 | 7 intent layout byte-equal（手动触发各 intent）| 全对齐 |
| 12 | GitHub Actions CI 全绿 | 含 docker-build job |

## §5 Commit 序列

```
[commit 0]  docs(v0.6.0.2): LOCKED 手册归档（本手册）
   │
commit 1   refactor(v0.6.0.2): F1 MetricCard 抽出（独立子文件）
   │       · ResultBlock/MetricCard.jsx [NEW 25 行]
   │       · ResultBlock.jsx 删 line 404-428（-25 行）
   │
commit 2   refactor(v0.6.0.2): F2-F3 TableContainer + InsightCard 抽出
   │       · ResultBlock/TableContainer.jsx [NEW ~80 行]
   │       · ResultBlock/InsightCard.jsx [NEW ~50 行]
   │       · ResultBlock.jsx -130 行
   │
commit 3   refactor(v0.6.0.2): F4-F5 BudgetBanner + ErrorBanner 抽出
   │       · ResultBlock/BudgetBanner.jsx [NEW ~60 行]
   │       · ResultBlock/ErrorBanner.jsx [NEW ~120 行 含 ERROR_KIND_META 7 kind]
   │       · ResultBlock.jsx -180 行
   │
commit 4   refactor(v0.6.0.2): F6 TokenMeter 抽出
   │       · ResultBlock/TokenMeter.jsx [NEW ~70 行]
   │       · ResultBlock.jsx -70 行
   │
commit 5   ci+test(v0.6.0.2): check_file_sizes LIMITS +6 + Preview MCP visual 验证
   │       · scripts/check_file_sizes.py LIMITS +6 新文件
   │       · Preview MCP 3 屏 × 2 主题 0 漂移验证（执行者本地跑）
   │
commit 6   chore(v0.6.0.2): 版本宣告 0.6.0.1 → 0.6.0.2
           · knot/main.py / tests/test_rename_smoke.py / Login.jsx 页脚
           · CHANGELOG v0.6.0.2 段
           · 三处版本同步守护（R-PA-17 sustained）
```

## §6 协议合规性

### 6.1 简化协议适用性

- v0.5.22 自审简化协议 sustained — v0.6.0.2 是**纯偿还性 micro PATCH**：
  1. R-341 是 v0.5.14 守护者 LOCKED 立约 → Stage 3 守护者已 explicit
  2. Phase B 预评估 §6.1 修订建议 5 守护者强制建议 → Stage 3 二次确认
  3. 6 子组件拆分内容客观（无歧义）
- Stage 2/3 跳过 / Stage 4 直接落地

### 6.2 不适用全 v3 三阶段评审的理由

- 纯偿还性（无新功能 / 无新红线引入除 R-PA-PB-V0 守护拆分本身）
- 视觉规范全 sustained（v0.5.6~v0.5.21 18 项契约 0 修改）
- 业务逻辑 byte-equal（R-PA-PB-V0.3 强制守护）

### 6.3 守护者轻量复核（推荐）

PR open 后建议召集 v0.5 守护者轻量复核（5-10 分钟）：
- 验证 R-PA-PB-V0.1~V0.6 字面 byte-equal
- 验证 Preview MCP 3 屏 × 2 主题 0 漂移截图
- 验证 R-341 偿还完整性

## §7 自检清单

合入前执行者自检：

- [ ] §1 F1-F7 全部 commit 已按 §5 顺序落地
- [ ] §3 红线 R-PA-PB-V0.1~V0.6 全过
- [ ] §4 验收闸门 1-12 全过
- [ ] CI GitHub Actions 全绿（含新 docker-build job）
- [ ] Preview MCP 浏览器 visual 3 屏 × 2 主题 0 漂移
- [ ] Conversation.jsx 调用点 git diff = 0 行
- [ ] 7 intent layout 手动触发验证（mock + curl）
- [ ] phase-b-proposal-draft.md / v0.6.0.2-locked.md untracked 状态 sustained（前者）

## §8 状态

| 字段 | 值 |
|---|---|
| 起草日期 | 2026-05-14 |
| 起草人 | v0.6 执行者 |
| 协议 | Loop Protocol v3 + v0.5.22 自审简化协议 sustained |
| 触发 | Phase B 预评估 §6.1 决议 B 修订版 / v0.5 守护者 §2 强制前置 |
| 归档位置 | `docs/plans/v0.6.0.2-locked.md` |
| 守护者参与 | v0.5 守护者 PR #74 §9 + Phase B 预评估 §6 修订建议 5 已 explicit |
| 完成判定 | §4 闸门 1-12 全过 + Preview MCP visual 0 漂移 + 资深架构师拍板 merge |

## §9 v0.6.0.2 与 Phase B v0.6.1+ 的关系

完成 v0.6.0.2 后，ResultBlock 拆分基础设施就绪。v0.6.1+ 后续 PATCH 加 props 路径：

| Phase B PATCH | ResultBlock 涉及子组件 | 新 prop |
|---|---|---|
| v0.6.2 长期记忆 | ResultBlock 主文件 + 新增 FeedbackButton（独立） | `onFeedback` |
| v0.6.3 预演模式 | ResultBlock 主文件 + TableContainer | `dryRunMode` |
| v0.7.2 校验段 | InsightCard + ErrorBanner 协同 | `validatorStep` |

子组件独立后 props 守护范围更小（仅子组件接口）→ R-548 核爆级 props 守护可控范围 ↑。

---

## 附录 A — v0.5.14 R-341 立约文本（引用追溯）

```
v0.5 ResultBlock 行数收官（440 final；v0.6 必须开启子组件拆分 6 候选：
MetricCard / TableContainer / InsightCard / BudgetBanner / ErrorBanner / TokenMeter）
```

来源：commit 8e9d2... (v0.5.14 ResultBlock 视觉大重构) commit message + LOCKED §3
